<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.algorithm.structured.strategy.solve

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.algorithm.structured.strategy.solve.ConstantStrategy.eliminate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/ConstantStrategy.html#eliminate-ffffff28)

```scala
override def eliminate(toEliminate: Set[Variable[_]], toPreserve: Set[Variable[_]], factors: List[Factor[Double]]): (List[Factor[Double]], Map[Variable[_], Factor[_]])
```

Solve the problem by eliminating variables, leaving only the ones that belong in the solution.

Type parameters: none.

Parameters, list 1: `` toEliminate: Set[Variable[_]] ``; `` toPreserve: Set[Variable[_]] ``; `` factors: List[Factor[Double]] ``.

Returns: `` (List[Factor[Double]], Map[Variable[_], Factor[_]]) ``.

Source contract/attributes: Solve the problem by eliminating variables, leaving only the ones that belong in the solution. Value parameters factors Factors over which to perform elimination. toEliminate Variables to eliminate. toPreserve Variables to preserve. Attributes Returns A list of factors over the variables to preserve representing their joint distribution, and a map of recording factors for MPE. Definition Classes SolvingStrategy

Invocation template:

```scala
receiver.eliminate(toEliminate, toPreserve, factors)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.ConstantStrategy.recurse ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/ConstantStrategy.html#recurse-fffff41a)

```scala
override def recurse(subproblem: NestedProblem[_]): RaisingStrategy
```

Returns a strategy that could be used to solve the nested problem.

Type parameters: none.

Parameters, list 1: `` subproblem: NestedProblem[_] ``.

Returns: `` RaisingStrategy ``.

Source contract/attributes: Returns a strategy that could be used to solve the nested problem. Value parameters subproblem Unsolved nested problem to recurse on. Attributes Returns A strategy to solve the nested problem. Definition Classes RaisingStrategy

Invocation template:

```scala
receiver.recurse(subproblem)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.ConstantStrategy.chainNonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/ConstantStrategy.html#chainNonConstraintFactors-fffffa11)

```scala
def chainNonConstraintFactors[ParentValue, Value](chainComp: ChainComponent[ParentValue, Value]): List[Factor[Double]]
```

Get non-constraint factors associated with a single Chain component.

Type parameters: `` ParentValue ``; `` Value ``.

Parameters, list 1: `` chainComp: ChainComponent[ParentValue, Value] ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get non-constraint factors associated with a single Chain component. Value parameters chainComp Chain component to process. Attributes Returns All factors associated with the chain component that are needed for solving. This includes (possibly eliminated) subproblem factors. Inherited from: RaisingStrategy

Invocation template:

```scala
receiver.chainNonConstraintFactors[ParentValue, Value](chainComp)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.ConstantStrategy.constraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/ConstantStrategy.html#constraintFactors-fffff66a)

```scala
def constraintFactors(bounds: Bounds): List[Factor[Double]]
```

Get all of the constraint factors needed for solving.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get all of the constraint factors needed for solving. Value parameters bounds Bounds for the returned constraint factors. Attributes Returns Constraint factors for solving. Inherited from: SolvingStrategy

Invocation template:

```scala
receiver.constraintFactors(bounds)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.ConstantStrategy.execute ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/ConstantStrategy.html#execute-fffff034)

```scala
def execute(bounds: Bounds = ...): Unit
```

Solve the problem defined by all the components' current factors. This involves solving and incorporating subproblems as well. This will set the globals accordingly. All components in this problem and contained subproblems should be eliminated in the solution.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Solve the problem defined by all the components' current factors. This involves solving and incorporating subproblems as well. This will set the globals accordingly. All components in this problem and contained subproblems should be eliminated in the solution. Value parameters bounds Bounds for constraint factors. Defaults to Lower. This default is intended for the cases where it does not matter which bounds should be used because both upper and lower bounds would be the same. Attributes Inherited from: SolvingStrategy

Invocation template:

```scala
receiver.execute(bounds)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.ConstantStrategy.nonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/ConstantStrategy.html#nonConstraintFactors-fffffc52)

```scala
override def nonConstraintFactors(): List[Factor[Double]]
```

Get all of the non-constraint factors needed for solving. This includes subproblem factors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get all of the non-constraint factors needed for solving. This includes subproblem factors. Attributes Returns Non-constraint factors for solving. Definition Classes RaisingStrategy -> SolvingStrategy Inherited from: RaisingStrategy

Invocation template:

```scala
receiver.nonConstraintFactors()
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.ConstantStrategy.subproblemNonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/ConstantStrategy.html#subproblemNonConstraintFactors-189)

```scala
def subproblemNonConstraintFactors[ParentValue, Value](chainComp: ChainComponent[ParentValue, Value]): Map[ParentValue, List[Factor[Double]]]
```

Get the non-constraint factors associated with all subproblems of a Chain component. This returns the existing solution if there is one. Otherwise, it chooses to solve or raise the subproblem based on the raising criteria.

Type parameters: `` ParentValue ``; `` Value ``.

Parameters, list 1: `` chainComp: ChainComponent[ParentValue, Value] ``.

Returns: `` Map[ParentValue, List[Factor[Double]]] ``.

Source contract/attributes: Get the non-constraint factors associated with all subproblems of a Chain component. This returns the existing solution if there is one. Otherwise, it chooses to solve or raise the subproblem based on the raising criteria. Value parameters chainComp Chain component whose subproblems are to be processed. Attributes Returns All factors associated with subproblems that are needed for solving, grouped by parent value. Inherited from: RaisingStrategy

Invocation template:

```scala
receiver.subproblemNonConstraintFactors[ParentValue, Value](chainComp)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.RaisingStrategy.recurse ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/RaisingStrategy.html#recurse-fffff41a)

```scala
def recurse(subproblem: NestedProblem[_]): RaisingStrategy
```

Returns a strategy that could be used to solve the nested problem.

Type parameters: none.

Parameters, list 1: `` subproblem: NestedProblem[_] ``.

Returns: `` RaisingStrategy ``.

Source contract/attributes: Returns a strategy that could be used to solve the nested problem. Value parameters subproblem Unsolved nested problem to recurse on. Attributes Returns A strategy to solve the nested problem.

Invocation template:

```scala
receiver.recurse(subproblem)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.RaisingStrategy.chainNonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/RaisingStrategy.html#chainNonConstraintFactors-fffffa11)

```scala
def chainNonConstraintFactors[ParentValue, Value](chainComp: ChainComponent[ParentValue, Value]): List[Factor[Double]]
```

Get non-constraint factors associated with a single Chain component.

Type parameters: `` ParentValue ``; `` Value ``.

Parameters, list 1: `` chainComp: ChainComponent[ParentValue, Value] ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get non-constraint factors associated with a single Chain component. Value parameters chainComp Chain component to process. Attributes Returns All factors associated with the chain component that are needed for solving. This includes (possibly eliminated) subproblem factors.

Invocation template:

```scala
receiver.chainNonConstraintFactors[ParentValue, Value](chainComp)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.RaisingStrategy.nonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/RaisingStrategy.html#nonConstraintFactors-fffffc52)

```scala
override def nonConstraintFactors(): List[Factor[Double]]
```

Get all of the non-constraint factors needed for solving. This includes subproblem factors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get all of the non-constraint factors needed for solving. This includes subproblem factors. Attributes Returns Non-constraint factors for solving. Definition Classes SolvingStrategy

Invocation template:

```scala
receiver.nonConstraintFactors()
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.RaisingStrategy.subproblemNonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/RaisingStrategy.html#subproblemNonConstraintFactors-189)

```scala
def subproblemNonConstraintFactors[ParentValue, Value](chainComp: ChainComponent[ParentValue, Value]): Map[ParentValue, List[Factor[Double]]]
```

Get the non-constraint factors associated with all subproblems of a Chain component. This returns the existing solution if there is one. Otherwise, it chooses to solve or raise the subproblem based on the raising criteria.

Type parameters: `` ParentValue ``; `` Value ``.

Parameters, list 1: `` chainComp: ChainComponent[ParentValue, Value] ``.

Returns: `` Map[ParentValue, List[Factor[Double]]] ``.

Source contract/attributes: Get the non-constraint factors associated with all subproblems of a Chain component. This returns the existing solution if there is one. Otherwise, it chooses to solve or raise the subproblem based on the raising criteria. Value parameters chainComp Chain component whose subproblems are to be processed. Attributes Returns All factors associated with subproblems that are needed for solving, grouped by parent value.

Invocation template:

```scala
receiver.subproblemNonConstraintFactors[ParentValue, Value](chainComp)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.RaisingStrategy.constraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/RaisingStrategy.html#constraintFactors-fffff66a)

```scala
def constraintFactors(bounds: Bounds): List[Factor[Double]]
```

Get all of the constraint factors needed for solving.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get all of the constraint factors needed for solving. Value parameters bounds Bounds for the returned constraint factors. Attributes Returns Constraint factors for solving. Inherited from: SolvingStrategy

Invocation template:

```scala
receiver.constraintFactors(bounds)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.RaisingStrategy.execute ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/RaisingStrategy.html#execute-fffff034)

```scala
def execute(bounds: Bounds = ...): Unit
```

Solve the problem defined by all the components' current factors. This involves solving and incorporating subproblems as well. This will set the globals accordingly. All components in this problem and contained subproblems should be eliminated in the solution.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Solve the problem defined by all the components' current factors. This involves solving and incorporating subproblems as well. This will set the globals accordingly. All components in this problem and contained subproblems should be eliminated in the solution. Value parameters bounds Bounds for constraint factors. Defaults to Lower. This default is intended for the cases where it does not matter which bounds should be used because both upper and lower bounds would be the same. Attributes Inherited from: SolvingStrategy

Invocation template:

```scala
receiver.execute(bounds)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.RaisingStrategy.eliminate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/RaisingStrategy.html#eliminate-ffffff28)

```scala
def eliminate(toEliminate: Set[Variable[_]], toPreserve: Set[Variable[_]], factors: List[Factor[Double]]): (List[Factor[Double]], Map[Variable[_], Factor[_]])
```

Solve the problem by eliminating variables, leaving only the ones that belong in the solution.

Type parameters: none.

Parameters, list 1: `` toEliminate: Set[Variable[_]] ``; `` toPreserve: Set[Variable[_]] ``; `` factors: List[Factor[Double]] ``.

Returns: `` (List[Factor[Double]], Map[Variable[_], Factor[_]]) ``.

Source contract/attributes: Solve the problem by eliminating variables, leaving only the ones that belong in the solution. Value parameters factors Factors over which to perform elimination. toEliminate Variables to eliminate. toPreserve Variables to preserve. Attributes Returns A list of factors over the variables to preserve representing their joint distribution, and a map of recording factors for MPE. Inherited from: SolvingStrategy

Invocation template:

```scala
receiver.eliminate(toEliminate, toPreserve, factors)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.SolvingStrategy.eliminate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/SolvingStrategy.html#eliminate-ffffff28)

```scala
def eliminate(toEliminate: Set[Variable[_]], toPreserve: Set[Variable[_]], factors: List[Factor[Double]]): (List[Factor[Double]], Map[Variable[_], Factor[_]])
```

Solve the problem by eliminating variables, leaving only the ones that belong in the solution.

Type parameters: none.

Parameters, list 1: `` toEliminate: Set[Variable[_]] ``; `` toPreserve: Set[Variable[_]] ``; `` factors: List[Factor[Double]] ``.

Returns: `` (List[Factor[Double]], Map[Variable[_], Factor[_]]) ``.

Source contract/attributes: Solve the problem by eliminating variables, leaving only the ones that belong in the solution. Value parameters factors Factors over which to perform elimination. toEliminate Variables to eliminate. toPreserve Variables to preserve. Attributes Returns A list of factors over the variables to preserve representing their joint distribution, and a map of recording factors for MPE.

Invocation template:

```scala
receiver.eliminate(toEliminate, toPreserve, factors)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.SolvingStrategy.constraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/SolvingStrategy.html#constraintFactors-fffff66a)

```scala
def constraintFactors(bounds: Bounds): List[Factor[Double]]
```

Get all of the constraint factors needed for solving.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get all of the constraint factors needed for solving. Value parameters bounds Bounds for the returned constraint factors. Attributes Returns Constraint factors for solving.

Invocation template:

```scala
receiver.constraintFactors(bounds)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.SolvingStrategy.execute ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/SolvingStrategy.html#execute-fffff034)

```scala
def execute(bounds: Bounds = ...): Unit
```

Solve the problem defined by all the components' current factors. This involves solving and incorporating subproblems as well. This will set the globals accordingly. All components in this problem and contained subproblems should be eliminated in the solution.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Solve the problem defined by all the components' current factors. This involves solving and incorporating subproblems as well. This will set the globals accordingly. All components in this problem and contained subproblems should be eliminated in the solution. Value parameters bounds Bounds for constraint factors. Defaults to Lower. This default is intended for the cases where it does not matter which bounds should be used because both upper and lower bounds would be the same. Attributes

Invocation template:

```scala
receiver.execute(bounds)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.SolvingStrategy.nonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/SolvingStrategy.html#nonConstraintFactors-fffffc52)

```scala
def nonConstraintFactors(): List[Factor[Double]]
```

Get all of the non-constraint factors needed for solving.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get all of the non-constraint factors needed for solving. Attributes Returns Non-constraint factors for solving.

Invocation template:

```scala
receiver.nonConstraintFactors()
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEBPGibbsStrategy.eliminate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEBPGibbsStrategy.html#eliminate-ffffff28)

```scala
override def eliminate(toEliminate: Set[Variable[_]], toPreserve: Set[Variable[_]], factors: List[Factor[Double]]): (List[Factor[Double]], Map[Variable[_], Factor[_]])
```

Solve the problem by eliminating variables, leaving only the ones that belong in the solution.

Type parameters: none.

Parameters, list 1: `` toEliminate: Set[Variable[_]] ``; `` toPreserve: Set[Variable[_]] ``; `` factors: List[Factor[Double]] ``.

Returns: `` (List[Factor[Double]], Map[Variable[_], Factor[_]]) ``.

Source contract/attributes: Solve the problem by eliminating variables, leaving only the ones that belong in the solution. Value parameters factors Factors over which to perform elimination. toEliminate Variables to eliminate. toPreserve Variables to preserve. Attributes Returns A list of factors over the variables to preserve representing their joint distribution, and a map of recording factors for MPE. Definition Classes SolvingStrategy

Invocation template:

```scala
receiver.eliminate(toEliminate, toPreserve, factors)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEBPGibbsStrategy.hasDeterminism ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEBPGibbsStrategy.html#hasDeterminism-ffffffb8)

```scala
def hasDeterminism(problem: Problem, v: Variable[_]): Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` problem: Problem ``; `` v: Variable[_] ``.

Returns: `` Boolean ``.

Invocation template:

```scala
receiver.hasDeterminism(problem, v)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEBPGibbsStrategy.recurse ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEBPGibbsStrategy.html#recurse-fffff41a)

```scala
override def recurse(subproblem: NestedProblem[_]): RaisingStrategy
```

Returns a strategy that could be used to solve the nested problem.

Type parameters: none.

Parameters, list 1: `` subproblem: NestedProblem[_] ``.

Returns: `` RaisingStrategy ``.

Source contract/attributes: Returns a strategy that could be used to solve the nested problem. Value parameters subproblem Unsolved nested problem to recurse on. Attributes Returns A strategy to solve the nested problem. Definition Classes RaisingStrategy

Invocation template:

```scala
receiver.recurse(subproblem)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEBPGibbsStrategy.chainNonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEBPGibbsStrategy.html#chainNonConstraintFactors-fffffa11)

```scala
def chainNonConstraintFactors[ParentValue, Value](chainComp: ChainComponent[ParentValue, Value]): List[Factor[Double]]
```

Get non-constraint factors associated with a single Chain component.

Type parameters: `` ParentValue ``; `` Value ``.

Parameters, list 1: `` chainComp: ChainComponent[ParentValue, Value] ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get non-constraint factors associated with a single Chain component. Value parameters chainComp Chain component to process. Attributes Returns All factors associated with the chain component that are needed for solving. This includes (possibly eliminated) subproblem factors. Inherited from: RaisingStrategy

Invocation template:

```scala
receiver.chainNonConstraintFactors[ParentValue, Value](chainComp)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEBPGibbsStrategy.constraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEBPGibbsStrategy.html#constraintFactors-fffff66a)

```scala
def constraintFactors(bounds: Bounds): List[Factor[Double]]
```

Get all of the constraint factors needed for solving.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get all of the constraint factors needed for solving. Value parameters bounds Bounds for the returned constraint factors. Attributes Returns Constraint factors for solving. Inherited from: SolvingStrategy

Invocation template:

```scala
receiver.constraintFactors(bounds)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEBPGibbsStrategy.execute ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEBPGibbsStrategy.html#execute-fffff034)

```scala
def execute(bounds: Bounds = ...): Unit
```

Solve the problem defined by all the components' current factors. This involves solving and incorporating subproblems as well. This will set the globals accordingly. All components in this problem and contained subproblems should be eliminated in the solution.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Solve the problem defined by all the components' current factors. This involves solving and incorporating subproblems as well. This will set the globals accordingly. All components in this problem and contained subproblems should be eliminated in the solution. Value parameters bounds Bounds for constraint factors. Defaults to Lower. This default is intended for the cases where it does not matter which bounds should be used because both upper and lower bounds would be the same. Attributes Inherited from: SolvingStrategy

Invocation template:

```scala
receiver.execute(bounds)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEBPGibbsStrategy.nonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEBPGibbsStrategy.html#nonConstraintFactors-fffffc52)

```scala
override def nonConstraintFactors(): List[Factor[Double]]
```

Get all of the non-constraint factors needed for solving. This includes subproblem factors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get all of the non-constraint factors needed for solving. This includes subproblem factors. Attributes Returns Non-constraint factors for solving. Definition Classes RaisingStrategy -> SolvingStrategy Inherited from: RaisingStrategy

Invocation template:

```scala
receiver.nonConstraintFactors()
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEBPGibbsStrategy.subproblemNonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEBPGibbsStrategy.html#subproblemNonConstraintFactors-189)

```scala
def subproblemNonConstraintFactors[ParentValue, Value](chainComp: ChainComponent[ParentValue, Value]): Map[ParentValue, List[Factor[Double]]]
```

Get the non-constraint factors associated with all subproblems of a Chain component. This returns the existing solution if there is one. Otherwise, it chooses to solve or raise the subproblem based on the raising criteria.

Type parameters: `` ParentValue ``; `` Value ``.

Parameters, list 1: `` chainComp: ChainComponent[ParentValue, Value] ``.

Returns: `` Map[ParentValue, List[Factor[Double]]] ``.

Source contract/attributes: Get the non-constraint factors associated with all subproblems of a Chain component. This returns the existing solution if there is one. Otherwise, it chooses to solve or raise the subproblem based on the raising criteria. Value parameters chainComp Chain component whose subproblems are to be processed. Attributes Returns All factors associated with subproblems that are needed for solving, grouped by parent value. Inherited from: RaisingStrategy

Invocation template:

```scala
receiver.subproblemNonConstraintFactors[ParentValue, Value](chainComp)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEBPStrategy.eliminate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEBPStrategy.html#eliminate-ffffff28)

```scala
override def eliminate(toEliminate: Set[Variable[_]], toPreserve: Set[Variable[_]], factors: List[Factor[Double]]): (List[Factor[Double]], Map[Variable[_], Factor[_]])
```

Solve the problem by eliminating variables, leaving only the ones that belong in the solution.

Type parameters: none.

Parameters, list 1: `` toEliminate: Set[Variable[_]] ``; `` toPreserve: Set[Variable[_]] ``; `` factors: List[Factor[Double]] ``.

Returns: `` (List[Factor[Double]], Map[Variable[_], Factor[_]]) ``.

Source contract/attributes: Solve the problem by eliminating variables, leaving only the ones that belong in the solution. Value parameters factors Factors over which to perform elimination. toEliminate Variables to eliminate. toPreserve Variables to preserve. Attributes Returns A list of factors over the variables to preserve representing their joint distribution, and a map of recording factors for MPE. Definition Classes SolvingStrategy

Invocation template:

```scala
receiver.eliminate(toEliminate, toPreserve, factors)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEBPStrategy.recurse ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEBPStrategy.html#recurse-fffff41a)

```scala
override def recurse(subproblem: NestedProblem[_]): RaisingStrategy
```

Returns a strategy that could be used to solve the nested problem.

Type parameters: none.

Parameters, list 1: `` subproblem: NestedProblem[_] ``.

Returns: `` RaisingStrategy ``.

Source contract/attributes: Returns a strategy that could be used to solve the nested problem. Value parameters subproblem Unsolved nested problem to recurse on. Attributes Returns A strategy to solve the nested problem. Definition Classes RaisingStrategy

Invocation template:

```scala
receiver.recurse(subproblem)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEBPStrategy.chainNonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEBPStrategy.html#chainNonConstraintFactors-fffffa11)

```scala
def chainNonConstraintFactors[ParentValue, Value](chainComp: ChainComponent[ParentValue, Value]): List[Factor[Double]]
```

Get non-constraint factors associated with a single Chain component.

Type parameters: `` ParentValue ``; `` Value ``.

Parameters, list 1: `` chainComp: ChainComponent[ParentValue, Value] ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get non-constraint factors associated with a single Chain component. Value parameters chainComp Chain component to process. Attributes Returns All factors associated with the chain component that are needed for solving. This includes (possibly eliminated) subproblem factors. Inherited from: RaisingStrategy

Invocation template:

```scala
receiver.chainNonConstraintFactors[ParentValue, Value](chainComp)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEBPStrategy.constraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEBPStrategy.html#constraintFactors-fffff66a)

```scala
def constraintFactors(bounds: Bounds): List[Factor[Double]]
```

Get all of the constraint factors needed for solving.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get all of the constraint factors needed for solving. Value parameters bounds Bounds for the returned constraint factors. Attributes Returns Constraint factors for solving. Inherited from: SolvingStrategy

Invocation template:

```scala
receiver.constraintFactors(bounds)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEBPStrategy.execute ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEBPStrategy.html#execute-fffff034)

```scala
def execute(bounds: Bounds = ...): Unit
```

Solve the problem defined by all the components' current factors. This involves solving and incorporating subproblems as well. This will set the globals accordingly. All components in this problem and contained subproblems should be eliminated in the solution.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Solve the problem defined by all the components' current factors. This involves solving and incorporating subproblems as well. This will set the globals accordingly. All components in this problem and contained subproblems should be eliminated in the solution. Value parameters bounds Bounds for constraint factors. Defaults to Lower. This default is intended for the cases where it does not matter which bounds should be used because both upper and lower bounds would be the same. Attributes Inherited from: SolvingStrategy

Invocation template:

```scala
receiver.execute(bounds)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEBPStrategy.nonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEBPStrategy.html#nonConstraintFactors-fffffc52)

```scala
override def nonConstraintFactors(): List[Factor[Double]]
```

Get all of the non-constraint factors needed for solving. This includes subproblem factors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get all of the non-constraint factors needed for solving. This includes subproblem factors. Attributes Returns Non-constraint factors for solving. Definition Classes RaisingStrategy -> SolvingStrategy Inherited from: RaisingStrategy

Invocation template:

```scala
receiver.nonConstraintFactors()
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEBPStrategy.subproblemNonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEBPStrategy.html#subproblemNonConstraintFactors-189)

```scala
def subproblemNonConstraintFactors[ParentValue, Value](chainComp: ChainComponent[ParentValue, Value]): Map[ParentValue, List[Factor[Double]]]
```

Get the non-constraint factors associated with all subproblems of a Chain component. This returns the existing solution if there is one. Otherwise, it chooses to solve or raise the subproblem based on the raising criteria.

Type parameters: `` ParentValue ``; `` Value ``.

Parameters, list 1: `` chainComp: ChainComponent[ParentValue, Value] ``.

Returns: `` Map[ParentValue, List[Factor[Double]]] ``.

Source contract/attributes: Get the non-constraint factors associated with all subproblems of a Chain component. This returns the existing solution if there is one. Otherwise, it chooses to solve or raise the subproblem based on the raising criteria. Value parameters chainComp Chain component whose subproblems are to be processed. Attributes Returns All factors associated with subproblems that are needed for solving, grouped by parent value. Inherited from: RaisingStrategy

Invocation template:

```scala
receiver.subproblemNonConstraintFactors[ParentValue, Value](chainComp)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEGibbsStrategy.eliminate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEGibbsStrategy.html#eliminate-ffffff28)

```scala
override def eliminate(toEliminate: Set[Variable[_]], toPreserve: Set[Variable[_]], factors: List[Factor[Double]]): (List[Factor[Double]], Map[Variable[_], Factor[_]])
```

Solve the problem by eliminating variables, leaving only the ones that belong in the solution.

Type parameters: none.

Parameters, list 1: `` toEliminate: Set[Variable[_]] ``; `` toPreserve: Set[Variable[_]] ``; `` factors: List[Factor[Double]] ``.

Returns: `` (List[Factor[Double]], Map[Variable[_], Factor[_]]) ``.

Source contract/attributes: Solve the problem by eliminating variables, leaving only the ones that belong in the solution. Value parameters factors Factors over which to perform elimination. toEliminate Variables to eliminate. toPreserve Variables to preserve. Attributes Returns A list of factors over the variables to preserve representing their joint distribution, and a map of recording factors for MPE. Definition Classes SolvingStrategy

Invocation template:

```scala
receiver.eliminate(toEliminate, toPreserve, factors)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEGibbsStrategy.recurse ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEGibbsStrategy.html#recurse-fffff41a)

```scala
override def recurse(subproblem: NestedProblem[_]): RaisingStrategy
```

Returns a strategy that could be used to solve the nested problem.

Type parameters: none.

Parameters, list 1: `` subproblem: NestedProblem[_] ``.

Returns: `` RaisingStrategy ``.

Source contract/attributes: Returns a strategy that could be used to solve the nested problem. Value parameters subproblem Unsolved nested problem to recurse on. Attributes Returns A strategy to solve the nested problem. Definition Classes RaisingStrategy

Invocation template:

```scala
receiver.recurse(subproblem)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEGibbsStrategy.chainNonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEGibbsStrategy.html#chainNonConstraintFactors-fffffa11)

```scala
def chainNonConstraintFactors[ParentValue, Value](chainComp: ChainComponent[ParentValue, Value]): List[Factor[Double]]
```

Get non-constraint factors associated with a single Chain component.

Type parameters: `` ParentValue ``; `` Value ``.

Parameters, list 1: `` chainComp: ChainComponent[ParentValue, Value] ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get non-constraint factors associated with a single Chain component. Value parameters chainComp Chain component to process. Attributes Returns All factors associated with the chain component that are needed for solving. This includes (possibly eliminated) subproblem factors. Inherited from: RaisingStrategy

Invocation template:

```scala
receiver.chainNonConstraintFactors[ParentValue, Value](chainComp)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEGibbsStrategy.constraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEGibbsStrategy.html#constraintFactors-fffff66a)

```scala
def constraintFactors(bounds: Bounds): List[Factor[Double]]
```

Get all of the constraint factors needed for solving.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get all of the constraint factors needed for solving. Value parameters bounds Bounds for the returned constraint factors. Attributes Returns Constraint factors for solving. Inherited from: SolvingStrategy

Invocation template:

```scala
receiver.constraintFactors(bounds)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEGibbsStrategy.execute ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEGibbsStrategy.html#execute-fffff034)

```scala
def execute(bounds: Bounds = ...): Unit
```

Solve the problem defined by all the components' current factors. This involves solving and incorporating subproblems as well. This will set the globals accordingly. All components in this problem and contained subproblems should be eliminated in the solution.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Solve the problem defined by all the components' current factors. This involves solving and incorporating subproblems as well. This will set the globals accordingly. All components in this problem and contained subproblems should be eliminated in the solution. Value parameters bounds Bounds for constraint factors. Defaults to Lower. This default is intended for the cases where it does not matter which bounds should be used because both upper and lower bounds would be the same. Attributes Inherited from: SolvingStrategy

Invocation template:

```scala
receiver.execute(bounds)
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEGibbsStrategy.nonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEGibbsStrategy.html#nonConstraintFactors-fffffc52)

```scala
override def nonConstraintFactors(): List[Factor[Double]]
```

Get all of the non-constraint factors needed for solving. This includes subproblem factors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get all of the non-constraint factors needed for solving. This includes subproblem factors. Attributes Returns Non-constraint factors for solving. Definition Classes RaisingStrategy -> SolvingStrategy Inherited from: RaisingStrategy

Invocation template:

```scala
receiver.nonConstraintFactors()
```

## `` com.cra.figaro.algorithm.structured.strategy.solve.VEGibbsStrategy.subproblemNonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve/VEGibbsStrategy.html#subproblemNonConstraintFactors-189)

```scala
def subproblemNonConstraintFactors[ParentValue, Value](chainComp: ChainComponent[ParentValue, Value]): Map[ParentValue, List[Factor[Double]]]
```

Get the non-constraint factors associated with all subproblems of a Chain component. This returns the existing solution if there is one. Otherwise, it chooses to solve or raise the subproblem based on the raising criteria.

Type parameters: `` ParentValue ``; `` Value ``.

Parameters, list 1: `` chainComp: ChainComponent[ParentValue, Value] ``.

Returns: `` Map[ParentValue, List[Factor[Double]]] ``.

Source contract/attributes: Get the non-constraint factors associated with all subproblems of a Chain component. This returns the existing solution if there is one. Otherwise, it chooses to solve or raise the subproblem based on the raising criteria. Value parameters chainComp Chain component whose subproblems are to be processed. Attributes Returns All factors associated with subproblems that are needed for solving, grouped by parent value. Inherited from: RaisingStrategy

Invocation template:

```scala
receiver.subproblemNonConstraintFactors[ParentValue, Value](chainComp)
```
