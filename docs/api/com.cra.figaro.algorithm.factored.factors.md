<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.algorithm.factored.factors

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.combination ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#combination-eb2)

```scala
override def combination(that: Factor[T], op: (T, T) => T): Factor[T]
```

produces a new Factor from the combined input variables of the input factors.

Type parameters: none.

Parameters, list 1: `` that: Factor[T] ``; `` op: (T, T) => T ``.

Returns: `` Factor[T] ``.

Source contract/attributes: produces a new Factor from the combined input variables of the input factors. The factor values are computed using the corresponding values from the input factors. This correspondence is determined by the indices of the input factors that overlap with each of the indices of the result factor Value parameters op The operation used to combine (multiply) factor values that The Factor to combine with this one Attributes Returns The new Factor containing the combined values of the inputs Definition Classes Factor

Invocation template:

```scala
receiver.combination(that, op)
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.deDuplicate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#deDuplicate-fffffbaf)

```scala
override def deDuplicate(): Factor[T]
```

Returns a new Factor with duplicate variable(s) removed

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Factor[T] ``.

Source contract/attributes: Returns a new Factor with duplicate variable(s) removed Attributes Definition Classes Factor

Invocation template:

```scala
receiver.deDuplicate()
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.fillByRule ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#fillByRule-fffffa40)

```scala
override def fillByRule(rule: List[Extended[_]] => T): Factor[T]
```

Fill the contents of this factor by applying a rule to every combination of parent and output values.

Type parameters: none.

Parameters, list 1: `` rule: List[Extended[_]] => T ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Fill the contents of this factor by applying a rule to every combination of parent and output values. Attributes Definition Classes Factor

Invocation template:

```scala
receiver.fillByRule(rule)
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.mapTo ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#mapTo-fffff0ed)

```scala
override def mapTo[U](fn: T => U, _semiring: Semiring[U] = ...): Factor[U]
```

Convert the contents of the target by applying the given function to all elements of this factor.

Type parameters: `` U ``.

Parameters, list 1: `` fn: T => U ``; `` _semiring: Semiring[U] = ... ``.

Returns: `` Factor[U] ``.

Source contract/attributes: Convert the contents of the target by applying the given function to all elements of this factor. Attributes Definition Classes Factor

Invocation template:

```scala
receiver.mapTo[U](fn, _semiring)
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.marginalizeToWithSum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#marginalizeToWithSum-fffff0ec)

```scala
override def marginalizeToWithSum(sum: (T, T) => T, targets: Variable[_]*): Factor[T]
```

Returns the marginalization of the factor to a variable according to the given addition function. Unlike marginalizeTo, this uses the provided sum function. This is useful e.g. to easily switch between max-product and sum-product operations when the data within are unchanged and the product operation is the same.

Type parameters: none.

Parameters, list 1: `` sum: (T, T) => T ``; `` targets: Variable[_]* ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the marginalization of the factor to a variable according to the given addition function. Unlike marginalizeTo, this uses the provided sum function. This is useful e.g. to easily switch between max-product and sum-product operations when the data within are unchanged and the product operation is the same. The returned factor uses the semiring associated with this factor; it does not override the sum function of the semiring with the function given here. Attributes Definition Classes Factor

Invocation template:

```scala
receiver.marginalizeToWithSum(sum, targets*)
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#product-fffffd5e)

```scala
override def product(that: Factor[T]): Factor[T]
```

returns the product of this factor with another factor according to a given multiplication function. The product is associated with the combined variables of the two inputs, and the value associated with an assignment is the product of the values in the two inputs.

Type parameters: none.

Parameters, list 1: `` that: Factor[T] ``.

Returns: `` Factor[T] ``.

Source contract/attributes: returns the product of this factor with another factor according to a given multiplication function. The product is associated with the combined variables of the two inputs, and the value associated with an assignment is the product of the values in the two inputs. Attributes Definition Classes Factor

Invocation template:

```scala
receiver.product(that)
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.recordArgMax ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#recordArgMax-fffff592)

```scala
override def recordArgMax[U](variable: Variable[U], comparator: (T, T) => Boolean, _semiring: Semiring[U] = ...): Factor[U]
```

Returns a factor that maps values of the other variables to the value of the given variable that maximizes the entry associated with that value, according to some maximization function. comparator defines the maximization. It returns true iff its second argument is greater than its first.

Type parameters: `` U ``.

Parameters, list 1: `` variable: Variable[U] ``; `` comparator: (T, T) => Boolean ``; `` _semiring: Semiring[U] = ... ``.

Returns: `` Factor[U] ``.

Source contract/attributes: Returns a factor that maps values of the other variables to the value of the given variable that maximizes the entry associated with that value, according to some maximization function. comparator defines the maximization. It returns true iff its second argument is greater than its first. Type parameters T The type of entries of this factor. U The type of element whose value is being recorded. The resulting factor maps values of other variables in this factor to this type. Attributes Definition Classes Factor

Invocation template:

```scala
receiver.recordArgMax[U](variable, comparator, _semiring)
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.sumOver ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#sumOver-fffffbe5)

```scala
override def sumOver(variable: Variable[_], sum: (T, T) => T = ...): Factor[T]
```

Returns the summation of the factor over a variable according to an addition function. The result is associated with all the variables in the input except for the summed over variable and the value for a set of assignments is the sum of the values of the corresponding assignments in the input.

Type parameters: none.

Parameters, list 1: `` variable: Variable[_] ``; `` sum: (T, T) => T = ... ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the summation of the factor over a variable according to an addition function. The result is associated with all the variables in the input except for the summed over variable and the value for a set of assignments is the sum of the values of the corresponding assignments in the input. If no funciton is provided, this defaults to the sum function in this factor's semiring. Attributes Definition Classes Factor

Invocation template:

```scala
receiver.sumOver(variable, sum)
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.toReadableString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#toReadableString-0)

```scala
override def toReadableString: String
```

Produce a readable string representation of the factor

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Produce a readable string representation of the factor Attributes Definition Classes Factor

Invocation template:

```scala
receiver.toReadableString
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.unionVars ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#unionVars-514)

```scala
def unionVars[U](that: Factor[U]): (List[Variable[_]], List[Variable[_]], List[Int], List[Int])
```

takes the variables in two factors and produces their set union.

Type parameters: `` U ``.

Parameters, list 1: `` that: Factor[U] ``.

Returns: `` (List[Variable[_]], List[Variable[_]], List[Int], List[Int]) ``.

Source contract/attributes: takes the variables in two factors and produces their set union. It produces a mapping from each original variable to its new location. Similarly it produces a mapping from each new variable to its new location. Attributes

Invocation template:

```scala
receiver.unionVars[U](that)
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.contains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#contains-fffff04e)

```scala
def contains(index: List[Int]): Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` index: List[Int] ``.

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.contains(index)
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.convertIndicesToValues ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#convertIndicesToValues-5c0)

```scala
def convertIndicesToValues(indices: List[Int]): List[Extended[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``.

Returns: `` List[Extended[_]] ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.convertIndicesToValues(indices)
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.foldLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#foldLeft-fffffe32)

```scala
def foldLeft(initial: T, fn: (T, T) => T): T
```

Fold the given function through the contents of the factor, beginning with the given initial values

Type parameters: none.

Parameters, list 1: `` initial: T ``; `` fn: (T, T) => T ``.

Returns: `` T ``.

Source contract/attributes: Fold the given function through the contents of the factor, beginning with the given initial values Attributes Inherited from: Factor

Invocation template:

```scala
receiver.foldLeft(initial, fn)
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.generateIndices ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#generateIndices-0)

```scala
def generateIndices: Indices
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Indices ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.generateIndices
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.getIndices ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#getIndices-0)

```scala
def getIndices: Indices
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Indices ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.getIndices
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.hasStar ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#hasStar-0)

```scala
def hasStar: Boolean
```

Indicates if any of this Factor's variables has Star

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Indicates if any of this Factor's variables has Star Attributes Inherited from: Factor

Invocation template:

```scala
receiver.hasStar
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.isEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#isEmpty-0)

```scala
def isEmpty: Boolean
```

Indicates if this Factor has any variables

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Indicates if this Factor has any variables Attributes Inherited from: Factor

Invocation template:

```scala
receiver.isEmpty
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.marginalizeTo ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#marginalizeTo-3da)

```scala
def marginalizeTo(targets: Variable[_]*): Factor[T]
```

Returns the marginalization of the factor to a variable according to the addition function in this factor's semiring. This involves summing out all other variables.

Type parameters: none.

Parameters, list 1: `` targets: Variable[_]* ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the marginalization of the factor to a variable according to the addition function in this factor's semiring. This involves summing out all other variables. Attributes Inherited from: Factor

Invocation template:

```scala
receiver.marginalizeTo(targets*)
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#toString-0)

```scala
override def toString: String
```

Description that includes the variable list and conditional probabilities

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Description that includes the variable list and conditional probabilities Attributes Definition Classes Factor -> Any Inherited from: Factor

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.variables ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#variables-0)

```scala
def variables: List[Variable[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Variable[_]] ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.variables
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.contentsContains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#contentsContains-fffff04e)

```scala
def contentsContains(index: List[Int]): Boolean
```

Test whether a factor contains a value for a specific index

Type parameters: none.

Parameters, list 1: `` index: List[Int] ``.

Returns: `` Boolean ``.

Source contract/attributes: Test whether a factor contains a value for a specific index Attributes Inherited from: Factor

Invocation template:

```scala
receiver.contentsContains(index)
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.createFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#createFactor-69e)

```scala
def createFactor[T](parents: List[Variable[_]], output: List[Variable[_]], _semiring: Semiring[T] = ...): Factor[T]
```

Creates a new factor of the same type

Type parameters: `` T ``.

Parameters, list 1: `` parents: List[Variable[_]] ``; `` output: List[Variable[_]] ``; `` _semiring: Semiring[T] = ... ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Creates a new factor of the same type Attributes Inherited from: Factor

Invocation template:

```scala
receiver.createFactor[T](parents, output, _semiring)
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#get-fffff1e5)

```scala
def get(indices: List[Int]): T
```

Get the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``.

Returns: `` T ``.

Source contract/attributes: Get the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined. Attributes Inherited from: Factor

Invocation template:

```scala
receiver.get(indices)
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.getContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#getContents-fffffd3a)

```scala
def getContents(): Iterable[T]
```

Get the contents of a factor as a traversable (note: no indicies provided)

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: Get the contents of a factor as a traversable (note: no indicies provided) Attributes Inherited from: Factor

Invocation template:

```scala
receiver.getContents()
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.output ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#output-0)

```scala
def output: List[Variable[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Variable[_]] ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.output
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.parents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#parents-0)

```scala
def parents: List[Variable[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Variable[_]] ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.parents
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.set ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#set-ffffff94)

```scala
def set(indices: List[Int], value: T): Factor[T]
```

Set the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``; `` value: T ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Set the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined. Attributes Inherited from: Factor

Invocation template:

```scala
receiver.set(indices, value)
```

## `` com.cra.figaro.algorithm.factored.factors.BasicFactor.stringContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BasicFactor.html#stringContents-665)

```scala
def stringContents(): String
```

Return the contents of a factor as a string

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Return the contents of a factor as a string Attributes Inherited from: Factor

Invocation template:

```scala
receiver.stringContents()
```

## `` com.cra.figaro.algorithm.factored.factors.BooleanSemiring.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BooleanSemiring.html#product-b60)

```scala
def product(x: Boolean, y: Boolean): Boolean
```

x AND y

Type parameters: none.

Parameters, list 1: `` x: Boolean ``; `` y: Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: x AND y Attributes

Invocation template:

```scala
receiver.product(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.BooleanSemiring.sum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BooleanSemiring.html#sum-b60)

```scala
def sum(x: Boolean, y: Boolean): Boolean
```

x OR y

Type parameters: none.

Parameters, list 1: `` x: Boolean ``; `` y: Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: x OR y Attributes

Invocation template:

```scala
receiver.sum(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.BooleanSemiring.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BooleanSemiring.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.algorithm.factored.factors.BooleanSemiring.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BooleanSemiring.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.algorithm.factored.factors.BooleanSemiring.sumMany ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BooleanSemiring.html#sumMany-2cd)

```scala
def sumMany(xs: Iterable[Boolean]): Boolean
```

Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations.

Type parameters: none.

Parameters, list 1: `` xs: Iterable[Boolean] ``.

Returns: `` Boolean ``.

Source contract/attributes: Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations. Attributes Inherited from: Semiring

Invocation template:

```scala
receiver.sumMany(xs)
```

## `` com.cra.figaro.algorithm.factored.factors.BoundsSumProductSemiring.divide ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BoundsSumProductSemiring.html#divide-fffffc96)

```scala
def divide(x: (Double, Double), y: (Double, Double)): (Double, Double)
```

Division of two entries, as defined by the particular inference problem.

Type parameters: none.

Parameters, list 1: `` x: (Double, Double) ``; `` y: (Double, Double) ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Division of two entries, as defined by the particular inference problem. Attributes

Invocation template:

```scala
receiver.divide(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.BoundsSumProductSemiring.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BoundsSumProductSemiring.html#product-fffffc96)

```scala
def product(x: (Double, Double), y: (Double, Double)): (Double, Double)
```

Product of two entries, as defined by the particular inference problem.

Type parameters: none.

Parameters, list 1: `` x: (Double, Double) ``; `` y: (Double, Double) ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Product of two entries, as defined by the particular inference problem. Attributes

Invocation template:

```scala
receiver.product(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.BoundsSumProductSemiring.sum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BoundsSumProductSemiring.html#sum-fffffc96)

```scala
def sum(x: (Double, Double), y: (Double, Double)): (Double, Double)
```

Sum of two entries, as defined by the particular inference problem. E.g., for computing conditional probabilities, sum is ordinary addition; for most probable explanation, it is max.

Type parameters: none.

Parameters, list 1: `` x: (Double, Double) ``; `` y: (Double, Double) ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Sum of two entries, as defined by the particular inference problem. E.g., for computing conditional probabilities, sum is ordinary addition; for most probable explanation, it is max. Attributes

Invocation template:

```scala
receiver.sum(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.BoundsSumProductSemiring.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BoundsSumProductSemiring.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.algorithm.factored.factors.BoundsSumProductSemiring.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BoundsSumProductSemiring.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.algorithm.factored.factors.BoundsSumProductSemiring.sumMany ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/BoundsSumProductSemiring.html#sumMany-2cd)

```scala
def sumMany(xs: Iterable[(Double, Double)]): (Double, Double)
```

Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations.

Type parameters: none.

Parameters, list 1: `` xs: Iterable[(Double, Double)] ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations. Attributes Inherited from: Semiring

Invocation template:

```scala
receiver.sumMany(xs)
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.createFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#createFactor-69e)

```scala
override def createFactor[T](_parents: List[Variable[_]], _output: List[Variable[_]], _semiring: Semiring[T] = ...): Factor[T]
```

Creates a new factor of the same type

Type parameters: `` T ``.

Parameters, list 1: `` _parents: List[Variable[_]] ``; `` _output: List[Variable[_]] ``; `` _semiring: Semiring[T] = ... ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Creates a new factor of the same type Attributes Definition Classes DenseFactor -> Factor

Invocation template:

```scala
receiver.createFactor[T](_parents, _output, _semiring)
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.mapTo ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#mapTo-fffff0ed)

```scala
override def mapTo[U](fn: T => U, _semiring: Semiring[U] = ...): Factor[U]
```

Convert the contents of the target by applying the given function to all elements of this factor.

Type parameters: `` U ``.

Parameters, list 1: `` fn: T => U ``; `` _semiring: Semiring[U] = ... ``.

Returns: `` Factor[U] ``.

Source contract/attributes: Convert the contents of the target by applying the given function to all elements of this factor. Attributes Definition Classes BasicFactor -> Factor

Invocation template:

```scala
receiver.mapTo[U](fn, _semiring)
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.combination ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#combination-eb2)

```scala
override def combination(that: Factor[T], op: (T, T) => T): Factor[T]
```

produces a new Factor from the combined input variables of the input factors.

Type parameters: none.

Parameters, list 1: `` that: Factor[T] ``; `` op: (T, T) => T ``.

Returns: `` Factor[T] ``.

Source contract/attributes: produces a new Factor from the combined input variables of the input factors. The factor values are computed using the corresponding values from the input factors. This correspondence is determined by the indices of the input factors that overlap with each of the indices of the result factor Value parameters op The operation used to combine (multiply) factor values that The Factor to combine with this one Attributes Returns The new Factor containing the combined values of the inputs Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.combination(that, op)
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.computeIndex ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#computeIndex-ffffff15)

```scala
def computeIndex(indices: List[Int]): Int
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``.

Returns: `` Int ``.

Source contract/attributes: Attributes Inherited from: DenseFactor

Invocation template:

```scala
receiver.computeIndex(indices)
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.contains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#contains-fffff04e)

```scala
def contains(index: List[Int]): Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` index: List[Int] ``.

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.contains(index)
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.contentsContains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#contentsContains-fffff04e)

```scala
def contentsContains(index: List[Int]): Boolean
```

Test whether a factor contains a value for a specific index

Type parameters: none.

Parameters, list 1: `` index: List[Int] ``.

Returns: `` Boolean ``.

Source contract/attributes: Test whether a factor contains a value for a specific index Attributes Inherited from: DenseFactor

Invocation template:

```scala
receiver.contentsContains(index)
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.convertIndicesToValues ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#convertIndicesToValues-5c0)

```scala
def convertIndicesToValues(indices: List[Int]): List[Extended[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``.

Returns: `` List[Extended[_]] ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.convertIndicesToValues(indices)
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.deDuplicate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#deDuplicate-fffffbaf)

```scala
override def deDuplicate(): Factor[T]
```

Returns a new Factor with duplicate variable(s) removed

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Factor[T] ``.

Source contract/attributes: Returns a new Factor with duplicate variable(s) removed Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.deDuplicate()
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.fillByRule ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#fillByRule-fffffa40)

```scala
override def fillByRule(rule: List[Extended[_]] => T): Factor[T]
```

Fill the contents of this factor by applying a rule to every combination of parent and output values.

Type parameters: none.

Parameters, list 1: `` rule: List[Extended[_]] => T ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Fill the contents of this factor by applying a rule to every combination of parent and output values. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.fillByRule(rule)
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.foldLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#foldLeft-fffffe32)

```scala
def foldLeft(initial: T, fn: (T, T) => T): T
```

Fold the given function through the contents of the factor, beginning with the given initial values

Type parameters: none.

Parameters, list 1: `` initial: T ``; `` fn: (T, T) => T ``.

Returns: `` T ``.

Source contract/attributes: Fold the given function through the contents of the factor, beginning with the given initial values Attributes Inherited from: Factor

Invocation template:

```scala
receiver.foldLeft(initial, fn)
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.generateIndices ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#generateIndices-0)

```scala
def generateIndices: Indices
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Indices ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.generateIndices
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#get-fffff1e5)

```scala
def get(indices: List[Int]): T
```

Get the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``.

Returns: `` T ``.

Source contract/attributes: Get the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined. Attributes Inherited from: DenseFactor

Invocation template:

```scala
receiver.get(indices)
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.getContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#getContents-fffffd3a)

```scala
def getContents(): Iterable[T]
```

Get the contents of a factor as a traversable (note: no indicies provided)

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: Get the contents of a factor as a traversable (note: no indicies provided) Attributes Inherited from: DenseFactor

Invocation template:

```scala
receiver.getContents()
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.getIndices ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#getIndices-0)

```scala
def getIndices: Indices
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Indices ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.getIndices
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.hasStar ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#hasStar-0)

```scala
def hasStar: Boolean
```

Indicates if any of this Factor's variables has Star

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Indicates if any of this Factor's variables has Star Attributes Inherited from: Factor

Invocation template:

```scala
receiver.hasStar
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.isEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#isEmpty-0)

```scala
def isEmpty: Boolean
```

Indicates if this Factor has any variables

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Indicates if this Factor has any variables Attributes Inherited from: Factor

Invocation template:

```scala
receiver.isEmpty
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.marginalizeTo ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#marginalizeTo-3da)

```scala
def marginalizeTo(targets: Variable[_]*): Factor[T]
```

Returns the marginalization of the factor to a variable according to the addition function in this factor's semiring. This involves summing out all other variables.

Type parameters: none.

Parameters, list 1: `` targets: Variable[_]* ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the marginalization of the factor to a variable according to the addition function in this factor's semiring. This involves summing out all other variables. Attributes Inherited from: Factor

Invocation template:

```scala
receiver.marginalizeTo(targets*)
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.marginalizeToWithSum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#marginalizeToWithSum-fffff0ec)

```scala
override def marginalizeToWithSum(sum: (T, T) => T, targets: Variable[_]*): Factor[T]
```

Returns the marginalization of the factor to a variable according to the given addition function. Unlike marginalizeTo, this uses the provided sum function. This is useful e.g. to easily switch between max-product and sum-product operations when the data within are unchanged and the product operation is the same.

Type parameters: none.

Parameters, list 1: `` sum: (T, T) => T ``; `` targets: Variable[_]* ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the marginalization of the factor to a variable according to the given addition function. Unlike marginalizeTo, this uses the provided sum function. This is useful e.g. to easily switch between max-product and sum-product operations when the data within are unchanged and the product operation is the same. The returned factor uses the semiring associated with this factor; it does not override the sum function of the semiring with the function given here. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.marginalizeToWithSum(sum, targets*)
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#product-fffffd5e)

```scala
override def product(that: Factor[T]): Factor[T]
```

returns the product of this factor with another factor according to a given multiplication function. The product is associated with the combined variables of the two inputs, and the value associated with an assignment is the product of the values in the two inputs.

Type parameters: none.

Parameters, list 1: `` that: Factor[T] ``.

Returns: `` Factor[T] ``.

Source contract/attributes: returns the product of this factor with another factor according to a given multiplication function. The product is associated with the combined variables of the two inputs, and the value associated with an assignment is the product of the values in the two inputs. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.product(that)
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.recordArgMax ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#recordArgMax-fffff592)

```scala
override def recordArgMax[U](variable: Variable[U], comparator: (T, T) => Boolean, _semiring: Semiring[U] = ...): Factor[U]
```

Returns a factor that maps values of the other variables to the value of the given variable that maximizes the entry associated with that value, according to some maximization function. comparator defines the maximization. It returns true iff its second argument is greater than its first.

Type parameters: `` U ``.

Parameters, list 1: `` variable: Variable[U] ``; `` comparator: (T, T) => Boolean ``; `` _semiring: Semiring[U] = ... ``.

Returns: `` Factor[U] ``.

Source contract/attributes: Returns a factor that maps values of the other variables to the value of the given variable that maximizes the entry associated with that value, according to some maximization function. comparator defines the maximization. It returns true iff its second argument is greater than its first. Type parameters T The type of entries of this factor. U The type of element whose value is being recorded. The resulting factor maps values of other variables in this factor to this type. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.recordArgMax[U](variable, comparator, _semiring)
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.set ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#set-ffffff94)

```scala
def set(indices: List[Int], value: T): Factor[T]
```

Set the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``; `` value: T ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Set the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined. Attributes Inherited from: DenseFactor

Invocation template:

```scala
receiver.set(indices, value)
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.stringContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#stringContents-665)

```scala
def stringContents(): String
```

Return the contents of a factor as a string

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Return the contents of a factor as a string Attributes Inherited from: DenseFactor

Invocation template:

```scala
receiver.stringContents()
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.sumOver ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#sumOver-fffffbe5)

```scala
override def sumOver(variable: Variable[_], sum: (T, T) => T = ...): Factor[T]
```

Returns the summation of the factor over a variable according to an addition function. The result is associated with all the variables in the input except for the summed over variable and the value for a set of assignments is the sum of the values of the corresponding assignments in the input.

Type parameters: none.

Parameters, list 1: `` variable: Variable[_] ``; `` sum: (T, T) => T = ... ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the summation of the factor over a variable according to an addition function. The result is associated with all the variables in the input except for the summed over variable and the value for a set of assignments is the sum of the values of the corresponding assignments in the input. If no funciton is provided, this defaults to the sum function in this factor's semiring. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.sumOver(variable, sum)
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.toReadableString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#toReadableString-0)

```scala
override def toReadableString: String
```

Produce a readable string representation of the factor

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Produce a readable string representation of the factor Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.toReadableString
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#toString-0)

```scala
override def toString: String
```

Description that includes the variable list and conditional probabilities

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Description that includes the variable list and conditional probabilities Attributes Definition Classes Factor -> Any Inherited from: Factor

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.unionVars ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#unionVars-514)

```scala
def unionVars[U](that: Factor[U]): (List[Variable[_]], List[Variable[_]], List[Int], List[Int])
```

takes the variables in two factors and produces their set union.

Type parameters: `` U ``.

Parameters, list 1: `` that: Factor[U] ``.

Returns: `` (List[Variable[_]], List[Variable[_]], List[Int], List[Int]) ``.

Source contract/attributes: takes the variables in two factors and produces their set union. It produces a mapping from each original variable to its new location. Similarly it produces a mapping from each new variable to its new location. Attributes Inherited from: BasicFactor

Invocation template:

```scala
receiver.unionVars[U](that)
```

## `` com.cra.figaro.algorithm.factored.factors.ConditionalSelector.variables ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConditionalSelector.html#variables-0)

```scala
def variables: List[Variable[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Variable[_]] ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.variables
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.combination ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#combination-eb2)

```scala
override def combination(that: Factor[T], op: (T, T) => T): Factor[T]
```

produces a new Factor from the combined input variables of the input factors.

Type parameters: none.

Parameters, list 1: `` that: Factor[T] ``; `` op: (T, T) => T ``.

Returns: `` Factor[T] ``.

Source contract/attributes: produces a new Factor from the combined input variables of the input factors. The factor values are computed using the corresponding values from the input factors. This correspondence is determined by the indices of the input factors that overlap with each of the indices of the result factor Value parameters op The operation used to combine (multiply) factor values that The Factor to combine with this one Attributes Returns The new Factor containing the combined values of the inputs Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.combination(that, op)
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.computeIndex ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#computeIndex-ffffff15)

```scala
def computeIndex(indices: List[Int]): Int
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``.

Returns: `` Int ``.

Source contract/attributes: Attributes Inherited from: DenseFactor

Invocation template:

```scala
receiver.computeIndex(indices)
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.contains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#contains-fffff04e)

```scala
def contains(index: List[Int]): Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` index: List[Int] ``.

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.contains(index)
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.contentsContains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#contentsContains-fffff04e)

```scala
def contentsContains(index: List[Int]): Boolean
```

Test whether a factor contains a value for a specific index

Type parameters: none.

Parameters, list 1: `` index: List[Int] ``.

Returns: `` Boolean ``.

Source contract/attributes: Test whether a factor contains a value for a specific index Attributes Inherited from: DenseFactor

Invocation template:

```scala
receiver.contentsContains(index)
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.convertIndicesToValues ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#convertIndicesToValues-5c0)

```scala
def convertIndicesToValues(indices: List[Int]): List[Extended[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``.

Returns: `` List[Extended[_]] ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.convertIndicesToValues(indices)
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.createFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#createFactor-69e)

```scala
override def createFactor[T](parents: List[Variable[_]], output: List[Variable[_]], _semiring: Semiring[T] = ...): Factor[T]
```

Creates a new factor of the same type

Type parameters: `` T ``.

Parameters, list 1: `` parents: List[Variable[_]] ``; `` output: List[Variable[_]] ``; `` _semiring: Semiring[T] = ... ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Creates a new factor of the same type Attributes Definition Classes DenseFactor -> Factor Inherited from: DenseFactor

Invocation template:

```scala
receiver.createFactor[T](parents, output, _semiring)
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.deDuplicate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#deDuplicate-fffffbaf)

```scala
override def deDuplicate(): Factor[T]
```

Returns a new Factor with duplicate variable(s) removed

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Factor[T] ``.

Source contract/attributes: Returns a new Factor with duplicate variable(s) removed Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.deDuplicate()
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.fillByRule ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#fillByRule-fffffa40)

```scala
override def fillByRule(rule: List[Extended[_]] => T): Factor[T]
```

Fill the contents of this factor by applying a rule to every combination of parent and output values.

Type parameters: none.

Parameters, list 1: `` rule: List[Extended[_]] => T ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Fill the contents of this factor by applying a rule to every combination of parent and output values. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.fillByRule(rule)
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.foldLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#foldLeft-fffffe32)

```scala
def foldLeft(initial: T, fn: (T, T) => T): T
```

Fold the given function through the contents of the factor, beginning with the given initial values

Type parameters: none.

Parameters, list 1: `` initial: T ``; `` fn: (T, T) => T ``.

Returns: `` T ``.

Source contract/attributes: Fold the given function through the contents of the factor, beginning with the given initial values Attributes Inherited from: Factor

Invocation template:

```scala
receiver.foldLeft(initial, fn)
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.generateIndices ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#generateIndices-0)

```scala
def generateIndices: Indices
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Indices ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.generateIndices
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#get-fffff1e5)

```scala
def get(indices: List[Int]): T
```

Get the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``.

Returns: `` T ``.

Source contract/attributes: Get the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined. Attributes Inherited from: DenseFactor

Invocation template:

```scala
receiver.get(indices)
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.getContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#getContents-fffffd3a)

```scala
def getContents(): Iterable[T]
```

Get the contents of a factor as a traversable (note: no indicies provided)

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: Get the contents of a factor as a traversable (note: no indicies provided) Attributes Inherited from: DenseFactor

Invocation template:

```scala
receiver.getContents()
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.getIndices ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#getIndices-0)

```scala
def getIndices: Indices
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Indices ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.getIndices
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.hasStar ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#hasStar-0)

```scala
def hasStar: Boolean
```

Indicates if any of this Factor's variables has Star

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Indicates if any of this Factor's variables has Star Attributes Inherited from: Factor

Invocation template:

```scala
receiver.hasStar
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.isEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#isEmpty-0)

```scala
def isEmpty: Boolean
```

Indicates if this Factor has any variables

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Indicates if this Factor has any variables Attributes Inherited from: Factor

Invocation template:

```scala
receiver.isEmpty
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.mapTo ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#mapTo-fffff0ed)

```scala
override def mapTo[U](fn: T => U, _semiring: Semiring[U] = ...): Factor[U]
```

Convert the contents of the target by applying the given function to all elements of this factor.

Type parameters: `` U ``.

Parameters, list 1: `` fn: T => U ``; `` _semiring: Semiring[U] = ... ``.

Returns: `` Factor[U] ``.

Source contract/attributes: Convert the contents of the target by applying the given function to all elements of this factor. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.mapTo[U](fn, _semiring)
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.marginalizeTo ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#marginalizeTo-3da)

```scala
def marginalizeTo(targets: Variable[_]*): Factor[T]
```

Returns the marginalization of the factor to a variable according to the addition function in this factor's semiring. This involves summing out all other variables.

Type parameters: none.

Parameters, list 1: `` targets: Variable[_]* ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the marginalization of the factor to a variable according to the addition function in this factor's semiring. This involves summing out all other variables. Attributes Inherited from: Factor

Invocation template:

```scala
receiver.marginalizeTo(targets*)
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.marginalizeToWithSum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#marginalizeToWithSum-fffff0ec)

```scala
override def marginalizeToWithSum(sum: (T, T) => T, targets: Variable[_]*): Factor[T]
```

Returns the marginalization of the factor to a variable according to the given addition function. Unlike marginalizeTo, this uses the provided sum function. This is useful e.g. to easily switch between max-product and sum-product operations when the data within are unchanged and the product operation is the same.

Type parameters: none.

Parameters, list 1: `` sum: (T, T) => T ``; `` targets: Variable[_]* ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the marginalization of the factor to a variable according to the given addition function. Unlike marginalizeTo, this uses the provided sum function. This is useful e.g. to easily switch between max-product and sum-product operations when the data within are unchanged and the product operation is the same. The returned factor uses the semiring associated with this factor; it does not override the sum function of the semiring with the function given here. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.marginalizeToWithSum(sum, targets*)
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#product-fffffd5e)

```scala
override def product(that: Factor[T]): Factor[T]
```

returns the product of this factor with another factor according to a given multiplication function. The product is associated with the combined variables of the two inputs, and the value associated with an assignment is the product of the values in the two inputs.

Type parameters: none.

Parameters, list 1: `` that: Factor[T] ``.

Returns: `` Factor[T] ``.

Source contract/attributes: returns the product of this factor with another factor according to a given multiplication function. The product is associated with the combined variables of the two inputs, and the value associated with an assignment is the product of the values in the two inputs. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.product(that)
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.recordArgMax ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#recordArgMax-fffff592)

```scala
override def recordArgMax[U](variable: Variable[U], comparator: (T, T) => Boolean, _semiring: Semiring[U] = ...): Factor[U]
```

Returns a factor that maps values of the other variables to the value of the given variable that maximizes the entry associated with that value, according to some maximization function. comparator defines the maximization. It returns true iff its second argument is greater than its first.

Type parameters: `` U ``.

Parameters, list 1: `` variable: Variable[U] ``; `` comparator: (T, T) => Boolean ``; `` _semiring: Semiring[U] = ... ``.

Returns: `` Factor[U] ``.

Source contract/attributes: Returns a factor that maps values of the other variables to the value of the given variable that maximizes the entry associated with that value, according to some maximization function. comparator defines the maximization. It returns true iff its second argument is greater than its first. Type parameters T The type of entries of this factor. U The type of element whose value is being recorded. The resulting factor maps values of other variables in this factor to this type. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.recordArgMax[U](variable, comparator, _semiring)
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.set ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#set-ffffff94)

```scala
def set(indices: List[Int], value: T): Factor[T]
```

Set the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``; `` value: T ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Set the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined. Attributes Inherited from: DenseFactor

Invocation template:

```scala
receiver.set(indices, value)
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.stringContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#stringContents-665)

```scala
def stringContents(): String
```

Return the contents of a factor as a string

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Return the contents of a factor as a string Attributes Inherited from: DenseFactor

Invocation template:

```scala
receiver.stringContents()
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.sumOver ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#sumOver-fffffbe5)

```scala
override def sumOver(variable: Variable[_], sum: (T, T) => T = ...): Factor[T]
```

Returns the summation of the factor over a variable according to an addition function. The result is associated with all the variables in the input except for the summed over variable and the value for a set of assignments is the sum of the values of the corresponding assignments in the input.

Type parameters: none.

Parameters, list 1: `` variable: Variable[_] ``; `` sum: (T, T) => T = ... ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the summation of the factor over a variable according to an addition function. The result is associated with all the variables in the input except for the summed over variable and the value for a set of assignments is the sum of the values of the corresponding assignments in the input. If no funciton is provided, this defaults to the sum function in this factor's semiring. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.sumOver(variable, sum)
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.toReadableString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#toReadableString-0)

```scala
override def toReadableString: String
```

Produce a readable string representation of the factor

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Produce a readable string representation of the factor Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.toReadableString
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#toString-0)

```scala
override def toString: String
```

Description that includes the variable list and conditional probabilities

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Description that includes the variable list and conditional probabilities Attributes Definition Classes Factor -> Any Inherited from: Factor

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.unionVars ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#unionVars-514)

```scala
def unionVars[U](that: Factor[U]): (List[Variable[_]], List[Variable[_]], List[Int], List[Int])
```

takes the variables in two factors and produces their set union.

Type parameters: `` U ``.

Parameters, list 1: `` that: Factor[U] ``.

Returns: `` (List[Variable[_]], List[Variable[_]], List[Int], List[Int]) ``.

Source contract/attributes: takes the variables in two factors and produces their set union. It produces a mapping from each original variable to its new location. Similarly it produces a mapping from each new variable to its new location. Attributes Inherited from: BasicFactor

Invocation template:

```scala
receiver.unionVars[U](that)
```

## `` com.cra.figaro.algorithm.factored.factors.ConstraintFactor.variables ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ConstraintFactor.html#variables-0)

```scala
def variables: List[Variable[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Variable[_]] ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.variables
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.computeIndex ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#computeIndex-ffffff15)

```scala
def computeIndex(indices: List[Int]): Int
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``.

Returns: `` Int ``.

Invocation template:

```scala
receiver.computeIndex(indices)
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.contentsContains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#contentsContains-fffff04e)

```scala
def contentsContains(index: List[Int]): Boolean
```

Test whether a factor contains a value for a specific index

Type parameters: none.

Parameters, list 1: `` index: List[Int] ``.

Returns: `` Boolean ``.

Source contract/attributes: Test whether a factor contains a value for a specific index Attributes

Invocation template:

```scala
receiver.contentsContains(index)
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.createFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#createFactor-69e)

```scala
override def createFactor[T](parents: List[Variable[_]], output: List[Variable[_]], _semiring: Semiring[T] = ...): Factor[T]
```

Creates a new factor of the same type

Type parameters: `` T ``.

Parameters, list 1: `` parents: List[Variable[_]] ``; `` output: List[Variable[_]] ``; `` _semiring: Semiring[T] = ... ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Creates a new factor of the same type Attributes Definition Classes Factor

Invocation template:

```scala
receiver.createFactor[T](parents, output, _semiring)
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#get-fffff1e5)

```scala
def get(indices: List[Int]): T
```

Get the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``.

Returns: `` T ``.

Source contract/attributes: Get the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined. Attributes

Invocation template:

```scala
receiver.get(indices)
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.getContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#getContents-fffffd3a)

```scala
def getContents(): Iterable[T]
```

Get the contents of a factor as a traversable (note: no indicies provided)

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: Get the contents of a factor as a traversable (note: no indicies provided) Attributes

Invocation template:

```scala
receiver.getContents()
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.set ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#set-ffffff94)

```scala
def set(indices: List[Int], value: T): Factor[T]
```

Set the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``; `` value: T ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Set the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined. Attributes

Invocation template:

```scala
receiver.set(indices, value)
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.stringContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#stringContents-665)

```scala
def stringContents(): String
```

Return the contents of a factor as a string

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Return the contents of a factor as a string Attributes

Invocation template:

```scala
receiver.stringContents()
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.combination ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#combination-eb2)

```scala
override def combination(that: Factor[T], op: (T, T) => T): Factor[T]
```

produces a new Factor from the combined input variables of the input factors.

Type parameters: none.

Parameters, list 1: `` that: Factor[T] ``; `` op: (T, T) => T ``.

Returns: `` Factor[T] ``.

Source contract/attributes: produces a new Factor from the combined input variables of the input factors. The factor values are computed using the corresponding values from the input factors. This correspondence is determined by the indices of the input factors that overlap with each of the indices of the result factor Value parameters op The operation used to combine (multiply) factor values that The Factor to combine with this one Attributes Returns The new Factor containing the combined values of the inputs Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.combination(that, op)
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.contains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#contains-fffff04e)

```scala
def contains(index: List[Int]): Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` index: List[Int] ``.

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.contains(index)
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.convertIndicesToValues ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#convertIndicesToValues-5c0)

```scala
def convertIndicesToValues(indices: List[Int]): List[Extended[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``.

Returns: `` List[Extended[_]] ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.convertIndicesToValues(indices)
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.deDuplicate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#deDuplicate-fffffbaf)

```scala
override def deDuplicate(): Factor[T]
```

Returns a new Factor with duplicate variable(s) removed

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Factor[T] ``.

Source contract/attributes: Returns a new Factor with duplicate variable(s) removed Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.deDuplicate()
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.fillByRule ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#fillByRule-fffffa40)

```scala
override def fillByRule(rule: List[Extended[_]] => T): Factor[T]
```

Fill the contents of this factor by applying a rule to every combination of parent and output values.

Type parameters: none.

Parameters, list 1: `` rule: List[Extended[_]] => T ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Fill the contents of this factor by applying a rule to every combination of parent and output values. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.fillByRule(rule)
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.foldLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#foldLeft-fffffe32)

```scala
def foldLeft(initial: T, fn: (T, T) => T): T
```

Fold the given function through the contents of the factor, beginning with the given initial values

Type parameters: none.

Parameters, list 1: `` initial: T ``; `` fn: (T, T) => T ``.

Returns: `` T ``.

Source contract/attributes: Fold the given function through the contents of the factor, beginning with the given initial values Attributes Inherited from: Factor

Invocation template:

```scala
receiver.foldLeft(initial, fn)
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.generateIndices ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#generateIndices-0)

```scala
def generateIndices: Indices
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Indices ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.generateIndices
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.getIndices ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#getIndices-0)

```scala
def getIndices: Indices
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Indices ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.getIndices
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.hasStar ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#hasStar-0)

```scala
def hasStar: Boolean
```

Indicates if any of this Factor's variables has Star

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Indicates if any of this Factor's variables has Star Attributes Inherited from: Factor

Invocation template:

```scala
receiver.hasStar
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.isEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#isEmpty-0)

```scala
def isEmpty: Boolean
```

Indicates if this Factor has any variables

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Indicates if this Factor has any variables Attributes Inherited from: Factor

Invocation template:

```scala
receiver.isEmpty
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.mapTo ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#mapTo-fffff0ed)

```scala
override def mapTo[U](fn: T => U, _semiring: Semiring[U] = ...): Factor[U]
```

Convert the contents of the target by applying the given function to all elements of this factor.

Type parameters: `` U ``.

Parameters, list 1: `` fn: T => U ``; `` _semiring: Semiring[U] = ... ``.

Returns: `` Factor[U] ``.

Source contract/attributes: Convert the contents of the target by applying the given function to all elements of this factor. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.mapTo[U](fn, _semiring)
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.marginalizeTo ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#marginalizeTo-3da)

```scala
def marginalizeTo(targets: Variable[_]*): Factor[T]
```

Returns the marginalization of the factor to a variable according to the addition function in this factor's semiring. This involves summing out all other variables.

Type parameters: none.

Parameters, list 1: `` targets: Variable[_]* ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the marginalization of the factor to a variable according to the addition function in this factor's semiring. This involves summing out all other variables. Attributes Inherited from: Factor

Invocation template:

```scala
receiver.marginalizeTo(targets*)
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.marginalizeToWithSum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#marginalizeToWithSum-fffff0ec)

```scala
override def marginalizeToWithSum(sum: (T, T) => T, targets: Variable[_]*): Factor[T]
```

Returns the marginalization of the factor to a variable according to the given addition function. Unlike marginalizeTo, this uses the provided sum function. This is useful e.g. to easily switch between max-product and sum-product operations when the data within are unchanged and the product operation is the same.

Type parameters: none.

Parameters, list 1: `` sum: (T, T) => T ``; `` targets: Variable[_]* ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the marginalization of the factor to a variable according to the given addition function. Unlike marginalizeTo, this uses the provided sum function. This is useful e.g. to easily switch between max-product and sum-product operations when the data within are unchanged and the product operation is the same. The returned factor uses the semiring associated with this factor; it does not override the sum function of the semiring with the function given here. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.marginalizeToWithSum(sum, targets*)
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#product-fffffd5e)

```scala
override def product(that: Factor[T]): Factor[T]
```

returns the product of this factor with another factor according to a given multiplication function. The product is associated with the combined variables of the two inputs, and the value associated with an assignment is the product of the values in the two inputs.

Type parameters: none.

Parameters, list 1: `` that: Factor[T] ``.

Returns: `` Factor[T] ``.

Source contract/attributes: returns the product of this factor with another factor according to a given multiplication function. The product is associated with the combined variables of the two inputs, and the value associated with an assignment is the product of the values in the two inputs. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.product(that)
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.recordArgMax ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#recordArgMax-fffff592)

```scala
override def recordArgMax[U](variable: Variable[U], comparator: (T, T) => Boolean, _semiring: Semiring[U] = ...): Factor[U]
```

Returns a factor that maps values of the other variables to the value of the given variable that maximizes the entry associated with that value, according to some maximization function. comparator defines the maximization. It returns true iff its second argument is greater than its first.

Type parameters: `` U ``.

Parameters, list 1: `` variable: Variable[U] ``; `` comparator: (T, T) => Boolean ``; `` _semiring: Semiring[U] = ... ``.

Returns: `` Factor[U] ``.

Source contract/attributes: Returns a factor that maps values of the other variables to the value of the given variable that maximizes the entry associated with that value, according to some maximization function. comparator defines the maximization. It returns true iff its second argument is greater than its first. Type parameters T The type of entries of this factor. U The type of element whose value is being recorded. The resulting factor maps values of other variables in this factor to this type. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.recordArgMax[U](variable, comparator, _semiring)
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.sumOver ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#sumOver-fffffbe5)

```scala
override def sumOver(variable: Variable[_], sum: (T, T) => T = ...): Factor[T]
```

Returns the summation of the factor over a variable according to an addition function. The result is associated with all the variables in the input except for the summed over variable and the value for a set of assignments is the sum of the values of the corresponding assignments in the input.

Type parameters: none.

Parameters, list 1: `` variable: Variable[_] ``; `` sum: (T, T) => T = ... ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the summation of the factor over a variable according to an addition function. The result is associated with all the variables in the input except for the summed over variable and the value for a set of assignments is the sum of the values of the corresponding assignments in the input. If no funciton is provided, this defaults to the sum function in this factor's semiring. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.sumOver(variable, sum)
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.toReadableString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#toReadableString-0)

```scala
override def toReadableString: String
```

Produce a readable string representation of the factor

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Produce a readable string representation of the factor Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.toReadableString
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#toString-0)

```scala
override def toString: String
```

Description that includes the variable list and conditional probabilities

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Description that includes the variable list and conditional probabilities Attributes Definition Classes Factor -> Any Inherited from: Factor

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.unionVars ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#unionVars-514)

```scala
def unionVars[U](that: Factor[U]): (List[Variable[_]], List[Variable[_]], List[Int], List[Int])
```

takes the variables in two factors and produces their set union.

Type parameters: `` U ``.

Parameters, list 1: `` that: Factor[U] ``.

Returns: `` (List[Variable[_]], List[Variable[_]], List[Int], List[Int]) ``.

Source contract/attributes: takes the variables in two factors and produces their set union. It produces a mapping from each original variable to its new location. Similarly it produces a mapping from each new variable to its new location. Attributes Inherited from: BasicFactor

Invocation template:

```scala
receiver.unionVars[U](that)
```

## `` com.cra.figaro.algorithm.factored.factors.DenseFactor.variables ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DenseFactor.html#variables-0)

```scala
def variables: List[Variable[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Variable[_]] ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.variables
```

## `` com.cra.figaro.algorithm.factored.factors.DivideableSemiRing.divide ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DivideableSemiRing.html#divide-fffffab9)

```scala
def divide(x: T, y: T): T
```

Division of two entries, as defined by the particular inference problem.

Type parameters: none.

Parameters, list 1: `` x: T ``; `` y: T ``.

Returns: `` T ``.

Source contract/attributes: Division of two entries, as defined by the particular inference problem. Attributes

Invocation template:

```scala
receiver.divide(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.DivideableSemiRing.sumMany ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DivideableSemiRing.html#sumMany-2cd)

```scala
def sumMany(xs: Iterable[T]): T
```

Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations.

Type parameters: none.

Parameters, list 1: `` xs: Iterable[T] ``.

Returns: `` T ``.

Source contract/attributes: Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations. Attributes Inherited from: Semiring

Invocation template:

```scala
receiver.sumMany(xs)
```

## `` com.cra.figaro.algorithm.factored.factors.DivideableSemiRing.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DivideableSemiRing.html#product-fffffab9)

```scala
def product(x: T, y: T): T
```

Product of two entries, as defined by the particular inference problem.

Type parameters: none.

Parameters, list 1: `` x: T ``; `` y: T ``.

Returns: `` T ``.

Source contract/attributes: Product of two entries, as defined by the particular inference problem. Attributes Inherited from: Semiring

Invocation template:

```scala
receiver.product(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.DivideableSemiRing.sum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/DivideableSemiRing.html#sum-fffffab9)

```scala
def sum(x: T, y: T): T
```

Sum of two entries, as defined by the particular inference problem. E.g., for computing conditional probabilities, sum is ordinary addition; for most probable explanation, it is max.

Type parameters: none.

Parameters, list 1: `` x: T ``; `` y: T ``.

Returns: `` T ``.

Source contract/attributes: Sum of two entries, as defined by the particular inference problem. E.g., for computing conditional probabilities, sum is ordinary addition; for most probable explanation, it is max. Attributes Inherited from: Semiring

Invocation template:

```scala
receiver.sum(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.ElementVariable.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ElementVariable.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Variable -> Any

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.algorithm.factored.factors.ElementVariable.equals ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ElementVariable.html#equals-4ad)

```scala
override def equals(o: Any): Boolean
```

Override equality of Variable. Variables are the same if their id's are the same

Type parameters: none.

Parameters, list 1: `` o: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Override equality of Variable. Variables are the same if their id's are the same Attributes Definition Classes Variable -> Any Inherited from: Variable

Invocation template:

```scala
receiver.equals(o)
```

## `` com.cra.figaro.algorithm.factored.factors.ElementVariable.hashCode ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ElementVariable.html#hashCode-0)

```scala
override def hashCode: Int
```

Calculates a hash code value for the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Calculates a hash code value for the object. The default hashing algorithm is platform dependent. Note that it is allowed for two objects to have identical hash codes (o1.hashCode.equals(o2.hashCode)) yet not be equal (o1.equals(o2) returns false). A degenerate implementation could always return 0. However, it is required that if two objects are equal (o1.equals(o2) returns true) that they have identical hash codes (o1.hashCode.equals(o2.hashCode)). Therefore, when overriding this method, be sure to verify that the behavior is consistent with the equals method. Attributes Returns the hash code value for this object. Definition Classes Variable -> Any Inherited from: Variable

Invocation template:

```scala
receiver.hashCode
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.combination ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#combination-eb2)

```scala
def combination(that: Factor[T], op: (T, T) => T): Factor[T]
```

Combination function for factors. By default, this is product, but other operations (such as divide that is a valid operation for some semirings) can use this

Type parameters: none.

Parameters, list 1: `` that: Factor[T] ``; `` op: (T, T) => T ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Combination function for factors. By default, this is product, but other operations (such as divide that is a valid operation for some semirings) can use this Attributes

Invocation template:

```scala
receiver.combination(that, op)
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.contentsContains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#contentsContains-fffff04e)

```scala
def contentsContains(index: List[Int]): Boolean
```

Test whether a factor contains a value for a specific index

Type parameters: none.

Parameters, list 1: `` index: List[Int] ``.

Returns: `` Boolean ``.

Source contract/attributes: Test whether a factor contains a value for a specific index Attributes

Invocation template:

```scala
receiver.contentsContains(index)
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.createFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#createFactor-69e)

```scala
def createFactor[T](parents: List[Variable[_]], output: List[Variable[_]], _semiring: Semiring[T] = ...): Factor[T]
```

Creates a new factor of the same type

Type parameters: `` T ``.

Parameters, list 1: `` parents: List[Variable[_]] ``; `` output: List[Variable[_]] ``; `` _semiring: Semiring[T] = ... ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Creates a new factor of the same type Attributes

Invocation template:

```scala
receiver.createFactor[T](parents, output, _semiring)
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.deDuplicate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#deDuplicate-fffffbaf)

```scala
def deDuplicate(): Factor[T]
```

Returns a new Factor with duplicate variable(s) removed

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Factor[T] ``.

Source contract/attributes: Returns a new Factor with duplicate variable(s) removed Attributes

Invocation template:

```scala
receiver.deDuplicate()
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.fillByRule ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#fillByRule-fffffa40)

```scala
def fillByRule(rule: List[Extended[_]] => T): Factor[T]
```

Fill the contents of this factor by applying a rule to every combination of values.

Type parameters: none.

Parameters, list 1: `` rule: List[Extended[_]] => T ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Fill the contents of this factor by applying a rule to every combination of values. Attributes

Invocation template:

```scala
receiver.fillByRule(rule)
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#get-fffff1e5)

```scala
def get(indices: List[Int]): T
```

Get the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``.

Returns: `` T ``.

Source contract/attributes: Get the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined. Attributes

Invocation template:

```scala
receiver.get(indices)
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.getContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#getContents-fffffd3a)

```scala
def getContents(): Iterable[T]
```

Get the contents of a factor as a traversable (note: no indicies provided)

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: Get the contents of a factor as a traversable (note: no indicies provided) Attributes

Invocation template:

```scala
receiver.getContents()
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.mapTo ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#mapTo-fffff0ed)

```scala
def mapTo[U](fn: T => U, _semiring: Semiring[U] = ...): Factor[U]
```

Fill the contents of the target by applying the given function to all elements of this factor.

Type parameters: `` U ``.

Parameters, list 1: `` fn: T => U ``; `` _semiring: Semiring[U] = ... ``.

Returns: `` Factor[U] ``.

Source contract/attributes: Fill the contents of the target by applying the given function to all elements of this factor. Attributes

Invocation template:

```scala
receiver.mapTo[U](fn, _semiring)
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.marginalizeToWithSum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#marginalizeToWithSum-fffff0ec)

```scala
def marginalizeToWithSum(sum: (T, T) => T, targets: Variable[_]*): Factor[T]
```

Returns the marginalization of the factor to a variable according to the given addition function. Unlike marginalizeTo, this uses the provided sum function. This is useful e.g. to easily switch between max-product and sum-product operations when the data within are unchanged and the product operation is the same.

Type parameters: none.

Parameters, list 1: `` sum: (T, T) => T ``; `` targets: Variable[_]* ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the marginalization of the factor to a variable according to the given addition function. Unlike marginalizeTo, this uses the provided sum function. This is useful e.g. to easily switch between max-product and sum-product operations when the data within are unchanged and the product operation is the same. The returned factor uses the semiring associated with this factor; it does not override the sum function of the semiring with the function given here. Attributes

Invocation template:

```scala
receiver.marginalizeToWithSum(sum, targets*)
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.output ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#output-0)

```scala
def output: List[Variable[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Variable[_]] ``.

Invocation template:

```scala
receiver.output
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.parents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#parents-0)

```scala
def parents: List[Variable[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Variable[_]] ``.

Invocation template:

```scala
receiver.parents
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#product-fffffd5e)

```scala
def product(that: Factor[T]): Factor[T]
```

Returns the product of this factor with another factor according to a given multiplication function. The product is associated with all variables in the two inputs, and the value associated with an assignment is the product of the values in the two inputs.

Type parameters: none.

Parameters, list 1: `` that: Factor[T] ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the product of this factor with another factor according to a given multiplication function. The product is associated with all variables in the two inputs, and the value associated with an assignment is the product of the values in the two inputs. Attributes

Invocation template:

```scala
receiver.product(that)
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.recordArgMax ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#recordArgMax-fffff592)

```scala
def recordArgMax[U](variable: Variable[U], comparator: (T, T) => Boolean, _semiring: Semiring[U] = ...): Factor[U]
```

Returns a factor that maps values of the other variables to the value of the given variable that maximizes the entry associated with that value, according to some maximization function. comparator defines the maximization. It returns true iff its second argument is greater than its first.

Type parameters: `` U ``.

Parameters, list 1: `` variable: Variable[U] ``; `` comparator: (T, T) => Boolean ``; `` _semiring: Semiring[U] = ... ``.

Returns: `` Factor[U] ``.

Source contract/attributes: Returns a factor that maps values of the other variables to the value of the given variable that maximizes the entry associated with that value, according to some maximization function. comparator defines the maximization. It returns true iff its second argument is greater than its first. Type parameters T The type of entries of this factor. U The type of element whose value is being recorded. The resulting factor maps values of other variables in this factor to this type. Attributes

Invocation template:

```scala
receiver.recordArgMax[U](variable, comparator, _semiring)
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.semiring ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#semiring-0)

```scala
def semiring: Semiring[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Semiring[T] ``.

Invocation template:

```scala
receiver.semiring
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.set ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#set-ffffff94)

```scala
def set(indices: List[Int], value: T): Factor[T]
```

Set the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``; `` value: T ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Set the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined. Attributes

Invocation template:

```scala
receiver.set(indices, value)
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.stringContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#stringContents-665)

```scala
def stringContents(): String
```

Return the contents of a factor as a string

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Return the contents of a factor as a string Attributes

Invocation template:

```scala
receiver.stringContents()
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.sumOver ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#sumOver-fffffbe5)

```scala
def sumOver(variable: Variable[_], sum: (T, T) => T = ...): Factor[T]
```

Returns the summation of the factor over a variable according to an addition function. The result is associated with all the variables in the input except for the summed over variable and the value for a set of assignments is the sum of the values of the corresponding assignments in the input.

Type parameters: none.

Parameters, list 1: `` variable: Variable[_] ``; `` sum: (T, T) => T = ... ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the summation of the factor over a variable according to an addition function. The result is associated with all the variables in the input except for the summed over variable and the value for a set of assignments is the sum of the values of the corresponding assignments in the input. If no funciton is provided, this defaults to the sum function in this factor's semiring. Attributes

Invocation template:

```scala
receiver.sumOver(variable, sum)
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.toReadableString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#toReadableString-0)

```scala
def toReadableString: String
```

Produce a readable string representation of the factor

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Produce a readable string representation of the factor Attributes

Invocation template:

```scala
receiver.toReadableString
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.contains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#contains-fffff04e)

```scala
def contains(index: List[Int]): Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` index: List[Int] ``.

Returns: `` Boolean ``.

Invocation template:

```scala
receiver.contains(index)
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.convertIndicesToValues ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#convertIndicesToValues-5c0)

```scala
def convertIndicesToValues(indices: List[Int]): List[Extended[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``.

Returns: `` List[Extended[_]] ``.

Invocation template:

```scala
receiver.convertIndicesToValues(indices)
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.foldLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#foldLeft-fffffe32)

```scala
def foldLeft(initial: T, fn: (T, T) => T): T
```

Fold the given function through the contents of the factor, beginning with the given initial values

Type parameters: none.

Parameters, list 1: `` initial: T ``; `` fn: (T, T) => T ``.

Returns: `` T ``.

Source contract/attributes: Fold the given function through the contents of the factor, beginning with the given initial values Attributes

Invocation template:

```scala
receiver.foldLeft(initial, fn)
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.generateIndices ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#generateIndices-0)

```scala
def generateIndices: Indices
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Indices ``.

Invocation template:

```scala
receiver.generateIndices
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.getIndices ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#getIndices-0)

```scala
def getIndices: Indices
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Indices ``.

Invocation template:

```scala
receiver.getIndices
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.hasStar ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#hasStar-0)

```scala
def hasStar: Boolean
```

Indicates if any of this Factor's variables has Star

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Indicates if any of this Factor's variables has Star Attributes

Invocation template:

```scala
receiver.hasStar
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.isEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#isEmpty-0)

```scala
def isEmpty: Boolean
```

Indicates if this Factor has any variables

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Indicates if this Factor has any variables Attributes

Invocation template:

```scala
receiver.isEmpty
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.marginalizeTo ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#marginalizeTo-3da)

```scala
def marginalizeTo(targets: Variable[_]*): Factor[T]
```

Returns the marginalization of the factor to a variable according to the addition function in this factor's semiring. This involves summing out all other variables.

Type parameters: none.

Parameters, list 1: `` targets: Variable[_]* ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the marginalization of the factor to a variable according to the addition function in this factor's semiring. This involves summing out all other variables. Attributes

Invocation template:

```scala
receiver.marginalizeTo(targets*)
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#toString-0)

```scala
override def toString: String
```

Description that includes the variable list and conditional probabilities

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Description that includes the variable list and conditional probabilities Attributes Definition Classes Any

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.algorithm.factored.factors.Factor.variables ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Factor.html#variables-0)

```scala
def variables: List[Variable[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Variable[_]] ``.

Invocation template:

```scala
receiver.variables
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.allIndices ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#allIndices-0)

```scala
def allIndices: List[List[Int]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[List[Int]] ``.

Invocation template:

```scala
receiver.allIndices
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.iterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#iterator-0)

```scala
def iterator: Iterator[List[Int]]
```

An scala.collection.Iterator over the elements of this iterable collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[List[Int]] ``.

Source contract/attributes: An scala.collection.Iterator over the elements of this iterable collection. If an IterableOnce object is in fact an scala.collection.Iterator, this method always returns itself, in its current state, but if it is an scala.collection.Iterable, this method always returns a new scala.collection.Iterator. Attributes Returns an iterator over all elements of this iterable collection

Invocation template:

```scala
receiver.iterator
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.nextIndices ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#nextIndices-fffff78f)

```scala
def nextIndices(indices: List[Int]): Option[List[Int]]
```

Given a list of indices corresponding to a row in the factor, returns the list of indices corresponding to the next row. Returns None if the last index list has been reached.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``.

Returns: `` Option[List[Int]] ``.

Source contract/attributes: Given a list of indices corresponding to a row in the factor, returns the list of indices corresponding to the next row. Returns None if the last index list has been reached. Attributes

Invocation template:

```scala
receiver.nextIndices(indices)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#++-1dd)

```scala
final def ++[B >: List[Int]](suffix: IterableOnce[B]): Iterable[B]
```

Alias for concat.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` suffix: IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Alias for concat. Type parameters B the element type of the returned collection Value parameters suffix the iterable to append to this iterable collection Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.++[B](suffix)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#addString-ff4)

```scala
final def addString(b: StringBuilder): b.type
```

Appends all elements of this iterable collection to a string builder. The written text consists of the string representations (w.r.t. the method toString) of all elements of this iterable collection without any separator string.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this iterable collection to a string builder. The written text consists of the string representations (w.r.t. the method toString) of all elements of this iterable collection without any separator string. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() val h = a.addString(b) // 1234 Value parameters b the string builder to which elements are appended. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#addString-e99)

```scala
final def addString(b: StringBuilder, sep: String): b.type
```

Appends all elements of this iterable collection to a string builder using a separator string. The written text consists of the string representations (w.r.t. the method toString) of all elements of this iterable collection, separated by the string sep.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``; `` sep: String ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this iterable collection to a string builder using a separator string. The written text consists of the string representations (w.r.t. the method toString) of all elements of this iterable collection, separated by the string sep. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() a.addString(b, ", ") // 1, 2, 3, 4 Value parameters b the string builder to which elements are appended. sep the separator string. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b, sep)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#addString-de3)

```scala
def addString(b: StringBuilder, start: String, sep: String, end: String): b.type
```

Appends all elements of this iterable collection to a string builder using start, end, and separator strings. The written text begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this iterable collection are separated by the string sep.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``; `` start: String ``; `` sep: String ``; `` end: String ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this iterable collection to a string builder using start, end, and separator strings. The written text begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this iterable collection are separated by the string sep. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() a.addString(b , "List(" , ", " , ")") // List(1, 2, 3, 4) Value parameters b the string builder to which elements are appended. end the ending string. sep the separator string. start the starting string. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b, start, sep, end)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.collect ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#collect-fffff5b3)

```scala
def collect[B](pf: PartialFunction[List[Int], B]): Iterable[B]
```

Builds a new iterable collection by applying a partial function to all elements of this iterable collection on which the function is defined.

Type parameters: `` B ``.

Parameters, list 1: `` pf: PartialFunction[List[Int], B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Builds a new iterable collection by applying a partial function to all elements of this iterable collection on which the function is defined. Type parameters B the element type of the returned iterable collection. Value parameters pf the partial function which filters and maps the iterable collection. Attributes Returns a new iterable collection resulting from applying the given partial function pf to each element on which it is defined and collecting the results. The order of the elements is preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.collect[B](pf)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.collectFirst ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#collectFirst-25d)

```scala
def collectFirst[B](pf: PartialFunction[List[Int], B]): Option[B]
```

Finds the first element of the iterable collection for which the given partial function is defined, and applies the partial function to it.

Type parameters: `` B ``.

Parameters, list 1: `` pf: PartialFunction[List[Int], B] ``.

Returns: `` Option[B] ``.

Source contract/attributes: Finds the first element of the iterable collection for which the given partial function is defined, and applies the partial function to it. Note: may not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Type parameters B the result type of the partial function Value parameters pf the partial function Attributes Returns an option value containing pf applied to the first value for which it is defined, or None if none exists. Example Seq("a", 1, 5L).collectFirst { case x: Int => x*10 } = Some(10) Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.collectFirst[B](pf)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.concat ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#concat-1dd)

```scala
def concat[B >: List[Int]](suffix: IterableOnce[B]): Iterable[B]
```

Returns a new iterable collection containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the iterable collection is the most specific superclass encompassing the element types of the two operands.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` suffix: IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Returns a new iterable collection containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the iterable collection is the most specific superclass encompassing the element types of the two operands. Type parameters B the element type of the returned collection. Value parameters suffix the iterable to append. Attributes Returns a new iterable collection which contains all elements of this iterable collection followed by all elements of suffix. Inherited from: IterableOps

Invocation template:

```scala
receiver.concat[B](suffix)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#copyToArray-fffffd03)

```scala
def copyToArray[B >: List[Int]](dest: Array[B], start: Int, n: Int): Int
```

Copies elements to an array and returns the number of elements written.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` dest: Array[B] ``; `` start: Int ``; `` n: Int ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array and returns the number of elements written. Fills the given array dest starting at index start with at most n elements of this iterable collection. Copying will stop once either all the elements of this iterable collection have been copied, or the end of the array is reached, or n elements have been copied. If start is less than zero, it is taken as zero. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. n the maximal number of elements to copy. start the starting index of xs. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest, start, n)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#copyToArray-fffffaa4)

```scala
def copyToArray[B >: List[Int]](dest: Array[B], start: Int): Int
```

Copies elements to an array, returning the number of elements written.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` dest: Array[B] ``; `` start: Int ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array, returning the number of elements written. Fills the given array dest starting at index start with values of this iterable collection. Copying will stop once either all the elements of this iterable collection have been copied, or the end of the array is reached. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. start the starting index of xs. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest, start)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#copyToArray-fffff3e3)

```scala
def copyToArray[B >: List[Int]](dest: Array[B]): Int
```

Copies elements to an array, returning the number of elements written.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` dest: Array[B] ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array, returning the number of elements written. Fills the given array dest starting at index start with values of this iterable collection. Copying will stop once either all the elements of this iterable collection have been copied, or the end of the array is reached. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.corresponds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#corresponds-958)

```scala
def corresponds[B](that: IterableOnce[B])(p: (List[Int], B) => Boolean): Boolean
```

Tests whether every element of this collection's iterator relates to the corresponding element of another collection by satisfying a test predicate.

Type parameters: `` B ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Parameters, list 2: `` p: (List[Int], B) => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether every element of this collection's iterator relates to the corresponding element of another collection by satisfying a test predicate. Note: will not terminate for infinite-sized collections. Type parameters B the type of the elements of that Value parameters p the test predicate, which relates elements from both collections that the other collection Attributes Returns true if both collections have the same length and p(x, y) is true for all corresponding elements x of this iterator and y of that, otherwise false Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.corresponds[B](that)(p)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.count ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#count-db6)

```scala
def count(p: List[Int] => Boolean): Int
```

Counts the number of elements in the iterable collection which satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` p: List[Int] => Boolean ``.

Returns: `` Int ``.

Source contract/attributes: Counts the number of elements in the iterable collection which satisfy a predicate. Note: will not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns the number of elements satisfying the predicate p. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.count(p)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.drop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#drop-aba)

```scala
def drop(n: Int): Iterable[List[Int]]
```

Selects all elements except the first n ones.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Selects all elements except the first n ones. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to drop from this iterable collection. Attributes Returns a iterable collection consisting of all elements of this iterable collection except the first n ones, or else the empty iterable collection, if this iterable collection has less than n elements. If n is negative, don't drop any elements. Inherited from: IterableOps

Invocation template:

```scala
receiver.drop(n)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.dropRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#dropRight-aba)

```scala
def dropRight(n: Int): Iterable[List[Int]]
```

Selects all elements except last n ones.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Selects all elements except last n ones. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to drop from this iterable collection. Attributes Returns a iterable collection consisting of all elements of this iterable collection except the last n ones, or else the empty iterable collection, if this iterable collection has less than n elements. If n is negative, don't drop any elements. Inherited from: IterableOps

Invocation template:

```scala
receiver.dropRight(n)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.dropWhile ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#dropWhile-c24)

```scala
def dropWhile(p: List[Int] => Boolean): Iterable[List[Int]]
```

Selects all elements except the longest prefix that satisfies a predicate.

Type parameters: none.

Parameters, list 1: `` p: List[Int] => Boolean ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Selects all elements except the longest prefix that satisfies a predicate. The matching prefix starts with the first element of this iterable collection, and the element following the prefix is the first element that does not satisfy the predicate. The matching prefix may be empty, so that this method returns the entire iterable collection. Example: List(1, 2, 3, 100, 4).dropWhile(n => n < 10) // List(100, 4) List(1, 2, 3, 100, 4).dropWhile(n => n == 0) // List(1, 2, 3, 100, 4) Use span to obtain both the prefix and suffix. Use filterNot to drop all elements that satisfy the predicate. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p The predicate used to test elements. Attributes Returns the longest suffix of this iterable collection whose first element does not satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.dropWhile(p)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.empty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#empty-0)

```scala
override def empty: Iterable[List[Int]]
```

The empty iterable collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: The empty iterable collection. Attributes Returns an empty iterable of type Iterable. Definition Classes IterableFactoryDefaults -> IterableOps Inherited from: IterableFactoryDefaults

Invocation template:

```scala
receiver.empty
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.exists ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#exists-fffff66f)

```scala
def exists(p: List[Int] => Boolean): Boolean
```

Tests whether a predicate holds for at least one element of this iterable collection.

Type parameters: none.

Parameters, list 1: `` p: List[Int] => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether a predicate holds for at least one element of this iterable collection. Note: may not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns true if the given predicate p is satisfied by at least one element of this iterable collection, otherwise false Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.exists(p)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.filter ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#filter-c24)

```scala
def filter(pred: List[Int] => Boolean): Iterable[List[Int]]
```

Selects all elements of this iterable collection which satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` pred: List[Int] => Boolean ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Selects all elements of this iterable collection which satisfy a predicate. Value parameters p the predicate used to test elements. Attributes Returns a new iterable collection consisting of all elements of this iterable collection that satisfy the given predicate p. The order of the elements is preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.filter(pred)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.filterNot ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#filterNot-c24)

```scala
def filterNot(pred: List[Int] => Boolean): Iterable[List[Int]]
```

Selects all elements of this iterable collection which do not satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` pred: List[Int] => Boolean ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Selects all elements of this iterable collection which do not satisfy a predicate. Value parameters pred the predicate used to test elements. Attributes Returns a new iterable collection consisting of all elements of this iterable collection that do not satisfy the given predicate pred. Their order may not be preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.filterNot(pred)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.find ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#find-94e)

```scala
def find(p: List[Int] => Boolean): Option[List[Int]]
```

Finds the first element of the iterable collection satisfying a predicate, if any.

Type parameters: none.

Parameters, list 1: `` p: List[Int] => Boolean ``.

Returns: `` Option[List[Int]] ``.

Source contract/attributes: Finds the first element of the iterable collection satisfying a predicate, if any. Note: may not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the predicate used to test elements. Attributes Returns an option value containing the first element in the iterable collection that satisfies p, or None if none exists. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.find(p)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#flatMap-5d3)

```scala
def flatMap[B](f: List[Int] => IterableOnce[B]): Iterable[B]
```

Builds a new iterable collection by applying a function to all elements of this iterable collection and using the elements of the resulting collections.

Type parameters: `` B ``.

Parameters, list 1: `` f: List[Int] => IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Builds a new iterable collection by applying a function to all elements of this iterable collection and using the elements of the resulting collections. For example: def getWords(lines: Seq[String]): Seq[String] = lines.flatMap(line => line.split("\\W+")) The type of the resulting collection is guided by the static type of this iterable collection. This might cause unexpected results sometimes. For example: // lettersOf will return a Seq[Char] of likely repeated letters, instead of a Set def lettersOf(words: Seq[String]) = words.flatMap(word => word.toSet) // lettersOf2 will return a Set[Char], not a Seq def lettersOf2(words: Seq[String]) = words.toSet.flatMap(word => word.toSeq) // xs will be an Iterable[Int] val xs = Map("a" -> List(11, 111), "b" -> List(22, 222)).flatMap(_._2) // ys will be a Map[Int, Int] val ys = Map("a" -> List(1 -> 11, 1 -> 111), "b" -> List(2 -> 22, 2 -> 222)).flatMap(_._2) Type parameters B the element type of the returned collection. Value parameters f the function to apply to each element. Attributes Returns a new iterable collection resulting from applying the given collection-valued function f to each element of this iterable collection and concatenating the results. Inherited from: IterableOps

Invocation template:

```scala
receiver.flatMap[B](f)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.flatten ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#flatten-5d3)

```scala
def flatten[B](implicit asIterable: List[Int] => IterableOnce[B]): Iterable[B]
```

Given that the elements of this collection are themselves iterable collections, converts this iterable collection into a iterable collection comprising the elements of these iterable collections.

Type parameters: `` B ``.

Context parameters, list 1: `` asIterable: List[Int] => IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Given that the elements of this collection are themselves iterable collections, converts this iterable collection into a iterable collection comprising the elements of these iterable collections. The resulting collection's type will be guided by the type of iterable collection. For example: val xs = List( Set(1, 2, 3), Set(1, 2, 3) ).flatten // xs == List(1, 2, 3, 1, 2, 3) val ys = Set( List(1, 2, 3), List(3, 2, 1) ).flatten // ys == Set(1, 2, 3) Type parameters B the type of the elements of each iterable collection. Value parameters asIterable an implicit conversion which asserts that the element type of this iterable collection is an Iterable. Attributes Returns a new iterable collection resulting from concatenating all element collections. Inherited from: IterableOps

Invocation template:

```scala
receiver.flatten[B](using asIterable)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.fold ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#fold-fffff9e1)

```scala
def fold[A1 >: List[Int]](z: A1)(op: (A1, A1) => A1): A1
```

Applies the given binary operator op to the given initial value z and all elements of this iterable collection.

Type parameters: `` A1 >: List[Int] ``.

Parameters, list 1: `` z: A1 ``.

Parameters, list 2: `` op: (A1, A1) => A1 ``.

Returns: `` A1 ``.

Source contract/attributes: Applies the given binary operator op to the given initial value z and all elements of this iterable collection. For each application of the operator, each operand is either an element of this iterable collection, the initial value, or another such application of the operator. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. The initial value may be used an arbitrary number of times, but at least once. If this collection is ordered, then for any application of the operator, the element(s) appearing in the left operand will precede those in the right. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. In either case, it is also necessary that the initial value be a neutral value for the operator, e.g. Nil for List concatenation or 1 for multiplication. The default implementation in IterableOnce is equivalent to foldLeft but may be overridden for more efficient traversal orders. Note: will not terminate for infinite-sized collections. Type parameters A1 The type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. z An initial value; may be used an arbitrary number of times in the computation of the result; must be a neutral value for op for the result to always be the same across runs. Attributes Returns The result of applying op between all the elements and z, or z if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.fold[A1](z)(op)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.foldLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#foldLeft-fffff9e1)

```scala
def foldLeft[B](z: B)(op: (B, List[Int]) => B): B
```

Applies the given binary operator op to the given initial value z and all elements of this iterable collection, going left to right. Returns the initial value if this iterable collection is empty.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, List[Int]) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to the given initial value z and all elements of this iterable collection, going left to right. Returns the initial value if this iterable collection is empty. "Going left to right" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this iterable collection, the result is op( op( ... op( op(z, x,,1,,), x,,2,,) ... ), x,,n,,). If this collection is not ordered, then for each application of the operator, each right operand is an element. In addition, the leftmost operand is the initial value, and each other left operand is itself an application of the operator. The elements of this iterable collection and the initial value all appear exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator. Value parameters op A binary operator. z An initial value. Attributes Returns The result of applying op to z and all elements of this iterable collection, going left to right. Returns z if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foldLeft[B](z)(op)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.foldRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#foldRight-fffff9e1)

```scala
def foldRight[B](z: B)(op: (List[Int], B) => B): B
```

Applies the given binary operator op to all elements of this iterable collection and the given initial value z, going right to left. Returns the initial value if this iterable collection is empty.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (List[Int], B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this iterable collection and the given initial value z, going right to left. Returns the initial value if this iterable collection is empty. "Going right to left" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this iterable collection, the result is op(x,,1,,, op(x,,2,,, op( ... op(x,,n,,, z) ... ))). If this collection is not ordered, then for each application of the operator, each left operand is an element. In addition, the rightmost operand is the initial value, and each other right operand is itself an application of the operator. The elements of this iterable collection and the initial value all appear exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator. Value parameters op A binary operator. z An initial value. Attributes Returns The result of applying op to all elements of this iterable collection and z, going right to left. Returns z if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foldRight[B](z)(op)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.forall ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#forall-fffff66f)

```scala
def forall(p: List[Int] => Boolean): Boolean
```

Tests whether a predicate holds for all elements of this iterable collection.

Type parameters: none.

Parameters, list 1: `` p: List[Int] => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether a predicate holds for all elements of this iterable collection. Note: may not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns true if this iterable collection is empty or the given predicate p holds for all elements of this iterable collection, otherwise false. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.forall(p)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.foreach ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#foreach-58c)

```scala
def foreach[U](f: List[Int] => U): Unit
```

Applies f to each element for its side effects. Note: U parameter needed to help scalac's type inference.

Type parameters: `` U ``.

Parameters, list 1: `` f: List[Int] => U ``.

Returns: `` Unit ``.

Source contract/attributes: Applies f to each element for its side effects. Note: U parameter needed to help scalac's type inference. Type parameters U the return type of f; the value is discarded, but the type parameter aids type inference Value parameters f the function to apply to each element for its side effects Attributes Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foreach[U](f)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.groupBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#groupBy-f68)

```scala
def groupBy[K](f: List[Int] => K): Map[K, Iterable[List[Int]]]
```

Partitions this iterable collection into a map of iterable collections according to some discriminator function.

Type parameters: `` K ``.

Parameters, list 1: `` f: List[Int] => K ``.

Returns: `` Map[K, Iterable[List[Int]]] ``.

Source contract/attributes: Partitions this iterable collection into a map of iterable collections according to some discriminator function. Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters K the type of keys returned by the discriminator function. Value parameters f the discriminator function. Attributes Returns A map from keys to iterable collections such that the following invariant holds: (xs groupBy f)(k) = xs filter (x => f(x) == k) That is, every key `k` is bound to a iterable collection of those elements `x` for which `f(x)` equals `k`. Inherited from: IterableOps

Invocation template:

```scala
receiver.groupBy[K](f)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.groupMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#groupMap-fffff03a)

```scala
def groupMap[K, B](key: List[Int] => K)(f: List[Int] => B): Map[K, Iterable[B]]
```

Partitions this iterable collection into a map of iterable collections according to a discriminator function key. Each element in a group is transformed into a value of type B using the value function.

Type parameters: `` K ``; `` B ``.

Parameters, list 1: `` key: List[Int] => K ``.

Parameters, list 2: `` f: List[Int] => B ``.

Returns: `` Map[K, Iterable[B]] ``.

Source contract/attributes: Partitions this iterable collection into a map of iterable collections according to a discriminator function key. Each element in a group is transformed into a value of type B using the value function. It is equivalent to groupBy(key).mapValues(_.map(f)), but more efficient. case class User(name: String, age: Int) def namesByAge(users: Seq[User]): Map[Int, Seq[String]] = users.groupMap(_.age)(_.name) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of values returned by the transformation function K the type of keys returned by the discriminator function Value parameters f the element transformation function key the discriminator function Attributes Returns a map associating each key k produced by key with a iterable collection of values produced by applying f to elements that map to k Inherited from: IterableOps

Invocation template:

```scala
receiver.groupMap[K, B](key)(f)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.groupMapReduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#groupMapReduce-8e8)

```scala
def groupMapReduce[K, B](key: List[Int] => K)(f: List[Int] => B)(reduce: (B, B) => B): Map[K, B]
```

Partitions this iterable collection into a map according to a discriminator function key. All the values that have the same discriminator are then transformed by the f function and then reduced into a single value with the reduce function.

Type parameters: `` K ``; `` B ``.

Parameters, list 1: `` key: List[Int] => K ``.

Parameters, list 2: `` f: List[Int] => B ``.

Parameters, list 3: `` reduce: (B, B) => B ``.

Returns: `` Map[K, B] ``.

Source contract/attributes: Partitions this iterable collection into a map according to a discriminator function key. All the values that have the same discriminator are then transformed by the f function and then reduced into a single value with the reduce function. It is equivalent to groupBy(key).mapValues(_.map(f).reduce(reduce)), but more efficient. def occurrences[A](as: Seq[A]): Map[A, Int] = as.groupMapReduce(identity)(_ => 1)(_ + _) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of values returned by the transformation function K the type of keys returned by the discriminator function Value parameters f the element transformation function key the discriminator function reduce the reduction function used to combine values mapped to the same key Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.groupMapReduce[K, B](key)(f)(reduce)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.grouped ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#grouped-fffffbef)

```scala
def grouped(size: Int): Iterator[Iterable[List[Int]]]
```

Partitions elements in fixed size iterable collections.

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Iterator[Iterable[List[Int]]] ``.

Source contract/attributes: Partitions elements in fixed size iterable collections. Value parameters size the number of elements per group Attributes Returns An iterator producing iterable collections of size size, except the last will be less than size size if the elements don't divide evenly. See also scala.collection.Iterator, method grouped Inherited from: IterableOps

Invocation template:

```scala
receiver.grouped(size)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.head ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#head-0)

```scala
def head: List[Int]
```

Selects the first element of this iterable collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Int] ``.

Source contract/attributes: Selects the first element of this iterable collection. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the first element of this iterable collection. Throws NoSuchElementException if the iterable collection is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.head
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.headOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#headOption-0)

```scala
def headOption: Option[List[Int]]
```

Optionally selects the first element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Option[List[Int]] ``.

Source contract/attributes: Optionally selects the first element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the first element of this iterable collection if it is nonempty, None if it is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.headOption
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.init ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#init-0)

```scala
def init: Iterable[List[Int]]
```

The initial part of the collection without its last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: The initial part of the collection without its last element. Note: Even when applied to a view or a lazy collection it will always force the elements. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.init
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.inits ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#inits-0)

```scala
def inits: Iterator[Iterable[List[Int]]]
```

Iterates over the inits of this iterable collection. The first value will be this iterable collection and the final one will be an empty iterable collection, with the intervening values the results of successive applications of init.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Iterable[List[Int]]] ``.

Source contract/attributes: Iterates over the inits of this iterable collection. The first value will be this iterable collection and the final one will be an empty iterable collection, with the intervening values the results of successive applications of init. Note: Even when applied to a view or a lazy collection it will always force the elements. Attributes Returns an iterator over all the inits of this iterable collection Example List(1,2,3).inits = Iterator(List(1,2,3), List(1,2), List(1), Nil) Inherited from: IterableOps

Invocation template:

```scala
receiver.inits
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.isEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#isEmpty-0)

```scala
def isEmpty: Boolean
```

Tests whether the iterable collection is empty.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether the iterable collection is empty. Note: The default implementation creates and discards an iterator. Note: Implementations in subclasses that are not repeatedly iterable must take care not to consume any elements when isEmpty is called. Attributes Returns true if the iterable collection contains no elements, false otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.isEmpty
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.isTraversableAgain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#isTraversableAgain-0)

```scala
override def isTraversableAgain: Boolean
```

Tests whether this iterable collection can be repeatedly traversed. Always true for Iterables and false for Iterators unless overridden.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this iterable collection can be repeatedly traversed. Always true for Iterables and false for Iterators unless overridden. Attributes Returns true if it is repeatedly traversable, false otherwise. Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.isTraversableAgain
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.iterableFactory ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#iterableFactory-0)

```scala
def iterableFactory: IterableFactory[Iterable]
```

The companion object of this iterable collection, providing various factory methods.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IterableFactory[Iterable] ``.

Source contract/attributes: The companion object of this iterable collection, providing various factory methods. Attributes Returns the companion factory object for this collection type Note When implementing a custom collection type and refining CC to the new type, this method needs to be overridden to return a factory for the new type (the compiler will issue an error otherwise). Inherited from: Iterable

Invocation template:

```scala
receiver.iterableFactory
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.knownSize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#knownSize-0)

```scala
def knownSize: Int
```

The number of elements in this iterable collection, if it can be cheaply computed, -1 otherwise. Cheaply usually means: Not requiring a collection traversal.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: The number of elements in this iterable collection, if it can be cheaply computed, -1 otherwise. Cheaply usually means: Not requiring a collection traversal. Attributes Inherited from: IterableOnce

Invocation template:

```scala
receiver.knownSize
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.last ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#last-0)

```scala
def last: List[Int]
```

Selects the last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Int] ``.

Source contract/attributes: Selects the last element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns The last element of this iterable collection. Throws NoSuchElementException If the iterable collection is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.last
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.lastOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#lastOption-0)

```scala
def lastOption: Option[List[Int]]
```

Optionally selects the last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Option[List[Int]] ``.

Source contract/attributes: Optionally selects the last element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the last element of this iterable collection if it is nonempty, None if it is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.lastOption
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.lazyZip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#lazyZip-fffffd84)

```scala
def lazyZip[B](that: Iterable[B]): LazyZip2[List[Int], B, this.type]
```

Analogous to zip except that the elements in each collection are not consumed until a strict operation is invoked on the returned LazyZip2 decorator.

Type parameters: `` B ``.

Parameters, list 1: `` that: Iterable[B] ``.

Returns: `` LazyZip2[List[Int], B, this.type] ``.

Source contract/attributes: Analogous to zip except that the elements in each collection are not consumed until a strict operation is invoked on the returned LazyZip2 decorator. Calls to lazyZip can be chained to support higher arities (up to 4) without incurring the expense of constructing and deconstructing intermediary tuples. val xs = List(1, 2, 3) val res = (xs lazyZip xs lazyZip xs lazyZip xs).map((a, b, c, d) => a + b + c + d) // res == List(4, 8, 12) Type parameters B the type of the second element in each eventual pair Value parameters that the iterable providing the second element of each eventual pair Attributes Returns a decorator LazyZip2 that allows strict operations to be performed on the lazily evaluated pairs or chained calls to lazyZip. Implicit conversion to Iterable[(A, B)] is also supported. Inherited from: Iterable

Invocation template:

```scala
receiver.lazyZip[B](that)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#map-5d3)

```scala
def map[B](f: List[Int] => B): Iterable[B]
```

Builds a new iterable collection by applying a function to all elements of this iterable collection.

Type parameters: `` B ``.

Parameters, list 1: `` f: List[Int] => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Builds a new iterable collection by applying a function to all elements of this iterable collection. Type parameters B the element type of the returned iterable collection . Value parameters f the function to apply to each element. Attributes Returns a new iterable collection resulting from applying the given function f to each element of this iterable collection and collecting the results. Inherited from: IterableOps

Invocation template:

```scala
receiver.map[B](f)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.max ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#max-ffffff34)

```scala
def max[B >: List[Int]](implicit ord: Ordering[B]): List[Int]
```

Finds the largest element.

Type parameters: `` B >: List[Int] ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` List[Int] ``.

Source contract/attributes: Finds the largest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns the largest element of this iterable collection with respect to the ordering ord. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.max[B](using ord)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.maxBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#maxBy-fffff0c5)

```scala
def maxBy[B](f: List[Int] => B)(implicit ord: Ordering[B]): List[Int]
```

Finds the first element which yields the largest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: List[Int] => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` List[Int] ``.

Source contract/attributes: Finds the first element which yields the largest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns the first element of this iterable collection with the largest value measured by function f with respect to the ordering ord. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxBy[B](f)(using ord)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.maxByOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#maxByOption-fffff66f)

```scala
def maxByOption[B](f: List[Int] => B)(implicit ord: Ordering[B]): Option[List[Int]]
```

Finds the first element which yields the largest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: List[Int] => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` Option[List[Int]] ``.

Source contract/attributes: Finds the first element which yields the largest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns an option value containing the first element of this iterable collection with the largest value measured by function f with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxByOption[B](f)(using ord)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.maxOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#maxOption-fffff45e)

```scala
def maxOption[B >: List[Int]](implicit ord: Ordering[B]): Option[List[Int]]
```

Finds the largest element.

Type parameters: `` B >: List[Int] ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` Option[List[Int]] ``.

Source contract/attributes: Finds the largest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns an option value containing the largest element of this iterable collection with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxOption[B](using ord)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.min ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#min-ffffff34)

```scala
def min[B >: List[Int]](implicit ord: Ordering[B]): List[Int]
```

Finds the smallest element.

Type parameters: `` B >: List[Int] ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` List[Int] ``.

Source contract/attributes: Finds the smallest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns the smallest element of this iterable collection with respect to the ordering ord. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.min[B](using ord)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.minBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#minBy-fffff0c5)

```scala
def minBy[B](f: List[Int] => B)(implicit ord: Ordering[B]): List[Int]
```

Finds the first element which yields the smallest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: List[Int] => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` List[Int] ``.

Source contract/attributes: Finds the first element which yields the smallest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns the first element of this iterable collection with the smallest value measured by function f with respect to the ordering ord. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minBy[B](f)(using ord)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.minByOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#minByOption-fffff66f)

```scala
def minByOption[B](f: List[Int] => B)(implicit ord: Ordering[B]): Option[List[Int]]
```

Finds the first element which yields the smallest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: List[Int] => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` Option[List[Int]] ``.

Source contract/attributes: Finds the first element which yields the smallest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns an option value containing the first element of this iterable collection with the smallest value measured by function f with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minByOption[B](f)(using ord)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.minOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#minOption-fffff45e)

```scala
def minOption[B >: List[Int]](implicit ord: Ordering[B]): Option[List[Int]]
```

Finds the smallest element.

Type parameters: `` B >: List[Int] ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` Option[List[Int]] ``.

Source contract/attributes: Finds the smallest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns an option value containing the smallest element of this iterable collection with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minOption[B](using ord)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#mkString-0)

```scala
final def mkString: String
```

Displays all elements of this iterable collection in a string.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Displays all elements of this iterable collection in a string. Delegates to addString, which can be overridden. Attributes Returns a string representation of this iterable collection. In the resulting string the string representations (w.r.t. the method toString) of all elements of this iterable collection follow each other without any separator string. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#mkString-fffff2ca)

```scala
final def mkString(sep: String): String
```

Displays all elements of this iterable collection in a string using a separator string.

Type parameters: none.

Parameters, list 1: `` sep: String ``.

Returns: `` String ``.

Source contract/attributes: Displays all elements of this iterable collection in a string using a separator string. Delegates to addString, which can be overridden. Value parameters sep the separator string. Attributes Returns a string representation of this iterable collection. In the resulting string the string representations (w.r.t. the method toString) of all elements of this iterable collection are separated by the string sep. Example List(1, 2, 3).mkString("|") = "1|2|3" Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString(sep)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#mkString-d94)

```scala
final def mkString(start: String, sep: String, end: String): String
```

Displays all elements of this iterable collection in a string using start, end, and separator strings.

Type parameters: none.

Parameters, list 1: `` start: String ``; `` sep: String ``; `` end: String ``.

Returns: `` String ``.

Source contract/attributes: Displays all elements of this iterable collection in a string using start, end, and separator strings. Delegates to addString, which can be overridden. Value parameters end the ending string. sep the separator string. start the starting string. Attributes Returns a string representation of this iterable collection. The resulting string begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this iterable collection are separated by the string sep. Example List(1, 2, 3).mkString("(", "; ", ")") = "(1; 2; 3)" Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString(start, sep, end)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.nonEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#nonEmpty-0)

```scala
def nonEmpty: Boolean
```

Tests whether the iterable collection is not empty.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether the iterable collection is not empty. Attributes Returns true if the iterable collection contains at least one element, false otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.nonEmpty
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.partition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#partition-243)

```scala
def partition(p: List[Int] => Boolean): (Iterable[List[Int]], Iterable[List[Int]])
```

A pair of, first, all elements that satisfy predicate p and, second, all elements that do not.

Type parameters: none.

Parameters, list 1: `` p: List[Int] => Boolean ``.

Returns: `` (Iterable[List[Int]], Iterable[List[Int]]) ``.

Source contract/attributes: A pair of, first, all elements that satisfy predicate p and, second, all elements that do not. The two iterable collection correspond to the result of filter and filterNot, respectively. The default implementation provided here needs to traverse the collection twice. Strict collections have an overridden version of partition in StrictOptimizedIterableOps, which requires only a single traversal. Value parameters p the predicate used to test elements Attributes Returns a pair of iterable collections: the first containing all elements that satisfy p, the second containing those that do not Inherited from: IterableOps

Invocation template:

```scala
receiver.partition(p)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.partitionMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#partitionMap-2d1)

```scala
def partitionMap[A1, A2](f: List[Int] => Either[A1, A2]): (Iterable[A1], Iterable[A2])
```

Applies a function f to each element of the iterable collection and returns a pair of iterable collections: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right.

Type parameters: `` A1 ``; `` A2 ``.

Parameters, list 1: `` f: List[Int] => Either[A1, A2] ``.

Returns: `` (Iterable[A1], Iterable[A2]) ``.

Source contract/attributes: Applies a function f to each element of the iterable collection and returns a pair of iterable collections: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right. Example: val xs = $Coll(1, "one", 2, "two", 3, "three") partitionMap { case i: Int => Left(i) case s: String => Right(s) } // xs == ($Coll(1, 2, 3), // $Coll(one, two, three)) Type parameters A1 the element type of the first resulting collection A2 the element type of the second resulting collection Value parameters f the 'split function' mapping the elements of this iterable collection to an scala.util.Either Attributes Returns a pair of iterable collections: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right. Inherited from: IterableOps

Invocation template:

```scala
receiver.partitionMap[A1, A2](f)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#product-533)

```scala
def product[B >: List[Int]](implicit num: Numeric[B]): B
```

Multiplies together the elements of this collection.

Type parameters: `` B >: List[Int] ``.

Context parameters, list 1: `` num: Numeric[B] ``.

Returns: `` B ``.

Source contract/attributes: Multiplies together the elements of this collection. The default implementation uses reduce for a known non-empty collection, foldLeft otherwise. Note: will not terminate for infinite-sized collections. Type parameters B the result type of the * operator. Value parameters num an implicit parameter defining a set of numeric operations which includes the * operator to be used in forming the product. Attributes Returns the product of all elements of this iterable collection with respect to the * operator in num. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.product[B](using num)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.reduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#reduce-fffff3d4)

```scala
def reduce[B >: List[Int]](op: (B, B) => B): B
```

Applies the given binary operator op to all elements of this iterable collection.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` op: (B, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this iterable collection. For each application of the operator, each operand is either an element of this iterable collection or another such application of the operator. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. If this collection is ordered, then for any application of the operator, the element(s) appearing in the left operand will precede those in the right. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. Attributes Returns The result of applying op between all the elements if the iterable collection is nonempty. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduce[B](op)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.reduceLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#reduceLeft-fffff3d4)

```scala
def reduceLeft[B >: List[Int]](op: (B, List[Int]) => B): B
```

Applies the given binary operator op to all elements of this iterable collection, going left to right.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` op: (B, List[Int]) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this iterable collection, going left to right. "Going left to right" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this iterable collection, the result is op( op( op( ... op(x,,1,,, x,,2,,) ... ), x,,n-1,,), x,,n,,). If this collection is not ordered, then for each application of the operator, each right operand is an element. In addition, the leftmost operand is the first element of this iterable collection and each other left operand is itself an application of the operator. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of applying op to all elements of this iterable collection, going left to right. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceLeft[B](op)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.reduceLeftOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#reduceLeftOption-8fe)

```scala
def reduceLeftOption[B >: List[Int]](op: (B, List[Int]) => B): Option[B]
```

If this iterable collection is nonempty, reduces it with the given binary operator op, going left to right.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` op: (B, List[Int]) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this iterable collection is nonempty, reduces it with the given binary operator op, going left to right. The behavior is the same as reduceLeft except that the value is None if the iterable collection is empty. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of reducing this iterable collection with op going left to right if the iterable collection is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceLeftOption[B](op)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.reduceOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#reduceOption-8fe)

```scala
def reduceOption[B >: List[Int]](op: (B, B) => B): Option[B]
```

If this iterable collection is nonempty, reduces it with the given binary operator op.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` op: (B, B) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this iterable collection is nonempty, reduces it with the given binary operator op. The behavior is the same as reduce except that the value is None if the iterable collection is empty. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B A type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. Attributes Returns The result of reducing this iterable collection with op if the iterable collection is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceOption[B](op)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.reduceRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#reduceRight-fffff3d4)

```scala
def reduceRight[B >: List[Int]](op: (List[Int], B) => B): B
```

Applies the given binary operator op to all elements of this iterable collection, going right to left.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` op: (List[Int], B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this iterable collection, going right to left. "Going right to left" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this iterable collection, the result is op(x,,1,,, op(x,,2,,, op( ... op(x,,n-1,,, x,,n,,) ... ))). If this collection is not ordered, then for each application of the operator, each left operand is an element. In addition, the rightmost operand is the last element of this iterable collection and each other right operand is itself an application of the operator. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of applying op to all elements of this iterable collection, going right to left. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceRight[B](op)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.reduceRightOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#reduceRightOption-8fe)

```scala
def reduceRightOption[B >: List[Int]](op: (List[Int], B) => B): Option[B]
```

If this iterable collection is nonempty, reduces it with the given binary operator op, going right to left.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` op: (List[Int], B) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this iterable collection is nonempty, reduces it with the given binary operator op, going right to left. The behavior is the same as reduceRight except that the value is None if the iterable collection is empty. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of reducing this iterable collection with op going right to left if the iterable collection is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceRightOption[B](op)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.scan ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#scan-fffff9e1)

```scala
def scan[B >: List[Int]](z: B)(op: (B, B) => B): Iterable[B]
```

Computes a prefix scan of the elements of the collection.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, B) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Computes a prefix scan of the elements of the collection. Note: The neutral element z may be applied more than once. Type parameters B element type of the resulting collection Value parameters op the associative operator for the scan z neutral element for the operator op Attributes Returns a new iterable collection containing the prefix scan of the elements in this iterable collection Inherited from: IterableOps

Invocation template:

```scala
receiver.scan[B](z)(op)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.scanLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#scanLeft-fffff9e1)

```scala
def scanLeft[B](z: B)(op: (B, List[Int]) => B): Iterable[B]
```

Produces a iterable collection containing cumulative results of applying the operator going left to right, including the initial value.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, List[Int]) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Produces a iterable collection containing cumulative results of applying the operator going left to right, including the initial value. Note: will not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Type parameters B the type of the elements in the resulting collection Value parameters op the binary operator applied to the intermediate result and the element z the initial value Attributes Returns collection with intermediate results Inherited from: IterableOps

Invocation template:

```scala
receiver.scanLeft[B](z)(op)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.scanRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#scanRight-fffff9e1)

```scala
def scanRight[B](z: B)(op: (List[Int], B) => B): Iterable[B]
```

Produces a collection containing cumulative results of applying the operator going right to left. The head of the collection is the last cumulative result.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (List[Int], B) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Produces a collection containing cumulative results of applying the operator going right to left. The head of the collection is the last cumulative result. Note: will not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: Even when applied to a view or a lazy collection it will always force the elements. Example: List(1, 2, 3, 4).scanRight(0)(_ + _) == List(10, 9, 7, 4, 0) Type parameters B the type of the elements in the resulting collection Value parameters op the binary operator applied to the intermediate result and the element z the initial value Attributes Returns collection with intermediate results Inherited from: IterableOps

Invocation template:

```scala
receiver.scanRight[B](z)(op)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.size ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#size-0)

```scala
def size: Int
```

The size of this iterable collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: The size of this iterable collection. Note: will not terminate for infinite-sized collections. Attributes Returns the number of elements in this iterable collection. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.size
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.sizeCompare ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#sizeCompare-32d)

```scala
def sizeCompare(that: Iterable[_]): Int
```

Compares the size of this iterable collection to the size of another Iterable.

Type parameters: none.

Parameters, list 1: `` that: Iterable[_] ``.

Returns: `` Int ``.

Source contract/attributes: Compares the size of this iterable collection to the size of another Iterable. Value parameters that the Iterable whose size is compared with this iterable collection's size. Attributes Returns A value x where x < 0 if this.size < that.size x == 0 if this.size == that.size x > 0 if this.size > that.size The method as implemented here does not call size directly; its running time is O(this.size min that.size) instead of O(this.size + that.size). The method should be overridden if computing size is cheap and knownSize returns -1. Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeCompare(that)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.sizeCompare ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#sizeCompare-fffffbe0)

```scala
def sizeCompare(otherSize: Int): Int
```

Compares the size of this iterable collection to a test value.

Type parameters: none.

Parameters, list 1: `` otherSize: Int ``.

Returns: `` Int ``.

Source contract/attributes: Compares the size of this iterable collection to a test value. Value parameters otherSize the test value that gets compared with the size. Attributes Returns A value x where x < 0 if this.size < otherSize x == 0 if this.size == otherSize x > 0 if this.size > otherSize The default implementation does not call size, which may be arbitrarily expensive. It will use knownSize if nonnegative. Otherwise, it will iterate to compute an element count, with complexity O(size min otherSize). This method should be overridden if computing size is cheap and knownSize returns -1. In that case, the custom implementation should just compare size and otherSize directly. See also sizeIs Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeCompare(otherSize)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.sizeIs ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#sizeIs-0)

```scala
final def sizeIs: SizeCompareOps
```

Returns a value class containing operations for comparing the size of this iterable collection to a test value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` SizeCompareOps ``.

Source contract/attributes: Returns a value class containing operations for comparing the size of this iterable collection to a test value. These operations are implemented in terms of `sizeCompare(Int)`, and allow the following more readable usages: this.sizeIs < size // this.sizeCompare(size) < 0 this.sizeIs <= size // this.sizeCompare(size) <= 0 this.sizeIs == size // this.sizeCompare(size) == 0 this.sizeIs != size // this.sizeCompare(size) != 0 this.sizeIs >= size // this.sizeCompare(size) >= 0 this.sizeIs > size // this.sizeCompare(size) > 0 Attributes Returns a wrapper that allows size comparisons using operators such as =, and > Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeIs
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.slice ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#slice-573)

```scala
def slice(from: Int, until: Int): Iterable[List[Int]]
```

Selects an interval of elements. The returned iterable collection is made up of all elements x which satisfy the invariant:

Type parameters: none.

Parameters, list 1: `` from: Int ``; `` until: Int ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Selects an interval of elements. The returned iterable collection is made up of all elements x which satisfy the invariant: from <= indexOf(x) < until Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters from the lowest index to include from this iterable collection. until the lowest index to EXCLUDE from this iterable collection. Attributes Returns a iterable collection containing the elements greater than or equal to index from extending up to (but not including) index until of this iterable collection. Example List('a', 'b', 'c', 'd', 'e').slice(1, 3) == List('b', 'c') Inherited from: IterableOps

Invocation template:

```scala
receiver.slice(from, until)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.sliding ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#sliding-fffff156)

```scala
def sliding(size: Int, step: Int): Iterator[Iterable[List[Int]]]
```

Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped).

Type parameters: none.

Parameters, list 1: `` size: Int ``; `` step: Int ``.

Returns: `` Iterator[Iterable[List[Int]]] ``.

Source contract/attributes: Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped). The returned iterator will be empty when called on an empty collection. The last element the iterator produces may be smaller than the window size when the original collection isn't exhausted by the window before it and its last element isn't skipped by the step before it. Value parameters size the number of elements per group step the distance between the first elements of successive groups Attributes Returns An iterator producing iterable collections of size size, except the last element (which may be the only element) will be smaller if there are fewer than size elements remaining to be grouped. See also scala.collection.Iterator, method sliding Example List(1, 2, 3, 4, 5).sliding(2, 2) = Iterator(List(1, 2), List(3, 4), List(5)) List(1, 2, 3, 4, 5, 6).sliding(2, 3) = Iterator(List(1, 2), List(4, 5)) Inherited from: IterableOps

Invocation template:

```scala
receiver.sliding(size, step)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.sliding ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#sliding-fffffbef)

```scala
def sliding(size: Int): Iterator[Iterable[List[Int]]]
```

Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped).

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Iterator[Iterable[List[Int]]] ``.

Source contract/attributes: Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped). An empty collection returns an empty iterator, and a non-empty collection containing fewer elements than the window size returns an iterator that will produce the original collection as its only element. Value parameters size the number of elements per group Attributes Returns An iterator producing iterable collections of size size, except for a non-empty collection with less than size elements, which returns an iterator that produces the source collection itself as its only element. See also scala.collection.Iterator, method sliding Example List().sliding(2) = empty iterator List(1).sliding(2) = Iterator(List(1)) List(1, 2).sliding(2) = Iterator(List(1, 2)) List(1, 2, 3).sliding(2) = Iterator(List(1, 2), List(2, 3)) Inherited from: IterableOps

Invocation template:

```scala
receiver.sliding(size)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.span ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#span-243)

```scala
def span(p: List[Int] => Boolean): (Iterable[List[Int]], Iterable[List[Int]])
```

Splits this iterable collection into a prefix/suffix pair according to a predicate.

Type parameters: none.

Parameters, list 1: `` p: List[Int] => Boolean ``.

Returns: `` (Iterable[List[Int]], Iterable[List[Int]]) ``.

Source contract/attributes: Splits this iterable collection into a prefix/suffix pair according to a predicate. Note: c span p is equivalent to (but possibly more efficient than) (c takeWhile p, c dropWhile p), provided the evaluation of the predicate p does not cause any side effects. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the test predicate Attributes Returns a pair consisting of the longest prefix of this iterable collection whose elements all satisfy p, and the rest of this iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.span(p)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.splitAt ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#splitAt-fffffbd9)

```scala
override def splitAt(n: Int): (Iterable[List[Int]], Iterable[List[Int]])
```

Splits this iterable collection into a prefix/suffix pair at a given position.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` (Iterable[List[Int]], Iterable[List[Int]]) ``.

Source contract/attributes: Splits this iterable collection into a prefix/suffix pair at a given position. Note: c splitAt n is equivalent to (but possibly more efficient than) (c take n, c drop n). Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the position at which to split. Attributes Returns a pair of iterable collections consisting of the first n elements of this iterable collection, and the other elements. Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.splitAt(n)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.stepper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#stepper-190)

```scala
def stepper[S <: Stepper[_]](implicit shape: StepperShape[List[Int], S]): S
```

Returns a scala.collection.Stepper for the elements of this collection.

Type parameters: `` S <: Stepper[_] ``.

Context parameters, list 1: `` shape: StepperShape[List[Int], S] ``.

Returns: `` S ``.

Source contract/attributes: Returns a scala.collection.Stepper for the elements of this collection. The Stepper enables creating a Java stream to operate on the collection, see scala.jdk.StreamConverters. For collections holding primitive values, the Stepper can be used as an iterator which doesn't box the elements. The implicit scala.collection.StepperShape parameter defines the resulting Stepper type according to the element type of this collection. For collections of Int, Short, Byte or Char, an scala.collection.IntStepper is returned For collections of Double or Float, a scala.collection.DoubleStepper is returned For collections of Long a scala.collection.LongStepper is returned For any other element type, an scala.collection.AnyStepper is returned Note that this method is overridden in subclasses and the return type is refined to S with EfficientSplit, for example scala.collection.IndexedSeqOps.stepper. For Steppers marked with scala.collection.Stepper.EfficientSplit, the converters in scala.jdk.StreamConverters allow creating parallel streams, whereas bare Steppers can be converted only to sequential streams. Type parameters S the type of the returned Stepper, determined by the implicit StepperShape Attributes Inherited from: IterableOnce

Invocation template:

```scala
receiver.stepper[S](using shape)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.sum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#sum-533)

```scala
def sum[B >: List[Int]](implicit num: Numeric[B]): B
```

Sums the elements of this collection.

Type parameters: `` B >: List[Int] ``.

Context parameters, list 1: `` num: Numeric[B] ``.

Returns: `` B ``.

Source contract/attributes: Sums the elements of this collection. The default implementation uses reduce for a known non-empty collection, foldLeft otherwise. Note: will not terminate for infinite-sized collections. Type parameters B the result type of the + operator. Value parameters num an implicit parameter defining a set of numeric operations which includes the + operator to be used in forming the sum. Attributes Returns the sum of all elements of this iterable collection with respect to the + operator in num. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.sum[B](using num)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.tail ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#tail-0)

```scala
def tail: Iterable[List[Int]]
```

The rest of the collection without its first element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: The rest of the collection without its first element. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.tail
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.tails ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#tails-0)

```scala
def tails: Iterator[Iterable[List[Int]]]
```

Iterates over the tails of this iterable collection. The first value will be this iterable collection and the final one will be an empty iterable collection, with the intervening values the results of successive applications of tail.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Iterable[List[Int]]] ``.

Source contract/attributes: Iterates over the tails of this iterable collection. The first value will be this iterable collection and the final one will be an empty iterable collection, with the intervening values the results of successive applications of tail. Attributes Returns an iterator over all the tails of this iterable collection Example List(1,2,3).tails = Iterator(List(1,2,3), List(2,3), List(3), Nil) Inherited from: IterableOps

Invocation template:

```scala
receiver.tails
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.take ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#take-aba)

```scala
def take(n: Int): Iterable[List[Int]]
```

Selects the first n elements.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Selects the first n elements. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to take from this iterable collection. Attributes Returns a iterable collection consisting only of the first n elements of this iterable collection, or else the whole iterable collection, if it has less than n elements. If n is negative, returns an empty iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.take(n)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.takeRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#takeRight-aba)

```scala
def takeRight(n: Int): Iterable[List[Int]]
```

Selects the last n elements.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Selects the last n elements. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to take from this iterable collection. Attributes Returns a iterable collection consisting only of the last n elements of this iterable collection, or else the whole iterable collection, if it has less than n elements. If n is negative, returns an empty iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.takeRight(n)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.takeWhile ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#takeWhile-c24)

```scala
def takeWhile(p: List[Int] => Boolean): Iterable[List[Int]]
```

Takes longest prefix of elements that satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` p: List[Int] => Boolean ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Takes longest prefix of elements that satisfy a predicate. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p The predicate used to test elements. Attributes Returns the longest prefix of this iterable collection whose elements all satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.takeWhile(p)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.tapEach ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#tapEach-5d3)

```scala
override def tapEach[U](f: List[Int] => U): Iterable[List[Int]]
```

Applies a side-effecting function to each element in this collection. Strict collections will apply f to their elements immediately, while lazy collections like Views and LazyLists will only apply f on each element if and when that element is evaluated, and each time that element is evaluated.

Type parameters: `` U ``.

Parameters, list 1: `` f: List[Int] => U ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Applies a side-effecting function to each element in this collection. Strict collections will apply f to their elements immediately, while lazy collections like Views and LazyLists will only apply f on each element if and when that element is evaluated, and each time that element is evaluated. Type parameters U the return type of f Value parameters f a function to apply to each element in this iterable collection Attributes Returns The same logical collection as this Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.tapEach[U](f)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.to ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#to-fffff006)

```scala
def to[C1](factory: Factory[List[Int], C1]): C1
```

Given a collection factory factory, converts this iterable collection to the appropriate representation for the current element type A. Example uses:

Type parameters: `` C1 ``.

Parameters, list 1: `` factory: Factory[List[Int], C1] ``.

Returns: `` C1 ``.

Source contract/attributes: Given a collection factory factory, converts this iterable collection to the appropriate representation for the current element type A. Example uses: import scala.collection.mutable.ArrayBuffer import scala.collection.immutable.BitSet val xs: Iterable[Int] = Seq(1, 2, 3, 4, 5) xs.to(List) xs.to(ArrayBuffer) xs.to(BitSet) // for xs: Iterable[Int] Type parameters C1 the target collection type Value parameters factory the factory for the target collection type Attributes Returns a new collection of type C1 containing all elements of this iterable collection Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.to[C1](factory)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.toArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#toArray-fffff5df)

```scala
def toArray[B >: List[Int] : ClassTag]: Array[B]
```

Converts this iterable collection to an Array.

Type parameters: `` B >: List[Int] : ClassTag ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Array[B] ``.

Source contract/attributes: Converts this iterable collection to an Array. Implementation note: DO NOT call Array.from from this method. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This iterable collection as an Array[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toArray[B]
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.toBuffer ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#toBuffer-fffff87f)

```scala
final def toBuffer[B >: List[Int]]: Buffer[B]
```

Converts this iterable collection to a Buffer.

Type parameters: `` B >: List[Int] ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Buffer[B] ``.

Source contract/attributes: Converts this iterable collection to a Buffer. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This iterable collection as a Buffer[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toBuffer[B]
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.toIndexedSeq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#toIndexedSeq-0)

```scala
def toIndexedSeq: IndexedSeq[List[Int]]
```

Converts this iterable collection to an IndexedSeq.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IndexedSeq[List[Int]] ``.

Source contract/attributes: Converts this iterable collection to an IndexedSeq. Attributes Returns This iterable collection as an IndexedSeq[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toIndexedSeq
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.toList ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#toList-0)

```scala
def toList: List[List[Int]]
```

Converts this iterable collection to a List.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[List[Int]] ``.

Source contract/attributes: Converts this iterable collection to a List. Attributes Returns This iterable collection as a List[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toList
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.toMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#toMap-c0c)

```scala
def toMap[K, V](implicit ev: List[Int] <:< (K, V)): Map[K, V]
```

Converts this iterable collection to a Map, given an implicit coercion from the iterable collection's type to a key-value tuple.

Type parameters: `` K ``; `` V ``.

Context parameters, list 1: `` ev: List[Int] <:< (K, V) ``.

Returns: `` Map[K, V] ``.

Source contract/attributes: Converts this iterable collection to a Map, given an implicit coercion from the iterable collection's type to a key-value tuple. Type parameters K The key type for the resulting map. V The value type for the resulting map. Value parameters ev an implicit evidence that A is a subtype of (K, V) Attributes Returns This iterable collection as a Map[K, V]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toMap[K, V](using ev)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.toSeq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#toSeq-0)

```scala
def toSeq: Seq[List[Int]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Seq[List[Int]] ``.

Source contract/attributes: Attributes Returns This iterable collection as a Seq[A]. This is equivalent to to(Seq) but might be faster. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toSeq
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.toSet ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#toSet-fffffe9f)

```scala
def toSet[B >: List[Int]]: Set[B]
```

Converts this iterable collection to a Set.

Type parameters: `` B >: List[Int] ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[B] ``.

Source contract/attributes: Converts this iterable collection to a Set. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This iterable collection as a Set[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toSet[B]
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#toString-665)

```scala
override def toString(): String
```

Converts this iterable collection to a string.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Converts this iterable collection to a string. Attributes Returns a string representation of this collection. By default this string consists of the className of this iterable collection, followed by all elements separated by commas and enclosed in parentheses. Definition Classes Iterable -> Any Inherited from: Iterable

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.toVector ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#toVector-0)

```scala
def toVector: Vector[List[Int]]
```

Converts this iterable collection to a Vector.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Vector[List[Int]] ``.

Source contract/attributes: Converts this iterable collection to a Vector. Attributes Returns This iterable collection as a Vector[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toVector
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.transpose ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#transpose-5d3)

```scala
def transpose[B](implicit asIterable: List[Int] => Iterable[B]): Iterable[Iterable[B]]
```

Transposes this iterable collection of iterable collections into a iterable collection of iterable collections.

Type parameters: `` B ``.

Context parameters, list 1: `` asIterable: List[Int] => Iterable[B] ``.

Returns: `` Iterable[Iterable[B]] ``.

Source contract/attributes: Transposes this iterable collection of iterable collections into a iterable collection of iterable collections. The resulting collection's type will be guided by the static type of iterable collection. For example: val xs = List( Set(1, 2, 3), Set(4, 5, 6)).transpose // xs == List( // List(1, 4), // List(2, 5), // List(3, 6)) val ys = Vector( List(1, 2, 3), List(4, 5, 6)).transpose // ys == Vector( // Vector(1, 4), // Vector(2, 5), // Vector(3, 6)) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of the elements of each iterable collection. Value parameters asIterable an implicit conversion which asserts that the element type of this iterable collection is an Iterable. Attributes Returns a two-dimensional iterable collection of iterable collections which has as nth row the nth column of this iterable collection. Throws IllegalArgumentException if all collections in this iterable collection are not of the same size. Inherited from: IterableOps

Invocation template:

```scala
receiver.transpose[B](using asIterable)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.unzip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#unzip-2d1)

```scala
def unzip[A1, A2](implicit asPair: List[Int] => (A1, A2)): (Iterable[A1], Iterable[A2])
```

Converts this iterable collection of pairs into two collections of the first and second half of each pair.

Type parameters: `` A1 ``; `` A2 ``.

Context parameters, list 1: `` asPair: List[Int] => (A1, A2) ``.

Returns: `` (Iterable[A1], Iterable[A2]) ``.

Source contract/attributes: Converts this iterable collection of pairs into two collections of the first and second half of each pair. val xs = $Coll( (1, "one"), (2, "two"), (3, "three")).unzip // xs == ($Coll(1, 2, 3), // $Coll(one, two, three)) Type parameters A1 the type of the first half of the element pairs A2 the type of the second half of the element pairs Value parameters asPair an implicit conversion which asserts that the element type of this iterable collection is a pair. Attributes Returns a pair of iterable collections, containing the first, respectively second half of each element pair of this iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.unzip[A1, A2](using asPair)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.unzip3 ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#unzip3-a31)

```scala
def unzip3[A1, A2, A3](implicit asTriple: List[Int] => (A1, A2, A3)): (Iterable[A1], Iterable[A2], Iterable[A3])
```

Converts this iterable collection of triples into three collections of the first, second, and third element of each triple.

Type parameters: `` A1 ``; `` A2 ``; `` A3 ``.

Context parameters, list 1: `` asTriple: List[Int] => (A1, A2, A3) ``.

Returns: `` (Iterable[A1], Iterable[A2], Iterable[A3]) ``.

Source contract/attributes: Converts this iterable collection of triples into three collections of the first, second, and third element of each triple. val xs = $Coll( (1, "one", '1'), (2, "two", '2'), (3, "three", '3')).unzip3 // xs == ($Coll(1, 2, 3), // $Coll(one, two, three), // $Coll(1, 2, 3)) Type parameters A1 the type of the first member of the element triples A2 the type of the second member of the element triples A3 the type of the third member of the element triples Value parameters asTriple an implicit conversion which asserts that the element type of this iterable collection is a triple. Attributes Returns a triple of iterable collections, containing the first, second, respectively third member of each element triple of this iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.unzip3[A1, A2, A3](using asTriple)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.view ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#view-0)

```scala
def view: View[List[Int]]
```

A view over the elements of this collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` View[List[Int]] ``.

Source contract/attributes: A view over the elements of this collection. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.view
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.withFilter ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#withFilter-fffffb75)

```scala
def withFilter(p: List[Int] => Boolean): WithFilter[List[Int], Iterable]
```

Creates a non-strict filter of this iterable collection.

Type parameters: none.

Parameters, list 1: `` p: List[Int] => Boolean ``.

Returns: `` WithFilter[List[Int], Iterable] ``.

Source contract/attributes: Creates a non-strict filter of this iterable collection. Note: the difference between c filter p and c withFilter p is that the former creates a new collection, whereas the latter only restricts the domain of subsequent map, flatMap, foreach, and withFilter operations. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the predicate used to test elements. Attributes Returns an object of class WithFilter, which supports map, flatMap, foreach, and withFilter operations. All these operations apply to those elements of this iterable collection which satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.withFilter(p)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.zip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#zip-1dd)

```scala
def zip[B](that: IterableOnce[B]): Iterable[(List[Int], B)]
```

Returns a iterable collection formed from this iterable collection and another iterable collection by combining corresponding elements in pairs. If one of the two collections is longer than the other, its remaining elements are ignored.

Type parameters: `` B ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Returns: `` Iterable[(List[Int], B)] ``.

Source contract/attributes: Returns a iterable collection formed from this iterable collection and another iterable collection by combining corresponding elements in pairs. If one of the two collections is longer than the other, its remaining elements are ignored. Type parameters B the type of the second half of the returned pairs Value parameters that The iterable providing the second half of each result pair Attributes Returns a new iterable collection containing pairs consisting of corresponding elements of this iterable collection and that. The length of the returned collection is the minimum of the lengths of this iterable collection and that. Inherited from: IterableOps

Invocation template:

```scala
receiver.zip[B](that)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.zipAll ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#zipAll-fffff401)

```scala
def zipAll[A1 >: List[Int], B](that: Iterable[B], thisElem: A1, thatElem: B): Iterable[(A1, B)]
```

Returns a iterable collection formed from this iterable collection and another iterable collection by combining corresponding elements in pairs. If one of the two collections is shorter than the other, placeholder elements are used to extend the shorter collection to the length of the longer.

Type parameters: `` A1 >: List[Int] ``; `` B ``.

Parameters, list 1: `` that: Iterable[B] ``; `` thisElem: A1 ``; `` thatElem: B ``.

Returns: `` Iterable[(A1, B)] ``.

Source contract/attributes: Returns a iterable collection formed from this iterable collection and another iterable collection by combining corresponding elements in pairs. If one of the two collections is shorter than the other, placeholder elements are used to extend the shorter collection to the length of the longer. Type parameters A1 the type of the first element in each result pair (supertype of A) B the type of the second element in each result pair Value parameters that the iterable providing the second half of each result pair thatElem the element to be used to fill up the result if that is shorter than this iterable collection. thisElem the element to be used to fill up the result if this iterable collection is shorter than that. Attributes Returns a new iterable collection containing pairs consisting of corresponding elements of this iterable collection and that. The length of the returned collection is the maximum of the lengths of this iterable collection and that. If this iterable collection is shorter than that, thisElem values are used to pad the result. If that is shorter than this iterable collection, thatElem values are used to pad the result. Inherited from: IterableOps

Invocation template:

```scala
receiver.zipAll[A1, B](that, thisElem, thatElem)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.zipWithIndex ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#zipWithIndex-0)

```scala
def zipWithIndex: Iterable[(List[Int], Int)]
```

Zips this iterable collection with its indices.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[(List[Int], Int)] ``.

Source contract/attributes: Zips this iterable collection with its indices. Attributes Returns A new iterable collection containing pairs consisting of all elements of this iterable collection paired with their index. Indices start at 0. Example List("a", "b", "c").zipWithIndex == List(("a", 0), ("b", 1), ("c", 2)) Inherited from: IterableOps

Invocation template:

```scala
receiver.zipWithIndex
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.++: ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#++:-1dd)

```scala
def ++:[B >: List[Int]](that: IterableOnce[B]): Iterable[B]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use ++ instead of ++: for collections of type Iterable Inherited from: IterableOps

Invocation template:

```scala
receiver.++:[B](that)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices./: ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#/:-fffff9e1)

```scala
final def /:[B](z: B)(op: (B, List[Int]) => B): B
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, List[Int]) => B ``.

Returns: `` B ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use foldLeft instead of /: Inherited from: IterableOnceOps

Invocation template:

```scala
receiver./:[B](z)(op)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.:\ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#:\-fffff9e1)

```scala
final def :\[B](z: B)(op: (List[Int], B) => B): B
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (List[Int], B) => B ``.

Returns: `` B ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use foldRight instead of :\\ Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.:\[B](z)(op)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.aggregate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#aggregate-952)

```scala
def aggregate[B](z: => B)(seqop: (B, List[Int]) => B, combop: (B, B) => B): B
```

Aggregates the results of applying an operator to subsequent elements.

Type parameters: `` B ``.

Parameters, list 1: `` z: => B ``.

Parameters, list 2: `` seqop: (B, List[Int]) => B ``; `` combop: (B, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Aggregates the results of applying an operator to subsequent elements. Since this method degenerates to foldLeft for sequential (non-parallel) collections, where the combining operation is ignored, it is advisable to prefer foldLeft for that case. For parallel collections, use the aggregate method specified by scala.collection.parallel.ParIterableLike. Type parameters B the result type, produced by seqop, combop, and by this function as a final result. Value parameters combop an associative operator for combining sequential results, unused for sequential collections. seqop the binary operator used to accumulate the result. z the start value, a neutral element for seqop. Attributes Deprecated [Since version 2.13.0] For sequential collections, prefer `foldLeft(z)(seqop)`. For parallel collections, use `ParIterableLike#aggregate`. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.aggregate[B](z)(seqop, combop)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.companion ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#companion-0)

```scala
def companion: IterableFactory[Iterable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IterableFactory[Iterable] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use iterableFactory instead Inherited from: IterableOps

Invocation template:

```scala
receiver.companion
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.copyToBuffer ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#copyToBuffer-f0b)

```scala
final def copyToBuffer[B >: List[Int]](dest: Buffer[B]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` dest: Buffer[B] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use `dest ++= coll` instead Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToBuffer[B](dest)
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.hasDefiniteSize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#hasDefiniteSize-0)

```scala
def hasDefiniteSize: Boolean
```

Tests whether this iterable collection is known to have a finite size. All strict collections are known to have finite size. For a non-strict collection such as Stream, the predicate returns **true** if all elements have been computed. It returns **false** if the stream is not yet evaluated to the end. Non-empty Iterators usually return **false** even if they were created from a collection with a known finite size.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this iterable collection is known to have a finite size. All strict collections are known to have finite size. For a non-strict collection such as Stream, the predicate returns **true** if all elements have been computed. It returns **false** if the stream is not yet evaluated to the end. Non-empty Iterators usually return **false** even if they were created from a collection with a known finite size. Note: many collection methods will not work on collections of infinite sizes. The typical failure mode is an infinite loop. These methods always attempt a traversal without checking first that hasDefiniteSize returns **true**. However, checking hasDefiniteSize can provide an assurance that size is well-defined and non-termination is not a concern. Attributes Returns **true** if this collection is known to have finite size, **false** otherwise. See also method knownSize for a more useful alternative Deprecated [Since version 2.13.0] Check .knownSize instead of .hasDefiniteSize for more actionable information (see scaladoc for details) This method is deprecated in 2.13 because it does not provide any actionable information. As noted above, even the collection library itself does not use it. When there is no guarantee that a collection is finite, it is generally best to attempt a computation anyway and document that it will not terminate for infinite collections rather than backing out because this would prevent performing the computation on collections that are in fact finite even though hasDefiniteSize returns false. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.hasDefiniteSize
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.repr ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#repr-0)

```scala
final def repr: Iterable[List[Int]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use coll instead of repr in a collection implementation, use the collection value itself from the outside Inherited from: IterableOps

Invocation template:

```scala
receiver.repr
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.seq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#seq-0)

```scala
def seq: this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` this.type ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Iterable.seq always returns the iterable itself Inherited from: Iterable

Invocation template:

```scala
receiver.seq
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.toIterable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#toIterable-0)

```scala
final def toIterable: this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` this.type ``.

Source contract/attributes: Attributes Returns This collection as an Iterable[A]. No new collection will be built if this is already an Iterable[A]. Deprecated [Since version 2.13.7] toIterable is internal and will be made protected; its name is similar to `toList` or `toSeq`, but it doesn\'t copy non-immutable collections Inherited from: Iterable

Invocation template:

```scala
receiver.toIterable
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.toIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#toIterator-0)

```scala
final def toIterator: Iterator[List[Int]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[List[Int]] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use .iterator instead of .toIterator Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toIterator
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.toStream ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#toStream-0)

```scala
final def toStream: Stream[List[Int]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Stream[List[Int]] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use .to(LazyList) instead of .toStream Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toStream
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.toTraversable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#toTraversable-0)

```scala
final def toTraversable: Iterable[List[Int]]
```

Converts this iterable collection to an unspecified Iterable. Will return the same collection if this instance is already Iterable.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Converts this iterable collection to an unspecified Iterable. Will return the same collection if this instance is already Iterable. Attributes Returns An Iterable containing all elements of this iterable collection. Deprecated [Since version 2.13.0] toTraversable is internal and will be made protected; its name is similar to `toList` or `toSeq`, but it doesn\'t copy non-immutable collections Inherited from: IterableOps

Invocation template:

```scala
receiver.toTraversable
```

## `` com.cra.figaro.algorithm.factored.factors.Indices.view ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Indices.html#view-fffff78d)

```scala
def view(from: Int, until: Int): View[List[Int]]
```

A view over a slice of the elements of this collection.

Type parameters: none.

Parameters, list 1: `` from: Int ``; `` until: Int ``.

Returns: `` View[List[Int]] ``.

Source contract/attributes: A view over a slice of the elements of this collection. Attributes Deprecated [Since version 2.13.0] Use .view.slice(from, until) instead of .view(from, until) Inherited from: IterableOps

Invocation template:

```scala
receiver.view(from, until)
```

## `` com.cra.figaro.algorithm.factored.factors.InternalChainVariable.equals ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/InternalChainVariable.html#equals-4ad)

```scala
override def equals(o: Any): Boolean
```

Override equality of Variable. Variables are the same if their id's are the same

Type parameters: none.

Parameters, list 1: `` o: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Override equality of Variable. Variables are the same if their id's are the same Attributes Definition Classes Variable -> Any Inherited from: Variable

Invocation template:

```scala
receiver.equals(o)
```

## `` com.cra.figaro.algorithm.factored.factors.InternalChainVariable.hashCode ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/InternalChainVariable.html#hashCode-0)

```scala
override def hashCode: Int
```

Calculates a hash code value for the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Calculates a hash code value for the object. The default hashing algorithm is platform dependent. Note that it is allowed for two objects to have identical hash codes (o1.hashCode.equals(o2.hashCode)) yet not be equal (o1.equals(o2) returns false). A degenerate implementation could always return 0. However, it is required that if two objects are equal (o1.equals(o2) returns true) that they have identical hash codes (o1.hashCode.equals(o2.hashCode)). Therefore, when overriding this method, be sure to verify that the behavior is consistent with the equals method. Attributes Returns the hash code value for this object. Definition Classes Variable -> Any Inherited from: Variable

Invocation template:

```scala
receiver.hashCode
```

## `` com.cra.figaro.algorithm.factored.factors.InternalChainVariable.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/InternalChainVariable.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes InternalVariable -> Variable -> Any Inherited from: InternalVariable

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.algorithm.factored.factors.InternalVariable.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/InternalVariable.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Variable -> Any

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.algorithm.factored.factors.InternalVariable.equals ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/InternalVariable.html#equals-4ad)

```scala
override def equals(o: Any): Boolean
```

Override equality of Variable. Variables are the same if their id's are the same

Type parameters: none.

Parameters, list 1: `` o: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Override equality of Variable. Variables are the same if their id's are the same Attributes Definition Classes Variable -> Any Inherited from: Variable

Invocation template:

```scala
receiver.equals(o)
```

## `` com.cra.figaro.algorithm.factored.factors.InternalVariable.hashCode ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/InternalVariable.html#hashCode-0)

```scala
override def hashCode: Int
```

Calculates a hash code value for the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Calculates a hash code value for the object. The default hashing algorithm is platform dependent. Note that it is allowed for two objects to have identical hash codes (o1.hashCode.equals(o2.hashCode)) yet not be equal (o1.equals(o2) returns false). A degenerate implementation could always return 0. However, it is required that if two objects are equal (o1.equals(o2) returns true) that they have identical hash codes (o1.hashCode.equals(o2.hashCode)). Therefore, when overriding this method, be sure to verify that the behavior is consistent with the equals method. Attributes Returns the hash code value for this object. Definition Classes Variable -> Any Inherited from: Variable

Invocation template:

```scala
receiver.hashCode
```

## `` com.cra.figaro.algorithm.factored.factors.LogConvertibleSemiRing.convert ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogConvertibleSemiRing.html#convert-fffff2df)

```scala
def convert(): LogConvertibleSemiRing[T]
```

Converts this semiring into its log inverse For non-log space semirings, converts to log, and for log semirings exponentiates

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` LogConvertibleSemiRing[T] ``.

Source contract/attributes: Converts this semiring into its log inverse For non-log space semirings, converts to log, and for log semirings exponentiates Attributes

Invocation template:

```scala
receiver.convert()
```

## `` com.cra.figaro.algorithm.factored.factors.LogConvertibleSemiRing.isLog ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogConvertibleSemiRing.html#isLog-0)

```scala
def isLog: Boolean
```

Is true if this semiring is in log space

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Is true if this semiring is in log space Attributes

Invocation template:

```scala
receiver.isLog
```

## `` com.cra.figaro.algorithm.factored.factors.LogConvertibleSemiRing.sumMany ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogConvertibleSemiRing.html#sumMany-2cd)

```scala
def sumMany(xs: Iterable[T]): T
```

Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations.

Type parameters: none.

Parameters, list 1: `` xs: Iterable[T] ``.

Returns: `` T ``.

Source contract/attributes: Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations. Attributes Inherited from: Semiring

Invocation template:

```scala
receiver.sumMany(xs)
```

## `` com.cra.figaro.algorithm.factored.factors.LogConvertibleSemiRing.divide ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogConvertibleSemiRing.html#divide-fffffab9)

```scala
def divide(x: T, y: T): T
```

Division of two entries, as defined by the particular inference problem.

Type parameters: none.

Parameters, list 1: `` x: T ``; `` y: T ``.

Returns: `` T ``.

Source contract/attributes: Division of two entries, as defined by the particular inference problem. Attributes Inherited from: DivideableSemiRing

Invocation template:

```scala
receiver.divide(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.LogConvertibleSemiRing.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogConvertibleSemiRing.html#product-fffffab9)

```scala
def product(x: T, y: T): T
```

Product of two entries, as defined by the particular inference problem.

Type parameters: none.

Parameters, list 1: `` x: T ``; `` y: T ``.

Returns: `` T ``.

Source contract/attributes: Product of two entries, as defined by the particular inference problem. Attributes Inherited from: Semiring

Invocation template:

```scala
receiver.product(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.LogConvertibleSemiRing.sum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogConvertibleSemiRing.html#sum-fffffab9)

```scala
def sum(x: T, y: T): T
```

Sum of two entries, as defined by the particular inference problem. E.g., for computing conditional probabilities, sum is ordinary addition; for most probable explanation, it is max.

Type parameters: none.

Parameters, list 1: `` x: T ``; `` y: T ``.

Returns: `` T ``.

Source contract/attributes: Sum of two entries, as defined by the particular inference problem. E.g., for computing conditional probabilities, sum is ordinary addition; for most probable explanation, it is max. Attributes Inherited from: Semiring

Invocation template:

```scala
receiver.sum(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.LogMaxProductSemiring.convert ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogMaxProductSemiring.html#convert-fffff2df)

```scala
def convert(): LogConvertibleSemiRing[Double]
```

Converts this semiring into its log inverse For non-log space semirings, converts to log, and for log semirings exponentiates

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` LogConvertibleSemiRing[Double] ``.

Source contract/attributes: Converts this semiring into its log inverse For non-log space semirings, converts to log, and for log semirings exponentiates Attributes

Invocation template:

```scala
receiver.convert()
```

## `` com.cra.figaro.algorithm.factored.factors.LogMaxProductSemiring.divide ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogMaxProductSemiring.html#divide-44b)

```scala
def divide(x: Double, y: Double): Double
```

Division of two entries, as defined by the particular inference problem.

Type parameters: none.

Parameters, list 1: `` x: Double ``; `` y: Double ``.

Returns: `` Double ``.

Source contract/attributes: Division of two entries, as defined by the particular inference problem. Attributes

Invocation template:

```scala
receiver.divide(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.LogMaxProductSemiring.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogMaxProductSemiring.html#product-44b)

```scala
def product(x: Double, y: Double): Double
```

Product of two entries, as defined by the particular inference problem.

Type parameters: none.

Parameters, list 1: `` x: Double ``; `` y: Double ``.

Returns: `` Double ``.

Source contract/attributes: Product of two entries, as defined by the particular inference problem. Attributes

Invocation template:

```scala
receiver.product(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.LogMaxProductSemiring.sum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogMaxProductSemiring.html#sum-44b)

```scala
def sum(x: Double, y: Double): Double
```

Sum of two entries, as defined by the particular inference problem. E.g., for computing conditional probabilities, sum is ordinary addition; for most probable explanation, it is max.

Type parameters: none.

Parameters, list 1: `` x: Double ``; `` y: Double ``.

Returns: `` Double ``.

Source contract/attributes: Sum of two entries, as defined by the particular inference problem. E.g., for computing conditional probabilities, sum is ordinary addition; for most probable explanation, it is max. Attributes

Invocation template:

```scala
receiver.sum(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.LogMaxProductSemiring.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogMaxProductSemiring.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.algorithm.factored.factors.LogMaxProductSemiring.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogMaxProductSemiring.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.algorithm.factored.factors.LogMaxProductSemiring.sumMany ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogMaxProductSemiring.html#sumMany-2cd)

```scala
def sumMany(xs: Iterable[Double]): Double
```

Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations.

Type parameters: none.

Parameters, list 1: `` xs: Iterable[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations. Attributes Inherited from: Semiring

Invocation template:

```scala
receiver.sumMany(xs)
```

## `` com.cra.figaro.algorithm.factored.factors.LogSumProductSemiring.convert ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogSumProductSemiring.html#convert-fffff2df)

```scala
def convert(): LogConvertibleSemiRing[Double]
```

Converts this semiring into its log inverse For non-log space semirings, converts to log, and for log semirings exponentiates

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` LogConvertibleSemiRing[Double] ``.

Source contract/attributes: Converts this semiring into its log inverse For non-log space semirings, converts to log, and for log semirings exponentiates Attributes

Invocation template:

```scala
receiver.convert()
```

## `` com.cra.figaro.algorithm.factored.factors.LogSumProductSemiring.divide ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogSumProductSemiring.html#divide-44b)

```scala
def divide(x: Double, y: Double): Double
```

Division of two entries, as defined by the particular inference problem.

Type parameters: none.

Parameters, list 1: `` x: Double ``; `` y: Double ``.

Returns: `` Double ``.

Source contract/attributes: Division of two entries, as defined by the particular inference problem. Attributes

Invocation template:

```scala
receiver.divide(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.LogSumProductSemiring.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogSumProductSemiring.html#product-44b)

```scala
def product(x: Double, y: Double): Double
```

Product of two entries, as defined by the particular inference problem.

Type parameters: none.

Parameters, list 1: `` x: Double ``; `` y: Double ``.

Returns: `` Double ``.

Source contract/attributes: Product of two entries, as defined by the particular inference problem. Attributes

Invocation template:

```scala
receiver.product(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.LogSumProductSemiring.sum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogSumProductSemiring.html#sum-44b)

```scala
def sum(x: Double, y: Double): Double
```

Sum of two entries, as defined by the particular inference problem. E.g., for computing conditional probabilities, sum is ordinary addition; for most probable explanation, it is max.

Type parameters: none.

Parameters, list 1: `` x: Double ``; `` y: Double ``.

Returns: `` Double ``.

Source contract/attributes: Sum of two entries, as defined by the particular inference problem. E.g., for computing conditional probabilities, sum is ordinary addition; for most probable explanation, it is max. Attributes

Invocation template:

```scala
receiver.sum(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.LogSumProductSemiring.sumMany ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogSumProductSemiring.html#sumMany-fd3)

```scala
override def sumMany(xs: Iterable[Double]): Double
```

Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations.

Type parameters: none.

Parameters, list 1: `` xs: Iterable[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations. Attributes Definition Classes Semiring

Invocation template:

```scala
receiver.sumMany(xs)
```

## `` com.cra.figaro.algorithm.factored.factors.LogSumProductSemiring.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogSumProductSemiring.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.algorithm.factored.factors.LogSumProductSemiring.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/LogSumProductSemiring.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.algorithm.factored.factors.MaxProductSemiring.convert ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/MaxProductSemiring.html#convert-fffff2df)

```scala
def convert(): LogConvertibleSemiRing[Double]
```

Converts this semiring into its log inverse For non-log space semirings, converts to log, and for log semirings exponentiates

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` LogConvertibleSemiRing[Double] ``.

Source contract/attributes: Converts this semiring into its log inverse For non-log space semirings, converts to log, and for log semirings exponentiates Attributes

Invocation template:

```scala
receiver.convert()
```

## `` com.cra.figaro.algorithm.factored.factors.MaxProductSemiring.divide ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/MaxProductSemiring.html#divide-44b)

```scala
def divide(x: Double, y: Double): Double
```

Standard division

Type parameters: none.

Parameters, list 1: `` x: Double ``; `` y: Double ``.

Returns: `` Double ``.

Source contract/attributes: Standard division Attributes

Invocation template:

```scala
receiver.divide(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.MaxProductSemiring.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/MaxProductSemiring.html#product-44b)

```scala
def product(x: Double, y: Double): Double
```

Standard multiplication

Type parameters: none.

Parameters, list 1: `` x: Double ``; `` y: Double ``.

Returns: `` Double ``.

Source contract/attributes: Standard multiplication Attributes

Invocation template:

```scala
receiver.product(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.MaxProductSemiring.sum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/MaxProductSemiring.html#sum-44b)

```scala
def sum(x: Double, y: Double): Double
```

The maximum of x and y.

Type parameters: none.

Parameters, list 1: `` x: Double ``; `` y: Double ``.

Returns: `` Double ``.

Source contract/attributes: The maximum of x and y. Attributes

Invocation template:

```scala
receiver.sum(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.MaxProductSemiring.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/MaxProductSemiring.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.algorithm.factored.factors.MaxProductSemiring.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/MaxProductSemiring.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.algorithm.factored.factors.MaxProductSemiring.sumMany ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/MaxProductSemiring.html#sumMany-2cd)

```scala
def sumMany(xs: Iterable[Double]): Double
```

Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations.

Type parameters: none.

Parameters, list 1: `` xs: Iterable[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations. Attributes Inherited from: Semiring

Invocation template:

```scala
receiver.sumMany(xs)
```

## `` com.cra.figaro.algorithm.factored.factors.ParameterizedVariable.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ParameterizedVariable.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes ElementVariable -> Variable -> Any

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.algorithm.factored.factors.ParameterizedVariable.equals ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ParameterizedVariable.html#equals-4ad)

```scala
override def equals(o: Any): Boolean
```

Override equality of Variable. Variables are the same if their id's are the same

Type parameters: none.

Parameters, list 1: `` o: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Override equality of Variable. Variables are the same if their id's are the same Attributes Definition Classes Variable -> Any Inherited from: Variable

Invocation template:

```scala
receiver.equals(o)
```

## `` com.cra.figaro.algorithm.factored.factors.ParameterizedVariable.hashCode ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/ParameterizedVariable.html#hashCode-0)

```scala
override def hashCode: Int
```

Calculates a hash code value for the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Calculates a hash code value for the object. The default hashing algorithm is platform dependent. Note that it is allowed for two objects to have identical hash codes (o1.hashCode.equals(o2.hashCode)) yet not be equal (o1.equals(o2) returns false). A degenerate implementation could always return 0. However, it is required that if two objects are equal (o1.equals(o2) returns true) that they have identical hash codes (o1.hashCode.equals(o2.hashCode)). Therefore, when overriding this method, be sure to verify that the behavior is consistent with the equals method. Attributes Returns the hash code value for this object. Definition Classes Variable -> Any Inherited from: Variable

Invocation template:

```scala
receiver.hashCode
```

## `` com.cra.figaro.algorithm.factored.factors.Semiring.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Semiring.html#product-fffffab9)

```scala
def product(x: T, y: T): T
```

Product of two entries, as defined by the particular inference problem.

Type parameters: none.

Parameters, list 1: `` x: T ``; `` y: T ``.

Returns: `` T ``.

Source contract/attributes: Product of two entries, as defined by the particular inference problem. Attributes

Invocation template:

```scala
receiver.product(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.Semiring.sum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Semiring.html#sum-fffffab9)

```scala
def sum(x: T, y: T): T
```

Sum of two entries, as defined by the particular inference problem. E.g., for computing conditional probabilities, sum is ordinary addition; for most probable explanation, it is max.

Type parameters: none.

Parameters, list 1: `` x: T ``; `` y: T ``.

Returns: `` T ``.

Source contract/attributes: Sum of two entries, as defined by the particular inference problem. E.g., for computing conditional probabilities, sum is ordinary addition; for most probable explanation, it is max. Attributes

Invocation template:

```scala
receiver.sum(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.Semiring.sumMany ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Semiring.html#sumMany-2cd)

```scala
def sumMany(xs: Iterable[T]): T
```

Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations.

Type parameters: none.

Parameters, list 1: `` xs: Iterable[T] ``.

Returns: `` T ``.

Source contract/attributes: Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations. Attributes

Invocation template:

```scala
receiver.sumMany(xs)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.iterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#iterator-0)

```scala
override def iterator: Iterator[List[Int]]
```

An scala.collection.Iterator over the elements of this iterable collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[List[Int]] ``.

Source contract/attributes: An scala.collection.Iterator over the elements of this iterable collection. If an IterableOnce object is in fact an scala.collection.Iterator, this method always returns itself, in its current state, but if it is an scala.collection.Iterable, this method always returns a new scala.collection.Iterator. Attributes Returns an iterator over all elements of this iterable collection Definition Classes Indices -> IterableOnce

Invocation template:

```scala
receiver.iterator
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#++-1dd)

```scala
final def ++[B >: List[Int]](suffix: IterableOnce[B]): Iterable[B]
```

Alias for concat.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` suffix: IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Alias for concat. Type parameters B the element type of the returned collection Value parameters suffix the iterable to append to this iterable collection Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.++[B](suffix)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#addString-ff4)

```scala
final def addString(b: StringBuilder): b.type
```

Appends all elements of this iterable collection to a string builder. The written text consists of the string representations (w.r.t. the method toString) of all elements of this iterable collection without any separator string.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this iterable collection to a string builder. The written text consists of the string representations (w.r.t. the method toString) of all elements of this iterable collection without any separator string. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() val h = a.addString(b) // 1234 Value parameters b the string builder to which elements are appended. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#addString-e99)

```scala
final def addString(b: StringBuilder, sep: String): b.type
```

Appends all elements of this iterable collection to a string builder using a separator string. The written text consists of the string representations (w.r.t. the method toString) of all elements of this iterable collection, separated by the string sep.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``; `` sep: String ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this iterable collection to a string builder using a separator string. The written text consists of the string representations (w.r.t. the method toString) of all elements of this iterable collection, separated by the string sep. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() a.addString(b, ", ") // 1, 2, 3, 4 Value parameters b the string builder to which elements are appended. sep the separator string. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b, sep)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#addString-de3)

```scala
def addString(b: StringBuilder, start: String, sep: String, end: String): b.type
```

Appends all elements of this iterable collection to a string builder using start, end, and separator strings. The written text begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this iterable collection are separated by the string sep.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``; `` start: String ``; `` sep: String ``; `` end: String ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this iterable collection to a string builder using start, end, and separator strings. The written text begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this iterable collection are separated by the string sep. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() a.addString(b , "List(" , ", " , ")") // List(1, 2, 3, 4) Value parameters b the string builder to which elements are appended. end the ending string. sep the separator string. start the starting string. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b, start, sep, end)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.allIndices ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#allIndices-0)

```scala
def allIndices: List[List[Int]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[List[Int]] ``.

Source contract/attributes: Attributes Inherited from: Indices

Invocation template:

```scala
receiver.allIndices
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.collect ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#collect-fffff5b3)

```scala
def collect[B](pf: PartialFunction[List[Int], B]): Iterable[B]
```

Builds a new iterable collection by applying a partial function to all elements of this iterable collection on which the function is defined.

Type parameters: `` B ``.

Parameters, list 1: `` pf: PartialFunction[List[Int], B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Builds a new iterable collection by applying a partial function to all elements of this iterable collection on which the function is defined. Type parameters B the element type of the returned iterable collection. Value parameters pf the partial function which filters and maps the iterable collection. Attributes Returns a new iterable collection resulting from applying the given partial function pf to each element on which it is defined and collecting the results. The order of the elements is preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.collect[B](pf)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.collectFirst ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#collectFirst-25d)

```scala
def collectFirst[B](pf: PartialFunction[List[Int], B]): Option[B]
```

Finds the first element of the iterable collection for which the given partial function is defined, and applies the partial function to it.

Type parameters: `` B ``.

Parameters, list 1: `` pf: PartialFunction[List[Int], B] ``.

Returns: `` Option[B] ``.

Source contract/attributes: Finds the first element of the iterable collection for which the given partial function is defined, and applies the partial function to it. Note: may not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Type parameters B the result type of the partial function Value parameters pf the partial function Attributes Returns an option value containing pf applied to the first value for which it is defined, or None if none exists. Example Seq("a", 1, 5L).collectFirst { case x: Int => x*10 } = Some(10) Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.collectFirst[B](pf)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.concat ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#concat-1dd)

```scala
def concat[B >: List[Int]](suffix: IterableOnce[B]): Iterable[B]
```

Returns a new iterable collection containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the iterable collection is the most specific superclass encompassing the element types of the two operands.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` suffix: IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Returns a new iterable collection containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the iterable collection is the most specific superclass encompassing the element types of the two operands. Type parameters B the element type of the returned collection. Value parameters suffix the iterable to append. Attributes Returns a new iterable collection which contains all elements of this iterable collection followed by all elements of suffix. Inherited from: IterableOps

Invocation template:

```scala
receiver.concat[B](suffix)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#copyToArray-fffffd03)

```scala
def copyToArray[B >: List[Int]](dest: Array[B], start: Int, n: Int): Int
```

Copies elements to an array and returns the number of elements written.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` dest: Array[B] ``; `` start: Int ``; `` n: Int ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array and returns the number of elements written. Fills the given array dest starting at index start with at most n elements of this iterable collection. Copying will stop once either all the elements of this iterable collection have been copied, or the end of the array is reached, or n elements have been copied. If start is less than zero, it is taken as zero. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. n the maximal number of elements to copy. start the starting index of xs. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest, start, n)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#copyToArray-fffffaa4)

```scala
def copyToArray[B >: List[Int]](dest: Array[B], start: Int): Int
```

Copies elements to an array, returning the number of elements written.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` dest: Array[B] ``; `` start: Int ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array, returning the number of elements written. Fills the given array dest starting at index start with values of this iterable collection. Copying will stop once either all the elements of this iterable collection have been copied, or the end of the array is reached. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. start the starting index of xs. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest, start)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#copyToArray-fffff3e3)

```scala
def copyToArray[B >: List[Int]](dest: Array[B]): Int
```

Copies elements to an array, returning the number of elements written.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` dest: Array[B] ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array, returning the number of elements written. Fills the given array dest starting at index start with values of this iterable collection. Copying will stop once either all the elements of this iterable collection have been copied, or the end of the array is reached. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.corresponds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#corresponds-958)

```scala
def corresponds[B](that: IterableOnce[B])(p: (List[Int], B) => Boolean): Boolean
```

Tests whether every element of this collection's iterator relates to the corresponding element of another collection by satisfying a test predicate.

Type parameters: `` B ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Parameters, list 2: `` p: (List[Int], B) => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether every element of this collection's iterator relates to the corresponding element of another collection by satisfying a test predicate. Note: will not terminate for infinite-sized collections. Type parameters B the type of the elements of that Value parameters p the test predicate, which relates elements from both collections that the other collection Attributes Returns true if both collections have the same length and p(x, y) is true for all corresponding elements x of this iterator and y of that, otherwise false Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.corresponds[B](that)(p)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.count ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#count-db6)

```scala
def count(p: List[Int] => Boolean): Int
```

Counts the number of elements in the iterable collection which satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` p: List[Int] => Boolean ``.

Returns: `` Int ``.

Source contract/attributes: Counts the number of elements in the iterable collection which satisfy a predicate. Note: will not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns the number of elements satisfying the predicate p. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.count(p)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.drop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#drop-aba)

```scala
def drop(n: Int): Iterable[List[Int]]
```

Selects all elements except the first n ones.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Selects all elements except the first n ones. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to drop from this iterable collection. Attributes Returns a iterable collection consisting of all elements of this iterable collection except the first n ones, or else the empty iterable collection, if this iterable collection has less than n elements. If n is negative, don't drop any elements. Inherited from: IterableOps

Invocation template:

```scala
receiver.drop(n)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.dropRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#dropRight-aba)

```scala
def dropRight(n: Int): Iterable[List[Int]]
```

Selects all elements except last n ones.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Selects all elements except last n ones. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to drop from this iterable collection. Attributes Returns a iterable collection consisting of all elements of this iterable collection except the last n ones, or else the empty iterable collection, if this iterable collection has less than n elements. If n is negative, don't drop any elements. Inherited from: IterableOps

Invocation template:

```scala
receiver.dropRight(n)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.dropWhile ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#dropWhile-c24)

```scala
def dropWhile(p: List[Int] => Boolean): Iterable[List[Int]]
```

Selects all elements except the longest prefix that satisfies a predicate.

Type parameters: none.

Parameters, list 1: `` p: List[Int] => Boolean ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Selects all elements except the longest prefix that satisfies a predicate. The matching prefix starts with the first element of this iterable collection, and the element following the prefix is the first element that does not satisfy the predicate. The matching prefix may be empty, so that this method returns the entire iterable collection. Example: List(1, 2, 3, 100, 4).dropWhile(n => n < 10) // List(100, 4) List(1, 2, 3, 100, 4).dropWhile(n => n == 0) // List(1, 2, 3, 100, 4) Use span to obtain both the prefix and suffix. Use filterNot to drop all elements that satisfy the predicate. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p The predicate used to test elements. Attributes Returns the longest suffix of this iterable collection whose first element does not satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.dropWhile(p)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.empty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#empty-0)

```scala
override def empty: Iterable[List[Int]]
```

The empty iterable collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: The empty iterable collection. Attributes Returns an empty iterable of type Iterable. Definition Classes IterableFactoryDefaults -> IterableOps Inherited from: IterableFactoryDefaults

Invocation template:

```scala
receiver.empty
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.exists ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#exists-fffff66f)

```scala
def exists(p: List[Int] => Boolean): Boolean
```

Tests whether a predicate holds for at least one element of this iterable collection.

Type parameters: none.

Parameters, list 1: `` p: List[Int] => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether a predicate holds for at least one element of this iterable collection. Note: may not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns true if the given predicate p is satisfied by at least one element of this iterable collection, otherwise false Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.exists(p)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.filter ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#filter-c24)

```scala
def filter(pred: List[Int] => Boolean): Iterable[List[Int]]
```

Selects all elements of this iterable collection which satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` pred: List[Int] => Boolean ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Selects all elements of this iterable collection which satisfy a predicate. Value parameters p the predicate used to test elements. Attributes Returns a new iterable collection consisting of all elements of this iterable collection that satisfy the given predicate p. The order of the elements is preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.filter(pred)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.filterNot ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#filterNot-c24)

```scala
def filterNot(pred: List[Int] => Boolean): Iterable[List[Int]]
```

Selects all elements of this iterable collection which do not satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` pred: List[Int] => Boolean ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Selects all elements of this iterable collection which do not satisfy a predicate. Value parameters pred the predicate used to test elements. Attributes Returns a new iterable collection consisting of all elements of this iterable collection that do not satisfy the given predicate pred. Their order may not be preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.filterNot(pred)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.find ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#find-94e)

```scala
def find(p: List[Int] => Boolean): Option[List[Int]]
```

Finds the first element of the iterable collection satisfying a predicate, if any.

Type parameters: none.

Parameters, list 1: `` p: List[Int] => Boolean ``.

Returns: `` Option[List[Int]] ``.

Source contract/attributes: Finds the first element of the iterable collection satisfying a predicate, if any. Note: may not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the predicate used to test elements. Attributes Returns an option value containing the first element in the iterable collection that satisfies p, or None if none exists. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.find(p)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#flatMap-5d3)

```scala
def flatMap[B](f: List[Int] => IterableOnce[B]): Iterable[B]
```

Builds a new iterable collection by applying a function to all elements of this iterable collection and using the elements of the resulting collections.

Type parameters: `` B ``.

Parameters, list 1: `` f: List[Int] => IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Builds a new iterable collection by applying a function to all elements of this iterable collection and using the elements of the resulting collections. For example: def getWords(lines: Seq[String]): Seq[String] = lines.flatMap(line => line.split("\\W+")) The type of the resulting collection is guided by the static type of this iterable collection. This might cause unexpected results sometimes. For example: // lettersOf will return a Seq[Char] of likely repeated letters, instead of a Set def lettersOf(words: Seq[String]) = words.flatMap(word => word.toSet) // lettersOf2 will return a Set[Char], not a Seq def lettersOf2(words: Seq[String]) = words.toSet.flatMap(word => word.toSeq) // xs will be an Iterable[Int] val xs = Map("a" -> List(11, 111), "b" -> List(22, 222)).flatMap(_._2) // ys will be a Map[Int, Int] val ys = Map("a" -> List(1 -> 11, 1 -> 111), "b" -> List(2 -> 22, 2 -> 222)).flatMap(_._2) Type parameters B the element type of the returned collection. Value parameters f the function to apply to each element. Attributes Returns a new iterable collection resulting from applying the given collection-valued function f to each element of this iterable collection and concatenating the results. Inherited from: IterableOps

Invocation template:

```scala
receiver.flatMap[B](f)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.flatten ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#flatten-5d3)

```scala
def flatten[B](implicit asIterable: List[Int] => IterableOnce[B]): Iterable[B]
```

Given that the elements of this collection are themselves iterable collections, converts this iterable collection into a iterable collection comprising the elements of these iterable collections.

Type parameters: `` B ``.

Context parameters, list 1: `` asIterable: List[Int] => IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Given that the elements of this collection are themselves iterable collections, converts this iterable collection into a iterable collection comprising the elements of these iterable collections. The resulting collection's type will be guided by the type of iterable collection. For example: val xs = List( Set(1, 2, 3), Set(1, 2, 3) ).flatten // xs == List(1, 2, 3, 1, 2, 3) val ys = Set( List(1, 2, 3), List(3, 2, 1) ).flatten // ys == Set(1, 2, 3) Type parameters B the type of the elements of each iterable collection. Value parameters asIterable an implicit conversion which asserts that the element type of this iterable collection is an Iterable. Attributes Returns a new iterable collection resulting from concatenating all element collections. Inherited from: IterableOps

Invocation template:

```scala
receiver.flatten[B](using asIterable)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.fold ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#fold-fffff9e1)

```scala
def fold[A1 >: List[Int]](z: A1)(op: (A1, A1) => A1): A1
```

Applies the given binary operator op to the given initial value z and all elements of this iterable collection.

Type parameters: `` A1 >: List[Int] ``.

Parameters, list 1: `` z: A1 ``.

Parameters, list 2: `` op: (A1, A1) => A1 ``.

Returns: `` A1 ``.

Source contract/attributes: Applies the given binary operator op to the given initial value z and all elements of this iterable collection. For each application of the operator, each operand is either an element of this iterable collection, the initial value, or another such application of the operator. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. The initial value may be used an arbitrary number of times, but at least once. If this collection is ordered, then for any application of the operator, the element(s) appearing in the left operand will precede those in the right. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. In either case, it is also necessary that the initial value be a neutral value for the operator, e.g. Nil for List concatenation or 1 for multiplication. The default implementation in IterableOnce is equivalent to foldLeft but may be overridden for more efficient traversal orders. Note: will not terminate for infinite-sized collections. Type parameters A1 The type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. z An initial value; may be used an arbitrary number of times in the computation of the result; must be a neutral value for op for the result to always be the same across runs. Attributes Returns The result of applying op between all the elements and z, or z if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.fold[A1](z)(op)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.foldLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#foldLeft-fffff9e1)

```scala
def foldLeft[B](z: B)(op: (B, List[Int]) => B): B
```

Applies the given binary operator op to the given initial value z and all elements of this iterable collection, going left to right. Returns the initial value if this iterable collection is empty.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, List[Int]) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to the given initial value z and all elements of this iterable collection, going left to right. Returns the initial value if this iterable collection is empty. "Going left to right" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this iterable collection, the result is op( op( ... op( op(z, x,,1,,), x,,2,,) ... ), x,,n,,). If this collection is not ordered, then for each application of the operator, each right operand is an element. In addition, the leftmost operand is the initial value, and each other left operand is itself an application of the operator. The elements of this iterable collection and the initial value all appear exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator. Value parameters op A binary operator. z An initial value. Attributes Returns The result of applying op to z and all elements of this iterable collection, going left to right. Returns z if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foldLeft[B](z)(op)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.foldRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#foldRight-fffff9e1)

```scala
def foldRight[B](z: B)(op: (List[Int], B) => B): B
```

Applies the given binary operator op to all elements of this iterable collection and the given initial value z, going right to left. Returns the initial value if this iterable collection is empty.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (List[Int], B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this iterable collection and the given initial value z, going right to left. Returns the initial value if this iterable collection is empty. "Going right to left" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this iterable collection, the result is op(x,,1,,, op(x,,2,,, op( ... op(x,,n,,, z) ... ))). If this collection is not ordered, then for each application of the operator, each left operand is an element. In addition, the rightmost operand is the initial value, and each other right operand is itself an application of the operator. The elements of this iterable collection and the initial value all appear exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator. Value parameters op A binary operator. z An initial value. Attributes Returns The result of applying op to all elements of this iterable collection and z, going right to left. Returns z if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foldRight[B](z)(op)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.forall ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#forall-fffff66f)

```scala
def forall(p: List[Int] => Boolean): Boolean
```

Tests whether a predicate holds for all elements of this iterable collection.

Type parameters: none.

Parameters, list 1: `` p: List[Int] => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether a predicate holds for all elements of this iterable collection. Note: may not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns true if this iterable collection is empty or the given predicate p holds for all elements of this iterable collection, otherwise false. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.forall(p)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.foreach ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#foreach-58c)

```scala
def foreach[U](f: List[Int] => U): Unit
```

Applies f to each element for its side effects. Note: U parameter needed to help scalac's type inference.

Type parameters: `` U ``.

Parameters, list 1: `` f: List[Int] => U ``.

Returns: `` Unit ``.

Source contract/attributes: Applies f to each element for its side effects. Note: U parameter needed to help scalac's type inference. Type parameters U the return type of f; the value is discarded, but the type parameter aids type inference Value parameters f the function to apply to each element for its side effects Attributes Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foreach[U](f)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.groupBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#groupBy-f68)

```scala
def groupBy[K](f: List[Int] => K): Map[K, Iterable[List[Int]]]
```

Partitions this iterable collection into a map of iterable collections according to some discriminator function.

Type parameters: `` K ``.

Parameters, list 1: `` f: List[Int] => K ``.

Returns: `` Map[K, Iterable[List[Int]]] ``.

Source contract/attributes: Partitions this iterable collection into a map of iterable collections according to some discriminator function. Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters K the type of keys returned by the discriminator function. Value parameters f the discriminator function. Attributes Returns A map from keys to iterable collections such that the following invariant holds: (xs groupBy f)(k) = xs filter (x => f(x) == k) That is, every key `k` is bound to a iterable collection of those elements `x` for which `f(x)` equals `k`. Inherited from: IterableOps

Invocation template:

```scala
receiver.groupBy[K](f)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.groupMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#groupMap-fffff03a)

```scala
def groupMap[K, B](key: List[Int] => K)(f: List[Int] => B): Map[K, Iterable[B]]
```

Partitions this iterable collection into a map of iterable collections according to a discriminator function key. Each element in a group is transformed into a value of type B using the value function.

Type parameters: `` K ``; `` B ``.

Parameters, list 1: `` key: List[Int] => K ``.

Parameters, list 2: `` f: List[Int] => B ``.

Returns: `` Map[K, Iterable[B]] ``.

Source contract/attributes: Partitions this iterable collection into a map of iterable collections according to a discriminator function key. Each element in a group is transformed into a value of type B using the value function. It is equivalent to groupBy(key).mapValues(_.map(f)), but more efficient. case class User(name: String, age: Int) def namesByAge(users: Seq[User]): Map[Int, Seq[String]] = users.groupMap(_.age)(_.name) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of values returned by the transformation function K the type of keys returned by the discriminator function Value parameters f the element transformation function key the discriminator function Attributes Returns a map associating each key k produced by key with a iterable collection of values produced by applying f to elements that map to k Inherited from: IterableOps

Invocation template:

```scala
receiver.groupMap[K, B](key)(f)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.groupMapReduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#groupMapReduce-8e8)

```scala
def groupMapReduce[K, B](key: List[Int] => K)(f: List[Int] => B)(reduce: (B, B) => B): Map[K, B]
```

Partitions this iterable collection into a map according to a discriminator function key. All the values that have the same discriminator are then transformed by the f function and then reduced into a single value with the reduce function.

Type parameters: `` K ``; `` B ``.

Parameters, list 1: `` key: List[Int] => K ``.

Parameters, list 2: `` f: List[Int] => B ``.

Parameters, list 3: `` reduce: (B, B) => B ``.

Returns: `` Map[K, B] ``.

Source contract/attributes: Partitions this iterable collection into a map according to a discriminator function key. All the values that have the same discriminator are then transformed by the f function and then reduced into a single value with the reduce function. It is equivalent to groupBy(key).mapValues(_.map(f).reduce(reduce)), but more efficient. def occurrences[A](as: Seq[A]): Map[A, Int] = as.groupMapReduce(identity)(_ => 1)(_ + _) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of values returned by the transformation function K the type of keys returned by the discriminator function Value parameters f the element transformation function key the discriminator function reduce the reduction function used to combine values mapped to the same key Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.groupMapReduce[K, B](key)(f)(reduce)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.grouped ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#grouped-fffffbef)

```scala
def grouped(size: Int): Iterator[Iterable[List[Int]]]
```

Partitions elements in fixed size iterable collections.

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Iterator[Iterable[List[Int]]] ``.

Source contract/attributes: Partitions elements in fixed size iterable collections. Value parameters size the number of elements per group Attributes Returns An iterator producing iterable collections of size size, except the last will be less than size size if the elements don't divide evenly. See also scala.collection.Iterator, method grouped Inherited from: IterableOps

Invocation template:

```scala
receiver.grouped(size)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.head ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#head-0)

```scala
def head: List[Int]
```

Selects the first element of this iterable collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Int] ``.

Source contract/attributes: Selects the first element of this iterable collection. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the first element of this iterable collection. Throws NoSuchElementException if the iterable collection is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.head
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.headOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#headOption-0)

```scala
def headOption: Option[List[Int]]
```

Optionally selects the first element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Option[List[Int]] ``.

Source contract/attributes: Optionally selects the first element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the first element of this iterable collection if it is nonempty, None if it is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.headOption
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.init ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#init-0)

```scala
def init: Iterable[List[Int]]
```

The initial part of the collection without its last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: The initial part of the collection without its last element. Note: Even when applied to a view or a lazy collection it will always force the elements. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.init
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.inits ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#inits-0)

```scala
def inits: Iterator[Iterable[List[Int]]]
```

Iterates over the inits of this iterable collection. The first value will be this iterable collection and the final one will be an empty iterable collection, with the intervening values the results of successive applications of init.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Iterable[List[Int]]] ``.

Source contract/attributes: Iterates over the inits of this iterable collection. The first value will be this iterable collection and the final one will be an empty iterable collection, with the intervening values the results of successive applications of init. Note: Even when applied to a view or a lazy collection it will always force the elements. Attributes Returns an iterator over all the inits of this iterable collection Example List(1,2,3).inits = Iterator(List(1,2,3), List(1,2), List(1), Nil) Inherited from: IterableOps

Invocation template:

```scala
receiver.inits
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.isEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#isEmpty-0)

```scala
def isEmpty: Boolean
```

Tests whether the iterable collection is empty.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether the iterable collection is empty. Note: The default implementation creates and discards an iterator. Note: Implementations in subclasses that are not repeatedly iterable must take care not to consume any elements when isEmpty is called. Attributes Returns true if the iterable collection contains no elements, false otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.isEmpty
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.isTraversableAgain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#isTraversableAgain-0)

```scala
override def isTraversableAgain: Boolean
```

Tests whether this iterable collection can be repeatedly traversed. Always true for Iterables and false for Iterators unless overridden.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this iterable collection can be repeatedly traversed. Always true for Iterables and false for Iterators unless overridden. Attributes Returns true if it is repeatedly traversable, false otherwise. Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.isTraversableAgain
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.iterableFactory ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#iterableFactory-0)

```scala
def iterableFactory: IterableFactory[Iterable]
```

The companion object of this iterable collection, providing various factory methods.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IterableFactory[Iterable] ``.

Source contract/attributes: The companion object of this iterable collection, providing various factory methods. Attributes Returns the companion factory object for this collection type Note When implementing a custom collection type and refining CC to the new type, this method needs to be overridden to return a factory for the new type (the compiler will issue an error otherwise). Inherited from: Iterable

Invocation template:

```scala
receiver.iterableFactory
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.knownSize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#knownSize-0)

```scala
def knownSize: Int
```

The number of elements in this iterable collection, if it can be cheaply computed, -1 otherwise. Cheaply usually means: Not requiring a collection traversal.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: The number of elements in this iterable collection, if it can be cheaply computed, -1 otherwise. Cheaply usually means: Not requiring a collection traversal. Attributes Inherited from: IterableOnce

Invocation template:

```scala
receiver.knownSize
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.last ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#last-0)

```scala
def last: List[Int]
```

Selects the last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Int] ``.

Source contract/attributes: Selects the last element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns The last element of this iterable collection. Throws NoSuchElementException If the iterable collection is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.last
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.lastOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#lastOption-0)

```scala
def lastOption: Option[List[Int]]
```

Optionally selects the last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Option[List[Int]] ``.

Source contract/attributes: Optionally selects the last element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the last element of this iterable collection if it is nonempty, None if it is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.lastOption
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.lazyZip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#lazyZip-fffffd84)

```scala
def lazyZip[B](that: Iterable[B]): LazyZip2[List[Int], B, this.type]
```

Analogous to zip except that the elements in each collection are not consumed until a strict operation is invoked on the returned LazyZip2 decorator.

Type parameters: `` B ``.

Parameters, list 1: `` that: Iterable[B] ``.

Returns: `` LazyZip2[List[Int], B, this.type] ``.

Source contract/attributes: Analogous to zip except that the elements in each collection are not consumed until a strict operation is invoked on the returned LazyZip2 decorator. Calls to lazyZip can be chained to support higher arities (up to 4) without incurring the expense of constructing and deconstructing intermediary tuples. val xs = List(1, 2, 3) val res = (xs lazyZip xs lazyZip xs lazyZip xs).map((a, b, c, d) => a + b + c + d) // res == List(4, 8, 12) Type parameters B the type of the second element in each eventual pair Value parameters that the iterable providing the second element of each eventual pair Attributes Returns a decorator LazyZip2 that allows strict operations to be performed on the lazily evaluated pairs or chained calls to lazyZip. Implicit conversion to Iterable[(A, B)] is also supported. Inherited from: Iterable

Invocation template:

```scala
receiver.lazyZip[B](that)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#map-5d3)

```scala
def map[B](f: List[Int] => B): Iterable[B]
```

Builds a new iterable collection by applying a function to all elements of this iterable collection.

Type parameters: `` B ``.

Parameters, list 1: `` f: List[Int] => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Builds a new iterable collection by applying a function to all elements of this iterable collection. Type parameters B the element type of the returned iterable collection . Value parameters f the function to apply to each element. Attributes Returns a new iterable collection resulting from applying the given function f to each element of this iterable collection and collecting the results. Inherited from: IterableOps

Invocation template:

```scala
receiver.map[B](f)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.max ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#max-ffffff34)

```scala
def max[B >: List[Int]](implicit ord: Ordering[B]): List[Int]
```

Finds the largest element.

Type parameters: `` B >: List[Int] ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` List[Int] ``.

Source contract/attributes: Finds the largest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns the largest element of this iterable collection with respect to the ordering ord. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.max[B](using ord)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.maxBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#maxBy-fffff0c5)

```scala
def maxBy[B](f: List[Int] => B)(implicit ord: Ordering[B]): List[Int]
```

Finds the first element which yields the largest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: List[Int] => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` List[Int] ``.

Source contract/attributes: Finds the first element which yields the largest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns the first element of this iterable collection with the largest value measured by function f with respect to the ordering ord. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxBy[B](f)(using ord)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.maxByOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#maxByOption-fffff66f)

```scala
def maxByOption[B](f: List[Int] => B)(implicit ord: Ordering[B]): Option[List[Int]]
```

Finds the first element which yields the largest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: List[Int] => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` Option[List[Int]] ``.

Source contract/attributes: Finds the first element which yields the largest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns an option value containing the first element of this iterable collection with the largest value measured by function f with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxByOption[B](f)(using ord)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.maxOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#maxOption-fffff45e)

```scala
def maxOption[B >: List[Int]](implicit ord: Ordering[B]): Option[List[Int]]
```

Finds the largest element.

Type parameters: `` B >: List[Int] ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` Option[List[Int]] ``.

Source contract/attributes: Finds the largest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns an option value containing the largest element of this iterable collection with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxOption[B](using ord)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.min ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#min-ffffff34)

```scala
def min[B >: List[Int]](implicit ord: Ordering[B]): List[Int]
```

Finds the smallest element.

Type parameters: `` B >: List[Int] ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` List[Int] ``.

Source contract/attributes: Finds the smallest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns the smallest element of this iterable collection with respect to the ordering ord. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.min[B](using ord)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.minBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#minBy-fffff0c5)

```scala
def minBy[B](f: List[Int] => B)(implicit ord: Ordering[B]): List[Int]
```

Finds the first element which yields the smallest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: List[Int] => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` List[Int] ``.

Source contract/attributes: Finds the first element which yields the smallest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns the first element of this iterable collection with the smallest value measured by function f with respect to the ordering ord. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minBy[B](f)(using ord)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.minByOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#minByOption-fffff66f)

```scala
def minByOption[B](f: List[Int] => B)(implicit ord: Ordering[B]): Option[List[Int]]
```

Finds the first element which yields the smallest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: List[Int] => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` Option[List[Int]] ``.

Source contract/attributes: Finds the first element which yields the smallest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns an option value containing the first element of this iterable collection with the smallest value measured by function f with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minByOption[B](f)(using ord)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.minOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#minOption-fffff45e)

```scala
def minOption[B >: List[Int]](implicit ord: Ordering[B]): Option[List[Int]]
```

Finds the smallest element.

Type parameters: `` B >: List[Int] ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` Option[List[Int]] ``.

Source contract/attributes: Finds the smallest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns an option value containing the smallest element of this iterable collection with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minOption[B](using ord)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#mkString-0)

```scala
final def mkString: String
```

Displays all elements of this iterable collection in a string.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Displays all elements of this iterable collection in a string. Delegates to addString, which can be overridden. Attributes Returns a string representation of this iterable collection. In the resulting string the string representations (w.r.t. the method toString) of all elements of this iterable collection follow each other without any separator string. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#mkString-fffff2ca)

```scala
final def mkString(sep: String): String
```

Displays all elements of this iterable collection in a string using a separator string.

Type parameters: none.

Parameters, list 1: `` sep: String ``.

Returns: `` String ``.

Source contract/attributes: Displays all elements of this iterable collection in a string using a separator string. Delegates to addString, which can be overridden. Value parameters sep the separator string. Attributes Returns a string representation of this iterable collection. In the resulting string the string representations (w.r.t. the method toString) of all elements of this iterable collection are separated by the string sep. Example List(1, 2, 3).mkString("|") = "1|2|3" Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString(sep)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#mkString-d94)

```scala
final def mkString(start: String, sep: String, end: String): String
```

Displays all elements of this iterable collection in a string using start, end, and separator strings.

Type parameters: none.

Parameters, list 1: `` start: String ``; `` sep: String ``; `` end: String ``.

Returns: `` String ``.

Source contract/attributes: Displays all elements of this iterable collection in a string using start, end, and separator strings. Delegates to addString, which can be overridden. Value parameters end the ending string. sep the separator string. start the starting string. Attributes Returns a string representation of this iterable collection. The resulting string begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this iterable collection are separated by the string sep. Example List(1, 2, 3).mkString("(", "; ", ")") = "(1; 2; 3)" Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString(start, sep, end)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.nextIndices ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#nextIndices-fffff78f)

```scala
def nextIndices(indices: List[Int]): Option[List[Int]]
```

Given a list of indices corresponding to a row in the factor, returns the list of indices corresponding to the next row. Returns None if the last index list has been reached.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``.

Returns: `` Option[List[Int]] ``.

Source contract/attributes: Given a list of indices corresponding to a row in the factor, returns the list of indices corresponding to the next row. Returns None if the last index list has been reached. Attributes Inherited from: Indices

Invocation template:

```scala
receiver.nextIndices(indices)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.nonEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#nonEmpty-0)

```scala
def nonEmpty: Boolean
```

Tests whether the iterable collection is not empty.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether the iterable collection is not empty. Attributes Returns true if the iterable collection contains at least one element, false otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.nonEmpty
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.partition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#partition-243)

```scala
def partition(p: List[Int] => Boolean): (Iterable[List[Int]], Iterable[List[Int]])
```

A pair of, first, all elements that satisfy predicate p and, second, all elements that do not.

Type parameters: none.

Parameters, list 1: `` p: List[Int] => Boolean ``.

Returns: `` (Iterable[List[Int]], Iterable[List[Int]]) ``.

Source contract/attributes: A pair of, first, all elements that satisfy predicate p and, second, all elements that do not. The two iterable collection correspond to the result of filter and filterNot, respectively. The default implementation provided here needs to traverse the collection twice. Strict collections have an overridden version of partition in StrictOptimizedIterableOps, which requires only a single traversal. Value parameters p the predicate used to test elements Attributes Returns a pair of iterable collections: the first containing all elements that satisfy p, the second containing those that do not Inherited from: IterableOps

Invocation template:

```scala
receiver.partition(p)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.partitionMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#partitionMap-2d1)

```scala
def partitionMap[A1, A2](f: List[Int] => Either[A1, A2]): (Iterable[A1], Iterable[A2])
```

Applies a function f to each element of the iterable collection and returns a pair of iterable collections: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right.

Type parameters: `` A1 ``; `` A2 ``.

Parameters, list 1: `` f: List[Int] => Either[A1, A2] ``.

Returns: `` (Iterable[A1], Iterable[A2]) ``.

Source contract/attributes: Applies a function f to each element of the iterable collection and returns a pair of iterable collections: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right. Example: val xs = $Coll(1, "one", 2, "two", 3, "three") partitionMap { case i: Int => Left(i) case s: String => Right(s) } // xs == ($Coll(1, 2, 3), // $Coll(one, two, three)) Type parameters A1 the element type of the first resulting collection A2 the element type of the second resulting collection Value parameters f the 'split function' mapping the elements of this iterable collection to an scala.util.Either Attributes Returns a pair of iterable collections: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right. Inherited from: IterableOps

Invocation template:

```scala
receiver.partitionMap[A1, A2](f)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#product-533)

```scala
def product[B >: List[Int]](implicit num: Numeric[B]): B
```

Multiplies together the elements of this collection.

Type parameters: `` B >: List[Int] ``.

Context parameters, list 1: `` num: Numeric[B] ``.

Returns: `` B ``.

Source contract/attributes: Multiplies together the elements of this collection. The default implementation uses reduce for a known non-empty collection, foldLeft otherwise. Note: will not terminate for infinite-sized collections. Type parameters B the result type of the * operator. Value parameters num an implicit parameter defining a set of numeric operations which includes the * operator to be used in forming the product. Attributes Returns the product of all elements of this iterable collection with respect to the * operator in num. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.product[B](using num)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.reduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#reduce-fffff3d4)

```scala
def reduce[B >: List[Int]](op: (B, B) => B): B
```

Applies the given binary operator op to all elements of this iterable collection.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` op: (B, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this iterable collection. For each application of the operator, each operand is either an element of this iterable collection or another such application of the operator. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. If this collection is ordered, then for any application of the operator, the element(s) appearing in the left operand will precede those in the right. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. Attributes Returns The result of applying op between all the elements if the iterable collection is nonempty. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduce[B](op)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.reduceLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#reduceLeft-fffff3d4)

```scala
def reduceLeft[B >: List[Int]](op: (B, List[Int]) => B): B
```

Applies the given binary operator op to all elements of this iterable collection, going left to right.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` op: (B, List[Int]) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this iterable collection, going left to right. "Going left to right" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this iterable collection, the result is op( op( op( ... op(x,,1,,, x,,2,,) ... ), x,,n-1,,), x,,n,,). If this collection is not ordered, then for each application of the operator, each right operand is an element. In addition, the leftmost operand is the first element of this iterable collection and each other left operand is itself an application of the operator. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of applying op to all elements of this iterable collection, going left to right. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceLeft[B](op)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.reduceLeftOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#reduceLeftOption-8fe)

```scala
def reduceLeftOption[B >: List[Int]](op: (B, List[Int]) => B): Option[B]
```

If this iterable collection is nonempty, reduces it with the given binary operator op, going left to right.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` op: (B, List[Int]) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this iterable collection is nonempty, reduces it with the given binary operator op, going left to right. The behavior is the same as reduceLeft except that the value is None if the iterable collection is empty. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of reducing this iterable collection with op going left to right if the iterable collection is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceLeftOption[B](op)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.reduceOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#reduceOption-8fe)

```scala
def reduceOption[B >: List[Int]](op: (B, B) => B): Option[B]
```

If this iterable collection is nonempty, reduces it with the given binary operator op.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` op: (B, B) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this iterable collection is nonempty, reduces it with the given binary operator op. The behavior is the same as reduce except that the value is None if the iterable collection is empty. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B A type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. Attributes Returns The result of reducing this iterable collection with op if the iterable collection is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceOption[B](op)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.reduceRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#reduceRight-fffff3d4)

```scala
def reduceRight[B >: List[Int]](op: (List[Int], B) => B): B
```

Applies the given binary operator op to all elements of this iterable collection, going right to left.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` op: (List[Int], B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this iterable collection, going right to left. "Going right to left" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this iterable collection, the result is op(x,,1,,, op(x,,2,,, op( ... op(x,,n-1,,, x,,n,,) ... ))). If this collection is not ordered, then for each application of the operator, each left operand is an element. In addition, the rightmost operand is the last element of this iterable collection and each other right operand is itself an application of the operator. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of applying op to all elements of this iterable collection, going right to left. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceRight[B](op)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.reduceRightOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#reduceRightOption-8fe)

```scala
def reduceRightOption[B >: List[Int]](op: (List[Int], B) => B): Option[B]
```

If this iterable collection is nonempty, reduces it with the given binary operator op, going right to left.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` op: (List[Int], B) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this iterable collection is nonempty, reduces it with the given binary operator op, going right to left. The behavior is the same as reduceRight except that the value is None if the iterable collection is empty. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of reducing this iterable collection with op going right to left if the iterable collection is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceRightOption[B](op)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.scan ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#scan-fffff9e1)

```scala
def scan[B >: List[Int]](z: B)(op: (B, B) => B): Iterable[B]
```

Computes a prefix scan of the elements of the collection.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, B) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Computes a prefix scan of the elements of the collection. Note: The neutral element z may be applied more than once. Type parameters B element type of the resulting collection Value parameters op the associative operator for the scan z neutral element for the operator op Attributes Returns a new iterable collection containing the prefix scan of the elements in this iterable collection Inherited from: IterableOps

Invocation template:

```scala
receiver.scan[B](z)(op)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.scanLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#scanLeft-fffff9e1)

```scala
def scanLeft[B](z: B)(op: (B, List[Int]) => B): Iterable[B]
```

Produces a iterable collection containing cumulative results of applying the operator going left to right, including the initial value.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, List[Int]) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Produces a iterable collection containing cumulative results of applying the operator going left to right, including the initial value. Note: will not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Type parameters B the type of the elements in the resulting collection Value parameters op the binary operator applied to the intermediate result and the element z the initial value Attributes Returns collection with intermediate results Inherited from: IterableOps

Invocation template:

```scala
receiver.scanLeft[B](z)(op)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.scanRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#scanRight-fffff9e1)

```scala
def scanRight[B](z: B)(op: (List[Int], B) => B): Iterable[B]
```

Produces a collection containing cumulative results of applying the operator going right to left. The head of the collection is the last cumulative result.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (List[Int], B) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Produces a collection containing cumulative results of applying the operator going right to left. The head of the collection is the last cumulative result. Note: will not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: Even when applied to a view or a lazy collection it will always force the elements. Example: List(1, 2, 3, 4).scanRight(0)(_ + _) == List(10, 9, 7, 4, 0) Type parameters B the type of the elements in the resulting collection Value parameters op the binary operator applied to the intermediate result and the element z the initial value Attributes Returns collection with intermediate results Inherited from: IterableOps

Invocation template:

```scala
receiver.scanRight[B](z)(op)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.size ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#size-0)

```scala
def size: Int
```

The size of this iterable collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: The size of this iterable collection. Note: will not terminate for infinite-sized collections. Attributes Returns the number of elements in this iterable collection. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.size
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.sizeCompare ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#sizeCompare-32d)

```scala
def sizeCompare(that: Iterable[_]): Int
```

Compares the size of this iterable collection to the size of another Iterable.

Type parameters: none.

Parameters, list 1: `` that: Iterable[_] ``.

Returns: `` Int ``.

Source contract/attributes: Compares the size of this iterable collection to the size of another Iterable. Value parameters that the Iterable whose size is compared with this iterable collection's size. Attributes Returns A value x where x < 0 if this.size < that.size x == 0 if this.size == that.size x > 0 if this.size > that.size The method as implemented here does not call size directly; its running time is O(this.size min that.size) instead of O(this.size + that.size). The method should be overridden if computing size is cheap and knownSize returns -1. Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeCompare(that)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.sizeCompare ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#sizeCompare-fffffbe0)

```scala
def sizeCompare(otherSize: Int): Int
```

Compares the size of this iterable collection to a test value.

Type parameters: none.

Parameters, list 1: `` otherSize: Int ``.

Returns: `` Int ``.

Source contract/attributes: Compares the size of this iterable collection to a test value. Value parameters otherSize the test value that gets compared with the size. Attributes Returns A value x where x < 0 if this.size < otherSize x == 0 if this.size == otherSize x > 0 if this.size > otherSize The default implementation does not call size, which may be arbitrarily expensive. It will use knownSize if nonnegative. Otherwise, it will iterate to compute an element count, with complexity O(size min otherSize). This method should be overridden if computing size is cheap and knownSize returns -1. In that case, the custom implementation should just compare size and otherSize directly. See also sizeIs Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeCompare(otherSize)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.sizeIs ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#sizeIs-0)

```scala
final def sizeIs: SizeCompareOps
```

Returns a value class containing operations for comparing the size of this iterable collection to a test value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` SizeCompareOps ``.

Source contract/attributes: Returns a value class containing operations for comparing the size of this iterable collection to a test value. These operations are implemented in terms of `sizeCompare(Int)`, and allow the following more readable usages: this.sizeIs < size // this.sizeCompare(size) < 0 this.sizeIs <= size // this.sizeCompare(size) <= 0 this.sizeIs == size // this.sizeCompare(size) == 0 this.sizeIs != size // this.sizeCompare(size) != 0 this.sizeIs >= size // this.sizeCompare(size) >= 0 this.sizeIs > size // this.sizeCompare(size) > 0 Attributes Returns a wrapper that allows size comparisons using operators such as =, and > Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeIs
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.slice ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#slice-573)

```scala
def slice(from: Int, until: Int): Iterable[List[Int]]
```

Selects an interval of elements. The returned iterable collection is made up of all elements x which satisfy the invariant:

Type parameters: none.

Parameters, list 1: `` from: Int ``; `` until: Int ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Selects an interval of elements. The returned iterable collection is made up of all elements x which satisfy the invariant: from <= indexOf(x) < until Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters from the lowest index to include from this iterable collection. until the lowest index to EXCLUDE from this iterable collection. Attributes Returns a iterable collection containing the elements greater than or equal to index from extending up to (but not including) index until of this iterable collection. Example List('a', 'b', 'c', 'd', 'e').slice(1, 3) == List('b', 'c') Inherited from: IterableOps

Invocation template:

```scala
receiver.slice(from, until)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.sliding ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#sliding-fffff156)

```scala
def sliding(size: Int, step: Int): Iterator[Iterable[List[Int]]]
```

Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped).

Type parameters: none.

Parameters, list 1: `` size: Int ``; `` step: Int ``.

Returns: `` Iterator[Iterable[List[Int]]] ``.

Source contract/attributes: Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped). The returned iterator will be empty when called on an empty collection. The last element the iterator produces may be smaller than the window size when the original collection isn't exhausted by the window before it and its last element isn't skipped by the step before it. Value parameters size the number of elements per group step the distance between the first elements of successive groups Attributes Returns An iterator producing iterable collections of size size, except the last element (which may be the only element) will be smaller if there are fewer than size elements remaining to be grouped. See also scala.collection.Iterator, method sliding Example List(1, 2, 3, 4, 5).sliding(2, 2) = Iterator(List(1, 2), List(3, 4), List(5)) List(1, 2, 3, 4, 5, 6).sliding(2, 3) = Iterator(List(1, 2), List(4, 5)) Inherited from: IterableOps

Invocation template:

```scala
receiver.sliding(size, step)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.sliding ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#sliding-fffffbef)

```scala
def sliding(size: Int): Iterator[Iterable[List[Int]]]
```

Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped).

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Iterator[Iterable[List[Int]]] ``.

Source contract/attributes: Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped). An empty collection returns an empty iterator, and a non-empty collection containing fewer elements than the window size returns an iterator that will produce the original collection as its only element. Value parameters size the number of elements per group Attributes Returns An iterator producing iterable collections of size size, except for a non-empty collection with less than size elements, which returns an iterator that produces the source collection itself as its only element. See also scala.collection.Iterator, method sliding Example List().sliding(2) = empty iterator List(1).sliding(2) = Iterator(List(1)) List(1, 2).sliding(2) = Iterator(List(1, 2)) List(1, 2, 3).sliding(2) = Iterator(List(1, 2), List(2, 3)) Inherited from: IterableOps

Invocation template:

```scala
receiver.sliding(size)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.span ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#span-243)

```scala
def span(p: List[Int] => Boolean): (Iterable[List[Int]], Iterable[List[Int]])
```

Splits this iterable collection into a prefix/suffix pair according to a predicate.

Type parameters: none.

Parameters, list 1: `` p: List[Int] => Boolean ``.

Returns: `` (Iterable[List[Int]], Iterable[List[Int]]) ``.

Source contract/attributes: Splits this iterable collection into a prefix/suffix pair according to a predicate. Note: c span p is equivalent to (but possibly more efficient than) (c takeWhile p, c dropWhile p), provided the evaluation of the predicate p does not cause any side effects. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the test predicate Attributes Returns a pair consisting of the longest prefix of this iterable collection whose elements all satisfy p, and the rest of this iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.span(p)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.splitAt ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#splitAt-fffffbd9)

```scala
override def splitAt(n: Int): (Iterable[List[Int]], Iterable[List[Int]])
```

Splits this iterable collection into a prefix/suffix pair at a given position.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` (Iterable[List[Int]], Iterable[List[Int]]) ``.

Source contract/attributes: Splits this iterable collection into a prefix/suffix pair at a given position. Note: c splitAt n is equivalent to (but possibly more efficient than) (c take n, c drop n). Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the position at which to split. Attributes Returns a pair of iterable collections consisting of the first n elements of this iterable collection, and the other elements. Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.splitAt(n)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.stepper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#stepper-190)

```scala
def stepper[S <: Stepper[_]](implicit shape: StepperShape[List[Int], S]): S
```

Returns a scala.collection.Stepper for the elements of this collection.

Type parameters: `` S <: Stepper[_] ``.

Context parameters, list 1: `` shape: StepperShape[List[Int], S] ``.

Returns: `` S ``.

Source contract/attributes: Returns a scala.collection.Stepper for the elements of this collection. The Stepper enables creating a Java stream to operate on the collection, see scala.jdk.StreamConverters. For collections holding primitive values, the Stepper can be used as an iterator which doesn't box the elements. The implicit scala.collection.StepperShape parameter defines the resulting Stepper type according to the element type of this collection. For collections of Int, Short, Byte or Char, an scala.collection.IntStepper is returned For collections of Double or Float, a scala.collection.DoubleStepper is returned For collections of Long a scala.collection.LongStepper is returned For any other element type, an scala.collection.AnyStepper is returned Note that this method is overridden in subclasses and the return type is refined to S with EfficientSplit, for example scala.collection.IndexedSeqOps.stepper. For Steppers marked with scala.collection.Stepper.EfficientSplit, the converters in scala.jdk.StreamConverters allow creating parallel streams, whereas bare Steppers can be converted only to sequential streams. Type parameters S the type of the returned Stepper, determined by the implicit StepperShape Attributes Inherited from: IterableOnce

Invocation template:

```scala
receiver.stepper[S](using shape)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.sum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#sum-533)

```scala
def sum[B >: List[Int]](implicit num: Numeric[B]): B
```

Sums the elements of this collection.

Type parameters: `` B >: List[Int] ``.

Context parameters, list 1: `` num: Numeric[B] ``.

Returns: `` B ``.

Source contract/attributes: Sums the elements of this collection. The default implementation uses reduce for a known non-empty collection, foldLeft otherwise. Note: will not terminate for infinite-sized collections. Type parameters B the result type of the + operator. Value parameters num an implicit parameter defining a set of numeric operations which includes the + operator to be used in forming the sum. Attributes Returns the sum of all elements of this iterable collection with respect to the + operator in num. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.sum[B](using num)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.tail ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#tail-0)

```scala
def tail: Iterable[List[Int]]
```

The rest of the collection without its first element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: The rest of the collection without its first element. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.tail
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.tails ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#tails-0)

```scala
def tails: Iterator[Iterable[List[Int]]]
```

Iterates over the tails of this iterable collection. The first value will be this iterable collection and the final one will be an empty iterable collection, with the intervening values the results of successive applications of tail.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Iterable[List[Int]]] ``.

Source contract/attributes: Iterates over the tails of this iterable collection. The first value will be this iterable collection and the final one will be an empty iterable collection, with the intervening values the results of successive applications of tail. Attributes Returns an iterator over all the tails of this iterable collection Example List(1,2,3).tails = Iterator(List(1,2,3), List(2,3), List(3), Nil) Inherited from: IterableOps

Invocation template:

```scala
receiver.tails
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.take ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#take-aba)

```scala
def take(n: Int): Iterable[List[Int]]
```

Selects the first n elements.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Selects the first n elements. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to take from this iterable collection. Attributes Returns a iterable collection consisting only of the first n elements of this iterable collection, or else the whole iterable collection, if it has less than n elements. If n is negative, returns an empty iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.take(n)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.takeRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#takeRight-aba)

```scala
def takeRight(n: Int): Iterable[List[Int]]
```

Selects the last n elements.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Selects the last n elements. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to take from this iterable collection. Attributes Returns a iterable collection consisting only of the last n elements of this iterable collection, or else the whole iterable collection, if it has less than n elements. If n is negative, returns an empty iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.takeRight(n)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.takeWhile ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#takeWhile-c24)

```scala
def takeWhile(p: List[Int] => Boolean): Iterable[List[Int]]
```

Takes longest prefix of elements that satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` p: List[Int] => Boolean ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Takes longest prefix of elements that satisfy a predicate. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p The predicate used to test elements. Attributes Returns the longest prefix of this iterable collection whose elements all satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.takeWhile(p)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.tapEach ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#tapEach-5d3)

```scala
override def tapEach[U](f: List[Int] => U): Iterable[List[Int]]
```

Applies a side-effecting function to each element in this collection. Strict collections will apply f to their elements immediately, while lazy collections like Views and LazyLists will only apply f on each element if and when that element is evaluated, and each time that element is evaluated.

Type parameters: `` U ``.

Parameters, list 1: `` f: List[Int] => U ``.

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Applies a side-effecting function to each element in this collection. Strict collections will apply f to their elements immediately, while lazy collections like Views and LazyLists will only apply f on each element if and when that element is evaluated, and each time that element is evaluated. Type parameters U the return type of f Value parameters f a function to apply to each element in this iterable collection Attributes Returns The same logical collection as this Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.tapEach[U](f)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.to ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#to-fffff006)

```scala
def to[C1](factory: Factory[List[Int], C1]): C1
```

Given a collection factory factory, converts this iterable collection to the appropriate representation for the current element type A. Example uses:

Type parameters: `` C1 ``.

Parameters, list 1: `` factory: Factory[List[Int], C1] ``.

Returns: `` C1 ``.

Source contract/attributes: Given a collection factory factory, converts this iterable collection to the appropriate representation for the current element type A. Example uses: import scala.collection.mutable.ArrayBuffer import scala.collection.immutable.BitSet val xs: Iterable[Int] = Seq(1, 2, 3, 4, 5) xs.to(List) xs.to(ArrayBuffer) xs.to(BitSet) // for xs: Iterable[Int] Type parameters C1 the target collection type Value parameters factory the factory for the target collection type Attributes Returns a new collection of type C1 containing all elements of this iterable collection Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.to[C1](factory)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.toArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#toArray-fffff5df)

```scala
def toArray[B >: List[Int] : ClassTag]: Array[B]
```

Converts this iterable collection to an Array.

Type parameters: `` B >: List[Int] : ClassTag ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Array[B] ``.

Source contract/attributes: Converts this iterable collection to an Array. Implementation note: DO NOT call Array.from from this method. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This iterable collection as an Array[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toArray[B]
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.toBuffer ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#toBuffer-fffff87f)

```scala
final def toBuffer[B >: List[Int]]: Buffer[B]
```

Converts this iterable collection to a Buffer.

Type parameters: `` B >: List[Int] ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Buffer[B] ``.

Source contract/attributes: Converts this iterable collection to a Buffer. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This iterable collection as a Buffer[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toBuffer[B]
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.toIndexedSeq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#toIndexedSeq-0)

```scala
def toIndexedSeq: IndexedSeq[List[Int]]
```

Converts this iterable collection to an IndexedSeq.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IndexedSeq[List[Int]] ``.

Source contract/attributes: Converts this iterable collection to an IndexedSeq. Attributes Returns This iterable collection as an IndexedSeq[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toIndexedSeq
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.toList ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#toList-0)

```scala
def toList: List[List[Int]]
```

Converts this iterable collection to a List.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[List[Int]] ``.

Source contract/attributes: Converts this iterable collection to a List. Attributes Returns This iterable collection as a List[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toList
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.toMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#toMap-c0c)

```scala
def toMap[K, V](implicit ev: List[Int] <:< (K, V)): Map[K, V]
```

Converts this iterable collection to a Map, given an implicit coercion from the iterable collection's type to a key-value tuple.

Type parameters: `` K ``; `` V ``.

Context parameters, list 1: `` ev: List[Int] <:< (K, V) ``.

Returns: `` Map[K, V] ``.

Source contract/attributes: Converts this iterable collection to a Map, given an implicit coercion from the iterable collection's type to a key-value tuple. Type parameters K The key type for the resulting map. V The value type for the resulting map. Value parameters ev an implicit evidence that A is a subtype of (K, V) Attributes Returns This iterable collection as a Map[K, V]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toMap[K, V](using ev)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.toSeq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#toSeq-0)

```scala
def toSeq: Seq[List[Int]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Seq[List[Int]] ``.

Source contract/attributes: Attributes Returns This iterable collection as a Seq[A]. This is equivalent to to(Seq) but might be faster. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toSeq
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.toSet ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#toSet-fffffe9f)

```scala
def toSet[B >: List[Int]]: Set[B]
```

Converts this iterable collection to a Set.

Type parameters: `` B >: List[Int] ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[B] ``.

Source contract/attributes: Converts this iterable collection to a Set. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This iterable collection as a Set[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toSet[B]
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#toString-665)

```scala
override def toString(): String
```

Converts this iterable collection to a string.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Converts this iterable collection to a string. Attributes Returns a string representation of this collection. By default this string consists of the className of this iterable collection, followed by all elements separated by commas and enclosed in parentheses. Definition Classes Iterable -> Any Inherited from: Iterable

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.toVector ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#toVector-0)

```scala
def toVector: Vector[List[Int]]
```

Converts this iterable collection to a Vector.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Vector[List[Int]] ``.

Source contract/attributes: Converts this iterable collection to a Vector. Attributes Returns This iterable collection as a Vector[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toVector
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.transpose ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#transpose-5d3)

```scala
def transpose[B](implicit asIterable: List[Int] => Iterable[B]): Iterable[Iterable[B]]
```

Transposes this iterable collection of iterable collections into a iterable collection of iterable collections.

Type parameters: `` B ``.

Context parameters, list 1: `` asIterable: List[Int] => Iterable[B] ``.

Returns: `` Iterable[Iterable[B]] ``.

Source contract/attributes: Transposes this iterable collection of iterable collections into a iterable collection of iterable collections. The resulting collection's type will be guided by the static type of iterable collection. For example: val xs = List( Set(1, 2, 3), Set(4, 5, 6)).transpose // xs == List( // List(1, 4), // List(2, 5), // List(3, 6)) val ys = Vector( List(1, 2, 3), List(4, 5, 6)).transpose // ys == Vector( // Vector(1, 4), // Vector(2, 5), // Vector(3, 6)) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of the elements of each iterable collection. Value parameters asIterable an implicit conversion which asserts that the element type of this iterable collection is an Iterable. Attributes Returns a two-dimensional iterable collection of iterable collections which has as nth row the nth column of this iterable collection. Throws IllegalArgumentException if all collections in this iterable collection are not of the same size. Inherited from: IterableOps

Invocation template:

```scala
receiver.transpose[B](using asIterable)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.unzip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#unzip-2d1)

```scala
def unzip[A1, A2](implicit asPair: List[Int] => (A1, A2)): (Iterable[A1], Iterable[A2])
```

Converts this iterable collection of pairs into two collections of the first and second half of each pair.

Type parameters: `` A1 ``; `` A2 ``.

Context parameters, list 1: `` asPair: List[Int] => (A1, A2) ``.

Returns: `` (Iterable[A1], Iterable[A2]) ``.

Source contract/attributes: Converts this iterable collection of pairs into two collections of the first and second half of each pair. val xs = $Coll( (1, "one"), (2, "two"), (3, "three")).unzip // xs == ($Coll(1, 2, 3), // $Coll(one, two, three)) Type parameters A1 the type of the first half of the element pairs A2 the type of the second half of the element pairs Value parameters asPair an implicit conversion which asserts that the element type of this iterable collection is a pair. Attributes Returns a pair of iterable collections, containing the first, respectively second half of each element pair of this iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.unzip[A1, A2](using asPair)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.unzip3 ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#unzip3-a31)

```scala
def unzip3[A1, A2, A3](implicit asTriple: List[Int] => (A1, A2, A3)): (Iterable[A1], Iterable[A2], Iterable[A3])
```

Converts this iterable collection of triples into three collections of the first, second, and third element of each triple.

Type parameters: `` A1 ``; `` A2 ``; `` A3 ``.

Context parameters, list 1: `` asTriple: List[Int] => (A1, A2, A3) ``.

Returns: `` (Iterable[A1], Iterable[A2], Iterable[A3]) ``.

Source contract/attributes: Converts this iterable collection of triples into three collections of the first, second, and third element of each triple. val xs = $Coll( (1, "one", '1'), (2, "two", '2'), (3, "three", '3')).unzip3 // xs == ($Coll(1, 2, 3), // $Coll(one, two, three), // $Coll(1, 2, 3)) Type parameters A1 the type of the first member of the element triples A2 the type of the second member of the element triples A3 the type of the third member of the element triples Value parameters asTriple an implicit conversion which asserts that the element type of this iterable collection is a triple. Attributes Returns a triple of iterable collections, containing the first, second, respectively third member of each element triple of this iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.unzip3[A1, A2, A3](using asTriple)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.view ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#view-0)

```scala
def view: View[List[Int]]
```

A view over the elements of this collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` View[List[Int]] ``.

Source contract/attributes: A view over the elements of this collection. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.view
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.withFilter ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#withFilter-fffffb75)

```scala
def withFilter(p: List[Int] => Boolean): WithFilter[List[Int], Iterable]
```

Creates a non-strict filter of this iterable collection.

Type parameters: none.

Parameters, list 1: `` p: List[Int] => Boolean ``.

Returns: `` WithFilter[List[Int], Iterable] ``.

Source contract/attributes: Creates a non-strict filter of this iterable collection. Note: the difference between c filter p and c withFilter p is that the former creates a new collection, whereas the latter only restricts the domain of subsequent map, flatMap, foreach, and withFilter operations. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the predicate used to test elements. Attributes Returns an object of class WithFilter, which supports map, flatMap, foreach, and withFilter operations. All these operations apply to those elements of this iterable collection which satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.withFilter(p)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.zip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#zip-1dd)

```scala
def zip[B](that: IterableOnce[B]): Iterable[(List[Int], B)]
```

Returns a iterable collection formed from this iterable collection and another iterable collection by combining corresponding elements in pairs. If one of the two collections is longer than the other, its remaining elements are ignored.

Type parameters: `` B ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Returns: `` Iterable[(List[Int], B)] ``.

Source contract/attributes: Returns a iterable collection formed from this iterable collection and another iterable collection by combining corresponding elements in pairs. If one of the two collections is longer than the other, its remaining elements are ignored. Type parameters B the type of the second half of the returned pairs Value parameters that The iterable providing the second half of each result pair Attributes Returns a new iterable collection containing pairs consisting of corresponding elements of this iterable collection and that. The length of the returned collection is the minimum of the lengths of this iterable collection and that. Inherited from: IterableOps

Invocation template:

```scala
receiver.zip[B](that)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.zipAll ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#zipAll-fffff401)

```scala
def zipAll[A1 >: List[Int], B](that: Iterable[B], thisElem: A1, thatElem: B): Iterable[(A1, B)]
```

Returns a iterable collection formed from this iterable collection and another iterable collection by combining corresponding elements in pairs. If one of the two collections is shorter than the other, placeholder elements are used to extend the shorter collection to the length of the longer.

Type parameters: `` A1 >: List[Int] ``; `` B ``.

Parameters, list 1: `` that: Iterable[B] ``; `` thisElem: A1 ``; `` thatElem: B ``.

Returns: `` Iterable[(A1, B)] ``.

Source contract/attributes: Returns a iterable collection formed from this iterable collection and another iterable collection by combining corresponding elements in pairs. If one of the two collections is shorter than the other, placeholder elements are used to extend the shorter collection to the length of the longer. Type parameters A1 the type of the first element in each result pair (supertype of A) B the type of the second element in each result pair Value parameters that the iterable providing the second half of each result pair thatElem the element to be used to fill up the result if that is shorter than this iterable collection. thisElem the element to be used to fill up the result if this iterable collection is shorter than that. Attributes Returns a new iterable collection containing pairs consisting of corresponding elements of this iterable collection and that. The length of the returned collection is the maximum of the lengths of this iterable collection and that. If this iterable collection is shorter than that, thisElem values are used to pad the result. If that is shorter than this iterable collection, thatElem values are used to pad the result. Inherited from: IterableOps

Invocation template:

```scala
receiver.zipAll[A1, B](that, thisElem, thatElem)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.zipWithIndex ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#zipWithIndex-0)

```scala
def zipWithIndex: Iterable[(List[Int], Int)]
```

Zips this iterable collection with its indices.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[(List[Int], Int)] ``.

Source contract/attributes: Zips this iterable collection with its indices. Attributes Returns A new iterable collection containing pairs consisting of all elements of this iterable collection paired with their index. Indices start at 0. Example List("a", "b", "c").zipWithIndex == List(("a", 0), ("b", 1), ("c", 2)) Inherited from: IterableOps

Invocation template:

```scala
receiver.zipWithIndex
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.++: ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#++:-1dd)

```scala
def ++:[B >: List[Int]](that: IterableOnce[B]): Iterable[B]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use ++ instead of ++: for collections of type Iterable Inherited from: IterableOps

Invocation template:

```scala
receiver.++:[B](that)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices./: ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#/:-fffff9e1)

```scala
final def /:[B](z: B)(op: (B, List[Int]) => B): B
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, List[Int]) => B ``.

Returns: `` B ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use foldLeft instead of /: Inherited from: IterableOnceOps

Invocation template:

```scala
receiver./:[B](z)(op)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.:\ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#:\-fffff9e1)

```scala
final def :\[B](z: B)(op: (List[Int], B) => B): B
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (List[Int], B) => B ``.

Returns: `` B ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use foldRight instead of :\\ Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.:\[B](z)(op)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.aggregate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#aggregate-952)

```scala
def aggregate[B](z: => B)(seqop: (B, List[Int]) => B, combop: (B, B) => B): B
```

Aggregates the results of applying an operator to subsequent elements.

Type parameters: `` B ``.

Parameters, list 1: `` z: => B ``.

Parameters, list 2: `` seqop: (B, List[Int]) => B ``; `` combop: (B, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Aggregates the results of applying an operator to subsequent elements. Since this method degenerates to foldLeft for sequential (non-parallel) collections, where the combining operation is ignored, it is advisable to prefer foldLeft for that case. For parallel collections, use the aggregate method specified by scala.collection.parallel.ParIterableLike. Type parameters B the result type, produced by seqop, combop, and by this function as a final result. Value parameters combop an associative operator for combining sequential results, unused for sequential collections. seqop the binary operator used to accumulate the result. z the start value, a neutral element for seqop. Attributes Deprecated [Since version 2.13.0] For sequential collections, prefer `foldLeft(z)(seqop)`. For parallel collections, use `ParIterableLike#aggregate`. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.aggregate[B](z)(seqop, combop)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.companion ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#companion-0)

```scala
def companion: IterableFactory[Iterable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IterableFactory[Iterable] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use iterableFactory instead Inherited from: IterableOps

Invocation template:

```scala
receiver.companion
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.copyToBuffer ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#copyToBuffer-f0b)

```scala
final def copyToBuffer[B >: List[Int]](dest: Buffer[B]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B >: List[Int] ``.

Parameters, list 1: `` dest: Buffer[B] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use `dest ++= coll` instead Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToBuffer[B](dest)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.hasDefiniteSize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#hasDefiniteSize-0)

```scala
def hasDefiniteSize: Boolean
```

Tests whether this iterable collection is known to have a finite size. All strict collections are known to have finite size. For a non-strict collection such as Stream, the predicate returns **true** if all elements have been computed. It returns **false** if the stream is not yet evaluated to the end. Non-empty Iterators usually return **false** even if they were created from a collection with a known finite size.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this iterable collection is known to have a finite size. All strict collections are known to have finite size. For a non-strict collection such as Stream, the predicate returns **true** if all elements have been computed. It returns **false** if the stream is not yet evaluated to the end. Non-empty Iterators usually return **false** even if they were created from a collection with a known finite size. Note: many collection methods will not work on collections of infinite sizes. The typical failure mode is an infinite loop. These methods always attempt a traversal without checking first that hasDefiniteSize returns **true**. However, checking hasDefiniteSize can provide an assurance that size is well-defined and non-termination is not a concern. Attributes Returns **true** if this collection is known to have finite size, **false** otherwise. See also method knownSize for a more useful alternative Deprecated [Since version 2.13.0] Check .knownSize instead of .hasDefiniteSize for more actionable information (see scaladoc for details) This method is deprecated in 2.13 because it does not provide any actionable information. As noted above, even the collection library itself does not use it. When there is no guarantee that a collection is finite, it is generally best to attempt a computation anyway and document that it will not terminate for infinite collections rather than backing out because this would prevent performing the computation on collections that are in fact finite even though hasDefiniteSize returns false. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.hasDefiniteSize
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.repr ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#repr-0)

```scala
final def repr: Iterable[List[Int]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use coll instead of repr in a collection implementation, use the collection value itself from the outside Inherited from: IterableOps

Invocation template:

```scala
receiver.repr
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.seq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#seq-0)

```scala
def seq: this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` this.type ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Iterable.seq always returns the iterable itself Inherited from: Iterable

Invocation template:

```scala
receiver.seq
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.toIterable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#toIterable-0)

```scala
final def toIterable: this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` this.type ``.

Source contract/attributes: Attributes Returns This collection as an Iterable[A]. No new collection will be built if this is already an Iterable[A]. Deprecated [Since version 2.13.7] toIterable is internal and will be made protected; its name is similar to `toList` or `toSeq`, but it doesn\'t copy non-immutable collections Inherited from: Iterable

Invocation template:

```scala
receiver.toIterable
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.toIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#toIterator-0)

```scala
final def toIterator: Iterator[List[Int]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[List[Int]] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use .iterator instead of .toIterator Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toIterator
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.toStream ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#toStream-0)

```scala
final def toStream: Stream[List[Int]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Stream[List[Int]] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use .to(LazyList) instead of .toStream Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toStream
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.toTraversable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#toTraversable-0)

```scala
final def toTraversable: Iterable[List[Int]]
```

Converts this iterable collection to an unspecified Iterable. Will return the same collection if this instance is already Iterable.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[List[Int]] ``.

Source contract/attributes: Converts this iterable collection to an unspecified Iterable. Will return the same collection if this instance is already Iterable. Attributes Returns An Iterable containing all elements of this iterable collection. Deprecated [Since version 2.13.0] toTraversable is internal and will be made protected; its name is similar to `toList` or `toSeq`, but it doesn\'t copy non-immutable collections Inherited from: IterableOps

Invocation template:

```scala
receiver.toTraversable
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.SparseIndices.view ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor$SparseIndices.html#view-fffff78d)

```scala
def view(from: Int, until: Int): View[List[Int]]
```

A view over a slice of the elements of this collection.

Type parameters: none.

Parameters, list 1: `` from: Int ``; `` until: Int ``.

Returns: `` View[List[Int]] ``.

Source contract/attributes: A view over a slice of the elements of this collection. Attributes Deprecated [Since version 2.13.0] Use .view.slice(from, until) instead of .view(from, until) Inherited from: IterableOps

Invocation template:

```scala
receiver.view(from, until)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.combination ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#combination-eb2)

```scala
override def combination(that: Factor[T], op: (T, T) => T): Factor[T]
```

produces a new Factor from the combined input variables of the input factors.

Type parameters: none.

Parameters, list 1: `` that: Factor[T] ``; `` op: (T, T) => T ``.

Returns: `` Factor[T] ``.

Source contract/attributes: produces a new Factor from the combined input variables of the input factors. The factor values are computed using the corresponding values from the input factors. This correspondence is determined by the overlap between the variables of this factor and the multiplier factor. When indices from this factor and indices from the multiplier match on the overlapping variables, a new set of indices is constructed for the new factor based on the two input indices and a new value is computed by applying the op to the two input values. The new factor is updated with the new indices and the new value. Value parameters op The operation used to combine (multiply) factor values that The Factor to combine with this one Attributes Returns The new Factor containing the combined values of the inputs Definition Classes BasicFactor -> Factor

Invocation template:

```scala
receiver.combination(that, op)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.contentsContains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#contentsContains-fffff04e)

```scala
def contentsContains(index: List[Int]): Boolean
```

Test whether a factor contains a value for a specific index

Type parameters: none.

Parameters, list 1: `` index: List[Int] ``.

Returns: `` Boolean ``.

Source contract/attributes: Test whether a factor contains a value for a specific index Attributes

Invocation template:

```scala
receiver.contentsContains(index)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.createFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#createFactor-69e)

```scala
override def createFactor[T](_parents: List[Variable[_]], _output: List[Variable[_]], _semiring: Semiring[T] = ...): Factor[T]
```

Creates a new factor of the same type

Type parameters: `` T ``.

Parameters, list 1: `` _parents: List[Variable[_]] ``; `` _output: List[Variable[_]] ``; `` _semiring: Semiring[T] = ... ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Creates a new factor of the same type Attributes Definition Classes Factor

Invocation template:

```scala
receiver.createFactor[T](_parents, _output, _semiring)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#get-fffff1e5)

```scala
override def get(indices: List[Int]): T
```

Get the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined. Rows with default values will be missing, so supply the missing value for these rows

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``.

Returns: `` T ``.

Source contract/attributes: Get the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined. Rows with default values will be missing, so supply the missing value for these rows Attributes Definition Classes Factor

Invocation template:

```scala
receiver.get(indices)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.getContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#getContents-fffffd3a)

```scala
def getContents(): Iterable[T]
```

Get the contents of a factor as a traversable (note: no indicies provided)

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: Get the contents of a factor as a traversable (note: no indicies provided) Attributes

Invocation template:

```scala
receiver.getContents()
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.getIndices ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#getIndices-0)

```scala
override def getIndices: Indices
```

List the indices with non-default values

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Indices ``.

Source contract/attributes: List the indices with non-default values Attributes Definition Classes Factor

Invocation template:

```scala
receiver.getIndices
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.mapTo ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#mapTo-fffff0ed)

```scala
override def mapTo[U](fn: T => U, _semiring: Semiring[U] = ...): Factor[U]
```

Creates a new SparseFactor by applying the supplied function to all values of this factor.

Type parameters: `` U ``.

Parameters, list 1: `` fn: T => U ``; `` _semiring: Semiring[U] = ... ``.

Returns: `` Factor[U] ``.

Source contract/attributes: Creates a new SparseFactor by applying the supplied function to all values of this factor. The new factor can have a new type parameter as well as a new semiring Value parameters fn The function to apply to each value of this Factor semiring A semiring to be used in the new Factor Attributes Definition Classes BasicFactor -> Factor

Invocation template:

```scala
receiver.mapTo[U](fn, _semiring)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.recordArgMax ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#recordArgMax-fffff592)

```scala
override def recordArgMax[U](variable: Variable[U], comparator: (T, T) => Boolean, _semiring: Semiring[U] = ...): Factor[U]
```

Returns a factor that maps values of the other variables to the value of the given variable that maximizes the entry associated with that value, according to some maximization function. comparator defines the maximization. It returns true iff its second argument is greater than its first.

Type parameters: `` U ``.

Parameters, list 1: `` variable: Variable[U] ``; `` comparator: (T, T) => Boolean ``; `` _semiring: Semiring[U] = ... ``.

Returns: `` Factor[U] ``.

Source contract/attributes: Returns a factor that maps values of the other variables to the value of the given variable that maximizes the entry associated with that value, according to some maximization function. comparator defines the maximization. It returns true iff its second argument is greater than its first. Type parameters T The type of entries of this factor. U The type of element whose value is being recorded. The resulting factor maps values of other variables in this factor to this type. Attributes Definition Classes BasicFactor -> Factor

Invocation template:

```scala
receiver.recordArgMax[U](variable, comparator, _semiring)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.set ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#set-ffffff94)

```scala
def set(indices: List[Int], value: T): Factor[T]
```

Set the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``; `` value: T ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Set the value associated with a row. The row is identified by an list of indices into the ranges of the variables over which the factor is defined. Attributes

Invocation template:

```scala
receiver.set(indices, value)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.stringContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#stringContents-665)

```scala
def stringContents(): String
```

Return the contents of a factor as a string

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Return the contents of a factor as a string Attributes

Invocation template:

```scala
receiver.stringContents()
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.contains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#contains-fffff04e)

```scala
def contains(index: List[Int]): Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` index: List[Int] ``.

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.contains(index)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.convertIndicesToValues ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#convertIndicesToValues-5c0)

```scala
def convertIndicesToValues(indices: List[Int]): List[Extended[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``.

Returns: `` List[Extended[_]] ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.convertIndicesToValues(indices)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.deDuplicate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#deDuplicate-fffffbaf)

```scala
override def deDuplicate(): Factor[T]
```

Returns a new Factor with duplicate variable(s) removed

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Factor[T] ``.

Source contract/attributes: Returns a new Factor with duplicate variable(s) removed Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.deDuplicate()
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.fillByRule ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#fillByRule-fffffa40)

```scala
override def fillByRule(rule: List[Extended[_]] => T): Factor[T]
```

Fill the contents of this factor by applying a rule to every combination of parent and output values.

Type parameters: none.

Parameters, list 1: `` rule: List[Extended[_]] => T ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Fill the contents of this factor by applying a rule to every combination of parent and output values. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.fillByRule(rule)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.foldLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#foldLeft-fffffe32)

```scala
def foldLeft(initial: T, fn: (T, T) => T): T
```

Fold the given function through the contents of the factor, beginning with the given initial values

Type parameters: none.

Parameters, list 1: `` initial: T ``; `` fn: (T, T) => T ``.

Returns: `` T ``.

Source contract/attributes: Fold the given function through the contents of the factor, beginning with the given initial values Attributes Inherited from: Factor

Invocation template:

```scala
receiver.foldLeft(initial, fn)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.generateIndices ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#generateIndices-0)

```scala
def generateIndices: Indices
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Indices ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.generateIndices
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.hasStar ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#hasStar-0)

```scala
def hasStar: Boolean
```

Indicates if any of this Factor's variables has Star

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Indicates if any of this Factor's variables has Star Attributes Inherited from: Factor

Invocation template:

```scala
receiver.hasStar
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.isEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#isEmpty-0)

```scala
def isEmpty: Boolean
```

Indicates if this Factor has any variables

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Indicates if this Factor has any variables Attributes Inherited from: Factor

Invocation template:

```scala
receiver.isEmpty
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.marginalizeTo ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#marginalizeTo-3da)

```scala
def marginalizeTo(targets: Variable[_]*): Factor[T]
```

Returns the marginalization of the factor to a variable according to the addition function in this factor's semiring. This involves summing out all other variables.

Type parameters: none.

Parameters, list 1: `` targets: Variable[_]* ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the marginalization of the factor to a variable according to the addition function in this factor's semiring. This involves summing out all other variables. Attributes Inherited from: Factor

Invocation template:

```scala
receiver.marginalizeTo(targets*)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.marginalizeToWithSum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#marginalizeToWithSum-fffff0ec)

```scala
override def marginalizeToWithSum(sum: (T, T) => T, targets: Variable[_]*): Factor[T]
```

Returns the marginalization of the factor to a variable according to the given addition function. Unlike marginalizeTo, this uses the provided sum function. This is useful e.g. to easily switch between max-product and sum-product operations when the data within are unchanged and the product operation is the same.

Type parameters: none.

Parameters, list 1: `` sum: (T, T) => T ``; `` targets: Variable[_]* ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the marginalization of the factor to a variable according to the given addition function. Unlike marginalizeTo, this uses the provided sum function. This is useful e.g. to easily switch between max-product and sum-product operations when the data within are unchanged and the product operation is the same. The returned factor uses the semiring associated with this factor; it does not override the sum function of the semiring with the function given here. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.marginalizeToWithSum(sum, targets*)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#product-fffffd5e)

```scala
override def product(that: Factor[T]): Factor[T]
```

returns the product of this factor with another factor according to a given multiplication function. The product is associated with the combined variables of the two inputs, and the value associated with an assignment is the product of the values in the two inputs.

Type parameters: none.

Parameters, list 1: `` that: Factor[T] ``.

Returns: `` Factor[T] ``.

Source contract/attributes: returns the product of this factor with another factor according to a given multiplication function. The product is associated with the combined variables of the two inputs, and the value associated with an assignment is the product of the values in the two inputs. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.product(that)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.sumOver ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#sumOver-fffffbe5)

```scala
override def sumOver(variable: Variable[_], sum: (T, T) => T = ...): Factor[T]
```

Returns the summation of the factor over a variable according to an addition function. The result is associated with all the variables in the input except for the summed over variable and the value for a set of assignments is the sum of the values of the corresponding assignments in the input.

Type parameters: none.

Parameters, list 1: `` variable: Variable[_] ``; `` sum: (T, T) => T = ... ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the summation of the factor over a variable according to an addition function. The result is associated with all the variables in the input except for the summed over variable and the value for a set of assignments is the sum of the values of the corresponding assignments in the input. If no funciton is provided, this defaults to the sum function in this factor's semiring. Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.sumOver(variable, sum)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.toReadableString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#toReadableString-0)

```scala
override def toReadableString: String
```

Produce a readable string representation of the factor

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Produce a readable string representation of the factor Attributes Definition Classes BasicFactor -> Factor Inherited from: BasicFactor

Invocation template:

```scala
receiver.toReadableString
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#toString-0)

```scala
override def toString: String
```

Description that includes the variable list and conditional probabilities

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Description that includes the variable list and conditional probabilities Attributes Definition Classes Factor -> Any Inherited from: Factor

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.unionVars ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#unionVars-514)

```scala
def unionVars[U](that: Factor[U]): (List[Variable[_]], List[Variable[_]], List[Int], List[Int])
```

takes the variables in two factors and produces their set union.

Type parameters: `` U ``.

Parameters, list 1: `` that: Factor[U] ``.

Returns: `` (List[Variable[_]], List[Variable[_]], List[Int], List[Int]) ``.

Source contract/attributes: takes the variables in two factors and produces their set union. It produces a mapping from each original variable to its new location. Similarly it produces a mapping from each new variable to its new location. Attributes Inherited from: BasicFactor

Invocation template:

```scala
receiver.unionVars[U](that)
```

## `` com.cra.figaro.algorithm.factored.factors.SparseFactor.variables ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SparseFactor.html#variables-0)

```scala
def variables: List[Variable[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Variable[_]] ``.

Source contract/attributes: Attributes Inherited from: Factor

Invocation template:

```scala
receiver.variables
```

## `` com.cra.figaro.algorithm.factored.factors.SufficientStatisticsSemiring.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SufficientStatisticsSemiring$.html#apply-227)

```scala
def apply(parameterMap: Map[Parameter[_], Seq[Double]]): SufficientStatisticsSemiring
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` parameterMap: Map[Parameter[_], Seq[Double]] ``.

Returns: `` SufficientStatisticsSemiring ``.

Invocation template:

```scala
com.cra.figaro.algorithm.factored.factors.SufficientStatisticsSemiring.apply(parameterMap)
```

## `` com.cra.figaro.algorithm.factored.factors.SufficientStatisticsSemiring.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SufficientStatisticsSemiring.html#product-fffffc96)

```scala
def product(xVector: (Double, Map[Parameter[_], Seq[Double]]), yVector: (Double, Map[Parameter[_], Seq[Double]])): (Double, Map[Parameter[_], Seq[Double]])
```

Probabilities are multiplied using standard multiplication. Sufficient statistics for each parameter are summed together.

Type parameters: none.

Parameters, list 1: `` xVector: (Double, Map[Parameter[_], Seq[Double]]) ``; `` yVector: (Double, Map[Parameter[_], Seq[Double]]) ``.

Returns: `` (Double, Map[Parameter[_], Seq[Double]]) ``.

Source contract/attributes: Probabilities are multiplied using standard multiplication. Sufficient statistics for each parameter are summed together. Attributes

Invocation template:

```scala
receiver.product(xVector, yVector)
```

## `` com.cra.figaro.algorithm.factored.factors.SufficientStatisticsSemiring.sum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SufficientStatisticsSemiring.html#sum-fffffc96)

```scala
def sum(xVector: (Double, Map[Parameter[_], Seq[Double]]), yVector: (Double, Map[Parameter[_], Seq[Double]])): (Double, Map[Parameter[_], Seq[Double]])
```

Probabilities are added using standard addition. Sufficient statistics for each parameter are weighted by their respective probabilities and summed together, then divided by the sum of both probabilities.

Type parameters: none.

Parameters, list 1: `` xVector: (Double, Map[Parameter[_], Seq[Double]]) ``; `` yVector: (Double, Map[Parameter[_], Seq[Double]]) ``.

Returns: `` (Double, Map[Parameter[_], Seq[Double]]) ``.

Source contract/attributes: Probabilities are added using standard addition. Sufficient statistics for each parameter are weighted by their respective probabilities and summed together, then divided by the sum of both probabilities. Attributes

Invocation template:

```scala
receiver.sum(xVector, yVector)
```

## `` com.cra.figaro.algorithm.factored.factors.SufficientStatisticsSemiring.sumMany ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SufficientStatisticsSemiring.html#sumMany-2cd)

```scala
def sumMany(xs: Iterable[(Double, Map[Parameter[_], Seq[Double]])]): (Double, Map[Parameter[_], Seq[Double]])
```

Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations.

Type parameters: none.

Parameters, list 1: `` xs: Iterable[(Double, Map[Parameter[_], Seq[Double]])] ``.

Returns: `` (Double, Map[Parameter[_], Seq[Double]]) ``.

Source contract/attributes: Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations. Attributes Inherited from: Semiring

Invocation template:

```scala
receiver.sumMany(xs)
```

## `` com.cra.figaro.algorithm.factored.factors.SumProductSemiring.convert ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SumProductSemiring.html#convert-fffff2df)

```scala
def convert(): LogConvertibleSemiRing[Double]
```

Converts this semiring into its log inverse For non-log space semirings, converts to log, and for log semirings exponentiates

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` LogConvertibleSemiRing[Double] ``.

Source contract/attributes: Converts this semiring into its log inverse For non-log space semirings, converts to log, and for log semirings exponentiates Attributes

Invocation template:

```scala
receiver.convert()
```

## `` com.cra.figaro.algorithm.factored.factors.SumProductSemiring.divide ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SumProductSemiring.html#divide-44b)

```scala
def divide(x: Double, y: Double): Double
```

Standard division

Type parameters: none.

Parameters, list 1: `` x: Double ``; `` y: Double ``.

Returns: `` Double ``.

Source contract/attributes: Standard division Attributes

Invocation template:

```scala
receiver.divide(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.SumProductSemiring.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SumProductSemiring.html#product-44b)

```scala
def product(x: Double, y: Double): Double
```

Standard multiplication

Type parameters: none.

Parameters, list 1: `` x: Double ``; `` y: Double ``.

Returns: `` Double ``.

Source contract/attributes: Standard multiplication Attributes

Invocation template:

```scala
receiver.product(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.SumProductSemiring.sum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SumProductSemiring.html#sum-44b)

```scala
def sum(x: Double, y: Double): Double
```

Standard addition

Type parameters: none.

Parameters, list 1: `` x: Double ``; `` y: Double ``.

Returns: `` Double ``.

Source contract/attributes: Standard addition Attributes

Invocation template:

```scala
receiver.sum(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.SumProductSemiring.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SumProductSemiring.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.algorithm.factored.factors.SumProductSemiring.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SumProductSemiring.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.algorithm.factored.factors.SumProductSemiring.sumMany ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SumProductSemiring.html#sumMany-2cd)

```scala
def sumMany(xs: Iterable[Double]): Double
```

Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations.

Type parameters: none.

Parameters, list 1: `` xs: Iterable[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations. Attributes Inherited from: Semiring

Invocation template:

```scala
receiver.sumMany(xs)
```

## `` com.cra.figaro.algorithm.factored.factors.SumProductUtilitySemiring.divide ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SumProductUtilitySemiring.html#divide-fffffc96)

```scala
def divide(x: (Double, Double), y: (Double, Double)): (Double, Double)
```

Decision joint factor division.

Type parameters: none.

Parameters, list 1: `` x: (Double, Double) ``; `` y: (Double, Double) ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Decision joint factor division. Attributes

Invocation template:

```scala
receiver.divide(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.SumProductUtilitySemiring.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SumProductUtilitySemiring.html#product-fffffc96)

```scala
def product(x: (Double, Double), y: (Double, Double)): (Double, Double)
```

Decision joint factor combination.

Type parameters: none.

Parameters, list 1: `` x: (Double, Double) ``; `` y: (Double, Double) ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Decision joint factor combination. Attributes

Invocation template:

```scala
receiver.product(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.SumProductUtilitySemiring.sum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SumProductUtilitySemiring.html#sum-fffffc96)

```scala
def sum(x: (Double, Double), y: (Double, Double)): (Double, Double)
```

Decision joint factor marginalization.

Type parameters: none.

Parameters, list 1: `` x: (Double, Double) ``; `` y: (Double, Double) ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Decision joint factor marginalization. Attributes

Invocation template:

```scala
receiver.sum(x, y)
```

## `` com.cra.figaro.algorithm.factored.factors.SumProductUtilitySemiring.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SumProductUtilitySemiring.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.algorithm.factored.factors.SumProductUtilitySemiring.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SumProductUtilitySemiring.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.algorithm.factored.factors.SumProductUtilitySemiring.sumMany ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/SumProductUtilitySemiring.html#sumMany-2cd)

```scala
def sumMany(xs: Iterable[(Double, Double)]): (Double, Double)
```

Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations.

Type parameters: none.

Parameters, list 1: `` xs: Iterable[(Double, Double)] ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Sum of many entries. Typically, this would be implemented by the ordinary sum, but there may be more efficient implementations. Attributes Inherited from: Semiring

Invocation template:

```scala
receiver.sumMany(xs)
```

## `` com.cra.figaro.algorithm.factored.factors.Variable.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Variable$.html#apply-fffffd5c)

```scala
def apply[T](elem: Element[T]): Variable[T]
```

Create the variable associated with an element. This method is memoized.

Type parameters: `` T ``.

Parameters, list 1: `` elem: Element[T] ``.

Returns: `` Variable[T] ``.

Source contract/attributes: Create the variable associated with an element. This method is memoized. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.factors.Variable.apply[T](elem)
```

## `` com.cra.figaro.algorithm.factored.factors.Variable.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Variable$.html#apply-fffffad9)

```scala
def apply[T](elem: Parameterized[T]): Variable[T]
```

Create the variable associated with a parame. This method is memoized.

Type parameters: `` T ``.

Parameters, list 1: `` elem: Parameterized[T] ``.

Returns: `` Variable[T] ``.

Source contract/attributes: Create the variable associated with a parame. This method is memoized. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.factors.Variable.apply[T](elem)
```

## `` com.cra.figaro.algorithm.factored.factors.Variable.clearCache ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Variable$.html#clearCache-94c)

```scala
def clearCache(): Unit
```

Clear the variable cache

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clear the variable cache Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.factors.Variable.clearCache()
```

## `` com.cra.figaro.algorithm.factored.factors.Variable.nextId ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Variable$.html#nextId-fffff376)

```scala
def nextId(elem: Element[_]): Int
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` elem: Element[_] ``.

Returns: `` Int ``.

Invocation template:

```scala
com.cra.figaro.algorithm.factored.factors.Variable.nextId(elem)
```

## `` com.cra.figaro.algorithm.factored.factors.Variable.equals ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Variable.html#equals-4ad)

```scala
override def equals(o: Any): Boolean
```

Override equality of Variable. Variables are the same if their id's are the same

Type parameters: none.

Parameters, list 1: `` o: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Override equality of Variable. Variables are the same if their id's are the same Attributes Definition Classes Any

Invocation template:

```scala
receiver.equals(o)
```

## `` com.cra.figaro.algorithm.factored.factors.Variable.hashCode ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Variable.html#hashCode-0)

```scala
override def hashCode: Int
```

Calculates a hash code value for the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Calculates a hash code value for the object. The default hashing algorithm is platform dependent. Note that it is allowed for two objects to have identical hash codes (o1.hashCode.equals(o2.hashCode)) yet not be equal (o1.equals(o2) returns false). A degenerate implementation could always return 0. However, it is required that if two objects are equal (o1.equals(o2) returns true) that they have identical hash codes (o1.hashCode.equals(o2.hashCode)). Therefore, when overriding this method, be sure to verify that the behavior is consistent with the equals method. Attributes Returns the hash code value for this object. Definition Classes Any

Invocation template:

```scala
receiver.hashCode
```

## `` com.cra.figaro.algorithm.factored.factors.Variable.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/factors/Variable.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Any

Invocation template:

```scala
receiver.toString
```
