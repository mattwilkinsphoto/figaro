<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.experimental.particlebp

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.experimental.particlebp.AnytimeParticleBeliefPropagation.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/AnytimeParticleBeliefPropagation.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Definition Classes Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.particlebp.AnytimeParticleBeliefPropagation.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/AnytimeParticleBeliefPropagation.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.experimental.particlebp.AnytimeParticleBeliefPropagation.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/AnytimeParticleBeliefPropagation.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.particlebp.AnytimeParticleBeliefPropagation.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/AnytimeParticleBeliefPropagation.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.particlebp.AnytimeParticleBeliefPropagation.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/AnytimeParticleBeliefPropagation.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.particlebp.AnytimeParticleBeliefPropagation.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/AnytimeParticleBeliefPropagation.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.particlebp.AnytimeParticleBeliefPropagation.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/AnytimeParticleBeliefPropagation.html#runStep-94c)

```scala
def runStep(): Unit
```

Runs this particle belief propagation algorithm for one iteration. An iteration here is one iteration of the outer loop. This means that the inner BP loop may run several iterations.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Runs this particle belief propagation algorithm for one iteration. An iteration here is one iteration of the outer loop. This means that the inner BP loop may run several iterations. Attributes Inherited from: ParticleBeliefPropagation

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.experimental.particlebp.AnytimeParticleBeliefPropagation.shutdown ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/AnytimeParticleBeliefPropagation.html#shutdown-0)

```scala
def shutdown: Unit
```

Release all resources from this anytime algorithm.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Unit ``.

Source contract/attributes: Release all resources from this anytime algorithm. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.shutdown
```

## `` com.cra.figaro.experimental.particlebp.AnytimeParticleBeliefPropagation.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/AnytimeParticleBeliefPropagation.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.particlebp.AnytimeParticleBeliefPropagation.starterElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/AnytimeParticleBeliefPropagation.html#starterElements-0)

```scala
def starterElements: List[Element[_]]
```

Elements towards which queries are directed. By default, these are the target elements.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Elements towards which queries are directed. By default, these are the target elements. Attributes Inherited from: ParticleBeliefPropagation

Invocation template:

```scala
receiver.starterElements
```

## `` com.cra.figaro.experimental.particlebp.AnytimeParticleBeliefPropagation.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/AnytimeParticleBeliefPropagation.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.particlebp.AnytimeParticleBeliefPropagation.stopUpdate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/AnytimeParticleBeliefPropagation.html#stopUpdate-94c)

```scala
def stopUpdate(): Unit
```

Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.stopUpdate()
```

## `` com.cra.figaro.experimental.particlebp.AnytimeParticleBeliefPropagation.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/AnytimeParticleBeliefPropagation.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.experimental.particlebp.AnytimeParticleBeliefPropagation.handle ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/AnytimeParticleBeliefPropagation.html#handle-fffff940)

```scala
def handle(service: Service): Response
```

A handler of services provided by the algorithm.

Type parameters: none.

Parameters, list 1: `` service: Service ``.

Returns: `` Response ``.

Source contract/attributes: A handler of services provided by the algorithm. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.handle(service)
```

## `` com.cra.figaro.experimental.particlebp.AutomaticDensityEstimator.getBandwidth ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/AutomaticDensityEstimator.html#getBandwidth-fffffaeb)

```scala
def getBandwidth(samples: List[(Double, Double)]): Double
```

Get the bandwidth to use for the normal kernel for this set of samples

Type parameters: none.

Parameters, list 1: `` samples: List[(Double, Double)] ``.

Returns: `` Double ``.

Source contract/attributes: Get the bandwidth to use for the normal kernel for this set of samples Attributes

Invocation template:

```scala
receiver.getBandwidth(samples)
```

## `` com.cra.figaro.experimental.particlebp.AutomaticDensityEstimator.getDensity ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/AutomaticDensityEstimator.html#getDensity-378)

```scala
def getDensity(pt: Any, samples: List[(Double, Any)]): Double
```

Get's the density of a point from a list of weighted samples

Type parameters: none.

Parameters, list 1: `` pt: Any ``; `` samples: List[(Double, Any)] ``.

Returns: `` Double ``.

Source contract/attributes: Get's the density of a point from a list of weighted samples Attributes Inherited from: NormalKernelDensityEstimator

Invocation template:

```scala
receiver.getDensity(pt, samples)
```

## `` com.cra.figaro.experimental.particlebp.AutomaticDensityEstimator.getDensity ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/AutomaticDensityEstimator.html#getDensity-fffff964)

```scala
def getDensity(pt: Int, samples: List[(Double, Int)]): Double
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` pt: Int ``; `` samples: List[(Double, Int)] ``.

Returns: `` Double ``.

Source contract/attributes: Attributes Inherited from: NormalKernelDensityEstimator

Invocation template:

```scala
receiver.getDensity(pt, samples)
```

## `` com.cra.figaro.experimental.particlebp.AutomaticDensityEstimator.getDensity ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/AutomaticDensityEstimator.html#getDensity-bb2)

```scala
def getDensity(pt: Double, samples: List[(Double, Double)]): Double
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` pt: Double ``; `` samples: List[(Double, Double)] ``.

Returns: `` Double ``.

Source contract/attributes: Attributes Inherited from: NormalKernelDensityEstimator

Invocation template:

```scala
receiver.getDensity(pt, samples)
```

## `` com.cra.figaro.experimental.particlebp.NormalKernelDensityEstimator.getBandwidth ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/NormalKernelDensityEstimator.html#getBandwidth-fffffaeb)

```scala
def getBandwidth(samples: List[(Double, Double)]): Double
```

Get the bandwidth to use for the normal kernel for this set of samples

Type parameters: none.

Parameters, list 1: `` samples: List[(Double, Double)] ``.

Returns: `` Double ``.

Source contract/attributes: Get the bandwidth to use for the normal kernel for this set of samples Attributes

Invocation template:

```scala
receiver.getBandwidth(samples)
```

## `` com.cra.figaro.experimental.particlebp.NormalKernelDensityEstimator.getDensity ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/NormalKernelDensityEstimator.html#getDensity-bb2)

```scala
def getDensity(pt: Double, samples: List[(Double, Double)]): Double
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` pt: Double ``; `` samples: List[(Double, Double)] ``.

Returns: `` Double ``.

Invocation template:

```scala
receiver.getDensity(pt, samples)
```

## `` com.cra.figaro.experimental.particlebp.NormalKernelDensityEstimator.getDensity ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/NormalKernelDensityEstimator.html#getDensity-fffff964)

```scala
def getDensity(pt: Int, samples: List[(Double, Int)]): Double
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` pt: Int ``; `` samples: List[(Double, Int)] ``.

Returns: `` Double ``.

Invocation template:

```scala
receiver.getDensity(pt, samples)
```

## `` com.cra.figaro.experimental.particlebp.NormalKernelDensityEstimator.getDensity ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/NormalKernelDensityEstimator.html#getDensity-378)

```scala
def getDensity(pt: Any, samples: List[(Double, Any)]): Double
```

Get's the density of a point from a list of weighted samples

Type parameters: none.

Parameters, list 1: `` pt: Any ``; `` samples: List[(Double, Any)] ``.

Returns: `` Double ``.

Source contract/attributes: Get's the density of a point from a list of weighted samples Attributes

Invocation template:

```scala
receiver.getDensity(pt, samples)
```

## `` com.cra.figaro.experimental.particlebp.OneTimeParticleBeliefPropagation.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/OneTimeParticleBeliefPropagation.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.experimental.particlebp.OneTimeParticleBeliefPropagation.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/OneTimeParticleBeliefPropagation.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.particlebp.OneTimeParticleBeliefPropagation.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/OneTimeParticleBeliefPropagation.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.experimental.particlebp.OneTimeParticleBeliefPropagation.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/OneTimeParticleBeliefPropagation.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.particlebp.OneTimeParticleBeliefPropagation.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/OneTimeParticleBeliefPropagation.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.particlebp.OneTimeParticleBeliefPropagation.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/OneTimeParticleBeliefPropagation.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.particlebp.OneTimeParticleBeliefPropagation.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/OneTimeParticleBeliefPropagation.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.particlebp.OneTimeParticleBeliefPropagation.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/OneTimeParticleBeliefPropagation.html#runStep-94c)

```scala
def runStep(): Unit
```

Runs this particle belief propagation algorithm for one iteration. An iteration here is one iteration of the outer loop. This means that the inner BP loop may run several iterations.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Runs this particle belief propagation algorithm for one iteration. An iteration here is one iteration of the outer loop. This means that the inner BP loop may run several iterations. Attributes Inherited from: ParticleBeliefPropagation

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.experimental.particlebp.OneTimeParticleBeliefPropagation.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/OneTimeParticleBeliefPropagation.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.particlebp.OneTimeParticleBeliefPropagation.starterElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/OneTimeParticleBeliefPropagation.html#starterElements-0)

```scala
def starterElements: List[Element[_]]
```

Elements towards which queries are directed. By default, these are the target elements.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Elements towards which queries are directed. By default, these are the target elements. Attributes Inherited from: ParticleBeliefPropagation

Invocation template:

```scala
receiver.starterElements
```

## `` com.cra.figaro.experimental.particlebp.OneTimeParticleBeliefPropagation.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/OneTimeParticleBeliefPropagation.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.particlebp.OneTimeParticleBeliefPropagation.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/OneTimeParticleBeliefPropagation.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ParticleBeliefPropagation$.html#apply-ed6)

```scala
def apply(myOuterIterations: Int, myInnerIterations: Int, targets: Element[_]*)(implicit universe: Universe): ProbQueryParticleBeliefPropagation & OneTimeParticleBeliefPropagation & OneTimeProbQuery
```

Creates a One Time belief propagation computer in the current default universe.

Type parameters: none.

Parameters, list 1: `` myOuterIterations: Int ``; `` myInnerIterations: Int ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryParticleBeliefPropagation & OneTimeParticleBeliefPropagation & OneTimeProbQuery ``.

Source contract/attributes: Creates a One Time belief propagation computer in the current default universe. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.apply(myOuterIterations, myInnerIterations, targets*)(using universe)
```

## `` com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ParticleBeliefPropagation$.html#apply-fffff4d6)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double, myOuterIterations: Int, myInnerIterations: Int, targets: Element[_]*)(implicit universe: Universe): ProbQueryParticleBeliefPropagation & OneTimeParticleBeliefPropagation & OneTimeProbQuery
```

Creates a One Time belief propagation computer in the current default universe. Use the dependent universe and algorithm to compute prob of evidence in dependent universe

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``; `` myOuterIterations: Int ``; `` myInnerIterations: Int ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryParticleBeliefPropagation & OneTimeParticleBeliefPropagation & OneTimeProbQuery ``.

Source contract/attributes: Creates a One Time belief propagation computer in the current default universe. Use the dependent universe and algorithm to compute prob of evidence in dependent universe Attributes

Invocation template:

```scala
com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.apply(dependentUniverses, dependentAlgorithm, myOuterIterations, myInnerIterations, targets*)(using universe)
```

## `` com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ParticleBeliefPropagation$.html#apply-fffffef6)

```scala
def apply(myOuterIterations: Int, myInnerIterations: Int, argSamples: Int, totalSamples: Int, targets: Element[_]*)(implicit universe: Universe): ProbQueryParticleBeliefPropagation & OneTimeParticleBeliefPropagation & OneTimeProbQuery
```

Creates a One Time belief propagation computer in the current default universe that specifies the number of samples to take for each element.

Type parameters: none.

Parameters, list 1: `` myOuterIterations: Int ``; `` myInnerIterations: Int ``; `` argSamples: Int ``; `` totalSamples: Int ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryParticleBeliefPropagation & OneTimeParticleBeliefPropagation & OneTimeProbQuery ``.

Source contract/attributes: Creates a One Time belief propagation computer in the current default universe that specifies the number of samples to take for each element. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.apply(myOuterIterations, myInnerIterations, argSamples, totalSamples, targets*)(using universe)
```

## `` com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ParticleBeliefPropagation$.html#apply-4f6)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double, myOuterIterations: Int, myInnerIterations: Int, argSamples: Int, totalSamples: Int, targets: Element[_]*)(implicit universe: Universe): ProbQueryParticleBeliefPropagation & OneTimeParticleBeliefPropagation & OneTimeProbQuery
```

Creates a One Time belief propagation computer in the current default universe that specifies the number of samples to take for each element. Use the dependent universe and algorithm to compute prob of evidence in dependent universe

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``; `` myOuterIterations: Int ``; `` myInnerIterations: Int ``; `` argSamples: Int ``; `` totalSamples: Int ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryParticleBeliefPropagation & OneTimeParticleBeliefPropagation & OneTimeProbQuery ``.

Source contract/attributes: Creates a One Time belief propagation computer in the current default universe that specifies the number of samples to take for each element. Use the dependent universe and algorithm to compute prob of evidence in dependent universe Attributes

Invocation template:

```scala
com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.apply(dependentUniverses, dependentAlgorithm, myOuterIterations, myInnerIterations, argSamples, totalSamples, targets*)(using universe)
```

## `` com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ParticleBeliefPropagation$.html#apply-bf2)

```scala
def apply(stepTimeMillis: Long, targets: Element[_]*)(implicit universe: Universe): ProbQueryParticleBeliefPropagation & AnytimeParticleBeliefPropagation & AnytimeProbQuery
```

Creates a Anytime belief propagation computer in the current default universe.

Type parameters: none.

Parameters, list 1: `` stepTimeMillis: Long ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryParticleBeliefPropagation & AnytimeParticleBeliefPropagation & AnytimeProbQuery ``.

Source contract/attributes: Creates a Anytime belief propagation computer in the current default universe. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.apply(stepTimeMillis, targets*)(using universe)
```

## `` com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ParticleBeliefPropagation$.html#apply-1f2)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double, stepTimeMillis: Long, targets: Element[_]*)(implicit universe: Universe): ProbQueryParticleBeliefPropagation & AnytimeParticleBeliefPropagation & AnytimeProbQuery
```

Creates a Anytime belief propagation computer in the current default universe. Use the dependent universe and algorithm to compute prob of evidence in dependent universe

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``; `` stepTimeMillis: Long ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryParticleBeliefPropagation & AnytimeParticleBeliefPropagation & AnytimeProbQuery ``.

Source contract/attributes: Creates a Anytime belief propagation computer in the current default universe. Use the dependent universe and algorithm to compute prob of evidence in dependent universe Attributes

Invocation template:

```scala
com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.apply(dependentUniverses, dependentAlgorithm, stepTimeMillis, targets*)(using universe)
```

## `` com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ParticleBeliefPropagation$.html#apply-fffffd12)

```scala
def apply(stepTimeMillis: Long, argSamples: Int, totalSamples: Int, targets: Element[_]*)(implicit universe: Universe): ProbQueryParticleBeliefPropagation & AnytimeParticleBeliefPropagation & AnytimeProbQuery
```

Creates a Anytime belief propagation computer in the current default universe that specifies the number of samples to take for each element.

Type parameters: none.

Parameters, list 1: `` stepTimeMillis: Long ``; `` argSamples: Int ``; `` totalSamples: Int ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryParticleBeliefPropagation & AnytimeParticleBeliefPropagation & AnytimeProbQuery ``.

Source contract/attributes: Creates a Anytime belief propagation computer in the current default universe that specifies the number of samples to take for each element. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.apply(stepTimeMillis, argSamples, totalSamples, targets*)(using universe)
```

## `` com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ParticleBeliefPropagation$.html#apply-312)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double, stepTimeMillis: Long, argSamples: Int, totalSamples: Int, targets: Element[_]*)(implicit universe: Universe): ProbQueryParticleBeliefPropagation & AnytimeParticleBeliefPropagation & AnytimeProbQuery
```

Creates a Anytime belief propagation computer in the current default universe that specifies the number of samples to take for each element. Use the dependent universe and algorithm to compute prob of evidence in dependent universe

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``; `` stepTimeMillis: Long ``; `` argSamples: Int ``; `` totalSamples: Int ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryParticleBeliefPropagation & AnytimeParticleBeliefPropagation & AnytimeProbQuery ``.

Source contract/attributes: Creates a Anytime belief propagation computer in the current default universe that specifies the number of samples to take for each element. Use the dependent universe and algorithm to compute prob of evidence in dependent universe Attributes

Invocation template:

```scala
com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.apply(dependentUniverses, dependentAlgorithm, stepTimeMillis, argSamples, totalSamples, targets*)(using universe)
```

## `` com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ParticleBeliefPropagation.html#runStep-94c)

```scala
def runStep(): Unit
```

Runs this particle belief propagation algorithm for one iteration. An iteration here is one iteration of the outer loop. This means that the inner BP loop may run several iterations.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Runs this particle belief propagation algorithm for one iteration. An iteration here is one iteration of the outer loop. This means that the inner BP loop may run several iterations. Attributes

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.starterElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ParticleBeliefPropagation.html#starterElements-0)

```scala
def starterElements: List[Element[_]]
```

Elements towards which queries are directed. By default, these are the target elements.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Elements towards which queries are directed. By default, these are the target elements. Attributes

Invocation template:

```scala
receiver.starterElements
```

## `` com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ParticleBeliefPropagation.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ParticleBeliefPropagation.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ParticleBeliefPropagation.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ParticleBeliefPropagation.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ParticleBeliefPropagation.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ParticleBeliefPropagation.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ParticleBeliefPropagation.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ParticleBeliefPropagation.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.particlebp.ParticleBeliefPropagation.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ParticleBeliefPropagation.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#computeDistribution-fffffdb4)

```scala
def computeDistribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Attributes

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#computeExpectation-c2a)

```scala
def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Attributes

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

Getting factors for PBP returns an empty list, since all of the factor creation is handled inside of the BP instances

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Getting factors for PBP returns an empty list, since all of the factor creation is handled inside of the BP instances Attributes

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#distribution-fffffef0)

```scala
def distribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#runStep-94c)

```scala
def runStep(): Unit
```

Runs this particle belief propagation algorithm for one iteration. An iteration here is one iteration of the outer loop. This means that the inner BP loop may run several iterations.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Runs this particle belief propagation algorithm for one iteration. An iteration here is one iteration of the outer loop. This means that the inner BP loop may run several iterations. Attributes Inherited from: ParticleBeliefPropagation

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.starterElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#starterElements-0)

```scala
def starterElements: List[Element[_]]
```

Elements towards which queries are directed. By default, these are the target elements.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Elements towards which queries are directed. By default, these are the target elements. Attributes Inherited from: ParticleBeliefPropagation

Invocation template:

```scala
receiver.starterElements
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.particlebp.ProbQueryParticleBeliefPropagation.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/particlebp/ProbQueryParticleBeliefPropagation.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```
