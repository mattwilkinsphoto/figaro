<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.algorithm.structured.strategy

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` .flatRaising ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve.html#flatRaising-d6b)

```scala
def flatRaising(problem: NestedProblem[_]): Boolean
```

Always raises a nested problem. This has the effect of "flattening" because all factors of problems strictly contained in the top-level problem are raised.

Type parameters: none.

Parameters, list 1: `` problem: NestedProblem[_] ``.

Returns: `` Boolean ``.

Source contract/attributes: Always raises a nested problem. This has the effect of "flattening" because all factors of problems strictly contained in the top-level problem are raised. Attributes

Invocation template:

```scala
receiver.flatRaising(problem)
```

## `` .raiseIfGlobal ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve.html#raiseIfGlobal-d6b)

```scala
def raiseIfGlobal(problem: NestedProblem[_]): Boolean
```

Raises a nested problem if any of its components are global.

Type parameters: none.

Parameters, list 1: `` problem: NestedProblem[_] ``.

Returns: `` Boolean ``.

Source contract/attributes: Raises a nested problem if any of its components are global. Attributes

Invocation template:

```scala
receiver.raiseIfGlobal(problem)
```

## `` .structuredRaising ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/strategy/solve.html#structuredRaising-d6b)

```scala
def structuredRaising(problem: NestedProblem[_]): Boolean
```

Never raises a nested problem without solving it.

Type parameters: none.

Parameters, list 1: `` problem: NestedProblem[_] ``.

Returns: `` Boolean ``.

Source contract/attributes: Never raises a nested problem without solving it. Attributes

Invocation template:

```scala
receiver.structuredRaising(problem)
```
