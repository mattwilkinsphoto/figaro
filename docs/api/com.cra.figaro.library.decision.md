<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.library.decision

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.library.decision.AlgorithmDecisionException.addSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/AlgorithmDecisionException.html#addSuppressed-4b8)

```scala
final def addSuppressed(x$0: Throwable): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.addSuppressed(x$0)
```

## `` com.cra.figaro.library.decision.AlgorithmDecisionException.fillInStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/AlgorithmDecisionException.html#fillInStackTrace-26c)

```scala
def fillInStackTrace(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.fillInStackTrace()
```

## `` com.cra.figaro.library.decision.AlgorithmDecisionException.getCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/AlgorithmDecisionException.html#getCause-26c)

```scala
def getCause(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getCause()
```

## `` com.cra.figaro.library.decision.AlgorithmDecisionException.getLocalizedMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/AlgorithmDecisionException.html#getLocalizedMessage-665)

```scala
def getLocalizedMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getLocalizedMessage()
```

## `` com.cra.figaro.library.decision.AlgorithmDecisionException.getMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/AlgorithmDecisionException.html#getMessage-665)

```scala
def getMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getMessage()
```

## `` com.cra.figaro.library.decision.AlgorithmDecisionException.getStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/AlgorithmDecisionException.html#getStackTrace-fffffa6d)

```scala
def getStackTrace(): Array[StackTraceElement]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[StackTraceElement] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getStackTrace()
```

## `` com.cra.figaro.library.decision.AlgorithmDecisionException.getSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/AlgorithmDecisionException.html#getSuppressed-fffff2ce)

```scala
final def getSuppressed(): Array[Throwable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[Throwable] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getSuppressed()
```

## `` com.cra.figaro.library.decision.AlgorithmDecisionException.initCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/AlgorithmDecisionException.html#initCause-fffff080)

```scala
def initCause(x$0: Throwable): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.initCause(x$0)
```

## `` com.cra.figaro.library.decision.AlgorithmDecisionException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/AlgorithmDecisionException.html#printStackTrace-fffff230)

```scala
def printStackTrace(x$0: PrintWriter): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintWriter ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.library.decision.AlgorithmDecisionException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/AlgorithmDecisionException.html#printStackTrace-fffff0fd)

```scala
def printStackTrace(x$0: PrintStream): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintStream ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.library.decision.AlgorithmDecisionException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/AlgorithmDecisionException.html#printStackTrace-94c)

```scala
def printStackTrace(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace()
```

## `` com.cra.figaro.library.decision.AlgorithmDecisionException.setStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/AlgorithmDecisionException.html#setStackTrace-f79)

```scala
def setStackTrace(x$0: Array[StackTraceElement]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Array[StackTraceElement] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.setStackTrace(x$0)
```

## `` com.cra.figaro.library.decision.AlgorithmDecisionException.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/AlgorithmDecisionException.html#toString-665)

```scala
def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Inherited from: Throwable

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.library.decision.CachingDecision.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision$.html#apply-fffffa8a)

```scala
def apply[U](fcn: () => Element[U])(implicit name: Name[U], collection: ElementCollection): Decision[Int, U]
```

Create a CachingDecision with no parent that uses an exact PolicyMaker.

Type parameters: `` U ``.

Parameters, list 1: `` fcn: () => Element[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Decision[Int, U] ``.

Source contract/attributes: Create a CachingDecision with no parent that uses an exact PolicyMaker. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.CachingDecision.apply[U](fcn)(using name, collection)
```

## `` com.cra.figaro.library.decision.CachingDecision.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision$.html#apply-757)

```scala
def apply[T, U](arg1: Element[T], fcn: T => Element[U])(implicit name: Name[U], collection: ElementCollection): Decision[T, U]
```

Create a CachingDecision with one parent and a default function from values of the parent to Element[U]. Uses an exact PolicyMaker.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` arg1: Element[T] ``; `` fcn: T => Element[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Decision[T, U] ``.

Source contract/attributes: Create a CachingDecision with one parent and a default function from values of the parent to Element[U]. Uses an exact PolicyMaker. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.CachingDecision.apply[T, U](arg1, fcn)(using name, collection)
```

## `` com.cra.figaro.library.decision.CachingDecision.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision$.html#apply-fffff34d)

```scala
def apply[U](range: Seq[U])(implicit name: Name[U], collection: ElementCollection): Decision[Int, U]
```

Create a CachingDecision with no parent and sequence of the possible actions of the decision. Uses an exact PolicyMaker.

Type parameters: `` U ``.

Parameters, list 1: `` range: Seq[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Decision[Int, U] ``.

Source contract/attributes: Create a CachingDecision with no parent and sequence of the possible actions of the decision. Uses an exact PolicyMaker. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.CachingDecision.apply[U](range)(using name, collection)
```

## `` com.cra.figaro.library.decision.CachingDecision.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision$.html#apply-a5f)

```scala
def apply[T, U](arg1: Element[T], range: Seq[U])(implicit name: Name[U], collection: ElementCollection): Decision[T, U]
```

Create a CachingDecision with one parent and sequence of the possible actions of the decision. Uses an exact PolicyMaker.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` arg1: Element[T] ``; `` range: Seq[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Decision[T, U] ``.

Source contract/attributes: Create a CachingDecision with one parent and sequence of the possible actions of the decision. Uses an exact PolicyMaker. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.CachingDecision.apply[T, U](arg1, range)(using name, collection)
```

## `` com.cra.figaro.library.decision.CachingDecision.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Chain -> Any

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.library.decision.CachingDecision.!== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#!==-894)

```scala
def !==(that: Element[Value])(implicit universe: Universe): Neq[U]
```

The element that tests inequality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Neq[U] ``.

Source contract/attributes: The element that tests inequality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.!==(that)(using universe)
```

## `` com.cra.figaro.library.decision.CachingDecision.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#===-f71)

```scala
def ===(that: Value)(implicit universe: Universe): Apply1[U, Boolean]
```

The element that tests whether the value of this element is equal to a particular value.

Type parameters: none.

Parameters, list 1: `` that: Value ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Apply1[U, Boolean] ``.

Source contract/attributes: The element that tests whether the value of this element is equal to a particular value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.library.decision.CachingDecision.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#===-672)

```scala
def ===(that: Element[Value])(implicit universe: Universe): Eq[U]
```

The element that tests equality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Eq[U] ``.

Source contract/attributes: The element that tests equality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.library.decision.CachingDecision.activate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#activate-94c)

```scala
def activate(): Unit
```

Activate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Activate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.activate()
```

## `` com.cra.figaro.library.decision.CachingDecision.addCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#addCondition-fffff9ad)

```scala
def addCondition(condition: Condition, contingency: Contingency = ...): Unit
```

Add the given condition to the existing conditions of the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` condition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add the given condition to the existing conditions of the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addCondition(condition, contingency)
```

## `` com.cra.figaro.library.decision.CachingDecision.addConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#addConstraint-fffff9ad)

```scala
def addConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addConstraint(constraint, contingency)
```

## `` com.cra.figaro.library.decision.CachingDecision.addLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#addLogConstraint-fffff9ad)

```scala
def addLogConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a log contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a log contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addLogConstraint(constraint, contingency)
```

## `` com.cra.figaro.library.decision.CachingDecision.addPragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#addPragma-b53)

```scala
def addPragma(pragma: Pragma[Value]): Unit
```

Add a pragma to the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Add a pragma to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addPragma(pragma)
```

## `` com.cra.figaro.library.decision.CachingDecision.allConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#allConditions-0)

```scala
def allConditions: List[(Condition, Contingency)]
```

All the conditions defined on this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Condition, Contingency)] ``.

Source contract/attributes: All the conditions defined on this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConditions
```

## `` com.cra.figaro.library.decision.CachingDecision.allConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#allConstraints-0)

```scala
def allConstraints: List[(Constraint, Contingency)]
```

The current soft constraints on the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Constraint, Contingency)] ``.

Source contract/attributes: The current soft constraints on the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConstraints
```

## `` com.cra.figaro.library.decision.CachingDecision.args ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#args-0)

```scala
def args: List[Element[_]]
```

The arguments on which the element depends.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: The arguments on which the element depends. Attributes Inherited from: Chain

Invocation template:

```scala
receiver.args
```

## `` com.cra.figaro.library.decision.CachingDecision.clearContext ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#clearContext-94c)

```scala
def clearContext(): Unit
```

Clears all the temporary elements associated with this element (all elements created in it's context).

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clears all the temporary elements associated with this element (all elements created in it's context). Attributes Inherited from: Element

Invocation template:

```scala
receiver.clearContext()
```

## `` com.cra.figaro.library.decision.CachingDecision.condition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#condition-4ad)

```scala
def condition(value: Any): Boolean
```

Tests whether all the element's contingent conditions are satisfied for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether all the element's contingent conditions are satisfied for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.condition(value)
```

## `` com.cra.figaro.library.decision.CachingDecision.conditionSatisfied ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#conditionSatisfied-0)

```scala
def conditionSatisfied: Boolean
```

Determines whether the condition on the element is satisfied by the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Determines whether the condition on the element is satisfied by the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.conditionSatisfied
```

## `` com.cra.figaro.library.decision.CachingDecision.constraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#constraint-fffffeac)

```scala
def constraint(value: Any): Double
```

Gets the result of all the element's contingent constraints for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Double ``.

Source contract/attributes: Gets the result of all the element's contingent constraints for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraint(value)
```

## `` com.cra.figaro.library.decision.CachingDecision.constraintValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#constraintValue-0)

```scala
def constraintValue: Double
```

Determines the value of the constraint on the element applied to the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Determines the value of the constraint on the element applied to the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraintValue
```

## `` com.cra.figaro.library.decision.CachingDecision.context ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#context-0)

```scala
def context: List[Element[_]]
```

The elements on which the existence of this element depends.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: The elements on which the existence of this element depends. Attributes Inherited from: Element

Invocation template:

```scala
receiver.context
```

## `` com.cra.figaro.library.decision.CachingDecision.deactivate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#deactivate-94c)

```scala
def deactivate(): Unit
```

Deactivate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Deactivate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.deactivate()
```

## `` com.cra.figaro.library.decision.CachingDecision.directContextContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#directContextContents-0)

```scala
def directContextContents: Set[Element[_]]
```

Returns the set of elements directly created in the context of this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the set of elements directly created in the context of this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.directContextContents
```

## `` com.cra.figaro.library.decision.CachingDecision.elementsIAmContingentOn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#elementsIAmContingentOn-0)

```scala
def elementsIAmContingentOn: Set[Element[_]]
```

Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value. Figaro takes care of handling all this under the hood. However, some algorithms may need to know which elements an element is contingent on. For example, sampling algorithms may need to sample those other elements first. This method is supplied to support this use case. Attributes Inherited from: Element

Invocation template:

```scala
receiver.elementsIAmContingentOn
```

## `` com.cra.figaro.library.decision.CachingDecision.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#flatMap-db8)

```scala
def flatMap[U](fn: Value => Element[U])(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => Element[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.flatMap[U](fn)(using name, collection)
```

## `` com.cra.figaro.library.decision.CachingDecision.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#generate-94c)

```scala
final def generate(): Unit
```

First generate the randomness, then generate the value given the randomness. Store the results in randomness and value.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: First generate the randomness, then generate the value given the randomness. Store the results in randomness and value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.generate()
```

## `` com.cra.figaro.library.decision.CachingDecision.generateRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#generateRandomness-54f)

```scala
def generateRandomness(): Randomness
```

Generate the randomness content.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Randomness ``.

Source contract/attributes: Generate the randomness content. Attributes Inherited from: Deterministic

Invocation template:

```scala
receiver.generateRandomness()
```

## `` com.cra.figaro.library.decision.CachingDecision.generateValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#generateValue-993)

```scala
def generateValue(): U
```

Generate a value with no randomness.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` U ``.

Source contract/attributes: Generate a value with no randomness. Attributes Inherited from: Chain

Invocation template:

```scala
receiver.generateValue()
```

## `` com.cra.figaro.library.decision.CachingDecision.generateValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#generateValue-fffff0e2)

```scala
def generateValue(rand: Randomness): U
```

Generate the value of the element deterministically given its randomness and the values of its arguments.

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` U ``.

Source contract/attributes: Generate the value of the element deterministically given its randomness and the values of its arguments. Attributes Inherited from: Deterministic

Invocation template:

```scala
receiver.generateValue(rand)
```

## `` com.cra.figaro.library.decision.CachingDecision.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#get-ade)

```scala
def get(parentValue: T): Element[U]
```

Get the distribution over the result corresponding to the given parent value.

Type parameters: none.

Parameters, list 1: `` parentValue: T ``.

Returns: `` Element[U] ``.

Source contract/attributes: Get the distribution over the result corresponding to the given parent value. Attributes Inherited from: Chain

Invocation template:

```scala
receiver.get(parentValue)
```

## `` com.cra.figaro.library.decision.CachingDecision.getPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#getPolicy-fffff2f6)

```scala
def getPolicy(): DecisionPolicy[T, U]
```

Get the complete policy object for this decision.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` DecisionPolicy[T, U] ``.

Source contract/attributes: Get the complete policy object for this decision. Attributes Inherited from: Decision

Invocation template:

```scala
receiver.getPolicy()
```

## `` com.cra.figaro.library.decision.CachingDecision.getPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#getPolicy-ade)

```scala
def getPolicy(p: T): Element[U]
```

Get the decision for a specific value of the parent.

Type parameters: none.

Parameters, list 1: `` p: T ``.

Returns: `` Element[U] ``.

Source contract/attributes: Get the decision for a specific value of the parent. Attributes Inherited from: Decision

Invocation template:

```scala
receiver.getPolicy(p)
```

## `` com.cra.figaro.library.decision.CachingDecision.intervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#intervene-fffff71f)

```scala
def intervene(v: Value): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` v: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.intervene(v)
```

## `` com.cra.figaro.library.decision.CachingDecision.isCachable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#isCachable-0)

```scala
def isCachable: Boolean
```

The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isCachable
```

## `` com.cra.figaro.library.decision.CachingDecision.isTemporary ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#isTemporary-0)

```scala
def isTemporary: Boolean
```

Returns true if this element is temporary, that is, was created in the context of another element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Returns true if this element is temporary, that is, was created in the context of another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isTemporary
```

## `` com.cra.figaro.library.decision.CachingDecision.makePolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#makePolicy-121)

```scala
def makePolicy(alg: DecisionAlgorithm[T, U]): DecisionPolicy[T, U]
```

Returns a DecisionPolicy from the given decision algorithm .

Type parameters: none.

Parameters, list 1: `` alg: DecisionAlgorithm[T, U] ``.

Returns: `` DecisionPolicy[T, U] ``.

Source contract/attributes: Returns a DecisionPolicy from the given decision algorithm . Attributes Inherited from: PolicyMaker

Invocation template:

```scala
receiver.makePolicy(alg)
```

## `` com.cra.figaro.library.decision.CachingDecision.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#map-db8)

```scala
def map[U](fn: Value => U)(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => U ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.map[U](fn)(using name, collection)
```

## `` com.cra.figaro.library.decision.CachingDecision.nextRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#nextRandomness-fffff446)

```scala
def nextRandomness(rand: Randomness): (Randomness, Double, Double)
```

Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is:

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` (Randomness, Double, Double) ``.

Source contract/attributes: Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is: P(new -> old) / P(old -> new) and the model probability ratio, which is: P(new) / P(old) The default implementation is to use generateRandomness and returns ones for the proposal and model probability ratios. Attributes Inherited from: Element

Invocation template:

```scala
receiver.nextRandomness(rand)
```

## `` com.cra.figaro.library.decision.CachingDecision.observe ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#observe-fffff71f)

```scala
def observe(observation: Value): Unit
```

Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated.

Type parameters: none.

Parameters, list 1: `` observation: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.observe(observation)
```

## `` com.cra.figaro.library.decision.CachingDecision.pragmas ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#pragmas-0)

```scala
def pragmas: List[Pragma[Value]]
```

The pragmas attached to the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Pragma[Value]] ``.

Source contract/attributes: The pragmas attached to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.pragmas
```

## `` com.cra.figaro.library.decision.CachingDecision.removeConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#removeConditions-71e)

```scala
def removeConditions(contingency: Contingency = ...): Unit
```

Remove all conditions associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all conditions associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConditions(contingency)
```

## `` com.cra.figaro.library.decision.CachingDecision.removeConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#removeConstraints-71e)

```scala
def removeConstraints(contingency: Contingency = ...): Unit
```

Remove all constraints associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all constraints associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConstraints(contingency)
```

## `` com.cra.figaro.library.decision.CachingDecision.removePragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#removePragma-b53)

```scala
def removePragma(pragma: Pragma[Value]): Unit
```

Remove a pragma from the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Remove a pragma from the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removePragma(pragma)
```

## `` com.cra.figaro.library.decision.CachingDecision.score ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#score-fffffdbf)

```scala
def score(oldValue: Value, newValue: Value): Double
```

Compute the constraints on the new value divided by the constraints on the old value.

Type parameters: none.

Parameters, list 1: `` oldValue: Value ``; `` newValue: Value ``.

Returns: `` Double ``.

Source contract/attributes: Compute the constraints on the new value divided by the constraints on the old value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.score(oldValue, newValue)
```

## `` com.cra.figaro.library.decision.CachingDecision.set ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#set-fffff71f)

```scala
def set(newValue: Value): Unit
```

Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called.

Type parameters: none.

Parameters, list 1: `` newValue: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called. Attributes Inherited from: Element

Invocation template:

```scala
receiver.set(newValue)
```

## `` com.cra.figaro.library.decision.CachingDecision.setCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#setCondition-fffff9ad)

```scala
def setCondition(newCondition: Condition, contingency: Contingency = ...): Unit
```

Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newCondition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setCondition(newCondition, contingency)
```

## `` com.cra.figaro.library.decision.CachingDecision.setConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#setConstraint-fffff9ad)

```scala
def setConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.library.decision.CachingDecision.setLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#setLogConstraint-fffff9ad)

```scala
def setLogConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setLogConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.library.decision.CachingDecision.setPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#setPolicy-c9d)

```scala
def setPolicy(new_fcn: T => Element[U]): Unit
```

Directly set the policy of this decision using a function from a parent value to a decision element.

Type parameters: none.

Parameters, list 1: `` new_fcn: T => Element[U] ``.

Returns: `` Unit ``.

Source contract/attributes: Directly set the policy of this decision using a function from a parent value to a decision element. Attributes Inherited from: Decision

Invocation template:

```scala
receiver.setPolicy(new_fcn)
```

## `` com.cra.figaro.library.decision.CachingDecision.setPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#setPolicy-fffffa74)

```scala
def setPolicy(policyMap: Map[(T, U), DecisionSample]): Unit
```

Set the policy for this decision using a map from parent/decision tuple values to decision samples.

Type parameters: none.

Parameters, list 1: `` policyMap: Map[(T, U), DecisionSample] ``.

Returns: `` Unit ``.

Source contract/attributes: Set the policy for this decision using a map from parent/decision tuple values to decision samples. Attributes Inherited from: Decision

Invocation template:

```scala
receiver.setPolicy(policyMap)
```

## `` com.cra.figaro.library.decision.CachingDecision.setPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#setPolicy-fb7)

```scala
def setPolicy(Alg: DecisionAlgorithm[T, U]): Unit
```

Set the policy for this decision from an Algorithm run on this decision.

Type parameters: none.

Parameters, list 1: `` Alg: DecisionAlgorithm[T, U] ``.

Returns: `` Unit ``.

Source contract/attributes: Set the policy for this decision from an Algorithm run on this decision. Attributes Inherited from: Decision

Invocation template:

```scala
receiver.setPolicy(Alg)
```

## `` com.cra.figaro.library.decision.CachingDecision.setPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#setPolicy-94c)

```scala
def setPolicy(): Unit
```

Set the policy for this decision using the DecisionPolicy class stored in the Decision.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Set the policy for this decision using the DecisionPolicy class stored in the Decision. Attributes Inherited from: Decision

Invocation template:

```scala
receiver.setPolicy()
```

## `` com.cra.figaro.library.decision.CachingDecision.setRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#setRandomness-fffff71f)

```scala
def setRandomness(newRandomness: Randomness): Unit
```

Set the randomness of this element.

Type parameters: none.

Parameters, list 1: `` newRandomness: Randomness ``.

Returns: `` Unit ``.

Source contract/attributes: Set the randomness of this element. Will generate its value using the new randomness and propagate the effects to elements that depend on it without changing their randomness. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setRandomness(newRandomness)
```

## `` com.cra.figaro.library.decision.CachingDecision.toNameString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#toNameString-0)

```scala
def toNameString: String
```

A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString. Attributes Inherited from: Element

Invocation template:

```scala
receiver.toNameString
```

## `` com.cra.figaro.library.decision.CachingDecision.unintervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#unintervene-94c)

```scala
def unintervene(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.unintervene()
```

## `` com.cra.figaro.library.decision.CachingDecision.unobserve ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#unobserve-94c)

```scala
def unobserve(): Unit
```

Removes conditions on the element and allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Removes conditions on the element and allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unobserve()
```

## `` com.cra.figaro.library.decision.CachingDecision.unset ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#unset-94c)

```scala
def unset(): Unit
```

Allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unset()
```

## `` com.cra.figaro.library.decision.CachingDecision.makePolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/CachingDecision.html#makePolicy-61e)

```scala
def makePolicy(policyMap: Map[(T, U), DecisionSample]): DecisionPolicy[T, U]
```

Returns a DecisionPolicy from a map of parent/decision tuple values to DecisionSamples.

Type parameters: none.

Parameters, list 1: `` policyMap: Map[(T, U), DecisionSample] ``.

Returns: `` DecisionPolicy[T, U] ``.

Source contract/attributes: Returns a DecisionPolicy from a map of parent/decision tuple values to DecisionSamples. Attributes Inherited from: PolicyMaker

Invocation template:

```scala
receiver.makePolicy(policyMap)
```

## `` com.cra.figaro.library.decision.Decision.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision$.html#apply-fffffa8a)

```scala
def apply[U](fcn: () => Element[U])(implicit name: Name[U], collection: ElementCollection): Decision[Int, U]
```

Create a CachingDecision with no parent that uses an exact PolicyMaker.

Type parameters: `` U ``.

Parameters, list 1: `` fcn: () => Element[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Decision[Int, U] ``.

Source contract/attributes: Create a CachingDecision with no parent that uses an exact PolicyMaker. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.Decision.apply[U](fcn)(using name, collection)
```

## `` com.cra.figaro.library.decision.Decision.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision$.html#apply-757)

```scala
def apply[T, U](arg1: Element[T], fcn: T => Element[U])(implicit name: Name[U], collection: ElementCollection): Decision[T, U]
```

Create a CachingDecision with one parent and a default function from values of the parent to Element[U]. Uses an exact PolicyMaker.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` arg1: Element[T] ``; `` fcn: T => Element[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Decision[T, U] ``.

Source contract/attributes: Create a CachingDecision with one parent and a default function from values of the parent to Element[U]. Uses an exact PolicyMaker. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.Decision.apply[T, U](arg1, fcn)(using name, collection)
```

## `` com.cra.figaro.library.decision.Decision.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision$.html#apply-fffff34d)

```scala
def apply[U](range: Seq[U])(implicit name: Name[U], collection: ElementCollection): Decision[Int, U]
```

Create a CachingDecision with no parent and sequence of the possible actions of the decision. Uses an exact PolicyMaker.

Type parameters: `` U ``.

Parameters, list 1: `` range: Seq[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Decision[Int, U] ``.

Source contract/attributes: Create a CachingDecision with no parent and sequence of the possible actions of the decision. Uses an exact PolicyMaker. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.Decision.apply[U](range)(using name, collection)
```

## `` com.cra.figaro.library.decision.Decision.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision$.html#apply-a5f)

```scala
def apply[T, U](arg1: Element[T], range: Seq[U])(implicit name: Name[U], collection: ElementCollection): Decision[T, U]
```

Create a CachingDecision with one parent and sequence of the possible actions of the decision. Uses an exact PolicyMaker.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` arg1: Element[T] ``; `` range: Seq[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Decision[T, U] ``.

Source contract/attributes: Create a CachingDecision with one parent and sequence of the possible actions of the decision. Uses an exact PolicyMaker. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.Decision.apply[T, U](arg1, range)(using name, collection)
```

## `` com.cra.figaro.library.decision.Decision.getPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#getPolicy-ade)

```scala
def getPolicy(p: T): Element[U]
```

Get the decision for a specific value of the parent.

Type parameters: none.

Parameters, list 1: `` p: T ``.

Returns: `` Element[U] ``.

Source contract/attributes: Get the decision for a specific value of the parent. Attributes

Invocation template:

```scala
receiver.getPolicy(p)
```

## `` com.cra.figaro.library.decision.Decision.getPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#getPolicy-fffff2f6)

```scala
def getPolicy(): DecisionPolicy[T, U]
```

Get the complete policy object for this decision.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` DecisionPolicy[T, U] ``.

Source contract/attributes: Get the complete policy object for this decision. Attributes

Invocation template:

```scala
receiver.getPolicy()
```

## `` com.cra.figaro.library.decision.Decision.setPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#setPolicy-94c)

```scala
def setPolicy(): Unit
```

Set the policy for this decision using the DecisionPolicy class stored in the Decision.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Set the policy for this decision using the DecisionPolicy class stored in the Decision. Attributes

Invocation template:

```scala
receiver.setPolicy()
```

## `` com.cra.figaro.library.decision.Decision.setPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#setPolicy-fb7)

```scala
def setPolicy(Alg: DecisionAlgorithm[T, U]): Unit
```

Set the policy for this decision from an Algorithm run on this decision.

Type parameters: none.

Parameters, list 1: `` Alg: DecisionAlgorithm[T, U] ``.

Returns: `` Unit ``.

Source contract/attributes: Set the policy for this decision from an Algorithm run on this decision. Attributes

Invocation template:

```scala
receiver.setPolicy(Alg)
```

## `` com.cra.figaro.library.decision.Decision.setPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#setPolicy-fffffa74)

```scala
def setPolicy(policyMap: Map[(T, U), DecisionSample]): Unit
```

Set the policy for this decision using a map from parent/decision tuple values to decision samples.

Type parameters: none.

Parameters, list 1: `` policyMap: Map[(T, U), DecisionSample] ``.

Returns: `` Unit ``.

Source contract/attributes: Set the policy for this decision using a map from parent/decision tuple values to decision samples. Attributes

Invocation template:

```scala
receiver.setPolicy(policyMap)
```

## `` com.cra.figaro.library.decision.Decision.setPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#setPolicy-c9d)

```scala
def setPolicy(new_fcn: T => Element[U]): Unit
```

Directly set the policy of this decision using a function from a parent value to a decision element.

Type parameters: none.

Parameters, list 1: `` new_fcn: T => Element[U] ``.

Returns: `` Unit ``.

Source contract/attributes: Directly set the policy of this decision using a function from a parent value to a decision element. Attributes

Invocation template:

```scala
receiver.setPolicy(new_fcn)
```

## `` com.cra.figaro.library.decision.Decision.!== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#!==-894)

```scala
def !==(that: Element[Value])(implicit universe: Universe): Neq[U]
```

The element that tests inequality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Neq[U] ``.

Source contract/attributes: The element that tests inequality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.!==(that)(using universe)
```

## `` com.cra.figaro.library.decision.Decision.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#===-f71)

```scala
def ===(that: Value)(implicit universe: Universe): Apply1[U, Boolean]
```

The element that tests whether the value of this element is equal to a particular value.

Type parameters: none.

Parameters, list 1: `` that: Value ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Apply1[U, Boolean] ``.

Source contract/attributes: The element that tests whether the value of this element is equal to a particular value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.library.decision.Decision.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#===-672)

```scala
def ===(that: Element[Value])(implicit universe: Universe): Eq[U]
```

The element that tests equality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Eq[U] ``.

Source contract/attributes: The element that tests equality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.library.decision.Decision.activate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#activate-94c)

```scala
def activate(): Unit
```

Activate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Activate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.activate()
```

## `` com.cra.figaro.library.decision.Decision.addCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#addCondition-fffff9ad)

```scala
def addCondition(condition: Condition, contingency: Contingency = ...): Unit
```

Add the given condition to the existing conditions of the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` condition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add the given condition to the existing conditions of the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addCondition(condition, contingency)
```

## `` com.cra.figaro.library.decision.Decision.addConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#addConstraint-fffff9ad)

```scala
def addConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addConstraint(constraint, contingency)
```

## `` com.cra.figaro.library.decision.Decision.addLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#addLogConstraint-fffff9ad)

```scala
def addLogConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a log contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a log contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addLogConstraint(constraint, contingency)
```

## `` com.cra.figaro.library.decision.Decision.addPragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#addPragma-b53)

```scala
def addPragma(pragma: Pragma[Value]): Unit
```

Add a pragma to the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Add a pragma to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addPragma(pragma)
```

## `` com.cra.figaro.library.decision.Decision.allConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#allConditions-0)

```scala
def allConditions: List[(Condition, Contingency)]
```

All the conditions defined on this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Condition, Contingency)] ``.

Source contract/attributes: All the conditions defined on this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConditions
```

## `` com.cra.figaro.library.decision.Decision.allConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#allConstraints-0)

```scala
def allConstraints: List[(Constraint, Contingency)]
```

The current soft constraints on the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Constraint, Contingency)] ``.

Source contract/attributes: The current soft constraints on the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConstraints
```

## `` com.cra.figaro.library.decision.Decision.args ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#args-0)

```scala
def args: List[Element[_]]
```

The arguments on which the element depends.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: The arguments on which the element depends. Attributes Inherited from: Chain

Invocation template:

```scala
receiver.args
```

## `` com.cra.figaro.library.decision.Decision.clearContext ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#clearContext-94c)

```scala
def clearContext(): Unit
```

Clears all the temporary elements associated with this element (all elements created in it's context).

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clears all the temporary elements associated with this element (all elements created in it's context). Attributes Inherited from: Element

Invocation template:

```scala
receiver.clearContext()
```

## `` com.cra.figaro.library.decision.Decision.condition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#condition-4ad)

```scala
def condition(value: Any): Boolean
```

Tests whether all the element's contingent conditions are satisfied for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether all the element's contingent conditions are satisfied for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.condition(value)
```

## `` com.cra.figaro.library.decision.Decision.conditionSatisfied ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#conditionSatisfied-0)

```scala
def conditionSatisfied: Boolean
```

Determines whether the condition on the element is satisfied by the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Determines whether the condition on the element is satisfied by the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.conditionSatisfied
```

## `` com.cra.figaro.library.decision.Decision.constraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#constraint-fffffeac)

```scala
def constraint(value: Any): Double
```

Gets the result of all the element's contingent constraints for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Double ``.

Source contract/attributes: Gets the result of all the element's contingent constraints for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraint(value)
```

## `` com.cra.figaro.library.decision.Decision.constraintValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#constraintValue-0)

```scala
def constraintValue: Double
```

Determines the value of the constraint on the element applied to the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Determines the value of the constraint on the element applied to the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraintValue
```

## `` com.cra.figaro.library.decision.Decision.context ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#context-0)

```scala
def context: List[Element[_]]
```

The elements on which the existence of this element depends.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: The elements on which the existence of this element depends. Attributes Inherited from: Element

Invocation template:

```scala
receiver.context
```

## `` com.cra.figaro.library.decision.Decision.deactivate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#deactivate-94c)

```scala
def deactivate(): Unit
```

Deactivate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Deactivate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.deactivate()
```

## `` com.cra.figaro.library.decision.Decision.directContextContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#directContextContents-0)

```scala
def directContextContents: Set[Element[_]]
```

Returns the set of elements directly created in the context of this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the set of elements directly created in the context of this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.directContextContents
```

## `` com.cra.figaro.library.decision.Decision.elementsIAmContingentOn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#elementsIAmContingentOn-0)

```scala
def elementsIAmContingentOn: Set[Element[_]]
```

Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value. Figaro takes care of handling all this under the hood. However, some algorithms may need to know which elements an element is contingent on. For example, sampling algorithms may need to sample those other elements first. This method is supplied to support this use case. Attributes Inherited from: Element

Invocation template:

```scala
receiver.elementsIAmContingentOn
```

## `` com.cra.figaro.library.decision.Decision.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#flatMap-db8)

```scala
def flatMap[U](fn: Value => Element[U])(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => Element[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.flatMap[U](fn)(using name, collection)
```

## `` com.cra.figaro.library.decision.Decision.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#generate-94c)

```scala
final def generate(): Unit
```

First generate the randomness, then generate the value given the randomness. Store the results in randomness and value.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: First generate the randomness, then generate the value given the randomness. Store the results in randomness and value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.generate()
```

## `` com.cra.figaro.library.decision.Decision.generateRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#generateRandomness-54f)

```scala
def generateRandomness(): Randomness
```

Generate the randomness content.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Randomness ``.

Source contract/attributes: Generate the randomness content. Attributes Inherited from: Deterministic

Invocation template:

```scala
receiver.generateRandomness()
```

## `` com.cra.figaro.library.decision.Decision.generateValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#generateValue-993)

```scala
def generateValue(): U
```

Generate a value with no randomness.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` U ``.

Source contract/attributes: Generate a value with no randomness. Attributes Inherited from: Chain

Invocation template:

```scala
receiver.generateValue()
```

## `` com.cra.figaro.library.decision.Decision.generateValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#generateValue-fffff0e2)

```scala
def generateValue(rand: Randomness): U
```

Generate the value of the element deterministically given its randomness and the values of its arguments.

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` U ``.

Source contract/attributes: Generate the value of the element deterministically given its randomness and the values of its arguments. Attributes Inherited from: Deterministic

Invocation template:

```scala
receiver.generateValue(rand)
```

## `` com.cra.figaro.library.decision.Decision.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#get-ade)

```scala
def get(parentValue: T): Element[U]
```

Get the distribution over the result corresponding to the given parent value.

Type parameters: none.

Parameters, list 1: `` parentValue: T ``.

Returns: `` Element[U] ``.

Source contract/attributes: Get the distribution over the result corresponding to the given parent value. Attributes Inherited from: Chain

Invocation template:

```scala
receiver.get(parentValue)
```

## `` com.cra.figaro.library.decision.Decision.intervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#intervene-fffff71f)

```scala
def intervene(v: Value): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` v: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.intervene(v)
```

## `` com.cra.figaro.library.decision.Decision.isCachable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#isCachable-0)

```scala
def isCachable: Boolean
```

The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isCachable
```

## `` com.cra.figaro.library.decision.Decision.isTemporary ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#isTemporary-0)

```scala
def isTemporary: Boolean
```

Returns true if this element is temporary, that is, was created in the context of another element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Returns true if this element is temporary, that is, was created in the context of another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isTemporary
```

## `` com.cra.figaro.library.decision.Decision.makePolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#makePolicy-121)

```scala
def makePolicy(alg: DecisionAlgorithm[T, U]): DecisionPolicy[T, U]
```

Returns a DecisionPolicy from the given decision algorithm .

Type parameters: none.

Parameters, list 1: `` alg: DecisionAlgorithm[T, U] ``.

Returns: `` DecisionPolicy[T, U] ``.

Source contract/attributes: Returns a DecisionPolicy from the given decision algorithm . Attributes Inherited from: PolicyMaker

Invocation template:

```scala
receiver.makePolicy(alg)
```

## `` com.cra.figaro.library.decision.Decision.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#map-db8)

```scala
def map[U](fn: Value => U)(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => U ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.map[U](fn)(using name, collection)
```

## `` com.cra.figaro.library.decision.Decision.nextRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#nextRandomness-fffff446)

```scala
def nextRandomness(rand: Randomness): (Randomness, Double, Double)
```

Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is:

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` (Randomness, Double, Double) ``.

Source contract/attributes: Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is: P(new -> old) / P(old -> new) and the model probability ratio, which is: P(new) / P(old) The default implementation is to use generateRandomness and returns ones for the proposal and model probability ratios. Attributes Inherited from: Element

Invocation template:

```scala
receiver.nextRandomness(rand)
```

## `` com.cra.figaro.library.decision.Decision.observe ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#observe-fffff71f)

```scala
def observe(observation: Value): Unit
```

Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated.

Type parameters: none.

Parameters, list 1: `` observation: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.observe(observation)
```

## `` com.cra.figaro.library.decision.Decision.pragmas ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#pragmas-0)

```scala
def pragmas: List[Pragma[Value]]
```

The pragmas attached to the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Pragma[Value]] ``.

Source contract/attributes: The pragmas attached to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.pragmas
```

## `` com.cra.figaro.library.decision.Decision.removeConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#removeConditions-71e)

```scala
def removeConditions(contingency: Contingency = ...): Unit
```

Remove all conditions associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all conditions associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConditions(contingency)
```

## `` com.cra.figaro.library.decision.Decision.removeConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#removeConstraints-71e)

```scala
def removeConstraints(contingency: Contingency = ...): Unit
```

Remove all constraints associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all constraints associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConstraints(contingency)
```

## `` com.cra.figaro.library.decision.Decision.removePragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#removePragma-b53)

```scala
def removePragma(pragma: Pragma[Value]): Unit
```

Remove a pragma from the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Remove a pragma from the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removePragma(pragma)
```

## `` com.cra.figaro.library.decision.Decision.score ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#score-fffffdbf)

```scala
def score(oldValue: Value, newValue: Value): Double
```

Compute the constraints on the new value divided by the constraints on the old value.

Type parameters: none.

Parameters, list 1: `` oldValue: Value ``; `` newValue: Value ``.

Returns: `` Double ``.

Source contract/attributes: Compute the constraints on the new value divided by the constraints on the old value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.score(oldValue, newValue)
```

## `` com.cra.figaro.library.decision.Decision.set ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#set-fffff71f)

```scala
def set(newValue: Value): Unit
```

Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called.

Type parameters: none.

Parameters, list 1: `` newValue: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called. Attributes Inherited from: Element

Invocation template:

```scala
receiver.set(newValue)
```

## `` com.cra.figaro.library.decision.Decision.setCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#setCondition-fffff9ad)

```scala
def setCondition(newCondition: Condition, contingency: Contingency = ...): Unit
```

Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newCondition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setCondition(newCondition, contingency)
```

## `` com.cra.figaro.library.decision.Decision.setConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#setConstraint-fffff9ad)

```scala
def setConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.library.decision.Decision.setLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#setLogConstraint-fffff9ad)

```scala
def setLogConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setLogConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.library.decision.Decision.setRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#setRandomness-fffff71f)

```scala
def setRandomness(newRandomness: Randomness): Unit
```

Set the randomness of this element.

Type parameters: none.

Parameters, list 1: `` newRandomness: Randomness ``.

Returns: `` Unit ``.

Source contract/attributes: Set the randomness of this element. Will generate its value using the new randomness and propagate the effects to elements that depend on it without changing their randomness. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setRandomness(newRandomness)
```

## `` com.cra.figaro.library.decision.Decision.toNameString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#toNameString-0)

```scala
def toNameString: String
```

A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString. Attributes Inherited from: Element

Invocation template:

```scala
receiver.toNameString
```

## `` com.cra.figaro.library.decision.Decision.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Chain -> Any Inherited from: Chain

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.library.decision.Decision.unintervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#unintervene-94c)

```scala
def unintervene(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.unintervene()
```

## `` com.cra.figaro.library.decision.Decision.unobserve ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#unobserve-94c)

```scala
def unobserve(): Unit
```

Removes conditions on the element and allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Removes conditions on the element and allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unobserve()
```

## `` com.cra.figaro.library.decision.Decision.unset ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#unset-94c)

```scala
def unset(): Unit
```

Allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unset()
```

## `` com.cra.figaro.library.decision.Decision.makePolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/Decision.html#makePolicy-61e)

```scala
def makePolicy(policyMap: Map[(T, U), DecisionSample]): DecisionPolicy[T, U]
```

Returns a DecisionPolicy from a map of parent/decision tuple values to DecisionSamples.

Type parameters: none.

Parameters, list 1: `` policyMap: Map[(T, U), DecisionSample] ``.

Returns: `` DecisionPolicy[T, U] ``.

Source contract/attributes: Returns a DecisionPolicy from a map of parent/decision tuple values to DecisionSamples. Attributes Inherited from: PolicyMaker

Invocation template:

```scala
receiver.makePolicy(policyMap)
```

## `` com.cra.figaro.library.decision.DecisionIllegalArgumentException.addSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionIllegalArgumentException.html#addSuppressed-4b8)

```scala
final def addSuppressed(x$0: Throwable): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.addSuppressed(x$0)
```

## `` com.cra.figaro.library.decision.DecisionIllegalArgumentException.fillInStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionIllegalArgumentException.html#fillInStackTrace-26c)

```scala
def fillInStackTrace(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.fillInStackTrace()
```

## `` com.cra.figaro.library.decision.DecisionIllegalArgumentException.getCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionIllegalArgumentException.html#getCause-26c)

```scala
def getCause(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getCause()
```

## `` com.cra.figaro.library.decision.DecisionIllegalArgumentException.getLocalizedMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionIllegalArgumentException.html#getLocalizedMessage-665)

```scala
def getLocalizedMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getLocalizedMessage()
```

## `` com.cra.figaro.library.decision.DecisionIllegalArgumentException.getMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionIllegalArgumentException.html#getMessage-665)

```scala
def getMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getMessage()
```

## `` com.cra.figaro.library.decision.DecisionIllegalArgumentException.getStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionIllegalArgumentException.html#getStackTrace-fffffa6d)

```scala
def getStackTrace(): Array[StackTraceElement]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[StackTraceElement] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getStackTrace()
```

## `` com.cra.figaro.library.decision.DecisionIllegalArgumentException.getSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionIllegalArgumentException.html#getSuppressed-fffff2ce)

```scala
final def getSuppressed(): Array[Throwable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[Throwable] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getSuppressed()
```

## `` com.cra.figaro.library.decision.DecisionIllegalArgumentException.initCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionIllegalArgumentException.html#initCause-fffff080)

```scala
def initCause(x$0: Throwable): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.initCause(x$0)
```

## `` com.cra.figaro.library.decision.DecisionIllegalArgumentException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionIllegalArgumentException.html#printStackTrace-fffff230)

```scala
def printStackTrace(x$0: PrintWriter): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintWriter ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.library.decision.DecisionIllegalArgumentException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionIllegalArgumentException.html#printStackTrace-fffff0fd)

```scala
def printStackTrace(x$0: PrintStream): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintStream ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.library.decision.DecisionIllegalArgumentException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionIllegalArgumentException.html#printStackTrace-94c)

```scala
def printStackTrace(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace()
```

## `` com.cra.figaro.library.decision.DecisionIllegalArgumentException.setStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionIllegalArgumentException.html#setStackTrace-f79)

```scala
def setStackTrace(x$0: Array[StackTraceElement]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Array[StackTraceElement] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.setStackTrace(x$0)
```

## `` com.cra.figaro.library.decision.DecisionIllegalArgumentException.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionIllegalArgumentException.html#toString-665)

```scala
def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Inherited from: Throwable

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.library.decision.DecisionSample.+ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionSample.html#+-c74)

```scala
def +(that: DecisionSample): DecisionSample
```

Adding two DecisionSamples produces the sum of their weighted utilities and their total weight.

Type parameters: none.

Parameters, list 1: `` that: DecisionSample ``.

Returns: `` DecisionSample ``.

Source contract/attributes: Adding two DecisionSamples produces the sum of their weighted utilities and their total weight. Attributes

Invocation template:

```scala
receiver.+(that)
```

## `` com.cra.figaro.library.decision.DecisionSample.norm ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionSample.html#norm-0)

```scala
def norm: Double
```

Normalize a Decision sample's weighted utility by its weight

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Normalize a Decision sample's weighted utility by its weight Attributes

Invocation template:

```scala
receiver.norm
```

## `` com.cra.figaro.library.decision.DecisionSample.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionSample.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.library.decision.DecisionSample.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionSample.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.library.decision.DecisionUtil.createDecisionDummy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionUtil$.html#createDecisionDummy-12f)

```scala
def createDecisionDummy[T, U](target: Decision[T, U]): Element[(T, U)]
```

Creates a dummy element pair of (parent, decision) used for inference algorithms.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` target: Decision[T, U] ``.

Returns: `` Element[(T, U)] ``.

Source contract/attributes: Creates a dummy element pair of (parent, decision) used for inference algorithms. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.DecisionUtil.createDecisionDummy[T, U](target)
```

## `` com.cra.figaro.library.decision.DecisionUtil.getDecisionOrder ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionUtil$.html#getDecisionOrder-fffff0c8)

```scala
def getDecisionOrder(Decisions: Iterable[Element[_]], universe: Universe): List[List[Element[_]]]
```

Computes the partial order of dependent decisions.

Type parameters: none.

Parameters, list 1: `` Decisions: Iterable[Element[_]] ``; `` universe: Universe ``.

Returns: `` List[List[Element[_]]] ``.

Source contract/attributes: Computes the partial order of dependent decisions. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.DecisionUtil.getDecisionOrder(Decisions, universe)
```

## `` com.cra.figaro.library.decision.DecisionUtil.getElemsForDecision ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionUtil$.html#getElemsForDecision-fffff251)

```scala
def getElemsForDecision(D: Element[_], utilities: List[Element[_]], Order: List[List[Element[_]]], universe: Universe): List[Element[_]]
```

Computes all the elements that need to be simulated in order to compute a strategy for a decision.

Type parameters: none.

Parameters, list 1: `` D: Element[_] ``; `` utilities: List[Element[_]] ``; `` Order: List[List[Element[_]]] ``; `` universe: Universe ``.

Returns: `` List[Element[_]] ``.

Source contract/attributes: Computes all the elements that need to be simulated in order to compute a strategy for a decision. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.DecisionUtil.getElemsForDecision(D, utilities, Order, universe)
```

## `` com.cra.figaro.library.decision.DecisionUtil.getPredDecisions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionUtil$.html#getPredDecisions-fffff9c1)

```scala
def getPredDecisions(D: Element[_], Order: List[List[Element[_]]], universe: Universe): List[Element[_]]
```

Computes all the relevant decisions that come before a decision in the decision order. It does not return decisions that are partially ordered with D.

Type parameters: none.

Parameters, list 1: `` D: Element[_] ``; `` Order: List[List[Element[_]]] ``; `` universe: Universe ``.

Returns: `` List[Element[_]] ``.

Source contract/attributes: Computes all the relevant decisions that come before a decision in the decision order. It does not return decisions that are partially ordered with D. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.DecisionUtil.getPredDecisions(D, Order, universe)
```

## `` com.cra.figaro.library.decision.DecisionUtil.getReleventUtil ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionUtil$.html#getReleventUtil-fffff251)

```scala
def getReleventUtil(D: Element[_], utilities: List[Element[_]], Order: List[List[Element[_]]], universe: Universe): List[Element[_]]
```

Computes the relevant utility elements for a specific decision.

Type parameters: none.

Parameters, list 1: `` D: Element[_] ``; `` utilities: List[Element[_]] ``; `` Order: List[List[Element[_]]] ``; `` universe: Universe ``.

Returns: `` List[Element[_]] ``.

Source contract/attributes: Computes the relevant utility elements for a specific decision. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.DecisionUtil.getReleventUtil(D, utilities, Order, universe)
```

## `` com.cra.figaro.library.decision.DecisionUtil.getSuccDecisions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionUtil$.html#getSuccDecisions-fffff9c1)

```scala
def getSuccDecisions(D: Element[_], Order: List[List[Element[_]]], universe: Universe): List[Element[_]]
```

Computes the decisions that come DIRECTLY after a decision.

Type parameters: none.

Parameters, list 1: `` D: Element[_] ``; `` Order: List[List[Element[_]]] ``; `` universe: Universe ``.

Returns: `` List[Element[_]] ``.

Source contract/attributes: Computes the decisions that come DIRECTLY after a decision. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.DecisionUtil.getSuccDecisions(D, Order, universe)
```

## `` com.cra.figaro.library.decision.DecisionUtil.utilitiesInElems ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionUtil$.html#utilitiesInElems-e92)

```scala
def utilitiesInElems(Elems: List[Element[_]], U: List[Element[_]]): List[Element[_]]
```

Returns the utilities in a list of elements from a reference list of utilities.

Type parameters: none.

Parameters, list 1: `` Elems: List[Element[_]] ``; `` U: List[Element[_]] ``.

Returns: `` List[Element[_]] ``.

Source contract/attributes: Returns the utilities in a list of elements from a reference list of utilities. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.DecisionUtil.utilitiesInElems(Elems, U)
```

## `` com.cra.figaro.library.decision.DecisionUtil.utilitiesUse ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/DecisionUtil$.html#utilitiesUse-ef8)

```scala
def utilitiesUse(U: Iterable[Element[_]], universe: Universe): Set[Element[_]]
```

Computes the set of elements that the given list of utilities depends upon for computation.

Type parameters: none.

Parameters, list 1: `` U: Iterable[Element[_]] ``; `` universe: Universe ``.

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Computes the set of elements that the given list of utilities depends upon for computation. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.DecisionUtil.utilitiesUse(U, universe)
```

## `` com.cra.figaro.library.decision.ExactPolicyMaker.makePolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/ExactPolicyMaker.html#makePolicy-61e)

```scala
def makePolicy(policyMap: Map[(T, U), DecisionSample]): DecisionPolicy[T, U]
```

Returns a DecisionPolicy from a map of parent/decision tuple values to DecisionSamples.

Type parameters: none.

Parameters, list 1: `` policyMap: Map[(T, U), DecisionSample] ``.

Returns: `` DecisionPolicy[T, U] ``.

Source contract/attributes: Returns a DecisionPolicy from a map of parent/decision tuple values to DecisionSamples. Attributes

Invocation template:

```scala
receiver.makePolicy(policyMap)
```

## `` com.cra.figaro.library.decision.ExactPolicyMaker.makePolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/ExactPolicyMaker.html#makePolicy-121)

```scala
def makePolicy(alg: DecisionAlgorithm[T, U]): DecisionPolicy[T, U]
```

Returns a DecisionPolicy from the given decision algorithm .

Type parameters: none.

Parameters, list 1: `` alg: DecisionAlgorithm[T, U] ``.

Returns: `` DecisionPolicy[T, U] ``.

Source contract/attributes: Returns a DecisionPolicy from the given decision algorithm . Attributes Inherited from: PolicyMaker

Invocation template:

```scala
receiver.makePolicy(alg)
```

## `` com.cra.figaro.library.decision.IllegalDecisionNetwork.addSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalDecisionNetwork.html#addSuppressed-4b8)

```scala
final def addSuppressed(x$0: Throwable): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.addSuppressed(x$0)
```

## `` com.cra.figaro.library.decision.IllegalDecisionNetwork.fillInStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalDecisionNetwork.html#fillInStackTrace-26c)

```scala
def fillInStackTrace(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.fillInStackTrace()
```

## `` com.cra.figaro.library.decision.IllegalDecisionNetwork.getCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalDecisionNetwork.html#getCause-26c)

```scala
def getCause(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getCause()
```

## `` com.cra.figaro.library.decision.IllegalDecisionNetwork.getLocalizedMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalDecisionNetwork.html#getLocalizedMessage-665)

```scala
def getLocalizedMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getLocalizedMessage()
```

## `` com.cra.figaro.library.decision.IllegalDecisionNetwork.getMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalDecisionNetwork.html#getMessage-665)

```scala
def getMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getMessage()
```

## `` com.cra.figaro.library.decision.IllegalDecisionNetwork.getStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalDecisionNetwork.html#getStackTrace-fffffa6d)

```scala
def getStackTrace(): Array[StackTraceElement]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[StackTraceElement] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getStackTrace()
```

## `` com.cra.figaro.library.decision.IllegalDecisionNetwork.getSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalDecisionNetwork.html#getSuppressed-fffff2ce)

```scala
final def getSuppressed(): Array[Throwable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[Throwable] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getSuppressed()
```

## `` com.cra.figaro.library.decision.IllegalDecisionNetwork.initCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalDecisionNetwork.html#initCause-fffff080)

```scala
def initCause(x$0: Throwable): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.initCause(x$0)
```

## `` com.cra.figaro.library.decision.IllegalDecisionNetwork.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalDecisionNetwork.html#printStackTrace-fffff230)

```scala
def printStackTrace(x$0: PrintWriter): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintWriter ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.library.decision.IllegalDecisionNetwork.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalDecisionNetwork.html#printStackTrace-fffff0fd)

```scala
def printStackTrace(x$0: PrintStream): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintStream ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.library.decision.IllegalDecisionNetwork.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalDecisionNetwork.html#printStackTrace-94c)

```scala
def printStackTrace(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace()
```

## `` com.cra.figaro.library.decision.IllegalDecisionNetwork.setStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalDecisionNetwork.html#setStackTrace-f79)

```scala
def setStackTrace(x$0: Array[StackTraceElement]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Array[StackTraceElement] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.setStackTrace(x$0)
```

## `` com.cra.figaro.library.decision.IllegalDecisionNetwork.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalDecisionNetwork.html#toString-665)

```scala
def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Inherited from: Throwable

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.library.decision.IllegalUtilityNodeException.addSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalUtilityNodeException.html#addSuppressed-4b8)

```scala
final def addSuppressed(x$0: Throwable): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.addSuppressed(x$0)
```

## `` com.cra.figaro.library.decision.IllegalUtilityNodeException.fillInStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalUtilityNodeException.html#fillInStackTrace-26c)

```scala
def fillInStackTrace(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.fillInStackTrace()
```

## `` com.cra.figaro.library.decision.IllegalUtilityNodeException.getCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalUtilityNodeException.html#getCause-26c)

```scala
def getCause(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getCause()
```

## `` com.cra.figaro.library.decision.IllegalUtilityNodeException.getLocalizedMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalUtilityNodeException.html#getLocalizedMessage-665)

```scala
def getLocalizedMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getLocalizedMessage()
```

## `` com.cra.figaro.library.decision.IllegalUtilityNodeException.getMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalUtilityNodeException.html#getMessage-665)

```scala
def getMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getMessage()
```

## `` com.cra.figaro.library.decision.IllegalUtilityNodeException.getStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalUtilityNodeException.html#getStackTrace-fffffa6d)

```scala
def getStackTrace(): Array[StackTraceElement]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[StackTraceElement] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getStackTrace()
```

## `` com.cra.figaro.library.decision.IllegalUtilityNodeException.getSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalUtilityNodeException.html#getSuppressed-fffff2ce)

```scala
final def getSuppressed(): Array[Throwable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[Throwable] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getSuppressed()
```

## `` com.cra.figaro.library.decision.IllegalUtilityNodeException.initCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalUtilityNodeException.html#initCause-fffff080)

```scala
def initCause(x$0: Throwable): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.initCause(x$0)
```

## `` com.cra.figaro.library.decision.IllegalUtilityNodeException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalUtilityNodeException.html#printStackTrace-fffff230)

```scala
def printStackTrace(x$0: PrintWriter): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintWriter ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.library.decision.IllegalUtilityNodeException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalUtilityNodeException.html#printStackTrace-fffff0fd)

```scala
def printStackTrace(x$0: PrintStream): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintStream ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.library.decision.IllegalUtilityNodeException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalUtilityNodeException.html#printStackTrace-94c)

```scala
def printStackTrace(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace()
```

## `` com.cra.figaro.library.decision.IllegalUtilityNodeException.setStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalUtilityNodeException.html#setStackTrace-f79)

```scala
def setStackTrace(x$0: Array[StackTraceElement]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Array[StackTraceElement] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.setStackTrace(x$0)
```

## `` com.cra.figaro.library.decision.IllegalUtilityNodeException.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/IllegalUtilityNodeException.html#toString-665)

```scala
def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Inherited from: Throwable

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.library.decision.NonCachingDecision.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision$.html#apply-c27)

```scala
def apply[U](fcn: () => Element[U])(implicit name: Name[U], collection: ElementCollection, conversion: Int => Distance[Int]): Decision[Int, U]
```

Create a NonCachingDecision with no parent that uses an approximate (kNN) PolicyMaker.

Type parameters: `` U ``.

Parameters, list 1: `` fcn: () => Element[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``; `` conversion: Int => Distance[Int] ``.

Returns: `` Decision[Int, U] ``.

Source contract/attributes: Create a NonCachingDecision with no parent that uses an approximate (kNN) PolicyMaker. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.NonCachingDecision.apply[U](fcn)(using name, collection, conversion)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision$.html#apply-bba)

```scala
def apply[T, U](arg1: Element[T], fcn: T => Element[U])(implicit name: Name[U], collection: ElementCollection, conversion: T => Distance[T]): Decision[T, U]
```

Create a NonCachingDecision with one parent and a default function from values of the parent to Element[U]. Uses an approximate (kNN) PolicyMaker.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` arg1: Element[T] ``; `` fcn: T => Element[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``; `` conversion: T => Distance[T] ``.

Returns: `` Decision[T, U] ``.

Source contract/attributes: Create a NonCachingDecision with one parent and a default function from values of the parent to Element[U]. Uses an approximate (kNN) PolicyMaker. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.NonCachingDecision.apply[T, U](arg1, fcn)(using name, collection, conversion)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision$.html#apply-fffff504)

```scala
def apply[U](range: Seq[U])(implicit name: Name[U], collection: ElementCollection, conversion: Int => Distance[Int]): Decision[Int, U]
```

Create a NonCachingDecision with no parent and sequence of the possible actions of the decision. Uses an approximate (kNN) PolicyMaker.

Type parameters: `` U ``.

Parameters, list 1: `` range: Seq[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``; `` conversion: Int => Distance[Int] ``.

Returns: `` Decision[Int, U] ``.

Source contract/attributes: Create a NonCachingDecision with no parent and sequence of the possible actions of the decision. Uses an approximate (kNN) PolicyMaker. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.NonCachingDecision.apply[U](range)(using name, collection, conversion)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision$.html#apply-fffff7b2)

```scala
def apply[T, U](arg1: Element[T], range: Seq[U])(implicit name: Name[U], collection: ElementCollection, conversion: T => Distance[T]): Decision[T, U]
```

Create a NonCachingDecision with one parent and sequence of the possible actions of the decision. Uses an approximate (kNN) PolicyMaker.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` arg1: Element[T] ``; `` range: Seq[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``; `` conversion: T => Distance[T] ``.

Returns: `` Decision[T, U] ``.

Source contract/attributes: Create a NonCachingDecision with one parent and sequence of the possible actions of the decision. Uses an approximate (kNN) PolicyMaker. Attributes

Invocation template:

```scala
com.cra.figaro.library.decision.NonCachingDecision.apply[T, U](arg1, range)(using name, collection, conversion)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Chain -> Any

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.library.decision.NonCachingDecision.!== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#!==-894)

```scala
def !==(that: Element[Value])(implicit universe: Universe): Neq[U]
```

The element that tests inequality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Neq[U] ``.

Source contract/attributes: The element that tests inequality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.!==(that)(using universe)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#===-f71)

```scala
def ===(that: Value)(implicit universe: Universe): Apply1[U, Boolean]
```

The element that tests whether the value of this element is equal to a particular value.

Type parameters: none.

Parameters, list 1: `` that: Value ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Apply1[U, Boolean] ``.

Source contract/attributes: The element that tests whether the value of this element is equal to a particular value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#===-672)

```scala
def ===(that: Element[Value])(implicit universe: Universe): Eq[U]
```

The element that tests equality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Eq[U] ``.

Source contract/attributes: The element that tests equality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.activate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#activate-94c)

```scala
def activate(): Unit
```

Activate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Activate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.activate()
```

## `` com.cra.figaro.library.decision.NonCachingDecision.addCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#addCondition-fffff9ad)

```scala
def addCondition(condition: Condition, contingency: Contingency = ...): Unit
```

Add the given condition to the existing conditions of the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` condition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add the given condition to the existing conditions of the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addCondition(condition, contingency)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.addConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#addConstraint-fffff9ad)

```scala
def addConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addConstraint(constraint, contingency)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.addLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#addLogConstraint-fffff9ad)

```scala
def addLogConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a log contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a log contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addLogConstraint(constraint, contingency)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.addPragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#addPragma-b53)

```scala
def addPragma(pragma: Pragma[Value]): Unit
```

Add a pragma to the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Add a pragma to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addPragma(pragma)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.allConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#allConditions-0)

```scala
def allConditions: List[(Condition, Contingency)]
```

All the conditions defined on this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Condition, Contingency)] ``.

Source contract/attributes: All the conditions defined on this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConditions
```

## `` com.cra.figaro.library.decision.NonCachingDecision.allConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#allConstraints-0)

```scala
def allConstraints: List[(Constraint, Contingency)]
```

The current soft constraints on the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Constraint, Contingency)] ``.

Source contract/attributes: The current soft constraints on the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConstraints
```

## `` com.cra.figaro.library.decision.NonCachingDecision.args ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#args-0)

```scala
def args: List[Element[_]]
```

The arguments on which the element depends.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: The arguments on which the element depends. Attributes Inherited from: Chain

Invocation template:

```scala
receiver.args
```

## `` com.cra.figaro.library.decision.NonCachingDecision.clearContext ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#clearContext-94c)

```scala
def clearContext(): Unit
```

Clears all the temporary elements associated with this element (all elements created in it's context).

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clears all the temporary elements associated with this element (all elements created in it's context). Attributes Inherited from: Element

Invocation template:

```scala
receiver.clearContext()
```

## `` com.cra.figaro.library.decision.NonCachingDecision.condition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#condition-4ad)

```scala
def condition(value: Any): Boolean
```

Tests whether all the element's contingent conditions are satisfied for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether all the element's contingent conditions are satisfied for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.condition(value)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.conditionSatisfied ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#conditionSatisfied-0)

```scala
def conditionSatisfied: Boolean
```

Determines whether the condition on the element is satisfied by the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Determines whether the condition on the element is satisfied by the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.conditionSatisfied
```

## `` com.cra.figaro.library.decision.NonCachingDecision.constraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#constraint-fffffeac)

```scala
def constraint(value: Any): Double
```

Gets the result of all the element's contingent constraints for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Double ``.

Source contract/attributes: Gets the result of all the element's contingent constraints for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraint(value)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.constraintValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#constraintValue-0)

```scala
def constraintValue: Double
```

Determines the value of the constraint on the element applied to the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Determines the value of the constraint on the element applied to the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraintValue
```

## `` com.cra.figaro.library.decision.NonCachingDecision.context ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#context-0)

```scala
def context: List[Element[_]]
```

The elements on which the existence of this element depends.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: The elements on which the existence of this element depends. Attributes Inherited from: Element

Invocation template:

```scala
receiver.context
```

## `` com.cra.figaro.library.decision.NonCachingDecision.deactivate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#deactivate-94c)

```scala
def deactivate(): Unit
```

Deactivate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Deactivate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.deactivate()
```

## `` com.cra.figaro.library.decision.NonCachingDecision.directContextContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#directContextContents-0)

```scala
def directContextContents: Set[Element[_]]
```

Returns the set of elements directly created in the context of this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the set of elements directly created in the context of this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.directContextContents
```

## `` com.cra.figaro.library.decision.NonCachingDecision.elementsIAmContingentOn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#elementsIAmContingentOn-0)

```scala
def elementsIAmContingentOn: Set[Element[_]]
```

Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value. Figaro takes care of handling all this under the hood. However, some algorithms may need to know which elements an element is contingent on. For example, sampling algorithms may need to sample those other elements first. This method is supplied to support this use case. Attributes Inherited from: Element

Invocation template:

```scala
receiver.elementsIAmContingentOn
```

## `` com.cra.figaro.library.decision.NonCachingDecision.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#flatMap-db8)

```scala
def flatMap[U](fn: Value => Element[U])(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => Element[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.flatMap[U](fn)(using name, collection)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#generate-94c)

```scala
final def generate(): Unit
```

First generate the randomness, then generate the value given the randomness. Store the results in randomness and value.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: First generate the randomness, then generate the value given the randomness. Store the results in randomness and value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.generate()
```

## `` com.cra.figaro.library.decision.NonCachingDecision.generateRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#generateRandomness-54f)

```scala
def generateRandomness(): Randomness
```

Generate the randomness content.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Randomness ``.

Source contract/attributes: Generate the randomness content. Attributes Inherited from: Deterministic

Invocation template:

```scala
receiver.generateRandomness()
```

## `` com.cra.figaro.library.decision.NonCachingDecision.generateValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#generateValue-993)

```scala
def generateValue(): U
```

Generate a value with no randomness.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` U ``.

Source contract/attributes: Generate a value with no randomness. Attributes Inherited from: Chain

Invocation template:

```scala
receiver.generateValue()
```

## `` com.cra.figaro.library.decision.NonCachingDecision.generateValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#generateValue-fffff0e2)

```scala
def generateValue(rand: Randomness): U
```

Generate the value of the element deterministically given its randomness and the values of its arguments.

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` U ``.

Source contract/attributes: Generate the value of the element deterministically given its randomness and the values of its arguments. Attributes Inherited from: Deterministic

Invocation template:

```scala
receiver.generateValue(rand)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#get-ade)

```scala
def get(parentValue: T): Element[U]
```

Get the distribution over the result corresponding to the given parent value.

Type parameters: none.

Parameters, list 1: `` parentValue: T ``.

Returns: `` Element[U] ``.

Source contract/attributes: Get the distribution over the result corresponding to the given parent value. Attributes Inherited from: Chain

Invocation template:

```scala
receiver.get(parentValue)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.getPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#getPolicy-fffff2f6)

```scala
def getPolicy(): DecisionPolicy[T, U]
```

Get the complete policy object for this decision.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` DecisionPolicy[T, U] ``.

Source contract/attributes: Get the complete policy object for this decision. Attributes Inherited from: Decision

Invocation template:

```scala
receiver.getPolicy()
```

## `` com.cra.figaro.library.decision.NonCachingDecision.getPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#getPolicy-ade)

```scala
def getPolicy(p: T): Element[U]
```

Get the decision for a specific value of the parent.

Type parameters: none.

Parameters, list 1: `` p: T ``.

Returns: `` Element[U] ``.

Source contract/attributes: Get the decision for a specific value of the parent. Attributes Inherited from: Decision

Invocation template:

```scala
receiver.getPolicy(p)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.intervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#intervene-fffff71f)

```scala
def intervene(v: Value): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` v: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.intervene(v)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.isCachable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#isCachable-0)

```scala
def isCachable: Boolean
```

The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isCachable
```

## `` com.cra.figaro.library.decision.NonCachingDecision.isTemporary ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#isTemporary-0)

```scala
def isTemporary: Boolean
```

Returns true if this element is temporary, that is, was created in the context of another element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Returns true if this element is temporary, that is, was created in the context of another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isTemporary
```

## `` com.cra.figaro.library.decision.NonCachingDecision.makePolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#makePolicy-121)

```scala
def makePolicy(alg: DecisionAlgorithm[T, U]): DecisionPolicy[T, U]
```

Returns a DecisionPolicy from the given decision algorithm .

Type parameters: none.

Parameters, list 1: `` alg: DecisionAlgorithm[T, U] ``.

Returns: `` DecisionPolicy[T, U] ``.

Source contract/attributes: Returns a DecisionPolicy from the given decision algorithm . Attributes Inherited from: PolicyMaker

Invocation template:

```scala
receiver.makePolicy(alg)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#map-db8)

```scala
def map[U](fn: Value => U)(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => U ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.map[U](fn)(using name, collection)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.nextRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#nextRandomness-fffff446)

```scala
def nextRandomness(rand: Randomness): (Randomness, Double, Double)
```

Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is:

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` (Randomness, Double, Double) ``.

Source contract/attributes: Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is: P(new -> old) / P(old -> new) and the model probability ratio, which is: P(new) / P(old) The default implementation is to use generateRandomness and returns ones for the proposal and model probability ratios. Attributes Inherited from: Element

Invocation template:

```scala
receiver.nextRandomness(rand)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.observe ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#observe-fffff71f)

```scala
def observe(observation: Value): Unit
```

Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated.

Type parameters: none.

Parameters, list 1: `` observation: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.observe(observation)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.pragmas ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#pragmas-0)

```scala
def pragmas: List[Pragma[Value]]
```

The pragmas attached to the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Pragma[Value]] ``.

Source contract/attributes: The pragmas attached to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.pragmas
```

## `` com.cra.figaro.library.decision.NonCachingDecision.removeConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#removeConditions-71e)

```scala
def removeConditions(contingency: Contingency = ...): Unit
```

Remove all conditions associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all conditions associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConditions(contingency)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.removeConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#removeConstraints-71e)

```scala
def removeConstraints(contingency: Contingency = ...): Unit
```

Remove all constraints associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all constraints associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConstraints(contingency)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.removePragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#removePragma-b53)

```scala
def removePragma(pragma: Pragma[Value]): Unit
```

Remove a pragma from the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Remove a pragma from the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removePragma(pragma)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.score ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#score-fffffdbf)

```scala
def score(oldValue: Value, newValue: Value): Double
```

Compute the constraints on the new value divided by the constraints on the old value.

Type parameters: none.

Parameters, list 1: `` oldValue: Value ``; `` newValue: Value ``.

Returns: `` Double ``.

Source contract/attributes: Compute the constraints on the new value divided by the constraints on the old value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.score(oldValue, newValue)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.set ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#set-fffff71f)

```scala
def set(newValue: Value): Unit
```

Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called.

Type parameters: none.

Parameters, list 1: `` newValue: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called. Attributes Inherited from: Element

Invocation template:

```scala
receiver.set(newValue)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.setCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#setCondition-fffff9ad)

```scala
def setCondition(newCondition: Condition, contingency: Contingency = ...): Unit
```

Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newCondition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setCondition(newCondition, contingency)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.setConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#setConstraint-fffff9ad)

```scala
def setConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.setLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#setLogConstraint-fffff9ad)

```scala
def setLogConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setLogConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.setPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#setPolicy-c9d)

```scala
def setPolicy(new_fcn: T => Element[U]): Unit
```

Directly set the policy of this decision using a function from a parent value to a decision element.

Type parameters: none.

Parameters, list 1: `` new_fcn: T => Element[U] ``.

Returns: `` Unit ``.

Source contract/attributes: Directly set the policy of this decision using a function from a parent value to a decision element. Attributes Inherited from: Decision

Invocation template:

```scala
receiver.setPolicy(new_fcn)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.setPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#setPolicy-fffffa74)

```scala
def setPolicy(policyMap: Map[(T, U), DecisionSample]): Unit
```

Set the policy for this decision using a map from parent/decision tuple values to decision samples.

Type parameters: none.

Parameters, list 1: `` policyMap: Map[(T, U), DecisionSample] ``.

Returns: `` Unit ``.

Source contract/attributes: Set the policy for this decision using a map from parent/decision tuple values to decision samples. Attributes Inherited from: Decision

Invocation template:

```scala
receiver.setPolicy(policyMap)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.setPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#setPolicy-fb7)

```scala
def setPolicy(Alg: DecisionAlgorithm[T, U]): Unit
```

Set the policy for this decision from an Algorithm run on this decision.

Type parameters: none.

Parameters, list 1: `` Alg: DecisionAlgorithm[T, U] ``.

Returns: `` Unit ``.

Source contract/attributes: Set the policy for this decision from an Algorithm run on this decision. Attributes Inherited from: Decision

Invocation template:

```scala
receiver.setPolicy(Alg)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.setPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#setPolicy-94c)

```scala
def setPolicy(): Unit
```

Set the policy for this decision using the DecisionPolicy class stored in the Decision.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Set the policy for this decision using the DecisionPolicy class stored in the Decision. Attributes Inherited from: Decision

Invocation template:

```scala
receiver.setPolicy()
```

## `` com.cra.figaro.library.decision.NonCachingDecision.setRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#setRandomness-fffff71f)

```scala
def setRandomness(newRandomness: Randomness): Unit
```

Set the randomness of this element.

Type parameters: none.

Parameters, list 1: `` newRandomness: Randomness ``.

Returns: `` Unit ``.

Source contract/attributes: Set the randomness of this element. Will generate its value using the new randomness and propagate the effects to elements that depend on it without changing their randomness. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setRandomness(newRandomness)
```

## `` com.cra.figaro.library.decision.NonCachingDecision.toNameString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#toNameString-0)

```scala
def toNameString: String
```

A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString. Attributes Inherited from: Element

Invocation template:

```scala
receiver.toNameString
```

## `` com.cra.figaro.library.decision.NonCachingDecision.unintervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#unintervene-94c)

```scala
def unintervene(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.unintervene()
```

## `` com.cra.figaro.library.decision.NonCachingDecision.unobserve ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#unobserve-94c)

```scala
def unobserve(): Unit
```

Removes conditions on the element and allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Removes conditions on the element and allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unobserve()
```

## `` com.cra.figaro.library.decision.NonCachingDecision.unset ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#unset-94c)

```scala
def unset(): Unit
```

Allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unset()
```

## `` com.cra.figaro.library.decision.NonCachingDecision.makePolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/NonCachingDecision.html#makePolicy-61e)

```scala
def makePolicy(policyMap: Map[(T, U), DecisionSample]): DecisionPolicy[T, U]
```

Returns a DecisionPolicy from a map of parent/decision tuple values to DecisionSamples.

Type parameters: none.

Parameters, list 1: `` policyMap: Map[(T, U), DecisionSample] ``.

Returns: `` DecisionPolicy[T, U] ``.

Source contract/attributes: Returns a DecisionPolicy from a map of parent/decision tuple values to DecisionSamples. Attributes Inherited from: PolicyMaker

Invocation template:

```scala
receiver.makePolicy(policyMap)
```

## `` com.cra.figaro.library.decision.ParentValueNotFoundException.addSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/ParentValueNotFoundException.html#addSuppressed-4b8)

```scala
final def addSuppressed(x$0: Throwable): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.addSuppressed(x$0)
```

## `` com.cra.figaro.library.decision.ParentValueNotFoundException.fillInStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/ParentValueNotFoundException.html#fillInStackTrace-26c)

```scala
def fillInStackTrace(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.fillInStackTrace()
```

## `` com.cra.figaro.library.decision.ParentValueNotFoundException.getCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/ParentValueNotFoundException.html#getCause-26c)

```scala
def getCause(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getCause()
```

## `` com.cra.figaro.library.decision.ParentValueNotFoundException.getLocalizedMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/ParentValueNotFoundException.html#getLocalizedMessage-665)

```scala
def getLocalizedMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getLocalizedMessage()
```

## `` com.cra.figaro.library.decision.ParentValueNotFoundException.getMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/ParentValueNotFoundException.html#getMessage-665)

```scala
def getMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getMessage()
```

## `` com.cra.figaro.library.decision.ParentValueNotFoundException.getStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/ParentValueNotFoundException.html#getStackTrace-fffffa6d)

```scala
def getStackTrace(): Array[StackTraceElement]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[StackTraceElement] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getStackTrace()
```

## `` com.cra.figaro.library.decision.ParentValueNotFoundException.getSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/ParentValueNotFoundException.html#getSuppressed-fffff2ce)

```scala
final def getSuppressed(): Array[Throwable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[Throwable] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getSuppressed()
```

## `` com.cra.figaro.library.decision.ParentValueNotFoundException.initCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/ParentValueNotFoundException.html#initCause-fffff080)

```scala
def initCause(x$0: Throwable): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.initCause(x$0)
```

## `` com.cra.figaro.library.decision.ParentValueNotFoundException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/ParentValueNotFoundException.html#printStackTrace-fffff230)

```scala
def printStackTrace(x$0: PrintWriter): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintWriter ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.library.decision.ParentValueNotFoundException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/ParentValueNotFoundException.html#printStackTrace-fffff0fd)

```scala
def printStackTrace(x$0: PrintStream): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintStream ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.library.decision.ParentValueNotFoundException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/ParentValueNotFoundException.html#printStackTrace-94c)

```scala
def printStackTrace(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace()
```

## `` com.cra.figaro.library.decision.ParentValueNotFoundException.setStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/ParentValueNotFoundException.html#setStackTrace-f79)

```scala
def setStackTrace(x$0: Array[StackTraceElement]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Array[StackTraceElement] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.setStackTrace(x$0)
```

## `` com.cra.figaro.library.decision.ParentValueNotFoundException.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/ParentValueNotFoundException.html#toString-665)

```scala
def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Inherited from: Throwable

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.library.decision.PolicyMaker.makePolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/PolicyMaker.html#makePolicy-61e)

```scala
def makePolicy(policyMap: Map[(T, U), DecisionSample]): DecisionPolicy[T, U]
```

Returns a DecisionPolicy from a map of parent/decision tuple values to DecisionSamples.

Type parameters: none.

Parameters, list 1: `` policyMap: Map[(T, U), DecisionSample] ``.

Returns: `` DecisionPolicy[T, U] ``.

Source contract/attributes: Returns a DecisionPolicy from a map of parent/decision tuple values to DecisionSamples. Attributes

Invocation template:

```scala
receiver.makePolicy(policyMap)
```

## `` com.cra.figaro.library.decision.PolicyMaker.makePolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/decision/PolicyMaker.html#makePolicy-121)

```scala
def makePolicy(alg: DecisionAlgorithm[T, U]): DecisionPolicy[T, U]
```

Returns a DecisionPolicy from the given decision algorithm .

Type parameters: none.

Parameters, list 1: `` alg: DecisionAlgorithm[T, U] ``.

Returns: `` DecisionPolicy[T, U] ``.

Source contract/attributes: Returns a DecisionPolicy from the given decision algorithm . Attributes

Invocation template:

```scala
receiver.makePolicy(alg)
```
