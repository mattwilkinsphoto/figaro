<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.patterns.learning

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.patterns.learning.ModelParameters.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters$.html#apply-fffffe83)

```scala
def apply(): ModelParameters
```

Create a new set of model parameters.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` ModelParameters ``.

Source contract/attributes: Create a new set of model parameters. Attributes

Invocation template:

```scala
com.cra.figaro.patterns.learning.ModelParameters.apply()
```

## `` com.cra.figaro.patterns.learning.ModelParameters.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters$.html#apply-fffffed5)

```scala
def apply(l: List[Parameter[_]]): ModelParameters
```

Create a new set of model parameters containing the list of parameters provided

Type parameters: none.

Parameters, list 1: `` l: List[Parameter[_]] ``.

Returns: `` ModelParameters ``.

Source contract/attributes: Create a new set of model parameters containing the list of parameters provided Attributes

Invocation template:

```scala
com.cra.figaro.patterns.learning.ModelParameters.apply(l)
```

## `` com.cra.figaro.patterns.learning.ModelParameters.ModelParametersDecodeJson ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters$.html#ModelParametersDecodeJson-fffff54a)

```scala
implicit def ModelParametersDecodeJson(implicit collection: ElementCollection): DecodeJson[ModelParameters]
```

Decode JSON into a set of model parameters

Type parameters: none.

Context parameters, list 1: `` collection: ElementCollection ``.

Returns: `` DecodeJson[ModelParameters] ``.

Source contract/attributes: Decode JSON into a set of model parameters Attributes

Invocation template:

```scala
com.cra.figaro.patterns.learning.ModelParameters.ModelParametersDecodeJson(using collection)
```

## `` com.cra.figaro.patterns.learning.ModelParameters.ModelParametersEncodeJson ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters$.html#ModelParametersEncodeJson-0)

```scala
implicit def ModelParametersEncodeJson: EncodeJson[ModelParameters]
```

Encode a set of model parameters into JSON

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` EncodeJson[ModelParameters] ``.

Source contract/attributes: Encode a set of model parameters into JSON Attributes

Invocation template:

```scala
com.cra.figaro.patterns.learning.ModelParameters.ModelParametersEncodeJson
```

## `` com.cra.figaro.patterns.learning.ModelParameters.add ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters.html#add-9cc)

```scala
override def add[T](element: Element[T]): Unit
```

Add a parameter to the collection

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` Unit ``.

Source contract/attributes: Add a parameter to the collection Attributes Definition Classes ElementCollection

Invocation template:

```scala
receiver.add[T](element)
```

## `` com.cra.figaro.patterns.learning.ModelParameters.convertToParameterList ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters.html#convertToParameterList-0)

```scala
def convertToParameterList: List[Parameter[_]]
```

Convert the contents of to a list of parameter elements

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Parameter[_]] ``.

Source contract/attributes: Convert the contents of to a list of parameter elements Attributes

Invocation template:

```scala
receiver.convertToParameterList
```

## `` com.cra.figaro.patterns.learning.ModelParameters.posteriorParameters ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters.html#posteriorParameters-0)

```scala
def posteriorParameters: ParameterCollection
```

Get the collection of posterior parameters (after learning)

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` ParameterCollection ``.

Source contract/attributes: Get the collection of posterior parameters (after learning) Attributes

Invocation template:

```scala
receiver.posteriorParameters
```

## `` com.cra.figaro.patterns.learning.ModelParameters.priorParameters ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters.html#priorParameters-0)

```scala
def priorParameters: ParameterCollection
```

Get the collection of prior parameters

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` ParameterCollection ``.

Source contract/attributes: Get the collection of prior parameters Attributes

Invocation template:

```scala
receiver.priorParameters
```

## `` com.cra.figaro.patterns.learning.ModelParameters.allPossibleResolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters.html#allPossibleResolutions-fffff5bd)

```scala
def allPossibleResolutions[T](reference: Reference[T]): Set[(Element[T], Contingency)]
```

Returns all resolutions of the given reference. Considers all possible values of each of the elements on the path and all elements with each name on the path. Along with each possible resolution, it returns the contingency (values of elements along the path) required to make that resolution be the one.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``.

Returns: `` Set[(Element[T], Contingency)] ``.

Source contract/attributes: Returns all resolutions of the given reference. Considers all possible values of each of the elements on the path and all elements with each name on the path. Along with each possible resolution, it returns the contingency (values of elements along the path) required to make that resolution be the one. Attributes Inherited from: ElementCollection

Invocation template:

```scala
receiver.allPossibleResolutions[T](reference)
```

## `` com.cra.figaro.patterns.learning.ModelParameters.assertEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters.html#assertEvidence-fffff54d)

```scala
def assertEvidence[T](reference: Reference[T], evidence: Evidence[T], contingency: Contingency = ...): Unit
```

Assert the given evidence on the given reference. The third argument is an optional contingency. This method makes sure to assert the evidence on all possible resolutions of the reference.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` evidence: Evidence[T] ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Assert the given evidence on the given reference. The third argument is an optional contingency. This method makes sure to assert the evidence on all possible resolutions of the reference. Attributes Inherited from: ElementCollection

Invocation template:

```scala
receiver.assertEvidence[T](reference, evidence, contingency)
```

## `` com.cra.figaro.patterns.learning.ModelParameters.assertEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters.html#assertEvidence-fffff9b7)

```scala
def assertEvidence(evidencePairs: Seq[NamedEvidence[_]]): Unit
```

Assert the given evidence associated with references to elements in the collection.

Type parameters: none.

Parameters, list 1: `` evidencePairs: Seq[NamedEvidence[_]] ``.

Returns: `` Unit ``.

Source contract/attributes: Assert the given evidence associated with references to elements in the collection. Attributes Inherited from: ElementCollection

Invocation template:

```scala
receiver.assertEvidence(evidencePairs)
```

## `` com.cra.figaro.patterns.learning.ModelParameters.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters.html#get-fffff866)

```scala
def get[T](reference: Reference[T]): SingleValuedReferenceElement[T]
```

Returns a reference element representing the single-valued reference. This method produces a new reference element every time it is called.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``.

Returns: `` SingleValuedReferenceElement[T] ``.

Source contract/attributes: Returns a reference element representing the single-valued reference. This method produces a new reference element every time it is called. Attributes Inherited from: ElementCollection

Invocation template:

```scala
receiver.get[T](reference)
```

## `` com.cra.figaro.patterns.learning.ModelParameters.getAggregate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters.html#getAggregate-fffff0d3)

```scala
def getAggregate[T, U](aggregate: MultiSet[T] => U)(reference: Reference[T]): Aggregate[T, U]
```

Returns a reference element representing the aggregate applied to the multi-valued reference.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` aggregate: MultiSet[T] => U ``.

Parameters, list 2: `` reference: Reference[T] ``.

Returns: `` Aggregate[T, U] ``.

Source contract/attributes: Returns a reference element representing the aggregate applied to the multi-valued reference. Attributes Inherited from: ElementCollection

Invocation template:

```scala
receiver.getAggregate[T, U](aggregate)(reference)
```

## `` com.cra.figaro.patterns.learning.ModelParameters.getElementByReference ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters.html#getElementByReference-d22)

```scala
def getElementByReference[T](reference: Reference[T]): Element[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``.

Returns: `` Element[T] ``.

Source contract/attributes: Attributes Inherited from: ElementCollection

Invocation template:

```scala
receiver.getElementByReference[T](reference)
```

## `` com.cra.figaro.patterns.learning.ModelParameters.getFirst ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters.html#getFirst-81)

```scala
def getFirst[T](reference: Reference[T]): (Element[_], Option[Reference[T]])
```

Gets the first element in the chain contained by the reference, together with an optional remaining reference. If the reference is simply a name, the element is the referred to element and the remainder is None.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``.

Returns: `` (Element[_], Option[Reference[T]]) ``.

Source contract/attributes: Gets the first element in the chain contained by the reference, together with an optional remaining reference. If the reference is simply a name, the element is the referred to element and the remainder is None. Attributes Inherited from: ElementCollection

Invocation template:

```scala
receiver.getFirst[T](reference)
```

## `` com.cra.figaro.patterns.learning.ModelParameters.hasRef ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters.html#hasRef-fffffff1)

```scala
def hasRef[T](reference: Reference[T]): Boolean
```

Returns true if the reference is resolvable on this collection. This will always use the most recent element with the reference name.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``.

Returns: `` Boolean ``.

Source contract/attributes: Returns true if the reference is resolvable on this collection. This will always use the most recent element with the reference name. Attributes Inherited from: ElementCollection

Invocation template:

```scala
receiver.hasRef[T](reference)
```

## `` com.cra.figaro.patterns.learning.ModelParameters.namedElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters.html#namedElements-0)

```scala
def namedElements: List[Element[_]]
```

All named elements in this collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: All named elements in this collection. Attributes Inherited from: ElementCollection

Invocation template:

```scala
receiver.namedElements
```

## `` com.cra.figaro.patterns.learning.ModelParameters.remove ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters.html#remove-9cc)

```scala
def remove[T](element: Element[T]): Unit
```

Remove an element from the element collection.

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` Unit ``.

Source contract/attributes: Remove an element from the element collection. Attributes Inherited from: ElementCollection

Invocation template:

```scala
receiver.remove[T](element)
```

## `` com.cra.figaro.patterns.learning.ModelParameters.removeEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters.html#removeEvidence-fffffc2f)

```scala
def removeEvidence[T](reference: Reference[T], contingency: Contingency = ...): Unit
```

Remove any evidence on the given reference. The second argument is an optional contingency. This method makes sure to remove evidence from all possible resolutions of the reference. Note: this method removes all conditions and constraints, no matter when they were added.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove any evidence on the given reference. The second argument is an optional contingency. This method makes sure to remove evidence from all possible resolutions of the reference. Note: this method removes all conditions and constraints, no matter when they were added. Attributes Inherited from: ElementCollection

Invocation template:

```scala
receiver.removeEvidence[T](reference, contingency)
```

## `` com.cra.figaro.patterns.learning.ModelParameters.allElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ModelParameters.html#allElements-0)

```scala
def allElements: List[Element[_]]
```

All named elements in this collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: All named elements in this collection. Attributes Deprecated [Since version 2.3.0.0] Use namedElements instead Inherited from: ElementCollection

Invocation template:

```scala
receiver.allElements
```

## `` com.cra.figaro.patterns.learning.ParameterArray.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ParameterArray.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Any

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.patterns.learning.ParameterArray.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ParameterArray.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.patterns.learning.ParameterArray.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ParameterArray.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.patterns.learning.ParameterCollection.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ParameterCollection.html#get-b8)

```scala
def get(s: String): ParameterType
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` s: String ``.

Returns: `` ParameterType ``.

Invocation template:

```scala
receiver.get(s)
```

## `` com.cra.figaro.patterns.learning.ParameterDouble.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ParameterDouble.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Any

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.patterns.learning.ParameterDouble.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ParameterDouble.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.patterns.learning.ParameterDouble.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ParameterDouble.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.patterns.learning.ParameterType.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ParameterType$.html#apply-fffff3c1)

```scala
def apply(d: Double): PrimitiveDouble
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` d: Double ``.

Returns: `` PrimitiveDouble ``.

Invocation template:

```scala
com.cra.figaro.patterns.learning.ParameterType.apply(d)
```

## `` com.cra.figaro.patterns.learning.ParameterType.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ParameterType$.html#apply-fffff0d1)

```scala
def apply(p: Parameter[_]): ParameterType
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` p: Parameter[_] ``.

Returns: `` ParameterType ``.

Invocation template:

```scala
com.cra.figaro.patterns.learning.ParameterType.apply(p)
```

## `` com.cra.figaro.patterns.learning.ParameterType.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/ParameterType$.html#apply-fffff307)

```scala
def apply(a: Array[Double]): PrimitiveArray
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` a: Array[Double] ``.

Returns: `` PrimitiveArray ``.

Invocation template:

```scala
com.cra.figaro.patterns.learning.ParameterType.apply(a)
```

## `` com.cra.figaro.patterns.learning.PrimitiveArray.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/PrimitiveArray.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Any

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.patterns.learning.PrimitiveArray.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/PrimitiveArray.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.patterns.learning.PrimitiveArray.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/PrimitiveArray.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.patterns.learning.PrimitiveDouble.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/PrimitiveDouble.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Any

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.patterns.learning.PrimitiveDouble.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/PrimitiveDouble.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.patterns.learning.PrimitiveDouble.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/patterns/learning/PrimitiveDouble.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```
