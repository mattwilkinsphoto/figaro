<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.algorithm.factored.gibbs

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.algorithm.factored.gibbs.BlockSampler.continuous ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/BlockSampler$.html#continuous-5d8)

```scala
def continuous(_variance: Double): BlockInfo => Cached & GaussianWeight & FactorProduct
```

Like BlockSampler.default, but also uses Gaussian weighting scheme when there exist Element[Double]s that are results of Chains

Type parameters: none.

Parameters, list 1: `` _variance: Double ``.

Returns: `` BlockInfo => Cached & GaussianWeight & FactorProduct ``.

Source contract/attributes: Like BlockSampler.default, but also uses Gaussian weighting scheme when there exist Element[Double]s that are results of Chains Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.gibbs.BlockSampler.continuous(_variance)
```

## `` com.cra.figaro.algorithm.factored.gibbs.BlockSampler.default ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/BlockSampler$.html#default-0)

```scala
def default: BlockInfo => Cached & FactorProduct
```

Caches 1000 factors and uses a VE-like procedure to compute joint factors

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` BlockInfo => Cached & FactorProduct ``.

Source contract/attributes: Caches 1000 factors and uses a VE-like procedure to compute joint factors Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.gibbs.BlockSampler.default
```

## `` com.cra.figaro.algorithm.factored.gibbs.BlockSampler.joint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/BlockSampler$.html#joint-0)

```scala
def joint: BlockInfo => Cached
```

Computes the joint distribution over the block by iterating through every combination of assignments of variables Takes time exponential in the size of the block, and is only recommended for blocks with no determinism

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` BlockInfo => Cached ``.

Source contract/attributes: Computes the joint distribution over the block by iterating through every combination of assignments of variables Takes time exponential in the size of the block, and is only recommended for blocks with no determinism Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.gibbs.BlockSampler.joint
```

## `` com.cra.figaro.algorithm.factored.gibbs.BlockSampler.getSamplingFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/BlockSampler.html#getSamplingFactor-fffff87b)

```scala
def getSamplingFactor(currentSamples: Map[Variable[_], Int]): Factor[Double]
```

Get the factor from which to sample this block Returns a non-logarithmic factor

Type parameters: none.

Parameters, list 1: `` currentSamples: Map[Variable[_], Int] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Get the factor from which to sample this block Returns a non-logarithmic factor Attributes

Invocation template:

```scala
receiver.getSamplingFactor(currentSamples)
```

## `` com.cra.figaro.algorithm.factored.gibbs.BlockSampler.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/BlockSampler.html#sample-ffffff18)

```scala
def sample(currentSamples: Map[Variable[_], Int]): Unit
```

Sample this block once

Type parameters: none.

Parameters, list 1: `` currentSamples: Map[Variable[_], Int] ``.

Returns: `` Unit ``.

Source contract/attributes: Sample this block once Attributes

Invocation template:

```scala
receiver.sample(currentSamples)
```

## `` com.cra.figaro.algorithm.factored.gibbs.BlockSampler.sampleFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/BlockSampler.html#sampleFactor-ac3)

```scala
def sampleFactor(factor: Factor[Double]): List[Int]
```

Select a set of indices in the factor according to the weights in the factor Works on a non-logarithmic factor

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` List[Int] ``.

Source contract/attributes: Select a set of indices in the factor according to the weights in the factor Works on a non-logarithmic factor Attributes

Invocation template:

```scala
receiver.sampleFactor(factor)
```

## `` com.cra.figaro.algorithm.factored.gibbs.Cached.getSamplingFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Cached.html#getSamplingFactor-fffff87b)

```scala
override def getSamplingFactor(currentSamples: Map[Variable[_], Int]): Factor[Double]
```

Get the factor from which to sample this block Returns a non-logarithmic factor

Type parameters: none.

Parameters, list 1: `` currentSamples: Map[Variable[_], Int] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Get the factor from which to sample this block Returns a non-logarithmic factor Attributes Definition Classes SimpleBlockSampler -> BlockSampler

Invocation template:

```scala
receiver.getSamplingFactor(currentSamples)
```

## `` com.cra.figaro.algorithm.factored.gibbs.Cached.computeSamplingFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Cached.html#computeSamplingFactor-fffff87b)

```scala
def computeSamplingFactor(currentSamples: Map[Variable[_], Int]): Factor[Double]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` currentSamples: Map[Variable[_], Int] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Attributes Inherited from: SimpleBlockSampler

Invocation template:

```scala
receiver.computeSamplingFactor(currentSamples)
```

## `` com.cra.figaro.algorithm.factored.gibbs.Cached.normalizeFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Cached.html#normalizeFactor-fffffd5e)

```scala
def normalizeFactor(factor: Factor[Double]): Factor[Double]
```

Normalize a factor so its weights sum to 1 Takes a logarithmic factor and returns a non-logarithmic factor

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Normalize a factor so its weights sum to 1 Takes a logarithmic factor and returns a non-logarithmic factor Attributes Inherited from: SimpleBlockSampler

Invocation template:

```scala
receiver.normalizeFactor(factor)
```

## `` com.cra.figaro.algorithm.factored.gibbs.Cached.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Cached.html#sample-ffffff18)

```scala
def sample(currentSamples: Map[Variable[_], Int]): Unit
```

Sample this block once

Type parameters: none.

Parameters, list 1: `` currentSamples: Map[Variable[_], Int] ``.

Returns: `` Unit ``.

Source contract/attributes: Sample this block once Attributes Inherited from: BlockSampler

Invocation template:

```scala
receiver.sample(currentSamples)
```

## `` com.cra.figaro.algorithm.factored.gibbs.Cached.sampleFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Cached.html#sampleFactor-ac3)

```scala
def sampleFactor(factor: Factor[Double]): List[Int]
```

Select a set of indices in the factor according to the weights in the factor Works on a non-logarithmic factor

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` List[Int] ``.

Source contract/attributes: Select a set of indices in the factor according to the weights in the factor Works on a non-logarithmic factor Attributes Inherited from: BlockSampler

Invocation template:

```scala
receiver.sampleFactor(factor)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ChainApplyBlockingGibbs.createBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ChainApplyBlockingGibbs.html#createBlocks-fffffc52)

```scala
def createBlocks(): List[Block]
```

Method to create a blocking scheme given information about the model and factors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` List[Block] ``.

Source contract/attributes: Method to create a blocking scheme given information about the model and factors. Attributes

Invocation template:

```scala
receiver.createBlocks()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ChainApplyBlockingGibbs.variableParentMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ChainApplyBlockingGibbs.html#variableParentMap-fffff328)

```scala
def variableParentMap(): Map[Variable[_], Set[Variable[_]]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[Variable[_], Set[Variable[_]]] ``.

Invocation template:

```scala
receiver.variableParentMap()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ChainApplyBlockingGibbs.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ChainApplyBlockingGibbs.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ChainApplyBlockingGibbs.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ChainApplyBlockingGibbs.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ChainApplyBlockingGibbs.getSampleCount ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ChainApplyBlockingGibbs.html#getSampleCount-0)

```scala
def getSampleCount: Int
```

Number of samples taken

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples taken Attributes Inherited from: BaseUnweightedSampler

Invocation template:

```scala
receiver.getSampleCount
```

## `` com.cra.figaro.algorithm.factored.gibbs.ChainApplyBlockingGibbs.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ChainApplyBlockingGibbs.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ChainApplyBlockingGibbs.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ChainApplyBlockingGibbs.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.factored.gibbs.ChainApplyBlockingGibbs.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ChainApplyBlockingGibbs.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ChainApplyBlockingGibbs.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ChainApplyBlockingGibbs.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ChainApplyBlockingGibbs.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ChainApplyBlockingGibbs.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ChainApplyBlockingGibbs.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ChainApplyBlockingGibbs.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ChainApplyBlockingGibbs.burnIn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ChainApplyBlockingGibbs.html#burnIn-0)

```scala
def burnIn: Int
```

Number of samples to throw away initially.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples to throw away initially. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.burnIn
```

## `` com.cra.figaro.algorithm.factored.gibbs.ChainApplyBlockingGibbs.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ChainApplyBlockingGibbs.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ChainApplyBlockingGibbs.interval ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ChainApplyBlockingGibbs.html#interval-0)

```scala
def interval: Int
```

Iterations thrown away between samples.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Iterations thrown away between samples. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.interval
```

## `` com.cra.figaro.algorithm.factored.gibbs.ChainApplyBlockingGibbs.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ChainApplyBlockingGibbs.html#sample-ffffff32)

```scala
def sample(): (Boolean, Sample)
```

Produce a single sample.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (Boolean, Sample) ``.

Source contract/attributes: Produce a single sample. Attributes Inherited from: BaseUnweightedSampler

Invocation template:

```scala
receiver.sample()
```

## `` com.cra.figaro.algorithm.factored.gibbs.DoubleWeight.logWeight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/DoubleWeight.html#logWeight-44b)

```scala
def logWeight(chainValue: Double, resultValue: Double): Double
```

Function with which to assign weights in place of -Infinity. It is assumed that if chainValue == resultValue, the result is 0.0. Observe that setting this function to:

Type parameters: none.

Parameters, list 1: `` chainValue: Double ``; `` resultValue: Double ``.

Returns: `` Double ``.

Source contract/attributes: Function with which to assign weights in place of -Infinity. It is assumed that if chainValue == resultValue, the result is 0.0. Observe that setting this function to: if(chainValue == resultValue) 0.0 else Double.NegativeInfinity has the same effect as not using the DoubleWeight trait at all. Attributes

Invocation template:

```scala
receiver.logWeight(chainValue, resultValue)
```

## `` com.cra.figaro.algorithm.factored.gibbs.DoubleWeight.computeSamplingFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/DoubleWeight.html#computeSamplingFactor-fffff87b)

```scala
def computeSamplingFactor(currentSamples: Map[Variable[_], Int]): Factor[Double]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` currentSamples: Map[Variable[_], Int] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Attributes Inherited from: SimpleBlockSampler

Invocation template:

```scala
receiver.computeSamplingFactor(currentSamples)
```

## `` com.cra.figaro.algorithm.factored.gibbs.DoubleWeight.getSamplingFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/DoubleWeight.html#getSamplingFactor-fffff87b)

```scala
def getSamplingFactor(currentSamples: Map[Variable[_], Int]): Factor[Double]
```

Get the factor from which to sample this block Returns a non-logarithmic factor

Type parameters: none.

Parameters, list 1: `` currentSamples: Map[Variable[_], Int] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Get the factor from which to sample this block Returns a non-logarithmic factor Attributes Inherited from: SimpleBlockSampler

Invocation template:

```scala
receiver.getSamplingFactor(currentSamples)
```

## `` com.cra.figaro.algorithm.factored.gibbs.DoubleWeight.normalizeFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/DoubleWeight.html#normalizeFactor-fffffd5e)

```scala
def normalizeFactor(factor: Factor[Double]): Factor[Double]
```

Normalize a factor so its weights sum to 1 Takes a logarithmic factor and returns a non-logarithmic factor

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Normalize a factor so its weights sum to 1 Takes a logarithmic factor and returns a non-logarithmic factor Attributes Inherited from: SimpleBlockSampler

Invocation template:

```scala
receiver.normalizeFactor(factor)
```

## `` com.cra.figaro.algorithm.factored.gibbs.DoubleWeight.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/DoubleWeight.html#sample-ffffff18)

```scala
def sample(currentSamples: Map[Variable[_], Int]): Unit
```

Sample this block once

Type parameters: none.

Parameters, list 1: `` currentSamples: Map[Variable[_], Int] ``.

Returns: `` Unit ``.

Source contract/attributes: Sample this block once Attributes Inherited from: BlockSampler

Invocation template:

```scala
receiver.sample(currentSamples)
```

## `` com.cra.figaro.algorithm.factored.gibbs.DoubleWeight.sampleFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/DoubleWeight.html#sampleFactor-ac3)

```scala
def sampleFactor(factor: Factor[Double]): List[Int]
```

Select a set of indices in the factor according to the weights in the factor Works on a non-logarithmic factor

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` List[Int] ``.

Source contract/attributes: Select a set of indices in the factor according to the weights in the factor Works on a non-logarithmic factor Attributes Inherited from: BlockSampler

Invocation template:

```scala
receiver.sampleFactor(factor)
```

## `` com.cra.figaro.algorithm.factored.gibbs.FactorProduct.computeSamplingFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/FactorProduct.html#computeSamplingFactor-fffff87b)

```scala
override def computeSamplingFactor(currentSamples: Map[Variable[_], Int]): Factor[Double]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` currentSamples: Map[Variable[_], Int] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Attributes Definition Classes SimpleBlockSampler

Invocation template:

```scala
receiver.computeSamplingFactor(currentSamples)
```

## `` com.cra.figaro.algorithm.factored.gibbs.FactorProduct.getSamplingFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/FactorProduct.html#getSamplingFactor-fffff87b)

```scala
def getSamplingFactor(currentSamples: Map[Variable[_], Int]): Factor[Double]
```

Get the factor from which to sample this block Returns a non-logarithmic factor

Type parameters: none.

Parameters, list 1: `` currentSamples: Map[Variable[_], Int] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Get the factor from which to sample this block Returns a non-logarithmic factor Attributes Inherited from: SimpleBlockSampler

Invocation template:

```scala
receiver.getSamplingFactor(currentSamples)
```

## `` com.cra.figaro.algorithm.factored.gibbs.FactorProduct.normalizeFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/FactorProduct.html#normalizeFactor-fffffd5e)

```scala
def normalizeFactor(factor: Factor[Double]): Factor[Double]
```

Normalize a factor so its weights sum to 1 Takes a logarithmic factor and returns a non-logarithmic factor

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Normalize a factor so its weights sum to 1 Takes a logarithmic factor and returns a non-logarithmic factor Attributes Inherited from: SimpleBlockSampler

Invocation template:

```scala
receiver.normalizeFactor(factor)
```

## `` com.cra.figaro.algorithm.factored.gibbs.FactorProduct.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/FactorProduct.html#sample-ffffff18)

```scala
def sample(currentSamples: Map[Variable[_], Int]): Unit
```

Sample this block once

Type parameters: none.

Parameters, list 1: `` currentSamples: Map[Variable[_], Int] ``.

Returns: `` Unit ``.

Source contract/attributes: Sample this block once Attributes Inherited from: BlockSampler

Invocation template:

```scala
receiver.sample(currentSamples)
```

## `` com.cra.figaro.algorithm.factored.gibbs.FactorProduct.sampleFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/FactorProduct.html#sampleFactor-ac3)

```scala
def sampleFactor(factor: Factor[Double]): List[Int]
```

Select a set of indices in the factor according to the weights in the factor Works on a non-logarithmic factor

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` List[Int] ``.

Source contract/attributes: Select a set of indices in the factor according to the weights in the factor Works on a non-logarithmic factor Attributes Inherited from: BlockSampler

Invocation template:

```scala
receiver.sampleFactor(factor)
```

## `` com.cra.figaro.algorithm.factored.gibbs.GaussianWeight.logWeight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/GaussianWeight.html#logWeight-44b)

```scala
def logWeight(resultValue: Double, chainValue: Double): Double
```

Function with which to assign weights in place of -Infinity. It is assumed that if chainValue == resultValue, the result is 0.0. Observe that setting this function to:

Type parameters: none.

Parameters, list 1: `` resultValue: Double ``; `` chainValue: Double ``.

Returns: `` Double ``.

Source contract/attributes: Function with which to assign weights in place of -Infinity. It is assumed that if chainValue == resultValue, the result is 0.0. Observe that setting this function to: if(chainValue == resultValue) 0.0 else Double.NegativeInfinity has the same effect as not using the DoubleWeight trait at all. Attributes

Invocation template:

```scala
receiver.logWeight(resultValue, chainValue)
```

## `` com.cra.figaro.algorithm.factored.gibbs.GaussianWeight.computeSamplingFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/GaussianWeight.html#computeSamplingFactor-fffff87b)

```scala
def computeSamplingFactor(currentSamples: Map[Variable[_], Int]): Factor[Double]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` currentSamples: Map[Variable[_], Int] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Attributes Inherited from: SimpleBlockSampler

Invocation template:

```scala
receiver.computeSamplingFactor(currentSamples)
```

## `` com.cra.figaro.algorithm.factored.gibbs.GaussianWeight.getSamplingFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/GaussianWeight.html#getSamplingFactor-fffff87b)

```scala
def getSamplingFactor(currentSamples: Map[Variable[_], Int]): Factor[Double]
```

Get the factor from which to sample this block Returns a non-logarithmic factor

Type parameters: none.

Parameters, list 1: `` currentSamples: Map[Variable[_], Int] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Get the factor from which to sample this block Returns a non-logarithmic factor Attributes Inherited from: SimpleBlockSampler

Invocation template:

```scala
receiver.getSamplingFactor(currentSamples)
```

## `` com.cra.figaro.algorithm.factored.gibbs.GaussianWeight.normalizeFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/GaussianWeight.html#normalizeFactor-fffffd5e)

```scala
def normalizeFactor(factor: Factor[Double]): Factor[Double]
```

Normalize a factor so its weights sum to 1 Takes a logarithmic factor and returns a non-logarithmic factor

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Normalize a factor so its weights sum to 1 Takes a logarithmic factor and returns a non-logarithmic factor Attributes Inherited from: SimpleBlockSampler

Invocation template:

```scala
receiver.normalizeFactor(factor)
```

## `` com.cra.figaro.algorithm.factored.gibbs.GaussianWeight.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/GaussianWeight.html#sample-ffffff18)

```scala
def sample(currentSamples: Map[Variable[_], Int]): Unit
```

Sample this block once

Type parameters: none.

Parameters, list 1: `` currentSamples: Map[Variable[_], Int] ``.

Returns: `` Unit ``.

Source contract/attributes: Sample this block once Attributes Inherited from: BlockSampler

Invocation template:

```scala
receiver.sample(currentSamples)
```

## `` com.cra.figaro.algorithm.factored.gibbs.GaussianWeight.sampleFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/GaussianWeight.html#sampleFactor-ac3)

```scala
def sampleFactor(factor: Factor[Double]): List[Int]
```

Select a set of indices in the factor according to the weights in the factor Works on a non-logarithmic factor

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` List[Int] ``.

Source contract/attributes: Select a set of indices in the factor according to the weights in the factor Works on a non-logarithmic factor Attributes Inherited from: BlockSampler

Invocation template:

```scala
receiver.sampleFactor(factor)
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs$.html#apply-533)

```scala
def apply(mySamples: Int, targets: Element[_]*)(implicit universe: Universe): ProbQueryGibbs & OneTimeProbQuerySampler & ChainApplyBlockingGibbs
```

Create a one-time Gibbs sampler using the given number of samples and target elements.

Type parameters: none.

Parameters, list 1: `` mySamples: Int ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryGibbs & OneTimeProbQuerySampler & ChainApplyBlockingGibbs ``.

Source contract/attributes: Create a one-time Gibbs sampler using the given number of samples and target elements. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.gibbs.Gibbs.apply(mySamples, targets*)(using universe)
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs$.html#apply-fffff0f6)

```scala
def apply(mySamples: Int, burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator, targets: Element[_]*)(implicit universe: Universe): ProbQueryGibbs & OneTimeProbQuerySampler & ChainApplyBlockingGibbs
```

Create a one-time Gibbs sampler using the given number of samples, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements.

Type parameters: none.

Parameters, list 1: `` mySamples: Int ``; `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryGibbs & OneTimeProbQuerySampler & ChainApplyBlockingGibbs ``.

Source contract/attributes: Create a one-time Gibbs sampler using the given number of samples, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.gibbs.Gibbs.apply(mySamples, burnIn, interval, blockToSampler, targets*)(using universe)
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs$.html#apply-af6)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double, mySamples: Int, burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator, targets: Element[_]*)(implicit universe: Universe): ProbQueryGibbs & OneTimeProbQuerySampler & ChainApplyBlockingGibbs
```

Create a one-time Gibbs sampler using the given dependent universes and algorithm, the number of samples, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``; `` mySamples: Int ``; `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryGibbs & OneTimeProbQuerySampler & ChainApplyBlockingGibbs ``.

Source contract/attributes: Create a one-time Gibbs sampler using the given dependent universes and algorithm, the number of samples, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.gibbs.Gibbs.apply(dependentUniverses, dependentAlgorithm, mySamples, burnIn, interval, blockToSampler, targets*)(using universe)
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs$.html#apply-38c)

```scala
def apply(targets: Element[_]*)(implicit universe: Universe): ProbQueryGibbs & AnytimeProbQuerySampler & ChainApplyBlockingGibbs
```

Create an anytime Gibbs sampler using the given target elements.

Type parameters: none.

Parameters, list 1: `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryGibbs & AnytimeProbQuerySampler & ChainApplyBlockingGibbs ``.

Source contract/attributes: Create an anytime Gibbs sampler using the given target elements. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.gibbs.Gibbs.apply(targets*)(using universe)
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs$.html#apply-fffff1bd)

```scala
def apply(burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator, targets: Element[_]*)(implicit universe: Universe): ProbQueryGibbs & AnytimeProbQuerySampler & ChainApplyBlockingGibbs
```

Create an anytime Gibbs sampler using the given number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements.

Type parameters: none.

Parameters, list 1: `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryGibbs & AnytimeProbQuerySampler & ChainApplyBlockingGibbs ``.

Source contract/attributes: Create an anytime Gibbs sampler using the given number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.gibbs.Gibbs.apply(burnIn, interval, blockToSampler, targets*)(using universe)
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs$.html#apply-fffff7bd)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double, burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator, targets: Element[_]*)(implicit universe: Universe): ProbQueryGibbs & AnytimeProbQuerySampler & ChainApplyBlockingGibbs
```

Create an anytime Gibbs sampler using the given dependent universes and algorithm, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``; `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryGibbs & AnytimeProbQuerySampler & ChainApplyBlockingGibbs ``.

Source contract/attributes: Create an anytime Gibbs sampler using the given dependent universes and algorithm, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.gibbs.Gibbs.apply(dependentUniverses, dependentAlgorithm, burnIn, interval, blockToSampler, targets*)(using universe)
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs$.html#probability-c2a)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Use Gibbs sampling to compute the probability that the given element satisfies the given predicate.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Use Gibbs sampling to compute the probability that the given element satisfies the given predicate. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.gibbs.Gibbs.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs$.html#probability-f2c)

```scala
def probability[T](target: Element[T], value: T): Double
```

Use Gibbs sampling to compute the probability that the given element has the given value.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Use Gibbs sampling to compute the probability that the given element has the given value. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.gibbs.Gibbs.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.burnIn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs.html#burnIn-0)

```scala
def burnIn: Int
```

Number of samples to throw away initially.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples to throw away initially. Attributes

Invocation template:

```scala
receiver.burnIn
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.createBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs.html#createBlocks-fffffc52)

```scala
def createBlocks(): List[Block]
```

Method to create a blocking scheme given information about the model and factors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` List[Block] ``.

Source contract/attributes: Method to create a blocking scheme given information about the model and factors. Attributes

Invocation template:

```scala
receiver.createBlocks()
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.interval ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs.html#interval-0)

```scala
def interval: Int
```

Iterations thrown away between samples.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Iterations thrown away between samples. Attributes

Invocation template:

```scala
receiver.interval
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.getSampleCount ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs.html#getSampleCount-0)

```scala
def getSampleCount: Int
```

Number of samples taken

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples taken Attributes Inherited from: BaseUnweightedSampler

Invocation template:

```scala
receiver.getSampleCount
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[T]]
```

All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[T]] ``.

Source contract/attributes: All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.algorithm.factored.gibbs.Gibbs.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/Gibbs.html#sample-ffffff32)

```scala
def sample(): (Boolean, Sample)
```

Produce a single sample.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (Boolean, Sample) ``.

Source contract/attributes: Produce a single sample. Attributes Inherited from: BaseUnweightedSampler

Invocation template:

```scala
receiver.sample()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.StarSampleException.addSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs$StarSampleException.html#addSuppressed-4b8)

```scala
final def addSuppressed(x$0: Throwable): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.addSuppressed(x$0)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.StarSampleException.fillInStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs$StarSampleException.html#fillInStackTrace-26c)

```scala
def fillInStackTrace(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.fillInStackTrace()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.StarSampleException.getCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs$StarSampleException.html#getCause-26c)

```scala
def getCause(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getCause()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.StarSampleException.getLocalizedMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs$StarSampleException.html#getLocalizedMessage-665)

```scala
def getLocalizedMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getLocalizedMessage()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.StarSampleException.getMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs$StarSampleException.html#getMessage-665)

```scala
def getMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getMessage()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.StarSampleException.getStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs$StarSampleException.html#getStackTrace-fffffa6d)

```scala
def getStackTrace(): Array[StackTraceElement]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[StackTraceElement] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getStackTrace()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.StarSampleException.getSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs$StarSampleException.html#getSuppressed-fffff2ce)

```scala
final def getSuppressed(): Array[Throwable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[Throwable] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getSuppressed()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.StarSampleException.initCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs$StarSampleException.html#initCause-fffff080)

```scala
def initCause(x$0: Throwable): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.initCause(x$0)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.StarSampleException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs$StarSampleException.html#printStackTrace-fffff230)

```scala
def printStackTrace(x$0: PrintWriter): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintWriter ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.StarSampleException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs$StarSampleException.html#printStackTrace-fffff0fd)

```scala
def printStackTrace(x$0: PrintStream): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintStream ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.StarSampleException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs$StarSampleException.html#printStackTrace-94c)

```scala
def printStackTrace(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.StarSampleException.setStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs$StarSampleException.html#setStackTrace-f79)

```scala
def setStackTrace(x$0: Array[StackTraceElement]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Array[StackTraceElement] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.setStackTrace(x$0)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.StarSampleException.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs$StarSampleException.html#toString-665)

```scala
def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Inherited from: Throwable

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes. Attributes

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs.html#sample-ffffff32)

```scala
def sample(): (Boolean, Sample)
```

Produce a single sample.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (Boolean, Sample) ``.

Source contract/attributes: Produce a single sample. Attributes

Invocation template:

```scala
receiver.sample()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.sampleAllBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs.html#sampleAllBlocks-94c)

```scala
def sampleAllBlocks(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Invocation template:

```scala
receiver.sampleAllBlocks()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.getSampleCount ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs.html#getSampleCount-0)

```scala
def getSampleCount: Int
```

Number of samples taken

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples taken Attributes Inherited from: BaseUnweightedSampler

Invocation template:

```scala
receiver.getSampleCount
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.burnIn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs.html#burnIn-0)

```scala
def burnIn: Int
```

Number of samples to throw away initially.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples to throw away initially. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.burnIn
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.createBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs.html#createBlocks-fffffc52)

```scala
def createBlocks(): List[Block]
```

Method to create a blocking scheme given information about the model and factors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` List[Block] ``.

Source contract/attributes: Method to create a blocking scheme given information about the model and factors. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.createBlocks()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbabilisticGibbs.interval ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbabilisticGibbs.html#interval-0)

```scala
def interval: Int
```

Iterations thrown away between samples.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Iterations thrown away between samples. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.interval
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.chainMapper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#chainMapper-ffffff84)

```scala
def chainMapper(chain: Chain[_, _]): Set[Variable[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` chain: Chain[_, _] ``.

Returns: `` Set[Variable[_]] ``.

Invocation template:

```scala
receiver.chainMapper(chain)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#initialize-94c)

```scala
override def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Definition Classes Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#computeDistribution-fffffef0)

```scala
def computeDistribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQuerySampler

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#computeExpectation-fffffa66)

```scala
def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQuerySampler

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#distribution-fffffef0)

```scala
def distribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes. Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.getSampleCount ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#getSampleCount-0)

```scala
def getSampleCount: Int
```

Number of samples taken

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples taken Attributes Inherited from: BaseUnweightedSampler

Invocation template:

```scala
receiver.getSampleCount
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.getTotalWeight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#getTotalWeight-0)

```scala
def getTotalWeight: Double
```

Total weight of samples taken, in log space

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Total weight of samples taken, in log space Attributes Inherited from: UnweightedSampler

Invocation template:

```scala
receiver.getTotalWeight
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#sample-ffffff32)

```scala
def sample(): (Boolean, Sample)
```

Produce a single sample.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (Boolean, Sample) ``.

Source contract/attributes: Produce a single sample. Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.sample()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.sampleAllBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#sampleAllBlocks-94c)

```scala
def sampleAllBlocks(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.sampleAllBlocks()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.sampleFromPosterior ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#sampleFromPosterior-fffffdb4)

```scala
def sampleFromPosterior[T](element: Element[T]): Stream[T]
```

Sample an value from the posterior of this element

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` Stream[T] ``.

Source contract/attributes: Sample an value from the posterior of this element Attributes Inherited from: UnweightedSampler

Invocation template:

```scala
receiver.sampleFromPosterior[T](element)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.factored.gibbs.ProbQueryGibbs.createBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/ProbQueryGibbs.html#createBlocks-fffffc52)

```scala
def createBlocks(): List[Block]
```

Method to create a blocking scheme given information about the model and factors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` List[Block] ``.

Source contract/attributes: Method to create a blocking scheme given information about the model and factors. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.createBlocks()
```

## `` com.cra.figaro.algorithm.factored.gibbs.SimpleBlockSampler.computeSamplingFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/SimpleBlockSampler.html#computeSamplingFactor-fffff87b)

```scala
def computeSamplingFactor(currentSamples: Map[Variable[_], Int]): Factor[Double]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` currentSamples: Map[Variable[_], Int] ``.

Returns: `` Factor[Double] ``.

Invocation template:

```scala
receiver.computeSamplingFactor(currentSamples)
```

## `` com.cra.figaro.algorithm.factored.gibbs.SimpleBlockSampler.getSamplingFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/SimpleBlockSampler.html#getSamplingFactor-fffff87b)

```scala
def getSamplingFactor(currentSamples: Map[Variable[_], Int]): Factor[Double]
```

Get the factor from which to sample this block Returns a non-logarithmic factor

Type parameters: none.

Parameters, list 1: `` currentSamples: Map[Variable[_], Int] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Get the factor from which to sample this block Returns a non-logarithmic factor Attributes

Invocation template:

```scala
receiver.getSamplingFactor(currentSamples)
```

## `` com.cra.figaro.algorithm.factored.gibbs.SimpleBlockSampler.normalizeFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/SimpleBlockSampler.html#normalizeFactor-fffffd5e)

```scala
def normalizeFactor(factor: Factor[Double]): Factor[Double]
```

Normalize a factor so its weights sum to 1 Takes a logarithmic factor and returns a non-logarithmic factor

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Normalize a factor so its weights sum to 1 Takes a logarithmic factor and returns a non-logarithmic factor Attributes

Invocation template:

```scala
receiver.normalizeFactor(factor)
```

## `` com.cra.figaro.algorithm.factored.gibbs.SimpleBlockSampler.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/SimpleBlockSampler.html#sample-ffffff18)

```scala
def sample(currentSamples: Map[Variable[_], Int]): Unit
```

Sample this block once

Type parameters: none.

Parameters, list 1: `` currentSamples: Map[Variable[_], Int] ``.

Returns: `` Unit ``.

Source contract/attributes: Sample this block once Attributes Inherited from: BlockSampler

Invocation template:

```scala
receiver.sample(currentSamples)
```

## `` com.cra.figaro.algorithm.factored.gibbs.SimpleBlockSampler.sampleFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/SimpleBlockSampler.html#sampleFactor-ac3)

```scala
def sampleFactor(factor: Factor[Double]): List[Int]
```

Select a set of indices in the factor according to the weights in the factor Works on a non-logarithmic factor

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` List[Int] ``.

Source contract/attributes: Select a set of indices in the factor according to the weights in the factor Works on a non-logarithmic factor Attributes Inherited from: BlockSampler

Invocation template:

```scala
receiver.sampleFactor(factor)
```

## `` com.cra.figaro.algorithm.factored.gibbs.StateNotFoundException.addSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/StateNotFoundException.html#addSuppressed-4b8)

```scala
final def addSuppressed(x$0: Throwable): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.addSuppressed(x$0)
```

## `` com.cra.figaro.algorithm.factored.gibbs.StateNotFoundException.fillInStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/StateNotFoundException.html#fillInStackTrace-26c)

```scala
def fillInStackTrace(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.fillInStackTrace()
```

## `` com.cra.figaro.algorithm.factored.gibbs.StateNotFoundException.getCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/StateNotFoundException.html#getCause-26c)

```scala
def getCause(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getCause()
```

## `` com.cra.figaro.algorithm.factored.gibbs.StateNotFoundException.getLocalizedMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/StateNotFoundException.html#getLocalizedMessage-665)

```scala
def getLocalizedMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getLocalizedMessage()
```

## `` com.cra.figaro.algorithm.factored.gibbs.StateNotFoundException.getMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/StateNotFoundException.html#getMessage-665)

```scala
def getMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getMessage()
```

## `` com.cra.figaro.algorithm.factored.gibbs.StateNotFoundException.getStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/StateNotFoundException.html#getStackTrace-fffffa6d)

```scala
def getStackTrace(): Array[StackTraceElement]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[StackTraceElement] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getStackTrace()
```

## `` com.cra.figaro.algorithm.factored.gibbs.StateNotFoundException.getSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/StateNotFoundException.html#getSuppressed-fffff2ce)

```scala
final def getSuppressed(): Array[Throwable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[Throwable] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getSuppressed()
```

## `` com.cra.figaro.algorithm.factored.gibbs.StateNotFoundException.initCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/StateNotFoundException.html#initCause-fffff080)

```scala
def initCause(x$0: Throwable): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.initCause(x$0)
```

## `` com.cra.figaro.algorithm.factored.gibbs.StateNotFoundException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/StateNotFoundException.html#printStackTrace-fffff230)

```scala
def printStackTrace(x$0: PrintWriter): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintWriter ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.algorithm.factored.gibbs.StateNotFoundException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/StateNotFoundException.html#printStackTrace-fffff0fd)

```scala
def printStackTrace(x$0: PrintStream): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintStream ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.algorithm.factored.gibbs.StateNotFoundException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/StateNotFoundException.html#printStackTrace-94c)

```scala
def printStackTrace(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace()
```

## `` com.cra.figaro.algorithm.factored.gibbs.StateNotFoundException.setStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/StateNotFoundException.html#setStackTrace-f79)

```scala
def setStackTrace(x$0: Array[StackTraceElement]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Array[StackTraceElement] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.setStackTrace(x$0)
```

## `` com.cra.figaro.algorithm.factored.gibbs.StateNotFoundException.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/StateNotFoundException.html#toString-665)

```scala
def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Inherited from: Throwable

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.algorithm.factored.gibbs.WalkSAT.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/gibbs/WalkSAT$.html#apply-cb6)

```scala
def apply[T](factors: List[Factor[T]], variables: Set[Variable[_]], semiring: Semiring[T], chainMapper: Chain[_, _] => Set[Variable[_]], prob: Double = ..., maxIterations: Int = ...): Map[Variable[_], Int]
```

Produce an assignment of values to variables constrained by the given factors.

Type parameters: `` T ``.

Parameters, list 1: `` factors: List[Factor[T]] ``; `` variables: Set[Variable[_]] ``; `` semiring: Semiring[T] ``; `` chainMapper: Chain[_, _] => Set[Variable[_]] ``; `` prob: Double = ... ``; `` maxIterations: Int = ... ``.

Returns: `` Map[Variable[_], Int] ``.

Source contract/attributes: Produce an assignment of values to variables constrained by the given factors. Value parameters factors The factors with which to constrain the variables. maxIterations Maximum number of iterations to run before throwing an exception for taking too long. prob The probability of reassigning a random variable in a factor instead of taking the greedy approach. semiring The semiring used in the factors. variables The set of variables over which to compute a sample. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.gibbs.WalkSAT.apply[T](factors, variables, semiring, chainMapper, prob, maxIterations)
```
