<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.algorithm.structured.algorithm

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructured.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructured.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default lazy ranging strategy with one value per iteration.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default lazy ranging strategy with one value per iteration. Attributes Definition Classes AnytimeStructured -> StructuredAlgorithm

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructured.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructured.html#checkConstraintBounds-94c)

```scala
override def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Definition Classes LazyStructured -> StructuredAlgorithm Inherited from: LazyStructured

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructured.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructured.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructured.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructured.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes StructuredAlgorithm -> Algorithm Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructured.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructured.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructured.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructured.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructured.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructured.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructured.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructured.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructured.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructured.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructured.shutdown ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructured.html#shutdown-0)

```scala
def shutdown: Unit
```

Release all resources from this anytime algorithm.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Unit ``.

Source contract/attributes: Release all resources from this anytime algorithm. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.shutdown
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructured.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructured.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructured.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructured.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructured.stopUpdate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructured.html#stopUpdate-94c)

```scala
def stopUpdate(): Unit
```

Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.stopUpdate()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructured.handle ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructured.html#handle-fffff940)

```scala
def handle(service: Service): Response
```

A handler of services provided by the algorithm.

Type parameters: none.

Parameters, list 1: `` service: Service ``.

Returns: `` Response ``.

Source contract/attributes: A handler of services provided by the algorithm. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.handle(service)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructured.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructured.html#problemTargets-0)

```scala
def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructured.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructured.html#processSolutions-fffffa74)

```scala
def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructured.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructured.html#refiningStrategy-fffffdd6)

```scala
def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructured.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructured.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.allProbabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#allProbabilityBounds-f66)

```scala
def allProbabilityBounds[T](target: Element[T]): LazyList[(Double, Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered. Value parameters target Element for which to compute bounds. Attributes Returns Bounds on the probability of each value for this element. Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.allProbabilityBounds[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#checkConstraintBounds-94c)

```scala
override def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Definition Classes LazyStructured -> StructuredAlgorithm Inherited from: LazyStructured

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.computeAllProbabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#computeAllProbabilityBounds-f66)

```scala
override def computeAllProbabilityBounds[T](target: Element[T]): LazyList[(Double, Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered. Attributes Definition Classes LazyStructuredProbQueryAlgorithm -> BoundsProbQueryAlgorithm Inherited from: LazyStructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeAllProbabilityBounds[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#computeDistribution-f66)

```scala
override def computeDistribution[T](target: Element[T]): LazyList[(Double, T)]
```

Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#computeExpectation-c2a)

```scala
override def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.computeExpectationBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#computeExpectationBounds-fffff300)

```scala
override def computeExpectationBounds[T](target: Element[T], function: T => Double, bounds: Option[(Double, Double)]): (Double, Double)
```

Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds, if provided. Otherwise, the lower and upper bounds of the function using the current known values of the target are used.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``; `` bounds: Option[(Double, Double)] ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds, if provided. Otherwise, the lower and upper bounds of the function using the current known values of the target are used. Attributes Definition Classes LazyStructuredProbQueryAlgorithm -> BoundsProbQueryAlgorithm Inherited from: LazyStructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectationBounds[T](target, function, bounds)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.computeProbabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#computeProbabilityBounds-1c3)

```scala
def computeProbabilityBounds[T](target: Element[T], predicate: T => Boolean): (Double, Double)
```

Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target. Attributes Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbabilityBounds[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#distribution-fffff1a2)

```scala
def distribution[T](target: Element[T]): LazyList[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#distribution-fffff084)

```scala
def distribution(target: List[Element[_]]): (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` target: List[Element[_]] ``.

Returns: `` (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])]) ``.

Source contract/attributes: Attributes Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.expectationBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#expectationBounds-1c3)

```scala
def expectationBounds[T](target: Element[T], function: T => Double): (Double, Double)
```

Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded according to the currently known values of the target. Thus, one should generally only use this when the range of the target is finite and known beforehand. Otherwise, one can use the overloaded version of this method that specifies explicit bounds on the function.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded according to the currently known values of the target. Thus, one should generally only use this when the range of the target is finite and known beforehand. Otherwise, one can use the overloaded version of this method that specifies explicit bounds on the function. Value parameters function Function whose expectation is computed. target Element for which to compute bounds. Attributes Returns Bounds on the expectation of this function for this element. Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.expectationBounds[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.expectationBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#expectationBounds-975)

```scala
def expectationBounds[T](target: Element[T], function: T => Double, lower: Double, upper: Double): (Double, Double)
```

Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``; `` lower: Double ``; `` upper: Double ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds. Value parameters function Function whose expectation is computed. lower Lower bound on the function. target Element for which to compute bounds. upper Upper bound on the function. Attributes Returns Bounds on the expectation of this function for this element. Throws AlgorithmInactiveException if the algorithm is inactive. IllegalArgumentException if the bounds given on the function are tighter than the actual bounds on the function, using the current known values of the target. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.expectationBounds[T](target, function, lower, upper)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.handle ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#handle-fffff940)

```scala
override def handle(service: Service): Response
```

A handler of services provided by the algorithm.

Type parameters: none.

Parameters, list 1: `` service: Service ``.

Returns: `` Response ``.

Source contract/attributes: A handler of services provided by the algorithm. Attributes Definition Classes AnytimeBoundsProbQuery -> AnytimeProbQuery -> Anytime Inherited from: AnytimeBoundsProbQuery

Invocation template:

```scala
receiver.handle(service)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes StructuredAlgorithm -> Algorithm Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.probabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#probabilityBounds-4c5)

```scala
def probabilityBounds[T](target: Element[T], value: T): (Double, Double)
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Attributes Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.probabilityBounds[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.probabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#probabilityBounds-1c3)

```scala
def probabilityBounds[T](target: Element[T], predicate: T => Boolean): (Double, Double)
```

Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target. Value parameters predicate Function whose probability of evaluating to true is computed. target Element for which to compute bounds. Attributes Returns Bounds on the probability of this function for this element. Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.probabilityBounds[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default lazy ranging strategy with one value per iteration.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default lazy ranging strategy with one value per iteration. Attributes Definition Classes AnytimeLazyStructured -> AnytimeStructured -> StructuredAlgorithm Inherited from: AnytimeLazyStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.shutdown ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#shutdown-0)

```scala
def shutdown: Unit
```

Release all resources from this anytime algorithm.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Unit ``.

Source contract/attributes: Release all resources from this anytime algorithm. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.shutdown
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.stopUpdate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#stopUpdate-94c)

```scala
def stopUpdate(): Unit
```

Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.stopUpdate()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#refiningStrategy-fffffdd6)

```scala
def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeLazyStructuredProbQuery.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeLazyStructuredProbQuery.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructured.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructured.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with one value per iteration.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with one value per iteration. Attributes Definition Classes StructuredAlgorithm

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructured.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructured.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructured.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructured.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructured.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructured.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes StructuredAlgorithm -> Algorithm Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructured.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructured.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructured.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructured.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructured.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructured.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructured.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructured.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructured.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructured.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructured.shutdown ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructured.html#shutdown-0)

```scala
def shutdown: Unit
```

Release all resources from this anytime algorithm.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Unit ``.

Source contract/attributes: Release all resources from this anytime algorithm. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.shutdown
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructured.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructured.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructured.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructured.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructured.stopUpdate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructured.html#stopUpdate-94c)

```scala
def stopUpdate(): Unit
```

Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.stopUpdate()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructured.handle ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructured.html#handle-fffff940)

```scala
def handle(service: Service): Response
```

A handler of services provided by the algorithm.

Type parameters: none.

Parameters, list 1: `` service: Service ``.

Returns: `` Response ``.

Source contract/attributes: A handler of services provided by the algorithm. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.handle(service)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructured.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructured.html#problemTargets-0)

```scala
def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructured.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructured.html#processSolutions-fffffa74)

```scala
def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructured.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructured.html#refiningStrategy-fffffdd6)

```scala
def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructured.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructured.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredMPE.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredMPE.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredMPE.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredMPE.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredMPE.handle ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredMPE.html#handle-fffff940)

```scala
def handle(service: Service): Response
```

A handler of services provided by the algorithm.

Type parameters: none.

Parameters, list 1: `` service: Service ``.

Returns: `` Response ``.

Source contract/attributes: A handler of services provided by the algorithm. Attributes Inherited from: AnytimeMPE

Invocation template:

```scala
receiver.handle(service)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredMPE.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredMPE.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes StructuredAlgorithm -> Algorithm Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredMPE.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredMPE.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredMPE.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredMPE.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredMPE.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredMPE.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star. Attributes Inherited from: StructuredMPEAlgorithm

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredMPE.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredMPE.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredMPE.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredMPE.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredMPEAlgorithm -> StructuredAlgorithm Inherited from: StructuredMPEAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredMPE.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredMPE.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredMPEAlgorithm -> StructuredAlgorithm Inherited from: StructuredMPEAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredMPE.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredMPE.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with one value per iteration.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with one value per iteration. Attributes Definition Classes AnytimeStructured -> StructuredAlgorithm Inherited from: AnytimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredMPE.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredMPE.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredMPE.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredMPE.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredMPE.shutdown ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredMPE.html#shutdown-0)

```scala
def shutdown: Unit
```

Release all resources from this anytime algorithm.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Unit ``.

Source contract/attributes: Release all resources from this anytime algorithm. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.shutdown
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredMPE.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredMPE.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredMPE.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredMPE.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredMPE.stopUpdate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredMPE.html#stopUpdate-94c)

```scala
def stopUpdate(): Unit
```

Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.stopUpdate()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredMPE.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredMPE.html#refiningStrategy-fffffdd6)

```scala
def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredMPE.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredMPE.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#computeDistribution-f66)

```scala
override def computeDistribution[T](target: Element[T]): LazyList[(Double, T)]
```

Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#computeExpectation-c2a)

```scala
override def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#distribution-fffff1a2)

```scala
def distribution[T](target: Element[T]): LazyList[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#distribution-fffff084)

```scala
def distribution(target: List[Element[_]]): (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` target: List[Element[_]] ``.

Returns: `` (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])]) ``.

Source contract/attributes: Attributes Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.handle ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#handle-fffff940)

```scala
def handle(service: Service): Response
```

A handler of services provided by the algorithm.

Type parameters: none.

Parameters, list 1: `` service: Service ``.

Returns: `` Response ``.

Source contract/attributes: A handler of services provided by the algorithm. Attributes Inherited from: AnytimeProbQuery

Invocation template:

```scala
receiver.handle(service)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes StructuredAlgorithm -> Algorithm Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with one value per iteration.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with one value per iteration. Attributes Definition Classes AnytimeStructured -> StructuredAlgorithm Inherited from: AnytimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.shutdown ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#shutdown-0)

```scala
def shutdown: Unit
```

Release all resources from this anytime algorithm.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Unit ``.

Source contract/attributes: Release all resources from this anytime algorithm. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.shutdown
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.stopUpdate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#stopUpdate-94c)

```scala
def stopUpdate(): Unit
```

Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.stopUpdate()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#refiningStrategy-fffffdd6)

```scala
def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.AnytimeStructuredProbQuery.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/AnytimeStructuredProbQuery.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionMPE.initialElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionMPE.html#initialElements-0)

```scala
override def initialElements: List[Element[_]]
```

Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe. Attributes Definition Classes DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialElements
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionMPE.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionMPE.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionMPE.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionMPE.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionMPE.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionMPE.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm -> Algorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionMPE.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionMPE.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionMPE.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionMPE.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionMPE.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionMPE.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star. Attributes Inherited from: StructuredMPEAlgorithm

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionMPE.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionMPE.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionMPE.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionMPE.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredMPEAlgorithm -> StructuredAlgorithm Inherited from: StructuredMPEAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionMPE.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionMPE.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredMPEAlgorithm -> StructuredAlgorithm Inherited from: StructuredMPEAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionMPE.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionMPE.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeStructured -> StructuredAlgorithm Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionMPE.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionMPE.html#refiningStrategy-fffffdd6)

```scala
override def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionMPE.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionMPE.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionMPE.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionMPE.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionMPE.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionMPE.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionMPE.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionMPE.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionMPE.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionMPE.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionMPE.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionMPE.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#computeDistribution-f66)

```scala
override def computeDistribution[T](target: Element[T]): LazyList[(Double, T)]
```

Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#computeExpectation-c2a)

```scala
override def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#distribution-fffff1a2)

```scala
def distribution[T](target: Element[T]): LazyList[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#distribution-fffff084)

```scala
def distribution(target: List[Element[_]]): (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` target: List[Element[_]] ``.

Returns: `` (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])]) ``.

Source contract/attributes: Attributes Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.initialElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#initialElements-0)

```scala
def initialElements: List[Element[_]]
```

Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe. Attributes Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialElements
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm -> Algorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeStructured -> StructuredAlgorithm Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#refiningStrategy-fffffdd6)

```scala
override def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionProbQuery.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionProbQuery.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionStructuredAlgorithm.initialElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionStructuredAlgorithm.html#initialElements-0)

```scala
def initialElements: List[Element[_]]
```

Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe. Attributes

Invocation template:

```scala
receiver.initialElements
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionStructuredAlgorithm.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionStructuredAlgorithm.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes StructuredAlgorithm -> Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionStructuredAlgorithm.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionStructuredAlgorithm.html#refiningStrategy-fffffdd6)

```scala
override def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Definition Classes StructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionStructuredAlgorithm.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionStructuredAlgorithm.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionStructuredAlgorithm.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionStructuredAlgorithm.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionStructuredAlgorithm.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionStructuredAlgorithm.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionStructuredAlgorithm.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionStructuredAlgorithm.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionStructuredAlgorithm.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionStructuredAlgorithm.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionStructuredAlgorithm.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionStructuredAlgorithm.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeStructured -> StructuredAlgorithm Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionStructuredAlgorithm.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionStructuredAlgorithm.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionStructuredAlgorithm.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionStructuredAlgorithm.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionStructuredAlgorithm.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionStructuredAlgorithm.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionStructuredAlgorithm.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionStructuredAlgorithm.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionStructuredAlgorithm.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionStructuredAlgorithm.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionStructuredAlgorithm.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionStructuredAlgorithm.html#problemTargets-0)

```scala
def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionStructuredAlgorithm.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionStructuredAlgorithm.html#processSolutions-fffffa74)

```scala
def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.DecompositionStructuredAlgorithm.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/DecompositionStructuredAlgorithm.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructured.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructured.html#checkConstraintBounds-94c)

```scala
override def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Definition Classes StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructured.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructured.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructured.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructured.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes StructuredAlgorithm -> Algorithm Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructured.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructured.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructured.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructured.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructured.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructured.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructured.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructured.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructured.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructured.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructured.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructured.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructured.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructured.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructured.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructured.html#problemTargets-0)

```scala
def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructured.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructured.html#processSolutions-fffffa74)

```scala
def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructured.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructured.html#rangingStrategy-0)

```scala
def rangingStrategy: RangingStrategy
```

Strategy to use for ranging atomic components. This is only called once. Note that this is called during initialization, so subclasses that override this can declare this as a lazy val or a def but not a val.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Strategy to use for ranging atomic components. This is only called once. Note that this is called during initialization, so subclasses that override this can declare this as a lazy val or a def but not a val. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructured.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructured.html#refiningStrategy-fffffdd6)

```scala
def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructured.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructured.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.this ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#<init>-fffffa4e)

```scala
def this(universe: Universe, queryTargets: Element[_]*)
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` universe: Universe ``; `` queryTargets: Element[_]* ``.

Returns: `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm ``.

Invocation template:

```scala
new com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm(universe, queryTargets*)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.computeAllProbabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#computeAllProbabilityBounds-f66)

```scala
override def computeAllProbabilityBounds[T](target: Element[T]): LazyList[(Double, Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered. Attributes Definition Classes BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.computeAllProbabilityBounds[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.computeExpectationBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#computeExpectationBounds-fffff300)

```scala
override def computeExpectationBounds[T](target: Element[T], function: T => Double, bounds: Option[(Double, Double)]): (Double, Double)
```

Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds, if provided. Otherwise, the lower and upper bounds of the function using the current known values of the target are used.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``; `` bounds: Option[(Double, Double)] ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds, if provided. Otherwise, the lower and upper bounds of the function using the current known values of the target are used. Attributes Definition Classes BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectationBounds[T](target, function, bounds)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.allProbabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#allProbabilityBounds-f66)

```scala
def allProbabilityBounds[T](target: Element[T]): LazyList[(Double, Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered. Value parameters target Element for which to compute bounds. Attributes Returns Bounds on the probability of each value for this element. Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.allProbabilityBounds[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#checkConstraintBounds-94c)

```scala
override def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Definition Classes LazyStructured -> StructuredAlgorithm Inherited from: LazyStructured

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#computeDistribution-f66)

```scala
override def computeDistribution[T](target: Element[T]): LazyList[(Double, T)]
```

Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#computeExpectation-c2a)

```scala
override def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.computeProbabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#computeProbabilityBounds-1c3)

```scala
def computeProbabilityBounds[T](target: Element[T], predicate: T => Boolean): (Double, Double)
```

Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target. Attributes Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbabilityBounds[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#distribution-fffff1a2)

```scala
def distribution[T](target: Element[T]): LazyList[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#distribution-fffff084)

```scala
def distribution(target: List[Element[_]]): (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` target: List[Element[_]] ``.

Returns: `` (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])]) ``.

Source contract/attributes: Attributes Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.expectationBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#expectationBounds-1c3)

```scala
def expectationBounds[T](target: Element[T], function: T => Double): (Double, Double)
```

Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded according to the currently known values of the target. Thus, one should generally only use this when the range of the target is finite and known beforehand. Otherwise, one can use the overloaded version of this method that specifies explicit bounds on the function.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded according to the currently known values of the target. Thus, one should generally only use this when the range of the target is finite and known beforehand. Otherwise, one can use the overloaded version of this method that specifies explicit bounds on the function. Value parameters function Function whose expectation is computed. target Element for which to compute bounds. Attributes Returns Bounds on the expectation of this function for this element. Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.expectationBounds[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.expectationBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#expectationBounds-975)

```scala
def expectationBounds[T](target: Element[T], function: T => Double, lower: Double, upper: Double): (Double, Double)
```

Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``; `` lower: Double ``; `` upper: Double ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds. Value parameters function Function whose expectation is computed. lower Lower bound on the function. target Element for which to compute bounds. upper Upper bound on the function. Attributes Returns Bounds on the expectation of this function for this element. Throws AlgorithmInactiveException if the algorithm is inactive. IllegalArgumentException if the bounds given on the function are tighter than the actual bounds on the function, using the current known values of the target. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.expectationBounds[T](target, function, lower, upper)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes StructuredAlgorithm -> Algorithm Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.probabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#probabilityBounds-4c5)

```scala
def probabilityBounds[T](target: Element[T], value: T): (Double, Double)
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Attributes Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.probabilityBounds[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.probabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#probabilityBounds-1c3)

```scala
def probabilityBounds[T](target: Element[T], predicate: T => Boolean): (Double, Double)
```

Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target. Value parameters predicate Function whose probability of evaluating to true is computed. target Element for which to compute bounds. Attributes Returns Bounds on the probability of this function for this element. Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.probabilityBounds[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#rangingStrategy-0)

```scala
def rangingStrategy: RangingStrategy
```

Strategy to use for ranging atomic components. This is only called once. Note that this is called during initialization, so subclasses that override this can declare this as a lazy val or a def but not a val.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Strategy to use for ranging atomic components. This is only called once. Note that this is called during initialization, so subclasses that override this can declare this as a lazy val or a def but not a val. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#refiningStrategy-fffffdd6)

```scala
def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.LazyStructuredProbQueryAlgorithm.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/LazyStructuredProbQueryAlgorithm.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructured.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructured.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default lazy ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default lazy ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeStructured -> StructuredAlgorithm

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructured.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructured.html#checkConstraintBounds-94c)

```scala
override def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Definition Classes LazyStructured -> StructuredAlgorithm Inherited from: LazyStructured

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructured.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructured.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructured.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructured.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes StructuredAlgorithm -> Algorithm Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructured.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructured.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructured.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructured.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructured.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructured.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructured.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructured.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructured.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructured.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructured.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructured.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructured.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructured.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructured.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructured.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructured.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructured.html#problemTargets-0)

```scala
def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructured.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructured.html#processSolutions-fffffa74)

```scala
def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructured.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructured.html#refiningStrategy-fffffdd6)

```scala
def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructured.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructured.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.allProbabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#allProbabilityBounds-f66)

```scala
def allProbabilityBounds[T](target: Element[T]): LazyList[(Double, Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered. Value parameters target Element for which to compute bounds. Attributes Returns Bounds on the probability of each value for this element. Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.allProbabilityBounds[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#checkConstraintBounds-94c)

```scala
override def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Definition Classes LazyStructured -> StructuredAlgorithm Inherited from: LazyStructured

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.computeAllProbabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#computeAllProbabilityBounds-f66)

```scala
override def computeAllProbabilityBounds[T](target: Element[T]): LazyList[(Double, Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered. Attributes Definition Classes LazyStructuredProbQueryAlgorithm -> BoundsProbQueryAlgorithm Inherited from: LazyStructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeAllProbabilityBounds[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#computeDistribution-f66)

```scala
override def computeDistribution[T](target: Element[T]): LazyList[(Double, T)]
```

Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#computeExpectation-c2a)

```scala
override def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.computeExpectationBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#computeExpectationBounds-fffff300)

```scala
override def computeExpectationBounds[T](target: Element[T], function: T => Double, bounds: Option[(Double, Double)]): (Double, Double)
```

Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds, if provided. Otherwise, the lower and upper bounds of the function using the current known values of the target are used.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``; `` bounds: Option[(Double, Double)] ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds, if provided. Otherwise, the lower and upper bounds of the function using the current known values of the target are used. Attributes Definition Classes LazyStructuredProbQueryAlgorithm -> BoundsProbQueryAlgorithm Inherited from: LazyStructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectationBounds[T](target, function, bounds)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.computeProbabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#computeProbabilityBounds-1c3)

```scala
def computeProbabilityBounds[T](target: Element[T], predicate: T => Boolean): (Double, Double)
```

Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target. Attributes Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbabilityBounds[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#distribution-fffff1a2)

```scala
def distribution[T](target: Element[T]): LazyList[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#distribution-fffff084)

```scala
def distribution(target: List[Element[_]]): (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` target: List[Element[_]] ``.

Returns: `` (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])]) ``.

Source contract/attributes: Attributes Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.expectationBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#expectationBounds-1c3)

```scala
def expectationBounds[T](target: Element[T], function: T => Double): (Double, Double)
```

Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded according to the currently known values of the target. Thus, one should generally only use this when the range of the target is finite and known beforehand. Otherwise, one can use the overloaded version of this method that specifies explicit bounds on the function.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded according to the currently known values of the target. Thus, one should generally only use this when the range of the target is finite and known beforehand. Otherwise, one can use the overloaded version of this method that specifies explicit bounds on the function. Value parameters function Function whose expectation is computed. target Element for which to compute bounds. Attributes Returns Bounds on the expectation of this function for this element. Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.expectationBounds[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.expectationBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#expectationBounds-975)

```scala
def expectationBounds[T](target: Element[T], function: T => Double, lower: Double, upper: Double): (Double, Double)
```

Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``; `` lower: Double ``; `` upper: Double ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds. Value parameters function Function whose expectation is computed. lower Lower bound on the function. target Element for which to compute bounds. upper Upper bound on the function. Attributes Returns Bounds on the expectation of this function for this element. Throws AlgorithmInactiveException if the algorithm is inactive. IllegalArgumentException if the bounds given on the function are tighter than the actual bounds on the function, using the current known values of the target. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.expectationBounds[T](target, function, lower, upper)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes StructuredAlgorithm -> Algorithm Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.probabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#probabilityBounds-4c5)

```scala
def probabilityBounds[T](target: Element[T], value: T): (Double, Double)
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Attributes Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.probabilityBounds[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.probabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#probabilityBounds-1c3)

```scala
def probabilityBounds[T](target: Element[T], predicate: T => Boolean): (Double, Double)
```

Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target. Value parameters predicate Function whose probability of evaluating to true is computed. target Element for which to compute bounds. Attributes Returns Bounds on the probability of this function for this element. Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.probabilityBounds[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default lazy ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default lazy ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeLazyStructured -> OneTimeStructured -> StructuredAlgorithm Inherited from: OneTimeLazyStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#refiningStrategy-fffffdd6)

```scala
def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeLazyStructuredProbQuery.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeLazyStructuredProbQuery.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructured.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructured.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes StructuredAlgorithm

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructured.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructured.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTime

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructured.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructured.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructured.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructured.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructured.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructured.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes StructuredAlgorithm -> Algorithm Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructured.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructured.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructured.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructured.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructured.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructured.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructured.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructured.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructured.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructured.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructured.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructured.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructured.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructured.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructured.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructured.html#problemTargets-0)

```scala
def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructured.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructured.html#processSolutions-fffffa74)

```scala
def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructured.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructured.html#refiningStrategy-fffffdd6)

```scala
def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructured.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructured.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredMPE.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredMPE.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredMPE.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredMPE.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredMPE.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredMPE.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes StructuredAlgorithm -> Algorithm Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredMPE.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredMPE.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredMPE.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredMPE.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredMPE.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredMPE.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star. Attributes Inherited from: StructuredMPEAlgorithm

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredMPE.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredMPE.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredMPE.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredMPE.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredMPEAlgorithm -> StructuredAlgorithm Inherited from: StructuredMPEAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredMPE.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredMPE.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredMPEAlgorithm -> StructuredAlgorithm Inherited from: StructuredMPEAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredMPE.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredMPE.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeStructured -> StructuredAlgorithm Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredMPE.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredMPE.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredMPE.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredMPE.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredMPE.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredMPE.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredMPE.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredMPE.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredMPE.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredMPE.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredMPE.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredMPE.html#refiningStrategy-fffffdd6)

```scala
def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredMPE.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredMPE.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#computeDistribution-f66)

```scala
override def computeDistribution[T](target: Element[T]): LazyList[(Double, T)]
```

Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#computeExpectation-c2a)

```scala
override def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#distribution-fffff1a2)

```scala
def distribution[T](target: Element[T]): LazyList[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#distribution-fffff084)

```scala
def distribution(target: List[Element[_]]): (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` target: List[Element[_]] ``.

Returns: `` (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])]) ``.

Source contract/attributes: Attributes Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes StructuredAlgorithm -> Algorithm Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeStructured -> StructuredAlgorithm Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#refiningStrategy-fffffdd6)

```scala
def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.OneTimeStructuredProbQuery.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/OneTimeStructuredProbQuery.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredAlgorithm.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredAlgorithm.html#problemTargets-0)

```scala
def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem.

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredAlgorithm.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredAlgorithm.html#processSolutions-fffffa74)

```scala
def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredAlgorithm.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredAlgorithm.html#rangingStrategy-0)

```scala
def rangingStrategy: RangingStrategy
```

Strategy to use for ranging atomic components. This is only called once. Note that this is called during initialization, so subclasses that override this can declare this as a lazy val or a def but not a val.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Strategy to use for ranging atomic components. This is only called once. Note that this is called during initialization, so subclasses that override this can declare this as a lazy val or a def but not a val. Attributes

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredAlgorithm.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredAlgorithm.html#refiningStrategy-fffffdd6)

```scala
def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration.

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredAlgorithm.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredAlgorithm.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration.

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredAlgorithm.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredAlgorithm.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredAlgorithm.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredAlgorithm.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredAlgorithm.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredAlgorithm.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions.

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredAlgorithm.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredAlgorithm.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredAlgorithm.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredAlgorithm.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredAlgorithm.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredAlgorithm.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredAlgorithm.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredAlgorithm.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredAlgorithm.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredAlgorithm.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredAlgorithm.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredAlgorithm.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredAlgorithm.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredAlgorithm.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredMPEAlgorithm.this ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredMPEAlgorithm.html#<init>-fffff940)

```scala
def this(universe: Universe)
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` universe: Universe ``.

Returns: `` com.cra.figaro.algorithm.structured.algorithm.StructuredMPEAlgorithm ``.

Invocation template:

```scala
new com.cra.figaro.algorithm.structured.algorithm.StructuredMPEAlgorithm(universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredMPEAlgorithm.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredMPEAlgorithm.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star. Attributes

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredMPEAlgorithm.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredMPEAlgorithm.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredMPEAlgorithm.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredMPEAlgorithm.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredMPEAlgorithm.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredMPEAlgorithm.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredMPEAlgorithm.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredMPEAlgorithm.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredMPEAlgorithm.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredMPEAlgorithm.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes StructuredAlgorithm -> Algorithm Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredMPEAlgorithm.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredMPEAlgorithm.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredMPEAlgorithm.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredMPEAlgorithm.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredMPEAlgorithm.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredMPEAlgorithm.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredMPEAlgorithm.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredMPEAlgorithm.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredMPEAlgorithm.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredMPEAlgorithm.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredMPEAlgorithm.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredMPEAlgorithm.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredMPEAlgorithm.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredMPEAlgorithm.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredMPEAlgorithm.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredMPEAlgorithm.html#rangingStrategy-0)

```scala
def rangingStrategy: RangingStrategy
```

Strategy to use for ranging atomic components. This is only called once. Note that this is called during initialization, so subclasses that override this can declare this as a lazy val or a def but not a val.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Strategy to use for ranging atomic components. This is only called once. Note that this is called during initialization, so subclasses that override this can declare this as a lazy val or a def but not a val. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredMPEAlgorithm.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredMPEAlgorithm.html#refiningStrategy-fffffdd6)

```scala
def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredMPEAlgorithm.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredMPEAlgorithm.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.this ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#<init>-fffff73a)

```scala
def this(universe: Universe, queryTargets: Element[_]*)
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` universe: Universe ``; `` queryTargets: Element[_]* ``.

Returns: `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm ``.

Invocation template:

```scala
new com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm(universe, queryTargets*)
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#computeDistribution-f66)

```scala
override def computeDistribution[T](target: Element[T]): LazyList[(Double, T)]
```

Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#computeExpectation-c2a)

```scala
override def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#distribution-fffff084)

```scala
def distribution(target: List[Element[_]]): (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` target: List[Element[_]] ``.

Returns: `` (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])]) ``.

Invocation template:

```scala
receiver.distribution(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#distribution-fffff1a2)

```scala
def distribution[T](target: Element[T]): LazyList[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes StructuredAlgorithm -> Algorithm Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#rangingStrategy-0)

```scala
def rangingStrategy: RangingStrategy
```

Strategy to use for ranging atomic components. This is only called once. Note that this is called during initialization, so subclasses that override this can declare this as a lazy val or a def but not a val.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Strategy to use for ranging atomic components. This is only called once. Note that this is called during initialization, so subclasses that override this can declare this as a lazy val or a def but not a val. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#refiningStrategy-fffffdd6)

```scala
def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.StructuredProbQueryAlgorithm.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/StructuredProbQueryAlgorithm.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```
