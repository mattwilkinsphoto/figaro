<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.algorithm.structured

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.algorithm.structured.ApplyComponent.generateRange ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ApplyComponent.html#generateRange-94c)

```scala
override def generateRange(): Unit
```

Generate a range of values for this component. Also sets the variable for this component. If an argument is not in the component collection, we do not generate the argument, but instead assume its only value is *. This doesn't change the range of any other element or expand any subproblems. The range will include * based on argument ranges including * or any subproblem not being expanded.\

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Generate a range of values for this component. Also sets the variable for this component. If an argument is not in the component collection, we do not generate the argument, but instead assume its only value is *. This doesn't change the range of any other element or expand any subproblems. The range will include * based on argument ranges including * or any subproblem not being expanded.\ Attributes Definition Classes ProblemComponent

Invocation template:

```scala
receiver.generateRange()
```

## `` com.cra.figaro.algorithm.structured.ApplyComponent.getMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ApplyComponent.html#getMap-fffff4cc)

```scala
def getMap(): Map[Any, Value]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[Any, Value] ``.

Invocation template:

```scala
receiver.getMap()
```

## `` com.cra.figaro.algorithm.structured.ApplyComponent.setMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ApplyComponent.html#setMap-ffffff18)

```scala
def setMap(m: Map[Any, Value]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` m: Map[Any, Value] ``.

Returns: `` Unit ``.

Invocation template:

```scala
receiver.setMap(m)
```

## `` com.cra.figaro.algorithm.structured.ApplyComponent.constraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ApplyComponent.html#constraintFactors-fffff66a)

```scala
def constraintFactors(bounds: Bounds = ...): List[Factor[Double]]
```

Gets the constraint factors for this component. Returns the lower bound factors unless an Upper argument is provided.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Gets the constraint factors for this component. Returns the lower bound factors unless an Upper argument is provided. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.constraintFactors(bounds)
```

## `` com.cra.figaro.algorithm.structured.ApplyComponent.nonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ApplyComponent.html#nonConstraintFactors-8f2)

```scala
def nonConstraintFactors(parameterized: Boolean = ...): List[Factor[Double]]
```

Generate the non-constraint factors based on the current range. For most elements, this just generates the factors in the usual way. For a chain, this does not include subproblem factors. The parameterized flag indicates whether parameterized elements should have special factors created that use the MAP values of their arguments. This defaults to false.

Type parameters: none.

Parameters, list 1: `` parameterized: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Generate the non-constraint factors based on the current range. For most elements, this just generates the factors in the usual way. For a chain, this does not include subproblem factors. The parameterized flag indicates whether parameterized elements should have special factors created that use the MAP values of their arguments. This defaults to false. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.nonConstraintFactors(parameterized)
```

## `` com.cra.figaro.algorithm.structured.ApplyComponent.setVariable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ApplyComponent.html#setVariable-fffff328)

```scala
def setVariable(v: Variable[Value]): Unit
```

Set the variable associated with this component to the given variable.

Type parameters: none.

Parameters, list 1: `` v: Variable[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Set the variable associated with this component to the given variable. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.setVariable(v)
```

## `` com.cra.figaro.algorithm.structured.ApplyComponent.variable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ApplyComponent.html#variable-0)

```scala
def variable: Variable[Value]
```

The current variable representing this component in factors. This is set automatically when the range is updated.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Variable[Value] ``.

Source contract/attributes: The current variable representing this component in factors. This is set automatically when the range is updated. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.variable
```

## `` com.cra.figaro.algorithm.structured.AtomicComponent.generateRange ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/AtomicComponent.html#generateRange-94c)

```scala
override def generateRange(): Unit
```

Generate a range of values for this component. Also sets the variable for this component. If an argument is not in the component collection, we do not generate the argument, but instead assume its only value is *. This doesn't change the range of any other element or expand any subproblems. The range will include * based on argument ranges including * or any subproblem not being expanded.\

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Generate a range of values for this component. Also sets the variable for this component. If an argument is not in the component collection, we do not generate the argument, but instead assume its only value is *. This doesn't change the range of any other element or expand any subproblems. The range will include * based on argument ranges including * or any subproblem not being expanded.\ Attributes Definition Classes ProblemComponent

Invocation template:

```scala
receiver.generateRange()
```

## `` com.cra.figaro.algorithm.structured.AtomicComponent.constraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/AtomicComponent.html#constraintFactors-fffff66a)

```scala
def constraintFactors(bounds: Bounds = ...): List[Factor[Double]]
```

Gets the constraint factors for this component. Returns the lower bound factors unless an Upper argument is provided.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Gets the constraint factors for this component. Returns the lower bound factors unless an Upper argument is provided. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.constraintFactors(bounds)
```

## `` com.cra.figaro.algorithm.structured.AtomicComponent.nonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/AtomicComponent.html#nonConstraintFactors-8f2)

```scala
def nonConstraintFactors(parameterized: Boolean = ...): List[Factor[Double]]
```

Generate the non-constraint factors based on the current range. For most elements, this just generates the factors in the usual way. For a chain, this does not include subproblem factors. The parameterized flag indicates whether parameterized elements should have special factors created that use the MAP values of their arguments. This defaults to false.

Type parameters: none.

Parameters, list 1: `` parameterized: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Generate the non-constraint factors based on the current range. For most elements, this just generates the factors in the usual way. For a chain, this does not include subproblem factors. The parameterized flag indicates whether parameterized elements should have special factors created that use the MAP values of their arguments. This defaults to false. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.nonConstraintFactors(parameterized)
```

## `` com.cra.figaro.algorithm.structured.AtomicComponent.setVariable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/AtomicComponent.html#setVariable-fffff328)

```scala
def setVariable(v: Variable[Value]): Unit
```

Set the variable associated with this component to the given variable.

Type parameters: none.

Parameters, list 1: `` v: Variable[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Set the variable associated with this component to the given variable. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.setVariable(v)
```

## `` com.cra.figaro.algorithm.structured.AtomicComponent.variable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/AtomicComponent.html#variable-0)

```scala
def variable: Variable[Value]
```

The current variable representing this component in factors. This is set automatically when the range is updated.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Variable[Value] ``.

Source contract/attributes: The current variable representing this component in factors. This is set automatically when the range is updated. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.variable
```

## `` com.cra.figaro.algorithm.structured.ChainComponent.expand ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ChainComponent.html#expand-fffff71f)

```scala
def expand(parentValue: ParentValue): Unit
```

Create a subproblem for a particular parent value. Memoized.

Type parameters: none.

Parameters, list 1: `` parentValue: ParentValue ``.

Returns: `` Unit ``.

Source contract/attributes: Create a subproblem for a particular parent value. Memoized. Attributes

Invocation template:

```scala
receiver.expand(parentValue)
```

## `` com.cra.figaro.algorithm.structured.ChainComponent.constraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ChainComponent.html#constraintFactors-fffff66a)

```scala
def constraintFactors(bounds: Bounds = ...): List[Factor[Double]]
```

Gets the constraint factors for this component. Returns the lower bound factors unless an Upper argument is provided.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Gets the constraint factors for this component. Returns the lower bound factors unless an Upper argument is provided. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.constraintFactors(bounds)
```

## `` com.cra.figaro.algorithm.structured.ChainComponent.expand ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ChainComponent.html#expand-94c)

```scala
def expand(): Unit
```

Expand for all values of the parent that were not previously expanded, based on the current range of the parent.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Expand for all values of the parent that were not previously expanded, based on the current range of the parent. Attributes Inherited from: ExpandableComponent

Invocation template:

```scala
receiver.expand()
```

## `` com.cra.figaro.algorithm.structured.ChainComponent.generateRange ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ChainComponent.html#generateRange-94c)

```scala
def generateRange(): Unit
```

Generate a range of values for this component. Also sets the variable for this component. If an argument is not in the component collection, we do not generate the argument, but instead assume its only value is *. This doesn't change the range of any other element or expand any subproblems. The range will include * based on argument ranges including * or any subproblem not being expanded.\

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Generate a range of values for this component. Also sets the variable for this component. If an argument is not in the component collection, we do not generate the argument, but instead assume its only value is *. This doesn't change the range of any other element or expand any subproblems. The range will include * based on argument ranges including * or any subproblem not being expanded.\ Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.generateRange()
```

## `` com.cra.figaro.algorithm.structured.ChainComponent.nonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ChainComponent.html#nonConstraintFactors-8f2)

```scala
def nonConstraintFactors(parameterized: Boolean = ...): List[Factor[Double]]
```

Generate the non-constraint factors based on the current range. For most elements, this just generates the factors in the usual way. For a chain, this does not include subproblem factors. The parameterized flag indicates whether parameterized elements should have special factors created that use the MAP values of their arguments. This defaults to false.

Type parameters: none.

Parameters, list 1: `` parameterized: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Generate the non-constraint factors based on the current range. For most elements, this just generates the factors in the usual way. For a chain, this does not include subproblem factors. The parameterized flag indicates whether parameterized elements should have special factors created that use the MAP values of their arguments. This defaults to false. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.nonConstraintFactors(parameterized)
```

## `` com.cra.figaro.algorithm.structured.ChainComponent.setVariable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ChainComponent.html#setVariable-fffff328)

```scala
def setVariable(v: Variable[Value]): Unit
```

Set the variable associated with this component to the given variable.

Type parameters: none.

Parameters, list 1: `` v: Variable[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Set the variable associated with this component to the given variable. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.setVariable(v)
```

## `` com.cra.figaro.algorithm.structured.ChainComponent.variable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ChainComponent.html#variable-0)

```scala
def variable: Variable[Value]
```

The current variable representing this component in factors. This is set automatically when the range is updated.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Variable[Value] ``.

Source contract/attributes: The current variable representing this component in factors. This is set automatically when the range is updated. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.variable
```

## `` com.cra.figaro.algorithm.structured.ComponentCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ComponentCollection.html#apply-c11)

```scala
def apply[T](element: Element[T]): ProblemComponent[T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` ProblemComponent[T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes

Invocation template:

```scala
receiver.apply[T](element)
```

## `` com.cra.figaro.algorithm.structured.ComponentCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ComponentCollection.html#apply-fffff253)

```scala
def apply[P, T](chain: Chain[P, T]): ChainComponent[P, T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` P ``; `` T ``.

Parameters, list 1: `` chain: Chain[P, T] ``.

Returns: `` ChainComponent[P, T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes

Invocation template:

```scala
receiver.apply[P, T](chain)
```

## `` com.cra.figaro.algorithm.structured.ComponentCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ComponentCollection.html#apply-fffff9d4)

```scala
def apply[T](apply: Apply[T]): ApplyComponent[T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` T ``.

Parameters, list 1: `` apply: Apply[T] ``.

Returns: `` ApplyComponent[T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes

Invocation template:

```scala
receiver.apply[T](apply)
```

## `` com.cra.figaro.algorithm.structured.ComponentCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ComponentCollection.html#apply-fffff00d)

```scala
def apply[T](makeArray: MakeArray[T]): MakeArrayComponent[T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` T ``.

Parameters, list 1: `` makeArray: MakeArray[T] ``.

Returns: `` MakeArrayComponent[T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes

Invocation template:

```scala
receiver.apply[T](makeArray)
```

## `` com.cra.figaro.algorithm.structured.ComponentCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ComponentCollection.html#apply-fffffeb8)

```scala
def apply[T](atomic: Atomic[T]): AtomicComponent[T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` T ``.

Parameters, list 1: `` atomic: Atomic[T] ``.

Returns: `` AtomicComponent[T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes

Invocation template:

```scala
receiver.apply[T](atomic)
```

## `` com.cra.figaro.algorithm.structured.ComponentCollection.contains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ComponentCollection.html#contains-6e0)

```scala
def contains[T](element: Element[T]): Boolean
```

Does the element have a component in this collection?

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` Boolean ``.

Source contract/attributes: Does the element have a component in this collection? Attributes

Invocation template:

```scala
receiver.contains[T](element)
```

## `` com.cra.figaro.algorithm.structured.ComponentHash.nextCode ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ComponentHash$.html#nextCode-0)

```scala
def nextCode: Int
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Invocation template:

```scala
com.cra.figaro.algorithm.structured.ComponentHash.nextCode
```

## `` com.cra.figaro.algorithm.structured.ExpandableComponent.expand ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ExpandableComponent.html#expand-fffff71f)

```scala
def expand(parentValue: ParentValue): Unit
```

Expand for a particular parent value.

Type parameters: none.

Parameters, list 1: `` parentValue: ParentValue ``.

Returns: `` Unit ``.

Source contract/attributes: Expand for a particular parent value. Attributes

Invocation template:

```scala
receiver.expand(parentValue)
```

## `` com.cra.figaro.algorithm.structured.ExpandableComponent.expand ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ExpandableComponent.html#expand-94c)

```scala
def expand(): Unit
```

Expand for all values of the parent that were not previously expanded, based on the current range of the parent.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Expand for all values of the parent that were not previously expanded, based on the current range of the parent. Attributes

Invocation template:

```scala
receiver.expand()
```

## `` com.cra.figaro.algorithm.structured.ExpandableComponent.constraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ExpandableComponent.html#constraintFactors-fffff66a)

```scala
def constraintFactors(bounds: Bounds = ...): List[Factor[Double]]
```

Gets the constraint factors for this component. Returns the lower bound factors unless an Upper argument is provided.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Gets the constraint factors for this component. Returns the lower bound factors unless an Upper argument is provided. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.constraintFactors(bounds)
```

## `` com.cra.figaro.algorithm.structured.ExpandableComponent.generateRange ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ExpandableComponent.html#generateRange-94c)

```scala
def generateRange(): Unit
```

Generate a range of values for this component. Also sets the variable for this component. If an argument is not in the component collection, we do not generate the argument, but instead assume its only value is *. This doesn't change the range of any other element or expand any subproblems. The range will include * based on argument ranges including * or any subproblem not being expanded.\

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Generate a range of values for this component. Also sets the variable for this component. If an argument is not in the component collection, we do not generate the argument, but instead assume its only value is *. This doesn't change the range of any other element or expand any subproblems. The range will include * based on argument ranges including * or any subproblem not being expanded.\ Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.generateRange()
```

## `` com.cra.figaro.algorithm.structured.ExpandableComponent.nonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ExpandableComponent.html#nonConstraintFactors-8f2)

```scala
def nonConstraintFactors(parameterized: Boolean = ...): List[Factor[Double]]
```

Generate the non-constraint factors based on the current range. For most elements, this just generates the factors in the usual way. For a chain, this does not include subproblem factors. The parameterized flag indicates whether parameterized elements should have special factors created that use the MAP values of their arguments. This defaults to false.

Type parameters: none.

Parameters, list 1: `` parameterized: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Generate the non-constraint factors based on the current range. For most elements, this just generates the factors in the usual way. For a chain, this does not include subproblem factors. The parameterized flag indicates whether parameterized elements should have special factors created that use the MAP values of their arguments. This defaults to false. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.nonConstraintFactors(parameterized)
```

## `` com.cra.figaro.algorithm.structured.ExpandableComponent.setVariable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ExpandableComponent.html#setVariable-fffff328)

```scala
def setVariable(v: Variable[Value]): Unit
```

Set the variable associated with this component to the given variable.

Type parameters: none.

Parameters, list 1: `` v: Variable[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Set the variable associated with this component to the given variable. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.setVariable(v)
```

## `` com.cra.figaro.algorithm.structured.ExpandableComponent.variable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ExpandableComponent.html#variable-0)

```scala
def variable: Variable[Value]
```

The current variable representing this component in factors. This is set automatically when the range is updated.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Variable[Value] ``.

Source contract/attributes: The current variable representing this component in factors. This is set automatically when the range is updated. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.variable
```

## `` com.cra.figaro.algorithm.structured.IncrementingCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/IncrementingCollection.html#apply-fffffeb8)

```scala
def apply[T](atomic: Atomic[T]): AtomicComponent[T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` T ``.

Parameters, list 1: `` atomic: Atomic[T] ``.

Returns: `` AtomicComponent[T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes Inherited from: ComponentCollection

Invocation template:

```scala
receiver.apply[T](atomic)
```

## `` com.cra.figaro.algorithm.structured.IncrementingCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/IncrementingCollection.html#apply-fffff00d)

```scala
def apply[T](makeArray: MakeArray[T]): MakeArrayComponent[T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` T ``.

Parameters, list 1: `` makeArray: MakeArray[T] ``.

Returns: `` MakeArrayComponent[T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes Inherited from: ComponentCollection

Invocation template:

```scala
receiver.apply[T](makeArray)
```

## `` com.cra.figaro.algorithm.structured.IncrementingCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/IncrementingCollection.html#apply-fffff9d4)

```scala
def apply[T](apply: Apply[T]): ApplyComponent[T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` T ``.

Parameters, list 1: `` apply: Apply[T] ``.

Returns: `` ApplyComponent[T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes Inherited from: ComponentCollection

Invocation template:

```scala
receiver.apply[T](apply)
```

## `` com.cra.figaro.algorithm.structured.IncrementingCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/IncrementingCollection.html#apply-fffff253)

```scala
def apply[P, T](chain: Chain[P, T]): ChainComponent[P, T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` P ``; `` T ``.

Parameters, list 1: `` chain: Chain[P, T] ``.

Returns: `` ChainComponent[P, T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes Inherited from: ComponentCollection

Invocation template:

```scala
receiver.apply[P, T](chain)
```

## `` com.cra.figaro.algorithm.structured.IncrementingCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/IncrementingCollection.html#apply-c11)

```scala
def apply[T](element: Element[T]): ProblemComponent[T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` ProblemComponent[T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes Inherited from: ComponentCollection

Invocation template:

```scala
receiver.apply[T](element)
```

## `` com.cra.figaro.algorithm.structured.IncrementingCollection.contains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/IncrementingCollection.html#contains-6e0)

```scala
def contains[T](element: Element[T]): Boolean
```

Does the element have a component in this collection?

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` Boolean ``.

Source contract/attributes: Does the element have a component in this collection? Attributes Inherited from: ComponentCollection

Invocation template:

```scala
receiver.contains[T](element)
```

## `` com.cra.figaro.algorithm.structured.Lower.fromProduct ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/Lower$.html#fromProduct-de7)

```scala
def fromProduct(p: Product): MirroredMonoType
```

Creates a new instance of type T with elements taken from product p.

Type parameters: none.

Parameters, list 1: `` p: Product ``.

Returns: `` MirroredMonoType ``.

Source contract/attributes: Creates a new instance of type T with elements taken from product p. Attributes Inherited from: Singleton

Invocation template:

```scala
com.cra.figaro.algorithm.structured.Lower.fromProduct(p)
```

## `` com.cra.figaro.algorithm.structured.Lower.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/Lower$.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
com.cra.figaro.algorithm.structured.Lower.productElementNames
```

## `` com.cra.figaro.algorithm.structured.Lower.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/Lower$.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
com.cra.figaro.algorithm.structured.Lower.productIterator
```

## `` com.cra.figaro.algorithm.structured.MakeArrayComponent.expand ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/MakeArrayComponent.html#expand-fffff3b3)

```scala
def expand(n: Int): Unit
```

Ensure that the given number of items is expanded.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Unit ``.

Source contract/attributes: Ensure that the given number of items is expanded. Attributes

Invocation template:

```scala
receiver.expand(n)
```

## `` com.cra.figaro.algorithm.structured.MakeArrayComponent.expand ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/MakeArrayComponent.html#expand-94c)

```scala
override def expand(): Unit
```

Expand all the potential items according to the maximum value in the range of the MakeArray's number of items.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Expand all the potential items according to the maximum value in the range of the MakeArray's number of items. Attributes Definition Classes ExpandableComponent

Invocation template:

```scala
receiver.expand()
```

## `` com.cra.figaro.algorithm.structured.MakeArrayComponent.constraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/MakeArrayComponent.html#constraintFactors-fffff66a)

```scala
def constraintFactors(bounds: Bounds = ...): List[Factor[Double]]
```

Gets the constraint factors for this component. Returns the lower bound factors unless an Upper argument is provided.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Gets the constraint factors for this component. Returns the lower bound factors unless an Upper argument is provided. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.constraintFactors(bounds)
```

## `` com.cra.figaro.algorithm.structured.MakeArrayComponent.generateRange ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/MakeArrayComponent.html#generateRange-94c)

```scala
def generateRange(): Unit
```

Generate a range of values for this component. Also sets the variable for this component. If an argument is not in the component collection, we do not generate the argument, but instead assume its only value is *. This doesn't change the range of any other element or expand any subproblems. The range will include * based on argument ranges including * or any subproblem not being expanded.\

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Generate a range of values for this component. Also sets the variable for this component. If an argument is not in the component collection, we do not generate the argument, but instead assume its only value is *. This doesn't change the range of any other element or expand any subproblems. The range will include * based on argument ranges including * or any subproblem not being expanded.\ Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.generateRange()
```

## `` com.cra.figaro.algorithm.structured.MakeArrayComponent.nonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/MakeArrayComponent.html#nonConstraintFactors-8f2)

```scala
def nonConstraintFactors(parameterized: Boolean = ...): List[Factor[Double]]
```

Generate the non-constraint factors based on the current range. For most elements, this just generates the factors in the usual way. For a chain, this does not include subproblem factors. The parameterized flag indicates whether parameterized elements should have special factors created that use the MAP values of their arguments. This defaults to false.

Type parameters: none.

Parameters, list 1: `` parameterized: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Generate the non-constraint factors based on the current range. For most elements, this just generates the factors in the usual way. For a chain, this does not include subproblem factors. The parameterized flag indicates whether parameterized elements should have special factors created that use the MAP values of their arguments. This defaults to false. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.nonConstraintFactors(parameterized)
```

## `` com.cra.figaro.algorithm.structured.MakeArrayComponent.setVariable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/MakeArrayComponent.html#setVariable-fffff328)

```scala
def setVariable(v: Variable[FixedSizeArray[Value]]): Unit
```

Set the variable associated with this component to the given variable.

Type parameters: none.

Parameters, list 1: `` v: Variable[FixedSizeArray[Value]] ``.

Returns: `` Unit ``.

Source contract/attributes: Set the variable associated with this component to the given variable. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.setVariable(v)
```

## `` com.cra.figaro.algorithm.structured.MakeArrayComponent.variable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/MakeArrayComponent.html#variable-0)

```scala
def variable: Variable[FixedSizeArray[Value]]
```

The current variable representing this component in factors. This is set automatically when the range is updated.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Variable[FixedSizeArray[Value]] ``.

Source contract/attributes: The current variable representing this component in factors. This is set automatically when the range is updated. Attributes Inherited from: ProblemComponent

Invocation template:

```scala
receiver.variable
```

## `` com.cra.figaro.algorithm.structured.MinimalIncrementingCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/MinimalIncrementingCollection.html#apply-fffffeb8)

```scala
def apply[T](atomic: Atomic[T]): AtomicComponent[T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` T ``.

Parameters, list 1: `` atomic: Atomic[T] ``.

Returns: `` AtomicComponent[T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes Inherited from: ComponentCollection

Invocation template:

```scala
receiver.apply[T](atomic)
```

## `` com.cra.figaro.algorithm.structured.MinimalIncrementingCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/MinimalIncrementingCollection.html#apply-fffff00d)

```scala
def apply[T](makeArray: MakeArray[T]): MakeArrayComponent[T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` T ``.

Parameters, list 1: `` makeArray: MakeArray[T] ``.

Returns: `` MakeArrayComponent[T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes Inherited from: ComponentCollection

Invocation template:

```scala
receiver.apply[T](makeArray)
```

## `` com.cra.figaro.algorithm.structured.MinimalIncrementingCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/MinimalIncrementingCollection.html#apply-fffff9d4)

```scala
def apply[T](apply: Apply[T]): ApplyComponent[T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` T ``.

Parameters, list 1: `` apply: Apply[T] ``.

Returns: `` ApplyComponent[T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes Inherited from: ComponentCollection

Invocation template:

```scala
receiver.apply[T](apply)
```

## `` com.cra.figaro.algorithm.structured.MinimalIncrementingCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/MinimalIncrementingCollection.html#apply-fffff253)

```scala
def apply[P, T](chain: Chain[P, T]): ChainComponent[P, T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` P ``; `` T ``.

Parameters, list 1: `` chain: Chain[P, T] ``.

Returns: `` ChainComponent[P, T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes Inherited from: ComponentCollection

Invocation template:

```scala
receiver.apply[P, T](chain)
```

## `` com.cra.figaro.algorithm.structured.MinimalIncrementingCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/MinimalIncrementingCollection.html#apply-c11)

```scala
def apply[T](element: Element[T]): ProblemComponent[T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` ProblemComponent[T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes Inherited from: ComponentCollection

Invocation template:

```scala
receiver.apply[T](element)
```

## `` com.cra.figaro.algorithm.structured.MinimalIncrementingCollection.contains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/MinimalIncrementingCollection.html#contains-6e0)

```scala
def contains[T](element: Element[T]): Boolean
```

Does the element have a component in this collection?

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` Boolean ``.

Source contract/attributes: Does the element have a component in this collection? Attributes Inherited from: ComponentCollection

Invocation template:

```scala
receiver.contains[T](element)
```

## `` com.cra.figaro.algorithm.structured.NestedProblem.add ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/NestedProblem.html#add-c11)

```scala
def add[T](element: Element[T]): ProblemComponent[T]
```

Add a component for the given element to this problem.

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` ProblemComponent[T] ``.

Source contract/attributes: Add a component for the given element to this problem. Attributes Inherited from: Problem

Invocation template:

```scala
receiver.add[T](element)
```

## `` com.cra.figaro.algorithm.structured.NestedProblem.contains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/NestedProblem.html#contains-fffffc14)

```scala
def contains(otherProblem: Problem): Boolean
```

Determines if this problem contains the given problem. Any variables in the contained problem should also be eliminated when this problem is solved.

Type parameters: none.

Parameters, list 1: `` otherProblem: Problem ``.

Returns: `` Boolean ``.

Source contract/attributes: Determines if this problem contains the given problem. Any variables in the contained problem should also be eliminated when this problem is solved. Attributes Inherited from: Problem

Invocation template:

```scala
receiver.contains(otherProblem)
```

## `` com.cra.figaro.algorithm.structured.NestedProblem.fullyRefined ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/NestedProblem.html#fullyRefined-0)

```scala
def fullyRefined: Boolean
```

Determines if this problem is fully refined. A problem is fully refined when all components within are fully refined, in which case none of their ranges or factors will change with further refinement.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Determines if this problem is fully refined. A problem is fully refined when all components within are fully refined, in which case none of their ranges or factors will change with further refinement. Attributes Inherited from: Problem

Invocation template:

```scala
receiver.fullyRefined
```

## `` com.cra.figaro.algorithm.structured.NestedProblem.global ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/NestedProblem.html#global-4)

```scala
def global(variable: Variable[_]): Boolean
```

Determines if a variable is in scope outside of this problem

Type parameters: none.

Parameters, list 1: `` variable: Variable[_] ``.

Returns: `` Boolean ``.

Source contract/attributes: Determines if a variable is in scope outside of this problem Attributes Inherited from: Problem

Invocation template:

```scala
receiver.global(variable)
```

## `` com.cra.figaro.algorithm.structured.NestedProblem.internal ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/NestedProblem.html#internal-4)

```scala
def internal(variable: Variable[_]): Boolean
```

Determines if a variable is internal to this problem and should be eliminated

Type parameters: none.

Parameters, list 1: `` variable: Variable[_] ``.

Returns: `` Boolean ``.

Source contract/attributes: Determines if a variable is internal to this problem and should be eliminated Attributes Inherited from: Problem

Invocation template:

```scala
receiver.internal(variable)
```

## `` com.cra.figaro.algorithm.structured.NestedProblem.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/NestedProblem.html#sample-ffffff32)

```scala
def sample(): (Map[Element[_], _], Double)
```

Produce a single weighted sample of all the elements in this problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (Map[Element[_], _], Double) ``.

Source contract/attributes: Produce a single weighted sample of all the elements in this problem. Attributes Inherited from: Problem

Invocation template:

```scala
receiver.sample()
```

## `` com.cra.figaro.algorithm.structured.NestedProblem.targetComponents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/NestedProblem.html#targetComponents-0)

```scala
def targetComponents: List[ProblemComponent[_]]
```

Targets of this problem as a list of components in the collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[ProblemComponent[_]] ``.

Source contract/attributes: Targets of this problem as a list of components in the collection. Attributes Inherited from: Problem

Invocation template:

```scala
receiver.targetComponents
```

## `` com.cra.figaro.algorithm.structured.Problem.add ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/Problem.html#add-c11)

```scala
def add[T](element: Element[T]): ProblemComponent[T]
```

Add a component for the given element to this problem.

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` ProblemComponent[T] ``.

Source contract/attributes: Add a component for the given element to this problem. Attributes

Invocation template:

```scala
receiver.add[T](element)
```

## `` com.cra.figaro.algorithm.structured.Problem.contains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/Problem.html#contains-fffffc14)

```scala
def contains(otherProblem: Problem): Boolean
```

Determines if this problem contains the given problem. Any variables in the contained problem should also be eliminated when this problem is solved.

Type parameters: none.

Parameters, list 1: `` otherProblem: Problem ``.

Returns: `` Boolean ``.

Source contract/attributes: Determines if this problem contains the given problem. Any variables in the contained problem should also be eliminated when this problem is solved. Attributes

Invocation template:

```scala
receiver.contains(otherProblem)
```

## `` com.cra.figaro.algorithm.structured.Problem.fullyRefined ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/Problem.html#fullyRefined-0)

```scala
def fullyRefined: Boolean
```

Determines if this problem is fully refined. A problem is fully refined when all components within are fully refined, in which case none of their ranges or factors will change with further refinement.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Determines if this problem is fully refined. A problem is fully refined when all components within are fully refined, in which case none of their ranges or factors will change with further refinement. Attributes

Invocation template:

```scala
receiver.fullyRefined
```

## `` com.cra.figaro.algorithm.structured.Problem.global ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/Problem.html#global-4)

```scala
def global(variable: Variable[_]): Boolean
```

Determines if a variable is in scope outside of this problem

Type parameters: none.

Parameters, list 1: `` variable: Variable[_] ``.

Returns: `` Boolean ``.

Source contract/attributes: Determines if a variable is in scope outside of this problem Attributes

Invocation template:

```scala
receiver.global(variable)
```

## `` com.cra.figaro.algorithm.structured.Problem.internal ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/Problem.html#internal-4)

```scala
def internal(variable: Variable[_]): Boolean
```

Determines if a variable is internal to this problem and should be eliminated

Type parameters: none.

Parameters, list 1: `` variable: Variable[_] ``.

Returns: `` Boolean ``.

Source contract/attributes: Determines if a variable is internal to this problem and should be eliminated Attributes

Invocation template:

```scala
receiver.internal(variable)
```

## `` com.cra.figaro.algorithm.structured.Problem.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/Problem.html#sample-ffffff32)

```scala
def sample(): (Map[Element[_], _], Double)
```

Produce a single weighted sample of all the elements in this problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (Map[Element[_], _], Double) ``.

Source contract/attributes: Produce a single weighted sample of all the elements in this problem. Attributes

Invocation template:

```scala
receiver.sample()
```

## `` com.cra.figaro.algorithm.structured.Problem.targetComponents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/Problem.html#targetComponents-0)

```scala
def targetComponents: List[ProblemComponent[_]]
```

Targets of this problem as a list of components in the collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[ProblemComponent[_]] ``.

Source contract/attributes: Targets of this problem as a list of components in the collection. Attributes

Invocation template:

```scala
receiver.targetComponents
```

## `` com.cra.figaro.algorithm.structured.ProblemComponent.constraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ProblemComponent.html#constraintFactors-fffff66a)

```scala
def constraintFactors(bounds: Bounds = ...): List[Factor[Double]]
```

Gets the constraint factors for this component. Returns the lower bound factors unless an Upper argument is provided.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Gets the constraint factors for this component. Returns the lower bound factors unless an Upper argument is provided. Attributes

Invocation template:

```scala
receiver.constraintFactors(bounds)
```

## `` com.cra.figaro.algorithm.structured.ProblemComponent.generateRange ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ProblemComponent.html#generateRange-94c)

```scala
def generateRange(): Unit
```

Generate a range of values for this component. Also sets the variable for this component. If an argument is not in the component collection, we do not generate the argument, but instead assume its only value is *. This doesn't change the range of any other element or expand any subproblems. The range will include * based on argument ranges including * or any subproblem not being expanded.\

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Generate a range of values for this component. Also sets the variable for this component. If an argument is not in the component collection, we do not generate the argument, but instead assume its only value is *. This doesn't change the range of any other element or expand any subproblems. The range will include * based on argument ranges including * or any subproblem not being expanded.\ Attributes

Invocation template:

```scala
receiver.generateRange()
```

## `` com.cra.figaro.algorithm.structured.ProblemComponent.nonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ProblemComponent.html#nonConstraintFactors-8f2)

```scala
def nonConstraintFactors(parameterized: Boolean = ...): List[Factor[Double]]
```

Generate the non-constraint factors based on the current range. For most elements, this just generates the factors in the usual way. For a chain, this does not include subproblem factors. The parameterized flag indicates whether parameterized elements should have special factors created that use the MAP values of their arguments. This defaults to false.

Type parameters: none.

Parameters, list 1: `` parameterized: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Generate the non-constraint factors based on the current range. For most elements, this just generates the factors in the usual way. For a chain, this does not include subproblem factors. The parameterized flag indicates whether parameterized elements should have special factors created that use the MAP values of their arguments. This defaults to false. Attributes

Invocation template:

```scala
receiver.nonConstraintFactors(parameterized)
```

## `` com.cra.figaro.algorithm.structured.ProblemComponent.setVariable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ProblemComponent.html#setVariable-fffff328)

```scala
def setVariable(v: Variable[Value]): Unit
```

Set the variable associated with this component to the given variable.

Type parameters: none.

Parameters, list 1: `` v: Variable[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Set the variable associated with this component to the given variable. Attributes

Invocation template:

```scala
receiver.setVariable(v)
```

## `` com.cra.figaro.algorithm.structured.ProblemComponent.variable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/ProblemComponent.html#variable-0)

```scala
def variable: Variable[Value]
```

The current variable representing this component in factors. This is set automatically when the range is updated.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Variable[Value] ``.

Source contract/attributes: The current variable representing this component in factors. This is set automatically when the range is updated. Attributes

Invocation template:

```scala
receiver.variable
```

## `` com.cra.figaro.algorithm.structured.Range.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/Range$.html#apply-5b5)

```scala
def apply[V](component: ProblemComponent[V]): ValueSet[V]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` V ``.

Parameters, list 1: `` component: ProblemComponent[V] ``.

Returns: `` ValueSet[V] ``.

Invocation template:

```scala
com.cra.figaro.algorithm.structured.Range.apply[V](component)
```

## `` com.cra.figaro.algorithm.structured.Range.getRangeOfFold ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/Range$.html#getRangeOfFold-a76)

```scala
def getRangeOfFold[T, U](cc: ComponentCollection, fold: FoldLeft[T, U]): ValueSet[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` cc: ComponentCollection ``; `` fold: FoldLeft[T, U] ``.

Returns: `` ValueSet[U] ``.

Invocation template:

```scala
com.cra.figaro.algorithm.structured.Range.getRangeOfFold[T, U](cc, fold)
```

## `` com.cra.figaro.algorithm.structured.SelectiveIncrementingCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/SelectiveIncrementingCollection.html#apply-fffffeb8)

```scala
def apply[T](atomic: Atomic[T]): AtomicComponent[T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` T ``.

Parameters, list 1: `` atomic: Atomic[T] ``.

Returns: `` AtomicComponent[T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes Inherited from: ComponentCollection

Invocation template:

```scala
receiver.apply[T](atomic)
```

## `` com.cra.figaro.algorithm.structured.SelectiveIncrementingCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/SelectiveIncrementingCollection.html#apply-fffff00d)

```scala
def apply[T](makeArray: MakeArray[T]): MakeArrayComponent[T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` T ``.

Parameters, list 1: `` makeArray: MakeArray[T] ``.

Returns: `` MakeArrayComponent[T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes Inherited from: ComponentCollection

Invocation template:

```scala
receiver.apply[T](makeArray)
```

## `` com.cra.figaro.algorithm.structured.SelectiveIncrementingCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/SelectiveIncrementingCollection.html#apply-fffff9d4)

```scala
def apply[T](apply: Apply[T]): ApplyComponent[T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` T ``.

Parameters, list 1: `` apply: Apply[T] ``.

Returns: `` ApplyComponent[T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes Inherited from: ComponentCollection

Invocation template:

```scala
receiver.apply[T](apply)
```

## `` com.cra.figaro.algorithm.structured.SelectiveIncrementingCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/SelectiveIncrementingCollection.html#apply-fffff253)

```scala
def apply[P, T](chain: Chain[P, T]): ChainComponent[P, T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` P ``; `` T ``.

Parameters, list 1: `` chain: Chain[P, T] ``.

Returns: `` ChainComponent[P, T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes Inherited from: ComponentCollection

Invocation template:

```scala
receiver.apply[P, T](chain)
```

## `` com.cra.figaro.algorithm.structured.SelectiveIncrementingCollection.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/SelectiveIncrementingCollection.html#apply-c11)

```scala
def apply[T](element: Element[T]): ProblemComponent[T]
```

Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component.

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` ProblemComponent[T] ``.

Source contract/attributes: Get the component associated with this element in this collection. Throws an exception if the element is not associated with any component. Attributes Inherited from: ComponentCollection

Invocation template:

```scala
receiver.apply[T](element)
```

## `` com.cra.figaro.algorithm.structured.SelectiveIncrementingCollection.contains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/SelectiveIncrementingCollection.html#contains-6e0)

```scala
def contains[T](element: Element[T]): Boolean
```

Does the element have a component in this collection?

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` Boolean ``.

Source contract/attributes: Does the element have a component in this collection? Attributes Inherited from: ComponentCollection

Invocation template:

```scala
receiver.contains[T](element)
```

## `` .marginalBeliefPropagation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/solver.html#marginalBeliefPropagation-e43)

```scala
def marginalBeliefPropagation(iterations: Int = ...)(problem: Problem, toEliminate: Set[Variable[_]], toPreserve: Set[Variable[_]], factors: List[Factor[Double]]): Solution
```

Creates a belief propagation solver.

Type parameters: none.

Parameters, list 1: `` iterations: Int = ... ``.

Parameters, list 2: `` problem: Problem ``; `` toEliminate: Set[Variable[_]] ``; `` toPreserve: Set[Variable[_]] ``; `` factors: List[Factor[Double]] ``.

Returns: `` Solution ``.

Source contract/attributes: Creates a belief propagation solver. Value parameters factors all the factors in the problem iterations number of iterations of BP to run problem the problem to solve toEliminate the variables to be eliminated toPreserve the variables to be preserved (not eliminated) Attributes

Invocation template:

```scala
receiver.marginalBeliefPropagation(iterations)(problem, toEliminate, toPreserve, factors)
```

## `` .marginalGibbs ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/solver.html#marginalGibbs-fffff2c6)

```scala
def marginalGibbs(numSamples: Int, burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator)(problem: Problem, toEliminate: Set[Variable[_]], toPreserve: Set[Variable[_]], factors: List[Factor[Double]]): Solution
```

Creates a Gibbs sampling solver.

Type parameters: none.

Parameters, list 1: `` numSamples: Int ``; `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``.

Parameters, list 2: `` problem: Problem ``; `` toEliminate: Set[Variable[_]] ``; `` toPreserve: Set[Variable[_]] ``; `` factors: List[Factor[Double]] ``.

Returns: `` Solution ``.

Source contract/attributes: Creates a Gibbs sampling solver. Value parameters blockToSampler function for creating Gibbs block samplers burnIn number of burn-in samples to throw away factors all the factors in the problem interval number of samples to throw away between recorded samples numSamples number of samples to take problem the problem to solve toEliminate the variables to be eliminated toPreserve the variables to be preserved (not eliminated) Attributes

Invocation template:

```scala
receiver.marginalGibbs(numSamples, burnIn, interval, blockToSampler)(problem, toEliminate, toPreserve, factors)
```

## `` .marginalVariableElimination ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/solver.html#marginalVariableElimination-fffffadc)

```scala
def marginalVariableElimination(problem: Problem, toEliminate: Set[Variable[_]], toPreserve: Set[Variable[_]], factors: List[Factor[Double]]): Solution
```

Creates a variable elimination solver.

Type parameters: none.

Parameters, list 1: `` problem: Problem ``; `` toEliminate: Set[Variable[_]] ``; `` toPreserve: Set[Variable[_]] ``; `` factors: List[Factor[Double]] ``.

Returns: `` Solution ``.

Source contract/attributes: Creates a variable elimination solver. Value parameters factors all the factors in the problem problem the problem to solve toEliminate the variables to be eliminated toPreserve the variables to be preserved (not eliminated) Attributes

Invocation template:

```scala
receiver.marginalVariableElimination(problem, toEliminate, toPreserve, factors)
```

## `` .mpeBeliefPropagation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/solver.html#mpeBeliefPropagation-e43)

```scala
def mpeBeliefPropagation(iterations: Int = ...)(problem: Problem, toEliminate: Set[Variable[_]], toPreserve: Set[Variable[_]], factors: List[Factor[Double]]): Solution
```

Creates an MPE belief propagation solver.

Type parameters: none.

Parameters, list 1: `` iterations: Int = ... ``.

Parameters, list 2: `` problem: Problem ``; `` toEliminate: Set[Variable[_]] ``; `` toPreserve: Set[Variable[_]] ``; `` factors: List[Factor[Double]] ``.

Returns: `` Solution ``.

Source contract/attributes: Creates an MPE belief propagation solver. Value parameters factors all the factors in the problem iterations number of iterations of BP to run problem the problem to solve toEliminate the variables to be eliminated toPreserve the variables to be preserved (not eliminated) Attributes

Invocation template:

```scala
receiver.mpeBeliefPropagation(iterations)(problem, toEliminate, toPreserve, factors)
```

## `` .mpeVariableElimination ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/solver.html#mpeVariableElimination-fffffadc)

```scala
def mpeVariableElimination(problem: Problem, toEliminate: Set[Variable[_]], toPreserve: Set[Variable[_]], factors: List[Factor[Double]]): Solution
```

Creates an MPE variable elimination solver.

Type parameters: none.

Parameters, list 1: `` problem: Problem ``; `` toEliminate: Set[Variable[_]] ``; `` toPreserve: Set[Variable[_]] ``; `` factors: List[Factor[Double]] ``.

Returns: `` Solution ``.

Source contract/attributes: Creates an MPE variable elimination solver. Value parameters factors all the factors in the problem problem the problem to solve toEliminate the variables to be eliminated toPreserve the variables to be preserved (not eliminated) Attributes

Invocation template:

```scala
receiver.mpeVariableElimination(problem, toEliminate, toPreserve, factors)
```

## `` com.cra.figaro.algorithm.structured.Upper.fromProduct ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/Upper$.html#fromProduct-de7)

```scala
def fromProduct(p: Product): MirroredMonoType
```

Creates a new instance of type T with elements taken from product p.

Type parameters: none.

Parameters, list 1: `` p: Product ``.

Returns: `` MirroredMonoType ``.

Source contract/attributes: Creates a new instance of type T with elements taken from product p. Attributes Inherited from: Singleton

Invocation template:

```scala
com.cra.figaro.algorithm.structured.Upper.fromProduct(p)
```

## `` com.cra.figaro.algorithm.structured.Upper.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/Upper$.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
com.cra.figaro.algorithm.structured.Upper.productElementNames
```

## `` com.cra.figaro.algorithm.structured.Upper.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/Upper$.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
com.cra.figaro.algorithm.structured.Upper.productIterator
```
