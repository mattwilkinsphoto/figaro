<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.algorithm.factored.beliefpropagation

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.algorithm.factored.beliefpropagation.AnytimeProbabilisticBeliefPropagation.belief ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/AnytimeProbabilisticBeliefPropagation.html#belief-e6a)

```scala
def belief(source: Node): Factor[Double]
```

Returns the product of all messages from a source node's neighbors to itself.

Type parameters: none.

Parameters, list 1: `` source: Node ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Returns the product of all messages from a source node's neighbors to itself. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.belief(source)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.AnytimeProbabilisticBeliefPropagation.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/AnytimeProbabilisticBeliefPropagation.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.AnytimeProbabilisticBeliefPropagation.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/AnytimeProbabilisticBeliefPropagation.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

Returns the factors needed for BP. Since BP operates on a complete factor graph, factors are created for all elements in the universe.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Returns the factors needed for BP. Since BP operates on a complete factor graph, factors are created for all elements in the universe. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.AnytimeProbabilisticBeliefPropagation.getFinalFactorForElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/AnytimeProbabilisticBeliefPropagation.html#getFinalFactorForElement-c2f)

```scala
def getFinalFactorForElement[T](target: Element[T]): Factor[Double]
```

Get the final factor for an element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Get the final factor for an element. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.getFinalFactorForElement[T](target)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.AnytimeProbabilisticBeliefPropagation.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/AnytimeProbabilisticBeliefPropagation.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.AnytimeProbabilisticBeliefPropagation.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/AnytimeProbabilisticBeliefPropagation.html#initialize-94c)

```scala
override def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Definition Classes BeliefPropagation -> Algorithm Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.AnytimeProbabilisticBeliefPropagation.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/AnytimeProbabilisticBeliefPropagation.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.AnytimeProbabilisticBeliefPropagation.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/AnytimeProbabilisticBeliefPropagation.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.AnytimeProbabilisticBeliefPropagation.normalize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/AnytimeProbabilisticBeliefPropagation.html#normalize-fffffd5e)

```scala
def normalize(factor: Factor[Double]): Factor[Double]
```

Normalize a factor.

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Normalize a factor. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.normalize(factor)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.AnytimeProbabilisticBeliefPropagation.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/AnytimeProbabilisticBeliefPropagation.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.AnytimeProbabilisticBeliefPropagation.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/AnytimeProbabilisticBeliefPropagation.html#runStep-94c)

```scala
def runStep(): Unit
```

Runs this belief propagation algorithm for one iteration. An iteration consists of each node of the factor graph sending a message to each of its neighbors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Runs this belief propagation algorithm for one iteration. An iteration consists of each node of the factor graph sending a message to each of its neighbors. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.AnytimeProbabilisticBeliefPropagation.shutdown ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/AnytimeProbabilisticBeliefPropagation.html#shutdown-0)

```scala
def shutdown: Unit
```

Release all resources from this anytime algorithm.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Unit ``.

Source contract/attributes: Release all resources from this anytime algorithm. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.shutdown
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.AnytimeProbabilisticBeliefPropagation.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/AnytimeProbabilisticBeliefPropagation.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.AnytimeProbabilisticBeliefPropagation.starterElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/AnytimeProbabilisticBeliefPropagation.html#starterElements-0)

```scala
def starterElements: List[Element[_]]
```

Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.starterElements
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.AnytimeProbabilisticBeliefPropagation.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/AnytimeProbabilisticBeliefPropagation.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.AnytimeProbabilisticBeliefPropagation.stopUpdate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/AnytimeProbabilisticBeliefPropagation.html#stopUpdate-94c)

```scala
def stopUpdate(): Unit
```

Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.stopUpdate()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.AnytimeProbabilisticBeliefPropagation.handle ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/AnytimeProbabilisticBeliefPropagation.html#handle-fffff940)

```scala
def handle(service: Service): Response
```

A handler of services provided by the algorithm.

Type parameters: none.

Parameters, list 1: `` service: Service ``.

Returns: `` Response ``.

Source contract/attributes: A handler of services provided by the algorithm. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.handle(service)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BasicFactorGraph.contains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BasicFactorGraph.html#contains-a85)

```scala
def contains(v: Node): Boolean
```

Returns true if the graph contains a node for a (single) variable.

Type parameters: none.

Parameters, list 1: `` v: Node ``.

Returns: `` Boolean ``.

Source contract/attributes: Returns true if the graph contains a node for a (single) variable. Attributes

Invocation template:

```scala
receiver.contains(v)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BasicFactorGraph.getFactorForNode ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BasicFactorGraph.html#getFactorForNode-399)

```scala
def getFactorForNode(fn: FactorNode): Factor[Double]
```

Gets the factor for a particular factor node.

Type parameters: none.

Parameters, list 1: `` fn: FactorNode ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Gets the factor for a particular factor node. Attributes

Invocation template:

```scala
receiver.getFactorForNode(fn)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BasicFactorGraph.getLastMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BasicFactorGraph.html#getLastMessage-425)

```scala
def getLastMessage(from: Node, to: Node): Factor[Double]
```

Gets the last message to a node from another.

Type parameters: none.

Parameters, list 1: `` from: Node ``; `` to: Node ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Gets the last message to a node from another. Attributes

Invocation template:

```scala
receiver.getLastMessage(from, to)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BasicFactorGraph.getMessagesForNode ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BasicFactorGraph.html#getMessagesForNode-df)

```scala
def getMessagesForNode(node: Node): Iterable[(Node, Factor[Double])]
```

Get a list of messages from the node.

Type parameters: none.

Parameters, list 1: `` node: Node ``.

Returns: `` Iterable[(Node, Factor[Double])] ``.

Source contract/attributes: Get a list of messages from the node. Attributes

Invocation template:

```scala
receiver.getMessagesForNode(node)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BasicFactorGraph.getNeighbors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BasicFactorGraph.html#getNeighbors-df)

```scala
def getNeighbors(source: Node): Iterable[Node]
```

Returns all neighbors of a given node.

Type parameters: none.

Parameters, list 1: `` source: Node ``.

Returns: `` Iterable[Node] ``.

Source contract/attributes: Returns all neighbors of a given node. Attributes

Invocation template:

```scala
receiver.getNeighbors(source)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BasicFactorGraph.getNeighbors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BasicFactorGraph.html#getNeighbors-184)

```scala
def getNeighbors(source: Node, excluding: Node): Iterable[Node]
```

Returns all neighbors of a given node excluding the node of the second argument.

Type parameters: none.

Parameters, list 1: `` source: Node ``; `` excluding: Node ``.

Returns: `` Iterable[Node] ``.

Source contract/attributes: Returns all neighbors of a given node excluding the node of the second argument. Attributes

Invocation template:

```scala
receiver.getNeighbors(source, excluding)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BasicFactorGraph.getNodes ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BasicFactorGraph.html#getNodes-fffffd3a)

```scala
def getNodes(): Iterable[Node]
```

Returns all nodes in the factor graph.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Iterable[Node] ``.

Source contract/attributes: Returns all nodes in the factor graph. Attributes

Invocation template:

```scala
receiver.getNodes()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BasicFactorGraph.toMutableMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BasicFactorGraph.html#toMutableMap-bf4)

```scala
def toMutableMap(m: Map[Node, Factor[Double]]): Map[Node, Factor[Double]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` m: Map[Node, Factor[Double]] ``.

Returns: `` Map[Node, Factor[Double]] ``.

Invocation template:

```scala
receiver.toMutableMap(m)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BasicFactorGraph.uniformFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BasicFactorGraph.html#uniformFactor-fffffc01)

```scala
def uniformFactor(v: List[Variable[_]]): Factor[Double]
```

Create a uniform factor to initialize the graph

Type parameters: none.

Parameters, list 1: `` v: List[Variable[_]] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Create a uniform factor to initialize the graph Attributes

Invocation template:

```scala
receiver.uniformFactor(v)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BasicFactorGraph.update ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BasicFactorGraph.html#update-fffff3a1)

```scala
def update(from: Node, to: Node, f: Factor[Double]): FactorGraph[Double]
```

Updates the factor graph with a message from a node to another. Returns a new factor graph, which can be the same as this one.

Type parameters: none.

Parameters, list 1: `` from: Node ``; `` to: Node ``; `` f: Factor[Double] ``.

Returns: `` FactorGraph[Double] ``.

Source contract/attributes: Updates the factor graph with a message from a node to another. Returns a new factor graph, which can be the same as this one. Attributes

Invocation template:

```scala
receiver.update(from, to, f)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation$.html#apply-fffff9d3)

```scala
def apply(myIterations: Int, targets: Element[_]*)(implicit universe: Universe): ProbQueryBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeProbQuery
```

Creates a One Time belief propagation computer in the current default universe.

Type parameters: none.

Parameters, list 1: `` myIterations: Int ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeProbQuery ``.

Source contract/attributes: Creates a One Time belief propagation computer in the current default universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.apply(myIterations, targets*)(using universe)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation$.html#apply-fffffd2c)

```scala
def apply(targets: Element[_]*)(implicit universe: Universe): ProbQueryBeliefPropagation & AnytimeProbabilisticBeliefPropagation & AnytimeProbQuery
```

Creates a Anytime belief propagation computer in the current default universe.

Type parameters: none.

Parameters, list 1: `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryBeliefPropagation & AnytimeProbabilisticBeliefPropagation & AnytimeProbQuery ``.

Source contract/attributes: Creates a Anytime belief propagation computer in the current default universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.apply(targets*)(using universe)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation$.html#apply-fffff4c1)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], myIterations: Int, targets: Element[_]*)(implicit universe: Universe): ProbQueryBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeProbQuery
```

Create a Anytime belief propagation computer using the given dependent universes in the current default universe.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` myIterations: Int ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeProbQuery ``.

Source contract/attributes: Create a Anytime belief propagation computer using the given dependent universes in the current default universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.apply(dependentUniverses, myIterations, targets*)(using universe)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation$.html#apply-e7e)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], targets: Element[_]*)(implicit universe: Universe): ProbQueryBeliefPropagation & AnytimeProbabilisticBeliefPropagation & AnytimeProbQuery
```

Create a One Time belief propagation computer using the given dependent universes in the current default universe.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryBeliefPropagation & AnytimeProbabilisticBeliefPropagation & AnytimeProbQuery ``.

Source contract/attributes: Create a One Time belief propagation computer using the given dependent universes in the current default universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.apply(dependentUniverses, targets*)(using universe)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation$.html#apply-fd3)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double, myIterations: Int, targets: Element[_]*)(implicit universe: Universe): ProbQueryBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeProbQuery
```

Create a One Time belief propagation computer using the given dependent universes in the current default universe. Use the given dependent algorithm function to determine the algorithm to use to compute probability of evidence in each dependent universe.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``; `` myIterations: Int ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeProbQuery ``.

Source contract/attributes: Create a One Time belief propagation computer using the given dependent universes in the current default universe. Use the given dependent algorithm function to determine the algorithm to use to compute probability of evidence in each dependent universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.apply(dependentUniverses, dependentAlgorithm, myIterations, targets*)(using universe)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation$.html#apply-fffff72c)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double, targets: Element[_]*)(implicit universe: Universe): ProbQueryBeliefPropagation & AnytimeProbabilisticBeliefPropagation & AnytimeProbQuery
```

Create a Anytime belief propagation computer using the given dependent universes in the current default universe. Use the given dependent algorithm function to determine the algorithm to use to compute probability of evidence in each dependent universe.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryBeliefPropagation & AnytimeProbabilisticBeliefPropagation & AnytimeProbQuery ``.

Source contract/attributes: Create a Anytime belief propagation computer using the given dependent universes in the current default universe. Use the given dependent algorithm function to determine the algorithm to use to compute probability of evidence in each dependent universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.apply(dependentUniverses, dependentAlgorithm, targets*)(using universe)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.debugged ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation$.html#debugged-fffff9d3)

```scala
def debugged(myIterations: Int, targets: Element[_]*)(implicit universe: Universe): ProbQueryBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeProbQuery
```

Create a One Time belief propagation computer current default universe, with debug information enabled.

Type parameters: none.

Parameters, list 1: `` myIterations: Int ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeProbQuery ``.

Source contract/attributes: Create a One Time belief propagation computer current default universe, with debug information enabled. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.debugged(myIterations, targets*)(using universe)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.lazyBP ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation$.html#lazyBP-fffff4ac)

```scala
def lazyBP(myIterations: Int, depth: Int, upperBounds: Boolean, targets: Element[_]*)(implicit universe: Universe): ProbQueryBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeProbQuery
```

Lazy version of BP that operates only on bounds.

Type parameters: none.

Parameters, list 1: `` myIterations: Int ``; `` depth: Int ``; `` upperBounds: Boolean ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeProbQuery ``.

Source contract/attributes: Lazy version of BP that operates only on bounds. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.lazyBP(myIterations, depth, upperBounds, targets*)(using universe)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation$.html#probability-c2a)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Use BP to compute the probability that the given element satisfies the given predicate.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Use BP to compute the probability that the given element satisfies the given predicate. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation$.html#probability-f2c)

```scala
def probability[T](target: Element[T], value: T): Double
```

Use BP to compute the probability that the given element has the given value.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Use BP to compute the probability that the given element has the given value. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.belief ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation.html#belief-e6a)

```scala
def belief(source: Node): Factor[T]
```

Returns the product of all messages from a source node's neighbors to itself.

Type parameters: none.

Parameters, list 1: `` source: Node ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns the product of all messages from a source node's neighbors to itself. Attributes

Invocation template:

```scala
receiver.belief(source)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation.html#initialize-94c)

```scala
override def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Definition Classes Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation.html#runStep-94c)

```scala
def runStep(): Unit
```

Runs this belief propagation algorithm for one iteration. An iteration consists of each node of the factor graph sending a message to each of its neighbors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Runs this belief propagation algorithm for one iteration. An iteration consists of each node of the factor graph sending a message to each of its neighbors. Attributes

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.starterElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation.html#starterElements-0)

```scala
def starterElements: List[Element[_]]
```

Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables. Attributes

Invocation template:

```scala
receiver.starterElements
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.BeliefPropagation.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/BeliefPropagation.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[T]]
```

All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[T]] ``.

Source contract/attributes: All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.FactorGraph.contains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/FactorGraph.html#contains-a85)

```scala
def contains(v: Node): Boolean
```

Returns true if the graph contains a node for a (single) variable.

Type parameters: none.

Parameters, list 1: `` v: Node ``.

Returns: `` Boolean ``.

Source contract/attributes: Returns true if the graph contains a node for a (single) variable. Attributes

Invocation template:

```scala
receiver.contains(v)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.FactorGraph.getFactorForNode ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/FactorGraph.html#getFactorForNode-399)

```scala
def getFactorForNode(fn: FactorNode): Factor[T]
```

Gets the factor for a particular factor node.

Type parameters: none.

Parameters, list 1: `` fn: FactorNode ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Gets the factor for a particular factor node. Attributes

Invocation template:

```scala
receiver.getFactorForNode(fn)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.FactorGraph.getLastMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/FactorGraph.html#getLastMessage-425)

```scala
def getLastMessage(from: Node, to: Node): Factor[T]
```

Gets the last message to a node from another.

Type parameters: none.

Parameters, list 1: `` from: Node ``; `` to: Node ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Gets the last message to a node from another. Attributes

Invocation template:

```scala
receiver.getLastMessage(from, to)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.FactorGraph.getMessagesForNode ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/FactorGraph.html#getMessagesForNode-df)

```scala
def getMessagesForNode(node: Node): Iterable[(Node, Factor[T])]
```

Get a list of messages from the node.

Type parameters: none.

Parameters, list 1: `` node: Node ``.

Returns: `` Iterable[(Node, Factor[T])] ``.

Source contract/attributes: Get a list of messages from the node. Attributes

Invocation template:

```scala
receiver.getMessagesForNode(node)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.FactorGraph.getNeighbors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/FactorGraph.html#getNeighbors-df)

```scala
def getNeighbors(source: Node): Iterable[Node]
```

Returns all neighbors of a given node.

Type parameters: none.

Parameters, list 1: `` source: Node ``.

Returns: `` Iterable[Node] ``.

Source contract/attributes: Returns all neighbors of a given node. Attributes

Invocation template:

```scala
receiver.getNeighbors(source)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.FactorGraph.getNeighbors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/FactorGraph.html#getNeighbors-184)

```scala
def getNeighbors(source: Node, excluding: Node): Iterable[Node]
```

Returns all neighbors of a given node excluding the node of the second argument.

Type parameters: none.

Parameters, list 1: `` source: Node ``; `` excluding: Node ``.

Returns: `` Iterable[Node] ``.

Source contract/attributes: Returns all neighbors of a given node excluding the node of the second argument. Attributes

Invocation template:

```scala
receiver.getNeighbors(source, excluding)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.FactorGraph.getNodes ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/FactorGraph.html#getNodes-fffffd3a)

```scala
def getNodes(): Iterable[Node]
```

Returns all nodes in the factor graph.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Iterable[Node] ``.

Source contract/attributes: Returns all nodes in the factor graph. Attributes

Invocation template:

```scala
receiver.getNodes()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.FactorGraph.uniformFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/FactorGraph.html#uniformFactor-fffffc01)

```scala
def uniformFactor(v: List[Variable[_]]): Factor[T]
```

Returns a uniform factor.

Type parameters: none.

Parameters, list 1: `` v: List[Variable[_]] ``.

Returns: `` Factor[T] ``.

Source contract/attributes: Returns a uniform factor. Attributes

Invocation template:

```scala
receiver.uniformFactor(v)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.FactorGraph.update ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/FactorGraph.html#update-fffff3a1)

```scala
def update(from: Node, to: Node, f: Factor[T]): FactorGraph[T]
```

Updates the factor graph with a message from a node to another. Returns a new factor graph, which can be the same as this one.

Type parameters: none.

Parameters, list 1: `` from: Node ``; `` to: Node ``; `` f: Factor[T] ``.

Returns: `` FactorGraph[T] ``.

Source contract/attributes: Updates the factor graph with a message from a node to another. Returns a new factor graph, which can be the same as this one. Attributes

Invocation template:

```scala
receiver.update(from, to, f)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.FactorNode.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/FactorNode.html#toString-665)

```scala
override def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Any

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.FactorNode.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/FactorNode.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.FactorNode.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/FactorNode.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation$.html#apply-fffff059)

```scala
def apply(myIterations: Int)(implicit universe: Universe): MPEBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeMPE
```

Create a most probable explanation computer using One time BP in the current default universe.

Type parameters: none.

Parameters, list 1: `` myIterations: Int ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` MPEBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeMPE ``.

Source contract/attributes: Create a most probable explanation computer using One time BP in the current default universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.apply(myIterations)(using universe)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation$.html#apply-c32)

```scala
def apply()(implicit universe: Universe): MPEBeliefPropagation & AnytimeProbabilisticBeliefPropagation & AnytimeMPE
```

Create a most probable explanation computer using Anytime BP in the current default universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Context parameters, list 2: `` universe: Universe ``.

Returns: `` MPEBeliefPropagation & AnytimeProbabilisticBeliefPropagation & AnytimeMPE ``.

Source contract/attributes: Create a most probable explanation computer using Anytime BP in the current default universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.apply()(using universe)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation$.html#apply-fffffa47)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], myIterations: Int)(implicit universe: Universe): MPEBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeMPE
```

Create a most probable explanation computer using One time BP using the given dependent universes in the current default universe.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` myIterations: Int ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` MPEBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeMPE ``.

Source contract/attributes: Create a most probable explanation computer using One time BP using the given dependent universes in the current default universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.apply(dependentUniverses, myIterations)(using universe)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation$.html#apply-e84)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])])(implicit universe: Universe): MPEBeliefPropagation & AnytimeProbabilisticBeliefPropagation & AnytimeMPE
```

Create a most probable explanation computer using Anytime BP using the given dependent universes in the current default universe.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` MPEBeliefPropagation & AnytimeProbabilisticBeliefPropagation & AnytimeMPE ``.

Source contract/attributes: Create a most probable explanation computer using Anytime BP using the given dependent universes in the current default universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.apply(dependentUniverses)(using universe)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation$.html#apply-659)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double, myIterations: Int)(implicit universe: Universe): MPEBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeMPE
```

Create a most probable explanation computer using One time BP using the given dependent universes in the current default universe. Use the given dependent algorithm function to determine the algorithm to use to compute probability of evidence in each dependent universe.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``; `` myIterations: Int ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` MPEBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeMPE ``.

Source contract/attributes: Create a most probable explanation computer using One time BP using the given dependent universes in the current default universe. Use the given dependent algorithm function to determine the algorithm to use to compute probability of evidence in each dependent universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.apply(dependentUniverses, dependentAlgorithm, myIterations)(using universe)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation$.html#apply-632)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double)(implicit universe: Universe): MPEBeliefPropagation & AnytimeProbabilisticBeliefPropagation & AnytimeMPE
```

Create a most probable explanation computer using Anytime BP using the given dependent universes in the current default universe. Use the given dependent algorithm function to determine the algorithm to use to compute probability of evidence in each dependent universe.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` MPEBeliefPropagation & AnytimeProbabilisticBeliefPropagation & AnytimeMPE ``.

Source contract/attributes: Create a most probable explanation computer using Anytime BP using the given dependent universes in the current default universe. Use the given dependent algorithm function to determine the algorithm to use to compute probability of evidence in each dependent universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.apply(dependentUniverses, dependentAlgorithm)(using universe)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation.html#getFactors-eb2)

```scala
override def getFactors(allElements: List[Element[_]], targetElements: List[Element[_]], upper: Boolean = ...): List[Factor[Double]]
```

Returns the factors needed for BP. Since BP operates on a complete factor graph, factors are created for all elements in the universe.

Type parameters: none.

Parameters, list 1: `` allElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upper: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Returns the factors needed for BP. Since BP operates on a complete factor graph, factors are created for all elements in the universe. Attributes Definition Classes ProbabilisticBeliefPropagation -> FactoredAlgorithm

Invocation template:

```scala
receiver.getFactors(allElements, targetElements, upper)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation.html#initialize-94c)

```scala
override def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Definition Classes BeliefPropagation -> Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns the most likely value for the target element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns the most likely value for the target element. Attributes

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.belief ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation.html#belief-e6a)

```scala
def belief(source: Node): Factor[Double]
```

Returns the product of all messages from a source node's neighbors to itself.

Type parameters: none.

Parameters, list 1: `` source: Node ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Returns the product of all messages from a source node's neighbors to itself. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.belief(source)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.getFinalFactorForElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation.html#getFinalFactorForElement-c2f)

```scala
def getFinalFactorForElement[T](target: Element[T]): Factor[Double]
```

Get the final factor for an element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Get the final factor for an element. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.getFinalFactorForElement[T](target)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.normalize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation.html#normalize-fffffd5e)

```scala
def normalize(factor: Factor[Double]): Factor[Double]
```

Normalize a factor.

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Normalize a factor. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.normalize(factor)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation.html#runStep-94c)

```scala
def runStep(): Unit
```

Runs this belief propagation algorithm for one iteration. An iteration consists of each node of the factor graph sending a message to each of its neighbors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Runs this belief propagation algorithm for one iteration. An iteration consists of each node of the factor graph sending a message to each of its neighbors. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.starterElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation.html#starterElements-0)

```scala
def starterElements: List[Element[_]]
```

Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.starterElements
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.MPEBeliefPropagation.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/MPEBeliefPropagation.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbabilisticBeliefPropagation.iterations ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbabilisticBeliefPropagation.html#iterations-0)

```scala
def iterations: Int
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Invocation template:

```scala
receiver.iterations
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbabilisticBeliefPropagation.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbabilisticBeliefPropagation.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbabilisticBeliefPropagation.belief ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbabilisticBeliefPropagation.html#belief-e6a)

```scala
def belief(source: Node): Factor[Double]
```

Returns the product of all messages from a source node's neighbors to itself.

Type parameters: none.

Parameters, list 1: `` source: Node ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Returns the product of all messages from a source node's neighbors to itself. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.belief(source)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbabilisticBeliefPropagation.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbabilisticBeliefPropagation.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbabilisticBeliefPropagation.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbabilisticBeliefPropagation.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

Returns the factors needed for BP. Since BP operates on a complete factor graph, factors are created for all elements in the universe.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Returns the factors needed for BP. Since BP operates on a complete factor graph, factors are created for all elements in the universe. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbabilisticBeliefPropagation.getFinalFactorForElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbabilisticBeliefPropagation.html#getFinalFactorForElement-c2f)

```scala
def getFinalFactorForElement[T](target: Element[T]): Factor[Double]
```

Get the final factor for an element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Get the final factor for an element. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.getFinalFactorForElement[T](target)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbabilisticBeliefPropagation.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbabilisticBeliefPropagation.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbabilisticBeliefPropagation.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbabilisticBeliefPropagation.html#initialize-94c)

```scala
override def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Definition Classes BeliefPropagation -> Algorithm Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbabilisticBeliefPropagation.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbabilisticBeliefPropagation.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbabilisticBeliefPropagation.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbabilisticBeliefPropagation.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbabilisticBeliefPropagation.normalize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbabilisticBeliefPropagation.html#normalize-fffffd5e)

```scala
def normalize(factor: Factor[Double]): Factor[Double]
```

Normalize a factor.

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Normalize a factor. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.normalize(factor)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbabilisticBeliefPropagation.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbabilisticBeliefPropagation.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbabilisticBeliefPropagation.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbabilisticBeliefPropagation.html#runStep-94c)

```scala
def runStep(): Unit
```

Runs this belief propagation algorithm for one iteration. An iteration consists of each node of the factor graph sending a message to each of its neighbors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Runs this belief propagation algorithm for one iteration. An iteration consists of each node of the factor graph sending a message to each of its neighbors. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbabilisticBeliefPropagation.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbabilisticBeliefPropagation.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbabilisticBeliefPropagation.starterElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbabilisticBeliefPropagation.html#starterElements-0)

```scala
def starterElements: List[Element[_]]
```

Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.starterElements
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbabilisticBeliefPropagation.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbabilisticBeliefPropagation.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.additionalEvidenceAlgorithm ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#additionalEvidenceAlgorithm-98b)

```scala
def additionalEvidenceAlgorithm(evidence: List[NamedEvidence[_]]): ProbEvidenceAlgorithm
```

The algorithm used to compute the probability of additional evidence, as created by probAdditionalEvidence. This algorithm can be different to the one defined in this class. (For example, a one-time algorithm can use an anytime algorithm for additional evidence.)

Type parameters: none.

Parameters, list 1: `` evidence: List[NamedEvidence[_]] ``.

Returns: `` ProbEvidenceAlgorithm ``.

Source contract/attributes: The algorithm used to compute the probability of additional evidence, as created by probAdditionalEvidence. This algorithm can be different to the one defined in this class. (For example, a one-time algorithm can use an anytime algorithm for additional evidence.) Attributes

Invocation template:

```scala
receiver.additionalEvidenceAlgorithm(evidence)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.belief ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#belief-e6a)

```scala
def belief(source: Node): Factor[Double]
```

Returns the product of all messages from a source node's neighbors to itself.

Type parameters: none.

Parameters, list 1: `` source: Node ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Returns the product of all messages from a source node's neighbors to itself. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.belief(source)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Removes the evidence provided in the constructor from the universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Removes the evidence provided in the constructor from the universe. Attributes Definition Classes ProbEvidenceAlgorithm -> Algorithm Inherited from: ProbEvidenceAlgorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.computedResult ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#computedResult-0)

```scala
def computedResult: Double
```

Compute the evidence of the model. Returns the probability of evidence on the model. This assumes that BP has already been run on this algorithm instance.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Compute the evidence of the model. Returns the probability of evidence on the model. This assumes that BP has already been run on this algorithm instance. Attributes Inherited from: ProbEvidenceBeliefPropagation

Invocation template:

```scala
receiver.computedResult
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

Returns the factors needed for BP. Since BP operates on a complete factor graph, factors are created for all elements in the universe.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Returns the factors needed for BP. Since BP operates on a complete factor graph, factors are created for all elements in the universe. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.getFinalFactorForElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#getFinalFactorForElement-c2f)

```scala
def getFinalFactorForElement[T](target: Element[T]): Factor[Double]
```

Get the final factor for an element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Get the final factor for an element. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.getFinalFactorForElement[T](target)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#initialize-94c)

```scala
override def initialize(): Unit
```

Since probability of evidence algorithms introduce additional evidence (namely, their evidence argument), into an existing universe, a mechanism must be provided for introducing the evidence when the algorithm begins and cleaning it up at the end. This is achieved with the initialize method, called when the algorithm starts, and the cleanUp method, called when the algorithm is killed.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Since probability of evidence algorithms introduce additional evidence (namely, their evidence argument), into an existing universe, a mechanism must be provided for introducing the evidence when the algorithm begins and cleaning it up at the end. This is achieved with the initialize method, called when the algorithm starts, and the cleanUp method, called when the algorithm is killed. Attributes Definition Classes ProbEvidenceAlgorithm -> Algorithm Inherited from: ProbEvidenceAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.logProbEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#logProbEvidence-0)

```scala
def logProbEvidence: Double
```

The computed log probability of evidence.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: The computed log probability of evidence. Attributes Inherited from: ProbEvidenceAlgorithm

Invocation template:

```scala
receiver.logProbEvidence
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.normalize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#normalize-fffffd5e)

```scala
def normalize(factor: Factor[Double]): Factor[Double]
```

Normalize a factor.

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Normalize a factor. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.normalize(factor)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.probAdditionalEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#probAdditionalEvidence-98b)

```scala
def probAdditionalEvidence(evidence: List[NamedEvidence[_]]): ProbEvidenceAlgorithm
```

Returns an algorithm to compute the probability of the additional evidence provided.

Type parameters: none.

Parameters, list 1: `` evidence: List[NamedEvidence[_]] ``.

Returns: `` ProbEvidenceAlgorithm ``.

Source contract/attributes: Returns an algorithm to compute the probability of the additional evidence provided. Attributes Inherited from: ProbEvidenceAlgorithm

Invocation template:

```scala
receiver.probAdditionalEvidence(evidence)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.probEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#probEvidence-0)

```scala
def probEvidence: Double
```

The computed probability of evidence.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: The computed probability of evidence. Attributes Inherited from: ProbEvidenceAlgorithm

Invocation template:

```scala
receiver.probEvidence
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.probabilityOfEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#probabilityOfEvidence-999)

```scala
def probabilityOfEvidence(): Double
```

Returns the probability of evidence of the universe on which the algorithm operates. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Double ``.

Source contract/attributes: Returns the probability of evidence of the universe on which the algorithm operates. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: OneTimeProbEvidence

Invocation template:

```scala
receiver.probabilityOfEvidence()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Inherited from: OneTimeProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#runStep-94c)

```scala
def runStep(): Unit
```

Runs this belief propagation algorithm for one iteration. An iteration consists of each node of the factor graph sending a message to each of its neighbors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Runs this belief propagation algorithm for one iteration. An iteration consists of each node of the factor graph sending a message to each of its neighbors. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.starterElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#starterElements-0)

```scala
def starterElements: List[Element[_]]
```

Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.starterElements
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.OneTimeProbEvidenceBeliefPropagation.iterations ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/OneTimeProbEvidenceBeliefPropagation.html#iterations-0)

```scala
def iterations: Int
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Attributes Inherited from: OneTimeProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.iterations
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbabilisticBeliefPropagation.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbabilisticBeliefPropagation.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

Returns the factors needed for BP. Since BP operates on a complete factor graph, factors are created for all elements in the universe.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Returns the factors needed for BP. Since BP operates on a complete factor graph, factors are created for all elements in the universe. Attributes

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbabilisticBeliefPropagation.getFinalFactorForElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbabilisticBeliefPropagation.html#getFinalFactorForElement-c2f)

```scala
def getFinalFactorForElement[T](target: Element[T]): Factor[Double]
```

Get the final factor for an element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Get the final factor for an element. Attributes

Invocation template:

```scala
receiver.getFinalFactorForElement[T](target)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbabilisticBeliefPropagation.normalize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbabilisticBeliefPropagation.html#normalize-fffffd5e)

```scala
def normalize(factor: Factor[Double]): Factor[Double]
```

Normalize a factor.

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Normalize a factor. Attributes

Invocation template:

```scala
receiver.normalize(factor)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbabilisticBeliefPropagation.belief ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbabilisticBeliefPropagation.html#belief-e6a)

```scala
def belief(source: Node): Factor[Double]
```

Returns the product of all messages from a source node's neighbors to itself.

Type parameters: none.

Parameters, list 1: `` source: Node ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Returns the product of all messages from a source node's neighbors to itself. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.belief(source)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbabilisticBeliefPropagation.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbabilisticBeliefPropagation.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbabilisticBeliefPropagation.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbabilisticBeliefPropagation.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbabilisticBeliefPropagation.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbabilisticBeliefPropagation.html#initialize-94c)

```scala
override def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Definition Classes BeliefPropagation -> Algorithm Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbabilisticBeliefPropagation.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbabilisticBeliefPropagation.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbabilisticBeliefPropagation.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbabilisticBeliefPropagation.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbabilisticBeliefPropagation.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbabilisticBeliefPropagation.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbabilisticBeliefPropagation.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbabilisticBeliefPropagation.html#runStep-94c)

```scala
def runStep(): Unit
```

Runs this belief propagation algorithm for one iteration. An iteration consists of each node of the factor graph sending a message to each of its neighbors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Runs this belief propagation algorithm for one iteration. An iteration consists of each node of the factor graph sending a message to each of its neighbors. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbabilisticBeliefPropagation.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbabilisticBeliefPropagation.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbabilisticBeliefPropagation.starterElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbabilisticBeliefPropagation.html#starterElements-0)

```scala
def starterElements: List[Element[_]]
```

Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.starterElements
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbabilisticBeliefPropagation.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbabilisticBeliefPropagation.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation$.html#apply-fffffed4)

```scala
def apply(myIterations: Int, evidence: List[NamedEvidence[_]])(implicit universe: Universe): ProbEvidenceAlgorithm
```

Creates a One Time belief propagation computer in the current default universe.

Type parameters: none.

Parameters, list 1: `` myIterations: Int ``; `` evidence: List[NamedEvidence[_]] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbEvidenceAlgorithm ``.

Source contract/attributes: Creates a One Time belief propagation computer in the current default universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.apply(myIterations, evidence)(using universe)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.computeProbEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation$.html#computeProbEvidence-fffff274)

```scala
def computeProbEvidence(myIterations: Int, evidence: List[NamedEvidence[_]])(implicit universe: Universe): Double
```

Use one-time sampling to compute the probability of the given named evidence. Takes the conditions and constraints in the model as part of the model definition. This method takes care of creating and running the necessary algorithms.

Type parameters: none.

Parameters, list 1: `` myIterations: Int ``; `` evidence: List[NamedEvidence[_]] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Double ``.

Source contract/attributes: Use one-time sampling to compute the probability of the given named evidence. Takes the conditions and constraints in the model as part of the model definition. This method takes care of creating and running the necessary algorithms. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.computeProbEvidence(myIterations, evidence)(using universe)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.computedResult ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation.html#computedResult-0)

```scala
def computedResult: Double
```

Compute the evidence of the model. Returns the probability of evidence on the model. This assumes that BP has already been run on this algorithm instance.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Compute the evidence of the model. Returns the probability of evidence on the model. This assumes that BP has already been run on this algorithm instance. Attributes

Invocation template:

```scala
receiver.computedResult
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.belief ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation.html#belief-e6a)

```scala
def belief(source: Node): Factor[Double]
```

Returns the product of all messages from a source node's neighbors to itself.

Type parameters: none.

Parameters, list 1: `` source: Node ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Returns the product of all messages from a source node's neighbors to itself. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.belief(source)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Removes the evidence provided in the constructor from the universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Removes the evidence provided in the constructor from the universe. Attributes Definition Classes ProbEvidenceAlgorithm -> Algorithm Inherited from: ProbEvidenceAlgorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

Returns the factors needed for BP. Since BP operates on a complete factor graph, factors are created for all elements in the universe.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Returns the factors needed for BP. Since BP operates on a complete factor graph, factors are created for all elements in the universe. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.getFinalFactorForElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation.html#getFinalFactorForElement-c2f)

```scala
def getFinalFactorForElement[T](target: Element[T]): Factor[Double]
```

Get the final factor for an element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Get the final factor for an element. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.getFinalFactorForElement[T](target)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation.html#initialize-94c)

```scala
override def initialize(): Unit
```

Since probability of evidence algorithms introduce additional evidence (namely, their evidence argument), into an existing universe, a mechanism must be provided for introducing the evidence when the algorithm begins and cleaning it up at the end. This is achieved with the initialize method, called when the algorithm starts, and the cleanUp method, called when the algorithm is killed.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Since probability of evidence algorithms introduce additional evidence (namely, their evidence argument), into an existing universe, a mechanism must be provided for introducing the evidence when the algorithm begins and cleaning it up at the end. This is achieved with the initialize method, called when the algorithm starts, and the cleanUp method, called when the algorithm is killed. Attributes Definition Classes ProbEvidenceAlgorithm -> Algorithm Inherited from: ProbEvidenceAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.logProbEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation.html#logProbEvidence-0)

```scala
def logProbEvidence: Double
```

The computed log probability of evidence.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: The computed log probability of evidence. Attributes Inherited from: ProbEvidenceAlgorithm

Invocation template:

```scala
receiver.logProbEvidence
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.normalize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation.html#normalize-fffffd5e)

```scala
def normalize(factor: Factor[Double]): Factor[Double]
```

Normalize a factor.

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Normalize a factor. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.normalize(factor)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.probAdditionalEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation.html#probAdditionalEvidence-98b)

```scala
def probAdditionalEvidence(evidence: List[NamedEvidence[_]]): ProbEvidenceAlgorithm
```

Returns an algorithm to compute the probability of the additional evidence provided.

Type parameters: none.

Parameters, list 1: `` evidence: List[NamedEvidence[_]] ``.

Returns: `` ProbEvidenceAlgorithm ``.

Source contract/attributes: Returns an algorithm to compute the probability of the additional evidence provided. Attributes Inherited from: ProbEvidenceAlgorithm

Invocation template:

```scala
receiver.probAdditionalEvidence(evidence)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.probEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation.html#probEvidence-0)

```scala
def probEvidence: Double
```

The computed probability of evidence.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: The computed probability of evidence. Attributes Inherited from: ProbEvidenceAlgorithm

Invocation template:

```scala
receiver.probEvidence
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation.html#runStep-94c)

```scala
def runStep(): Unit
```

Runs this belief propagation algorithm for one iteration. An iteration consists of each node of the factor graph sending a message to each of its neighbors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Runs this belief propagation algorithm for one iteration. An iteration consists of each node of the factor graph sending a message to each of its neighbors. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.starterElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation.html#starterElements-0)

```scala
def starterElements: List[Element[_]]
```

Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.starterElements
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbEvidenceBeliefPropagation.additionalEvidenceAlgorithm ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbEvidenceBeliefPropagation.html#additionalEvidenceAlgorithm-98b)

```scala
def additionalEvidenceAlgorithm(evidence: List[NamedEvidence[_]]): ProbEvidenceAlgorithm
```

The algorithm used to compute the probability of additional evidence, as created by probAdditionalEvidence. This algorithm can be different to the one defined in this class. (For example, a one-time algorithm can use an anytime algorithm for additional evidence.)

Type parameters: none.

Parameters, list 1: `` evidence: List[NamedEvidence[_]] ``.

Returns: `` ProbEvidenceAlgorithm ``.

Source contract/attributes: The algorithm used to compute the probability of additional evidence, as created by probAdditionalEvidence. This algorithm can be different to the one defined in this class. (For example, a one-time algorithm can use an anytime algorithm for additional evidence.) Attributes Inherited from: ProbEvidenceAlgorithm

Invocation template:

```scala
receiver.additionalEvidenceAlgorithm(evidence)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#computeDistribution-fffffdb4)

```scala
def computeDistribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Attributes

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#computeExpectation-c2a)

```scala
def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Attributes

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.generateGraph ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#generateGraph-94c)

```scala
def generateGraph(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Invocation template:

```scala
receiver.generateGraph()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#initialize-94c)

```scala
override def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Definition Classes BeliefPropagation -> Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.belief ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#belief-e6a)

```scala
def belief(source: Node): Factor[Double]
```

Returns the product of all messages from a source node's neighbors to itself.

Type parameters: none.

Parameters, list 1: `` source: Node ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Returns the product of all messages from a source node's neighbors to itself. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.belief(source)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#distribution-fffffef0)

```scala
def distribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

Returns the factors needed for BP. Since BP operates on a complete factor graph, factors are created for all elements in the universe.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Returns the factors needed for BP. Since BP operates on a complete factor graph, factors are created for all elements in the universe. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.getFinalFactorForElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#getFinalFactorForElement-c2f)

```scala
def getFinalFactorForElement[T](target: Element[T]): Factor[Double]
```

Get the final factor for an element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Get the final factor for an element. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.getFinalFactorForElement[T](target)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.normalize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#normalize-fffffd5e)

```scala
def normalize(factor: Factor[Double]): Factor[Double]
```

Normalize a factor.

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Normalize a factor. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.normalize(factor)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#runStep-94c)

```scala
def runStep(): Unit
```

Runs this belief propagation algorithm for one iteration. An iteration consists of each node of the factor graph sending a message to each of its neighbors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Runs this belief propagation algorithm for one iteration. An iteration consists of each node of the factor graph sending a message to each of its neighbors. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.starterElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#starterElements-0)

```scala
def starterElements: List[Element[_]]
```

Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.starterElements
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.ProbQueryBeliefPropagation.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/ProbQueryBeliefPropagation.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.VariableNode.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/VariableNode.html#toString-665)

```scala
override def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Any

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.VariableNode.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/VariableNode.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.algorithm.factored.beliefpropagation.VariableNode.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/beliefpropagation/VariableNode.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```
