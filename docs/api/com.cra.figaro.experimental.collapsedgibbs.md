<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.experimental.collapsedgibbs

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#apply-49e)

```scala
def apply(mySamples: Int, targets: Element[_]*)(implicit universe: Universe): CollapsedProbQueryGibbs
```

Create a one-time collapsed Gibbs sampler using the given number of samples and target elements. This sampler will use the default collapsing strategy (Heuristic) and default params for that strategy.

Type parameters: none.

Parameters, list 1: `` mySamples: Int ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` CollapsedProbQueryGibbs ``.

Source contract/attributes: Create a one-time collapsed Gibbs sampler using the given number of samples and target elements. This sampler will use the default collapsing strategy (Heuristic) and default params for that strategy. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply(mySamples, targets*)(using universe)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#apply-fd9)

```scala
def apply(strategy: String, mySamples: Int, targets: Element[_]*)(implicit universe: Universe): CollapsedProbQueryGibbs
```

Create a one-time collapsed Gibbs sampler using the given strategy for collapsing, number of samples, and target elements. Uses the default params for the strategy.

Type parameters: none.

Parameters, list 1: `` strategy: String ``; `` mySamples: Int ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` CollapsedProbQueryGibbs ``.

Source contract/attributes: Create a one-time collapsed Gibbs sampler using the given strategy for collapsing, number of samples, and target elements. Uses the default params for the strategy. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply(strategy, mySamples, targets*)(using universe)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#apply-cce)

```scala
def apply(strategy: String, collapseParameters: Seq[Int], mySamples: Int, targets: Element[_]*)(implicit universe: Universe): CollapsedProbQueryGibbs
```

Create a one-time collapsed Gibbs sampler using the specified strategy and parameters, and the given number of samples and target elements.

Type parameters: none.

Parameters, list 1: `` strategy: String ``; `` collapseParameters: Seq[Int] ``; `` mySamples: Int ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` CollapsedProbQueryGibbs ``.

Source contract/attributes: Create a one-time collapsed Gibbs sampler using the specified strategy and parameters, and the given number of samples and target elements. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply(strategy, collapseParameters, mySamples, targets*)(using universe)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#apply-fffff3e1)

```scala
def apply(mySamples: Int, burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator, targets: Element[_]*)(implicit universe: Universe): CollapsedProbQueryGibbs
```

Create a one-time collapsed Gibbs sampler using the given number of samples, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements.

Type parameters: none.

Parameters, list 1: `` mySamples: Int ``; `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` CollapsedProbQueryGibbs ``.

Source contract/attributes: Create a one-time collapsed Gibbs sampler using the given number of samples, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply(mySamples, burnIn, interval, blockToSampler, targets*)(using universe)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#apply-c06)

```scala
def apply(strategy: String, mySamples: Int, burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator, targets: Element[_]*)(implicit universe: Universe): CollapsedProbQueryGibbs
```

Create a one-time collapsed Gibbs sampler using the given strategy, number of samples, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements. Uses the default params for the strategy.

Type parameters: none.

Parameters, list 1: `` strategy: String ``; `` mySamples: Int ``; `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` CollapsedProbQueryGibbs ``.

Source contract/attributes: Create a one-time collapsed Gibbs sampler using the given strategy, number of samples, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements. Uses the default params for the strategy. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply(strategy, mySamples, burnIn, interval, blockToSampler, targets*)(using universe)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#apply-1b1)

```scala
def apply(strategy: String, collapseParameters: Seq[Int], mySamples: Int, burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator, targets: Element[_]*)(implicit universe: Universe): CollapsedProbQueryGibbs
```

Create a one-time collapsed Gibbs sampler using the specified strategy and parameters, number of samples, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements.

Type parameters: none.

Parameters, list 1: `` strategy: String ``; `` collapseParameters: Seq[Int] ``; `` mySamples: Int ``; `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` CollapsedProbQueryGibbs ``.

Source contract/attributes: Create a one-time collapsed Gibbs sampler using the specified strategy and parameters, number of samples, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply(strategy, collapseParameters, mySamples, burnIn, interval, blockToSampler, targets*)(using universe)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#apply-fffffde1)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double, mySamples: Int, burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator, targets: Element[_]*)(implicit universe: Universe): CollapsedProbQueryGibbs
```

Create a one-time collapsed Gibbs sampler using the default strategy with default parameters, given dependent universes and algorithm, the number of samples, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``; `` mySamples: Int ``; `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` CollapsedProbQueryGibbs ``.

Source contract/attributes: Create a one-time collapsed Gibbs sampler using the default strategy with default parameters, given dependent universes and algorithm, the number of samples, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply(dependentUniverses, dependentAlgorithm, mySamples, burnIn, interval, blockToSampler, targets*)(using universe)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#apply-fffffd46)

```scala
def apply(strategy: String, dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double, mySamples: Int, burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator, targets: Element[_]*)(implicit universe: Universe): CollapsedProbQueryGibbs & OneTimeProbQuerySampler & ChainApplyBlockingGibbs
```

Create a one-time collapsed Gibbs sampler using the given strategy, dependent universes and algorithm, the number of samples, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements.

Type parameters: none.

Parameters, list 1: `` strategy: String ``; `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``; `` mySamples: Int ``; `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` CollapsedProbQueryGibbs & OneTimeProbQuerySampler & ChainApplyBlockingGibbs ``.

Source contract/attributes: Create a one-time collapsed Gibbs sampler using the given strategy, dependent universes and algorithm, the number of samples, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply(strategy, dependentUniverses, dependentAlgorithm, mySamples, burnIn, interval, blockToSampler, targets*)(using universe)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#apply-ffffffb1)

```scala
def apply(strategy: String, collapseParameters: Seq[Int], dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double, mySamples: Int, burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator, targets: Element[_]*)(implicit universe: Universe): CollapsedProbQueryGibbs & OneTimeProbQuerySampler & ChainApplyBlockingGibbs
```

Create a one-time collapsed Gibbs sampler using the given strategy and parameters, dependent universes and algorithm, the number of samples, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements.

Type parameters: none.

Parameters, list 1: `` strategy: String ``; `` collapseParameters: Seq[Int] ``; `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``; `` mySamples: Int ``; `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` CollapsedProbQueryGibbs & OneTimeProbQuerySampler & ChainApplyBlockingGibbs ``.

Source contract/attributes: Create a one-time collapsed Gibbs sampler using the given strategy and parameters, dependent universes and algorithm, the number of samples, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply(strategy, collapseParameters, dependentUniverses, dependentAlgorithm, mySamples, burnIn, interval, blockToSampler, targets*)(using universe)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#apply-577)

```scala
def apply(targets: Element[_]*)(implicit universe: Universe): CollapsedProbQueryGibbs
```

Create an anytime collapsed Gibbs sampler using the given target elements.

Type parameters: none.

Parameters, list 1: `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` CollapsedProbQueryGibbs ``.

Source contract/attributes: Create an anytime collapsed Gibbs sampler using the given target elements. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply(targets*)(using universe)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#apply-fffffbdc)

```scala
def apply(strategy: String, targets: Element[_]*)(implicit universe: Universe): CollapsedProbQueryGibbs
```

Create an anytime collapsed Gibbs sampler using the given strategy and target elements.

Type parameters: none.

Parameters, list 1: `` strategy: String ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` CollapsedProbQueryGibbs ``.

Source contract/attributes: Create an anytime collapsed Gibbs sampler using the given strategy and target elements. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply(strategy, targets*)(using universe)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#apply-747)

```scala
def apply(strategy: String, collapseParameters: Seq[Int], targets: Element[_]*)(implicit universe: Universe): CollapsedProbQueryGibbs
```

Create an anytime collapsed Gibbs sampler using the given strategy, parameters, and target elements.

Type parameters: none.

Parameters, list 1: `` strategy: String ``; `` collapseParameters: Seq[Int] ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` CollapsedProbQueryGibbs ``.

Source contract/attributes: Create an anytime collapsed Gibbs sampler using the given strategy, parameters, and target elements. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply(strategy, collapseParameters, targets*)(using universe)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#apply-228)

```scala
def apply(burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator, targets: Element[_]*)(implicit universe: Universe): CollapsedProbQueryGibbs
```

Create an anytime collapsed Gibbs sampler using the default strategy with default parameters, given number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements.

Type parameters: none.

Parameters, list 1: `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` CollapsedProbQueryGibbs ``.

Source contract/attributes: Create an anytime collapsed Gibbs sampler using the default strategy with default parameters, given number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply(burnIn, interval, blockToSampler, targets*)(using universe)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#apply-c63)

```scala
def apply(strategy: String, burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator, targets: Element[_]*)(implicit universe: Universe): CollapsedProbQueryGibbs
```

Create an anytime collapsed Gibbs sampler using the given strategy, number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements.

Type parameters: none.

Parameters, list 1: `` strategy: String ``; `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` CollapsedProbQueryGibbs ``.

Source contract/attributes: Create an anytime collapsed Gibbs sampler using the given strategy, number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply(strategy, burnIn, interval, blockToSampler, targets*)(using universe)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#apply-fffffa58)

```scala
def apply(strategy: String, collapseParameters: Seq[Int], burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator, targets: Element[_]*)(implicit universe: Universe): CollapsedProbQueryGibbs
```

Create an anytime collapsed Gibbs sampler using the given number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements.

Type parameters: none.

Parameters, list 1: `` strategy: String ``; `` collapseParameters: Seq[Int] ``; `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` CollapsedProbQueryGibbs ``.

Source contract/attributes: Create an anytime collapsed Gibbs sampler using the given number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply(strategy, collapseParameters, burnIn, interval, blockToSampler, targets*)(using universe)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#apply-fffff828)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double, burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator, targets: Element[_]*)(implicit universe: Universe): CollapsedProbQueryGibbs
```

Create an anytime collapsed Gibbs sampler using the default strategy with default paramters, given dependent universes and algorithm, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``; `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` CollapsedProbQueryGibbs ``.

Source contract/attributes: Create an anytime collapsed Gibbs sampler using the default strategy with default paramters, given dependent universes and algorithm, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply(dependentUniverses, dependentAlgorithm, burnIn, interval, blockToSampler, targets*)(using universe)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#apply-323)

```scala
def apply(strategy: String, dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double, burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator, targets: Element[_]*)(implicit universe: Universe): CollapsedProbQueryGibbs & AnytimeProbQuerySampler & ChainApplyBlockingGibbs
```

Create an anytime collapsed Gibbs sampler using the given strategy (with default parameters), given dependent universes and algorithm, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements.

Type parameters: none.

Parameters, list 1: `` strategy: String ``; `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``; `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` CollapsedProbQueryGibbs & AnytimeProbQuerySampler & ChainApplyBlockingGibbs ``.

Source contract/attributes: Create an anytime collapsed Gibbs sampler using the given strategy (with default parameters), given dependent universes and algorithm, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply(strategy, dependentUniverses, dependentAlgorithm, burnIn, interval, blockToSampler, targets*)(using universe)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#apply-c58)

```scala
def apply(strategy: String, collapseParameters: Seq[Int], dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double, burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator, targets: Element[_]*)(implicit universe: Universe): CollapsedProbQueryGibbs & AnytimeProbQuerySampler & ChainApplyBlockingGibbs
```

Create an anytime collapsed Gibbs sampler using the given strategy, parameters, dependent universes and algorithm, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements.

Type parameters: none.

Parameters, list 1: `` strategy: String ``; `` collapseParameters: Seq[Int] ``; `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``; `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` CollapsedProbQueryGibbs & AnytimeProbQuerySampler & ChainApplyBlockingGibbs ``.

Source contract/attributes: Create an anytime collapsed Gibbs sampler using the given strategy, parameters, dependent universes and algorithm, the number of samples to burn in, the sampling interval, the BlockSampler generator, and target elements. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.apply(strategy, collapseParameters, dependentUniverses, dependentAlgorithm, burnIn, interval, blockToSampler, targets*)(using universe)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#probability-c2a)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Use Gibbs sampling to compute the probability that the given element satisfies the given predicate.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Use Gibbs sampling to compute the probability that the given element satisfies the given predicate. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.probability[T](target, predicate)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedGibbs$.html#probability-f2c)

```scala
def probability[T](target: Element[T], value: T): Double
```

Use Gibbs sampling to compute the probability that the given element has the given value.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Use Gibbs sampling to compute the probability that the given element has the given value. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.collapsedgibbs.CollapsedGibbs.probability[T](target, value)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.addFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#addFactor-cf8)

```scala
def addFactor[T](factor: Factor[T], map: Map[Variable[_], MultiSet[Factor[T]]]): Unit
```

add a factor to the list

Type parameters: `` T ``.

Parameters, list 1: `` factor: Factor[T] ``; `` map: Map[Variable[_], MultiSet[Factor[T]]] ``.

Returns: `` Unit ``.

Source contract/attributes: add a factor to the list Attributes

Invocation template:

```scala
receiver.addFactor[T](factor, map)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.collapseVariables ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#collapseVariables-94c)

```scala
def collapseVariables(): Unit
```

Perform the collapsing step.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Perform the collapsing step. Attributes

Invocation template:

```scala
receiver.collapseVariables()
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.correctBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#correctBlocks-5c0)

```scala
def correctBlocks(originalBlocks: List[Block]): List[Block]
```

We want to alter the original blocks so that we filter out any variables which have been eliminated. If the original blocks overlapped a lot, then there'll be a lot of redundancy in the filtered blocks, so we take a further step of eliminating any block xs which is fully contained in another block ys.

Type parameters: none.

Parameters, list 1: `` originalBlocks: List[Block] ``.

Returns: `` List[Block] ``.

Source contract/attributes: We want to alter the original blocks so that we filter out any variables which have been eliminated. If the original blocks overlapped a lot, then there'll be a lot of redundancy in the filtered blocks, so we take a further step of eliminating any block xs which is fully contained in another block ys. Attributes

Invocation template:

```scala
receiver.correctBlocks(originalBlocks)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.eliminate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#eliminate-fffffdbe)

```scala
def eliminate(variable: Variable[_], factors: MultiSet[Factor[Double]], map: Map[Variable[_], MultiSet[Factor[Double]]]): Unit
```

Eliminate a variable. This follows the same approach as in VariableElimination.scala. }

Type parameters: none.

Parameters, list 1: `` variable: Variable[_] ``; `` factors: MultiSet[Factor[Double]] ``; `` map: Map[Variable[_], MultiSet[Factor[Double]]] ``.

Returns: `` Unit ``.

Source contract/attributes: Eliminate a variable. This follows the same approach as in VariableElimination.scala. } Attributes

Invocation template:

```scala
receiver.eliminate(variable, factors, map)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.graphHeuristicFunction ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#graphHeuristicFunction-ce6)

```scala
def graphHeuristicFunction[T](var1: Variable[T]): Double
```

The heuristic of a node is how many edges would be added to the primal graph by removing that variable. Because we make a clique over the variable's neighbors. Since we only eliminate variables with alpha or fewer neighbors, this is capped at (alpha C 2). So we return the number of edges as a percentage of (alpha C 2).

Type parameters: `` T ``.

Parameters, list 1: `` var1: Variable[T] ``.

Returns: `` Double ``.

Source contract/attributes: The heuristic of a node is how many edges would be added to the primal graph by removing that variable. Because we make a clique over the variable's neighbors. Since we only eliminate variables with alpha or fewer neighbors, this is capped at (alpha C 2). So we return the number of edges as a percentage of (alpha C 2). Attributes

Invocation template:

```scala
receiver.graphHeuristicFunction[T](var1)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.graphTerm ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#graphTerm-ce6)

```scala
def graphTerm[T](var1: Variable[T]): Double
```

Returns how many edges would be added to the primal graph by removing var1. Note: this is number of edges added, NOT net edges added and removed. Source paper is somewhat ambiguous on whether this should be added or net.

Type parameters: `` T ``.

Parameters, list 1: `` var1: Variable[T] ``.

Returns: `` Double ``.

Source contract/attributes: Returns how many edges would be added to the primal graph by removing var1. Note: this is number of edges added, NOT net edges added and removed. Source paper is somewhat ambiguous on whether this should be added or net. Attributes

Invocation template:

```scala
receiver.graphTerm[T](var1)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.makeResultFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#makeResultFactor-ff9)

```scala
def makeResultFactor(factorsAfterElimination: MultiSet[Factor[Double]]): Factor[Double]
```

Combine all the remaining factors into one 'result factor', as in VE.

Type parameters: none.

Parameters, list 1: `` factorsAfterElimination: MultiSet[Factor[Double]] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Combine all the remaining factors into one 'result factor', as in VE. Attributes

Invocation template:

```scala
receiver.makeResultFactor(factorsAfterElimination)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.marginalize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#marginalize-ac3)

```scala
def marginalize(resultFactor: Factor[Double]): List[Factor[Double]]
```

Marginalize all factors to their component variables.

Type parameters: none.

Parameters, list 1: `` resultFactor: Factor[Double] ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Marginalize all factors to their component variables. Attributes

Invocation template:

```scala
receiver.marginalize(resultFactor)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.marginalizeToTarget ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#marginalizeToTarget-87a)

```scala
def marginalizeToTarget(factor: Factor[Double], target: Variable[_]): Factor[Double]
```

Marginalize a factor to a particular variable.

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``; `` target: Variable[_] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Marginalize a factor to a particular variable. Attributes

Invocation template:

```scala
receiver.marginalizeToTarget(factor, target)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.removeFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#removeFactor-cf8)

```scala
def removeFactor[T](factor: Factor[T], map: Map[Variable[_], MultiSet[Factor[T]]]): Unit
```

remove a factor from the list

Type parameters: `` T ``.

Parameters, list 1: `` factor: Factor[T] ``; `` map: Map[Variable[_], MultiSet[Factor[T]]] ``.

Returns: `` Unit ``.

Source contract/attributes: remove a factor from the list Attributes

Invocation template:

```scala
receiver.removeFactor[T](factor, map)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.sortByHeuristic ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#sortByHeuristic-674)

```scala
def sortByHeuristic(varList: List[Variable[_]], HeuristicMap: Map[Variable[_], Double]): List[Variable[_]]
```

Sort variables by the target heuristic, if they have fewer than alpha neighbors and are not targets.

Type parameters: none.

Parameters, list 1: `` varList: List[Variable[_]] ``; `` HeuristicMap: Map[Variable[_], Double] ``.

Returns: `` List[Variable[_]] ``.

Source contract/attributes: Sort variables by the target heuristic, if they have fewer than alpha neighbors and are not targets. Attributes

Invocation template:

```scala
receiver.sortByHeuristic(varList, HeuristicMap)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes. Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.getSampleCount ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#getSampleCount-0)

```scala
def getSampleCount: Int
```

Number of samples taken

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples taken Attributes Inherited from: BaseUnweightedSampler

Invocation template:

```scala
receiver.getSampleCount
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#sample-ffffff32)

```scala
def sample(): (Boolean, Sample)
```

Produce a single sample.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (Boolean, Sample) ``.

Source contract/attributes: Produce a single sample. Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.sample()
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.sampleAllBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#sampleAllBlocks-94c)

```scala
def sampleAllBlocks(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.sampleAllBlocks()
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.burnIn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#burnIn-0)

```scala
def burnIn: Int
```

Number of samples to throw away initially.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples to throw away initially. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.burnIn
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.createBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#createBlocks-fffffc52)

```scala
def createBlocks(): List[Block]
```

Method to create a blocking scheme given information about the model and factors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` List[Block] ``.

Source contract/attributes: Method to create a blocking scheme given information about the model and factors. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.createBlocks()
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbabilisticGibbs.interval ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbabilisticGibbs.html#interval-0)

```scala
def interval: Int
```

Iterations thrown away between samples.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Iterations thrown away between samples. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.interval
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#initialize-94c)

```scala
override def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Definition Classes ProbQueryGibbs -> Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.addFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#addFactor-cf8)

```scala
def addFactor[T](factor: Factor[T], map: Map[Variable[_], MultiSet[Factor[T]]]): Unit
```

add a factor to the list

Type parameters: `` T ``.

Parameters, list 1: `` factor: Factor[T] ``; `` map: Map[Variable[_], MultiSet[Factor[T]]] ``.

Returns: `` Unit ``.

Source contract/attributes: add a factor to the list Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.addFactor[T](factor, map)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.chainMapper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#chainMapper-ffffff84)

```scala
def chainMapper(chain: Chain[_, _]): Set[Variable[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` chain: Chain[_, _] ``.

Returns: `` Set[Variable[_]] ``.

Source contract/attributes: Attributes Inherited from: ProbQueryGibbs

Invocation template:

```scala
receiver.chainMapper(chain)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.collapseVariables ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#collapseVariables-94c)

```scala
def collapseVariables(): Unit
```

Perform the collapsing step.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Perform the collapsing step. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.collapseVariables()
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#computeDistribution-fffffef0)

```scala
def computeDistribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQuerySampler

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#computeExpectation-fffffa66)

```scala
def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQuerySampler

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.correctBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#correctBlocks-5c0)

```scala
def correctBlocks(originalBlocks: List[Block]): List[Block]
```

We want to alter the original blocks so that we filter out any variables which have been eliminated. If the original blocks overlapped a lot, then there'll be a lot of redundancy in the filtered blocks, so we take a further step of eliminating any block xs which is fully contained in another block ys.

Type parameters: none.

Parameters, list 1: `` originalBlocks: List[Block] ``.

Returns: `` List[Block] ``.

Source contract/attributes: We want to alter the original blocks so that we filter out any variables which have been eliminated. If the original blocks overlapped a lot, then there'll be a lot of redundancy in the filtered blocks, so we take a further step of eliminating any block xs which is fully contained in another block ys. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.correctBlocks(originalBlocks)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#distribution-fffffef0)

```scala
def distribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.eliminate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#eliminate-fffffdbe)

```scala
def eliminate(variable: Variable[_], factors: MultiSet[Factor[Double]], map: Map[Variable[_], MultiSet[Factor[Double]]]): Unit
```

Eliminate a variable. This follows the same approach as in VariableElimination.scala. }

Type parameters: none.

Parameters, list 1: `` variable: Variable[_] ``; `` factors: MultiSet[Factor[Double]] ``; `` map: Map[Variable[_], MultiSet[Factor[Double]]] ``.

Returns: `` Unit ``.

Source contract/attributes: Eliminate a variable. This follows the same approach as in VariableElimination.scala. } Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.eliminate(variable, factors, map)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes. Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.getSampleCount ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#getSampleCount-0)

```scala
def getSampleCount: Int
```

Number of samples taken

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples taken Attributes Inherited from: BaseUnweightedSampler

Invocation template:

```scala
receiver.getSampleCount
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.getTotalWeight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#getTotalWeight-0)

```scala
def getTotalWeight: Double
```

Total weight of samples taken, in log space

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Total weight of samples taken, in log space Attributes Inherited from: UnweightedSampler

Invocation template:

```scala
receiver.getTotalWeight
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.graphHeuristicFunction ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#graphHeuristicFunction-ce6)

```scala
def graphHeuristicFunction[T](var1: Variable[T]): Double
```

The heuristic of a node is how many edges would be added to the primal graph by removing that variable. Because we make a clique over the variable's neighbors. Since we only eliminate variables with alpha or fewer neighbors, this is capped at (alpha C 2). So we return the number of edges as a percentage of (alpha C 2).

Type parameters: `` T ``.

Parameters, list 1: `` var1: Variable[T] ``.

Returns: `` Double ``.

Source contract/attributes: The heuristic of a node is how many edges would be added to the primal graph by removing that variable. Because we make a clique over the variable's neighbors. Since we only eliminate variables with alpha or fewer neighbors, this is capped at (alpha C 2). So we return the number of edges as a percentage of (alpha C 2). Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.graphHeuristicFunction[T](var1)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.graphTerm ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#graphTerm-ce6)

```scala
def graphTerm[T](var1: Variable[T]): Double
```

Returns how many edges would be added to the primal graph by removing var1. Note: this is number of edges added, NOT net edges added and removed. Source paper is somewhat ambiguous on whether this should be added or net.

Type parameters: `` T ``.

Parameters, list 1: `` var1: Variable[T] ``.

Returns: `` Double ``.

Source contract/attributes: Returns how many edges would be added to the primal graph by removing var1. Note: this is number of edges added, NOT net edges added and removed. Source paper is somewhat ambiguous on whether this should be added or net. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.graphTerm[T](var1)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.makeResultFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#makeResultFactor-ff9)

```scala
def makeResultFactor(factorsAfterElimination: MultiSet[Factor[Double]]): Factor[Double]
```

Combine all the remaining factors into one 'result factor', as in VE.

Type parameters: none.

Parameters, list 1: `` factorsAfterElimination: MultiSet[Factor[Double]] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Combine all the remaining factors into one 'result factor', as in VE. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.makeResultFactor(factorsAfterElimination)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.marginalize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#marginalize-ac3)

```scala
def marginalize(resultFactor: Factor[Double]): List[Factor[Double]]
```

Marginalize all factors to their component variables.

Type parameters: none.

Parameters, list 1: `` resultFactor: Factor[Double] ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Marginalize all factors to their component variables. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.marginalize(resultFactor)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.marginalizeToTarget ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#marginalizeToTarget-87a)

```scala
def marginalizeToTarget(factor: Factor[Double], target: Variable[_]): Factor[Double]
```

Marginalize a factor to a particular variable.

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``; `` target: Variable[_] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Marginalize a factor to a particular variable. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.marginalizeToTarget(factor, target)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.removeFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#removeFactor-cf8)

```scala
def removeFactor[T](factor: Factor[T], map: Map[Variable[_], MultiSet[Factor[T]]]): Unit
```

remove a factor from the list

Type parameters: `` T ``.

Parameters, list 1: `` factor: Factor[T] ``; `` map: Map[Variable[_], MultiSet[Factor[T]]] ``.

Returns: `` Unit ``.

Source contract/attributes: remove a factor from the list Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.removeFactor[T](factor, map)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#sample-ffffff32)

```scala
def sample(): (Boolean, Sample)
```

Produce a single sample.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (Boolean, Sample) ``.

Source contract/attributes: Produce a single sample. Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.sample()
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.sampleAllBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#sampleAllBlocks-94c)

```scala
def sampleAllBlocks(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.sampleAllBlocks()
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.sampleFromPosterior ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#sampleFromPosterior-fffffdb4)

```scala
def sampleFromPosterior[T](element: Element[T]): Stream[T]
```

Sample an value from the posterior of this element

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` Stream[T] ``.

Source contract/attributes: Sample an value from the posterior of this element Attributes Inherited from: UnweightedSampler

Invocation template:

```scala
receiver.sampleFromPosterior[T](element)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.sortByHeuristic ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#sortByHeuristic-674)

```scala
def sortByHeuristic(varList: List[Variable[_]], HeuristicMap: Map[Variable[_], Double]): List[Variable[_]]
```

Sort variables by the target heuristic, if they have fewer than alpha neighbors and are not targets.

Type parameters: none.

Parameters, list 1: `` varList: List[Variable[_]] ``; `` HeuristicMap: Map[Variable[_], Double] ``.

Returns: `` List[Variable[_]] ``.

Source contract/attributes: Sort variables by the target heuristic, if they have fewer than alpha neighbors and are not targets. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.sortByHeuristic(varList, HeuristicMap)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.experimental.collapsedgibbs.CollapsedProbQueryGibbs.createBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/CollapsedProbQueryGibbs.html#createBlocks-fffffc52)

```scala
def createBlocks(): List[Block]
```

Method to create a blocking scheme given information about the model and factors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` List[Block] ``.

Source contract/attributes: Method to create a blocking scheme given information about the model and factors. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.createBlocks()
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.graphHeuristicFunction ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#graphHeuristicFunction-ce6)

```scala
override def graphHeuristicFunction[T](var1: Variable[T]): Double
```

Override the heuristic function to use the new measure of distance.

Type parameters: `` T ``.

Parameters, list 1: `` var1: Variable[T] ``.

Returns: `` Double ``.

Source contract/attributes: Override the heuristic function to use the new measure of distance. Attributes Definition Classes CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.graphHeuristicFunction[T](var1)
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#initialize-94c)

```scala
override def initialize(): Unit
```

Unlike other

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Unlike other Attributes Definition Classes Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.updateDistances ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#updateDistances-94c)

```scala
def updateDistances(): Unit
```

Override the distance function.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Override the distance function. Attributes

Invocation template:

```scala
receiver.updateDistances()
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.addFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#addFactor-cf8)

```scala
def addFactor[T](factor: Factor[T], map: Map[Variable[_], MultiSet[Factor[T]]]): Unit
```

add a factor to the list

Type parameters: `` T ``.

Parameters, list 1: `` factor: Factor[T] ``; `` map: Map[Variable[_], MultiSet[Factor[T]]] ``.

Returns: `` Unit ``.

Source contract/attributes: add a factor to the list Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.addFactor[T](factor, map)
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.collapseVariables ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#collapseVariables-94c)

```scala
def collapseVariables(): Unit
```

Perform the collapsing step.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Perform the collapsing step. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.collapseVariables()
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.correctBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#correctBlocks-5c0)

```scala
def correctBlocks(originalBlocks: List[Block]): List[Block]
```

We want to alter the original blocks so that we filter out any variables which have been eliminated. If the original blocks overlapped a lot, then there'll be a lot of redundancy in the filtered blocks, so we take a further step of eliminating any block xs which is fully contained in another block ys.

Type parameters: none.

Parameters, list 1: `` originalBlocks: List[Block] ``.

Returns: `` List[Block] ``.

Source contract/attributes: We want to alter the original blocks so that we filter out any variables which have been eliminated. If the original blocks overlapped a lot, then there'll be a lot of redundancy in the filtered blocks, so we take a further step of eliminating any block xs which is fully contained in another block ys. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.correctBlocks(originalBlocks)
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.eliminate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#eliminate-fffffdbe)

```scala
def eliminate(variable: Variable[_], factors: MultiSet[Factor[Double]], map: Map[Variable[_], MultiSet[Factor[Double]]]): Unit
```

Eliminate a variable. This follows the same approach as in VariableElimination.scala. }

Type parameters: none.

Parameters, list 1: `` variable: Variable[_] ``; `` factors: MultiSet[Factor[Double]] ``; `` map: Map[Variable[_], MultiSet[Factor[Double]]] ``.

Returns: `` Unit ``.

Source contract/attributes: Eliminate a variable. This follows the same approach as in VariableElimination.scala. } Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.eliminate(variable, factors, map)
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes. Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.getSampleCount ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#getSampleCount-0)

```scala
def getSampleCount: Int
```

Number of samples taken

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples taken Attributes Inherited from: BaseUnweightedSampler

Invocation template:

```scala
receiver.getSampleCount
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.graphTerm ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#graphTerm-ce6)

```scala
def graphTerm[T](var1: Variable[T]): Double
```

Returns how many edges would be added to the primal graph by removing var1. Note: this is number of edges added, NOT net edges added and removed. Source paper is somewhat ambiguous on whether this should be added or net.

Type parameters: `` T ``.

Parameters, list 1: `` var1: Variable[T] ``.

Returns: `` Double ``.

Source contract/attributes: Returns how many edges would be added to the primal graph by removing var1. Note: this is number of edges added, NOT net edges added and removed. Source paper is somewhat ambiguous on whether this should be added or net. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.graphTerm[T](var1)
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.makeResultFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#makeResultFactor-ff9)

```scala
def makeResultFactor(factorsAfterElimination: MultiSet[Factor[Double]]): Factor[Double]
```

Combine all the remaining factors into one 'result factor', as in VE.

Type parameters: none.

Parameters, list 1: `` factorsAfterElimination: MultiSet[Factor[Double]] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Combine all the remaining factors into one 'result factor', as in VE. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.makeResultFactor(factorsAfterElimination)
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.marginalize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#marginalize-ac3)

```scala
def marginalize(resultFactor: Factor[Double]): List[Factor[Double]]
```

Marginalize all factors to their component variables.

Type parameters: none.

Parameters, list 1: `` resultFactor: Factor[Double] ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Marginalize all factors to their component variables. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.marginalize(resultFactor)
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.marginalizeToTarget ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#marginalizeToTarget-87a)

```scala
def marginalizeToTarget(factor: Factor[Double], target: Variable[_]): Factor[Double]
```

Marginalize a factor to a particular variable.

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``; `` target: Variable[_] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Marginalize a factor to a particular variable. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.marginalizeToTarget(factor, target)
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.removeFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#removeFactor-cf8)

```scala
def removeFactor[T](factor: Factor[T], map: Map[Variable[_], MultiSet[Factor[T]]]): Unit
```

remove a factor from the list

Type parameters: `` T ``.

Parameters, list 1: `` factor: Factor[T] ``; `` map: Map[Variable[_], MultiSet[Factor[T]]] ``.

Returns: `` Unit ``.

Source contract/attributes: remove a factor from the list Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.removeFactor[T](factor, map)
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#sample-ffffff32)

```scala
def sample(): (Boolean, Sample)
```

Produce a single sample.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (Boolean, Sample) ``.

Source contract/attributes: Produce a single sample. Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.sample()
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.sampleAllBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#sampleAllBlocks-94c)

```scala
def sampleAllBlocks(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.sampleAllBlocks()
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.sortByHeuristic ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#sortByHeuristic-674)

```scala
def sortByHeuristic(varList: List[Variable[_]], HeuristicMap: Map[Variable[_], Double]): List[Variable[_]]
```

Sort variables by the target heuristic, if they have fewer than alpha neighbors and are not targets.

Type parameters: none.

Parameters, list 1: `` varList: List[Variable[_]] ``; `` HeuristicMap: Map[Variable[_], Double] ``.

Returns: `` List[Variable[_]] ``.

Source contract/attributes: Sort variables by the target heuristic, if they have fewer than alpha neighbors and are not targets. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.sortByHeuristic(varList, HeuristicMap)
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.burnIn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#burnIn-0)

```scala
def burnIn: Int
```

Number of samples to throw away initially.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples to throw away initially. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.burnIn
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.createBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#createBlocks-fffffc52)

```scala
def createBlocks(): List[Block]
```

Method to create a blocking scheme given information about the model and factors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` List[Block] ``.

Source contract/attributes: Method to create a blocking scheme given information about the model and factors. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.createBlocks()
```

## `` com.cra.figaro.experimental.collapsedgibbs.DeterministicCollapseStrategy.interval ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/DeterministicCollapseStrategy.html#interval-0)

```scala
def interval: Int
```

Iterations thrown away between samples.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Iterations thrown away between samples. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.interval
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.collapseVariables ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#collapseVariables-94c)

```scala
override def collapseVariables(): Unit
```

Collapse variables

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Collapse variables Attributes Definition Classes CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.collapseVariables()
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.makeResultFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#makeResultFactor-ff9)

```scala
override def makeResultFactor(factorsAfterElimination: MultiSet[Factor[Double]]): Factor[Double]
```

Combine all the remaining factors into one 'result factor', as in VE.

Type parameters: none.

Parameters, list 1: `` factorsAfterElimination: MultiSet[Factor[Double]] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Combine all the remaining factors into one 'result factor', as in VE. Attributes Definition Classes CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.makeResultFactor(factorsAfterElimination)
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.addFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#addFactor-cf8)

```scala
def addFactor[T](factor: Factor[T], map: Map[Variable[_], MultiSet[Factor[T]]]): Unit
```

add a factor to the list

Type parameters: `` T ``.

Parameters, list 1: `` factor: Factor[T] ``; `` map: Map[Variable[_], MultiSet[Factor[T]]] ``.

Returns: `` Unit ``.

Source contract/attributes: add a factor to the list Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.addFactor[T](factor, map)
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.correctBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#correctBlocks-5c0)

```scala
def correctBlocks(originalBlocks: List[Block]): List[Block]
```

We want to alter the original blocks so that we filter out any variables which have been eliminated. If the original blocks overlapped a lot, then there'll be a lot of redundancy in the filtered blocks, so we take a further step of eliminating any block xs which is fully contained in another block ys.

Type parameters: none.

Parameters, list 1: `` originalBlocks: List[Block] ``.

Returns: `` List[Block] ``.

Source contract/attributes: We want to alter the original blocks so that we filter out any variables which have been eliminated. If the original blocks overlapped a lot, then there'll be a lot of redundancy in the filtered blocks, so we take a further step of eliminating any block xs which is fully contained in another block ys. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.correctBlocks(originalBlocks)
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.eliminate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#eliminate-fffffdbe)

```scala
def eliminate(variable: Variable[_], factors: MultiSet[Factor[Double]], map: Map[Variable[_], MultiSet[Factor[Double]]]): Unit
```

Eliminate a variable. This follows the same approach as in VariableElimination.scala. }

Type parameters: none.

Parameters, list 1: `` variable: Variable[_] ``; `` factors: MultiSet[Factor[Double]] ``; `` map: Map[Variable[_], MultiSet[Factor[Double]]] ``.

Returns: `` Unit ``.

Source contract/attributes: Eliminate a variable. This follows the same approach as in VariableElimination.scala. } Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.eliminate(variable, factors, map)
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes. Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.getSampleCount ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#getSampleCount-0)

```scala
def getSampleCount: Int
```

Number of samples taken

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples taken Attributes Inherited from: BaseUnweightedSampler

Invocation template:

```scala
receiver.getSampleCount
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.graphHeuristicFunction ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#graphHeuristicFunction-ce6)

```scala
def graphHeuristicFunction[T](var1: Variable[T]): Double
```

The heuristic of a node is how many edges would be added to the primal graph by removing that variable. Because we make a clique over the variable's neighbors. Since we only eliminate variables with alpha or fewer neighbors, this is capped at (alpha C 2). So we return the number of edges as a percentage of (alpha C 2).

Type parameters: `` T ``.

Parameters, list 1: `` var1: Variable[T] ``.

Returns: `` Double ``.

Source contract/attributes: The heuristic of a node is how many edges would be added to the primal graph by removing that variable. Because we make a clique over the variable's neighbors. Since we only eliminate variables with alpha or fewer neighbors, this is capped at (alpha C 2). So we return the number of edges as a percentage of (alpha C 2). Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.graphHeuristicFunction[T](var1)
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.graphTerm ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#graphTerm-ce6)

```scala
def graphTerm[T](var1: Variable[T]): Double
```

Returns how many edges would be added to the primal graph by removing var1. Note: this is number of edges added, NOT net edges added and removed. Source paper is somewhat ambiguous on whether this should be added or net.

Type parameters: `` T ``.

Parameters, list 1: `` var1: Variable[T] ``.

Returns: `` Double ``.

Source contract/attributes: Returns how many edges would be added to the primal graph by removing var1. Note: this is number of edges added, NOT net edges added and removed. Source paper is somewhat ambiguous on whether this should be added or net. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.graphTerm[T](var1)
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.marginalize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#marginalize-ac3)

```scala
def marginalize(resultFactor: Factor[Double]): List[Factor[Double]]
```

Marginalize all factors to their component variables.

Type parameters: none.

Parameters, list 1: `` resultFactor: Factor[Double] ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Marginalize all factors to their component variables. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.marginalize(resultFactor)
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.marginalizeToTarget ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#marginalizeToTarget-87a)

```scala
def marginalizeToTarget(factor: Factor[Double], target: Variable[_]): Factor[Double]
```

Marginalize a factor to a particular variable.

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``; `` target: Variable[_] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Marginalize a factor to a particular variable. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.marginalizeToTarget(factor, target)
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.removeFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#removeFactor-cf8)

```scala
def removeFactor[T](factor: Factor[T], map: Map[Variable[_], MultiSet[Factor[T]]]): Unit
```

remove a factor from the list

Type parameters: `` T ``.

Parameters, list 1: `` factor: Factor[T] ``; `` map: Map[Variable[_], MultiSet[Factor[T]]] ``.

Returns: `` Unit ``.

Source contract/attributes: remove a factor from the list Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.removeFactor[T](factor, map)
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#sample-ffffff32)

```scala
def sample(): (Boolean, Sample)
```

Produce a single sample.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (Boolean, Sample) ``.

Source contract/attributes: Produce a single sample. Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.sample()
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.sampleAllBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#sampleAllBlocks-94c)

```scala
def sampleAllBlocks(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.sampleAllBlocks()
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.sortByHeuristic ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#sortByHeuristic-674)

```scala
def sortByHeuristic(varList: List[Variable[_]], HeuristicMap: Map[Variable[_], Double]): List[Variable[_]]
```

Sort variables by the target heuristic, if they have fewer than alpha neighbors and are not targets.

Type parameters: none.

Parameters, list 1: `` varList: List[Variable[_]] ``; `` HeuristicMap: Map[Variable[_], Double] ``.

Returns: `` List[Variable[_]] ``.

Source contract/attributes: Sort variables by the target heuristic, if they have fewer than alpha neighbors and are not targets. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.sortByHeuristic(varList, HeuristicMap)
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.burnIn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#burnIn-0)

```scala
def burnIn: Int
```

Number of samples to throw away initially.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples to throw away initially. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.burnIn
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.createBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#createBlocks-fffffc52)

```scala
def createBlocks(): List[Block]
```

Method to create a blocking scheme given information about the model and factors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` List[Block] ``.

Source contract/attributes: Method to create a blocking scheme given information about the model and factors. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.createBlocks()
```

## `` com.cra.figaro.experimental.collapsedgibbs.FactorSizeCollapseStrategy.interval ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/FactorSizeCollapseStrategy.html#interval-0)

```scala
def interval: Int
```

Iterations thrown away between samples.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Iterations thrown away between samples. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.interval
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.collapseVariables ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#collapseVariables-94c)

```scala
override def collapseVariables(): Unit
```

Perform the collapsing step.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Perform the collapsing step. Attributes Definition Classes CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.collapseVariables()
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.distributionDistance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#distributionDistance-fffff483)

```scala
def distributionDistance[T, U](var1: Variable[T], var2: Variable[U]): Double
```

Hellinger distance is defined in the source paper (amongst other places). It's the sum over all values of X1 and X2 of (sqrt(P(X1,X2)) - sqrt(P(X1)*P(X2)))^2

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` var1: Variable[T] ``; `` var2: Variable[U] ``.

Returns: `` Double ``.

Source contract/attributes: Hellinger distance is defined in the source paper (amongst other places). It's the sum over all values of X1 and X2 of (sqrt(P(X1,X2)) - sqrt(P(X1)*P(X2)))^2 Attributes

Invocation template:

```scala
receiver.distributionDistance[T, U](var1, var2)
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.graphHeuristicFunction ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#graphHeuristicFunction-ce6)

```scala
override def graphHeuristicFunction[T](var1: Variable[T]): Double
```

Compute the score of a given variable.

Type parameters: `` T ``.

Parameters, list 1: `` var1: Variable[T] ``.

Returns: `` Double ``.

Source contract/attributes: Compute the score of a given variable. Attributes Definition Classes CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.graphHeuristicFunction[T](var1)
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#initialize-94c)

```scala
override def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Definition Classes Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.resetMarginals ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#resetMarginals-94c)

```scala
def resetMarginals(): Unit
```

Reset all the marginal and p.m. maps to empty maps.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Reset all the marginal and p.m. maps to empty maps. Attributes

Invocation template:

```scala
receiver.resetMarginals()
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.sampleAllBlocksWithTracking ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#sampleAllBlocksWithTracking-94c)

```scala
def sampleAllBlocksWithTracking(): Unit
```

Sample all blocks, then store that sample in the marginal and p.m. maps.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Sample all blocks, then store that sample in the marginal and p.m. maps. Attributes

Invocation template:

```scala
receiver.sampleAllBlocksWithTracking()
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.updateDistances ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#updateDistances-94c)

```scala
def updateDistances(): Unit
```

Use the marginal maps to compute Hellinger maps.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Use the marginal maps to compute Hellinger maps. Attributes

Invocation template:

```scala
receiver.updateDistances()
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.addFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#addFactor-cf8)

```scala
def addFactor[T](factor: Factor[T], map: Map[Variable[_], MultiSet[Factor[T]]]): Unit
```

add a factor to the list

Type parameters: `` T ``.

Parameters, list 1: `` factor: Factor[T] ``; `` map: Map[Variable[_], MultiSet[Factor[T]]] ``.

Returns: `` Unit ``.

Source contract/attributes: add a factor to the list Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.addFactor[T](factor, map)
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.correctBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#correctBlocks-5c0)

```scala
def correctBlocks(originalBlocks: List[Block]): List[Block]
```

We want to alter the original blocks so that we filter out any variables which have been eliminated. If the original blocks overlapped a lot, then there'll be a lot of redundancy in the filtered blocks, so we take a further step of eliminating any block xs which is fully contained in another block ys.

Type parameters: none.

Parameters, list 1: `` originalBlocks: List[Block] ``.

Returns: `` List[Block] ``.

Source contract/attributes: We want to alter the original blocks so that we filter out any variables which have been eliminated. If the original blocks overlapped a lot, then there'll be a lot of redundancy in the filtered blocks, so we take a further step of eliminating any block xs which is fully contained in another block ys. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.correctBlocks(originalBlocks)
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.eliminate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#eliminate-fffffdbe)

```scala
def eliminate(variable: Variable[_], factors: MultiSet[Factor[Double]], map: Map[Variable[_], MultiSet[Factor[Double]]]): Unit
```

Eliminate a variable. This follows the same approach as in VariableElimination.scala. }

Type parameters: none.

Parameters, list 1: `` variable: Variable[_] ``; `` factors: MultiSet[Factor[Double]] ``; `` map: Map[Variable[_], MultiSet[Factor[Double]]] ``.

Returns: `` Unit ``.

Source contract/attributes: Eliminate a variable. This follows the same approach as in VariableElimination.scala. } Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.eliminate(variable, factors, map)
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes. Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.getSampleCount ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#getSampleCount-0)

```scala
def getSampleCount: Int
```

Number of samples taken

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples taken Attributes Inherited from: BaseUnweightedSampler

Invocation template:

```scala
receiver.getSampleCount
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.graphTerm ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#graphTerm-ce6)

```scala
def graphTerm[T](var1: Variable[T]): Double
```

Returns how many edges would be added to the primal graph by removing var1. Note: this is number of edges added, NOT net edges added and removed. Source paper is somewhat ambiguous on whether this should be added or net.

Type parameters: `` T ``.

Parameters, list 1: `` var1: Variable[T] ``.

Returns: `` Double ``.

Source contract/attributes: Returns how many edges would be added to the primal graph by removing var1. Note: this is number of edges added, NOT net edges added and removed. Source paper is somewhat ambiguous on whether this should be added or net. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.graphTerm[T](var1)
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.makeResultFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#makeResultFactor-ff9)

```scala
def makeResultFactor(factorsAfterElimination: MultiSet[Factor[Double]]): Factor[Double]
```

Combine all the remaining factors into one 'result factor', as in VE.

Type parameters: none.

Parameters, list 1: `` factorsAfterElimination: MultiSet[Factor[Double]] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Combine all the remaining factors into one 'result factor', as in VE. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.makeResultFactor(factorsAfterElimination)
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.marginalize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#marginalize-ac3)

```scala
def marginalize(resultFactor: Factor[Double]): List[Factor[Double]]
```

Marginalize all factors to their component variables.

Type parameters: none.

Parameters, list 1: `` resultFactor: Factor[Double] ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Marginalize all factors to their component variables. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.marginalize(resultFactor)
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.marginalizeToTarget ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#marginalizeToTarget-87a)

```scala
def marginalizeToTarget(factor: Factor[Double], target: Variable[_]): Factor[Double]
```

Marginalize a factor to a particular variable.

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``; `` target: Variable[_] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Marginalize a factor to a particular variable. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.marginalizeToTarget(factor, target)
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.removeFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#removeFactor-cf8)

```scala
def removeFactor[T](factor: Factor[T], map: Map[Variable[_], MultiSet[Factor[T]]]): Unit
```

remove a factor from the list

Type parameters: `` T ``.

Parameters, list 1: `` factor: Factor[T] ``; `` map: Map[Variable[_], MultiSet[Factor[T]]] ``.

Returns: `` Unit ``.

Source contract/attributes: remove a factor from the list Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.removeFactor[T](factor, map)
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#sample-ffffff32)

```scala
def sample(): (Boolean, Sample)
```

Produce a single sample.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (Boolean, Sample) ``.

Source contract/attributes: Produce a single sample. Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.sample()
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.sampleAllBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#sampleAllBlocks-94c)

```scala
def sampleAllBlocks(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.sampleAllBlocks()
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.sortByHeuristic ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#sortByHeuristic-674)

```scala
def sortByHeuristic(varList: List[Variable[_]], HeuristicMap: Map[Variable[_], Double]): List[Variable[_]]
```

Sort variables by the target heuristic, if they have fewer than alpha neighbors and are not targets.

Type parameters: none.

Parameters, list 1: `` varList: List[Variable[_]] ``; `` HeuristicMap: Map[Variable[_], Double] ``.

Returns: `` List[Variable[_]] ``.

Source contract/attributes: Sort variables by the target heuristic, if they have fewer than alpha neighbors and are not targets. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.sortByHeuristic(varList, HeuristicMap)
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.burnIn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#burnIn-0)

```scala
def burnIn: Int
```

Number of samples to throw away initially.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples to throw away initially. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.burnIn
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.createBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#createBlocks-fffffc52)

```scala
def createBlocks(): List[Block]
```

Method to create a blocking scheme given information about the model and factors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` List[Block] ``.

Source contract/attributes: Method to create a blocking scheme given information about the model and factors. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.createBlocks()
```

## `` com.cra.figaro.experimental.collapsedgibbs.HeuristicCollapseStrategy.interval ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/HeuristicCollapseStrategy.html#interval-0)

```scala
def interval: Int
```

Iterations thrown away between samples.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Iterations thrown away between samples. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.interval
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.resetMarginals ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#resetMarginals-94c)

```scala
override def resetMarginals(): Unit
```

We override resetMarginals() to do nothing so that we don't get rid of our saved marginal data every time we re-initialize. If we haven't built the marginal maps yes, build them, else do nothing.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: We override resetMarginals() to do nothing so that we don't get rid of our saved marginal data every time we re-initialize. If we haven't built the marginal maps yes, build them, else do nothing. Attributes Definition Classes HeuristicCollapseStrategy

Invocation template:

```scala
receiver.resetMarginals()
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.sampleAllBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#sampleAllBlocks-94c)

```scala
override def sampleAllBlocks(): Unit
```

Every sampleRecurrence many samples, we alter the marginals and PairwiseMarginal Maps to record the current sample. Every sampleReset many samples, we re-initialize.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Every sampleRecurrence many samples, we alter the marginals and PairwiseMarginal Maps to record the current sample. Every sampleReset many samples, we re-initialize. Attributes Definition Classes ProbabilisticGibbs

Invocation template:

```scala
receiver.sampleAllBlocks()
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.addFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#addFactor-cf8)

```scala
def addFactor[T](factor: Factor[T], map: Map[Variable[_], MultiSet[Factor[T]]]): Unit
```

add a factor to the list

Type parameters: `` T ``.

Parameters, list 1: `` factor: Factor[T] ``; `` map: Map[Variable[_], MultiSet[Factor[T]]] ``.

Returns: `` Unit ``.

Source contract/attributes: add a factor to the list Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.addFactor[T](factor, map)
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.collapseVariables ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#collapseVariables-94c)

```scala
override def collapseVariables(): Unit
```

Perform the collapsing step.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Perform the collapsing step. Attributes Definition Classes HeuristicCollapseStrategy -> CollapsedProbabilisticGibbs Inherited from: HeuristicCollapseStrategy

Invocation template:

```scala
receiver.collapseVariables()
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.correctBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#correctBlocks-5c0)

```scala
def correctBlocks(originalBlocks: List[Block]): List[Block]
```

We want to alter the original blocks so that we filter out any variables which have been eliminated. If the original blocks overlapped a lot, then there'll be a lot of redundancy in the filtered blocks, so we take a further step of eliminating any block xs which is fully contained in another block ys.

Type parameters: none.

Parameters, list 1: `` originalBlocks: List[Block] ``.

Returns: `` List[Block] ``.

Source contract/attributes: We want to alter the original blocks so that we filter out any variables which have been eliminated. If the original blocks overlapped a lot, then there'll be a lot of redundancy in the filtered blocks, so we take a further step of eliminating any block xs which is fully contained in another block ys. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.correctBlocks(originalBlocks)
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.distributionDistance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#distributionDistance-fffff483)

```scala
def distributionDistance[T, U](var1: Variable[T], var2: Variable[U]): Double
```

Hellinger distance is defined in the source paper (amongst other places). It's the sum over all values of X1 and X2 of (sqrt(P(X1,X2)) - sqrt(P(X1)*P(X2)))^2

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` var1: Variable[T] ``; `` var2: Variable[U] ``.

Returns: `` Double ``.

Source contract/attributes: Hellinger distance is defined in the source paper (amongst other places). It's the sum over all values of X1 and X2 of (sqrt(P(X1,X2)) - sqrt(P(X1)*P(X2)))^2 Attributes Inherited from: HeuristicCollapseStrategy

Invocation template:

```scala
receiver.distributionDistance[T, U](var1, var2)
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.eliminate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#eliminate-fffffdbe)

```scala
def eliminate(variable: Variable[_], factors: MultiSet[Factor[Double]], map: Map[Variable[_], MultiSet[Factor[Double]]]): Unit
```

Eliminate a variable. This follows the same approach as in VariableElimination.scala. }

Type parameters: none.

Parameters, list 1: `` variable: Variable[_] ``; `` factors: MultiSet[Factor[Double]] ``; `` map: Map[Variable[_], MultiSet[Factor[Double]]] ``.

Returns: `` Unit ``.

Source contract/attributes: Eliminate a variable. This follows the same approach as in VariableElimination.scala. } Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.eliminate(variable, factors, map)
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes. Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.getSampleCount ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#getSampleCount-0)

```scala
def getSampleCount: Int
```

Number of samples taken

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples taken Attributes Inherited from: BaseUnweightedSampler

Invocation template:

```scala
receiver.getSampleCount
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.graphHeuristicFunction ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#graphHeuristicFunction-ce6)

```scala
override def graphHeuristicFunction[T](var1: Variable[T]): Double
```

Compute the score of a given variable.

Type parameters: `` T ``.

Parameters, list 1: `` var1: Variable[T] ``.

Returns: `` Double ``.

Source contract/attributes: Compute the score of a given variable. Attributes Definition Classes HeuristicCollapseStrategy -> CollapsedProbabilisticGibbs Inherited from: HeuristicCollapseStrategy

Invocation template:

```scala
receiver.graphHeuristicFunction[T](var1)
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.graphTerm ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#graphTerm-ce6)

```scala
def graphTerm[T](var1: Variable[T]): Double
```

Returns how many edges would be added to the primal graph by removing var1. Note: this is number of edges added, NOT net edges added and removed. Source paper is somewhat ambiguous on whether this should be added or net.

Type parameters: `` T ``.

Parameters, list 1: `` var1: Variable[T] ``.

Returns: `` Double ``.

Source contract/attributes: Returns how many edges would be added to the primal graph by removing var1. Note: this is number of edges added, NOT net edges added and removed. Source paper is somewhat ambiguous on whether this should be added or net. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.graphTerm[T](var1)
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#initialize-94c)

```scala
override def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Definition Classes HeuristicCollapseStrategy -> Algorithm Inherited from: HeuristicCollapseStrategy

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.makeResultFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#makeResultFactor-ff9)

```scala
def makeResultFactor(factorsAfterElimination: MultiSet[Factor[Double]]): Factor[Double]
```

Combine all the remaining factors into one 'result factor', as in VE.

Type parameters: none.

Parameters, list 1: `` factorsAfterElimination: MultiSet[Factor[Double]] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Combine all the remaining factors into one 'result factor', as in VE. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.makeResultFactor(factorsAfterElimination)
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.marginalize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#marginalize-ac3)

```scala
def marginalize(resultFactor: Factor[Double]): List[Factor[Double]]
```

Marginalize all factors to their component variables.

Type parameters: none.

Parameters, list 1: `` resultFactor: Factor[Double] ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Marginalize all factors to their component variables. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.marginalize(resultFactor)
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.marginalizeToTarget ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#marginalizeToTarget-87a)

```scala
def marginalizeToTarget(factor: Factor[Double], target: Variable[_]): Factor[Double]
```

Marginalize a factor to a particular variable.

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``; `` target: Variable[_] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Marginalize a factor to a particular variable. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.marginalizeToTarget(factor, target)
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.removeFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#removeFactor-cf8)

```scala
def removeFactor[T](factor: Factor[T], map: Map[Variable[_], MultiSet[Factor[T]]]): Unit
```

remove a factor from the list

Type parameters: `` T ``.

Parameters, list 1: `` factor: Factor[T] ``; `` map: Map[Variable[_], MultiSet[Factor[T]]] ``.

Returns: `` Unit ``.

Source contract/attributes: remove a factor from the list Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.removeFactor[T](factor, map)
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#sample-ffffff32)

```scala
def sample(): (Boolean, Sample)
```

Produce a single sample.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (Boolean, Sample) ``.

Source contract/attributes: Produce a single sample. Attributes Inherited from: ProbabilisticGibbs

Invocation template:

```scala
receiver.sample()
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.sampleAllBlocksWithTracking ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#sampleAllBlocksWithTracking-94c)

```scala
def sampleAllBlocksWithTracking(): Unit
```

Sample all blocks, then store that sample in the marginal and p.m. maps.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Sample all blocks, then store that sample in the marginal and p.m. maps. Attributes Inherited from: HeuristicCollapseStrategy

Invocation template:

```scala
receiver.sampleAllBlocksWithTracking()
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.sortByHeuristic ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#sortByHeuristic-674)

```scala
def sortByHeuristic(varList: List[Variable[_]], HeuristicMap: Map[Variable[_], Double]): List[Variable[_]]
```

Sort variables by the target heuristic, if they have fewer than alpha neighbors and are not targets.

Type parameters: none.

Parameters, list 1: `` varList: List[Variable[_]] ``; `` HeuristicMap: Map[Variable[_], Double] ``.

Returns: `` List[Variable[_]] ``.

Source contract/attributes: Sort variables by the target heuristic, if they have fewer than alpha neighbors and are not targets. Attributes Inherited from: CollapsedProbabilisticGibbs

Invocation template:

```scala
receiver.sortByHeuristic(varList, HeuristicMap)
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.updateDistances ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#updateDistances-94c)

```scala
def updateDistances(): Unit
```

Use the marginal maps to compute Hellinger maps.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Use the marginal maps to compute Hellinger maps. Attributes Inherited from: HeuristicCollapseStrategy

Invocation template:

```scala
receiver.updateDistances()
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.burnIn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#burnIn-0)

```scala
def burnIn: Int
```

Number of samples to throw away initially.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples to throw away initially. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.burnIn
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.createBlocks ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#createBlocks-fffffc52)

```scala
def createBlocks(): List[Block]
```

Method to create a blocking scheme given information about the model and factors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` List[Block] ``.

Source contract/attributes: Method to create a blocking scheme given information about the model and factors. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.createBlocks()
```

## `` com.cra.figaro.experimental.collapsedgibbs.RecurringCollapseStrategy.interval ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/collapsedgibbs/RecurringCollapseStrategy.html#interval-0)

```scala
def interval: Int
```

Iterations thrown away between samples.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Iterations thrown away between samples. Attributes Inherited from: Gibbs

Invocation template:

```scala
receiver.interval
```
