<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.algorithm.learning

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.algorithm.learning.EMTerminationCriteria.maxIterations ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMTerminationCriteria$.html#maxIterations-fffff289)

```scala
def maxIterations(max: Int): () => EMTerminationCriteria
```

Terminate when the maximum number of iterations has been reached

Type parameters: none.

Parameters, list 1: `` max: Int ``.

Returns: `` () => EMTerminationCriteria ``.

Source contract/attributes: Terminate when the maximum number of iterations has been reached Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMTerminationCriteria.maxIterations(max)
```

## `` com.cra.figaro.algorithm.learning.EMTerminationCriteria.sufficientStatisticsMagnitude ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMTerminationCriteria$.html#sufficientStatisticsMagnitude-5d7)

```scala
def sufficientStatisticsMagnitude(tolerance: Double): () => EMTerminationCriteria
```

Terminate when the magnitude of sufficient statistics does not exhibit a change greater than the specified tolerance.

Type parameters: none.

Parameters, list 1: `` tolerance: Double ``.

Returns: `` () => EMTerminationCriteria ``.

Source contract/attributes: Terminate when the magnitude of sufficient statistics does not exhibit a change greater than the specified tolerance. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMTerminationCriteria.sufficientStatisticsMagnitude(tolerance)
```

## `` com.cra.figaro.algorithm.learning.EMTerminationCriteria.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMTerminationCriteria.html#apply-538)

```scala
def apply(s: SufficientStatistics): Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` s: SufficientStatistics ``.

Returns: `` Boolean ``.

Invocation template:

```scala
receiver.apply(s)
```

## `` com.cra.figaro.algorithm.learning.EMWithBP.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithBP$.html#apply-d27)

```scala
def apply(params: ModelParameters)(implicit universe: Universe): GeneralizedEM
```

An expectation maximization algorithm using Belief Propagation sampling for inference.

Type parameters: none.

Parameters, list 1: `` params: ModelParameters ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedEM ``.

Source contract/attributes: An expectation maximization algorithm using Belief Propagation sampling for inference. Value parameters params parameters to target with EM algorithm Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithBP.apply(params)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithBP.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithBP$.html#apply-fffff547)

```scala
def apply(emIterations: Int, bpIterations: Int, p: ModelParameters)(implicit universe: Universe): GeneralizedEM
```

An expectation maximization algorithm using Belief Propagation for inference.

Type parameters: none.

Parameters, list 1: `` emIterations: Int ``; `` bpIterations: Int ``; `` p: ModelParameters ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedEM ``.

Source contract/attributes: An expectation maximization algorithm using Belief Propagation for inference. Value parameters bpIterations number of iterations of the BP algorithm emIterations number of iterations of the EM algorithm params parameters to target with EM algorithm Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithBP.apply(emIterations, bpIterations, p)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithBP.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithBP$.html#apply-fffffc7f)

```scala
def apply(params: Parameter[_]*)(implicit universe: Universe): GeneralizedEM
```

An expectation maximization algorithm using Belief Propagation for inference.

Type parameters: none.

Parameters, list 1: `` params: Parameter[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedEM ``.

Source contract/attributes: An expectation maximization algorithm using Belief Propagation for inference. Value parameters params parameters to target with EM algorithm Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithBP.apply(params*)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithBP.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithBP$.html#apply-c9f)

```scala
def apply(emIterations: Int, bpIterations: Int, params: Parameter[_]*)(implicit universe: Universe): GeneralizedEM
```

An expectation maximization algorithm using Belief Propagation for inference.

Type parameters: none.

Parameters, list 1: `` emIterations: Int ``; `` bpIterations: Int ``; `` params: Parameter[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedEM ``.

Source contract/attributes: An expectation maximization algorithm using Belief Propagation for inference. Value parameters bpIterations number of iterations of the BP algorithm emIterations number of iterations of the EM algorithm params parameters to target with EM algorithm Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithBP.apply(emIterations, bpIterations, params*)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithBP.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithBP$.html#apply-fffff148)

```scala
def apply(terminationCriteria: () => EMTerminationCriteria, bpIterations: Int, params: Parameter[_]*)(implicit universe: Universe): GeneralizedEM
```

An expectation maximization algorithm using Belief Propagation for inference.

Type parameters: none.

Parameters, list 1: `` terminationCriteria: () => EMTerminationCriteria ``; `` bpIterations: Int ``; `` params: Parameter[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedEM ``.

Source contract/attributes: An expectation maximization algorithm using Belief Propagation for inference. Value parameters bpIterations number of iterations of the BP algorithm params parameters to target with EM algorithm terminationCriteria criteria for stopping the EM algorithm Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithBP.apply(terminationCriteria, bpIterations, params*)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithBP.online ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithBP$.html#online-262)

```scala
def online(transition: () => Universe, p: Parameter[_]*)(implicit universe: Universe): GeneralizedOnlineEM
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` transition: () => Universe ``; `` p: Parameter[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedOnlineEM ``.

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithBP.online(transition, p*)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithBP.online ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithBP$.html#online-fffff90a)

```scala
def online(transition: () => Universe, p: ModelParameters)(implicit universe: Universe): GeneralizedOnlineEM
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` transition: () => Universe ``; `` p: ModelParameters ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedOnlineEM ``.

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithBP.online(transition, p)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithImportance.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithImportance$.html#apply-c9f)

```scala
def apply(emIterations: Int, importanceParticles: Int, p: Parameter[_]*)(implicit universe: Universe): GeneralizedEM
```

An expectation maximization algorithm using importance sampling for inference.

Type parameters: none.

Parameters, list 1: `` emIterations: Int ``; `` importanceParticles: Int ``; `` p: Parameter[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedEM ``.

Source contract/attributes: An expectation maximization algorithm using importance sampling for inference. Value parameters emIterations number of iterations of the EM algorithm importanceParticles number of particles of the importance sampling algorithm Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithImportance.apply(emIterations, importanceParticles, p*)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithImportance.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithImportance$.html#apply-fffff148)

```scala
def apply(terminationCriteria: () => EMTerminationCriteria, importanceParticles: Int, p: Parameter[_]*)(implicit universe: Universe): GeneralizedEM
```

An expectation maximization algorithm using importance sampling for inference.

Type parameters: none.

Parameters, list 1: `` terminationCriteria: () => EMTerminationCriteria ``; `` importanceParticles: Int ``; `` p: Parameter[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedEM ``.

Source contract/attributes: An expectation maximization algorithm using importance sampling for inference. Value parameters importanceParticles number of particles of the importance sampling algorithm terminationCriteria criteria for stopping the EM algorithm Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithImportance.apply(terminationCriteria, importanceParticles, p*)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithImportance.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithImportance$.html#apply-d27)

```scala
def apply(params: ModelParameters)(implicit universe: Universe): GeneralizedEM
```

An expectation maximization algorithm using importance sampling for inference.

Type parameters: none.

Parameters, list 1: `` params: ModelParameters ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedEM ``.

Source contract/attributes: An expectation maximization algorithm using importance sampling for inference. Value parameters params parameters to target with EM algorithm Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithImportance.apply(params)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithImportance.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithImportance$.html#apply-fffff547)

```scala
def apply(emIterations: Int, importanceParticles: Int, params: ModelParameters)(implicit universe: Universe): GeneralizedEM
```

An expectation maximization algorithm using importance sampling for inference.

Type parameters: none.

Parameters, list 1: `` emIterations: Int ``; `` importanceParticles: Int ``; `` params: ModelParameters ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedEM ``.

Source contract/attributes: An expectation maximization algorithm using importance sampling for inference. Value parameters emIterations number of iterations of the EM algorithm importanceParticles number of particles of the importance sampling algorithm params parameters to target with EM algorithm Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithImportance.apply(emIterations, importanceParticles, params)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithImportance.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithImportance$.html#apply-1b0)

```scala
def apply(terminationCriteria: () => EMTerminationCriteria, importanceParticles: Int, params: ModelParameters)(implicit universe: Universe): GeneralizedEM
```

An expectation maximization algorithm using importance sampling for inference.

Type parameters: none.

Parameters, list 1: `` terminationCriteria: () => EMTerminationCriteria ``; `` importanceParticles: Int ``; `` params: ModelParameters ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedEM ``.

Source contract/attributes: An expectation maximization algorithm using importance sampling for inference. Value parameters importanceParticles number of particles of the importance sampling algorithm params parameters to target with EM algorithm terminationCriteria criteria for stopping the EM algorithm Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithImportance.apply(terminationCriteria, importanceParticles, params)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithImportance.online ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithImportance$.html#online-262)

```scala
def online(transition: () => Universe, p: Parameter[_]*)(implicit universe: Universe): GeneralizedOnlineEM
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` transition: () => Universe ``; `` p: Parameter[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedOnlineEM ``.

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithImportance.online(transition, p*)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithImportance.online ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithImportance$.html#online-fffff90a)

```scala
def online(transition: () => Universe, p: ModelParameters)(implicit universe: Universe): GeneralizedOnlineEM
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` transition: () => Universe ``; `` p: ModelParameters ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedOnlineEM ``.

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithImportance.online(transition, p)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithMH.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithMH$.html#apply-c9f)

```scala
def apply(emIterations: Int, mhParticles: Int, p: Parameter[_]*)(implicit universe: Universe): GeneralizedEM
```

An expectation maximization algorithm using Metropolis Hastings for inference.

Type parameters: none.

Parameters, list 1: `` emIterations: Int ``; `` mhParticles: Int ``; `` p: Parameter[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedEM ``.

Source contract/attributes: An expectation maximization algorithm using Metropolis Hastings for inference. Value parameters emIterations number of iterations of the EM algorithm mhParticles number of particles of the MH algorithm Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithMH.apply(emIterations, mhParticles, p*)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithMH.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithMH$.html#apply-fffff148)

```scala
def apply(terminationCriteria: () => EMTerminationCriteria, mhParticles: Int, params: Parameter[_]*)(implicit universe: Universe): GeneralizedEM
```

An expectation maximization algorithm using Metropolis Hastings for inference.

Type parameters: none.

Parameters, list 1: `` terminationCriteria: () => EMTerminationCriteria ``; `` mhParticles: Int ``; `` params: Parameter[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedEM ``.

Source contract/attributes: An expectation maximization algorithm using Metropolis Hastings for inference. Value parameters mhParticles number of particles of the MH algorithm params parameters to target in EM algorithm terminationCriteria criteria for stopping the EM algorithm Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithMH.apply(terminationCriteria, mhParticles, params*)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithMH.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithMH$.html#apply-e8b)

```scala
def apply(emIterations: Int, mhParticles: Int, proposalScheme: ProposalScheme, params: Parameter[_]*)(implicit universe: Universe): GeneralizedEM
```

An expectation maximization algorithm using Metropolis Hastings for inference.

Type parameters: none.

Parameters, list 1: `` emIterations: Int ``; `` mhParticles: Int ``; `` proposalScheme: ProposalScheme ``; `` params: Parameter[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedEM ``.

Source contract/attributes: An expectation maximization algorithm using Metropolis Hastings for inference. Value parameters iterations number of iterations of the EM algorithm mhParticles number of particles of the MH algorithm params parameters to target in EM algorithm proposalScheme proposal scheme for MH algorithm Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithMH.apply(emIterations, mhParticles, proposalScheme, params*)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithMH.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithMH$.html#apply-d27)

```scala
def apply(p: ModelParameters)(implicit universe: Universe): GeneralizedEM
```

An expectation maximization algorithm using Metropolis Hastings for inference.

Type parameters: none.

Parameters, list 1: `` p: ModelParameters ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedEM ``.

Source contract/attributes: An expectation maximization algorithm using Metropolis Hastings for inference. Value parameters params parameters to target in EM algorithm Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithMH.apply(p)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithMH.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithMH$.html#apply-433)

```scala
def apply(emIterations: Int, mhParticles: Int, proposalScheme: ProposalScheme, p: ModelParameters)(implicit universe: Universe): GeneralizedEM
```

An expectation maximization algorithm using Metropolis Hastings for inference.

Type parameters: none.

Parameters, list 1: `` emIterations: Int ``; `` mhParticles: Int ``; `` proposalScheme: ProposalScheme ``; `` p: ModelParameters ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedEM ``.

Source contract/attributes: An expectation maximization algorithm using Metropolis Hastings for inference. Value parameters iterations number of iterations of the EM algorithm mhParticles number of particles of the MH algorithm params parameters to target in EM algorithm proposalScheme proposal scheme for MH algorithm Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithMH.apply(emIterations, mhParticles, proposalScheme, p)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithMH.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithMH$.html#apply-a9c)

```scala
def apply(terminationCriteria: () => EMTerminationCriteria, mhParticles: Int, proposalScheme: ProposalScheme, params: ModelParameters)(implicit universe: Universe): GeneralizedEM
```

An expectation maximization algorithm using Metropolis Hastings for inference.

Type parameters: none.

Parameters, list 1: `` terminationCriteria: () => EMTerminationCriteria ``; `` mhParticles: Int ``; `` proposalScheme: ProposalScheme ``; `` params: ModelParameters ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedEM ``.

Source contract/attributes: An expectation maximization algorithm using Metropolis Hastings for inference. Value parameters mhParticles number of particles of the MH algorithm params parameters to target in EM algorithm proposalScheme proposal scheme for MH algorithm terminationCriteria criteria for stopping the EM algorithm Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithMH.apply(terminationCriteria, mhParticles, proposalScheme, params)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithMH.online ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithMH$.html#online-262)

```scala
def online(transition: () => Universe, p: Parameter[_]*)(implicit universe: Universe): GeneralizedOnlineEM
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` transition: () => Universe ``; `` p: Parameter[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedOnlineEM ``.

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithMH.online(transition, p*)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithMH.online ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithMH$.html#online-fffff90a)

```scala
def online(transition: () => Universe, p: ModelParameters)(implicit universe: Universe): GeneralizedOnlineEM
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` transition: () => Universe ``; `` p: ModelParameters ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` GeneralizedOnlineEM ``.

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithMH.online(transition, p)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithVE.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithVE$.html#apply-e07)

```scala
def apply(p: Parameter[_]*)(implicit universe: Universe): ExpectationMaximizationWithFactors
```

An expectation maximization algorithm which will run for the default of 10 iterations.

Type parameters: none.

Parameters, list 1: `` p: Parameter[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ExpectationMaximizationWithFactors ``.

Source contract/attributes: An expectation maximization algorithm which will run for the default of 10 iterations. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithVE.apply(p*)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithVE.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithVE$.html#apply-fffff65f)

```scala
def apply(p: ModelParameters)(implicit universe: Universe): ExpectationMaximizationWithFactors
```

An expectation maximization algorithm which will run for the default of 10 iterations.

Type parameters: none.

Parameters, list 1: `` p: ModelParameters ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ExpectationMaximizationWithFactors ``.

Source contract/attributes: An expectation maximization algorithm which will run for the default of 10 iterations. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithVE.apply(p)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithVE.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithVE$.html#apply-c06)

```scala
def apply(iterations: Int, p: ModelParameters)(implicit universe: Universe): ExpectationMaximizationWithFactors
```

An expectation maximization algorithm which will run for the number of iterations specified.

Type parameters: none.

Parameters, list 1: `` iterations: Int ``; `` p: ModelParameters ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ExpectationMaximizationWithFactors ``.

Source contract/attributes: An expectation maximization algorithm which will run for the number of iterations specified. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithVE.apply(iterations, p)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithVE.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithVE$.html#apply-f6e)

```scala
def apply(iterations: Int, p: Parameter[_]*)(implicit universe: Universe): ExpectationMaximizationWithFactors
```

An expectation maximization algorithm which will run for the number of iterations specified.

Type parameters: none.

Parameters, list 1: `` iterations: Int ``; `` p: Parameter[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ExpectationMaximizationWithFactors ``.

Source contract/attributes: An expectation maximization algorithm which will run for the number of iterations specified. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithVE.apply(iterations, p*)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithVE.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithVE$.html#apply-fffffe97)

```scala
def apply(terminationCriteria: () => EMTerminationCriteria, p: Parameter[_]*)(implicit universe: Universe): ExpectationMaximizationWithFactors
```

An expectation maximization algorithm which will stop according to a user specified termination criteria.

Type parameters: none.

Parameters, list 1: `` terminationCriteria: () => EMTerminationCriteria ``; `` p: Parameter[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ExpectationMaximizationWithFactors ``.

Source contract/attributes: An expectation maximization algorithm which will stop according to a user specified termination criteria. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithVE.apply(terminationCriteria, p*)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithVE.online ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithVE$.html#online-b8a)

```scala
def online(transition: () => Universe, p: Parameter[_]*)(implicit universe: Universe): OnlineExpectationMaximizationWithFactors
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` transition: () => Universe ``; `` p: Parameter[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` OnlineExpectationMaximizationWithFactors ``.

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithVE.online(transition, p*)(using universe)
```

## `` com.cra.figaro.algorithm.learning.EMWithVE.online ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/EMWithVE$.html#online-fffffde2)

```scala
def online(transition: () => Universe, p: ModelParameters)(implicit universe: Universe): OnlineExpectationMaximizationWithFactors
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` transition: () => Universe ``; `` p: ModelParameters ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` OnlineExpectationMaximizationWithFactors ``.

Invocation template:

```scala
com.cra.figaro.algorithm.learning.EMWithVE.online(transition, p)(using universe)
```

## `` com.cra.figaro.algorithm.learning.ExpectationMaximization.iteration ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/ExpectationMaximization.html#iteration-94c)

```scala
def iteration(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Invocation template:

```scala
receiver.iteration()
```

## `` com.cra.figaro.algorithm.learning.ExpectationMaximization.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/ExpectationMaximization.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.learning.ExpectationMaximization.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/ExpectationMaximization.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.learning.ExpectationMaximization.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/ExpectationMaximization.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.learning.ExpectationMaximization.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/ExpectationMaximization.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.learning.ExpectationMaximization.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/ExpectationMaximization.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.learning.ExpectationMaximization.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/ExpectationMaximization.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.learning.ExpectationMaximization.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/ExpectationMaximization.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.learning.ExpectationMaximizationWithFactors.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/ExpectationMaximizationWithFactors.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.learning.ExpectationMaximizationWithFactors.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/ExpectationMaximizationWithFactors.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.learning.ExpectationMaximizationWithFactors.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/ExpectationMaximizationWithFactors.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.learning.ExpectationMaximizationWithFactors.iteration ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/ExpectationMaximizationWithFactors.html#iteration-94c)

```scala
def iteration(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: ExpectationMaximization

Invocation template:

```scala
receiver.iteration()
```

## `` com.cra.figaro.algorithm.learning.ExpectationMaximizationWithFactors.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/ExpectationMaximizationWithFactors.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.learning.ExpectationMaximizationWithFactors.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/ExpectationMaximizationWithFactors.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.learning.ExpectationMaximizationWithFactors.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/ExpectationMaximizationWithFactors.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.learning.ExpectationMaximizationWithFactors.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/ExpectationMaximizationWithFactors.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.learning.GeneralizedEM.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/GeneralizedEM.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.learning.GeneralizedEM.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/GeneralizedEM.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.learning.GeneralizedEM.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/GeneralizedEM.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.learning.GeneralizedEM.iteration ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/GeneralizedEM.html#iteration-94c)

```scala
def iteration(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: ExpectationMaximization

Invocation template:

```scala
receiver.iteration()
```

## `` com.cra.figaro.algorithm.learning.GeneralizedEM.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/GeneralizedEM.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.learning.GeneralizedEM.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/GeneralizedEM.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.learning.GeneralizedEM.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/GeneralizedEM.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.learning.GeneralizedEM.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/GeneralizedEM.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.learning.GeneralizedOnlineEM.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/GeneralizedOnlineEM.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.learning.GeneralizedOnlineEM.doStart ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/GeneralizedOnlineEM.html#doStart-94c)

```scala
override def doStart(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Definition Classes OnlineExpectationMaximization -> ExpectationMaximization -> Algorithm Inherited from: OnlineExpectationMaximization

Invocation template:

```scala
receiver.doStart()
```

## `` com.cra.figaro.algorithm.learning.GeneralizedOnlineEM.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/GeneralizedOnlineEM.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.learning.GeneralizedOnlineEM.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/GeneralizedOnlineEM.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.learning.GeneralizedOnlineEM.iteration ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/GeneralizedOnlineEM.html#iteration-94c)

```scala
def iteration(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: ExpectationMaximization

Invocation template:

```scala
receiver.iteration()
```

## `` com.cra.figaro.algorithm.learning.GeneralizedOnlineEM.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/GeneralizedOnlineEM.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.learning.GeneralizedOnlineEM.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/GeneralizedOnlineEM.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.learning.GeneralizedOnlineEM.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/GeneralizedOnlineEM.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.learning.GeneralizedOnlineEM.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/GeneralizedOnlineEM.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.learning.GeneralizedOnlineEM.update ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/GeneralizedOnlineEM.html#update-fffff9b7)

```scala
def update(evidence: Seq[NamedEvidence[_]] = ...): Unit
```

Observe new evidence and perform one expectation step and one maximization step

Type parameters: none.

Parameters, list 1: `` evidence: Seq[NamedEvidence[_]] = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Observe new evidence and perform one expectation step and one maximization step Attributes Inherited from: OnlineExpectationMaximization

Invocation template:

```scala
receiver.update(evidence)
```

## `` com.cra.figaro.algorithm.learning.MaxIterations.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/MaxIterations.html#apply-538)

```scala
override def apply(s: SufficientStatistics): Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` s: SufficientStatistics ``.

Returns: `` Boolean ``.

Source contract/attributes: Attributes Definition Classes EMTerminationCriteria

Invocation template:

```scala
receiver.apply(s)
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximization.doStart ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximization.html#doStart-94c)

```scala
override def doStart(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Definition Classes ExpectationMaximization -> Algorithm

Invocation template:

```scala
receiver.doStart()
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximization.update ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximization.html#update-fffff9b7)

```scala
def update(evidence: Seq[NamedEvidence[_]] = ...): Unit
```

Observe new evidence and perform one expectation step and one maximization step

Type parameters: none.

Parameters, list 1: `` evidence: Seq[NamedEvidence[_]] = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Observe new evidence and perform one expectation step and one maximization step Attributes

Invocation template:

```scala
receiver.update(evidence)
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximization.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximization.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximization.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximization.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximization.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximization.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximization.iteration ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximization.html#iteration-94c)

```scala
def iteration(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: ExpectationMaximization

Invocation template:

```scala
receiver.iteration()
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximization.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximization.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximization.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximization.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximization.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximization.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximization.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximization.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximizationWithFactors.doExpectationStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximizationWithFactors.html#doExpectationStep-fffff328)

```scala
def doExpectationStep(): Map[Parameter[_], Seq[Double]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[Parameter[_], Seq[Double]] ``.

Invocation template:

```scala
receiver.doExpectationStep()
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximizationWithFactors.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximizationWithFactors.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximizationWithFactors.doStart ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximizationWithFactors.html#doStart-94c)

```scala
override def doStart(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Definition Classes OnlineExpectationMaximization -> ExpectationMaximization -> Algorithm Inherited from: OnlineExpectationMaximization

Invocation template:

```scala
receiver.doStart()
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximizationWithFactors.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximizationWithFactors.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximizationWithFactors.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximizationWithFactors.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximizationWithFactors.iteration ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximizationWithFactors.html#iteration-94c)

```scala
def iteration(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: ExpectationMaximization

Invocation template:

```scala
receiver.iteration()
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximizationWithFactors.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximizationWithFactors.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximizationWithFactors.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximizationWithFactors.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximizationWithFactors.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximizationWithFactors.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximizationWithFactors.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximizationWithFactors.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.learning.OnlineExpectationMaximizationWithFactors.update ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/OnlineExpectationMaximizationWithFactors.html#update-fffff9b7)

```scala
def update(evidence: Seq[NamedEvidence[_]] = ...): Unit
```

Observe new evidence and perform one expectation step and one maximization step

Type parameters: none.

Parameters, list 1: `` evidence: Seq[NamedEvidence[_]] = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Observe new evidence and perform one expectation step and one maximization step Attributes Inherited from: OnlineExpectationMaximization

Invocation template:

```scala
receiver.update(evidence)
```

## `` com.cra.figaro.algorithm.learning.SufficientStatisticsFactor.convertFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/SufficientStatisticsFactor.html#convertFactor-fffff18f)

```scala
def convertFactor[T](factor: Factor[Double]): Factor[(Double, Map[Parameter[_], Seq[Double]])]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` Factor[(Double, Map[Parameter[_], Seq[Double]])] ``.

Invocation template:

```scala
receiver.convertFactor[T](factor)
```

## `` com.cra.figaro.algorithm.learning.SufficientStatisticsFactor.make ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/SufficientStatisticsFactor.html#make-b61)

```scala
def make(elem: Element[_]): List[Factor[(Double, Map[Parameter[_], Seq[Double]])]]
```

Create the probabilistic factors associated with an element. This method is memoized.

Type parameters: none.

Parameters, list 1: `` elem: Element[_] ``.

Returns: `` List[Factor[(Double, Map[Parameter[_], Seq[Double]])]] ``.

Source contract/attributes: Create the probabilistic factors associated with an element. This method is memoized. Attributes

Invocation template:

```scala
receiver.make(elem)
```

## `` com.cra.figaro.algorithm.learning.SufficientStatisticsFactor.makeDependentFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/SufficientStatisticsFactor.html#makeDependentFactor-fffff4f7)

```scala
def makeDependentFactor(cc: ComponentCollection, parentUniverse: Universe, dependentUniverse: Universe, probEvidenceComputer: () => Double): Factor[(Double, Map[Parameter[_], Seq[Double]])]
```

Create the probabilistic factor encoding the probability of evidence in the dependent universe as a function of the values of variables in the parent universe. The third argument is the the function to use for computing probability of evidence in the dependent universe. It is assumed that the definition of this function will already contain the right evidence.

Type parameters: none.

Parameters, list 1: `` cc: ComponentCollection ``; `` parentUniverse: Universe ``; `` dependentUniverse: Universe ``; `` probEvidenceComputer: () => Double ``.

Returns: `` Factor[(Double, Map[Parameter[_], Seq[Double]])] ``.

Source contract/attributes: Create the probabilistic factor encoding the probability of evidence in the dependent universe as a function of the values of variables in the parent universe. The third argument is the the function to use for computing probability of evidence in the dependent universe. It is assumed that the definition of this function will already contain the right evidence. Attributes

Invocation template:

```scala
receiver.makeDependentFactor(cc, parentUniverse, dependentUniverse, probEvidenceComputer)
```

## `` com.cra.figaro.algorithm.learning.SufficientStatisticsFactor.partitionConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/SufficientStatisticsFactor.html#partitionConstraintFactors-fffff084)

```scala
def partitionConstraintFactors(factors: List[Factor[Double]]): (List[Factor[Double]], List[Factor[Double]])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` factors: List[Factor[Double]] ``.

Returns: `` (List[Factor[Double]], List[Factor[Double]]) ``.

Invocation template:

```scala
receiver.partitionConstraintFactors(factors)
```

## `` com.cra.figaro.algorithm.learning.SufficientStatisticsMagnitudes.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/SufficientStatisticsMagnitudes.html#apply-538)

```scala
override def apply(s: SufficientStatistics): Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` s: SufficientStatistics ``.

Returns: `` Boolean ``.

Source contract/attributes: Attributes Definition Classes EMTerminationCriteria

Invocation template:

```scala
receiver.apply(s)
```

## `` com.cra.figaro.algorithm.learning.SufficientStatisticsMagnitudes.difference ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/learning/SufficientStatisticsMagnitudes.html#difference-faf)

```scala
def difference(x: Seq[Double], y: Seq[Double]): Double
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x: Seq[Double] ``; `` y: Seq[Double] ``.

Returns: `` Double ``.

Invocation template:

```scala
receiver.difference(x, y)
```
