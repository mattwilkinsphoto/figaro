<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.algorithm.decision.index

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.algorithm.decision.index.Distance.boolean2Dist ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/Distance$.html#boolean2Dist-fffff3f9)

```scala
implicit def boolean2Dist(x: Boolean): BooleanDistance
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x: Boolean ``.

Returns: `` BooleanDistance ``.

Invocation template:

```scala
com.cra.figaro.algorithm.decision.index.Distance.boolean2Dist(x)
```

## `` com.cra.figaro.algorithm.decision.index.Distance.boolean2DistTuple ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/Distance$.html#boolean2DistTuple-b0b)

```scala
implicit def boolean2DistTuple(x: (Boolean, Boolean)): TupleDistance2[Boolean, Boolean]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x: (Boolean, Boolean) ``.

Returns: `` TupleDistance2[Boolean, Boolean] ``.

Invocation template:

```scala
com.cra.figaro.algorithm.decision.index.Distance.boolean2DistTuple(x)
```

## `` com.cra.figaro.algorithm.decision.index.Distance.double2Dist ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/Distance$.html#double2Dist-fffff123)

```scala
implicit def double2Dist(x: Double): DoubleDistance
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x: Double ``.

Returns: `` DoubleDistance ``.

Invocation template:

```scala
com.cra.figaro.algorithm.decision.index.Distance.double2Dist(x)
```

## `` com.cra.figaro.algorithm.decision.index.Distance.double2DistTuple ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/Distance$.html#double2DistTuple-b0b)

```scala
implicit def double2DistTuple(x: (Double, Double)): TupleDistance2[Double, Double]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x: (Double, Double) ``.

Returns: `` TupleDistance2[Double, Double] ``.

Invocation template:

```scala
com.cra.figaro.algorithm.decision.index.Distance.double2DistTuple(x)
```

## `` com.cra.figaro.algorithm.decision.index.Distance.int2Dist ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/Distance$.html#int2Dist-f99)

```scala
implicit def int2Dist(x: Int): IntDistance
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x: Int ``.

Returns: `` IntDistance ``.

Invocation template:

```scala
com.cra.figaro.algorithm.decision.index.Distance.int2Dist(x)
```

## `` com.cra.figaro.algorithm.decision.index.Distance.int2DistTuple ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/Distance$.html#int2DistTuple-b0b)

```scala
implicit def int2DistTuple(x: (Int, Int)): TupleDistance2[Int, Int]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x: (Int, Int) ``.

Returns: `` TupleDistance2[Int, Int] ``.

Invocation template:

```scala
com.cra.figaro.algorithm.decision.index.Distance.int2DistTuple(x)
```

## `` com.cra.figaro.algorithm.decision.index.Distance.distance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/Distance.html#distance-fffffeac)

```scala
def distance(that: T): Double
```

Return the distance from this value to another.

Type parameters: none.

Parameters, list 1: `` that: T ``.

Returns: `` Double ``.

Source contract/attributes: Return the distance from this value to another. Attributes

Invocation template:

```scala
receiver.distance(that)
```

## `` com.cra.figaro.algorithm.decision.index.FlatIndex.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/FlatIndex$.html#apply-fffff85e)

```scala
def apply[T : DistanceConversion, U](strat: Map[(T, U), DecisionSample]): FlatIndex[T, U]
```

Create a flat index from a map of (parent, decision) -> Decision values.

Type parameters: `` T : DistanceConversion ``; `` U ``.

Parameters, list 1: `` strat: Map[(T, U), DecisionSample] ``.

Returns: `` FlatIndex[T, U] ``.

Source contract/attributes: Create a flat index from a map of (parent, decision) -> Decision values. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.index.FlatIndex.apply[T, U](strat)
```

## `` com.cra.figaro.algorithm.decision.index.FlatIndex.getNN ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/FlatIndex.html#getNN-2de)

```scala
def getNN(parent: T, num: Int): List[(Double, U, DecisionSample)]
```

Get the specified number of nearest neighbors of the parent value. Returns a tuple list of (distance to parent, decision value, weighted utility).

Type parameters: none.

Parameters, list 1: `` parent: T ``; `` num: Int ``.

Returns: `` List[(Double, U, DecisionSample)] ``.

Source contract/attributes: Get the specified number of nearest neighbors of the parent value. Returns a tuple list of (distance to parent, decision value, weighted utility). Attributes

Invocation template:

```scala
receiver.getNN(parent, num)
```

## `` com.cra.figaro.algorithm.decision.index.FlatIndex.size ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/FlatIndex.html#size-0)

```scala
def size: Int
```

Get the size of the index.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Get the size of the index. Attributes Inherited from: Index

Invocation template:

```scala
receiver.size
```

## `` com.cra.figaro.algorithm.decision.index.Index.getNN ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/Index.html#getNN-2de)

```scala
def getNN(parent: T, num: Int): List[(Double, U, DecisionSample)]
```

Get the specified number of nearest neighbors of the parent value. Returns a tuple list of (distance to parent, decision value, weighted utility).

Type parameters: none.

Parameters, list 1: `` parent: T ``; `` num: Int ``.

Returns: `` List[(Double, U, DecisionSample)] ``.

Source contract/attributes: Get the specified number of nearest neighbors of the parent value. Returns a tuple list of (distance to parent, decision value, weighted utility). Attributes

Invocation template:

```scala
receiver.getNN(parent, num)
```

## `` com.cra.figaro.algorithm.decision.index.Index.size ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/Index.html#size-0)

```scala
def size: Int
```

Get the size of the index.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Get the size of the index. Attributes

Invocation template:

```scala
receiver.size
```

## `` com.cra.figaro.algorithm.decision.index.INode.oDist ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/INode.html#oDist-f07)

```scala
def oDist(o: T): Iterable[(Double, Set[U])]
```

Defines the distance between a query and the leaf nodes of an index.

Type parameters: none.

Parameters, list 1: `` o: T ``.

Returns: `` Iterable[(Double, Set[U])] ``.

Source contract/attributes: Defines the distance between a query and the leaf nodes of an index. Attributes

Invocation template:

```scala
receiver.oDist(o)
```

## `` com.cra.figaro.algorithm.decision.index.INode.addChild ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/INode.html#addChild-fffff632)

```scala
def addChild(n: Node[T, U]): Unit
```

Adds a child node to this node.

Type parameters: none.

Parameters, list 1: `` n: Node[T, U] ``.

Returns: `` Unit ``.

Source contract/attributes: Adds a child node to this node. Attributes Inherited from: Node

Invocation template:

```scala
receiver.addChild(n)
```

## `` com.cra.figaro.algorithm.decision.index.INode.iDist ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/INode.html#iDist-f07)

```scala
def iDist(v: T): Iterable[(Double, Node[T, U])]
```

Defines the distance between a query and the internal nodes of an index.

Type parameters: none.

Parameters, list 1: `` v: T ``.

Returns: `` Iterable[(Double, Node[T, U])] ``.

Source contract/attributes: Defines the distance between a query and the internal nodes of an index. Attributes Inherited from: Node

Invocation template:

```scala
receiver.iDist(v)
```

## `` com.cra.figaro.algorithm.decision.index.L2Norm.reduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/L2Norm.html#reduce-fffffb44)

```scala
def reduce(v: Double*): Double
```

Reduce a variable list of doubles using the L2 norm.

Type parameters: none.

Parameters, list 1: `` v: Double* ``.

Returns: `` Double ``.

Source contract/attributes: Reduce a variable list of doubles using the L2 norm. Attributes

Invocation template:

```scala
receiver.reduce(v*)
```

## `` com.cra.figaro.algorithm.decision.index.LNode.addObject ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/LNode.html#addObject-8ca)

```scala
def addObject(k: Distance[T], v: U): ObjectMapType
```

Adds an object of Distance[T] to the node that contains a value v of type U.

Type parameters: none.

Parameters, list 1: `` k: Distance[T] ``; `` v: U ``.

Returns: `` ObjectMapType ``.

Source contract/attributes: Adds an object of Distance[T] to the node that contains a value v of type U. Attributes

Invocation template:

```scala
receiver.addObject(k, v)
```

## `` com.cra.figaro.algorithm.decision.index.LNode.iDist ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/LNode.html#iDist-f07)

```scala
def iDist(o: T): Iterable[(Double, Node[T, U])]
```

Defines the distance between a query and the internal nodes of an index.

Type parameters: none.

Parameters, list 1: `` o: T ``.

Returns: `` Iterable[(Double, Node[T, U])] ``.

Source contract/attributes: Defines the distance between a query and the internal nodes of an index. Attributes

Invocation template:

```scala
receiver.iDist(o)
```

## `` com.cra.figaro.algorithm.decision.index.LNode.oDist ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/LNode.html#oDist-f07)

```scala
def oDist(o: T): Iterable[(Double, Set[U])]
```

Compute the distance between each object stored in the leaf node and a query value of type Distance[T]. Will return the distances in sorted order.

Type parameters: none.

Parameters, list 1: `` o: T ``.

Returns: `` Iterable[(Double, Set[U])] ``.

Source contract/attributes: Compute the distance between each object stored in the leaf node and a query value of type Distance[T]. Will return the distances in sorted order. Attributes

Invocation template:

```scala
receiver.oDist(o)
```

## `` com.cra.figaro.algorithm.decision.index.LNode.addChild ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/LNode.html#addChild-fffff632)

```scala
def addChild(n: Node[T, U]): Unit
```

Adds a child node to this node.

Type parameters: none.

Parameters, list 1: `` n: Node[T, U] ``.

Returns: `` Unit ``.

Source contract/attributes: Adds a child node to this node. Attributes Inherited from: Node

Invocation template:

```scala
receiver.addChild(n)
```

## `` com.cra.figaro.algorithm.decision.index.Node.iDist ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/Node.html#iDist-f07)

```scala
def iDist(v: T): Iterable[(Double, Node[T, U])]
```

Defines the distance between a query and the internal nodes of an index.

Type parameters: none.

Parameters, list 1: `` v: T ``.

Returns: `` Iterable[(Double, Node[T, U])] ``.

Source contract/attributes: Defines the distance between a query and the internal nodes of an index. Attributes

Invocation template:

```scala
receiver.iDist(v)
```

## `` com.cra.figaro.algorithm.decision.index.Node.oDist ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/Node.html#oDist-f07)

```scala
def oDist(v: T): Iterable[(Double, Set[U])]
```

Defines the distance between a query and the leaf nodes of an index.

Type parameters: none.

Parameters, list 1: `` v: T ``.

Returns: `` Iterable[(Double, Set[U])] ``.

Source contract/attributes: Defines the distance between a query and the leaf nodes of an index. Attributes

Invocation template:

```scala
receiver.oDist(v)
```

## `` com.cra.figaro.algorithm.decision.index.Node.addChild ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/Node.html#addChild-fffff632)

```scala
def addChild(n: Node[T, U]): Unit
```

Adds a child node to this node.

Type parameters: none.

Parameters, list 1: `` n: Node[T, U] ``.

Returns: `` Unit ``.

Source contract/attributes: Adds a child node to this node. Attributes

Invocation template:

```scala
receiver.addChild(n)
```

## `` com.cra.figaro.algorithm.decision.index.TupleDistance.reduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/TupleDistance.html#reduce-fffffb44)

```scala
def reduce(v: Double*): Double
```

Reduce the set of doubles corresponding to the distance between each pair of components to a single double representing the distance between the tuples.

Type parameters: none.

Parameters, list 1: `` v: Double* ``.

Returns: `` Double ``.

Source contract/attributes: Reduce the set of doubles corresponding to the distance between each pair of components to a single double representing the distance between the tuples. Attributes

Invocation template:

```scala
receiver.reduce(v*)
```

## `` com.cra.figaro.algorithm.decision.index.TupleDistance2.distance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/TupleDistance2.html#distance-dcb)

```scala
def distance(that: (T1, T2)): Double
```

Return the distance from this value to another.

Type parameters: none.

Parameters, list 1: `` that: (T1, T2) ``.

Returns: `` Double ``.

Source contract/attributes: Return the distance from this value to another. Attributes

Invocation template:

```scala
receiver.distance(that)
```

## `` com.cra.figaro.algorithm.decision.index.TupleDistance2.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/TupleDistance2.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.algorithm.decision.index.TupleDistance2.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/TupleDistance2.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.algorithm.decision.index.TupleDistance2.reduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/TupleDistance2.html#reduce-fffffb44)

```scala
def reduce(v: Double*): Double
```

Reduce a variable list of doubles using the L2 norm.

Type parameters: none.

Parameters, list 1: `` v: Double* ``.

Returns: `` Double ``.

Source contract/attributes: Reduce a variable list of doubles using the L2 norm. Attributes Inherited from: L2Norm

Invocation template:

```scala
receiver.reduce(v*)
```

## `` com.cra.figaro.algorithm.decision.index.VPIndex.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/VPIndex$.html#apply-fffff090)

```scala
def apply[T : DistanceConversion, U](strat: Map[(T, U), DecisionSample], capacity: Int): VPIndex[T, U]
```

Create a VP index from a map of (parent, decision) -> DecisionSamples with the given leaf node capacity

Type parameters: `` T : DistanceConversion ``; `` U ``.

Parameters, list 1: `` strat: Map[(T, U), DecisionSample] ``; `` capacity: Int ``.

Returns: `` VPIndex[T, U] ``.

Source contract/attributes: Create a VP index from a map of (parent, decision) -> DecisionSamples with the given leaf node capacity Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.index.VPIndex.apply[T, U](strat, capacity)
```

## `` com.cra.figaro.algorithm.decision.index.VPIndex.getNN ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/VPIndex.html#getNN-2de)

```scala
def getNN(parent: T, num: Int): List[(Double, U, DecisionSample)]
```

Return the k nearest neighbor samples from the parent value

Type parameters: none.

Parameters, list 1: `` parent: T ``; `` num: Int ``.

Returns: `` List[(Double, U, DecisionSample)] ``.

Source contract/attributes: Return the k nearest neighbor samples from the parent value Attributes

Invocation template:

```scala
receiver.getNN(parent, num)
```

## `` com.cra.figaro.algorithm.decision.index.VPIndex.getNNDebug ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/VPIndex.html#getNNDebug-fffffcc6)

```scala
override def getNNDebug(parent: T, num: Int): Iterable[(Double, Set[(U, DecisionSample)])]
```

The debug version of the getNN returns the raw data from the index node.

Type parameters: none.

Parameters, list 1: `` parent: T ``; `` num: Int ``.

Returns: `` Iterable[(Double, Set[(U, DecisionSample)])] ``.

Source contract/attributes: The debug version of the getNN returns the raw data from the index node. Attributes Definition Classes Index

Invocation template:

```scala
receiver.getNNDebug(parent, num)
```

## `` com.cra.figaro.algorithm.decision.index.VPIndex.size ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/index/VPIndex.html#size-0)

```scala
def size: Int
```

Get the size of the index.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Get the size of the index. Attributes Inherited from: Index

Invocation template:

```scala
receiver.size
```
