<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.experimental.marginalmap

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.experimental.marginalmap.AnytimeMarginalMAP.ComputeMostLikelyValue.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeMarginalMAP$ComputeMostLikelyValue.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeMarginalMAP.ComputeMostLikelyValue.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeMarginalMAP$ComputeMostLikelyValue.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeMarginalMAP.MostLikelyValue.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeMarginalMAP$MostLikelyValue.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeMarginalMAP.MostLikelyValue.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeMarginalMAP$MostLikelyValue.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeMarginalMAP.handle ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeMarginalMAP.html#handle-fffff940)

```scala
def handle(service: Service): Response
```

A handler of services provided by the algorithm.

Type parameters: none.

Parameters, list 1: `` service: Service ``.

Returns: `` Response ``.

Source contract/attributes: A handler of services provided by the algorithm. Attributes

Invocation template:

```scala
receiver.handle(service)
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeMarginalMAP.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeMarginalMAP.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeMarginalMAP.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeMarginalMAP.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeMarginalMAP.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeMarginalMAP.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeMarginalMAP.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeMarginalMAP.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeMarginalMAP.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeMarginalMAP.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns an estimate of the max a posteriori value of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns an estimate of the max a posteriori value of the target. Attributes Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of MAP elements. Inherited from: MarginalMAPAlgorithm

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeMarginalMAP.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeMarginalMAP.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeMarginalMAP.shutdown ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeMarginalMAP.html#shutdown-0)

```scala
def shutdown: Unit
```

Release all resources from this anytime algorithm.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Unit ``.

Source contract/attributes: Release all resources from this anytime algorithm. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.shutdown
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeMarginalMAP.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeMarginalMAP.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeMarginalMAP.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeMarginalMAP.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeMarginalMAP.stopUpdate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeMarginalMAP.html#stopUpdate-94c)

```scala
def stopUpdate(): Unit
```

Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.stopUpdate()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeMarginalMAP.computeMostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeMarginalMAP.html#computeMostLikelyValue-fffffa13)

```scala
def computeMostLikelyValue[T](target: Element[T]): T
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Attributes Inherited from: MarginalMAPAlgorithm

Invocation template:

```scala
receiver.computeMostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeMarginalMAP.mapElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeMarginalMAP.html#mapElements-0)

```scala
def mapElements: Seq[Element[_]]
```

Elements for which to perform MAP queries. This algorithm marginalizes over elements not in this list.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Seq[Element[_]] ``.

Source contract/attributes: Elements for which to perform MAP queries. This algorithm marginalizes over elements not in this list. Attributes Inherited from: MarginalMAPAlgorithm

Invocation template:

```scala
receiver.mapElements
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeMarginalMAP.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeMarginalMAP.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single step of the algorithm. The algorithm must be able to provide answers after each step.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single step of the algorithm. The algorithm must be able to provide answers after each step. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeMarginalMAP.universe ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeMarginalMAP.html#universe-0)

```scala
def universe: Universe
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Universe ``.

Source contract/attributes: Attributes Inherited from: MarginalMAPAlgorithm

Invocation template:

```scala
receiver.universe
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeProbEvidenceMarginalMAP.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeProbEvidenceMarginalMAP.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the algorithm.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the algorithm. Attributes Definition Classes AnytimeSampler -> Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeProbEvidenceMarginalMAP.acceptRejectRatio ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeProbEvidenceMarginalMAP.html#acceptRejectRatio-0)

```scala
def acceptRejectRatio: Double
```

Get the acceptance ratio for the sampler.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Get the acceptance ratio for the sampler. Attributes Inherited from: MetropolisHastings

Invocation template:

```scala
receiver.acceptRejectRatio
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeProbEvidenceMarginalMAP.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeProbEvidenceMarginalMAP.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Clean up the sampler, freeing memory.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clean up the sampler, freeing memory. Attributes Definition Classes ProbEvidenceMarginalMAP -> MetropolisHastings -> Algorithm Inherited from: ProbEvidenceMarginalMAP

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeProbEvidenceMarginalMAP.computeMostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeProbEvidenceMarginalMAP.html#computeMostLikelyValue-fffffa13)

```scala
override def computeMostLikelyValue[T](target: Element[T]): T
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Attributes Definition Classes ProbEvidenceMarginalMAP -> MarginalMAPAlgorithm Inherited from: ProbEvidenceMarginalMAP

Invocation template:

```scala
receiver.computeMostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeProbEvidenceMarginalMAP.getSampleCount ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeProbEvidenceMarginalMAP.html#getSampleCount-0)

```scala
def getSampleCount: Int
```

Number of samples taken

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples taken Attributes Inherited from: BaseUnweightedSampler

Invocation template:

```scala
receiver.getSampleCount
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeProbEvidenceMarginalMAP.getTemperature ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeProbEvidenceMarginalMAP.html#getTemperature-0)

```scala
def getTemperature: Double
```

Get the current temperature. Used for debugging.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Get the current temperature. Used for debugging. Attributes Inherited from: ProbEvidenceMarginalMAP

Invocation template:

```scala
receiver.getTemperature
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeProbEvidenceMarginalMAP.handle ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeProbEvidenceMarginalMAP.html#handle-fffff940)

```scala
def handle(service: Service): Response
```

A handler of services provided by the algorithm.

Type parameters: none.

Parameters, list 1: `` service: Service ``.

Returns: `` Response ``.

Source contract/attributes: A handler of services provided by the algorithm. Attributes Inherited from: AnytimeMarginalMAP

Invocation template:

```scala
receiver.handle(service)
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeProbEvidenceMarginalMAP.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeProbEvidenceMarginalMAP.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeProbEvidenceMarginalMAP.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeProbEvidenceMarginalMAP.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeProbEvidenceMarginalMAP.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeProbEvidenceMarginalMAP.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns an estimate of the max a posteriori value of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns an estimate of the max a posteriori value of the target. Attributes Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of MAP elements. Inherited from: MarginalMAPAlgorithm

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeProbEvidenceMarginalMAP.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeProbEvidenceMarginalMAP.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeProbEvidenceMarginalMAP.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeProbEvidenceMarginalMAP.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single step of the algorithm. The algorithm must be able to provide answers after each step.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single step of the algorithm. The algorithm must be able to provide answers after each step. Attributes Inherited from: AnytimeSampler

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeProbEvidenceMarginalMAP.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeProbEvidenceMarginalMAP.html#sample-ffffff32)

```scala
override def sample(): (Boolean, Sample)
```

Produce a single sample.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (Boolean, Sample) ``.

Source contract/attributes: Produce a single sample. Attributes Definition Classes ProbEvidenceMarginalMAP -> MetropolisHastings -> BaseUnweightedSampler Inherited from: ProbEvidenceMarginalMAP

Invocation template:

```scala
receiver.sample()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeProbEvidenceMarginalMAP.shutdown ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeProbEvidenceMarginalMAP.html#shutdown-0)

```scala
def shutdown: Unit
```

Release all resources from this anytime algorithm.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Unit ``.

Source contract/attributes: Release all resources from this anytime algorithm. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.shutdown
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeProbEvidenceMarginalMAP.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeProbEvidenceMarginalMAP.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeProbEvidenceMarginalMAP.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeProbEvidenceMarginalMAP.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeProbEvidenceMarginalMAP.stopUpdate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeProbEvidenceMarginalMAP.html#stopUpdate-94c)

```scala
override def stopUpdate(): Unit
```

Override the stopUpdate function in anytime to call the sampler update function

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Override the stopUpdate function in anytime to call the sampler update function Attributes Definition Classes AnytimeSampler -> Anytime Inherited from: AnytimeSampler

Invocation template:

```scala
receiver.stopUpdate()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeProbEvidenceMarginalMAP.test ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeProbEvidenceMarginalMAP.html#test-fffff570)

```scala
def test(numSamples: Int, predicates: Seq[Predicate[_]], elementsToTrack: Seq[Element[_]]): (Double, Map[Predicate[_], Double], Map[Element[_], Double])
```

Test Metropolis-Hastings by repeatedly running a single step from the same initial state. For each of a set of predicates, the fraction of times the predicate is satisfied by the resulting state is returned. By the resulting state, we mean the new state if it is accepted and the original state if not.

Type parameters: none.

Parameters, list 1: `` numSamples: Int ``; `` predicates: Seq[Predicate[_]] ``; `` elementsToTrack: Seq[Element[_]] ``.

Returns: `` (Double, Map[Predicate[_], Double], Map[Element[_], Double]) ``.

Source contract/attributes: Test Metropolis-Hastings by repeatedly running a single step from the same initial state. For each of a set of predicates, the fraction of times the predicate is satisfied by the resulting state is returned. By the resulting state, we mean the new state if it is accepted and the original state if not. Attributes Inherited from: MetropolisHastings

Invocation template:

```scala
receiver.test(numSamples, predicates, elementsToTrack)
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeProbEvidenceMarginalMAP.update ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeProbEvidenceMarginalMAP.html#update-94c)

```scala
override def update(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Definition Classes ProbEvidenceMarginalMAP -> BaseUnweightedSampler -> Sampler Inherited from: ProbEvidenceMarginalMAP

Invocation template:

```scala
receiver.update()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.computeMostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#computeMostLikelyValue-fffffa13)

```scala
def computeMostLikelyValue[T](target: Element[T]): T
```

Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star. Attributes Inherited from: StructuredMarginalMAPAlgorithm

Invocation template:

```scala
receiver.computeMostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.handle ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#handle-fffff940)

```scala
def handle(service: Service): Response
```

A handler of services provided by the algorithm.

Type parameters: none.

Parameters, list 1: `` service: Service ``.

Returns: `` Response ``.

Source contract/attributes: A handler of services provided by the algorithm. Attributes Inherited from: AnytimeMarginalMAP

Invocation template:

```scala
receiver.handle(service)
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes StructuredAlgorithm -> Algorithm Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns an estimate of the max a posteriori value of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns an estimate of the max a posteriori value of the target. Attributes Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of MAP elements. Inherited from: MarginalMAPAlgorithm

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredMarginalMAPAlgorithm -> StructuredAlgorithm Inherited from: StructuredMarginalMAPAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredMarginalMAPAlgorithm -> StructuredAlgorithm Inherited from: StructuredMarginalMAPAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with one value per iteration.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with one value per iteration. Attributes Definition Classes AnytimeStructured -> StructuredAlgorithm Inherited from: AnytimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.shutdown ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#shutdown-0)

```scala
def shutdown: Unit
```

Release all resources from this anytime algorithm.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Unit ``.

Source contract/attributes: Release all resources from this anytime algorithm. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.shutdown
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.stopUpdate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#stopUpdate-94c)

```scala
def stopUpdate(): Unit
```

Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.stopUpdate()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#refiningStrategy-fffffdd6)

```scala
def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.experimental.marginalmap.AnytimeStructuredMarginalMAP.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/AnytimeStructuredMarginalMAP.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.experimental.marginalmap.DecompositionMarginalMAP.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/DecompositionMarginalMAP.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.experimental.marginalmap.DecompositionMarginalMAP.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/DecompositionMarginalMAP.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.marginalmap.DecompositionMarginalMAP.computeMostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/DecompositionMarginalMAP.html#computeMostLikelyValue-fffffa13)

```scala
def computeMostLikelyValue[T](target: Element[T]): T
```

Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star. Attributes Inherited from: StructuredMarginalMAPAlgorithm

Invocation template:

```scala
receiver.computeMostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.DecompositionMarginalMAP.initialElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/DecompositionMarginalMAP.html#initialElements-0)

```scala
def initialElements: List[Element[_]]
```

Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe. Attributes Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialElements
```

## `` com.cra.figaro.experimental.marginalmap.DecompositionMarginalMAP.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/DecompositionMarginalMAP.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm -> Algorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.marginalmap.DecompositionMarginalMAP.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/DecompositionMarginalMAP.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.marginalmap.DecompositionMarginalMAP.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/DecompositionMarginalMAP.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.marginalmap.DecompositionMarginalMAP.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/DecompositionMarginalMAP.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns an estimate of the max a posteriori value of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns an estimate of the max a posteriori value of the target. Attributes Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of MAP elements. Inherited from: MarginalMAPAlgorithm

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.DecompositionMarginalMAP.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/DecompositionMarginalMAP.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.experimental.marginalmap.DecompositionMarginalMAP.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/DecompositionMarginalMAP.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredMarginalMAPAlgorithm -> StructuredAlgorithm Inherited from: StructuredMarginalMAPAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.experimental.marginalmap.DecompositionMarginalMAP.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/DecompositionMarginalMAP.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredMarginalMAPAlgorithm -> StructuredAlgorithm Inherited from: StructuredMarginalMAPAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.experimental.marginalmap.DecompositionMarginalMAP.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/DecompositionMarginalMAP.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeStructured -> StructuredAlgorithm Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.experimental.marginalmap.DecompositionMarginalMAP.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/DecompositionMarginalMAP.html#refiningStrategy-fffffdd6)

```scala
override def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.experimental.marginalmap.DecompositionMarginalMAP.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/DecompositionMarginalMAP.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.marginalmap.DecompositionMarginalMAP.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/DecompositionMarginalMAP.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.experimental.marginalmap.DecompositionMarginalMAP.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/DecompositionMarginalMAP.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.experimental.marginalmap.DecompositionMarginalMAP.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/DecompositionMarginalMAP.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.marginalmap.DecompositionMarginalMAP.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/DecompositionMarginalMAP.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.marginalmap.DecompositionMarginalMAP.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/DecompositionMarginalMAP.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.NotATargetException.addSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm$NotATargetException.html#addSuppressed-4b8)

```scala
final def addSuppressed(x$0: Throwable): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.addSuppressed(x$0)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.NotATargetException.fillInStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm$NotATargetException.html#fillInStackTrace-26c)

```scala
def fillInStackTrace(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.fillInStackTrace()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.NotATargetException.getCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm$NotATargetException.html#getCause-26c)

```scala
def getCause(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getCause()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.NotATargetException.getLocalizedMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm$NotATargetException.html#getLocalizedMessage-665)

```scala
def getLocalizedMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getLocalizedMessage()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.NotATargetException.getMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm$NotATargetException.html#getMessage-665)

```scala
def getMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getMessage()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.NotATargetException.getStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm$NotATargetException.html#getStackTrace-fffffa6d)

```scala
def getStackTrace(): Array[StackTraceElement]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[StackTraceElement] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getStackTrace()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.NotATargetException.getSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm$NotATargetException.html#getSuppressed-fffff2ce)

```scala
final def getSuppressed(): Array[Throwable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[Throwable] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getSuppressed()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.NotATargetException.initCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm$NotATargetException.html#initCause-fffff080)

```scala
def initCause(x$0: Throwable): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.initCause(x$0)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.NotATargetException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm$NotATargetException.html#printStackTrace-fffff230)

```scala
def printStackTrace(x$0: PrintWriter): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintWriter ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.NotATargetException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm$NotATargetException.html#printStackTrace-fffff0fd)

```scala
def printStackTrace(x$0: PrintStream): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintStream ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.NotATargetException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm$NotATargetException.html#printStackTrace-94c)

```scala
def printStackTrace(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.NotATargetException.setStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm$NotATargetException.html#setStackTrace-f79)

```scala
def setStackTrace(x$0: Array[StackTraceElement]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Array[StackTraceElement] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.setStackTrace(x$0)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.NotATargetException.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm$NotATargetException.html#toString-665)

```scala
def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Inherited from: Throwable

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.computeMostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm.html#computeMostLikelyValue-fffffa13)

```scala
def computeMostLikelyValue[T](target: Element[T]): T
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Invocation template:

```scala
receiver.computeMostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.mapElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm.html#mapElements-0)

```scala
def mapElements: Seq[Element[_]]
```

Elements for which to perform MAP queries. This algorithm marginalizes over elements not in this list.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Seq[Element[_]] ``.

Source contract/attributes: Elements for which to perform MAP queries. This algorithm marginalizes over elements not in this list. Attributes

Invocation template:

```scala
receiver.mapElements
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.universe ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm.html#universe-0)

```scala
def universe: Universe
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Universe ``.

Invocation template:

```scala
receiver.universe
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns an estimate of the max a posteriori value of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns an estimate of the max a posteriori value of the target. Attributes Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of MAP elements.

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPAlgorithm.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPAlgorithm.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation$.html#apply-fffff0f0)

```scala
def apply(myIterations: Int, targets: Element[_]*)(implicit universe: Universe): MarginalMAPBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeMarginalMAP
```

Creates a One Time marginal MAP belief propagation computer in the current default universe.

Type parameters: none.

Parameters, list 1: `` myIterations: Int ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` MarginalMAPBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeMarginalMAP ``.

Source contract/attributes: Creates a One Time marginal MAP belief propagation computer in the current default universe. Value parameters myIterations Iterations of mixed-product BP to run. targets MAP elements, which can be queried. Elements not supplied here are summed over. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.apply(myIterations, targets*)(using universe)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation$.html#apply-ffffff89)

```scala
def apply(targets: Element[_]*)(implicit universe: Universe): MarginalMAPBeliefPropagation & AnytimeProbabilisticBeliefPropagation & AnytimeMarginalMAP
```

Creates an Anytime marginal MAP belief propagation computer in the current default universe.

Type parameters: none.

Parameters, list 1: `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` MarginalMAPBeliefPropagation & AnytimeProbabilisticBeliefPropagation & AnytimeMarginalMAP ``.

Source contract/attributes: Creates an Anytime marginal MAP belief propagation computer in the current default universe. Value parameters targets MAP elements, which can be queried. Elements not supplied here are summed over. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.apply(targets*)(using universe)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation$.html#apply-d5e)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], myIterations: Int, targets: Element[_]*)(implicit universe: Universe): MarginalMAPBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeMarginalMAP
```

Creates a One Time marginal MAP belief propagation computer in the current default universe.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` myIterations: Int ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` MarginalMAPBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeMarginalMAP ``.

Source contract/attributes: Creates a One Time marginal MAP belief propagation computer in the current default universe. Value parameters dependentUniverses Dependent universes for this algorithm. myIterations Iterations of mixed-product BP to run. targets MAP elements, which can be queried. Elements not supplied here are summed over. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.apply(dependentUniverses, myIterations, targets*)(using universe)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation$.html#apply-ffffff5b)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], targets: Element[_]*)(implicit universe: Universe): MarginalMAPBeliefPropagation & AnytimeProbabilisticBeliefPropagation & AnytimeMarginalMAP
```

Creates an Anytime marginal MAP belief propagation computer in the current default universe.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` MarginalMAPBeliefPropagation & AnytimeProbabilisticBeliefPropagation & AnytimeMarginalMAP ``.

Source contract/attributes: Creates an Anytime marginal MAP belief propagation computer in the current default universe. Value parameters dependentUniverses Dependent universes for this algorithm. targets MAP elements, which can be queried. Elements not supplied here are summed over. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.apply(dependentUniverses, targets*)(using universe)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation$.html#apply-fffff6f0)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double, myIterations: Int, targets: Element[_]*)(implicit universe: Universe): MarginalMAPBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeMarginalMAP
```

Creates a One Time marginal MAP belief propagation computer in the current default universe.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``; `` myIterations: Int ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` MarginalMAPBeliefPropagation & OneTimeProbabilisticBeliefPropagation & OneTimeMarginalMAP ``.

Source contract/attributes: Creates a One Time marginal MAP belief propagation computer in the current default universe. Value parameters dependentAlgorithm Used to determine algorithm for computing probability of evidence in dependent universes. dependentUniverses Dependent universes for this algorithm. myIterations Iterations of mixed-product BP to run. targets MAP elements, which can be queried. Elements not supplied here are summed over. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.apply(dependentUniverses, dependentAlgorithm, myIterations, targets*)(using universe)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation$.html#apply-989)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double, targets: Element[_]*)(implicit universe: Universe): MarginalMAPBeliefPropagation & AnytimeProbabilisticBeliefPropagation & AnytimeMarginalMAP
```

Creates an Anytime marginal MAP belief propagation computer in the current default universe.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` MarginalMAPBeliefPropagation & AnytimeProbabilisticBeliefPropagation & AnytimeMarginalMAP ``.

Source contract/attributes: Creates an Anytime marginal MAP belief propagation computer in the current default universe. Value parameters dependentAlgorithm Used to determine algorithm for computing probability of evidence in dependent universes. dependentUniverses Dependent universes for this algorithm. targets MAP elements, which can be queried. Elements not supplied here are summed over. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.apply(dependentUniverses, dependentAlgorithm, targets*)(using universe)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation$.html#mostLikelyValue-e3e)

```scala
def mostLikelyValue[T](target: Element[T], mapElements: Element[_]*): T
```

Use belief propagation to compute the most likely value of the given element. Runs 10 iterations of mixed-product BP.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` mapElements: Element[_]* ``.

Returns: `` T ``.

Source contract/attributes: Use belief propagation to compute the most likely value of the given element. Runs 10 iterations of mixed-product BP. Value parameters mapElements Additional elements to MAP. Elements not in this list are summed over. target Element for which to compute MAP value. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.mostLikelyValue[T](target, mapElements*)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.computeMostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation.html#computeMostLikelyValue-fffffa13)

```scala
def computeMostLikelyValue[T](target: Element[T]): T
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Invocation template:

```scala
receiver.computeMostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation.html#initialize-94c)

```scala
override def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Definition Classes BeliefPropagation -> Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.belief ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation.html#belief-e6a)

```scala
def belief(source: Node): Factor[Double]
```

Returns the product of all messages from a source node's neighbors to itself.

Type parameters: none.

Parameters, list 1: `` source: Node ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Returns the product of all messages from a source node's neighbors to itself. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.belief(source)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

Returns the factors needed for BP. Since BP operates on a complete factor graph, factors are created for all elements in the universe.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Returns the factors needed for BP. Since BP operates on a complete factor graph, factors are created for all elements in the universe. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.getFinalFactorForElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation.html#getFinalFactorForElement-c2f)

```scala
def getFinalFactorForElement[T](target: Element[T]): Factor[Double]
```

Get the final factor for an element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Get the final factor for an element. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.getFinalFactorForElement[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns an estimate of the max a posteriori value of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns an estimate of the max a posteriori value of the target. Attributes Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of MAP elements. Inherited from: MarginalMAPAlgorithm

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.normalize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation.html#normalize-fffffd5e)

```scala
def normalize(factor: Factor[Double]): Factor[Double]
```

Normalize a factor.

Type parameters: none.

Parameters, list 1: `` factor: Factor[Double] ``.

Returns: `` Factor[Double] ``.

Source contract/attributes: Normalize a factor. Attributes Inherited from: ProbabilisticBeliefPropagation

Invocation template:

```scala
receiver.normalize(factor)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation.html#runStep-94c)

```scala
def runStep(): Unit
```

Runs this belief propagation algorithm for one iteration. An iteration consists of each node of the factor graph sending a message to each of its neighbors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Runs this belief propagation algorithm for one iteration. An iteration consists of each node of the factor graph sending a message to each of its neighbors. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.starterElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation.html#starterElements-0)

```scala
def starterElements: List[Element[_]]
```

Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables. Attributes Inherited from: BeliefPropagation

Invocation template:

```scala
receiver.starterElements
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPBeliefPropagation.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPBeliefPropagation.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPVEStrategy.eliminate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPVEStrategy.html#eliminate-ffffff28)

```scala
def eliminate(toEliminate: Set[Variable[_]], toPreserve: Set[Variable[_]], factors: List[Factor[Double]]): (List[Factor[Double]], Map[Variable[_], Factor[_]])
```

Solve the problem by eliminating variables, leaving only the ones that belong in the solution.

Type parameters: none.

Parameters, list 1: `` toEliminate: Set[Variable[_]] ``; `` toPreserve: Set[Variable[_]] ``; `` factors: List[Factor[Double]] ``.

Returns: `` (List[Factor[Double]], Map[Variable[_], Factor[_]]) ``.

Source contract/attributes: Solve the problem by eliminating variables, leaving only the ones that belong in the solution. Value parameters factors Factors over which to perform elimination. toEliminate Variables to eliminate. toPreserve Variables to preserve. Attributes Returns A list of factors over the variables to preserve representing their joint distribution, and a map of recording factors for MPE.

Invocation template:

```scala
receiver.eliminate(toEliminate, toPreserve, factors)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPVEStrategy.recurse ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPVEStrategy.html#recurse-fffff41a)

```scala
override def recurse(subproblem: NestedProblem[_]): RaisingStrategy
```

Returns a strategy that could be used to solve the nested problem.

Type parameters: none.

Parameters, list 1: `` subproblem: NestedProblem[_] ``.

Returns: `` RaisingStrategy ``.

Source contract/attributes: Returns a strategy that could be used to solve the nested problem. Value parameters subproblem Unsolved nested problem to recurse on. Attributes Returns A strategy to solve the nested problem. Definition Classes RaisingStrategy

Invocation template:

```scala
receiver.recurse(subproblem)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPVEStrategy.chainNonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPVEStrategy.html#chainNonConstraintFactors-fffffa11)

```scala
def chainNonConstraintFactors[ParentValue, Value](chainComp: ChainComponent[ParentValue, Value]): List[Factor[Double]]
```

Get non-constraint factors associated with a single Chain component.

Type parameters: `` ParentValue ``; `` Value ``.

Parameters, list 1: `` chainComp: ChainComponent[ParentValue, Value] ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get non-constraint factors associated with a single Chain component. Value parameters chainComp Chain component to process. Attributes Returns All factors associated with the chain component that are needed for solving. This includes (possibly eliminated) subproblem factors. Inherited from: RaisingStrategy

Invocation template:

```scala
receiver.chainNonConstraintFactors[ParentValue, Value](chainComp)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPVEStrategy.constraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPVEStrategy.html#constraintFactors-fffff66a)

```scala
def constraintFactors(bounds: Bounds): List[Factor[Double]]
```

Get all of the constraint factors needed for solving.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get all of the constraint factors needed for solving. Value parameters bounds Bounds for the returned constraint factors. Attributes Returns Constraint factors for solving. Inherited from: SolvingStrategy

Invocation template:

```scala
receiver.constraintFactors(bounds)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPVEStrategy.execute ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPVEStrategy.html#execute-fffff034)

```scala
def execute(bounds: Bounds = ...): Unit
```

Solve the problem defined by all the components' current factors. This involves solving and incorporating subproblems as well. This will set the globals accordingly. All components in this problem and contained subproblems should be eliminated in the solution.

Type parameters: none.

Parameters, list 1: `` bounds: Bounds = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Solve the problem defined by all the components' current factors. This involves solving and incorporating subproblems as well. This will set the globals accordingly. All components in this problem and contained subproblems should be eliminated in the solution. Value parameters bounds Bounds for constraint factors. Defaults to Lower. This default is intended for the cases where it does not matter which bounds should be used because both upper and lower bounds would be the same. Attributes Inherited from: SolvingStrategy

Invocation template:

```scala
receiver.execute(bounds)
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPVEStrategy.nonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPVEStrategy.html#nonConstraintFactors-fffffc52)

```scala
override def nonConstraintFactors(): List[Factor[Double]]
```

Get all of the non-constraint factors needed for solving. This includes subproblem factors.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Get all of the non-constraint factors needed for solving. This includes subproblem factors. Attributes Returns Non-constraint factors for solving. Definition Classes RaisingStrategy -> SolvingStrategy Inherited from: RaisingStrategy

Invocation template:

```scala
receiver.nonConstraintFactors()
```

## `` com.cra.figaro.experimental.marginalmap.MarginalMAPVEStrategy.subproblemNonConstraintFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/MarginalMAPVEStrategy.html#subproblemNonConstraintFactors-189)

```scala
def subproblemNonConstraintFactors[ParentValue, Value](chainComp: ChainComponent[ParentValue, Value]): Map[ParentValue, List[Factor[Double]]]
```

Get the non-constraint factors associated with all subproblems of a Chain component. This returns the existing solution if there is one. Otherwise, it chooses to solve or raise the subproblem based on the raising criteria.

Type parameters: `` ParentValue ``; `` Value ``.

Parameters, list 1: `` chainComp: ChainComponent[ParentValue, Value] ``.

Returns: `` Map[ParentValue, List[Factor[Double]]] ``.

Source contract/attributes: Get the non-constraint factors associated with all subproblems of a Chain component. This returns the existing solution if there is one. Otherwise, it chooses to solve or raise the subproblem based on the raising criteria. Value parameters chainComp Chain component whose subproblems are to be processed. Attributes Returns All factors associated with subproblems that are needed for solving, grouped by parent value. Inherited from: RaisingStrategy

Invocation template:

```scala
receiver.subproblemNonConstraintFactors[ParentValue, Value](chainComp)
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeMarginalMAP.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeMarginalMAP.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeMarginalMAP.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeMarginalMAP.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeMarginalMAP.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeMarginalMAP.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeMarginalMAP.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeMarginalMAP.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeMarginalMAP.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeMarginalMAP.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns an estimate of the max a posteriori value of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns an estimate of the max a posteriori value of the target. Attributes Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of MAP elements. Inherited from: MarginalMAPAlgorithm

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeMarginalMAP.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeMarginalMAP.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeMarginalMAP.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeMarginalMAP.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeMarginalMAP.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeMarginalMAP.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeMarginalMAP.computeMostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeMarginalMAP.html#computeMostLikelyValue-fffffa13)

```scala
def computeMostLikelyValue[T](target: Element[T]): T
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Attributes Inherited from: MarginalMAPAlgorithm

Invocation template:

```scala
receiver.computeMostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeMarginalMAP.mapElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeMarginalMAP.html#mapElements-0)

```scala
def mapElements: Seq[Element[_]]
```

Elements for which to perform MAP queries. This algorithm marginalizes over elements not in this list.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Seq[Element[_]] ``.

Source contract/attributes: Elements for which to perform MAP queries. This algorithm marginalizes over elements not in this list. Attributes Inherited from: MarginalMAPAlgorithm

Invocation template:

```scala
receiver.mapElements
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeMarginalMAP.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeMarginalMAP.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Inherited from: OneTime

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeMarginalMAP.universe ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeMarginalMAP.html#universe-0)

```scala
def universe: Universe
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Universe ``.

Source contract/attributes: Attributes Inherited from: MarginalMAPAlgorithm

Invocation template:

```scala
receiver.universe
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeProbEvidenceMarginalMAP.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeProbEvidenceMarginalMAP.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeSampler -> OneTime

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeProbEvidenceMarginalMAP.acceptRejectRatio ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeProbEvidenceMarginalMAP.html#acceptRejectRatio-0)

```scala
def acceptRejectRatio: Double
```

Get the acceptance ratio for the sampler.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Get the acceptance ratio for the sampler. Attributes Inherited from: MetropolisHastings

Invocation template:

```scala
receiver.acceptRejectRatio
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeProbEvidenceMarginalMAP.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeProbEvidenceMarginalMAP.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Clean up the sampler, freeing memory.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clean up the sampler, freeing memory. Attributes Definition Classes ProbEvidenceMarginalMAP -> MetropolisHastings -> Algorithm Inherited from: ProbEvidenceMarginalMAP

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeProbEvidenceMarginalMAP.computeMostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeProbEvidenceMarginalMAP.html#computeMostLikelyValue-fffffa13)

```scala
override def computeMostLikelyValue[T](target: Element[T]): T
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Attributes Definition Classes ProbEvidenceMarginalMAP -> MarginalMAPAlgorithm Inherited from: ProbEvidenceMarginalMAP

Invocation template:

```scala
receiver.computeMostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeProbEvidenceMarginalMAP.getSampleCount ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeProbEvidenceMarginalMAP.html#getSampleCount-0)

```scala
def getSampleCount: Int
```

Number of samples taken

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples taken Attributes Inherited from: BaseUnweightedSampler

Invocation template:

```scala
receiver.getSampleCount
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeProbEvidenceMarginalMAP.getTemperature ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeProbEvidenceMarginalMAP.html#getTemperature-0)

```scala
def getTemperature: Double
```

Get the current temperature. Used for debugging.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Get the current temperature. Used for debugging. Attributes Inherited from: ProbEvidenceMarginalMAP

Invocation template:

```scala
receiver.getTemperature
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeProbEvidenceMarginalMAP.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeProbEvidenceMarginalMAP.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeProbEvidenceMarginalMAP.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeProbEvidenceMarginalMAP.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeProbEvidenceMarginalMAP.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeProbEvidenceMarginalMAP.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeProbEvidenceMarginalMAP.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeProbEvidenceMarginalMAP.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns an estimate of the max a posteriori value of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns an estimate of the max a posteriori value of the target. Attributes Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of MAP elements. Inherited from: MarginalMAPAlgorithm

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeProbEvidenceMarginalMAP.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeProbEvidenceMarginalMAP.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeProbEvidenceMarginalMAP.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeProbEvidenceMarginalMAP.html#sample-ffffff32)

```scala
override def sample(): (Boolean, Sample)
```

Produce a single sample.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (Boolean, Sample) ``.

Source contract/attributes: Produce a single sample. Attributes Definition Classes ProbEvidenceMarginalMAP -> MetropolisHastings -> BaseUnweightedSampler Inherited from: ProbEvidenceMarginalMAP

Invocation template:

```scala
receiver.sample()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeProbEvidenceMarginalMAP.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeProbEvidenceMarginalMAP.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeProbEvidenceMarginalMAP.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeProbEvidenceMarginalMAP.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeProbEvidenceMarginalMAP.test ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeProbEvidenceMarginalMAP.html#test-fffff570)

```scala
def test(numSamples: Int, predicates: Seq[Predicate[_]], elementsToTrack: Seq[Element[_]]): (Double, Map[Predicate[_], Double], Map[Element[_], Double])
```

Test Metropolis-Hastings by repeatedly running a single step from the same initial state. For each of a set of predicates, the fraction of times the predicate is satisfied by the resulting state is returned. By the resulting state, we mean the new state if it is accepted and the original state if not.

Type parameters: none.

Parameters, list 1: `` numSamples: Int ``; `` predicates: Seq[Predicate[_]] ``; `` elementsToTrack: Seq[Element[_]] ``.

Returns: `` (Double, Map[Predicate[_], Double], Map[Element[_], Double]) ``.

Source contract/attributes: Test Metropolis-Hastings by repeatedly running a single step from the same initial state. For each of a set of predicates, the fraction of times the predicate is satisfied by the resulting state is returned. By the resulting state, we mean the new state if it is accepted and the original state if not. Attributes Inherited from: MetropolisHastings

Invocation template:

```scala
receiver.test(numSamples, predicates, elementsToTrack)
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeProbEvidenceMarginalMAP.update ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeProbEvidenceMarginalMAP.html#update-94c)

```scala
override def update(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Definition Classes ProbEvidenceMarginalMAP -> BaseUnweightedSampler -> Sampler Inherited from: ProbEvidenceMarginalMAP

Invocation template:

```scala
receiver.update()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeStructuredMarginalMAP.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeStructuredMarginalMAP.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeStructuredMarginalMAP.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeStructuredMarginalMAP.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeStructuredMarginalMAP.computeMostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeStructuredMarginalMAP.html#computeMostLikelyValue-fffffa13)

```scala
def computeMostLikelyValue[T](target: Element[T]): T
```

Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star. Attributes Inherited from: StructuredMarginalMAPAlgorithm

Invocation template:

```scala
receiver.computeMostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeStructuredMarginalMAP.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeStructuredMarginalMAP.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes StructuredAlgorithm -> Algorithm Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeStructuredMarginalMAP.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeStructuredMarginalMAP.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeStructuredMarginalMAP.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeStructuredMarginalMAP.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeStructuredMarginalMAP.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeStructuredMarginalMAP.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns an estimate of the max a posteriori value of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns an estimate of the max a posteriori value of the target. Attributes Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of MAP elements. Inherited from: MarginalMAPAlgorithm

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeStructuredMarginalMAP.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeStructuredMarginalMAP.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeStructuredMarginalMAP.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeStructuredMarginalMAP.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredMarginalMAPAlgorithm -> StructuredAlgorithm Inherited from: StructuredMarginalMAPAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeStructuredMarginalMAP.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeStructuredMarginalMAP.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredMarginalMAPAlgorithm -> StructuredAlgorithm Inherited from: StructuredMarginalMAPAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeStructuredMarginalMAP.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeStructuredMarginalMAP.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeStructured -> StructuredAlgorithm Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeStructuredMarginalMAP.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeStructuredMarginalMAP.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeStructuredMarginalMAP.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeStructuredMarginalMAP.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeStructuredMarginalMAP.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeStructuredMarginalMAP.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeStructuredMarginalMAP.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeStructuredMarginalMAP.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeStructuredMarginalMAP.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeStructuredMarginalMAP.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeStructuredMarginalMAP.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeStructuredMarginalMAP.html#refiningStrategy-fffffdd6)

```scala
def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.experimental.marginalmap.OneTimeStructuredMarginalMAP.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/OneTimeStructuredMarginalMAP.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP$.html#apply-fffff74d)

```scala
def apply(iterations: Int, tolerance: Double, samplesPerIteration: Int, maxRuns: Int, proposalScheme: ProposalScheme, schedule: Schedule, mapElements: Element[_]*)(implicit universe: Universe): OneTimeProbEvidenceMarginalMAP
```

Creates a one time marginal MAP algorithm that uses probability of evidence.

Type parameters: none.

Parameters, list 1: `` iterations: Int ``; `` tolerance: Double ``; `` samplesPerIteration: Int ``; `` maxRuns: Int ``; `` proposalScheme: ProposalScheme ``; `` schedule: Schedule ``; `` mapElements: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` OneTimeProbEvidenceMarginalMAP ``.

Source contract/attributes: Creates a one time marginal MAP algorithm that uses probability of evidence. Attributes See also com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP abstract class for a complete description of the parameters.

Invocation template:

```scala
com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.apply(iterations, tolerance, samplesPerIteration, maxRuns, proposalScheme, schedule, mapElements*)(using universe)
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP$.html#apply-fffff580)

```scala
def apply(tolerance: Double, samplesPerIteration: Int, maxRuns: Int, proposalScheme: ProposalScheme, schedule: Schedule, mapElements: Element[_]*)(implicit universe: Universe): AnytimeProbEvidenceMarginalMAP
```

Creates an anytime marginal MAP algorithm that uses probability of evidence.

Type parameters: none.

Parameters, list 1: `` tolerance: Double ``; `` samplesPerIteration: Int ``; `` maxRuns: Int ``; `` proposalScheme: ProposalScheme ``; `` schedule: Schedule ``; `` mapElements: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` AnytimeProbEvidenceMarginalMAP ``.

Source contract/attributes: Creates an anytime marginal MAP algorithm that uses probability of evidence. Attributes See also com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP abstract class for a complete description of the parameters.

Invocation template:

```scala
com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.apply(tolerance, samplesPerIteration, maxRuns, proposalScheme, schedule, mapElements*)(using universe)
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP$.html#apply-fffff38c)

```scala
def apply(iterations: Int, mapElements: Element[_]*)(implicit universe: Universe): OneTimeProbEvidenceMarginalMAP
```

Creates a one time marginal MAP algorithm that uses probability of evidence. Takes 100 samples per iteration at a tolerance of 0.05, up to a maximum of 100 runs. Uses the default proposal scheme and schedule.

Type parameters: none.

Parameters, list 1: `` iterations: Int ``; `` mapElements: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` OneTimeProbEvidenceMarginalMAP ``.

Source contract/attributes: Creates a one time marginal MAP algorithm that uses probability of evidence. Takes 100 samples per iteration at a tolerance of 0.05, up to a maximum of 100 runs. Uses the default proposal scheme and schedule. Attributes See also com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP abstract class for a complete description of the parameters.

Invocation template:

```scala
com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.apply(iterations, mapElements*)(using universe)
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP$.html#apply-8bf)

```scala
def apply(mapElements: Element[_]*)(implicit universe: Universe): AnytimeProbEvidenceMarginalMAP
```

Creates an anytime marginal MAP algorithm that uses probability of evidence. Takes 100 samples per iteration at a tolerance of 0.05, up to a maximum of 100 runs. Uses the default proposal scheme and schedule.

Type parameters: none.

Parameters, list 1: `` mapElements: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` AnytimeProbEvidenceMarginalMAP ``.

Source contract/attributes: Creates an anytime marginal MAP algorithm that uses probability of evidence. Takes 100 samples per iteration at a tolerance of 0.05, up to a maximum of 100 runs. Uses the default proposal scheme and schedule. Attributes See also com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP abstract class for a complete description of the parameters.

Invocation template:

```scala
com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.apply(mapElements*)(using universe)
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.MMAPProbEvidenceSampler.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP$MMAPProbEvidenceSampler.html#run-94c)

```scala
override def run(): Unit
```

Observe the necessary values of MAP elements, then run the algorithm. After this is initialized, calling this method again is allowed. The additional samples are accounted for when returning the total log statistics.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Observe the necessary values of MAP elements, then run the algorithm. After this is initialized, calling this method again is allowed. The additional samples are accounted for when returning the total log statistics. Attributes Definition Classes OneTimeSampler -> OneTime

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.MMAPProbEvidenceSampler.additionalEvidenceAlgorithm ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP$MMAPProbEvidenceSampler.html#additionalEvidenceAlgorithm-98b)

```scala
def additionalEvidenceAlgorithm(evidence: List[NamedEvidence[_]]): ProbEvidenceAlgorithm
```

The algorithm used to compute the probability of additional evidence, as created by probAdditionalEvidence. This algorithm can be different to the one defined in this class. (For example, a one-time algorithm can use an anytime algorithm for additional evidence.)

Type parameters: none.

Parameters, list 1: `` evidence: List[NamedEvidence[_]] ``.

Returns: `` ProbEvidenceAlgorithm ``.

Source contract/attributes: The algorithm used to compute the probability of additional evidence, as created by probAdditionalEvidence. This algorithm can be different to the one defined in this class. (For example, a one-time algorithm can use an anytime algorithm for additional evidence.) Attributes Inherited from: OneTimeProbEvidenceSampler

Invocation template:

```scala
receiver.additionalEvidenceAlgorithm(evidence)
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.MMAPProbEvidenceSampler.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP$MMAPProbEvidenceSampler.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Removes the evidence provided in the constructor from the universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Removes the evidence provided in the constructor from the universe. Attributes Definition Classes ProbEvidenceSampler -> ProbEvidenceAlgorithm -> Algorithm Inherited from: ProbEvidenceSampler

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.MMAPProbEvidenceSampler.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP$MMAPProbEvidenceSampler.html#initialize-94c)

```scala
override def initialize(): Unit
```

Since probability of evidence algorithms introduce additional evidence (namely, their evidence argument), into an existing universe, a mechanism must be provided for introducing the evidence when the algorithm begins and cleaning it up at the end. This is achieved with the initialize method, called when the algorithm starts, and the cleanUp method, called when the algorithm is killed.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Since probability of evidence algorithms introduce additional evidence (namely, their evidence argument), into an existing universe, a mechanism must be provided for introducing the evidence when the algorithm begins and cleaning it up at the end. This is achieved with the initialize method, called when the algorithm starts, and the cleanUp method, called when the algorithm is killed. Attributes Definition Classes ProbEvidenceAlgorithm -> Algorithm Inherited from: ProbEvidenceAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.MMAPProbEvidenceSampler.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP$MMAPProbEvidenceSampler.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.MMAPProbEvidenceSampler.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP$MMAPProbEvidenceSampler.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.MMAPProbEvidenceSampler.logProbEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP$MMAPProbEvidenceSampler.html#logProbEvidence-0)

```scala
override def logProbEvidence: Double
```

The computed log probability of evidence.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: The computed log probability of evidence. Attributes Definition Classes ProbEvidenceSampler -> ProbEvidenceAlgorithm Inherited from: ProbEvidenceSampler

Invocation template:

```scala
receiver.logProbEvidence
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.MMAPProbEvidenceSampler.probAdditionalEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP$MMAPProbEvidenceSampler.html#probAdditionalEvidence-98b)

```scala
def probAdditionalEvidence(evidence: List[NamedEvidence[_]]): ProbEvidenceAlgorithm
```

Returns an algorithm to compute the probability of the additional evidence provided.

Type parameters: none.

Parameters, list 1: `` evidence: List[NamedEvidence[_]] ``.

Returns: `` ProbEvidenceAlgorithm ``.

Source contract/attributes: Returns an algorithm to compute the probability of the additional evidence provided. Attributes Inherited from: ProbEvidenceAlgorithm

Invocation template:

```scala
receiver.probAdditionalEvidence(evidence)
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.MMAPProbEvidenceSampler.probEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP$MMAPProbEvidenceSampler.html#probEvidence-0)

```scala
def probEvidence: Double
```

The computed probability of evidence.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: The computed probability of evidence. Attributes Inherited from: ProbEvidenceAlgorithm

Invocation template:

```scala
receiver.probEvidence
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.MMAPProbEvidenceSampler.probabilityOfEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP$MMAPProbEvidenceSampler.html#probabilityOfEvidence-999)

```scala
def probabilityOfEvidence(): Double
```

Returns the probability of evidence of the universe on which the algorithm operates. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Double ``.

Source contract/attributes: Returns the probability of evidence of the universe on which the algorithm operates. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: OneTimeProbEvidence

Invocation template:

```scala
receiver.probabilityOfEvidence()
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.MMAPProbEvidenceSampler.record ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP$MMAPProbEvidenceSampler.html#record-fffff7a5)

```scala
def record(logWeight: Double): Unit
```

Record the weight in the rolling mean and variance computation.

Type parameters: none.

Parameters, list 1: `` logWeight: Double ``.

Returns: `` Unit ``.

Source contract/attributes: Record the weight in the rolling mean and variance computation. Value parameters logWeight Log of the weight to record. Attributes Inherited from: OnlineLogStatistics

Invocation template:

```scala
receiver.record(logWeight)
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.MMAPProbEvidenceSampler.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP$MMAPProbEvidenceSampler.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.MMAPProbEvidenceSampler.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP$MMAPProbEvidenceSampler.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.MMAPProbEvidenceSampler.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP$MMAPProbEvidenceSampler.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.MMAPProbEvidenceSampler.totalLogStatistics ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP$MMAPProbEvidenceSampler.html#totalLogStatistics-0)

```scala
def totalLogStatistics: LogStatistics
```

Return the combined statistics for the log probability of evidence over all runs of this sampler. If the number of observations is 0, the returned log mean is -Infinity. If the number of observations is 0 or 1, the returned log variance is NaN.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` LogStatistics ``.

Source contract/attributes: Return the combined statistics for the log probability of evidence over all runs of this sampler. If the number of observations is 0, the returned log mean is -Infinity. If the number of observations is 0 or 1, the returned log variance is NaN. Attributes Inherited from: OnlineLogStatistics

Invocation template:

```scala
receiver.totalLogStatistics
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Clean up the sampler, freeing memory.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clean up the sampler, freeing memory. Attributes Definition Classes MetropolisHastings -> Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.computeMostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP.html#computeMostLikelyValue-fffffa13)

```scala
override def computeMostLikelyValue[T](target: Element[T]): T
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Attributes Definition Classes MarginalMAPAlgorithm

Invocation template:

```scala
receiver.computeMostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.getTemperature ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP.html#getTemperature-0)

```scala
def getTemperature: Double
```

Get the current temperature. Used for debugging.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Get the current temperature. Used for debugging. Attributes

Invocation template:

```scala
receiver.getTemperature
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP.html#sample-ffffff32)

```scala
override def sample(): (Boolean, Sample)
```

Produce a single sample.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (Boolean, Sample) ``.

Source contract/attributes: Produce a single sample. Attributes Definition Classes MetropolisHastings -> BaseUnweightedSampler

Invocation template:

```scala
receiver.sample()
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.update ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP.html#update-94c)

```scala
override def update(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Definition Classes BaseUnweightedSampler -> Sampler

Invocation template:

```scala
receiver.update()
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.acceptRejectRatio ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP.html#acceptRejectRatio-0)

```scala
def acceptRejectRatio: Double
```

Get the acceptance ratio for the sampler.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Get the acceptance ratio for the sampler. Attributes Inherited from: MetropolisHastings

Invocation template:

```scala
receiver.acceptRejectRatio
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.getSampleCount ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP.html#getSampleCount-0)

```scala
def getSampleCount: Int
```

Number of samples taken

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples taken Attributes Inherited from: BaseUnweightedSampler

Invocation template:

```scala
receiver.getSampleCount
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns an estimate of the max a posteriori value of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns an estimate of the max a posteriori value of the target. Attributes Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of MAP elements. Inherited from: MarginalMAPAlgorithm

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.marginalmap.ProbEvidenceMarginalMAP.test ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/ProbEvidenceMarginalMAP.html#test-fffff570)

```scala
def test(numSamples: Int, predicates: Seq[Predicate[_]], elementsToTrack: Seq[Element[_]]): (Double, Map[Predicate[_], Double], Map[Element[_], Double])
```

Test Metropolis-Hastings by repeatedly running a single step from the same initial state. For each of a set of predicates, the fraction of times the predicate is satisfied by the resulting state is returned. By the resulting state, we mean the new state if it is accepted and the original state if not.

Type parameters: none.

Parameters, list 1: `` numSamples: Int ``; `` predicates: Seq[Predicate[_]] ``; `` elementsToTrack: Seq[Element[_]] ``.

Returns: `` (Double, Map[Predicate[_], Double], Map[Element[_], Double]) ``.

Source contract/attributes: Test Metropolis-Hastings by repeatedly running a single step from the same initial state. For each of a set of predicates, the fraction of times the predicate is satisfied by the resulting state is returned. By the resulting state, we mean the new state if it is accepted and the original state if not. Attributes Inherited from: MetropolisHastings

Invocation template:

```scala
receiver.test(numSamples, predicates, elementsToTrack)
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm.this ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPAlgorithm.html#<init>-a3e)

```scala
def this(universe: Universe, mapElements: Element[_]*)
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` universe: Universe ``; `` mapElements: Element[_]* ``.

Returns: `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm ``.

Invocation template:

```scala
new com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm(universe, mapElements*)
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm.computeMostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPAlgorithm.html#computeMostLikelyValue-fffffa13)

```scala
def computeMostLikelyValue[T](target: Element[T]): T
```

Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star. Attributes

Invocation template:

```scala
receiver.computeMostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPAlgorithm.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPAlgorithm.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPAlgorithm.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPAlgorithm.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPAlgorithm.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes StructuredAlgorithm -> Algorithm Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPAlgorithm.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPAlgorithm.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPAlgorithm.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns an estimate of the max a posteriori value of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns an estimate of the max a posteriori value of the target. Attributes Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of MAP elements. Inherited from: MarginalMAPAlgorithm

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPAlgorithm.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPAlgorithm.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPAlgorithm.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPAlgorithm.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPAlgorithm.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPAlgorithm.html#rangingStrategy-0)

```scala
def rangingStrategy: RangingStrategy
```

Strategy to use for ranging atomic components. This is only called once. Note that this is called during initialization, so subclasses that override this can declare this as a lazy val or a def but not a val.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Strategy to use for ranging atomic components. This is only called once. Note that this is called during initialization, so subclasses that override this can declare this as a lazy val or a def but not a val. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPAlgorithm.html#refiningStrategy-fffffdd6)

```scala
def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPAlgorithm.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPAlgorithm.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE$.html#apply-3a0)

```scala
def apply(mapElements: Element[_]*)(implicit universe: Universe): StructuredMarginalMAPVE
```

Create a structured variable elimination algorithm with the given query targets.

Type parameters: none.

Parameters, list 1: `` mapElements: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` StructuredMarginalMAPVE ``.

Source contract/attributes: Create a structured variable elimination algorithm with the given query targets. Value parameters mapElements Elements for which to compute MAP queries. Elements not in this list are summed over, and cannot be queried. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.apply(mapElements*)(using universe)
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE$.html#mostLikelyValue-e3e)

```scala
def mostLikelyValue[T](target: Element[T], mapElements: Element[_]*): T
```

Use variable elimination to compute the most likely value of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` mapElements: Element[_]* ``.

Returns: `` T ``.

Source contract/attributes: Use variable elimination to compute the most likely value of the given element. Value parameters mapElements Additional elements to MAP. Elements not in this list are summed over. target Element for which to compute MAP value. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.mostLikelyValue[T](target, mapElements*)
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration.

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.computeMostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE.html#computeMostLikelyValue-fffffa13)

```scala
def computeMostLikelyValue[T](target: Element[T]): T
```

Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star. Attributes Inherited from: StructuredMarginalMAPAlgorithm

Invocation template:

```scala
receiver.computeMostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.initialElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE.html#initialElements-0)

```scala
def initialElements: List[Element[_]]
```

Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe. Attributes Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialElements
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm -> Algorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns an estimate of the max a posteriori value of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns an estimate of the max a posteriori value of the target. Attributes Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of MAP elements. Inherited from: MarginalMAPAlgorithm

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredMarginalMAPAlgorithm -> StructuredAlgorithm Inherited from: StructuredMarginalMAPAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredMarginalMAPAlgorithm -> StructuredAlgorithm Inherited from: StructuredMarginalMAPAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeStructured -> StructuredAlgorithm Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE.html#refiningStrategy-fffffdd6)

```scala
override def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.experimental.marginalmap.StructuredMarginalMAPVE.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/marginalmap/StructuredMarginalMAPVE.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```
