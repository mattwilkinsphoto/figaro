<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.algorithm.structured.algorithm.hybrid

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE$.html#apply-3a3)

```scala
def apply(targets: Element[_]*): RaisingVE
```

Create a structured variable elimination algorithm with the given query targets.

Type parameters: none.

Parameters, list 1: `` targets: Element[_]* ``.

Returns: `` RaisingVE ``.

Source contract/attributes: Create a structured variable elimination algorithm with the given query targets. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.apply(targets*)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE$.html#probability-c2a)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Use VE to compute the probability that the given element satisfies the given predicate.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Use VE to compute the probability that the given element satisfies the given predicate. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE$.html#probability-f2c)

```scala
def probability[T](target: Element[T], value: T): Double
```

Use VE to compute the probability that the given element has the given value.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Use VE to compute the probability that the given element has the given value. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration.

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#computeDistribution-fffffdb4)

```scala
override def computeDistribution[T](target: Element[T]): Stream[(Double, T)]
```

Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#computeExpectation-c2a)

```scala
override def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#distribution-fffffef0)

```scala
def distribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#distribution-fffff084)

```scala
def distribution(target: List[Element[_]]): (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` target: List[Element[_]] ``.

Returns: `` (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])]) ``.

Source contract/attributes: Attributes Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.initialElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#initialElements-0)

```scala
def initialElements: List[Element[_]]
```

Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe. Attributes Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialElements
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm -> Algorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeStructured -> StructuredAlgorithm Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#refiningStrategy-fffffdd6)

```scala
override def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.RaisingVE.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/RaisingVE.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser$.html#apply-fffffe06)

```scala
def apply(scoreThreshold: Double, BPIterations: Int, targets: Element[_]*): StructuredVEBPChooser
```

Create a hybrid algorithm that chooses between variable elimination and belief propagation on each subproblem.

Type parameters: none.

Parameters, list 1: `` scoreThreshold: Double ``; `` BPIterations: Int ``; `` targets: Element[_]* ``.

Returns: `` StructuredVEBPChooser ``.

Source contract/attributes: Create a hybrid algorithm that chooses between variable elimination and belief propagation on each subproblem. Value parameters bpIterations The number of iterations to use when BP is chosen for a subproblem. scoreThreshold The minimum value of the increase in score caused by eliminating a variable that causes the hybrid algorithm to choose BP for a subproblem. targets The query targets Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.apply(scoreThreshold, BPIterations, targets*)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser$.html#probability-fffff856)

```scala
def probability[T](target: Element[T], predicate: T => Boolean, threshold: Double, iterations: Int): Double
```

Use the hybrid algorithm to compute the probability that the given element satisfies the given predicate.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``; `` threshold: Double ``; `` iterations: Int ``.

Returns: `` Double ``.

Source contract/attributes: Use the hybrid algorithm to compute the probability that the given element satisfies the given predicate. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.probability[T](target, predicate, threshold, iterations)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser$.html#probability-a94)

```scala
def probability[T](target: Element[T], value: T, threshold: Double = ..., iterations: Int = ...): Double
```

Use the hybrid algorithm to compute the probability that the given element has the given value.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``; `` threshold: Double = ... ``; `` iterations: Int = ... ``.

Returns: `` Double ``.

Source contract/attributes: Use the hybrid algorithm to compute the probability that the given element has the given value. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.probability[T](target, value, threshold, iterations)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration.

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#computeDistribution-fffffdb4)

```scala
override def computeDistribution[T](target: Element[T]): Stream[(Double, T)]
```

Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#computeExpectation-c2a)

```scala
override def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#distribution-fffffef0)

```scala
def distribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#distribution-fffff084)

```scala
def distribution(target: List[Element[_]]): (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` target: List[Element[_]] ``.

Returns: `` (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])]) ``.

Source contract/attributes: Attributes Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.initialElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#initialElements-0)

```scala
def initialElements: List[Element[_]]
```

Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe. Attributes Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialElements
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm -> Algorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeStructured -> StructuredAlgorithm Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#refiningStrategy-fffffdd6)

```scala
override def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPChooser.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPChooser.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser$.html#apply-fffff51b)

```scala
def apply(scoreThreshold: Double, determThreshold: Double, bpIters: Int, numSamples: Int, targets: Element[_]*): StructuredVEBPGibbsChooser
```

Create a hybrid algorithm that chooses between variable elimination and Gibbs sampling on each subproblem.

Type parameters: none.

Parameters, list 1: `` scoreThreshold: Double ``; `` determThreshold: Double ``; `` bpIters: Int ``; `` numSamples: Int ``; `` targets: Element[_]* ``.

Returns: `` StructuredVEBPGibbsChooser ``.

Source contract/attributes: Create a hybrid algorithm that chooses between variable elimination and Gibbs sampling on each subproblem. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.apply(scoreThreshold, determThreshold, bpIters, numSamples, targets*)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser$.html#apply-16e)

```scala
def apply(scoreThreshold: Double, determThreshold: Double, bpIters: Int, numSamples: Int, burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator, targets: Element[_]*): StructuredVEBPGibbsChooser
```

Create a hybrid algorithm that chooses between variable elimination and Gibbs sampling on each subproblem.

Type parameters: none.

Parameters, list 1: `` scoreThreshold: Double ``; `` determThreshold: Double ``; `` bpIters: Int ``; `` numSamples: Int ``; `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``; `` targets: Element[_]* ``.

Returns: `` StructuredVEBPGibbsChooser ``.

Source contract/attributes: Create a hybrid algorithm that chooses between variable elimination and Gibbs sampling on each subproblem. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.apply(scoreThreshold, determThreshold, bpIters, numSamples, burnIn, interval, blockToSampler, targets*)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser$.html#probability-fffff3dc)

```scala
def probability[T](target: Element[T], predicate: T => Boolean, threshold: Double, determThreshold: Double): Double
```

Use the hybrid algorithm to compute the probability that the given element satisfies the given predicate.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``; `` threshold: Double ``; `` determThreshold: Double ``.

Returns: `` Double ``.

Source contract/attributes: Use the hybrid algorithm to compute the probability that the given element satisfies the given predicate. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.probability[T](target, predicate, threshold, determThreshold)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser$.html#probability-fffff0de)

```scala
def probability[T](target: Element[T], value: T, threshold: Double = ..., determThreshold: Double = ...): Double
```

Use the hybrid algorithm to compute the probability that the given element has the given value.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``; `` threshold: Double = ... ``; `` determThreshold: Double = ... ``.

Returns: `` Double ``.

Source contract/attributes: Use the hybrid algorithm to compute the probability that the given element has the given value. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.probability[T](target, value, threshold, determThreshold)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration.

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#computeDistribution-fffffdb4)

```scala
override def computeDistribution[T](target: Element[T]): Stream[(Double, T)]
```

Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#computeExpectation-c2a)

```scala
override def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#distribution-fffffef0)

```scala
def distribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#distribution-fffff084)

```scala
def distribution(target: List[Element[_]]): (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` target: List[Element[_]] ``.

Returns: `` (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])]) ``.

Source contract/attributes: Attributes Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.initialElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#initialElements-0)

```scala
def initialElements: List[Element[_]]
```

Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe. Attributes Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialElements
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm -> Algorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeStructured -> StructuredAlgorithm Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#refiningStrategy-fffffdd6)

```scala
override def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEBPGibbsChooser.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEBPGibbsChooser.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser$.html#apply-fffff471)

```scala
def apply(scoreThreshold: Double, numSamples: Int, targets: Element[_]*): StructuredVEGibbsChooser
```

Create a hybrid algorithm that chooses between variable elimination and Gibbs sampling on each subproblem.

Type parameters: none.

Parameters, list 1: `` scoreThreshold: Double ``; `` numSamples: Int ``; `` targets: Element[_]* ``.

Returns: `` StructuredVEGibbsChooser ``.

Source contract/attributes: Create a hybrid algorithm that chooses between variable elimination and Gibbs sampling on each subproblem. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.apply(scoreThreshold, numSamples, targets*)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser$.html#apply-fffffa3c)

```scala
def apply(scoreThreshold: Double, numSamples: Int, burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator, targets: Element[_]*): StructuredVEGibbsChooser
```

Create a hybrid algorithm that chooses between variable elimination and Gibbs sampling on each subproblem.

Type parameters: none.

Parameters, list 1: `` scoreThreshold: Double ``; `` numSamples: Int ``; `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``; `` targets: Element[_]* ``.

Returns: `` StructuredVEGibbsChooser ``.

Source contract/attributes: Create a hybrid algorithm that chooses between variable elimination and Gibbs sampling on each subproblem. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.apply(scoreThreshold, numSamples, burnIn, interval, blockToSampler, targets*)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser$.html#probability-6c3)

```scala
def probability[T](target: Element[T], predicate: T => Boolean, threshold: Double): Double
```

Use the hybrid algorithm to compute the probability that the given element satisfies the given predicate.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``; `` threshold: Double ``.

Returns: `` Double ``.

Source contract/attributes: Use the hybrid algorithm to compute the probability that the given element satisfies the given predicate. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.probability[T](target, predicate, threshold)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser$.html#probability-fffff6c5)

```scala
def probability[T](target: Element[T], value: T, threshold: Double = ...): Double
```

Use the hybrid algorithm to compute the probability that the given element has the given value.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``; `` threshold: Double = ... ``.

Returns: `` Double ``.

Source contract/attributes: Use the hybrid algorithm to compute the probability that the given element has the given value. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.probability[T](target, value, threshold)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration.

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#computeDistribution-fffffdb4)

```scala
override def computeDistribution[T](target: Element[T]): Stream[(Double, T)]
```

Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#computeExpectation-c2a)

```scala
override def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#distribution-fffffef0)

```scala
def distribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#distribution-fffff084)

```scala
def distribution(target: List[Element[_]]): (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` target: List[Element[_]] ``.

Returns: `` (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])]) ``.

Source contract/attributes: Attributes Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.initialElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#initialElements-0)

```scala
def initialElements: List[Element[_]]
```

Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe. Attributes Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialElements
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm -> Algorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeStructured -> StructuredAlgorithm Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#refiningStrategy-fffffdd6)

```scala
override def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.hybrid.StructuredVEGibbsChooser.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/hybrid/StructuredVEGibbsChooser.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```
