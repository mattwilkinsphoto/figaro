<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.algorithm.structured.algorithm.laziness

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.depth ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#depth-0)

```scala
override def depth: Int
```

Depth to which to expand the model at the current iteration.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Depth to which to expand the model at the current iteration. Attributes Definition Classes LazyStructuredVE

Invocation template:

```scala
receiver.depth
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default lazy ranging strategy with one value per iteration.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default lazy ranging strategy with one value per iteration. Attributes Definition Classes AnytimeLazyStructured -> AnytimeStructured -> StructuredAlgorithm

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.allProbabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#allProbabilityBounds-fffffdb4)

```scala
def allProbabilityBounds[T](target: Element[T]): Stream[(Double, Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered. Value parameters target Element for which to compute bounds. Attributes Returns Bounds on the probability of each value for this element. Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.allProbabilityBounds[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#checkConstraintBounds-94c)

```scala
override def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Definition Classes LazyStructured -> StructuredAlgorithm Inherited from: LazyStructured

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.computeAllProbabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#computeAllProbabilityBounds-fffffdb4)

```scala
override def computeAllProbabilityBounds[T](target: Element[T]): Stream[(Double, Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered. Attributes Definition Classes LazyStructuredProbQueryAlgorithm -> BoundsProbQueryAlgorithm Inherited from: LazyStructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeAllProbabilityBounds[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#computeDistribution-fffffdb4)

```scala
override def computeDistribution[T](target: Element[T]): Stream[(Double, T)]
```

Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#computeExpectation-c2a)

```scala
override def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.computeExpectationBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#computeExpectationBounds-fffff300)

```scala
override def computeExpectationBounds[T](target: Element[T], function: T => Double, bounds: Option[(Double, Double)]): (Double, Double)
```

Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds, if provided. Otherwise, the lower and upper bounds of the function using the current known values of the target are used.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``; `` bounds: Option[(Double, Double)] ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds, if provided. Otherwise, the lower and upper bounds of the function using the current known values of the target are used. Attributes Definition Classes LazyStructuredProbQueryAlgorithm -> BoundsProbQueryAlgorithm Inherited from: LazyStructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectationBounds[T](target, function, bounds)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.computeProbabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#computeProbabilityBounds-1c3)

```scala
def computeProbabilityBounds[T](target: Element[T], predicate: T => Boolean): (Double, Double)
```

Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target. Attributes Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbabilityBounds[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#distribution-fffffef0)

```scala
def distribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#distribution-fffff084)

```scala
def distribution(target: List[Element[_]]): (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` target: List[Element[_]] ``.

Returns: `` (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])]) ``.

Source contract/attributes: Attributes Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.expectationBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#expectationBounds-1c3)

```scala
def expectationBounds[T](target: Element[T], function: T => Double): (Double, Double)
```

Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded according to the currently known values of the target. Thus, one should generally only use this when the range of the target is finite and known beforehand. Otherwise, one can use the overloaded version of this method that specifies explicit bounds on the function.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded according to the currently known values of the target. Thus, one should generally only use this when the range of the target is finite and known beforehand. Otherwise, one can use the overloaded version of this method that specifies explicit bounds on the function. Value parameters function Function whose expectation is computed. target Element for which to compute bounds. Attributes Returns Bounds on the expectation of this function for this element. Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.expectationBounds[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.expectationBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#expectationBounds-975)

```scala
def expectationBounds[T](target: Element[T], function: T => Double, lower: Double, upper: Double): (Double, Double)
```

Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``; `` lower: Double ``; `` upper: Double ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds. Value parameters function Function whose expectation is computed. lower Lower bound on the function. target Element for which to compute bounds. upper Upper bound on the function. Attributes Returns Bounds on the expectation of this function for this element. Throws AlgorithmInactiveException if the algorithm is inactive. IllegalArgumentException if the bounds given on the function are tighter than the actual bounds on the function, using the current known values of the target. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.expectationBounds[T](target, function, lower, upper)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.handle ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#handle-fffff940)

```scala
override def handle(service: Service): Response
```

A handler of services provided by the algorithm.

Type parameters: none.

Parameters, list 1: `` service: Service ``.

Returns: `` Response ``.

Source contract/attributes: A handler of services provided by the algorithm. Attributes Definition Classes AnytimeBoundsProbQuery -> AnytimeProbQuery -> Anytime Inherited from: AnytimeBoundsProbQuery

Invocation template:

```scala
receiver.handle(service)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes LazyStructuredVE -> StructuredAlgorithm -> Algorithm Inherited from: LazyStructuredVE

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.probabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#probabilityBounds-4c5)

```scala
def probabilityBounds[T](target: Element[T], value: T): (Double, Double)
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Attributes Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.probabilityBounds[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.probabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#probabilityBounds-1c3)

```scala
def probabilityBounds[T](target: Element[T], predicate: T => Boolean): (Double, Double)
```

Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target. Value parameters predicate Function whose probability of evaluating to true is computed. target Element for which to compute bounds. Attributes Returns Bounds on the probability of this function for this element. Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.probabilityBounds[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#refiningStrategy-fffffdd6)

```scala
override def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Definition Classes LazyStructuredVE -> StructuredAlgorithm Inherited from: LazyStructuredVE

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.shutdown ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#shutdown-0)

```scala
def shutdown: Unit
```

Release all resources from this anytime algorithm.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Unit ``.

Source contract/attributes: Release all resources from this anytime algorithm. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.shutdown
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#solvingStrategy-dd8)

```scala
override def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Definition Classes LazyStructuredVE -> StructuredAlgorithm Inherited from: LazyStructuredVE

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.stopUpdate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#stopUpdate-94c)

```scala
def stopUpdate(): Unit
```

Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Optional function to run when the algorithm is stopped (not killed). Used in samplers to update lazy values. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.stopUpdate()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.AnytimeLSVE.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/AnytimeLSVE.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.anytime ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE$.html#anytime-fffffaa0)

```scala
def anytime(depthIncrement: Int, targets: Element[_]*): AnytimeLSVE
```

Creates an anytime lazy structured variable elimination algorithm that expands the depth by the given increment at each iteration.

Type parameters: none.

Parameters, list 1: `` depthIncrement: Int ``; `` targets: Element[_]* ``.

Returns: `` AnytimeLSVE ``.

Source contract/attributes: Creates an anytime lazy structured variable elimination algorithm that expands the depth by the given increment at each iteration. Value parameters depthIncrement Depth by which to increase the expansion at each iteration. Must be positive. targets Query targets. Attributes Returns An anytime lazy structured VE algorithm.

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.anytime(depthIncrement, targets*)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE$.html#apply-93a)

```scala
def apply(depth: Int, targets: Element[_]*): OneTimeLSVE
```

Creates a one time lazy structured variable elimination algorithm that expands to the given depth.

Type parameters: none.

Parameters, list 1: `` depth: Int ``; `` targets: Element[_]* ``.

Returns: `` OneTimeLSVE ``.

Source contract/attributes: Creates a one time lazy structured variable elimination algorithm that expands to the given depth. Value parameters depth Nonnegative depth of expansion. targets Query targets. Attributes Returns A one time lazy structured VE algorithm.

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.apply(depth, targets*)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE$.html#apply-f79)

```scala
def apply(targets: Element[_]*): AnytimeLSVE
```

Creates an anytime lazy structured variable elimination algorithm that expands the depth by 1 at each iteration.

Type parameters: none.

Parameters, list 1: `` targets: Element[_]* ``.

Returns: `` AnytimeLSVE ``.

Source contract/attributes: Creates an anytime lazy structured variable elimination algorithm that expands the depth by 1 at each iteration. Value parameters targets Query targets. Attributes Returns An anytime lazy structured VE algorithm.

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.apply(targets*)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.depth ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#depth-0)

```scala
def depth: Int
```

Depth to which to expand the model at the current iteration.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Depth to which to expand the model at the current iteration. Attributes

Invocation template:

```scala
receiver.depth
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes StructuredAlgorithm -> Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#refiningStrategy-fffffdd6)

```scala
override def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Definition Classes StructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#solvingStrategy-dd8)

```scala
override def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Definition Classes StructuredAlgorithm

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.allProbabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#allProbabilityBounds-fffffdb4)

```scala
def allProbabilityBounds[T](target: Element[T]): Stream[(Double, Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered. Value parameters target Element for which to compute bounds. Attributes Returns Bounds on the probability of each value for this element. Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.allProbabilityBounds[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#checkConstraintBounds-94c)

```scala
override def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Definition Classes LazyStructured -> StructuredAlgorithm Inherited from: LazyStructured

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.computeAllProbabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#computeAllProbabilityBounds-fffffdb4)

```scala
override def computeAllProbabilityBounds[T](target: Element[T]): Stream[(Double, Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered. Attributes Definition Classes LazyStructuredProbQueryAlgorithm -> BoundsProbQueryAlgorithm Inherited from: LazyStructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeAllProbabilityBounds[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#computeDistribution-fffffdb4)

```scala
override def computeDistribution[T](target: Element[T]): Stream[(Double, T)]
```

Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#computeExpectation-c2a)

```scala
override def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.computeExpectationBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#computeExpectationBounds-fffff300)

```scala
override def computeExpectationBounds[T](target: Element[T], function: T => Double, bounds: Option[(Double, Double)]): (Double, Double)
```

Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds, if provided. Otherwise, the lower and upper bounds of the function using the current known values of the target are used.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``; `` bounds: Option[(Double, Double)] ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds, if provided. Otherwise, the lower and upper bounds of the function using the current known values of the target are used. Attributes Definition Classes LazyStructuredProbQueryAlgorithm -> BoundsProbQueryAlgorithm Inherited from: LazyStructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectationBounds[T](target, function, bounds)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.computeProbabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#computeProbabilityBounds-1c3)

```scala
def computeProbabilityBounds[T](target: Element[T], predicate: T => Boolean): (Double, Double)
```

Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target. Attributes Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbabilityBounds[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#distribution-fffffef0)

```scala
def distribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#distribution-fffff084)

```scala
def distribution(target: List[Element[_]]): (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` target: List[Element[_]] ``.

Returns: `` (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])]) ``.

Source contract/attributes: Attributes Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.expectationBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#expectationBounds-1c3)

```scala
def expectationBounds[T](target: Element[T], function: T => Double): (Double, Double)
```

Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded according to the currently known values of the target. Thus, one should generally only use this when the range of the target is finite and known beforehand. Otherwise, one can use the overloaded version of this method that specifies explicit bounds on the function.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded according to the currently known values of the target. Thus, one should generally only use this when the range of the target is finite and known beforehand. Otherwise, one can use the overloaded version of this method that specifies explicit bounds on the function. Value parameters function Function whose expectation is computed. target Element for which to compute bounds. Attributes Returns Bounds on the expectation of this function for this element. Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.expectationBounds[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.expectationBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#expectationBounds-975)

```scala
def expectationBounds[T](target: Element[T], function: T => Double, lower: Double, upper: Double): (Double, Double)
```

Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``; `` lower: Double ``; `` upper: Double ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds. Value parameters function Function whose expectation is computed. lower Lower bound on the function. target Element for which to compute bounds. upper Upper bound on the function. Attributes Returns Bounds on the expectation of this function for this element. Throws AlgorithmInactiveException if the algorithm is inactive. IllegalArgumentException if the bounds given on the function are tighter than the actual bounds on the function, using the current known values of the target. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.expectationBounds[T](target, function, lower, upper)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.probabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#probabilityBounds-4c5)

```scala
def probabilityBounds[T](target: Element[T], value: T): (Double, Double)
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Attributes Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.probabilityBounds[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.probabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#probabilityBounds-1c3)

```scala
def probabilityBounds[T](target: Element[T], predicate: T => Boolean): (Double, Double)
```

Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target. Value parameters predicate Function whose probability of evaluating to true is computed. target Element for which to compute bounds. Attributes Returns Bounds on the probability of this function for this element. Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.probabilityBounds[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.LazyStructuredVE.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/LazyStructuredVE.html#rangingStrategy-0)

```scala
def rangingStrategy: RangingStrategy
```

Strategy to use for ranging atomic components. This is only called once. Note that this is called during initialization, so subclasses that override this can declare this as a lazy val or a def but not a val.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Strategy to use for ranging atomic components. This is only called once. Note that this is called during initialization, so subclasses that override this can declare this as a lazy val or a def but not a val. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default lazy ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default lazy ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeLazyStructured -> OneTimeStructured -> StructuredAlgorithm

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.allProbabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#allProbabilityBounds-fffffdb4)

```scala
def allProbabilityBounds[T](target: Element[T]): Stream[(Double, Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered. Value parameters target Element for which to compute bounds. Attributes Returns Bounds on the probability of each value for this element. Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.allProbabilityBounds[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#checkConstraintBounds-94c)

```scala
override def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Definition Classes LazyStructured -> StructuredAlgorithm Inherited from: LazyStructured

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.computeAllProbabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#computeAllProbabilityBounds-fffffdb4)

```scala
override def computeAllProbabilityBounds[T](target: Element[T]): Stream[(Double, Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each value with its probability bounds. Each entry is a triple (lower, upper, value). The result is a lazy stream. It is up to the algorithm how the stream is ordered. Attributes Definition Classes LazyStructuredProbQueryAlgorithm -> BoundsProbQueryAlgorithm Inherited from: LazyStructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeAllProbabilityBounds[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#computeDistribution-fffffdb4)

```scala
override def computeDistribution[T](target: Element[T]): Stream[(Double, T)]
```

Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#computeExpectation-c2a)

```scala
override def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.computeExpectationBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#computeExpectationBounds-fffff300)

```scala
override def computeExpectationBounds[T](target: Element[T], function: T => Double, bounds: Option[(Double, Double)]): (Double, Double)
```

Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds, if provided. Otherwise, the lower and upper bounds of the function using the current known values of the target are used.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``; `` bounds: Option[(Double, Double)] ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds, if provided. Otherwise, the lower and upper bounds of the function using the current known values of the target are used. Attributes Definition Classes LazyStructuredProbQueryAlgorithm -> BoundsProbQueryAlgorithm Inherited from: LazyStructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectationBounds[T](target, function, bounds)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.computeProbabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#computeProbabilityBounds-1c3)

```scala
def computeProbabilityBounds[T](target: Element[T], predicate: T => Boolean): (Double, Double)
```

Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target. Attributes Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbabilityBounds[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#distribution-fffffef0)

```scala
def distribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#distribution-fffff084)

```scala
def distribution(target: List[Element[_]]): (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` target: List[Element[_]] ``.

Returns: `` (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])]) ``.

Source contract/attributes: Attributes Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.expectationBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#expectationBounds-1c3)

```scala
def expectationBounds[T](target: Element[T], function: T => Double): (Double, Double)
```

Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded according to the currently known values of the target. Thus, one should generally only use this when the range of the target is finite and known beforehand. Otherwise, one can use the overloaded version of this method that specifies explicit bounds on the function.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded according to the currently known values of the target. Thus, one should generally only use this when the range of the target is finite and known beforehand. Otherwise, one can use the overloaded version of this method that specifies explicit bounds on the function. Value parameters function Function whose expectation is computed. target Element for which to compute bounds. Attributes Returns Bounds on the expectation of this function for this element. Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.expectationBounds[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.expectationBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#expectationBounds-975)

```scala
def expectationBounds[T](target: Element[T], function: T => Double, lower: Double, upper: Double): (Double, Double)
```

Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``; `` lower: Double ``; `` upper: Double ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the bounds on the expectation of the function under the marginal probability distribution of the target. The function is assumed to be bounded between the specified lower and upper bounds. Value parameters function Function whose expectation is computed. lower Lower bound on the function. target Element for which to compute bounds. upper Upper bound on the function. Attributes Returns Bounds on the expectation of this function for this element. Throws AlgorithmInactiveException if the algorithm is inactive. IllegalArgumentException if the bounds given on the function are tighter than the actual bounds on the function, using the current known values of the target. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.expectationBounds[T](target, function, lower, upper)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes LazyStructuredVE -> StructuredAlgorithm -> Algorithm Inherited from: LazyStructuredVE

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.probabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#probabilityBounds-4c5)

```scala
def probabilityBounds[T](target: Element[T], value: T): (Double, Double)
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Attributes Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.probabilityBounds[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.probabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#probabilityBounds-1c3)

```scala
def probabilityBounds[T](target: Element[T], predicate: T => Boolean): (Double, Double)
```

Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Return an estimate of the probability of the bounds on the predicate under the marginal probability distribution of the target. Value parameters predicate Function whose probability of evaluating to true is computed. target Element for which to compute bounds. Attributes Returns Bounds on the probability of this function for this element. Throws AlgorithmInactiveException if the algorithm is inactive. NotATargetException if called on a target that is not in the list of targets of the algorithm. Inherited from: BoundsProbQueryAlgorithm

Invocation template:

```scala
receiver.probabilityBounds[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#refiningStrategy-fffffdd6)

```scala
override def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Definition Classes LazyStructuredVE -> StructuredAlgorithm Inherited from: LazyStructuredVE

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#solvingStrategy-dd8)

```scala
override def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration. Definition Classes LazyStructuredVE -> StructuredAlgorithm Inherited from: LazyStructuredVE

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.laziness.OneTimeLSVE.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/laziness/OneTimeLSVE.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```
