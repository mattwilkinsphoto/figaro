<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.algorithm.sampling.parallel

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.algorithm.sampling.parallel.ParAnytime.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParAnytime.html#runStep-94c)

```scala
def runStep(): Unit
```

Run one step of the algorithm on each thread.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run one step of the algorithm on each thread. Attributes

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParAnytime.shutdown ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParAnytime.html#shutdown-0)

```scala
def shutdown: Unit
```

Release all resources from this anytime algorithm.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Unit ``.

Source contract/attributes: Release all resources from this anytime algorithm. Attributes

Invocation template:

```scala
receiver.shutdown
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParAnytime.stopUpdate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParAnytime.html#stopUpdate-94c)

```scala
def stopUpdate(): Unit
```

Call stopUpdate on algorithms in all threads.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Call stopUpdate on algorithms in all threads. Attributes

Invocation template:

```scala
receiver.stopUpdate()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParAnytime.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParAnytime.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Calls cleanUp() on all algorithms.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Calls cleanUp() on all algorithms. Attributes Definition Classes ParSamplingAlgorithm -> Algorithm Inherited from: ParSamplingAlgorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParAnytime.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParAnytime.html#initialize-94c)

```scala
override def initialize(): Unit
```

Calls initialize() on all algorithms.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Calls initialize() on all algorithms. Attributes Definition Classes ParSamplingAlgorithm -> Algorithm Inherited from: ParSamplingAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParAnytime.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParAnytime.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParAnytime.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParAnytime.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParAnytime.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParAnytime.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParAnytime.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParAnytime.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParAnytime.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParAnytime.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParImportance.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParImportance$.html#apply-62f)

```scala
def apply(generator: () => Universe, numThreads: Int, targets: Reference[_]*): ParSampler & ParAnytime { Unsupported[Not supported type in refinement AppliedType(TypeRef(TermRef(ThisType(TypeRef(NoPrefix,module class collection)),object parallel),ParSeq),List(AndType(TypeRef(TermRef(ThisType(TypeRef(NoPrefix,module class algorithm)),object sampling),Importance),TypeRef(TermRef(ThisType(TypeRef(NoPrefix,module class algorithm)),object sampling),AnytimeProbQuerySampler))))]; }
```

Create a parallel anytime importance sampler with the given target query references.

Type parameters: none.

Parameters, list 1: `` generator: () => Universe ``; `` numThreads: Int ``; `` targets: Reference[_]* ``.

Returns: `` ParSampler & ParAnytime { Unsupported[Not supported type in refinement AppliedType(TypeRef(TermRef(ThisType(TypeRef(NoPrefix,module class collection)),object parallel),ParSeq),List(AndType(TypeRef(TermRef(ThisType(TypeRef(NoPrefix,module class algorithm)),object sampling),Importance),TypeRef(TermRef(ThisType(TypeRef(NoPrefix,module class algorithm)),object sampling),AnytimeProbQuerySampler))))]; } ``.

Source contract/attributes: Create a parallel anytime importance sampler with the given target query references. Value parameters generator a function that returns a universe, with any evidence applied numThreads the number of threads to spawn targets references to the target elements Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.sampling.parallel.ParImportance.apply(generator, numThreads, targets*)
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParImportance.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParImportance$.html#apply-5b6)

```scala
def apply(generator: () => Universe, numThreads: Int, numSamples: Int, targets: Reference[_]*): ParSampler & ParOneTime & ProbEvidenceQuery { Unsupported[Not supported type in refinement AppliedType(TypeRef(TermRef(ThisType(TypeRef(NoPrefix,module class collection)),object parallel),ParSeq),List(AndType(AndType(TypeRef(TermRef(ThisType(TypeRef(NoPrefix,module class algorithm)),object sampling),Importance),TypeRef(TermRef(ThisType(TypeRef(NoPrefix,module class algorithm)),object sampling),OneTimeProbQuerySampler)),TypeRef(TermRef(ThisType(TypeRef(NoPrefix,module class figaro)),object algorithm),ProbEvidenceQuery))))]; }
```

Create a parallel one-time importance sampler with the given target query references using the given number of samples.

Type parameters: none.

Parameters, list 1: `` generator: () => Universe ``; `` numThreads: Int ``; `` numSamples: Int ``; `` targets: Reference[_]* ``.

Returns: `` ParSampler & ParOneTime & ProbEvidenceQuery { Unsupported[Not supported type in refinement AppliedType(TypeRef(TermRef(ThisType(TypeRef(NoPrefix,module class collection)),object parallel),ParSeq),List(AndType(AndType(TypeRef(TermRef(ThisType(TypeRef(NoPrefix,module class algorithm)),object sampling),Importance),TypeRef(TermRef(ThisType(TypeRef(NoPrefix,module class algorithm)),object sampling),OneTimeProbQuerySampler)),TypeRef(TermRef(ThisType(TypeRef(NoPrefix,module class figaro)),object algorithm),ProbEvidenceQuery))))]; } ``.

Source contract/attributes: Create a parallel one-time importance sampler with the given target query references using the given number of samples. Value parameters generator a function that returns a universe, with any evidence applied numSamples the number of samples to take, total, across the threads numThreads the number of threads to spawn targets references to the target elements Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.sampling.parallel.ParImportance.apply(generator, numThreads, numSamples, targets*)
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParImportance.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParImportance$.html#probability-892)

```scala
def probability[T](generator: () => Universe, numThreads: Int, target: Reference[T], predicate: T => Boolean): Double
```

Use parallel IS to compute the probability that the given reference element satisfies the given predicate.

Type parameters: `` T ``.

Parameters, list 1: `` generator: () => Universe ``; `` numThreads: Int ``; `` target: Reference[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Use parallel IS to compute the probability that the given reference element satisfies the given predicate. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.sampling.parallel.ParImportance.probability[T](generator, numThreads, target, predicate)
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParImportance.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParImportance$.html#probability-fc4)

```scala
def probability[T](generator: () => Universe, numThreads: Int, target: Reference[T], value: T): Double
```

Use parallel IS to compute the probability that the given reference element has the given value.

Type parameters: `` T ``.

Parameters, list 1: `` generator: () => Universe ``; `` numThreads: Int ``; `` target: Reference[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Use parallel IS to compute the probability that the given reference element has the given value. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.sampling.parallel.ParImportance.probability[T](generator, numThreads, target, value)
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParOneTime.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParOneTime.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithms in parallel, performing their computations to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithms in parallel, performing their computations to completion. Attributes Definition Classes OneTime

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParOneTime.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParOneTime.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Calls cleanUp() on all algorithms.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Calls cleanUp() on all algorithms. Attributes Definition Classes ParSamplingAlgorithm -> Algorithm Inherited from: ParSamplingAlgorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParOneTime.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParOneTime.html#initialize-94c)

```scala
override def initialize(): Unit
```

Calls initialize() on all algorithms.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Calls initialize() on all algorithms. Attributes Definition Classes ParSamplingAlgorithm -> Algorithm Inherited from: ParSamplingAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParOneTime.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParOneTime.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParOneTime.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParOneTime.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParOneTime.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParOneTime.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParOneTime.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParOneTime.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParOneTime.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParOneTime.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSampler.getTotalWeight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSampler.html#getTotalWeight-0)

```scala
def getTotalWeight: Double
```

Log sum of total weights of individual algorithms *

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Log sum of total weights of individual algorithms * Attributes

Invocation template:

```scala
receiver.getTotalWeight
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSampler.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSampler.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Calls cleanUp() on all algorithms.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Calls cleanUp() on all algorithms. Attributes Definition Classes ParSamplingAlgorithm -> Algorithm Inherited from: ParSamplingAlgorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSampler.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSampler.html#computeDistribution-fffff1a2)

```scala
def computeDistribution[T](target: Reference[T]): LazyList[(Double, T)]
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Reference[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQuerySampler

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSampler.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSampler.html#computeExpectation-fffffa66)

```scala
def computeExpectation[T](target: Reference[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Reference[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQuerySampler

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSampler.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSampler.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Reference[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Reference[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSampler.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSampler.html#distribution-fffff1a2)

```scala
def distribution[T](target: Reference[T]): LazyList[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Reference[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSampler.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSampler.html#expectation-579)

```scala
def expectation[T](target: Reference[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Reference[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSampler.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSampler.html#expectation-fffffa66)

```scala
def expectation[T](target: Reference[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Reference[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSampler.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSampler.html#initialize-94c)

```scala
override def initialize(): Unit
```

Calls initialize() on all algorithms.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Calls initialize() on all algorithms. Attributes Definition Classes ParSamplingAlgorithm -> Algorithm Inherited from: ParSamplingAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSampler.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSampler.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSampler.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSampler.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSampler.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSampler.html#mean-fffffeac)

```scala
def mean(target: Reference[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Reference[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSampler.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSampler.html#probability-870)

```scala
def probability[T](target: Reference[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Reference[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSampler.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSampler.html#probability-579)

```scala
def probability[T](target: Reference[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Reference[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSampler.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSampler.html#probability-fffffa66)

```scala
def probability[T](target: Reference[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Reference[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSampler.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSampler.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSampler.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSampler.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSampler.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSampler.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSampler.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSampler.html#variance-fffffeac)

```scala
def variance(target: Reference[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Reference[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSamplingAlgorithm.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSamplingAlgorithm.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Calls cleanUp() on all algorithms.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Calls cleanUp() on all algorithms. Attributes Definition Classes Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSamplingAlgorithm.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSamplingAlgorithm.html#initialize-94c)

```scala
override def initialize(): Unit
```

Calls initialize() on all algorithms.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Calls initialize() on all algorithms. Attributes Definition Classes Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSamplingAlgorithm.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSamplingAlgorithm.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSamplingAlgorithm.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSamplingAlgorithm.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSamplingAlgorithm.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSamplingAlgorithm.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSamplingAlgorithm.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSamplingAlgorithm.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.sampling.parallel.ParSamplingAlgorithm.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/sampling/parallel/ParSamplingAlgorithm.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```
