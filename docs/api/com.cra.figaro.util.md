<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.util

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.util.ColorGradient.buildGradientMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ColorGradient.html#buildGradientMap-0)

```scala
def buildGradientMap: Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Unit ``.

Invocation template:

```scala
receiver.buildGradientMap
```

## `` com.cra.figaro.util.ColorGradient.currentGradient ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ColorGradient.html#currentGradient-0)

```scala
def currentGradient: List[ColorPoint]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[ColorPoint] ``.

Invocation template:

```scala
receiver.currentGradient
```

## `` com.cra.figaro.util.ColorGradient.getColorAtValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ColorGradient.html#getColorAtValue-3d1)

```scala
def getColorAtValue(value: Float): Option[ColorPoint]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` value: Float ``.

Returns: `` Option[ColorPoint] ``.

Invocation template:

```scala
receiver.getColorAtValue(value)
```

## `` com.cra.figaro.util.ColorGradient.heatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ColorGradient.html#heatMap-0)

```scala
def heatMap: List[ColorPoint]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[ColorPoint] ``.

Invocation template:

```scala
receiver.heatMap
```

## `` com.cra.figaro.util.ColorGradient.monochromeMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ColorGradient.html#monochromeMap-0)

```scala
def monochromeMap: List[ColorPoint]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[ColorPoint] ``.

Invocation template:

```scala
receiver.monochromeMap
```

## `` com.cra.figaro.util.ColorGradient.setGradient ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ColorGradient.html#setGradient-571)

```scala
def setGradient(gradient: String): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` gradient: String ``.

Returns: `` Unit ``.

Invocation template:

```scala
receiver.setGradient(gradient)
```

## `` com.cra.figaro.util.ColorGradient.singleColor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ColorGradient.html#singleColor-0)

```scala
def singleColor: List[ColorPoint]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[ColorPoint] ``.

Invocation template:

```scala
receiver.singleColor
```

## `` com.cra.figaro.util.ColorGradient.twoColorMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ColorGradient.html#twoColorMap-0)

```scala
def twoColorMap: List[ColorPoint]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[ColorPoint] ``.

Invocation template:

```scala
receiver.twoColorMap
```

## `` com.cra.figaro.util.ColorPoint.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ColorPoint.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.util.ColorPoint.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ColorPoint.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.util.HashMultiSet.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet$.html#apply-fffffd94)

```scala
def apply[T](elems: T*): HashMultiSet[T]
```

Create a new HashMultiSet and adds the given values.

Type parameters: `` T ``.

Parameters, list 1: `` elems: T* ``.

Returns: `` HashMultiSet[T] ``.

Source contract/attributes: Create a new HashMultiSet and adds the given values. Attributes

Invocation template:

```scala
com.cra.figaro.util.HashMultiSet.apply[T](elems*)
```

## `` com.cra.figaro.util.HashMultiSet.addMany ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#addMany-9ec)

```scala
def addMany(t: T, count: Int): HashMultiSet[T]
```

Add several instances of this value to the set.

Type parameters: none.

Parameters, list 1: `` t: T ``; `` count: Int ``.

Returns: `` HashMultiSet[T] ``.

Source contract/attributes: Add several instances of this value to the set. Attributes

Invocation template:

```scala
receiver.addMany(t, count)
```

## `` com.cra.figaro.util.HashMultiSet.addOne ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#addOne-c8b)

```scala
def addOne(t: T): HashMultiSet[T]
```

Add an instance of this value to the set.

Type parameters: none.

Parameters, list 1: `` t: T ``.

Returns: `` HashMultiSet[T] ``.

Source contract/attributes: Add an instance of this value to the set. Attributes

Invocation template:

```scala
receiver.addOne(t)
```

## `` com.cra.figaro.util.HashMultiSet.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#apply-fffffaf4)

```scala
def apply(t: T): Int
```

Return the number of instances of the value in the set.

Type parameters: none.

Parameters, list 1: `` t: T ``.

Returns: `` Int ``.

Source contract/attributes: Return the number of instances of the value in the set. Attributes

Invocation template:

```scala
receiver.apply(t)
```

## `` com.cra.figaro.util.HashMultiSet.counts ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#counts-0)

```scala
def counts: Set[(T, Int)]
```

Creates a list of (element, count) pairs. Order is arbitrary.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[(T, Int)] ``.

Source contract/attributes: Creates a list of (element, count) pairs. Order is arbitrary. Attributes

Invocation template:

```scala
receiver.counts
```

## `` com.cra.figaro.util.HashMultiSet.elements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#elements-0)

```scala
def elements: List[T]
```

Creates a list of elements, where each element may appear multiple times. Order is arbitrary.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[T] ``.

Source contract/attributes: Creates a list of elements, where each element may appear multiple times. Order is arbitrary. Attributes

Invocation template:

```scala
receiver.elements
```

## `` com.cra.figaro.util.HashMultiSet.foreach ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#foreach-58c)

```scala
override def foreach[U](fn: T => U): Unit
```

Iterate over instances in this set and apply the given function.

Type parameters: `` U ``.

Parameters, list 1: `` fn: T => U ``.

Returns: `` Unit ``.

Source contract/attributes: Iterate over instances in this set and apply the given function. Attributes Definition Classes MultiSet -> IterableOnceOps

Invocation template:

```scala
receiver.foreach[U](fn)
```

## `` com.cra.figaro.util.HashMultiSet.iterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#iterator-0)

```scala
def iterator: Iterator[T]
```

An scala.collection.Iterator over the elements of this iterable collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[T] ``.

Source contract/attributes: An scala.collection.Iterator over the elements of this iterable collection. If an IterableOnce object is in fact an scala.collection.Iterator, this method always returns itself, in its current state, but if it is an scala.collection.Iterable, this method always returns a new scala.collection.Iterator. Attributes Returns an iterator over all elements of this iterable collection

Invocation template:

```scala
receiver.iterator
```

## `` com.cra.figaro.util.HashMultiSet.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#map-b38)

```scala
override def map[U](fn: T => U): HashMultiSet[U]
```

Map this set to another set by applying the supplied function.

Type parameters: `` U ``.

Parameters, list 1: `` fn: T => U ``.

Returns: `` HashMultiSet[U] ``.

Source contract/attributes: Map this set to another set by applying the supplied function. Attributes Definition Classes MultiSet -> IterableOps -> IterableOnceOps

Invocation template:

```scala
receiver.map[U](fn)
```

## `` com.cra.figaro.util.HashMultiSet.removeAll ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#removeAll-c8b)

```scala
def removeAll(t: T): HashMultiSet[T]
```

Remove all instances of this value from the set.

Type parameters: none.

Parameters, list 1: `` t: T ``.

Returns: `` HashMultiSet[T] ``.

Source contract/attributes: Remove all instances of this value from the set. Attributes

Invocation template:

```scala
receiver.removeAll(t)
```

## `` com.cra.figaro.util.HashMultiSet.removeOne ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#removeOne-c8b)

```scala
def removeOne(t: T): HashMultiSet[T]
```

Remove one instance of this value from the set.

Type parameters: none.

Parameters, list 1: `` t: T ``.

Returns: `` HashMultiSet[T] ``.

Source contract/attributes: Remove one instance of this value from the set. Attributes

Invocation template:

```scala
receiver.removeOne(t)
```

## `` com.cra.figaro.util.HashMultiSet.union ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#union-d94)

```scala
def union(that: MultiSet[T]): MultiSet[T]
```

Return the union of another set with this set.

Type parameters: none.

Parameters, list 1: `` that: MultiSet[T] ``.

Returns: `` MultiSet[T] ``.

Source contract/attributes: Return the union of another set with this set. Attributes

Invocation template:

```scala
receiver.union(that)
```

## `` com.cra.figaro.util.HashMultiSet.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#++-1dd)

```scala
final def ++[B >: T](suffix: IterableOnce[B]): Iterable[B]
```

Alias for concat.

Type parameters: `` B >: T ``.

Parameters, list 1: `` suffix: IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Alias for concat. Type parameters B the element type of the returned collection Value parameters suffix the iterable to append to this iterable collection Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.++[B](suffix)
```

## `` com.cra.figaro.util.HashMultiSet.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#addString-ff4)

```scala
final def addString(b: StringBuilder): b.type
```

Appends all elements of this iterable collection to a string builder. The written text consists of the string representations (w.r.t. the method toString) of all elements of this iterable collection without any separator string.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this iterable collection to a string builder. The written text consists of the string representations (w.r.t. the method toString) of all elements of this iterable collection without any separator string. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() val h = a.addString(b) // 1234 Value parameters b the string builder to which elements are appended. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b)
```

## `` com.cra.figaro.util.HashMultiSet.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#addString-e99)

```scala
final def addString(b: StringBuilder, sep: String): b.type
```

Appends all elements of this iterable collection to a string builder using a separator string. The written text consists of the string representations (w.r.t. the method toString) of all elements of this iterable collection, separated by the string sep.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``; `` sep: String ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this iterable collection to a string builder using a separator string. The written text consists of the string representations (w.r.t. the method toString) of all elements of this iterable collection, separated by the string sep. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() a.addString(b, ", ") // 1, 2, 3, 4 Value parameters b the string builder to which elements are appended. sep the separator string. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b, sep)
```

## `` com.cra.figaro.util.HashMultiSet.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#addString-de3)

```scala
def addString(b: StringBuilder, start: String, sep: String, end: String): b.type
```

Appends all elements of this iterable collection to a string builder using start, end, and separator strings. The written text begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this iterable collection are separated by the string sep.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``; `` start: String ``; `` sep: String ``; `` end: String ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this iterable collection to a string builder using start, end, and separator strings. The written text begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this iterable collection are separated by the string sep. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() a.addString(b , "List(" , ", " , ")") // List(1, 2, 3, 4) Value parameters b the string builder to which elements are appended. end the ending string. sep the separator string. start the starting string. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b, start, sep, end)
```

## `` com.cra.figaro.util.HashMultiSet.collect ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#collect-fffff5b3)

```scala
def collect[B](pf: PartialFunction[T, B]): Iterable[B]
```

Builds a new iterable collection by applying a partial function to all elements of this iterable collection on which the function is defined.

Type parameters: `` B ``.

Parameters, list 1: `` pf: PartialFunction[T, B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Builds a new iterable collection by applying a partial function to all elements of this iterable collection on which the function is defined. Type parameters B the element type of the returned iterable collection. Value parameters pf the partial function which filters and maps the iterable collection. Attributes Returns a new iterable collection resulting from applying the given partial function pf to each element on which it is defined and collecting the results. The order of the elements is preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.collect[B](pf)
```

## `` com.cra.figaro.util.HashMultiSet.collectFirst ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#collectFirst-25d)

```scala
def collectFirst[B](pf: PartialFunction[T, B]): Option[B]
```

Finds the first element of the iterable collection for which the given partial function is defined, and applies the partial function to it.

Type parameters: `` B ``.

Parameters, list 1: `` pf: PartialFunction[T, B] ``.

Returns: `` Option[B] ``.

Source contract/attributes: Finds the first element of the iterable collection for which the given partial function is defined, and applies the partial function to it. Note: may not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Type parameters B the result type of the partial function Value parameters pf the partial function Attributes Returns an option value containing pf applied to the first value for which it is defined, or None if none exists. Example Seq("a", 1, 5L).collectFirst { case x: Int => x*10 } = Some(10) Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.collectFirst[B](pf)
```

## `` com.cra.figaro.util.HashMultiSet.concat ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#concat-1dd)

```scala
def concat[B >: T](suffix: IterableOnce[B]): Iterable[B]
```

Returns a new iterable collection containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the iterable collection is the most specific superclass encompassing the element types of the two operands.

Type parameters: `` B >: T ``.

Parameters, list 1: `` suffix: IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Returns a new iterable collection containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the iterable collection is the most specific superclass encompassing the element types of the two operands. Type parameters B the element type of the returned collection. Value parameters suffix the iterable to append. Attributes Returns a new iterable collection which contains all elements of this iterable collection followed by all elements of suffix. Inherited from: IterableOps

Invocation template:

```scala
receiver.concat[B](suffix)
```

## `` com.cra.figaro.util.HashMultiSet.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#copyToArray-fffffd03)

```scala
def copyToArray[B >: T](dest: Array[B], start: Int, n: Int): Int
```

Copies elements to an array and returns the number of elements written.

Type parameters: `` B >: T ``.

Parameters, list 1: `` dest: Array[B] ``; `` start: Int ``; `` n: Int ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array and returns the number of elements written. Fills the given array dest starting at index start with at most n elements of this iterable collection. Copying will stop once either all the elements of this iterable collection have been copied, or the end of the array is reached, or n elements have been copied. If start is less than zero, it is taken as zero. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. n the maximal number of elements to copy. start the starting index of xs. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest, start, n)
```

## `` com.cra.figaro.util.HashMultiSet.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#copyToArray-fffffaa4)

```scala
def copyToArray[B >: T](dest: Array[B], start: Int): Int
```

Copies elements to an array, returning the number of elements written.

Type parameters: `` B >: T ``.

Parameters, list 1: `` dest: Array[B] ``; `` start: Int ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array, returning the number of elements written. Fills the given array dest starting at index start with values of this iterable collection. Copying will stop once either all the elements of this iterable collection have been copied, or the end of the array is reached. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. start the starting index of xs. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest, start)
```

## `` com.cra.figaro.util.HashMultiSet.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#copyToArray-fffff3e3)

```scala
def copyToArray[B >: T](dest: Array[B]): Int
```

Copies elements to an array, returning the number of elements written.

Type parameters: `` B >: T ``.

Parameters, list 1: `` dest: Array[B] ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array, returning the number of elements written. Fills the given array dest starting at index start with values of this iterable collection. Copying will stop once either all the elements of this iterable collection have been copied, or the end of the array is reached. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest)
```

## `` com.cra.figaro.util.HashMultiSet.corresponds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#corresponds-958)

```scala
def corresponds[B](that: IterableOnce[B])(p: (T, B) => Boolean): Boolean
```

Tests whether every element of this collection's iterator relates to the corresponding element of another collection by satisfying a test predicate.

Type parameters: `` B ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Parameters, list 2: `` p: (T, B) => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether every element of this collection's iterator relates to the corresponding element of another collection by satisfying a test predicate. Note: will not terminate for infinite-sized collections. Type parameters B the type of the elements of that Value parameters p the test predicate, which relates elements from both collections that the other collection Attributes Returns true if both collections have the same length and p(x, y) is true for all corresponding elements x of this iterator and y of that, otherwise false Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.corresponds[B](that)(p)
```

## `` com.cra.figaro.util.HashMultiSet.count ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#count-db6)

```scala
def count(p: T => Boolean): Int
```

Counts the number of elements in the iterable collection which satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Int ``.

Source contract/attributes: Counts the number of elements in the iterable collection which satisfy a predicate. Note: will not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns the number of elements satisfying the predicate p. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.count(p)
```

## `` com.cra.figaro.util.HashMultiSet.drop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#drop-aba)

```scala
def drop(n: Int): Iterable[T]
```

Selects all elements except the first n ones.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Selects all elements except the first n ones. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to drop from this iterable collection. Attributes Returns a iterable collection consisting of all elements of this iterable collection except the first n ones, or else the empty iterable collection, if this iterable collection has less than n elements. If n is negative, don't drop any elements. Inherited from: IterableOps

Invocation template:

```scala
receiver.drop(n)
```

## `` com.cra.figaro.util.HashMultiSet.dropRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#dropRight-aba)

```scala
def dropRight(n: Int): Iterable[T]
```

Selects all elements except last n ones.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Selects all elements except last n ones. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to drop from this iterable collection. Attributes Returns a iterable collection consisting of all elements of this iterable collection except the last n ones, or else the empty iterable collection, if this iterable collection has less than n elements. If n is negative, don't drop any elements. Inherited from: IterableOps

Invocation template:

```scala
receiver.dropRight(n)
```

## `` com.cra.figaro.util.HashMultiSet.dropWhile ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#dropWhile-c24)

```scala
def dropWhile(p: T => Boolean): Iterable[T]
```

Selects all elements except the longest prefix that satisfies a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Selects all elements except the longest prefix that satisfies a predicate. The matching prefix starts with the first element of this iterable collection, and the element following the prefix is the first element that does not satisfy the predicate. The matching prefix may be empty, so that this method returns the entire iterable collection. Example: List(1, 2, 3, 100, 4).dropWhile(n => n < 10) // List(100, 4) List(1, 2, 3, 100, 4).dropWhile(n => n == 0) // List(1, 2, 3, 100, 4) Use span to obtain both the prefix and suffix. Use filterNot to drop all elements that satisfy the predicate. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p The predicate used to test elements. Attributes Returns the longest suffix of this iterable collection whose first element does not satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.dropWhile(p)
```

## `` com.cra.figaro.util.HashMultiSet.empty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#empty-0)

```scala
override def empty: Iterable[T]
```

The empty iterable collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: The empty iterable collection. Attributes Returns an empty iterable of type Iterable. Definition Classes IterableFactoryDefaults -> IterableOps Inherited from: IterableFactoryDefaults

Invocation template:

```scala
receiver.empty
```

## `` com.cra.figaro.util.HashMultiSet.equals ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#equals-4ad)

```scala
override def equals(that: Any): Boolean
```

Returns true if the counts are the same.

Type parameters: none.

Parameters, list 1: `` that: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Returns true if the counts are the same. Attributes Definition Classes MultiSet -> Any Inherited from: MultiSet

Invocation template:

```scala
receiver.equals(that)
```

## `` com.cra.figaro.util.HashMultiSet.exists ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#exists-fffff66f)

```scala
def exists(p: T => Boolean): Boolean
```

Tests whether a predicate holds for at least one element of this iterable collection.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether a predicate holds for at least one element of this iterable collection. Note: may not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns true if the given predicate p is satisfied by at least one element of this iterable collection, otherwise false Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.exists(p)
```

## `` com.cra.figaro.util.HashMultiSet.filter ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#filter-c24)

```scala
def filter(pred: T => Boolean): Iterable[T]
```

Selects all elements of this iterable collection which satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` pred: T => Boolean ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Selects all elements of this iterable collection which satisfy a predicate. Value parameters p the predicate used to test elements. Attributes Returns a new iterable collection consisting of all elements of this iterable collection that satisfy the given predicate p. The order of the elements is preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.filter(pred)
```

## `` com.cra.figaro.util.HashMultiSet.filterNot ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#filterNot-c24)

```scala
def filterNot(pred: T => Boolean): Iterable[T]
```

Selects all elements of this iterable collection which do not satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` pred: T => Boolean ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Selects all elements of this iterable collection which do not satisfy a predicate. Value parameters pred the predicate used to test elements. Attributes Returns a new iterable collection consisting of all elements of this iterable collection that do not satisfy the given predicate pred. Their order may not be preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.filterNot(pred)
```

## `` com.cra.figaro.util.HashMultiSet.find ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#find-94e)

```scala
def find(p: T => Boolean): Option[T]
```

Finds the first element of the iterable collection satisfying a predicate, if any.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the first element of the iterable collection satisfying a predicate, if any. Note: may not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the predicate used to test elements. Attributes Returns an option value containing the first element in the iterable collection that satisfies p, or None if none exists. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.find(p)
```

## `` com.cra.figaro.util.HashMultiSet.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#flatMap-5d3)

```scala
def flatMap[B](f: T => IterableOnce[B]): Iterable[B]
```

Builds a new iterable collection by applying a function to all elements of this iterable collection and using the elements of the resulting collections.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Builds a new iterable collection by applying a function to all elements of this iterable collection and using the elements of the resulting collections. For example: def getWords(lines: Seq[String]): Seq[String] = lines.flatMap(line => line.split("\\W+")) The type of the resulting collection is guided by the static type of this iterable collection. This might cause unexpected results sometimes. For example: // lettersOf will return a Seq[Char] of likely repeated letters, instead of a Set def lettersOf(words: Seq[String]) = words.flatMap(word => word.toSet) // lettersOf2 will return a Set[Char], not a Seq def lettersOf2(words: Seq[String]) = words.toSet.flatMap(word => word.toSeq) // xs will be an Iterable[Int] val xs = Map("a" -> List(11, 111), "b" -> List(22, 222)).flatMap(_._2) // ys will be a Map[Int, Int] val ys = Map("a" -> List(1 -> 11, 1 -> 111), "b" -> List(2 -> 22, 2 -> 222)).flatMap(_._2) Type parameters B the element type of the returned collection. Value parameters f the function to apply to each element. Attributes Returns a new iterable collection resulting from applying the given collection-valued function f to each element of this iterable collection and concatenating the results. Inherited from: IterableOps

Invocation template:

```scala
receiver.flatMap[B](f)
```

## `` com.cra.figaro.util.HashMultiSet.flatten ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#flatten-5d3)

```scala
def flatten[B](implicit asIterable: T => IterableOnce[B]): Iterable[B]
```

Given that the elements of this collection are themselves iterable collections, converts this iterable collection into a iterable collection comprising the elements of these iterable collections.

Type parameters: `` B ``.

Context parameters, list 1: `` asIterable: T => IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Given that the elements of this collection are themselves iterable collections, converts this iterable collection into a iterable collection comprising the elements of these iterable collections. The resulting collection's type will be guided by the type of iterable collection. For example: val xs = List( Set(1, 2, 3), Set(1, 2, 3) ).flatten // xs == List(1, 2, 3, 1, 2, 3) val ys = Set( List(1, 2, 3), List(3, 2, 1) ).flatten // ys == Set(1, 2, 3) Type parameters B the type of the elements of each iterable collection. Value parameters asIterable an implicit conversion which asserts that the element type of this iterable collection is an Iterable. Attributes Returns a new iterable collection resulting from concatenating all element collections. Inherited from: IterableOps

Invocation template:

```scala
receiver.flatten[B](using asIterable)
```

## `` com.cra.figaro.util.HashMultiSet.fold ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#fold-fffff9e1)

```scala
def fold[A1 >: T](z: A1)(op: (A1, A1) => A1): A1
```

Applies the given binary operator op to the given initial value z and all elements of this iterable collection.

Type parameters: `` A1 >: T ``.

Parameters, list 1: `` z: A1 ``.

Parameters, list 2: `` op: (A1, A1) => A1 ``.

Returns: `` A1 ``.

Source contract/attributes: Applies the given binary operator op to the given initial value z and all elements of this iterable collection. For each application of the operator, each operand is either an element of this iterable collection, the initial value, or another such application of the operator. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. The initial value may be used an arbitrary number of times, but at least once. If this collection is ordered, then for any application of the operator, the element(s) appearing in the left operand will precede those in the right. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. In either case, it is also necessary that the initial value be a neutral value for the operator, e.g. Nil for List concatenation or 1 for multiplication. The default implementation in IterableOnce is equivalent to foldLeft but may be overridden for more efficient traversal orders. Note: will not terminate for infinite-sized collections. Type parameters A1 The type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. z An initial value; may be used an arbitrary number of times in the computation of the result; must be a neutral value for op for the result to always be the same across runs. Attributes Returns The result of applying op between all the elements and z, or z if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.fold[A1](z)(op)
```

## `` com.cra.figaro.util.HashMultiSet.foldLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#foldLeft-fffff9e1)

```scala
def foldLeft[B](z: B)(op: (B, T) => B): B
```

Applies the given binary operator op to the given initial value z and all elements of this iterable collection, going left to right. Returns the initial value if this iterable collection is empty.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, T) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to the given initial value z and all elements of this iterable collection, going left to right. Returns the initial value if this iterable collection is empty. "Going left to right" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this iterable collection, the result is op( op( ... op( op(z, x,,1,,), x,,2,,) ... ), x,,n,,). If this collection is not ordered, then for each application of the operator, each right operand is an element. In addition, the leftmost operand is the initial value, and each other left operand is itself an application of the operator. The elements of this iterable collection and the initial value all appear exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator. Value parameters op A binary operator. z An initial value. Attributes Returns The result of applying op to z and all elements of this iterable collection, going left to right. Returns z if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foldLeft[B](z)(op)
```

## `` com.cra.figaro.util.HashMultiSet.foldRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#foldRight-fffff9e1)

```scala
def foldRight[B](z: B)(op: (T, B) => B): B
```

Applies the given binary operator op to all elements of this iterable collection and the given initial value z, going right to left. Returns the initial value if this iterable collection is empty.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (T, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this iterable collection and the given initial value z, going right to left. Returns the initial value if this iterable collection is empty. "Going right to left" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this iterable collection, the result is op(x,,1,,, op(x,,2,,, op( ... op(x,,n,,, z) ... ))). If this collection is not ordered, then for each application of the operator, each left operand is an element. In addition, the rightmost operand is the initial value, and each other right operand is itself an application of the operator. The elements of this iterable collection and the initial value all appear exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator. Value parameters op A binary operator. z An initial value. Attributes Returns The result of applying op to all elements of this iterable collection and z, going right to left. Returns z if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foldRight[B](z)(op)
```

## `` com.cra.figaro.util.HashMultiSet.forall ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#forall-fffff66f)

```scala
def forall(p: T => Boolean): Boolean
```

Tests whether a predicate holds for all elements of this iterable collection.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether a predicate holds for all elements of this iterable collection. Note: may not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns true if this iterable collection is empty or the given predicate p holds for all elements of this iterable collection, otherwise false. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.forall(p)
```

## `` com.cra.figaro.util.HashMultiSet.groupBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#groupBy-f68)

```scala
def groupBy[K](f: T => K): Map[K, Iterable[T]]
```

Partitions this iterable collection into a map of iterable collections according to some discriminator function.

Type parameters: `` K ``.

Parameters, list 1: `` f: T => K ``.

Returns: `` Map[K, Iterable[T]] ``.

Source contract/attributes: Partitions this iterable collection into a map of iterable collections according to some discriminator function. Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters K the type of keys returned by the discriminator function. Value parameters f the discriminator function. Attributes Returns A map from keys to iterable collections such that the following invariant holds: (xs groupBy f)(k) = xs filter (x => f(x) == k) That is, every key `k` is bound to a iterable collection of those elements `x` for which `f(x)` equals `k`. Inherited from: IterableOps

Invocation template:

```scala
receiver.groupBy[K](f)
```

## `` com.cra.figaro.util.HashMultiSet.groupMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#groupMap-fffff03a)

```scala
def groupMap[K, B](key: T => K)(f: T => B): Map[K, Iterable[B]]
```

Partitions this iterable collection into a map of iterable collections according to a discriminator function key. Each element in a group is transformed into a value of type B using the value function.

Type parameters: `` K ``; `` B ``.

Parameters, list 1: `` key: T => K ``.

Parameters, list 2: `` f: T => B ``.

Returns: `` Map[K, Iterable[B]] ``.

Source contract/attributes: Partitions this iterable collection into a map of iterable collections according to a discriminator function key. Each element in a group is transformed into a value of type B using the value function. It is equivalent to groupBy(key).mapValues(_.map(f)), but more efficient. case class User(name: String, age: Int) def namesByAge(users: Seq[User]): Map[Int, Seq[String]] = users.groupMap(_.age)(_.name) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of values returned by the transformation function K the type of keys returned by the discriminator function Value parameters f the element transformation function key the discriminator function Attributes Returns a map associating each key k produced by key with a iterable collection of values produced by applying f to elements that map to k Inherited from: IterableOps

Invocation template:

```scala
receiver.groupMap[K, B](key)(f)
```

## `` com.cra.figaro.util.HashMultiSet.groupMapReduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#groupMapReduce-8e8)

```scala
def groupMapReduce[K, B](key: T => K)(f: T => B)(reduce: (B, B) => B): Map[K, B]
```

Partitions this iterable collection into a map according to a discriminator function key. All the values that have the same discriminator are then transformed by the f function and then reduced into a single value with the reduce function.

Type parameters: `` K ``; `` B ``.

Parameters, list 1: `` key: T => K ``.

Parameters, list 2: `` f: T => B ``.

Parameters, list 3: `` reduce: (B, B) => B ``.

Returns: `` Map[K, B] ``.

Source contract/attributes: Partitions this iterable collection into a map according to a discriminator function key. All the values that have the same discriminator are then transformed by the f function and then reduced into a single value with the reduce function. It is equivalent to groupBy(key).mapValues(_.map(f).reduce(reduce)), but more efficient. def occurrences[A](as: Seq[A]): Map[A, Int] = as.groupMapReduce(identity)(_ => 1)(_ + _) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of values returned by the transformation function K the type of keys returned by the discriminator function Value parameters f the element transformation function key the discriminator function reduce the reduction function used to combine values mapped to the same key Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.groupMapReduce[K, B](key)(f)(reduce)
```

## `` com.cra.figaro.util.HashMultiSet.grouped ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#grouped-fffffbef)

```scala
def grouped(size: Int): Iterator[Iterable[T]]
```

Partitions elements in fixed size iterable collections.

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Iterator[Iterable[T]] ``.

Source contract/attributes: Partitions elements in fixed size iterable collections. Value parameters size the number of elements per group Attributes Returns An iterator producing iterable collections of size size, except the last will be less than size size if the elements don't divide evenly. See also scala.collection.Iterator, method grouped Inherited from: IterableOps

Invocation template:

```scala
receiver.grouped(size)
```

## `` com.cra.figaro.util.HashMultiSet.hashCode ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#hashCode-0)

```scala
override def hashCode: Int
```

Calculates a hash code value for the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Calculates a hash code value for the object. The default hashing algorithm is platform dependent. Note that it is allowed for two objects to have identical hash codes (o1.hashCode.equals(o2.hashCode)) yet not be equal (o1.equals(o2) returns false). A degenerate implementation could always return 0. However, it is required that if two objects are equal (o1.equals(o2) returns true) that they have identical hash codes (o1.hashCode.equals(o2.hashCode)). Therefore, when overriding this method, be sure to verify that the behavior is consistent with the equals method. Attributes Returns the hash code value for this object. Definition Classes MultiSet -> Any Inherited from: MultiSet

Invocation template:

```scala
receiver.hashCode
```

## `` com.cra.figaro.util.HashMultiSet.head ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#head-0)

```scala
def head: T
```

Selects the first element of this iterable collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` T ``.

Source contract/attributes: Selects the first element of this iterable collection. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the first element of this iterable collection. Throws NoSuchElementException if the iterable collection is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.head
```

## `` com.cra.figaro.util.HashMultiSet.headOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#headOption-0)

```scala
def headOption: Option[T]
```

Optionally selects the first element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Option[T] ``.

Source contract/attributes: Optionally selects the first element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the first element of this iterable collection if it is nonempty, None if it is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.headOption
```

## `` com.cra.figaro.util.HashMultiSet.init ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#init-0)

```scala
def init: Iterable[T]
```

The initial part of the collection without its last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: The initial part of the collection without its last element. Note: Even when applied to a view or a lazy collection it will always force the elements. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.init
```

## `` com.cra.figaro.util.HashMultiSet.inits ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#inits-0)

```scala
def inits: Iterator[Iterable[T]]
```

Iterates over the inits of this iterable collection. The first value will be this iterable collection and the final one will be an empty iterable collection, with the intervening values the results of successive applications of init.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Iterable[T]] ``.

Source contract/attributes: Iterates over the inits of this iterable collection. The first value will be this iterable collection and the final one will be an empty iterable collection, with the intervening values the results of successive applications of init. Note: Even when applied to a view or a lazy collection it will always force the elements. Attributes Returns an iterator over all the inits of this iterable collection Example List(1,2,3).inits = Iterator(List(1,2,3), List(1,2), List(1), Nil) Inherited from: IterableOps

Invocation template:

```scala
receiver.inits
```

## `` com.cra.figaro.util.HashMultiSet.isEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#isEmpty-0)

```scala
def isEmpty: Boolean
```

Tests whether the iterable collection is empty.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether the iterable collection is empty. Note: The default implementation creates and discards an iterator. Note: Implementations in subclasses that are not repeatedly iterable must take care not to consume any elements when isEmpty is called. Attributes Returns true if the iterable collection contains no elements, false otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.isEmpty
```

## `` com.cra.figaro.util.HashMultiSet.isTraversableAgain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#isTraversableAgain-0)

```scala
override def isTraversableAgain: Boolean
```

Tests whether this iterable collection can be repeatedly traversed. Always true for Iterables and false for Iterators unless overridden.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this iterable collection can be repeatedly traversed. Always true for Iterables and false for Iterators unless overridden. Attributes Returns true if it is repeatedly traversable, false otherwise. Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.isTraversableAgain
```

## `` com.cra.figaro.util.HashMultiSet.iterableFactory ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#iterableFactory-0)

```scala
def iterableFactory: IterableFactory[Iterable]
```

The companion object of this iterable collection, providing various factory methods.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IterableFactory[Iterable] ``.

Source contract/attributes: The companion object of this iterable collection, providing various factory methods. Attributes Returns the companion factory object for this collection type Note When implementing a custom collection type and refining CC to the new type, this method needs to be overridden to return a factory for the new type (the compiler will issue an error otherwise). Inherited from: Iterable

Invocation template:

```scala
receiver.iterableFactory
```

## `` com.cra.figaro.util.HashMultiSet.knownSize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#knownSize-0)

```scala
def knownSize: Int
```

The number of elements in this iterable collection, if it can be cheaply computed, -1 otherwise. Cheaply usually means: Not requiring a collection traversal.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: The number of elements in this iterable collection, if it can be cheaply computed, -1 otherwise. Cheaply usually means: Not requiring a collection traversal. Attributes Inherited from: IterableOnce

Invocation template:

```scala
receiver.knownSize
```

## `` com.cra.figaro.util.HashMultiSet.last ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#last-0)

```scala
def last: T
```

Selects the last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` T ``.

Source contract/attributes: Selects the last element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns The last element of this iterable collection. Throws NoSuchElementException If the iterable collection is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.last
```

## `` com.cra.figaro.util.HashMultiSet.lastOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#lastOption-0)

```scala
def lastOption: Option[T]
```

Optionally selects the last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Option[T] ``.

Source contract/attributes: Optionally selects the last element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the last element of this iterable collection if it is nonempty, None if it is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.lastOption
```

## `` com.cra.figaro.util.HashMultiSet.lazyZip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#lazyZip-fffffd84)

```scala
def lazyZip[B](that: Iterable[B]): LazyZip2[T, B, this.type]
```

Analogous to zip except that the elements in each collection are not consumed until a strict operation is invoked on the returned LazyZip2 decorator.

Type parameters: `` B ``.

Parameters, list 1: `` that: Iterable[B] ``.

Returns: `` LazyZip2[T, B, this.type] ``.

Source contract/attributes: Analogous to zip except that the elements in each collection are not consumed until a strict operation is invoked on the returned LazyZip2 decorator. Calls to lazyZip can be chained to support higher arities (up to 4) without incurring the expense of constructing and deconstructing intermediary tuples. val xs = List(1, 2, 3) val res = (xs lazyZip xs lazyZip xs lazyZip xs).map((a, b, c, d) => a + b + c + d) // res == List(4, 8, 12) Type parameters B the type of the second element in each eventual pair Value parameters that the iterable providing the second element of each eventual pair Attributes Returns a decorator LazyZip2 that allows strict operations to be performed on the lazily evaluated pairs or chained calls to lazyZip. Implicit conversion to Iterable[(A, B)] is also supported. Inherited from: Iterable

Invocation template:

```scala
receiver.lazyZip[B](that)
```

## `` com.cra.figaro.util.HashMultiSet.max ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#max-ffffff34)

```scala
def max[B >: T](implicit ord: Ordering[B]): T
```

Finds the largest element.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` T ``.

Source contract/attributes: Finds the largest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns the largest element of this iterable collection with respect to the ordering ord. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.max[B](using ord)
```

## `` com.cra.figaro.util.HashMultiSet.maxBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#maxBy-fffff0c5)

```scala
def maxBy[B](f: T => B)(implicit ord: Ordering[B]): T
```

Finds the first element which yields the largest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` T ``.

Source contract/attributes: Finds the first element which yields the largest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns the first element of this iterable collection with the largest value measured by function f with respect to the ordering ord. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxBy[B](f)(using ord)
```

## `` com.cra.figaro.util.HashMultiSet.maxByOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#maxByOption-fffff66f)

```scala
def maxByOption[B](f: T => B)(implicit ord: Ordering[B]): Option[T]
```

Finds the first element which yields the largest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the first element which yields the largest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns an option value containing the first element of this iterable collection with the largest value measured by function f with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxByOption[B](f)(using ord)
```

## `` com.cra.figaro.util.HashMultiSet.maxOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#maxOption-fffff45e)

```scala
def maxOption[B >: T](implicit ord: Ordering[B]): Option[T]
```

Finds the largest element.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the largest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns an option value containing the largest element of this iterable collection with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxOption[B](using ord)
```

## `` com.cra.figaro.util.HashMultiSet.min ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#min-ffffff34)

```scala
def min[B >: T](implicit ord: Ordering[B]): T
```

Finds the smallest element.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` T ``.

Source contract/attributes: Finds the smallest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns the smallest element of this iterable collection with respect to the ordering ord. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.min[B](using ord)
```

## `` com.cra.figaro.util.HashMultiSet.minBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#minBy-fffff0c5)

```scala
def minBy[B](f: T => B)(implicit ord: Ordering[B]): T
```

Finds the first element which yields the smallest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` T ``.

Source contract/attributes: Finds the first element which yields the smallest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns the first element of this iterable collection with the smallest value measured by function f with respect to the ordering ord. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minBy[B](f)(using ord)
```

## `` com.cra.figaro.util.HashMultiSet.minByOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#minByOption-fffff66f)

```scala
def minByOption[B](f: T => B)(implicit ord: Ordering[B]): Option[T]
```

Finds the first element which yields the smallest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the first element which yields the smallest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns an option value containing the first element of this iterable collection with the smallest value measured by function f with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minByOption[B](f)(using ord)
```

## `` com.cra.figaro.util.HashMultiSet.minOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#minOption-fffff45e)

```scala
def minOption[B >: T](implicit ord: Ordering[B]): Option[T]
```

Finds the smallest element.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the smallest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns an option value containing the smallest element of this iterable collection with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minOption[B](using ord)
```

## `` com.cra.figaro.util.HashMultiSet.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#mkString-0)

```scala
final def mkString: String
```

Displays all elements of this iterable collection in a string.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Displays all elements of this iterable collection in a string. Delegates to addString, which can be overridden. Attributes Returns a string representation of this iterable collection. In the resulting string the string representations (w.r.t. the method toString) of all elements of this iterable collection follow each other without any separator string. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString
```

## `` com.cra.figaro.util.HashMultiSet.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#mkString-fffff2ca)

```scala
final def mkString(sep: String): String
```

Displays all elements of this iterable collection in a string using a separator string.

Type parameters: none.

Parameters, list 1: `` sep: String ``.

Returns: `` String ``.

Source contract/attributes: Displays all elements of this iterable collection in a string using a separator string. Delegates to addString, which can be overridden. Value parameters sep the separator string. Attributes Returns a string representation of this iterable collection. In the resulting string the string representations (w.r.t. the method toString) of all elements of this iterable collection are separated by the string sep. Example List(1, 2, 3).mkString("|") = "1|2|3" Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString(sep)
```

## `` com.cra.figaro.util.HashMultiSet.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#mkString-d94)

```scala
final def mkString(start: String, sep: String, end: String): String
```

Displays all elements of this iterable collection in a string using start, end, and separator strings.

Type parameters: none.

Parameters, list 1: `` start: String ``; `` sep: String ``; `` end: String ``.

Returns: `` String ``.

Source contract/attributes: Displays all elements of this iterable collection in a string using start, end, and separator strings. Delegates to addString, which can be overridden. Value parameters end the ending string. sep the separator string. start the starting string. Attributes Returns a string representation of this iterable collection. The resulting string begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this iterable collection are separated by the string sep. Example List(1, 2, 3).mkString("(", "; ", ")") = "(1; 2; 3)" Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString(start, sep, end)
```

## `` com.cra.figaro.util.HashMultiSet.nonEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#nonEmpty-0)

```scala
def nonEmpty: Boolean
```

Tests whether the iterable collection is not empty.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether the iterable collection is not empty. Attributes Returns true if the iterable collection contains at least one element, false otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.nonEmpty
```

## `` com.cra.figaro.util.HashMultiSet.partition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#partition-243)

```scala
def partition(p: T => Boolean): (Iterable[T], Iterable[T])
```

A pair of, first, all elements that satisfy predicate p and, second, all elements that do not.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` (Iterable[T], Iterable[T]) ``.

Source contract/attributes: A pair of, first, all elements that satisfy predicate p and, second, all elements that do not. The two iterable collection correspond to the result of filter and filterNot, respectively. The default implementation provided here needs to traverse the collection twice. Strict collections have an overridden version of partition in StrictOptimizedIterableOps, which requires only a single traversal. Value parameters p the predicate used to test elements Attributes Returns a pair of iterable collections: the first containing all elements that satisfy p, the second containing those that do not Inherited from: IterableOps

Invocation template:

```scala
receiver.partition(p)
```

## `` com.cra.figaro.util.HashMultiSet.partitionMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#partitionMap-2d1)

```scala
def partitionMap[A1, A2](f: T => Either[A1, A2]): (Iterable[A1], Iterable[A2])
```

Applies a function f to each element of the iterable collection and returns a pair of iterable collections: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right.

Type parameters: `` A1 ``; `` A2 ``.

Parameters, list 1: `` f: T => Either[A1, A2] ``.

Returns: `` (Iterable[A1], Iterable[A2]) ``.

Source contract/attributes: Applies a function f to each element of the iterable collection and returns a pair of iterable collections: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right. Example: val xs = $Coll(1, "one", 2, "two", 3, "three") partitionMap { case i: Int => Left(i) case s: String => Right(s) } // xs == ($Coll(1, 2, 3), // $Coll(one, two, three)) Type parameters A1 the element type of the first resulting collection A2 the element type of the second resulting collection Value parameters f the 'split function' mapping the elements of this iterable collection to an scala.util.Either Attributes Returns a pair of iterable collections: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right. Inherited from: IterableOps

Invocation template:

```scala
receiver.partitionMap[A1, A2](f)
```

## `` com.cra.figaro.util.HashMultiSet.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#product-533)

```scala
def product[B >: T](implicit num: Numeric[B]): B
```

Multiplies together the elements of this collection.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` num: Numeric[B] ``.

Returns: `` B ``.

Source contract/attributes: Multiplies together the elements of this collection. The default implementation uses reduce for a known non-empty collection, foldLeft otherwise. Note: will not terminate for infinite-sized collections. Type parameters B the result type of the * operator. Value parameters num an implicit parameter defining a set of numeric operations which includes the * operator to be used in forming the product. Attributes Returns the product of all elements of this iterable collection with respect to the * operator in num. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.product[B](using num)
```

## `` com.cra.figaro.util.HashMultiSet.reduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#reduce-fffff3d4)

```scala
def reduce[B >: T](op: (B, B) => B): B
```

Applies the given binary operator op to all elements of this iterable collection.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (B, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this iterable collection. For each application of the operator, each operand is either an element of this iterable collection or another such application of the operator. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. If this collection is ordered, then for any application of the operator, the element(s) appearing in the left operand will precede those in the right. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. Attributes Returns The result of applying op between all the elements if the iterable collection is nonempty. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduce[B](op)
```

## `` com.cra.figaro.util.HashMultiSet.reduceLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#reduceLeft-fffff3d4)

```scala
def reduceLeft[B >: T](op: (B, T) => B): B
```

Applies the given binary operator op to all elements of this iterable collection, going left to right.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (B, T) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this iterable collection, going left to right. "Going left to right" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this iterable collection, the result is op( op( op( ... op(x,,1,,, x,,2,,) ... ), x,,n-1,,), x,,n,,). If this collection is not ordered, then for each application of the operator, each right operand is an element. In addition, the leftmost operand is the first element of this iterable collection and each other left operand is itself an application of the operator. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of applying op to all elements of this iterable collection, going left to right. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceLeft[B](op)
```

## `` com.cra.figaro.util.HashMultiSet.reduceLeftOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#reduceLeftOption-8fe)

```scala
def reduceLeftOption[B >: T](op: (B, T) => B): Option[B]
```

If this iterable collection is nonempty, reduces it with the given binary operator op, going left to right.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (B, T) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this iterable collection is nonempty, reduces it with the given binary operator op, going left to right. The behavior is the same as reduceLeft except that the value is None if the iterable collection is empty. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of reducing this iterable collection with op going left to right if the iterable collection is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceLeftOption[B](op)
```

## `` com.cra.figaro.util.HashMultiSet.reduceOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#reduceOption-8fe)

```scala
def reduceOption[B >: T](op: (B, B) => B): Option[B]
```

If this iterable collection is nonempty, reduces it with the given binary operator op.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (B, B) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this iterable collection is nonempty, reduces it with the given binary operator op. The behavior is the same as reduce except that the value is None if the iterable collection is empty. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B A type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. Attributes Returns The result of reducing this iterable collection with op if the iterable collection is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceOption[B](op)
```

## `` com.cra.figaro.util.HashMultiSet.reduceRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#reduceRight-fffff3d4)

```scala
def reduceRight[B >: T](op: (T, B) => B): B
```

Applies the given binary operator op to all elements of this iterable collection, going right to left.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (T, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this iterable collection, going right to left. "Going right to left" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this iterable collection, the result is op(x,,1,,, op(x,,2,,, op( ... op(x,,n-1,,, x,,n,,) ... ))). If this collection is not ordered, then for each application of the operator, each left operand is an element. In addition, the rightmost operand is the last element of this iterable collection and each other right operand is itself an application of the operator. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of applying op to all elements of this iterable collection, going right to left. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceRight[B](op)
```

## `` com.cra.figaro.util.HashMultiSet.reduceRightOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#reduceRightOption-8fe)

```scala
def reduceRightOption[B >: T](op: (T, B) => B): Option[B]
```

If this iterable collection is nonempty, reduces it with the given binary operator op, going right to left.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (T, B) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this iterable collection is nonempty, reduces it with the given binary operator op, going right to left. The behavior is the same as reduceRight except that the value is None if the iterable collection is empty. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of reducing this iterable collection with op going right to left if the iterable collection is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceRightOption[B](op)
```

## `` com.cra.figaro.util.HashMultiSet.scan ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#scan-fffff9e1)

```scala
def scan[B >: T](z: B)(op: (B, B) => B): Iterable[B]
```

Computes a prefix scan of the elements of the collection.

Type parameters: `` B >: T ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, B) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Computes a prefix scan of the elements of the collection. Note: The neutral element z may be applied more than once. Type parameters B element type of the resulting collection Value parameters op the associative operator for the scan z neutral element for the operator op Attributes Returns a new iterable collection containing the prefix scan of the elements in this iterable collection Inherited from: IterableOps

Invocation template:

```scala
receiver.scan[B](z)(op)
```

## `` com.cra.figaro.util.HashMultiSet.scanLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#scanLeft-fffff9e1)

```scala
def scanLeft[B](z: B)(op: (B, T) => B): Iterable[B]
```

Produces a iterable collection containing cumulative results of applying the operator going left to right, including the initial value.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, T) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Produces a iterable collection containing cumulative results of applying the operator going left to right, including the initial value. Note: will not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Type parameters B the type of the elements in the resulting collection Value parameters op the binary operator applied to the intermediate result and the element z the initial value Attributes Returns collection with intermediate results Inherited from: IterableOps

Invocation template:

```scala
receiver.scanLeft[B](z)(op)
```

## `` com.cra.figaro.util.HashMultiSet.scanRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#scanRight-fffff9e1)

```scala
def scanRight[B](z: B)(op: (T, B) => B): Iterable[B]
```

Produces a collection containing cumulative results of applying the operator going right to left. The head of the collection is the last cumulative result.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (T, B) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Produces a collection containing cumulative results of applying the operator going right to left. The head of the collection is the last cumulative result. Note: will not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: Even when applied to a view or a lazy collection it will always force the elements. Example: List(1, 2, 3, 4).scanRight(0)(_ + _) == List(10, 9, 7, 4, 0) Type parameters B the type of the elements in the resulting collection Value parameters op the binary operator applied to the intermediate result and the element z the initial value Attributes Returns collection with intermediate results Inherited from: IterableOps

Invocation template:

```scala
receiver.scanRight[B](z)(op)
```

## `` com.cra.figaro.util.HashMultiSet.size ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#size-0)

```scala
def size: Int
```

The size of this iterable collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: The size of this iterable collection. Note: will not terminate for infinite-sized collections. Attributes Returns the number of elements in this iterable collection. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.size
```

## `` com.cra.figaro.util.HashMultiSet.sizeCompare ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#sizeCompare-32d)

```scala
def sizeCompare(that: Iterable[_]): Int
```

Compares the size of this iterable collection to the size of another Iterable.

Type parameters: none.

Parameters, list 1: `` that: Iterable[_] ``.

Returns: `` Int ``.

Source contract/attributes: Compares the size of this iterable collection to the size of another Iterable. Value parameters that the Iterable whose size is compared with this iterable collection's size. Attributes Returns A value x where x < 0 if this.size < that.size x == 0 if this.size == that.size x > 0 if this.size > that.size The method as implemented here does not call size directly; its running time is O(this.size min that.size) instead of O(this.size + that.size). The method should be overridden if computing size is cheap and knownSize returns -1. Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeCompare(that)
```

## `` com.cra.figaro.util.HashMultiSet.sizeCompare ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#sizeCompare-fffffbe0)

```scala
def sizeCompare(otherSize: Int): Int
```

Compares the size of this iterable collection to a test value.

Type parameters: none.

Parameters, list 1: `` otherSize: Int ``.

Returns: `` Int ``.

Source contract/attributes: Compares the size of this iterable collection to a test value. Value parameters otherSize the test value that gets compared with the size. Attributes Returns A value x where x < 0 if this.size < otherSize x == 0 if this.size == otherSize x > 0 if this.size > otherSize The default implementation does not call size, which may be arbitrarily expensive. It will use knownSize if nonnegative. Otherwise, it will iterate to compute an element count, with complexity O(size min otherSize). This method should be overridden if computing size is cheap and knownSize returns -1. In that case, the custom implementation should just compare size and otherSize directly. See also sizeIs Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeCompare(otherSize)
```

## `` com.cra.figaro.util.HashMultiSet.sizeIs ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#sizeIs-0)

```scala
final def sizeIs: SizeCompareOps
```

Returns a value class containing operations for comparing the size of this iterable collection to a test value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` SizeCompareOps ``.

Source contract/attributes: Returns a value class containing operations for comparing the size of this iterable collection to a test value. These operations are implemented in terms of `sizeCompare(Int)`, and allow the following more readable usages: this.sizeIs < size // this.sizeCompare(size) < 0 this.sizeIs <= size // this.sizeCompare(size) <= 0 this.sizeIs == size // this.sizeCompare(size) == 0 this.sizeIs != size // this.sizeCompare(size) != 0 this.sizeIs >= size // this.sizeCompare(size) >= 0 this.sizeIs > size // this.sizeCompare(size) > 0 Attributes Returns a wrapper that allows size comparisons using operators such as =, and > Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeIs
```

## `` com.cra.figaro.util.HashMultiSet.slice ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#slice-573)

```scala
def slice(from: Int, until: Int): Iterable[T]
```

Selects an interval of elements. The returned iterable collection is made up of all elements x which satisfy the invariant:

Type parameters: none.

Parameters, list 1: `` from: Int ``; `` until: Int ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Selects an interval of elements. The returned iterable collection is made up of all elements x which satisfy the invariant: from <= indexOf(x) < until Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters from the lowest index to include from this iterable collection. until the lowest index to EXCLUDE from this iterable collection. Attributes Returns a iterable collection containing the elements greater than or equal to index from extending up to (but not including) index until of this iterable collection. Example List('a', 'b', 'c', 'd', 'e').slice(1, 3) == List('b', 'c') Inherited from: IterableOps

Invocation template:

```scala
receiver.slice(from, until)
```

## `` com.cra.figaro.util.HashMultiSet.sliding ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#sliding-fffff156)

```scala
def sliding(size: Int, step: Int): Iterator[Iterable[T]]
```

Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped).

Type parameters: none.

Parameters, list 1: `` size: Int ``; `` step: Int ``.

Returns: `` Iterator[Iterable[T]] ``.

Source contract/attributes: Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped). The returned iterator will be empty when called on an empty collection. The last element the iterator produces may be smaller than the window size when the original collection isn't exhausted by the window before it and its last element isn't skipped by the step before it. Value parameters size the number of elements per group step the distance between the first elements of successive groups Attributes Returns An iterator producing iterable collections of size size, except the last element (which may be the only element) will be smaller if there are fewer than size elements remaining to be grouped. See also scala.collection.Iterator, method sliding Example List(1, 2, 3, 4, 5).sliding(2, 2) = Iterator(List(1, 2), List(3, 4), List(5)) List(1, 2, 3, 4, 5, 6).sliding(2, 3) = Iterator(List(1, 2), List(4, 5)) Inherited from: IterableOps

Invocation template:

```scala
receiver.sliding(size, step)
```

## `` com.cra.figaro.util.HashMultiSet.sliding ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#sliding-fffffbef)

```scala
def sliding(size: Int): Iterator[Iterable[T]]
```

Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped).

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Iterator[Iterable[T]] ``.

Source contract/attributes: Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped). An empty collection returns an empty iterator, and a non-empty collection containing fewer elements than the window size returns an iterator that will produce the original collection as its only element. Value parameters size the number of elements per group Attributes Returns An iterator producing iterable collections of size size, except for a non-empty collection with less than size elements, which returns an iterator that produces the source collection itself as its only element. See also scala.collection.Iterator, method sliding Example List().sliding(2) = empty iterator List(1).sliding(2) = Iterator(List(1)) List(1, 2).sliding(2) = Iterator(List(1, 2)) List(1, 2, 3).sliding(2) = Iterator(List(1, 2), List(2, 3)) Inherited from: IterableOps

Invocation template:

```scala
receiver.sliding(size)
```

## `` com.cra.figaro.util.HashMultiSet.span ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#span-243)

```scala
def span(p: T => Boolean): (Iterable[T], Iterable[T])
```

Splits this iterable collection into a prefix/suffix pair according to a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` (Iterable[T], Iterable[T]) ``.

Source contract/attributes: Splits this iterable collection into a prefix/suffix pair according to a predicate. Note: c span p is equivalent to (but possibly more efficient than) (c takeWhile p, c dropWhile p), provided the evaluation of the predicate p does not cause any side effects. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the test predicate Attributes Returns a pair consisting of the longest prefix of this iterable collection whose elements all satisfy p, and the rest of this iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.span(p)
```

## `` com.cra.figaro.util.HashMultiSet.splitAt ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#splitAt-fffffbd9)

```scala
override def splitAt(n: Int): (Iterable[T], Iterable[T])
```

Splits this iterable collection into a prefix/suffix pair at a given position.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` (Iterable[T], Iterable[T]) ``.

Source contract/attributes: Splits this iterable collection into a prefix/suffix pair at a given position. Note: c splitAt n is equivalent to (but possibly more efficient than) (c take n, c drop n). Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the position at which to split. Attributes Returns a pair of iterable collections consisting of the first n elements of this iterable collection, and the other elements. Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.splitAt(n)
```

## `` com.cra.figaro.util.HashMultiSet.stepper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#stepper-190)

```scala
def stepper[S <: Stepper[_]](implicit shape: StepperShape[T, S]): S
```

Returns a scala.collection.Stepper for the elements of this collection.

Type parameters: `` S <: Stepper[_] ``.

Context parameters, list 1: `` shape: StepperShape[T, S] ``.

Returns: `` S ``.

Source contract/attributes: Returns a scala.collection.Stepper for the elements of this collection. The Stepper enables creating a Java stream to operate on the collection, see scala.jdk.StreamConverters. For collections holding primitive values, the Stepper can be used as an iterator which doesn't box the elements. The implicit scala.collection.StepperShape parameter defines the resulting Stepper type according to the element type of this collection. For collections of Int, Short, Byte or Char, an scala.collection.IntStepper is returned For collections of Double or Float, a scala.collection.DoubleStepper is returned For collections of Long a scala.collection.LongStepper is returned For any other element type, an scala.collection.AnyStepper is returned Note that this method is overridden in subclasses and the return type is refined to S with EfficientSplit, for example scala.collection.IndexedSeqOps.stepper. For Steppers marked with scala.collection.Stepper.EfficientSplit, the converters in scala.jdk.StreamConverters allow creating parallel streams, whereas bare Steppers can be converted only to sequential streams. Type parameters S the type of the returned Stepper, determined by the implicit StepperShape Attributes Inherited from: IterableOnce

Invocation template:

```scala
receiver.stepper[S](using shape)
```

## `` com.cra.figaro.util.HashMultiSet.sum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#sum-533)

```scala
def sum[B >: T](implicit num: Numeric[B]): B
```

Sums the elements of this collection.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` num: Numeric[B] ``.

Returns: `` B ``.

Source contract/attributes: Sums the elements of this collection. The default implementation uses reduce for a known non-empty collection, foldLeft otherwise. Note: will not terminate for infinite-sized collections. Type parameters B the result type of the + operator. Value parameters num an implicit parameter defining a set of numeric operations which includes the + operator to be used in forming the sum. Attributes Returns the sum of all elements of this iterable collection with respect to the + operator in num. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.sum[B](using num)
```

## `` com.cra.figaro.util.HashMultiSet.tail ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#tail-0)

```scala
def tail: Iterable[T]
```

The rest of the collection without its first element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: The rest of the collection without its first element. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.tail
```

## `` com.cra.figaro.util.HashMultiSet.tails ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#tails-0)

```scala
def tails: Iterator[Iterable[T]]
```

Iterates over the tails of this iterable collection. The first value will be this iterable collection and the final one will be an empty iterable collection, with the intervening values the results of successive applications of tail.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Iterable[T]] ``.

Source contract/attributes: Iterates over the tails of this iterable collection. The first value will be this iterable collection and the final one will be an empty iterable collection, with the intervening values the results of successive applications of tail. Attributes Returns an iterator over all the tails of this iterable collection Example List(1,2,3).tails = Iterator(List(1,2,3), List(2,3), List(3), Nil) Inherited from: IterableOps

Invocation template:

```scala
receiver.tails
```

## `` com.cra.figaro.util.HashMultiSet.take ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#take-aba)

```scala
def take(n: Int): Iterable[T]
```

Selects the first n elements.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Selects the first n elements. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to take from this iterable collection. Attributes Returns a iterable collection consisting only of the first n elements of this iterable collection, or else the whole iterable collection, if it has less than n elements. If n is negative, returns an empty iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.take(n)
```

## `` com.cra.figaro.util.HashMultiSet.takeRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#takeRight-aba)

```scala
def takeRight(n: Int): Iterable[T]
```

Selects the last n elements.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Selects the last n elements. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to take from this iterable collection. Attributes Returns a iterable collection consisting only of the last n elements of this iterable collection, or else the whole iterable collection, if it has less than n elements. If n is negative, returns an empty iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.takeRight(n)
```

## `` com.cra.figaro.util.HashMultiSet.takeWhile ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#takeWhile-c24)

```scala
def takeWhile(p: T => Boolean): Iterable[T]
```

Takes longest prefix of elements that satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Takes longest prefix of elements that satisfy a predicate. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p The predicate used to test elements. Attributes Returns the longest prefix of this iterable collection whose elements all satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.takeWhile(p)
```

## `` com.cra.figaro.util.HashMultiSet.tapEach ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#tapEach-5d3)

```scala
override def tapEach[U](f: T => U): Iterable[T]
```

Applies a side-effecting function to each element in this collection. Strict collections will apply f to their elements immediately, while lazy collections like Views and LazyLists will only apply f on each element if and when that element is evaluated, and each time that element is evaluated.

Type parameters: `` U ``.

Parameters, list 1: `` f: T => U ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Applies a side-effecting function to each element in this collection. Strict collections will apply f to their elements immediately, while lazy collections like Views and LazyLists will only apply f on each element if and when that element is evaluated, and each time that element is evaluated. Type parameters U the return type of f Value parameters f a function to apply to each element in this iterable collection Attributes Returns The same logical collection as this Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.tapEach[U](f)
```

## `` com.cra.figaro.util.HashMultiSet.to ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#to-fffff006)

```scala
def to[C1](factory: Factory[T, C1]): C1
```

Given a collection factory factory, converts this iterable collection to the appropriate representation for the current element type A. Example uses:

Type parameters: `` C1 ``.

Parameters, list 1: `` factory: Factory[T, C1] ``.

Returns: `` C1 ``.

Source contract/attributes: Given a collection factory factory, converts this iterable collection to the appropriate representation for the current element type A. Example uses: import scala.collection.mutable.ArrayBuffer import scala.collection.immutable.BitSet val xs: Iterable[Int] = Seq(1, 2, 3, 4, 5) xs.to(List) xs.to(ArrayBuffer) xs.to(BitSet) // for xs: Iterable[Int] Type parameters C1 the target collection type Value parameters factory the factory for the target collection type Attributes Returns a new collection of type C1 containing all elements of this iterable collection Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.to[C1](factory)
```

## `` com.cra.figaro.util.HashMultiSet.toArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#toArray-fffff5df)

```scala
def toArray[B >: T : ClassTag]: Array[B]
```

Converts this iterable collection to an Array.

Type parameters: `` B >: T : ClassTag ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Array[B] ``.

Source contract/attributes: Converts this iterable collection to an Array. Implementation note: DO NOT call Array.from from this method. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This iterable collection as an Array[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toArray[B]
```

## `` com.cra.figaro.util.HashMultiSet.toBuffer ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#toBuffer-fffff87f)

```scala
final def toBuffer[B >: T]: Buffer[B]
```

Converts this iterable collection to a Buffer.

Type parameters: `` B >: T ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Buffer[B] ``.

Source contract/attributes: Converts this iterable collection to a Buffer. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This iterable collection as a Buffer[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toBuffer[B]
```

## `` com.cra.figaro.util.HashMultiSet.toIndexedSeq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#toIndexedSeq-0)

```scala
def toIndexedSeq: IndexedSeq[T]
```

Converts this iterable collection to an IndexedSeq.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IndexedSeq[T] ``.

Source contract/attributes: Converts this iterable collection to an IndexedSeq. Attributes Returns This iterable collection as an IndexedSeq[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toIndexedSeq
```

## `` com.cra.figaro.util.HashMultiSet.toList ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#toList-0)

```scala
def toList: List[T]
```

Converts this iterable collection to a List.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[T] ``.

Source contract/attributes: Converts this iterable collection to a List. Attributes Returns This iterable collection as a List[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toList
```

## `` com.cra.figaro.util.HashMultiSet.toMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#toMap-c0c)

```scala
def toMap[K, V](implicit ev: T <:< (K, V)): Map[K, V]
```

Converts this iterable collection to a Map, given an implicit coercion from the iterable collection's type to a key-value tuple.

Type parameters: `` K ``; `` V ``.

Context parameters, list 1: `` ev: T <:< (K, V) ``.

Returns: `` Map[K, V] ``.

Source contract/attributes: Converts this iterable collection to a Map, given an implicit coercion from the iterable collection's type to a key-value tuple. Type parameters K The key type for the resulting map. V The value type for the resulting map. Value parameters ev an implicit evidence that A is a subtype of (K, V) Attributes Returns This iterable collection as a Map[K, V]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toMap[K, V](using ev)
```

## `` com.cra.figaro.util.HashMultiSet.toSeq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#toSeq-0)

```scala
def toSeq: Seq[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Seq[T] ``.

Source contract/attributes: Attributes Returns This iterable collection as a Seq[A]. This is equivalent to to(Seq) but might be faster. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toSeq
```

## `` com.cra.figaro.util.HashMultiSet.toSet ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#toSet-fffffe9f)

```scala
def toSet[B >: T]: Set[B]
```

Converts this iterable collection to a Set.

Type parameters: `` B >: T ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[B] ``.

Source contract/attributes: Converts this iterable collection to a Set. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This iterable collection as a Set[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toSet[B]
```

## `` com.cra.figaro.util.HashMultiSet.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#toString-665)

```scala
override def toString(): String
```

Converts this iterable collection to a string.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Converts this iterable collection to a string. Attributes Returns a string representation of this collection. By default this string consists of the className of this iterable collection, followed by all elements separated by commas and enclosed in parentheses. Definition Classes Iterable -> Any Inherited from: Iterable

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.util.HashMultiSet.toVector ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#toVector-0)

```scala
def toVector: Vector[T]
```

Converts this iterable collection to a Vector.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Vector[T] ``.

Source contract/attributes: Converts this iterable collection to a Vector. Attributes Returns This iterable collection as a Vector[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toVector
```

## `` com.cra.figaro.util.HashMultiSet.transpose ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#transpose-5d3)

```scala
def transpose[B](implicit asIterable: T => Iterable[B]): Iterable[Iterable[B]]
```

Transposes this iterable collection of iterable collections into a iterable collection of iterable collections.

Type parameters: `` B ``.

Context parameters, list 1: `` asIterable: T => Iterable[B] ``.

Returns: `` Iterable[Iterable[B]] ``.

Source contract/attributes: Transposes this iterable collection of iterable collections into a iterable collection of iterable collections. The resulting collection's type will be guided by the static type of iterable collection. For example: val xs = List( Set(1, 2, 3), Set(4, 5, 6)).transpose // xs == List( // List(1, 4), // List(2, 5), // List(3, 6)) val ys = Vector( List(1, 2, 3), List(4, 5, 6)).transpose // ys == Vector( // Vector(1, 4), // Vector(2, 5), // Vector(3, 6)) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of the elements of each iterable collection. Value parameters asIterable an implicit conversion which asserts that the element type of this iterable collection is an Iterable. Attributes Returns a two-dimensional iterable collection of iterable collections which has as nth row the nth column of this iterable collection. Throws IllegalArgumentException if all collections in this iterable collection are not of the same size. Inherited from: IterableOps

Invocation template:

```scala
receiver.transpose[B](using asIterable)
```

## `` com.cra.figaro.util.HashMultiSet.unzip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#unzip-2d1)

```scala
def unzip[A1, A2](implicit asPair: T => (A1, A2)): (Iterable[A1], Iterable[A2])
```

Converts this iterable collection of pairs into two collections of the first and second half of each pair.

Type parameters: `` A1 ``; `` A2 ``.

Context parameters, list 1: `` asPair: T => (A1, A2) ``.

Returns: `` (Iterable[A1], Iterable[A2]) ``.

Source contract/attributes: Converts this iterable collection of pairs into two collections of the first and second half of each pair. val xs = $Coll( (1, "one"), (2, "two"), (3, "three")).unzip // xs == ($Coll(1, 2, 3), // $Coll(one, two, three)) Type parameters A1 the type of the first half of the element pairs A2 the type of the second half of the element pairs Value parameters asPair an implicit conversion which asserts that the element type of this iterable collection is a pair. Attributes Returns a pair of iterable collections, containing the first, respectively second half of each element pair of this iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.unzip[A1, A2](using asPair)
```

## `` com.cra.figaro.util.HashMultiSet.unzip3 ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#unzip3-a31)

```scala
def unzip3[A1, A2, A3](implicit asTriple: T => (A1, A2, A3)): (Iterable[A1], Iterable[A2], Iterable[A3])
```

Converts this iterable collection of triples into three collections of the first, second, and third element of each triple.

Type parameters: `` A1 ``; `` A2 ``; `` A3 ``.

Context parameters, list 1: `` asTriple: T => (A1, A2, A3) ``.

Returns: `` (Iterable[A1], Iterable[A2], Iterable[A3]) ``.

Source contract/attributes: Converts this iterable collection of triples into three collections of the first, second, and third element of each triple. val xs = $Coll( (1, "one", '1'), (2, "two", '2'), (3, "three", '3')).unzip3 // xs == ($Coll(1, 2, 3), // $Coll(one, two, three), // $Coll(1, 2, 3)) Type parameters A1 the type of the first member of the element triples A2 the type of the second member of the element triples A3 the type of the third member of the element triples Value parameters asTriple an implicit conversion which asserts that the element type of this iterable collection is a triple. Attributes Returns a triple of iterable collections, containing the first, second, respectively third member of each element triple of this iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.unzip3[A1, A2, A3](using asTriple)
```

## `` com.cra.figaro.util.HashMultiSet.view ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#view-0)

```scala
def view: View[T]
```

A view over the elements of this collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` View[T] ``.

Source contract/attributes: A view over the elements of this collection. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.view
```

## `` com.cra.figaro.util.HashMultiSet.withFilter ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#withFilter-fffffb75)

```scala
def withFilter(p: T => Boolean): WithFilter[T, Iterable]
```

Creates a non-strict filter of this iterable collection.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` WithFilter[T, Iterable] ``.

Source contract/attributes: Creates a non-strict filter of this iterable collection. Note: the difference between c filter p and c withFilter p is that the former creates a new collection, whereas the latter only restricts the domain of subsequent map, flatMap, foreach, and withFilter operations. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the predicate used to test elements. Attributes Returns an object of class WithFilter, which supports map, flatMap, foreach, and withFilter operations. All these operations apply to those elements of this iterable collection which satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.withFilter(p)
```

## `` com.cra.figaro.util.HashMultiSet.zip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#zip-1dd)

```scala
def zip[B](that: IterableOnce[B]): Iterable[(T, B)]
```

Returns a iterable collection formed from this iterable collection and another iterable collection by combining corresponding elements in pairs. If one of the two collections is longer than the other, its remaining elements are ignored.

Type parameters: `` B ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Returns: `` Iterable[(T, B)] ``.

Source contract/attributes: Returns a iterable collection formed from this iterable collection and another iterable collection by combining corresponding elements in pairs. If one of the two collections is longer than the other, its remaining elements are ignored. Type parameters B the type of the second half of the returned pairs Value parameters that The iterable providing the second half of each result pair Attributes Returns a new iterable collection containing pairs consisting of corresponding elements of this iterable collection and that. The length of the returned collection is the minimum of the lengths of this iterable collection and that. Inherited from: IterableOps

Invocation template:

```scala
receiver.zip[B](that)
```

## `` com.cra.figaro.util.HashMultiSet.zipAll ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#zipAll-fffff401)

```scala
def zipAll[A1 >: T, B](that: Iterable[B], thisElem: A1, thatElem: B): Iterable[(A1, B)]
```

Returns a iterable collection formed from this iterable collection and another iterable collection by combining corresponding elements in pairs. If one of the two collections is shorter than the other, placeholder elements are used to extend the shorter collection to the length of the longer.

Type parameters: `` A1 >: T ``; `` B ``.

Parameters, list 1: `` that: Iterable[B] ``; `` thisElem: A1 ``; `` thatElem: B ``.

Returns: `` Iterable[(A1, B)] ``.

Source contract/attributes: Returns a iterable collection formed from this iterable collection and another iterable collection by combining corresponding elements in pairs. If one of the two collections is shorter than the other, placeholder elements are used to extend the shorter collection to the length of the longer. Type parameters A1 the type of the first element in each result pair (supertype of A) B the type of the second element in each result pair Value parameters that the iterable providing the second half of each result pair thatElem the element to be used to fill up the result if that is shorter than this iterable collection. thisElem the element to be used to fill up the result if this iterable collection is shorter than that. Attributes Returns a new iterable collection containing pairs consisting of corresponding elements of this iterable collection and that. The length of the returned collection is the maximum of the lengths of this iterable collection and that. If this iterable collection is shorter than that, thisElem values are used to pad the result. If that is shorter than this iterable collection, thatElem values are used to pad the result. Inherited from: IterableOps

Invocation template:

```scala
receiver.zipAll[A1, B](that, thisElem, thatElem)
```

## `` com.cra.figaro.util.HashMultiSet.zipWithIndex ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#zipWithIndex-0)

```scala
def zipWithIndex: Iterable[(T, Int)]
```

Zips this iterable collection with its indices.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[(T, Int)] ``.

Source contract/attributes: Zips this iterable collection with its indices. Attributes Returns A new iterable collection containing pairs consisting of all elements of this iterable collection paired with their index. Indices start at 0. Example List("a", "b", "c").zipWithIndex == List(("a", 0), ("b", 1), ("c", 2)) Inherited from: IterableOps

Invocation template:

```scala
receiver.zipWithIndex
```

## `` com.cra.figaro.util.HashMultiSet.++: ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#++:-1dd)

```scala
def ++:[B >: T](that: IterableOnce[B]): Iterable[B]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B >: T ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use ++ instead of ++: for collections of type Iterable Inherited from: IterableOps

Invocation template:

```scala
receiver.++:[B](that)
```

## `` com.cra.figaro.util.HashMultiSet./: ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#/:-fffff9e1)

```scala
final def /:[B](z: B)(op: (B, T) => B): B
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, T) => B ``.

Returns: `` B ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use foldLeft instead of /: Inherited from: IterableOnceOps

Invocation template:

```scala
receiver./:[B](z)(op)
```

## `` com.cra.figaro.util.HashMultiSet.:\ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#:\-fffff9e1)

```scala
final def :\[B](z: B)(op: (T, B) => B): B
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (T, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use foldRight instead of :\\ Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.:\[B](z)(op)
```

## `` com.cra.figaro.util.HashMultiSet.aggregate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#aggregate-952)

```scala
def aggregate[B](z: => B)(seqop: (B, T) => B, combop: (B, B) => B): B
```

Aggregates the results of applying an operator to subsequent elements.

Type parameters: `` B ``.

Parameters, list 1: `` z: => B ``.

Parameters, list 2: `` seqop: (B, T) => B ``; `` combop: (B, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Aggregates the results of applying an operator to subsequent elements. Since this method degenerates to foldLeft for sequential (non-parallel) collections, where the combining operation is ignored, it is advisable to prefer foldLeft for that case. For parallel collections, use the aggregate method specified by scala.collection.parallel.ParIterableLike. Type parameters B the result type, produced by seqop, combop, and by this function as a final result. Value parameters combop an associative operator for combining sequential results, unused for sequential collections. seqop the binary operator used to accumulate the result. z the start value, a neutral element for seqop. Attributes Deprecated [Since version 2.13.0] For sequential collections, prefer `foldLeft(z)(seqop)`. For parallel collections, use `ParIterableLike#aggregate`. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.aggregate[B](z)(seqop, combop)
```

## `` com.cra.figaro.util.HashMultiSet.companion ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#companion-0)

```scala
def companion: IterableFactory[Iterable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IterableFactory[Iterable] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use iterableFactory instead Inherited from: IterableOps

Invocation template:

```scala
receiver.companion
```

## `` com.cra.figaro.util.HashMultiSet.copyToBuffer ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#copyToBuffer-f0b)

```scala
final def copyToBuffer[B >: T](dest: Buffer[B]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B >: T ``.

Parameters, list 1: `` dest: Buffer[B] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use `dest ++= coll` instead Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToBuffer[B](dest)
```

## `` com.cra.figaro.util.HashMultiSet.hasDefiniteSize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#hasDefiniteSize-0)

```scala
def hasDefiniteSize: Boolean
```

Tests whether this iterable collection is known to have a finite size. All strict collections are known to have finite size. For a non-strict collection such as Stream, the predicate returns **true** if all elements have been computed. It returns **false** if the stream is not yet evaluated to the end. Non-empty Iterators usually return **false** even if they were created from a collection with a known finite size.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this iterable collection is known to have a finite size. All strict collections are known to have finite size. For a non-strict collection such as Stream, the predicate returns **true** if all elements have been computed. It returns **false** if the stream is not yet evaluated to the end. Non-empty Iterators usually return **false** even if they were created from a collection with a known finite size. Note: many collection methods will not work on collections of infinite sizes. The typical failure mode is an infinite loop. These methods always attempt a traversal without checking first that hasDefiniteSize returns **true**. However, checking hasDefiniteSize can provide an assurance that size is well-defined and non-termination is not a concern. Attributes Returns **true** if this collection is known to have finite size, **false** otherwise. See also method knownSize for a more useful alternative Deprecated [Since version 2.13.0] Check .knownSize instead of .hasDefiniteSize for more actionable information (see scaladoc for details) This method is deprecated in 2.13 because it does not provide any actionable information. As noted above, even the collection library itself does not use it. When there is no guarantee that a collection is finite, it is generally best to attempt a computation anyway and document that it will not terminate for infinite collections rather than backing out because this would prevent performing the computation on collections that are in fact finite even though hasDefiniteSize returns false. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.hasDefiniteSize
```

## `` com.cra.figaro.util.HashMultiSet.repr ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#repr-0)

```scala
final def repr: Iterable[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use coll instead of repr in a collection implementation, use the collection value itself from the outside Inherited from: IterableOps

Invocation template:

```scala
receiver.repr
```

## `` com.cra.figaro.util.HashMultiSet.seq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#seq-0)

```scala
def seq: this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` this.type ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Iterable.seq always returns the iterable itself Inherited from: Iterable

Invocation template:

```scala
receiver.seq
```

## `` com.cra.figaro.util.HashMultiSet.toIterable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#toIterable-0)

```scala
final def toIterable: this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` this.type ``.

Source contract/attributes: Attributes Returns This collection as an Iterable[A]. No new collection will be built if this is already an Iterable[A]. Deprecated [Since version 2.13.7] toIterable is internal and will be made protected; its name is similar to `toList` or `toSeq`, but it doesn\'t copy non-immutable collections Inherited from: Iterable

Invocation template:

```scala
receiver.toIterable
```

## `` com.cra.figaro.util.HashMultiSet.toIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#toIterator-0)

```scala
final def toIterator: Iterator[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use .iterator instead of .toIterator Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toIterator
```

## `` com.cra.figaro.util.HashMultiSet.toStream ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#toStream-0)

```scala
final def toStream: Stream[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Stream[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use .to(LazyList) instead of .toStream Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toStream
```

## `` com.cra.figaro.util.HashMultiSet.toTraversable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#toTraversable-0)

```scala
final def toTraversable: Iterable[T]
```

Converts this iterable collection to an unspecified Iterable. Will return the same collection if this instance is already Iterable.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: Converts this iterable collection to an unspecified Iterable. Will return the same collection if this instance is already Iterable. Attributes Returns An Iterable containing all elements of this iterable collection. Deprecated [Since version 2.13.0] toTraversable is internal and will be made protected; its name is similar to `toList` or `toSeq`, but it doesn\'t copy non-immutable collections Inherited from: IterableOps

Invocation template:

```scala
receiver.toTraversable
```

## `` com.cra.figaro.util.HashMultiSet.view ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashMultiSet.html#view-fffff78d)

```scala
def view(from: Int, until: Int): View[T]
```

A view over a slice of the elements of this collection.

Type parameters: none.

Parameters, list 1: `` from: Int ``; `` until: Int ``.

Returns: `` View[T] ``.

Source contract/attributes: A view over a slice of the elements of this collection. Attributes Deprecated [Since version 2.13.0] Use .view.slice(from, until) instead of .view(from, until) Inherited from: IterableOps

Invocation template:

```scala
receiver.view(from, until)
```

## `` com.cra.figaro.util.HashSelectableSet.addOne ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#addOne-14a)

```scala
def addOne(elem: T): this.type
```

Add an element to the set.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` this.type ``.

Source contract/attributes: Add an element to the set. Attributes

Invocation template:

```scala
receiver.addOne(elem)
```

## `` com.cra.figaro.util.HashSelectableSet.clear ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#clear-94c)

```scala
def clear(): Unit
```

Clears the contents of this builder. After execution of this method the builder will contain no elements.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clears the contents of this builder. After execution of this method the builder will contain no elements. Attributes

Invocation template:

```scala
receiver.clear()
```

## `` com.cra.figaro.util.HashSelectableSet.clone ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#clone-0)

```scala
override def clone: SelectableSet[T]
```

Make a copy of this HashSelectableSet, returning a new HashSelectableSet

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` SelectableSet[T] ``.

Source contract/attributes: Make a copy of this HashSelectableSet, returning a new HashSelectableSet Attributes Definition Classes SetOps -> Cloneable -> Object

Invocation template:

```scala
receiver.clone
```

## `` com.cra.figaro.util.HashSelectableSet.contains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#contains-4ad)

```scala
def contains(elem: T): Boolean
```

Test whether the set contains the given element.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` Boolean ``.

Source contract/attributes: Test whether the set contains the given element. Attributes

Invocation template:

```scala
receiver.contains(elem)
```

## `` com.cra.figaro.util.HashSelectableSet.iterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#iterator-0)

```scala
def iterator: Iterator[T]
```

An scala.collection.Iterator over the elements of this mutable set.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[T] ``.

Source contract/attributes: An scala.collection.Iterator over the elements of this mutable set. If an IterableOnce object is in fact an scala.collection.Iterator, this method always returns itself, in its current state, but if it is an scala.collection.Iterable, this method always returns a new scala.collection.Iterator. Attributes Returns an iterator over all elements of this mutable set

Invocation template:

```scala
receiver.iterator
```

## `` com.cra.figaro.util.HashSelectableSet.select ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#select-993)

```scala
def select(): T
```

Select an element uniformly at random.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` T ``.

Source contract/attributes: Select an element uniformly at random. Attributes

Invocation template:

```scala
receiver.select()
```

## `` com.cra.figaro.util.HashSelectableSet.subtractOne ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#subtractOne-14a)

```scala
def subtractOne(elem: T): this.type
```

Remove an element from the set.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` this.type ``.

Source contract/attributes: Remove an element from the set. Attributes

Invocation template:

```scala
receiver.subtractOne(elem)
```

## `` com.cra.figaro.util.HashSelectableSet.toList ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#toList-0)

```scala
override def toList: List[T]
```

Converts this mutable set to a List.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[T] ``.

Source contract/attributes: Converts this mutable set to a List. Attributes Returns This mutable set as a List[A]. Definition Classes IterableOnceOps

Invocation template:

```scala
receiver.toList
```

## `` com.cra.figaro.util.HashSelectableSet.& ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#&-dde)

```scala
final def &(that: Set[T]): Set[T]
```

Alias for intersect.

Type parameters: none.

Parameters, list 1: `` that: Set[T] ``.

Returns: `` Set[T] ``.

Source contract/attributes: Alias for intersect. Value parameters that the set to intersect with Attributes Inherited from: SetOps

Invocation template:

```scala
receiver.&(that)
```

## `` com.cra.figaro.util.HashSelectableSet.&~ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#&~-dde)

```scala
final def &~(that: Set[T]): Set[T]
```

Alias for diff.

Type parameters: none.

Parameters, list 1: `` that: Set[T] ``.

Returns: `` Set[T] ``.

Source contract/attributes: Alias for diff. Value parameters that the set of elements to exclude Attributes Inherited from: SetOps

Invocation template:

```scala
receiver.&~(that)
```

## `` com.cra.figaro.util.HashSelectableSet.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#++-1dd)

```scala
final def ++[B >: T](suffix: IterableOnce[B]): Set[B]
```

Alias for concat.

Type parameters: `` B >: T ``.

Parameters, list 1: `` suffix: IterableOnce[B] ``.

Returns: `` Set[B] ``.

Source contract/attributes: Alias for concat. Type parameters B the element type of the returned collection Value parameters suffix the iterable to append to this mutable set Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.++[B](suffix)
```

## `` com.cra.figaro.util.HashSelectableSet.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#++-fffff87d)

```scala
final def ++(that: IterableOnce[T]): Set[T]
```

Alias for concat.

Type parameters: none.

Parameters, list 1: `` that: IterableOnce[T] ``.

Returns: `` Set[T] ``.

Source contract/attributes: Alias for concat. Value parameters that the collection containing the elements to add Attributes Inherited from: SetOps

Invocation template:

```scala
receiver.++(that)
```

## `` com.cra.figaro.util.HashSelectableSet.++= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#++=-fffffb02)

```scala
final def ++=(elems: IterableOnce[T]): this.type
```

Alias for addAll.

Type parameters: none.

Parameters, list 1: `` elems: IterableOnce[T] ``.

Returns: `` this.type ``.

Source contract/attributes: Alias for addAll. Attributes Inherited from: Growable

Invocation template:

```scala
receiver.++=(elems)
```

## `` com.cra.figaro.util.HashSelectableSet.+= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#+=-b4a)

```scala
final def +=(elem: T): this.type
```

Alias for addOne.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` this.type ``.

Source contract/attributes: Alias for addOne. Attributes Inherited from: Growable

Invocation template:

```scala
receiver.+=(elem)
```

## `` com.cra.figaro.util.HashSelectableSet.--= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#--=-fffff8b8)

```scala
final def --=(xs: IterableOnce[T]): this.type
```

Alias for subtractAll.

Type parameters: none.

Parameters, list 1: `` xs: IterableOnce[T] ``.

Returns: `` this.type ``.

Source contract/attributes: Alias for subtractAll. Attributes Inherited from: Shrinkable

Invocation template:

```scala
receiver.--=(xs)
```

## `` com.cra.figaro.util.HashSelectableSet.-= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#-=-fffff700)

```scala
final def -=(elem: T): this.type
```

Alias for subtractOne.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` this.type ``.

Source contract/attributes: Alias for subtractOne. Attributes Inherited from: Shrinkable

Invocation template:

```scala
receiver.-=(elem)
```

## `` com.cra.figaro.util.HashSelectableSet.add ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#add-4ad)

```scala
def add(elem: T): Boolean
```

Checks whether the set contains the given element, and adds it if not.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` Boolean ``.

Source contract/attributes: Checks whether the set contains the given element, and adds it if not. Value parameters elem the element to be added Attributes Returns true if the element was added Inherited from: SetOps

Invocation template:

```scala
receiver.add(elem)
```

## `` com.cra.figaro.util.HashSelectableSet.addAll ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#addAll-fffffb02)

```scala
def addAll(elems: IterableOnce[T]): this.type
```

Add s all elements produced by an IterableOnce to this mutable set.

Type parameters: none.

Parameters, list 1: `` elems: IterableOnce[T] ``.

Returns: `` this.type ``.

Source contract/attributes: Add s all elements produced by an IterableOnce to this mutable set. Value parameters elems the IterableOnce producing the elements to add. Attributes Returns the mutable set itself. Inherited from: Growable

Invocation template:

```scala
receiver.addAll(elems)
```

## `` com.cra.figaro.util.HashSelectableSet.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#addString-ff4)

```scala
final def addString(b: StringBuilder): b.type
```

Appends all elements of this mutable set to a string builder. The written text consists of the string representations (w.r.t. the method toString) of all elements of this mutable set without any separator string.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this mutable set to a string builder. The written text consists of the string representations (w.r.t. the method toString) of all elements of this mutable set without any separator string. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() val h = a.addString(b) // 1234 Value parameters b the string builder to which elements are appended. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b)
```

## `` com.cra.figaro.util.HashSelectableSet.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#addString-e99)

```scala
final def addString(b: StringBuilder, sep: String): b.type
```

Appends all elements of this mutable set to a string builder using a separator string. The written text consists of the string representations (w.r.t. the method toString) of all elements of this mutable set, separated by the string sep.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``; `` sep: String ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this mutable set to a string builder using a separator string. The written text consists of the string representations (w.r.t. the method toString) of all elements of this mutable set, separated by the string sep. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() a.addString(b, ", ") // 1, 2, 3, 4 Value parameters b the string builder to which elements are appended. sep the separator string. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b, sep)
```

## `` com.cra.figaro.util.HashSelectableSet.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#addString-de3)

```scala
def addString(b: StringBuilder, start: String, sep: String, end: String): b.type
```

Appends all elements of this mutable set to a string builder using start, end, and separator strings. The written text begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this mutable set are separated by the string sep.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``; `` start: String ``; `` sep: String ``; `` end: String ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this mutable set to a string builder using start, end, and separator strings. The written text begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this mutable set are separated by the string sep. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() a.addString(b , "List(" , ", " , ")") // List(1, 2, 3, 4) Value parameters b the string builder to which elements are appended. end the ending string. sep the separator string. start the starting string. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b, start, sep, end)
```

## `` com.cra.figaro.util.HashSelectableSet.andThen ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#andThen-51)

```scala
def andThen[A](g: Boolean => A): T => A
```

Composes two instances of Function1 in a new Function1, with this function applied first.

Type parameters: `` A ``.

Parameters, list 1: `` g: Boolean => A ``.

Returns: `` T => A ``.

Source contract/attributes: Composes two instances of Function1 in a new Function1, with this function applied first. Type parameters A the result type of function g Value parameters g a function R => A Attributes Returns a new function f such that f(x) == g(apply(x)) Inherited from: Function1

Invocation template:

```scala
receiver.andThen[A](g)
```

## `` com.cra.figaro.util.HashSelectableSet.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#apply-4ad)

```scala
final def apply(elem: T): Boolean
```

Tests if some element is contained in this set.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests if some element is contained in this set. This method is equivalent to contains. It allows sets to be interpreted as predicates. Value parameters elem the element to test for membership. Attributes Returns true if elem is contained in this set, false otherwise. Inherited from: SetOps

Invocation template:

```scala
receiver.apply(elem)
```

## `` com.cra.figaro.util.HashSelectableSet.canEqual ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#canEqual-4ad)

```scala
def canEqual(that: Any): Boolean
```

Checks whether this instance can possibly equal that.

Type parameters: none.

Parameters, list 1: `` that: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Checks whether this instance can possibly equal that. A method that should be called from every well-designed equals method that is open to be overridden in a subclass. See Programming in Scala, Chapter 28 for discussion and design. Value parameters that the value being probed for possible equality Attributes Returns true if this instance can possibly equal that, otherwise false Inherited from: Set

Invocation template:

```scala
receiver.canEqual(that)
```

## `` com.cra.figaro.util.HashSelectableSet.collect ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#collect-fffff5b3)

```scala
def collect[B](pf: PartialFunction[T, B]): Set[B]
```

Builds a new mutable set by applying a partial function to all elements of this mutable set on which the function is defined.

Type parameters: `` B ``.

Parameters, list 1: `` pf: PartialFunction[T, B] ``.

Returns: `` Set[B] ``.

Source contract/attributes: Builds a new mutable set by applying a partial function to all elements of this mutable set on which the function is defined. Type parameters B the element type of the returned mutable set. Value parameters pf the partial function which filters and maps the mutable set. Attributes Returns a new mutable set resulting from applying the given partial function pf to each element on which it is defined and collecting the results. The order of the elements is preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.collect[B](pf)
```

## `` com.cra.figaro.util.HashSelectableSet.collectFirst ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#collectFirst-25d)

```scala
def collectFirst[B](pf: PartialFunction[T, B]): Option[B]
```

Finds the first element of the mutable set for which the given partial function is defined, and applies the partial function to it.

Type parameters: `` B ``.

Parameters, list 1: `` pf: PartialFunction[T, B] ``.

Returns: `` Option[B] ``.

Source contract/attributes: Finds the first element of the mutable set for which the given partial function is defined, and applies the partial function to it. Note: may not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Type parameters B the result type of the partial function Value parameters pf the partial function Attributes Returns an option value containing pf applied to the first value for which it is defined, or None if none exists. Example Seq("a", 1, 5L).collectFirst { case x: Int => x*10 } = Some(10) Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.collectFirst[B](pf)
```

## `` com.cra.figaro.util.HashSelectableSet.compose ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#compose-51)

```scala
def compose[A](g: A => T): A => Boolean
```

Composes two instances of Function1 in a new Function1, with this function applied last.

Type parameters: `` A ``.

Parameters, list 1: `` g: A => T ``.

Returns: `` A => Boolean ``.

Source contract/attributes: Composes two instances of Function1 in a new Function1, with this function applied last. Type parameters A the type to which function g can be applied Value parameters g a function A => T1 Attributes Returns a new function f such that f(x) == apply(g(x)) Inherited from: Function1

Invocation template:

```scala
receiver.compose[A](g)
```

## `` com.cra.figaro.util.HashSelectableSet.concat ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#concat-1dd)

```scala
def concat[B >: T](suffix: IterableOnce[B]): Set[B]
```

Returns a new mutable set containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the mutable set is the most specific superclass encompassing the element types of the two operands.

Type parameters: `` B >: T ``.

Parameters, list 1: `` suffix: IterableOnce[B] ``.

Returns: `` Set[B] ``.

Source contract/attributes: Returns a new mutable set containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the mutable set is the most specific superclass encompassing the element types of the two operands. Type parameters B the element type of the returned collection. Value parameters suffix the iterable to append. Attributes Returns a new mutable set which contains all elements of this mutable set followed by all elements of suffix. Inherited from: IterableOps

Invocation template:

```scala
receiver.concat[B](suffix)
```

## `` com.cra.figaro.util.HashSelectableSet.concat ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#concat-fffff87d)

```scala
def concat(that: IterableOnce[T]): Set[T]
```

Creates a new mutable set by adding all elements contained in another collection to this mutable set, omitting duplicates.

Type parameters: none.

Parameters, list 1: `` that: IterableOnce[T] ``.

Returns: `` Set[T] ``.

Source contract/attributes: Creates a new mutable set by adding all elements contained in another collection to this mutable set, omitting duplicates. Example: scala> val a = Set(1, 2) concat Set(2, 3) a: scala.collection.immutable.Set[Int] = Set(1, 2, 3) Value parameters that the collection containing the elements to add. Attributes Returns a new mutable set with the given elements added, omitting duplicates. Inherited from: SetOps

Invocation template:

```scala
receiver.concat(that)
```

## `` com.cra.figaro.util.HashSelectableSet.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#copyToArray-fffffd03)

```scala
def copyToArray[B >: T](dest: Array[B], start: Int, n: Int): Int
```

Copies elements to an array and returns the number of elements written.

Type parameters: `` B >: T ``.

Parameters, list 1: `` dest: Array[B] ``; `` start: Int ``; `` n: Int ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array and returns the number of elements written. Fills the given array dest starting at index start with at most n elements of this mutable set. Copying will stop once either all the elements of this mutable set have been copied, or the end of the array is reached, or n elements have been copied. If start is less than zero, it is taken as zero. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. n the maximal number of elements to copy. start the starting index of xs. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest, start, n)
```

## `` com.cra.figaro.util.HashSelectableSet.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#copyToArray-fffffaa4)

```scala
def copyToArray[B >: T](dest: Array[B], start: Int): Int
```

Copies elements to an array, returning the number of elements written.

Type parameters: `` B >: T ``.

Parameters, list 1: `` dest: Array[B] ``; `` start: Int ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array, returning the number of elements written. Fills the given array dest starting at index start with values of this mutable set. Copying will stop once either all the elements of this mutable set have been copied, or the end of the array is reached. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. start the starting index of xs. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest, start)
```

## `` com.cra.figaro.util.HashSelectableSet.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#copyToArray-fffff3e3)

```scala
def copyToArray[B >: T](dest: Array[B]): Int
```

Copies elements to an array, returning the number of elements written.

Type parameters: `` B >: T ``.

Parameters, list 1: `` dest: Array[B] ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array, returning the number of elements written. Fills the given array dest starting at index start with values of this mutable set. Copying will stop once either all the elements of this mutable set have been copied, or the end of the array is reached. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest)
```

## `` com.cra.figaro.util.HashSelectableSet.corresponds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#corresponds-958)

```scala
def corresponds[B](that: IterableOnce[B])(p: (T, B) => Boolean): Boolean
```

Tests whether every element of this collection's iterator relates to the corresponding element of another collection by satisfying a test predicate.

Type parameters: `` B ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Parameters, list 2: `` p: (T, B) => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether every element of this collection's iterator relates to the corresponding element of another collection by satisfying a test predicate. Note: will not terminate for infinite-sized collections. Type parameters B the type of the elements of that Value parameters p the test predicate, which relates elements from both collections that the other collection Attributes Returns true if both collections have the same length and p(x, y) is true for all corresponding elements x of this iterator and y of that, otherwise false Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.corresponds[B](that)(p)
```

## `` com.cra.figaro.util.HashSelectableSet.count ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#count-db6)

```scala
def count(p: T => Boolean): Int
```

Counts the number of elements in the mutable set which satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Int ``.

Source contract/attributes: Counts the number of elements in the mutable set which satisfy a predicate. Note: will not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns the number of elements satisfying the predicate p. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.count(p)
```

## `` com.cra.figaro.util.HashSelectableSet.diff ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#diff-7a6)

```scala
def diff(that: Set[T]): Set[T]
```

Computes the difference of this set and another set.

Type parameters: none.

Parameters, list 1: `` that: Set[T] ``.

Returns: `` Set[T] ``.

Source contract/attributes: Computes the difference of this set and another set. Value parameters that the set of elements to exclude. Attributes Returns a set containing those elements of this set that are not also contained in the given set that. Inherited from: SetOps

Invocation template:

```scala
receiver.diff(that)
```

## `` com.cra.figaro.util.HashSelectableSet.drop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#drop-aba)

```scala
def drop(n: Int): Set[T]
```

Selects all elements except the first n ones.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Set[T] ``.

Source contract/attributes: Selects all elements except the first n ones. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to drop from this mutable set. Attributes Returns a mutable set consisting of all elements of this mutable set except the first n ones, or else the empty mutable set, if this mutable set has less than n elements. If n is negative, don't drop any elements. Inherited from: IterableOps

Invocation template:

```scala
receiver.drop(n)
```

## `` com.cra.figaro.util.HashSelectableSet.dropRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#dropRight-aba)

```scala
def dropRight(n: Int): Set[T]
```

Selects all elements except last n ones.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Set[T] ``.

Source contract/attributes: Selects all elements except last n ones. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to drop from this mutable set. Attributes Returns a mutable set consisting of all elements of this mutable set except the last n ones, or else the empty mutable set, if this mutable set has less than n elements. If n is negative, don't drop any elements. Inherited from: IterableOps

Invocation template:

```scala
receiver.dropRight(n)
```

## `` com.cra.figaro.util.HashSelectableSet.dropWhile ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#dropWhile-c24)

```scala
def dropWhile(p: T => Boolean): Set[T]
```

Selects all elements except the longest prefix that satisfies a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Set[T] ``.

Source contract/attributes: Selects all elements except the longest prefix that satisfies a predicate. The matching prefix starts with the first element of this mutable set, and the element following the prefix is the first element that does not satisfy the predicate. The matching prefix may be empty, so that this method returns the entire mutable set. Example: List(1, 2, 3, 100, 4).dropWhile(n => n < 10) // List(100, 4) List(1, 2, 3, 100, 4).dropWhile(n => n == 0) // List(1, 2, 3, 100, 4) Use span to obtain both the prefix and suffix. Use filterNot to drop all elements that satisfy the predicate. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p The predicate used to test elements. Attributes Returns the longest suffix of this mutable set whose first element does not satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.dropWhile(p)
```

## `` com.cra.figaro.util.HashSelectableSet.empty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#empty-0)

```scala
override def empty: Set[T]
```

The empty mutable set.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[T] ``.

Source contract/attributes: The empty mutable set. Attributes Returns an empty iterable of type mutable.Set . Definition Classes IterableFactoryDefaults -> IterableOps Inherited from: IterableFactoryDefaults

Invocation template:

```scala
receiver.empty
```

## `` com.cra.figaro.util.HashSelectableSet.equals ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#equals-4ad)

```scala
override def equals(that: Any): Boolean
```

Equality of sets is implemented using the lookup method contains. This method returns true if

Type parameters: none.

Parameters, list 1: `` that: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Equality of sets is implemented using the lookup method contains. This method returns true if the argument that is a Set, the two sets have the same size, and for every element this set, other.contains(element) == true. The implementation of equals checks the canEqual method, so subclasses of Set can narrow down the equality to specific set types. The Set implementations in the standard library can all be compared, their canEqual methods return true. Note: The equals method only respects the equality laws (symmetry, transitivity) if the two sets use the same element equivalence function in their lookup operation. For example, the element equivalence operation in a scala.collection.immutable.TreeSet is defined by its ordering. Comparing a TreeSet with a HashSet leads to unexpected results if ordering.equiv(e1, e2) (used for lookup in TreeSet) is different from e1 == e2 (used for lookup in HashSet). import scala.collection.immutable._ val ord: Ordering[String] = _ compareToIgnoreCase _ val result1 = TreeSet("A")(using ord) == HashSet("a") // false val result2 = HashSet("a") == TreeSet("A")(using ord) // true Value parameters that The set to which this set is compared Attributes Returns true if the two sets are equal according to the description Definition Classes Set -> Equals -> Any Inherited from: Set

Invocation template:

```scala
receiver.equals(that)
```

## `` com.cra.figaro.util.HashSelectableSet.exists ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#exists-fffff66f)

```scala
def exists(p: T => Boolean): Boolean
```

Tests whether a predicate holds for at least one element of this mutable set.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether a predicate holds for at least one element of this mutable set. Note: may not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns true if the given predicate p is satisfied by at least one element of this mutable set, otherwise false Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.exists(p)
```

## `` com.cra.figaro.util.HashSelectableSet.filter ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#filter-c24)

```scala
def filter(pred: T => Boolean): Set[T]
```

Selects all elements of this mutable set which satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` pred: T => Boolean ``.

Returns: `` Set[T] ``.

Source contract/attributes: Selects all elements of this mutable set which satisfy a predicate. Value parameters p the predicate used to test elements. Attributes Returns a new mutable set consisting of all elements of this mutable set that satisfy the given predicate p. The order of the elements is preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.filter(pred)
```

## `` com.cra.figaro.util.HashSelectableSet.filterInPlace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#filterInPlace-acf)

```scala
def filterInPlace(p: T => Boolean): this.type
```

Removes all elements from the set for which do not satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` this.type ``.

Source contract/attributes: Removes all elements from the set for which do not satisfy a predicate. Value parameters p the predicate used to test elements. Only elements for which p returns true are retained in the set; all others are removed. Attributes Inherited from: SetOps

Invocation template:

```scala
receiver.filterInPlace(p)
```

## `` com.cra.figaro.util.HashSelectableSet.filterNot ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#filterNot-c24)

```scala
def filterNot(pred: T => Boolean): Set[T]
```

Selects all elements of this mutable set which do not satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` pred: T => Boolean ``.

Returns: `` Set[T] ``.

Source contract/attributes: Selects all elements of this mutable set which do not satisfy a predicate. Value parameters pred the predicate used to test elements. Attributes Returns a new mutable set consisting of all elements of this mutable set that do not satisfy the given predicate pred. Their order may not be preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.filterNot(pred)
```

## `` com.cra.figaro.util.HashSelectableSet.find ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#find-94e)

```scala
def find(p: T => Boolean): Option[T]
```

Finds the first element of the mutable set satisfying a predicate, if any.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the first element of the mutable set satisfying a predicate, if any. Note: may not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the predicate used to test elements. Attributes Returns an option value containing the first element in the mutable set that satisfies p, or None if none exists. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.find(p)
```

## `` com.cra.figaro.util.HashSelectableSet.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#flatMap-5d3)

```scala
def flatMap[B](f: T => IterableOnce[B]): Set[B]
```

Builds a new mutable set by applying a function to all elements of this mutable set and using the elements of the resulting collections.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => IterableOnce[B] ``.

Returns: `` Set[B] ``.

Source contract/attributes: Builds a new mutable set by applying a function to all elements of this mutable set and using the elements of the resulting collections. For example: def getWords(lines: Seq[String]): Seq[String] = lines.flatMap(line => line.split("\\W+")) The type of the resulting collection is guided by the static type of this mutable set. This might cause unexpected results sometimes. For example: // lettersOf will return a Seq[Char] of likely repeated letters, instead of a Set def lettersOf(words: Seq[String]) = words.flatMap(word => word.toSet) // lettersOf2 will return a Set[Char], not a Seq def lettersOf2(words: Seq[String]) = words.toSet.flatMap(word => word.toSeq) // xs will be an Iterable[Int] val xs = Map("a" -> List(11, 111), "b" -> List(22, 222)).flatMap(_._2) // ys will be a Map[Int, Int] val ys = Map("a" -> List(1 -> 11, 1 -> 111), "b" -> List(2 -> 22, 2 -> 222)).flatMap(_._2) Type parameters B the element type of the returned collection. Value parameters f the function to apply to each element. Attributes Returns a new mutable set resulting from applying the given collection-valued function f to each element of this mutable set and concatenating the results. Inherited from: IterableOps

Invocation template:

```scala
receiver.flatMap[B](f)
```

## `` com.cra.figaro.util.HashSelectableSet.flatten ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#flatten-5d3)

```scala
def flatten[B](implicit asIterable: T => IterableOnce[B]): Set[B]
```

Given that the elements of this collection are themselves iterable collections, converts this mutable set into a mutable set comprising the elements of these iterable collections.

Type parameters: `` B ``.

Context parameters, list 1: `` asIterable: T => IterableOnce[B] ``.

Returns: `` Set[B] ``.

Source contract/attributes: Given that the elements of this collection are themselves iterable collections, converts this mutable set into a mutable set comprising the elements of these iterable collections. The resulting collection's type will be guided by the type of mutable set. For example: val xs = List( Set(1, 2, 3), Set(1, 2, 3) ).flatten // xs == List(1, 2, 3, 1, 2, 3) val ys = Set( List(1, 2, 3), List(3, 2, 1) ).flatten // ys == Set(1, 2, 3) Type parameters B the type of the elements of each iterable collection. Value parameters asIterable an implicit conversion which asserts that the element type of this mutable set is an Iterable. Attributes Returns a new mutable set resulting from concatenating all element collections. Inherited from: IterableOps

Invocation template:

```scala
receiver.flatten[B](using asIterable)
```

## `` com.cra.figaro.util.HashSelectableSet.fold ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#fold-fffff9e1)

```scala
def fold[A1 >: T](z: A1)(op: (A1, A1) => A1): A1
```

Applies the given binary operator op to the given initial value z and all elements of this mutable set.

Type parameters: `` A1 >: T ``.

Parameters, list 1: `` z: A1 ``.

Parameters, list 2: `` op: (A1, A1) => A1 ``.

Returns: `` A1 ``.

Source contract/attributes: Applies the given binary operator op to the given initial value z and all elements of this mutable set. For each application of the operator, each operand is either an element of this mutable set, the initial value, or another such application of the operator. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. The initial value may be used an arbitrary number of times, but at least once. If this collection is ordered, then for any application of the operator, the element(s) appearing in the left operand will precede those in the right. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. In either case, it is also necessary that the initial value be a neutral value for the operator, e.g. Nil for List concatenation or 1 for multiplication. The default implementation in IterableOnce is equivalent to foldLeft but may be overridden for more efficient traversal orders. Note: will not terminate for infinite-sized collections. Type parameters A1 The type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. z An initial value; may be used an arbitrary number of times in the computation of the result; must be a neutral value for op for the result to always be the same across runs. Attributes Returns The result of applying op between all the elements and z, or z if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.fold[A1](z)(op)
```

## `` com.cra.figaro.util.HashSelectableSet.foldLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#foldLeft-fffff9e1)

```scala
def foldLeft[B](z: B)(op: (B, T) => B): B
```

Applies the given binary operator op to the given initial value z and all elements of this mutable set, going left to right. Returns the initial value if this mutable set is empty.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, T) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to the given initial value z and all elements of this mutable set, going left to right. Returns the initial value if this mutable set is empty. "Going left to right" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this mutable set, the result is op( op( ... op( op(z, x,,1,,), x,,2,,) ... ), x,,n,,). If this collection is not ordered, then for each application of the operator, each right operand is an element. In addition, the leftmost operand is the initial value, and each other left operand is itself an application of the operator. The elements of this mutable set and the initial value all appear exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator. Value parameters op A binary operator. z An initial value. Attributes Returns The result of applying op to z and all elements of this mutable set, going left to right. Returns z if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foldLeft[B](z)(op)
```

## `` com.cra.figaro.util.HashSelectableSet.foldRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#foldRight-fffff9e1)

```scala
def foldRight[B](z: B)(op: (T, B) => B): B
```

Applies the given binary operator op to all elements of this mutable set and the given initial value z, going right to left. Returns the initial value if this mutable set is empty.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (T, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this mutable set and the given initial value z, going right to left. Returns the initial value if this mutable set is empty. "Going right to left" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this mutable set, the result is op(x,,1,,, op(x,,2,,, op( ... op(x,,n,,, z) ... ))). If this collection is not ordered, then for each application of the operator, each left operand is an element. In addition, the rightmost operand is the initial value, and each other right operand is itself an application of the operator. The elements of this mutable set and the initial value all appear exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator. Value parameters op A binary operator. z An initial value. Attributes Returns The result of applying op to all elements of this mutable set and z, going right to left. Returns z if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foldRight[B](z)(op)
```

## `` com.cra.figaro.util.HashSelectableSet.forall ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#forall-fffff66f)

```scala
def forall(p: T => Boolean): Boolean
```

Tests whether a predicate holds for all elements of this mutable set.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether a predicate holds for all elements of this mutable set. Note: may not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns true if this mutable set is empty or the given predicate p holds for all elements of this mutable set, otherwise false. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.forall(p)
```

## `` com.cra.figaro.util.HashSelectableSet.foreach ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#foreach-58c)

```scala
def foreach[U](f: T => U): Unit
```

Applies f to each element for its side effects. Note: U parameter needed to help scalac's type inference.

Type parameters: `` U ``.

Parameters, list 1: `` f: T => U ``.

Returns: `` Unit ``.

Source contract/attributes: Applies f to each element for its side effects. Note: U parameter needed to help scalac's type inference. Type parameters U the return type of f; the value is discarded, but the type parameter aids type inference Value parameters f the function to apply to each element for its side effects Attributes Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foreach[U](f)
```

## `` com.cra.figaro.util.HashSelectableSet.groupBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#groupBy-f68)

```scala
def groupBy[K](f: T => K): Map[K, Set[T]]
```

Partitions this mutable set into a map of mutable sets according to some discriminator function.

Type parameters: `` K ``.

Parameters, list 1: `` f: T => K ``.

Returns: `` Map[K, Set[T]] ``.

Source contract/attributes: Partitions this mutable set into a map of mutable sets according to some discriminator function. Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters K the type of keys returned by the discriminator function. Value parameters f the discriminator function. Attributes Returns A map from keys to mutable sets such that the following invariant holds: (xs groupBy f)(k) = xs filter (x => f(x) == k) That is, every key `k` is bound to a mutable set of those elements `x` for which `f(x)` equals `k`. Inherited from: IterableOps

Invocation template:

```scala
receiver.groupBy[K](f)
```

## `` com.cra.figaro.util.HashSelectableSet.groupMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#groupMap-fffff03a)

```scala
def groupMap[K, B](key: T => K)(f: T => B): Map[K, Set[B]]
```

Partitions this mutable set into a map of mutable sets according to a discriminator function key. Each element in a group is transformed into a value of type B using the value function.

Type parameters: `` K ``; `` B ``.

Parameters, list 1: `` key: T => K ``.

Parameters, list 2: `` f: T => B ``.

Returns: `` Map[K, Set[B]] ``.

Source contract/attributes: Partitions this mutable set into a map of mutable sets according to a discriminator function key. Each element in a group is transformed into a value of type B using the value function. It is equivalent to groupBy(key).mapValues(_.map(f)), but more efficient. case class User(name: String, age: Int) def namesByAge(users: Seq[User]): Map[Int, Seq[String]] = users.groupMap(_.age)(_.name) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of values returned by the transformation function K the type of keys returned by the discriminator function Value parameters f the element transformation function key the discriminator function Attributes Returns a map associating each key k produced by key with a mutable set of values produced by applying f to elements that map to k Inherited from: IterableOps

Invocation template:

```scala
receiver.groupMap[K, B](key)(f)
```

## `` com.cra.figaro.util.HashSelectableSet.groupMapReduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#groupMapReduce-8e8)

```scala
def groupMapReduce[K, B](key: T => K)(f: T => B)(reduce: (B, B) => B): Map[K, B]
```

Partitions this mutable set into a map according to a discriminator function key. All the values that have the same discriminator are then transformed by the f function and then reduced into a single value with the reduce function.

Type parameters: `` K ``; `` B ``.

Parameters, list 1: `` key: T => K ``.

Parameters, list 2: `` f: T => B ``.

Parameters, list 3: `` reduce: (B, B) => B ``.

Returns: `` Map[K, B] ``.

Source contract/attributes: Partitions this mutable set into a map according to a discriminator function key. All the values that have the same discriminator are then transformed by the f function and then reduced into a single value with the reduce function. It is equivalent to groupBy(key).mapValues(_.map(f).reduce(reduce)), but more efficient. def occurrences[A](as: Seq[A]): Map[A, Int] = as.groupMapReduce(identity)(_ => 1)(_ + _) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of values returned by the transformation function K the type of keys returned by the discriminator function Value parameters f the element transformation function key the discriminator function reduce the reduction function used to combine values mapped to the same key Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.groupMapReduce[K, B](key)(f)(reduce)
```

## `` com.cra.figaro.util.HashSelectableSet.grouped ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#grouped-fffffbef)

```scala
def grouped(size: Int): Iterator[Set[T]]
```

Partitions elements in fixed size mutable sets.

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Iterator[Set[T]] ``.

Source contract/attributes: Partitions elements in fixed size mutable sets. Value parameters size the number of elements per group Attributes Returns An iterator producing mutable sets of size size, except the last will be less than size size if the elements don't divide evenly. See also scala.collection.Iterator, method grouped Inherited from: IterableOps

Invocation template:

```scala
receiver.grouped(size)
```

## `` com.cra.figaro.util.HashSelectableSet.hashCode ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#hashCode-ffffff27)

```scala
override def hashCode(): Int
```

Calculates a hash code value for the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Int ``.

Source contract/attributes: Calculates a hash code value for the object. The default hashing algorithm is platform dependent. Note that it is allowed for two objects to have identical hash codes (o1.hashCode.equals(o2.hashCode)) yet not be equal (o1.equals(o2) returns false). A degenerate implementation could always return 0. However, it is required that if two objects are equal (o1.equals(o2) returns true) that they have identical hash codes (o1.hashCode.equals(o2.hashCode)). Therefore, when overriding this method, be sure to verify that the behavior is consistent with the equals method. Attributes Returns the hash code value for this object. Definition Classes Set -> Any Inherited from: Set

Invocation template:

```scala
receiver.hashCode()
```

## `` com.cra.figaro.util.HashSelectableSet.head ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#head-0)

```scala
def head: T
```

Selects the first element of this mutable set.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` T ``.

Source contract/attributes: Selects the first element of this mutable set. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the first element of this mutable set. Throws NoSuchElementException if the mutable set is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.head
```

## `` com.cra.figaro.util.HashSelectableSet.headOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#headOption-0)

```scala
def headOption: Option[T]
```

Optionally selects the first element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Option[T] ``.

Source contract/attributes: Optionally selects the first element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the first element of this mutable set if it is nonempty, None if it is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.headOption
```

## `` com.cra.figaro.util.HashSelectableSet.init ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#init-0)

```scala
def init: Set[T]
```

The initial part of the collection without its last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[T] ``.

Source contract/attributes: The initial part of the collection without its last element. Note: Even when applied to a view or a lazy collection it will always force the elements. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.init
```

## `` com.cra.figaro.util.HashSelectableSet.inits ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#inits-0)

```scala
def inits: Iterator[Set[T]]
```

Iterates over the inits of this mutable set. The first value will be this mutable set and the final one will be an empty mutable set, with the intervening values the results of successive applications of init.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Set[T]] ``.

Source contract/attributes: Iterates over the inits of this mutable set. The first value will be this mutable set and the final one will be an empty mutable set, with the intervening values the results of successive applications of init. Note: Even when applied to a view or a lazy collection it will always force the elements. Attributes Returns an iterator over all the inits of this mutable set Example List(1,2,3).inits = Iterator(List(1,2,3), List(1,2), List(1), Nil) Inherited from: IterableOps

Invocation template:

```scala
receiver.inits
```

## `` com.cra.figaro.util.HashSelectableSet.intersect ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#intersect-dde)

```scala
def intersect(that: Set[T]): Set[T]
```

Computes the intersection between this set and another set.

Type parameters: none.

Parameters, list 1: `` that: Set[T] ``.

Returns: `` Set[T] ``.

Source contract/attributes: Computes the intersection between this set and another set. Value parameters that the set to intersect with. Attributes Returns a new set consisting of all elements that are both in this set and in the given set that. Inherited from: SetOps

Invocation template:

```scala
receiver.intersect(that)
```

## `` com.cra.figaro.util.HashSelectableSet.isEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#isEmpty-0)

```scala
def isEmpty: Boolean
```

Tests whether the mutable set is empty.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether the mutable set is empty. Note: The default implementation creates and discards an iterator. Note: Implementations in subclasses that are not repeatedly iterable must take care not to consume any elements when isEmpty is called. Attributes Returns true if the mutable set contains no elements, false otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.isEmpty
```

## `` com.cra.figaro.util.HashSelectableSet.isTraversableAgain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#isTraversableAgain-0)

```scala
override def isTraversableAgain: Boolean
```

Tests whether this mutable set can be repeatedly traversed. Always true for Iterables and false for Iterators unless overridden.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this mutable set can be repeatedly traversed. Always true for Iterables and false for Iterators unless overridden. Attributes Returns true if it is repeatedly traversable, false otherwise. Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.isTraversableAgain
```

## `` com.cra.figaro.util.HashSelectableSet.iterableFactory ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#iterableFactory-0)

```scala
override def iterableFactory: IterableFactory[Set]
```

The companion object of this mutable set, providing various factory methods.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IterableFactory[Set] ``.

Source contract/attributes: The companion object of this mutable set, providing various factory methods. Attributes Returns the companion factory object for this collection type Note When implementing a custom collection type and refining CC to the new type, this method needs to be overridden to return a factory for the new type (the compiler will issue an error otherwise). Definition Classes Set -> Set -> Iterable -> Iterable -> IterableOps Inherited from: Set

Invocation template:

```scala
receiver.iterableFactory
```

## `` com.cra.figaro.util.HashSelectableSet.knownSize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#knownSize-0)

```scala
override def knownSize: Int
```

The number of elements in the collection under construction, if it can be cheaply computed, -1 otherwise.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: The number of elements in the collection under construction, if it can be cheaply computed, -1 otherwise. Attributes Returns The number of elements. The default implementation always returns -1. Definition Classes SetOps -> Growable -> IterableOnce Inherited from: SetOps

Invocation template:

```scala
receiver.knownSize
```

## `` com.cra.figaro.util.HashSelectableSet.last ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#last-0)

```scala
def last: T
```

Selects the last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` T ``.

Source contract/attributes: Selects the last element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns The last element of this mutable set. Throws NoSuchElementException If the mutable set is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.last
```

## `` com.cra.figaro.util.HashSelectableSet.lastOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#lastOption-0)

```scala
def lastOption: Option[T]
```

Optionally selects the last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Option[T] ``.

Source contract/attributes: Optionally selects the last element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the last element of this mutable set if it is nonempty, None if it is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.lastOption
```

## `` com.cra.figaro.util.HashSelectableSet.lazyZip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#lazyZip-fffffd84)

```scala
def lazyZip[B](that: Iterable[B]): LazyZip2[T, B, this.type]
```

Analogous to zip except that the elements in each collection are not consumed until a strict operation is invoked on the returned LazyZip2 decorator.

Type parameters: `` B ``.

Parameters, list 1: `` that: Iterable[B] ``.

Returns: `` LazyZip2[T, B, this.type] ``.

Source contract/attributes: Analogous to zip except that the elements in each collection are not consumed until a strict operation is invoked on the returned LazyZip2 decorator. Calls to lazyZip can be chained to support higher arities (up to 4) without incurring the expense of constructing and deconstructing intermediary tuples. val xs = List(1, 2, 3) val res = (xs lazyZip xs lazyZip xs lazyZip xs).map((a, b, c, d) => a + b + c + d) // res == List(4, 8, 12) Type parameters B the type of the second element in each eventual pair Value parameters that the iterable providing the second element of each eventual pair Attributes Returns a decorator LazyZip2 that allows strict operations to be performed on the lazily evaluated pairs or chained calls to lazyZip. Implicit conversion to Iterable[(A, B)] is also supported. Inherited from: Iterable

Invocation template:

```scala
receiver.lazyZip[B](that)
```

## `` com.cra.figaro.util.HashSelectableSet.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#map-5d3)

```scala
def map[B](f: T => B): Set[B]
```

Builds a new mutable set by applying a function to all elements of this mutable set.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => B ``.

Returns: `` Set[B] ``.

Source contract/attributes: Builds a new mutable set by applying a function to all elements of this mutable set. Type parameters B the element type of the returned mutable set . Value parameters f the function to apply to each element. Attributes Returns a new mutable set resulting from applying the given function f to each element of this mutable set and collecting the results. Inherited from: IterableOps

Invocation template:

```scala
receiver.map[B](f)
```

## `` com.cra.figaro.util.HashSelectableSet.mapResult ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#mapResult-34b)

```scala
def mapResult[NewTo](f: Set[T] => NewTo): Builder[T, NewTo]
```

A builder resulting from this builder by mapping the result using f.

Type parameters: `` NewTo ``.

Parameters, list 1: `` f: Set[T] => NewTo ``.

Returns: `` Builder[T, NewTo] ``.

Source contract/attributes: A builder resulting from this builder by mapping the result using f. Type parameters NewTo the type of collection produced by the returned builder Value parameters f the function to apply to this builder's result Attributes Inherited from: Builder

Invocation template:

```scala
receiver.mapResult[NewTo](f)
```

## `` com.cra.figaro.util.HashSelectableSet.max ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#max-ffffff34)

```scala
def max[B >: T](implicit ord: Ordering[B]): T
```

Finds the largest element.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` T ``.

Source contract/attributes: Finds the largest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns the largest element of this mutable set with respect to the ordering ord. Throws UnsupportedOperationException if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.max[B](using ord)
```

## `` com.cra.figaro.util.HashSelectableSet.maxBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#maxBy-fffff0c5)

```scala
def maxBy[B](f: T => B)(implicit ord: Ordering[B]): T
```

Finds the first element which yields the largest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` T ``.

Source contract/attributes: Finds the first element which yields the largest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns the first element of this mutable set with the largest value measured by function f with respect to the ordering ord. Throws UnsupportedOperationException if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxBy[B](f)(using ord)
```

## `` com.cra.figaro.util.HashSelectableSet.maxByOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#maxByOption-fffff66f)

```scala
def maxByOption[B](f: T => B)(implicit ord: Ordering[B]): Option[T]
```

Finds the first element which yields the largest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the first element which yields the largest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns an option value containing the first element of this mutable set with the largest value measured by function f with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxByOption[B](f)(using ord)
```

## `` com.cra.figaro.util.HashSelectableSet.maxOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#maxOption-fffff45e)

```scala
def maxOption[B >: T](implicit ord: Ordering[B]): Option[T]
```

Finds the largest element.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the largest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns an option value containing the largest element of this mutable set with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxOption[B](using ord)
```

## `` com.cra.figaro.util.HashSelectableSet.min ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#min-ffffff34)

```scala
def min[B >: T](implicit ord: Ordering[B]): T
```

Finds the smallest element.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` T ``.

Source contract/attributes: Finds the smallest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns the smallest element of this mutable set with respect to the ordering ord. Throws UnsupportedOperationException if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.min[B](using ord)
```

## `` com.cra.figaro.util.HashSelectableSet.minBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#minBy-fffff0c5)

```scala
def minBy[B](f: T => B)(implicit ord: Ordering[B]): T
```

Finds the first element which yields the smallest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` T ``.

Source contract/attributes: Finds the first element which yields the smallest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns the first element of this mutable set with the smallest value measured by function f with respect to the ordering ord. Throws UnsupportedOperationException if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minBy[B](f)(using ord)
```

## `` com.cra.figaro.util.HashSelectableSet.minByOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#minByOption-fffff66f)

```scala
def minByOption[B](f: T => B)(implicit ord: Ordering[B]): Option[T]
```

Finds the first element which yields the smallest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the first element which yields the smallest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns an option value containing the first element of this mutable set with the smallest value measured by function f with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minByOption[B](f)(using ord)
```

## `` com.cra.figaro.util.HashSelectableSet.minOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#minOption-fffff45e)

```scala
def minOption[B >: T](implicit ord: Ordering[B]): Option[T]
```

Finds the smallest element.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the smallest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns an option value containing the smallest element of this mutable set with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minOption[B](using ord)
```

## `` com.cra.figaro.util.HashSelectableSet.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#mkString-0)

```scala
final def mkString: String
```

Displays all elements of this mutable set in a string.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Displays all elements of this mutable set in a string. Delegates to addString, which can be overridden. Attributes Returns a string representation of this mutable set. In the resulting string the string representations (w.r.t. the method toString) of all elements of this mutable set follow each other without any separator string. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString
```

## `` com.cra.figaro.util.HashSelectableSet.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#mkString-fffff2ca)

```scala
final def mkString(sep: String): String
```

Displays all elements of this mutable set in a string using a separator string.

Type parameters: none.

Parameters, list 1: `` sep: String ``.

Returns: `` String ``.

Source contract/attributes: Displays all elements of this mutable set in a string using a separator string. Delegates to addString, which can be overridden. Value parameters sep the separator string. Attributes Returns a string representation of this mutable set. In the resulting string the string representations (w.r.t. the method toString) of all elements of this mutable set are separated by the string sep. Example List(1, 2, 3).mkString("|") = "1|2|3" Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString(sep)
```

## `` com.cra.figaro.util.HashSelectableSet.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#mkString-d94)

```scala
final def mkString(start: String, sep: String, end: String): String
```

Displays all elements of this mutable set in a string using start, end, and separator strings.

Type parameters: none.

Parameters, list 1: `` start: String ``; `` sep: String ``; `` end: String ``.

Returns: `` String ``.

Source contract/attributes: Displays all elements of this mutable set in a string using start, end, and separator strings. Delegates to addString, which can be overridden. Value parameters end the ending string. sep the separator string. start the starting string. Attributes Returns a string representation of this mutable set. The resulting string begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this mutable set are separated by the string sep. Example List(1, 2, 3).mkString("(", "; ", ")") = "(1; 2; 3)" Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString(start, sep, end)
```

## `` com.cra.figaro.util.HashSelectableSet.nonEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#nonEmpty-0)

```scala
def nonEmpty: Boolean
```

Tests whether the mutable set is not empty.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether the mutable set is not empty. Attributes Returns true if the mutable set contains at least one element, false otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.nonEmpty
```

## `` com.cra.figaro.util.HashSelectableSet.partition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#partition-243)

```scala
def partition(p: T => Boolean): (Set[T], Set[T])
```

A pair of, first, all elements that satisfy predicate p and, second, all elements that do not.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` (Set[T], Set[T]) ``.

Source contract/attributes: A pair of, first, all elements that satisfy predicate p and, second, all elements that do not. The two mutable set correspond to the result of filter and filterNot, respectively. The default implementation provided here needs to traverse the collection twice. Strict collections have an overridden version of partition in StrictOptimizedIterableOps, which requires only a single traversal. Value parameters p the predicate used to test elements Attributes Returns a pair of mutable sets: the first containing all elements that satisfy p, the second containing those that do not Inherited from: IterableOps

Invocation template:

```scala
receiver.partition(p)
```

## `` com.cra.figaro.util.HashSelectableSet.partitionMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#partitionMap-2d1)

```scala
def partitionMap[A1, A2](f: T => Either[A1, A2]): (Set[A1], Set[A2])
```

Applies a function f to each element of the mutable set and returns a pair of mutable sets: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right.

Type parameters: `` A1 ``; `` A2 ``.

Parameters, list 1: `` f: T => Either[A1, A2] ``.

Returns: `` (Set[A1], Set[A2]) ``.

Source contract/attributes: Applies a function f to each element of the mutable set and returns a pair of mutable sets: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right. Example: val xs = $Coll(1, "one", 2, "two", 3, "three") partitionMap { case i: Int => Left(i) case s: String => Right(s) } // xs == ($Coll(1, 2, 3), // $Coll(one, two, three)) Type parameters A1 the element type of the first resulting collection A2 the element type of the second resulting collection Value parameters f the 'split function' mapping the elements of this mutable set to an scala.util.Either Attributes Returns a pair of mutable sets: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right. Inherited from: IterableOps

Invocation template:

```scala
receiver.partitionMap[A1, A2](f)
```

## `` com.cra.figaro.util.HashSelectableSet.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#product-533)

```scala
def product[B >: T](implicit num: Numeric[B]): B
```

Multiplies together the elements of this collection.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` num: Numeric[B] ``.

Returns: `` B ``.

Source contract/attributes: Multiplies together the elements of this collection. The default implementation uses reduce for a known non-empty collection, foldLeft otherwise. Note: will not terminate for infinite-sized collections. Type parameters B the result type of the * operator. Value parameters num an implicit parameter defining a set of numeric operations which includes the * operator to be used in forming the product. Attributes Returns the product of all elements of this mutable set with respect to the * operator in num. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.product[B](using num)
```

## `` com.cra.figaro.util.HashSelectableSet.reduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#reduce-fffff3d4)

```scala
def reduce[B >: T](op: (B, B) => B): B
```

Applies the given binary operator op to all elements of this mutable set.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (B, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this mutable set. For each application of the operator, each operand is either an element of this mutable set or another such application of the operator. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. If this collection is ordered, then for any application of the operator, the element(s) appearing in the left operand will precede those in the right. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. Attributes Returns The result of applying op between all the elements if the mutable set is nonempty. Throws UnsupportedOperationException if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduce[B](op)
```

## `` com.cra.figaro.util.HashSelectableSet.reduceLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#reduceLeft-fffff3d4)

```scala
def reduceLeft[B >: T](op: (B, T) => B): B
```

Applies the given binary operator op to all elements of this mutable set, going left to right.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (B, T) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this mutable set, going left to right. "Going left to right" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this mutable set, the result is op( op( op( ... op(x,,1,,, x,,2,,) ... ), x,,n-1,,), x,,n,,). If this collection is not ordered, then for each application of the operator, each right operand is an element. In addition, the leftmost operand is the first element of this mutable set and each other left operand is itself an application of the operator. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of applying op to all elements of this mutable set, going left to right. Throws UnsupportedOperationException if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceLeft[B](op)
```

## `` com.cra.figaro.util.HashSelectableSet.reduceLeftOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#reduceLeftOption-8fe)

```scala
def reduceLeftOption[B >: T](op: (B, T) => B): Option[B]
```

If this mutable set is nonempty, reduces it with the given binary operator op, going left to right.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (B, T) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this mutable set is nonempty, reduces it with the given binary operator op, going left to right. The behavior is the same as reduceLeft except that the value is None if the mutable set is empty. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of reducing this mutable set with op going left to right if the mutable set is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceLeftOption[B](op)
```

## `` com.cra.figaro.util.HashSelectableSet.reduceOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#reduceOption-8fe)

```scala
def reduceOption[B >: T](op: (B, B) => B): Option[B]
```

If this mutable set is nonempty, reduces it with the given binary operator op.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (B, B) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this mutable set is nonempty, reduces it with the given binary operator op. The behavior is the same as reduce except that the value is None if the mutable set is empty. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B A type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. Attributes Returns The result of reducing this mutable set with op if the mutable set is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceOption[B](op)
```

## `` com.cra.figaro.util.HashSelectableSet.reduceRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#reduceRight-fffff3d4)

```scala
def reduceRight[B >: T](op: (T, B) => B): B
```

Applies the given binary operator op to all elements of this mutable set, going right to left.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (T, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this mutable set, going right to left. "Going right to left" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this mutable set, the result is op(x,,1,,, op(x,,2,,, op( ... op(x,,n-1,,, x,,n,,) ... ))). If this collection is not ordered, then for each application of the operator, each left operand is an element. In addition, the rightmost operand is the last element of this mutable set and each other right operand is itself an application of the operator. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of applying op to all elements of this mutable set, going right to left. Throws UnsupportedOperationException if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceRight[B](op)
```

## `` com.cra.figaro.util.HashSelectableSet.reduceRightOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#reduceRightOption-8fe)

```scala
def reduceRightOption[B >: T](op: (T, B) => B): Option[B]
```

If this mutable set is nonempty, reduces it with the given binary operator op, going right to left.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (T, B) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this mutable set is nonempty, reduces it with the given binary operator op, going right to left. The behavior is the same as reduceRight except that the value is None if the mutable set is empty. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of reducing this mutable set with op going right to left if the mutable set is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceRightOption[B](op)
```

## `` com.cra.figaro.util.HashSelectableSet.remove ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#remove-4ad)

```scala
def remove(elem: T): Boolean
```

Removes an element from this set.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` Boolean ``.

Source contract/attributes: Removes an element from this set. Value parameters elem the element to be removed Attributes Returns true if this set contained the element before it was removed Inherited from: SetOps

Invocation template:

```scala
receiver.remove(elem)
```

## `` com.cra.figaro.util.HashSelectableSet.result ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#result-180)

```scala
def result(): Set[T]
```

Result collection consisting of all elements appended so far.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[T] ``.

Source contract/attributes: Result collection consisting of all elements appended so far. Attributes Inherited from: SetOps

Invocation template:

```scala
receiver.result()
```

## `` com.cra.figaro.util.HashSelectableSet.scan ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#scan-fffff9e1)

```scala
def scan[B >: T](z: B)(op: (B, B) => B): Set[B]
```

Computes a prefix scan of the elements of the collection.

Type parameters: `` B >: T ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, B) => B ``.

Returns: `` Set[B] ``.

Source contract/attributes: Computes a prefix scan of the elements of the collection. Note: The neutral element z may be applied more than once. Type parameters B element type of the resulting collection Value parameters op the associative operator for the scan z neutral element for the operator op Attributes Returns a new mutable set containing the prefix scan of the elements in this mutable set Inherited from: IterableOps

Invocation template:

```scala
receiver.scan[B](z)(op)
```

## `` com.cra.figaro.util.HashSelectableSet.scanLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#scanLeft-fffff9e1)

```scala
def scanLeft[B](z: B)(op: (B, T) => B): Set[B]
```

Produces a mutable set containing cumulative results of applying the operator going left to right, including the initial value.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, T) => B ``.

Returns: `` Set[B] ``.

Source contract/attributes: Produces a mutable set containing cumulative results of applying the operator going left to right, including the initial value. Note: will not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Type parameters B the type of the elements in the resulting collection Value parameters op the binary operator applied to the intermediate result and the element z the initial value Attributes Returns collection with intermediate results Inherited from: IterableOps

Invocation template:

```scala
receiver.scanLeft[B](z)(op)
```

## `` com.cra.figaro.util.HashSelectableSet.scanRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#scanRight-fffff9e1)

```scala
def scanRight[B](z: B)(op: (T, B) => B): Set[B]
```

Produces a collection containing cumulative results of applying the operator going right to left. The head of the collection is the last cumulative result.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (T, B) => B ``.

Returns: `` Set[B] ``.

Source contract/attributes: Produces a collection containing cumulative results of applying the operator going right to left. The head of the collection is the last cumulative result. Note: will not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: Even when applied to a view or a lazy collection it will always force the elements. Example: List(1, 2, 3, 4).scanRight(0)(_ + _) == List(10, 9, 7, 4, 0) Type parameters B the type of the elements in the resulting collection Value parameters op the binary operator applied to the intermediate result and the element z the initial value Attributes Returns collection with intermediate results Inherited from: IterableOps

Invocation template:

```scala
receiver.scanRight[B](z)(op)
```

## `` com.cra.figaro.util.HashSelectableSet.size ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#size-0)

```scala
def size: Int
```

The size of this mutable set.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: The size of this mutable set. Note: will not terminate for infinite-sized collections. Attributes Returns the number of elements in this mutable set. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.size
```

## `` com.cra.figaro.util.HashSelectableSet.sizeCompare ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#sizeCompare-32d)

```scala
def sizeCompare(that: Iterable[_]): Int
```

Compares the size of this mutable set to the size of another Iterable.

Type parameters: none.

Parameters, list 1: `` that: Iterable[_] ``.

Returns: `` Int ``.

Source contract/attributes: Compares the size of this mutable set to the size of another Iterable. Value parameters that the Iterable whose size is compared with this mutable set's size. Attributes Returns A value x where x < 0 if this.size < that.size x == 0 if this.size == that.size x > 0 if this.size > that.size The method as implemented here does not call size directly; its running time is O(this.size min that.size) instead of O(this.size + that.size). The method should be overridden if computing size is cheap and knownSize returns -1. Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeCompare(that)
```

## `` com.cra.figaro.util.HashSelectableSet.sizeCompare ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#sizeCompare-fffffbe0)

```scala
def sizeCompare(otherSize: Int): Int
```

Compares the size of this mutable set to a test value.

Type parameters: none.

Parameters, list 1: `` otherSize: Int ``.

Returns: `` Int ``.

Source contract/attributes: Compares the size of this mutable set to a test value. Value parameters otherSize the test value that gets compared with the size. Attributes Returns A value x where x < 0 if this.size < otherSize x == 0 if this.size == otherSize x > 0 if this.size > otherSize The default implementation does not call size, which may be arbitrarily expensive. It will use knownSize if nonnegative. Otherwise, it will iterate to compute an element count, with complexity O(size min otherSize). This method should be overridden if computing size is cheap and knownSize returns -1. In that case, the custom implementation should just compare size and otherSize directly. See also sizeIs Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeCompare(otherSize)
```

## `` com.cra.figaro.util.HashSelectableSet.sizeHint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#sizeHint-fffff4f8)

```scala
final def sizeHint(coll: IterableOnce[_], delta: Int = ...): Unit
```

Gives a hint that the result of this builder is expected to have the same size as the given collection, plus some delta.

Type parameters: none.

Parameters, list 1: `` coll: IterableOnce[_] ``; `` delta: Int = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Gives a hint that the result of this builder is expected to have the same size as the given collection, plus some delta. This method provides a hint only if the collection has a known size, as specified by the following code: val coll: scala.collection.IterableOnce[?] = ??? val delta: Int = 0 def sizeHint(size: Int): Unit = () if (coll.knownSize != -1) if (coll.knownSize + delta <= 0) sizeHint(0) else sizeHint(coll.knownSize + delta) If the delta is negative and the result size is known to be negative, then the size hint is issued at zero. Some builder classes will optimize their representation based on the hint. However, builder implementations are required to work correctly even if the hint is wrong, i.e., if a different number of elements is added. Value parameters coll the collection which serves as a hint for the result's size. delta a correction to add to the coll.size to produce the size hint (zero if omitted). Attributes Inherited from: Builder

Invocation template:

```scala
receiver.sizeHint(coll, delta)
```

## `` com.cra.figaro.util.HashSelectableSet.sizeHint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#sizeHint-fffff3b3)

```scala
def sizeHint(size: Int): Unit
```

Gives a hint how many elements are expected to be added in total by the time result is called.

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Unit ``.

Source contract/attributes: Gives a hint how many elements are expected to be added in total by the time result is called. Some builder classes will optimize their representation based on the hint. However, builder implementations are required to work correctly even if the hint is wrong, e.g., a different number of elements is added, or the hint is out of range. The default implementation simply ignores the hint. Value parameters size the hint how many elements will be added. Attributes Inherited from: Builder

Invocation template:

```scala
receiver.sizeHint(size)
```

## `` com.cra.figaro.util.HashSelectableSet.sizeHintBounded ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#sizeHintBounded-fffffd7f)

```scala
final def sizeHintBounded(size: Int, boundingColl: Iterable[_]): Unit
```

Gives a hint how many elements are expected to be added when the next result is called, together with an upper bound given by the size of some other collection. Some builder classes will optimize their representation based on the hint. However, builder implementations are still required to work correctly even if the hint is wrong, i.e. a different number of elements is added.

Type parameters: none.

Parameters, list 1: `` size: Int ``; `` boundingColl: Iterable[_] ``.

Returns: `` Unit ``.

Source contract/attributes: Gives a hint how many elements are expected to be added when the next result is called, together with an upper bound given by the size of some other collection. Some builder classes will optimize their representation based on the hint. However, builder implementations are still required to work correctly even if the hint is wrong, i.e. a different number of elements is added. Value parameters boundingColl the bounding collection. If it is an IndexedSeqLike, then sizes larger than collection's size are reduced. size the hint how many elements will be added. Attributes Inherited from: Builder

Invocation template:

```scala
receiver.sizeHintBounded(size, boundingColl)
```

## `` com.cra.figaro.util.HashSelectableSet.sizeIs ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#sizeIs-0)

```scala
final def sizeIs: SizeCompareOps
```

Returns a value class containing operations for comparing the size of this mutable set to a test value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` SizeCompareOps ``.

Source contract/attributes: Returns a value class containing operations for comparing the size of this mutable set to a test value. These operations are implemented in terms of `sizeCompare(Int)`, and allow the following more readable usages: this.sizeIs < size // this.sizeCompare(size) < 0 this.sizeIs <= size // this.sizeCompare(size) <= 0 this.sizeIs == size // this.sizeCompare(size) == 0 this.sizeIs != size // this.sizeCompare(size) != 0 this.sizeIs >= size // this.sizeCompare(size) >= 0 this.sizeIs > size // this.sizeCompare(size) > 0 Attributes Returns a wrapper that allows size comparisons using operators such as =, and > Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeIs
```

## `` com.cra.figaro.util.HashSelectableSet.slice ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#slice-573)

```scala
def slice(from: Int, until: Int): Set[T]
```

Selects an interval of elements. The returned mutable set is made up of all elements x which satisfy the invariant:

Type parameters: none.

Parameters, list 1: `` from: Int ``; `` until: Int ``.

Returns: `` Set[T] ``.

Source contract/attributes: Selects an interval of elements. The returned mutable set is made up of all elements x which satisfy the invariant: from <= indexOf(x) < until Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters from the lowest index to include from this mutable set. until the lowest index to EXCLUDE from this mutable set. Attributes Returns a mutable set containing the elements greater than or equal to index from extending up to (but not including) index until of this mutable set. Example List('a', 'b', 'c', 'd', 'e').slice(1, 3) == List('b', 'c') Inherited from: IterableOps

Invocation template:

```scala
receiver.slice(from, until)
```

## `` com.cra.figaro.util.HashSelectableSet.sliding ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#sliding-fffff156)

```scala
def sliding(size: Int, step: Int): Iterator[Set[T]]
```

Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped).

Type parameters: none.

Parameters, list 1: `` size: Int ``; `` step: Int ``.

Returns: `` Iterator[Set[T]] ``.

Source contract/attributes: Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped). The returned iterator will be empty when called on an empty collection. The last element the iterator produces may be smaller than the window size when the original collection isn't exhausted by the window before it and its last element isn't skipped by the step before it. Value parameters size the number of elements per group step the distance between the first elements of successive groups Attributes Returns An iterator producing mutable sets of size size, except the last element (which may be the only element) will be smaller if there are fewer than size elements remaining to be grouped. See also scala.collection.Iterator, method sliding Example List(1, 2, 3, 4, 5).sliding(2, 2) = Iterator(List(1, 2), List(3, 4), List(5)) List(1, 2, 3, 4, 5, 6).sliding(2, 3) = Iterator(List(1, 2), List(4, 5)) Inherited from: IterableOps

Invocation template:

```scala
receiver.sliding(size, step)
```

## `` com.cra.figaro.util.HashSelectableSet.sliding ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#sliding-fffffbef)

```scala
def sliding(size: Int): Iterator[Set[T]]
```

Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped).

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Iterator[Set[T]] ``.

Source contract/attributes: Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped). An empty collection returns an empty iterator, and a non-empty collection containing fewer elements than the window size returns an iterator that will produce the original collection as its only element. Value parameters size the number of elements per group Attributes Returns An iterator producing mutable sets of size size, except for a non-empty collection with less than size elements, which returns an iterator that produces the source collection itself as its only element. See also scala.collection.Iterator, method sliding Example List().sliding(2) = empty iterator List(1).sliding(2) = Iterator(List(1)) List(1, 2).sliding(2) = Iterator(List(1, 2)) List(1, 2, 3).sliding(2) = Iterator(List(1, 2), List(2, 3)) Inherited from: IterableOps

Invocation template:

```scala
receiver.sliding(size)
```

## `` com.cra.figaro.util.HashSelectableSet.span ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#span-243)

```scala
def span(p: T => Boolean): (Set[T], Set[T])
```

Splits this mutable set into a prefix/suffix pair according to a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` (Set[T], Set[T]) ``.

Source contract/attributes: Splits this mutable set into a prefix/suffix pair according to a predicate. Note: c span p is equivalent to (but possibly more efficient than) (c takeWhile p, c dropWhile p), provided the evaluation of the predicate p does not cause any side effects. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the test predicate Attributes Returns a pair consisting of the longest prefix of this mutable set whose elements all satisfy p, and the rest of this mutable set. Inherited from: IterableOps

Invocation template:

```scala
receiver.span(p)
```

## `` com.cra.figaro.util.HashSelectableSet.splitAt ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#splitAt-fffffbd9)

```scala
override def splitAt(n: Int): (Set[T], Set[T])
```

Splits this mutable set into a prefix/suffix pair at a given position.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` (Set[T], Set[T]) ``.

Source contract/attributes: Splits this mutable set into a prefix/suffix pair at a given position. Note: c splitAt n is equivalent to (but possibly more efficient than) (c take n, c drop n). Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the position at which to split. Attributes Returns a pair of mutable sets consisting of the first n elements of this mutable set, and the other elements. Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.splitAt(n)
```

## `` com.cra.figaro.util.HashSelectableSet.stepper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#stepper-190)

```scala
def stepper[S <: Stepper[_]](implicit shape: StepperShape[T, S]): S
```

Returns a scala.collection.Stepper for the elements of this collection.

Type parameters: `` S <: Stepper[_] ``.

Context parameters, list 1: `` shape: StepperShape[T, S] ``.

Returns: `` S ``.

Source contract/attributes: Returns a scala.collection.Stepper for the elements of this collection. The Stepper enables creating a Java stream to operate on the collection, see scala.jdk.StreamConverters. For collections holding primitive values, the Stepper can be used as an iterator which doesn't box the elements. The implicit scala.collection.StepperShape parameter defines the resulting Stepper type according to the element type of this collection. For collections of Int, Short, Byte or Char, an scala.collection.IntStepper is returned For collections of Double or Float, a scala.collection.DoubleStepper is returned For collections of Long a scala.collection.LongStepper is returned For any other element type, an scala.collection.AnyStepper is returned Note that this method is overridden in subclasses and the return type is refined to S with EfficientSplit, for example scala.collection.IndexedSeqOps.stepper. For Steppers marked with scala.collection.Stepper.EfficientSplit, the converters in scala.jdk.StreamConverters allow creating parallel streams, whereas bare Steppers can be converted only to sequential streams. Type parameters S the type of the returned Stepper, determined by the implicit StepperShape Attributes Inherited from: IterableOnce

Invocation template:

```scala
receiver.stepper[S](using shape)
```

## `` com.cra.figaro.util.HashSelectableSet.subsetOf ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#subsetOf-b06)

```scala
def subsetOf(that: Set[T]): Boolean
```

Tests whether this set is a subset of another set.

Type parameters: none.

Parameters, list 1: `` that: Set[T] ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this set is a subset of another set. Value parameters that the set to test. Attributes Returns true if this set is a subset of that, i.e. if every element of this set is also an element of that. Inherited from: SetOps

Invocation template:

```scala
receiver.subsetOf(that)
```

## `` com.cra.figaro.util.HashSelectableSet.subsets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#subsets-fffff136)

```scala
def subsets(): Iterator[Set[T]]
```

An iterator over all subsets of this set.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Iterator[Set[T]] ``.

Source contract/attributes: An iterator over all subsets of this set. Attributes Returns the iterator. Inherited from: SetOps

Invocation template:

```scala
receiver.subsets()
```

## `` com.cra.figaro.util.HashSelectableSet.subsets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#subsets-fffffbef)

```scala
def subsets(len: Int): Iterator[Set[T]]
```

An iterator over all subsets of this set of the given size. If the requested size is impossible, an empty iterator is returned.

Type parameters: none.

Parameters, list 1: `` len: Int ``.

Returns: `` Iterator[Set[T]] ``.

Source contract/attributes: An iterator over all subsets of this set of the given size. If the requested size is impossible, an empty iterator is returned. Value parameters len the size of the subsets. Attributes Returns the iterator. Inherited from: SetOps

Invocation template:

```scala
receiver.subsets(len)
```

## `` com.cra.figaro.util.HashSelectableSet.subtractAll ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#subtractAll-fffff8b8)

```scala
def subtractAll(xs: IterableOnce[T]): this.type
```

Removes all elements produced by an iterator from this mutable set.

Type parameters: none.

Parameters, list 1: `` xs: IterableOnce[T] ``.

Returns: `` this.type ``.

Source contract/attributes: Removes all elements produced by an iterator from this mutable set. Value parameters xs the iterator producing the elements to remove. Attributes Returns the mutable set itself Inherited from: Shrinkable

Invocation template:

```scala
receiver.subtractAll(xs)
```

## `` com.cra.figaro.util.HashSelectableSet.sum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#sum-533)

```scala
def sum[B >: T](implicit num: Numeric[B]): B
```

Sums the elements of this collection.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` num: Numeric[B] ``.

Returns: `` B ``.

Source contract/attributes: Sums the elements of this collection. The default implementation uses reduce for a known non-empty collection, foldLeft otherwise. Note: will not terminate for infinite-sized collections. Type parameters B the result type of the + operator. Value parameters num an implicit parameter defining a set of numeric operations which includes the + operator to be used in forming the sum. Attributes Returns the sum of all elements of this mutable set with respect to the + operator in num. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.sum[B](using num)
```

## `` com.cra.figaro.util.HashSelectableSet.tail ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#tail-0)

```scala
def tail: Set[T]
```

The rest of the collection without its first element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[T] ``.

Source contract/attributes: The rest of the collection without its first element. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.tail
```

## `` com.cra.figaro.util.HashSelectableSet.tails ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#tails-0)

```scala
def tails: Iterator[Set[T]]
```

Iterates over the tails of this mutable set. The first value will be this mutable set and the final one will be an empty mutable set, with the intervening values the results of successive applications of tail.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Set[T]] ``.

Source contract/attributes: Iterates over the tails of this mutable set. The first value will be this mutable set and the final one will be an empty mutable set, with the intervening values the results of successive applications of tail. Attributes Returns an iterator over all the tails of this mutable set Example List(1,2,3).tails = Iterator(List(1,2,3), List(2,3), List(3), Nil) Inherited from: IterableOps

Invocation template:

```scala
receiver.tails
```

## `` com.cra.figaro.util.HashSelectableSet.take ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#take-aba)

```scala
def take(n: Int): Set[T]
```

Selects the first n elements.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Set[T] ``.

Source contract/attributes: Selects the first n elements. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to take from this mutable set. Attributes Returns a mutable set consisting only of the first n elements of this mutable set, or else the whole mutable set, if it has less than n elements. If n is negative, returns an empty mutable set. Inherited from: IterableOps

Invocation template:

```scala
receiver.take(n)
```

## `` com.cra.figaro.util.HashSelectableSet.takeRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#takeRight-aba)

```scala
def takeRight(n: Int): Set[T]
```

Selects the last n elements.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Set[T] ``.

Source contract/attributes: Selects the last n elements. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to take from this mutable set. Attributes Returns a mutable set consisting only of the last n elements of this mutable set, or else the whole mutable set, if it has less than n elements. If n is negative, returns an empty mutable set. Inherited from: IterableOps

Invocation template:

```scala
receiver.takeRight(n)
```

## `` com.cra.figaro.util.HashSelectableSet.takeWhile ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#takeWhile-c24)

```scala
def takeWhile(p: T => Boolean): Set[T]
```

Takes longest prefix of elements that satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Set[T] ``.

Source contract/attributes: Takes longest prefix of elements that satisfy a predicate. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p The predicate used to test elements. Attributes Returns the longest prefix of this mutable set whose elements all satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.takeWhile(p)
```

## `` com.cra.figaro.util.HashSelectableSet.tapEach ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#tapEach-5d3)

```scala
override def tapEach[U](f: T => U): Set[T]
```

Applies a side-effecting function to each element in this collection. Strict collections will apply f to their elements immediately, while lazy collections like Views and LazyLists will only apply f on each element if and when that element is evaluated, and each time that element is evaluated.

Type parameters: `` U ``.

Parameters, list 1: `` f: T => U ``.

Returns: `` Set[T] ``.

Source contract/attributes: Applies a side-effecting function to each element in this collection. Strict collections will apply f to their elements immediately, while lazy collections like Views and LazyLists will only apply f on each element if and when that element is evaluated, and each time that element is evaluated. Type parameters U the return type of f Value parameters f a function to apply to each element in this mutable set Attributes Returns The same logical collection as this Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.tapEach[U](f)
```

## `` com.cra.figaro.util.HashSelectableSet.to ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#to-fffff006)

```scala
def to[C1](factory: Factory[T, C1]): C1
```

Given a collection factory factory, converts this mutable set to the appropriate representation for the current element type A. Example uses:

Type parameters: `` C1 ``.

Parameters, list 1: `` factory: Factory[T, C1] ``.

Returns: `` C1 ``.

Source contract/attributes: Given a collection factory factory, converts this mutable set to the appropriate representation for the current element type A. Example uses: import scala.collection.mutable.ArrayBuffer import scala.collection.immutable.BitSet val xs: Iterable[Int] = Seq(1, 2, 3, 4, 5) xs.to(List) xs.to(ArrayBuffer) xs.to(BitSet) // for xs: Iterable[Int] Type parameters C1 the target collection type Value parameters factory the factory for the target collection type Attributes Returns a new collection of type C1 containing all elements of this mutable set Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.to[C1](factory)
```

## `` com.cra.figaro.util.HashSelectableSet.toArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#toArray-fffff5df)

```scala
def toArray[B >: T : ClassTag]: Array[B]
```

Converts this mutable set to an Array.

Type parameters: `` B >: T : ClassTag ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Array[B] ``.

Source contract/attributes: Converts this mutable set to an Array. Implementation note: DO NOT call Array.from from this method. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This mutable set as an Array[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toArray[B]
```

## `` com.cra.figaro.util.HashSelectableSet.toBuffer ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#toBuffer-fffff87f)

```scala
final def toBuffer[B >: T]: Buffer[B]
```

Converts this mutable set to a Buffer.

Type parameters: `` B >: T ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Buffer[B] ``.

Source contract/attributes: Converts this mutable set to a Buffer. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This mutable set as a Buffer[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toBuffer[B]
```

## `` com.cra.figaro.util.HashSelectableSet.toIndexedSeq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#toIndexedSeq-0)

```scala
def toIndexedSeq: IndexedSeq[T]
```

Converts this mutable set to an IndexedSeq.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IndexedSeq[T] ``.

Source contract/attributes: Converts this mutable set to an IndexedSeq. Attributes Returns This mutable set as an IndexedSeq[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toIndexedSeq
```

## `` com.cra.figaro.util.HashSelectableSet.toMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#toMap-c0c)

```scala
def toMap[K, V](implicit ev: T <:< (K, V)): Map[K, V]
```

Converts this mutable set to a Map, given an implicit coercion from the mutable set's type to a key-value tuple.

Type parameters: `` K ``; `` V ``.

Context parameters, list 1: `` ev: T <:< (K, V) ``.

Returns: `` Map[K, V] ``.

Source contract/attributes: Converts this mutable set to a Map, given an implicit coercion from the mutable set's type to a key-value tuple. Type parameters K The key type for the resulting map. V The value type for the resulting map. Value parameters ev an implicit evidence that A is a subtype of (K, V) Attributes Returns This mutable set as a Map[K, V]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toMap[K, V](using ev)
```

## `` com.cra.figaro.util.HashSelectableSet.toSeq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#toSeq-0)

```scala
def toSeq: Seq[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Seq[T] ``.

Source contract/attributes: Attributes Returns This mutable set as a Seq[A]. This is equivalent to to(Seq) but might be faster. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toSeq
```

## `` com.cra.figaro.util.HashSelectableSet.toSet ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#toSet-fffffe9f)

```scala
def toSet[B >: T]: Set[B]
```

Converts this mutable set to a Set.

Type parameters: `` B >: T ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[B] ``.

Source contract/attributes: Converts this mutable set to a Set. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This mutable set as a Set[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toSet[B]
```

## `` com.cra.figaro.util.HashSelectableSet.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#toString-665)

```scala
override def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Set -> Function1 -> Iterable -> Any Inherited from: Set

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.util.HashSelectableSet.toVector ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#toVector-0)

```scala
def toVector: Vector[T]
```

Converts this mutable set to a Vector.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Vector[T] ``.

Source contract/attributes: Converts this mutable set to a Vector. Attributes Returns This mutable set as a Vector[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toVector
```

## `` com.cra.figaro.util.HashSelectableSet.transpose ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#transpose-5d3)

```scala
def transpose[B](implicit asIterable: T => Iterable[B]): Set[Set[B]]
```

Transposes this mutable set of iterable collections into a mutable set of mutable sets.

Type parameters: `` B ``.

Context parameters, list 1: `` asIterable: T => Iterable[B] ``.

Returns: `` Set[Set[B]] ``.

Source contract/attributes: Transposes this mutable set of iterable collections into a mutable set of mutable sets. The resulting collection's type will be guided by the static type of mutable set. For example: val xs = List( Set(1, 2, 3), Set(4, 5, 6)).transpose // xs == List( // List(1, 4), // List(2, 5), // List(3, 6)) val ys = Vector( List(1, 2, 3), List(4, 5, 6)).transpose // ys == Vector( // Vector(1, 4), // Vector(2, 5), // Vector(3, 6)) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of the elements of each iterable collection. Value parameters asIterable an implicit conversion which asserts that the element type of this mutable set is an Iterable. Attributes Returns a two-dimensional mutable set of mutable sets which has as nth row the nth column of this mutable set. Throws IllegalArgumentException if all collections in this mutable set are not of the same size. Inherited from: IterableOps

Invocation template:

```scala
receiver.transpose[B](using asIterable)
```

## `` com.cra.figaro.util.HashSelectableSet.union ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#union-dde)

```scala
final def union(that: Set[T]): Set[T]
```

Computes the union between of set and another set.

Type parameters: none.

Parameters, list 1: `` that: Set[T] ``.

Returns: `` Set[T] ``.

Source contract/attributes: Computes the union between of set and another set. Value parameters that the set to form the union with. Attributes Returns a new set consisting of all elements that are in this set or in the given set that. Inherited from: SetOps

Invocation template:

```scala
receiver.union(that)
```

## `` com.cra.figaro.util.HashSelectableSet.unzip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#unzip-2d1)

```scala
def unzip[A1, A2](implicit asPair: T => (A1, A2)): (Set[A1], Set[A2])
```

Converts this mutable set of pairs into two collections of the first and second half of each pair.

Type parameters: `` A1 ``; `` A2 ``.

Context parameters, list 1: `` asPair: T => (A1, A2) ``.

Returns: `` (Set[A1], Set[A2]) ``.

Source contract/attributes: Converts this mutable set of pairs into two collections of the first and second half of each pair. val xs = $Coll( (1, "one"), (2, "two"), (3, "three")).unzip // xs == ($Coll(1, 2, 3), // $Coll(one, two, three)) Type parameters A1 the type of the first half of the element pairs A2 the type of the second half of the element pairs Value parameters asPair an implicit conversion which asserts that the element type of this mutable set is a pair. Attributes Returns a pair of mutable sets, containing the first, respectively second half of each element pair of this mutable set. Inherited from: IterableOps

Invocation template:

```scala
receiver.unzip[A1, A2](using asPair)
```

## `` com.cra.figaro.util.HashSelectableSet.unzip3 ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#unzip3-a31)

```scala
def unzip3[A1, A2, A3](implicit asTriple: T => (A1, A2, A3)): (Set[A1], Set[A2], Set[A3])
```

Converts this mutable set of triples into three collections of the first, second, and third element of each triple.

Type parameters: `` A1 ``; `` A2 ``; `` A3 ``.

Context parameters, list 1: `` asTriple: T => (A1, A2, A3) ``.

Returns: `` (Set[A1], Set[A2], Set[A3]) ``.

Source contract/attributes: Converts this mutable set of triples into three collections of the first, second, and third element of each triple. val xs = $Coll( (1, "one", '1'), (2, "two", '2'), (3, "three", '3')).unzip3 // xs == ($Coll(1, 2, 3), // $Coll(one, two, three), // $Coll(1, 2, 3)) Type parameters A1 the type of the first member of the element triples A2 the type of the second member of the element triples A3 the type of the third member of the element triples Value parameters asTriple an implicit conversion which asserts that the element type of this mutable set is a triple. Attributes Returns a triple of mutable sets, containing the first, second, respectively third member of each element triple of this mutable set. Inherited from: IterableOps

Invocation template:

```scala
receiver.unzip3[A1, A2, A3](using asTriple)
```

## `` com.cra.figaro.util.HashSelectableSet.update ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#update-9b9)

```scala
def update(elem: T, included: Boolean): Unit
```

Updates the presence of a single element in this set.

Type parameters: none.

Parameters, list 1: `` elem: T ``; `` included: Boolean ``.

Returns: `` Unit ``.

Source contract/attributes: Updates the presence of a single element in this set. This method allows one to add or remove an element elem from this set depending on the value of parameter included. Typically, one would use the following syntax: val set = scala.collection.mutable.Set(1) val elem = 1 set(elem) = true // adds element set(elem) = false // removes element Value parameters elem the element to be added or removed included a flag indicating whether element should be included or excluded. Attributes Inherited from: SetOps

Invocation template:

```scala
receiver.update(elem, included)
```

## `` com.cra.figaro.util.HashSelectableSet.view ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#view-0)

```scala
def view: View[T]
```

A view over the elements of this collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` View[T] ``.

Source contract/attributes: A view over the elements of this collection. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.view
```

## `` com.cra.figaro.util.HashSelectableSet.withFilter ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#withFilter-fffffb75)

```scala
def withFilter(p: T => Boolean): WithFilter[T, Set]
```

Creates a non-strict filter of this mutable set.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` WithFilter[T, Set] ``.

Source contract/attributes: Creates a non-strict filter of this mutable set. Note: the difference between c filter p and c withFilter p is that the former creates a new collection, whereas the latter only restricts the domain of subsequent map, flatMap, foreach, and withFilter operations. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the predicate used to test elements. Attributes Returns an object of class WithFilter, which supports map, flatMap, foreach, and withFilter operations. All these operations apply to those elements of this mutable set which satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.withFilter(p)
```

## `` com.cra.figaro.util.HashSelectableSet.zip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#zip-1dd)

```scala
def zip[B](that: IterableOnce[B]): Set[(T, B)]
```

Returns a mutable set formed from this mutable set and another iterable collection by combining corresponding elements in pairs. If one of the two collections is longer than the other, its remaining elements are ignored.

Type parameters: `` B ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Returns: `` Set[(T, B)] ``.

Source contract/attributes: Returns a mutable set formed from this mutable set and another iterable collection by combining corresponding elements in pairs. If one of the two collections is longer than the other, its remaining elements are ignored. Type parameters B the type of the second half of the returned pairs Value parameters that The iterable providing the second half of each result pair Attributes Returns a new mutable set containing pairs consisting of corresponding elements of this mutable set and that. The length of the returned collection is the minimum of the lengths of this mutable set and that. Inherited from: IterableOps

Invocation template:

```scala
receiver.zip[B](that)
```

## `` com.cra.figaro.util.HashSelectableSet.zipAll ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#zipAll-fffff401)

```scala
def zipAll[A1 >: T, B](that: Iterable[B], thisElem: A1, thatElem: B): Set[(A1, B)]
```

Returns a mutable set formed from this mutable set and another iterable collection by combining corresponding elements in pairs. If one of the two collections is shorter than the other, placeholder elements are used to extend the shorter collection to the length of the longer.

Type parameters: `` A1 >: T ``; `` B ``.

Parameters, list 1: `` that: Iterable[B] ``; `` thisElem: A1 ``; `` thatElem: B ``.

Returns: `` Set[(A1, B)] ``.

Source contract/attributes: Returns a mutable set formed from this mutable set and another iterable collection by combining corresponding elements in pairs. If one of the two collections is shorter than the other, placeholder elements are used to extend the shorter collection to the length of the longer. Type parameters A1 the type of the first element in each result pair (supertype of A) B the type of the second element in each result pair Value parameters that the iterable providing the second half of each result pair thatElem the element to be used to fill up the result if that is shorter than this mutable set. thisElem the element to be used to fill up the result if this mutable set is shorter than that. Attributes Returns a new mutable set containing pairs consisting of corresponding elements of this mutable set and that. The length of the returned collection is the maximum of the lengths of this mutable set and that. If this mutable set is shorter than that, thisElem values are used to pad the result. If that is shorter than this mutable set, thatElem values are used to pad the result. Inherited from: IterableOps

Invocation template:

```scala
receiver.zipAll[A1, B](that, thisElem, thatElem)
```

## `` com.cra.figaro.util.HashSelectableSet.zipWithIndex ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#zipWithIndex-0)

```scala
def zipWithIndex: Set[(T, Int)]
```

Zips this mutable set with its indices.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[(T, Int)] ``.

Source contract/attributes: Zips this mutable set with its indices. Attributes Returns A new mutable set containing pairs consisting of all elements of this mutable set paired with their index. Indices start at 0. Example List("a", "b", "c").zipWithIndex == List(("a", 0), ("b", 1), ("c", 2)) Inherited from: IterableOps

Invocation template:

```scala
receiver.zipWithIndex
```

## `` com.cra.figaro.util.HashSelectableSet.| ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#|-dde)

```scala
final def |(that: Set[T]): Set[T]
```

Alias for union.

Type parameters: none.

Parameters, list 1: `` that: Set[T] ``.

Returns: `` Set[T] ``.

Source contract/attributes: Alias for union. Value parameters that the set to form the union with Attributes Inherited from: SetOps

Invocation template:

```scala
receiver.|(that)
```

## `` com.cra.figaro.util.HashSelectableSet.+ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#+-fffff5c7)

```scala
def +(elem1: T, elem2: T, elems: T*): Set[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` elem1: T ``; `` elem2: T ``; `` elems: T* ``.

Returns: `` Set[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use ++ with an explicit collection argument instead of + with varargs Inherited from: SetOps

Invocation template:

```scala
receiver.+(elem1, elem2, elems*)
```

## `` com.cra.figaro.util.HashSelectableSet.+ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#+-fffff2c5)

```scala
def +(elem: T): Set[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` Set[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Consider requiring an immutable Set or fall back to Set.union Inherited from: SetOps

Invocation template:

```scala
receiver.+(elem)
```

## `` com.cra.figaro.util.HashSelectableSet.++: ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#++:-1dd)

```scala
def ++:[B >: T](that: IterableOnce[B]): Set[B]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B >: T ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Returns: `` Set[B] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use ++ instead of ++: for collections of type Iterable Inherited from: IterableOps

Invocation template:

```scala
receiver.++:[B](that)
```

## `` com.cra.figaro.util.HashSelectableSet.+= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#+=-fffff3cc)

```scala
final def +=(elem1: T, elem2: T, elems: T*): this.type
```

Add s two or more elements to this mutable set.

Type parameters: none.

Parameters, list 1: `` elem1: T ``; `` elem2: T ``; `` elems: T* ``.

Returns: `` this.type ``.

Source contract/attributes: Add s two or more elements to this mutable set. Value parameters elem1 the first element to add. elem2 the second element to add. elems the remaining elements to add. Attributes Returns the mutable set itself Deprecated [Since version 2.13.0] Use `++=` aka `addAll` instead of varargs `+=`; infix operations with an operand of multiple args will be deprecated Inherited from: Growable

Invocation template:

```scala
receiver.+=(elem1, elem2, elems*)
```

## `` com.cra.figaro.util.HashSelectableSet.- ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#--fffff5c7)

```scala
def -(elem1: T, elem2: T, elems: T*): Set[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` elem1: T ``; `` elem2: T ``; `` elems: T* ``.

Returns: `` Set[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use &- with an explicit collection argument instead of - with varargs Inherited from: SetOps

Invocation template:

```scala
receiver.-(elem1, elem2, elems*)
```

## `` com.cra.figaro.util.HashSelectableSet.- ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#--fffff2c5)

```scala
def -(elem: T): Set[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` Set[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Consider requiring an immutable Set or fall back to Set.diff Inherited from: SetOps

Invocation template:

```scala
receiver.-(elem)
```

## `` com.cra.figaro.util.HashSelectableSet.-- ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#---fffff87d)

```scala
def --(that: IterableOnce[T]): Set[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` that: IterableOnce[T] ``.

Returns: `` Set[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Consider requiring an immutable Set Inherited from: SetOps

Invocation template:

```scala
receiver.--(that)
```

## `` com.cra.figaro.util.HashSelectableSet.-= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#-=-fffff702)

```scala
def -=(elem1: T, elem2: T, elems: T*): this.type
```

Removes two or more elements from this mutable set.

Type parameters: none.

Parameters, list 1: `` elem1: T ``; `` elem2: T ``; `` elems: T* ``.

Returns: `` this.type ``.

Source contract/attributes: Removes two or more elements from this mutable set. Value parameters elem1 the first element to remove. elem2 the second element to remove. elems the remaining elements to remove. Attributes Returns the mutable set itself Deprecated [Since version 2.13.3] Use `--=` aka `subtractAll` instead of varargs `-=`; infix operations with an operand of multiple args will be deprecated Inherited from: Shrinkable

Invocation template:

```scala
receiver.-=(elem1, elem2, elems*)
```

## `` com.cra.figaro.util.HashSelectableSet./: ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#/:-fffff9e1)

```scala
final def /:[B](z: B)(op: (B, T) => B): B
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, T) => B ``.

Returns: `` B ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use foldLeft instead of /: Inherited from: IterableOnceOps

Invocation template:

```scala
receiver./:[B](z)(op)
```

## `` com.cra.figaro.util.HashSelectableSet.:\ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#:\-fffff9e1)

```scala
final def :\[B](z: B)(op: (T, B) => B): B
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (T, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use foldRight instead of :\\ Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.:\[B](z)(op)
```

## `` com.cra.figaro.util.HashSelectableSet.aggregate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#aggregate-952)

```scala
def aggregate[B](z: => B)(seqop: (B, T) => B, combop: (B, B) => B): B
```

Aggregates the results of applying an operator to subsequent elements.

Type parameters: `` B ``.

Parameters, list 1: `` z: => B ``.

Parameters, list 2: `` seqop: (B, T) => B ``; `` combop: (B, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Aggregates the results of applying an operator to subsequent elements. Since this method degenerates to foldLeft for sequential (non-parallel) collections, where the combining operation is ignored, it is advisable to prefer foldLeft for that case. For parallel collections, use the aggregate method specified by scala.collection.parallel.ParIterableLike. Type parameters B the result type, produced by seqop, combop, and by this function as a final result. Value parameters combop an associative operator for combining sequential results, unused for sequential collections. seqop the binary operator used to accumulate the result. z the start value, a neutral element for seqop. Attributes Deprecated [Since version 2.13.0] For sequential collections, prefer `foldLeft(z)(seqop)`. For parallel collections, use `ParIterableLike#aggregate`. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.aggregate[B](z)(seqop, combop)
```

## `` com.cra.figaro.util.HashSelectableSet.companion ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#companion-0)

```scala
def companion: IterableFactory[Set]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IterableFactory[Set] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use iterableFactory instead Inherited from: IterableOps

Invocation template:

```scala
receiver.companion
```

## `` com.cra.figaro.util.HashSelectableSet.copyToBuffer ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#copyToBuffer-f0b)

```scala
final def copyToBuffer[B >: T](dest: Buffer[B]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B >: T ``.

Parameters, list 1: `` dest: Buffer[B] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use `dest ++= coll` instead Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToBuffer[B](dest)
```

## `` com.cra.figaro.util.HashSelectableSet.hasDefiniteSize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#hasDefiniteSize-0)

```scala
def hasDefiniteSize: Boolean
```

Tests whether this mutable set is known to have a finite size. All strict collections are known to have finite size. For a non-strict collection such as Stream, the predicate returns **true** if all elements have been computed. It returns **false** if the stream is not yet evaluated to the end. Non-empty Iterators usually return **false** even if they were created from a collection with a known finite size.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this mutable set is known to have a finite size. All strict collections are known to have finite size. For a non-strict collection such as Stream, the predicate returns **true** if all elements have been computed. It returns **false** if the stream is not yet evaluated to the end. Non-empty Iterators usually return **false** even if they were created from a collection with a known finite size. Note: many collection methods will not work on collections of infinite sizes. The typical failure mode is an infinite loop. These methods always attempt a traversal without checking first that hasDefiniteSize returns **true**. However, checking hasDefiniteSize can provide an assurance that size is well-defined and non-termination is not a concern. Attributes Returns **true** if this collection is known to have finite size, **false** otherwise. See also method knownSize for a more useful alternative Deprecated [Since version 2.13.0] Check .knownSize instead of .hasDefiniteSize for more actionable information (see scaladoc for details) This method is deprecated in 2.13 because it does not provide any actionable information. As noted above, even the collection library itself does not use it. When there is no guarantee that a collection is finite, it is generally best to attempt a computation anyway and document that it will not terminate for infinite collections rather than backing out because this would prevent performing the computation on collections that are in fact finite even though hasDefiniteSize returns false. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.hasDefiniteSize
```

## `` com.cra.figaro.util.HashSelectableSet.repr ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#repr-0)

```scala
final def repr: Set[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use coll instead of repr in a collection implementation, use the collection value itself from the outside Inherited from: IterableOps

Invocation template:

```scala
receiver.repr
```

## `` com.cra.figaro.util.HashSelectableSet.retain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#retain-c9d)

```scala
final def retain(p: T => Boolean): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use filterInPlace instead Inherited from: SetOps

Invocation template:

```scala
receiver.retain(p)
```

## `` com.cra.figaro.util.HashSelectableSet.seq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#seq-0)

```scala
def seq: this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` this.type ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Iterable.seq always returns the iterable itself Inherited from: Iterable

Invocation template:

```scala
receiver.seq
```

## `` com.cra.figaro.util.HashSelectableSet.toIterable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#toIterable-0)

```scala
final def toIterable: this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` this.type ``.

Source contract/attributes: Attributes Returns This collection as an Iterable[A]. No new collection will be built if this is already an Iterable[A]. Deprecated [Since version 2.13.7] toIterable is internal and will be made protected; its name is similar to `toList` or `toSeq`, but it doesn\'t copy non-immutable collections Inherited from: Iterable

Invocation template:

```scala
receiver.toIterable
```

## `` com.cra.figaro.util.HashSelectableSet.toIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#toIterator-0)

```scala
final def toIterator: Iterator[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use .iterator instead of .toIterator Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toIterator
```

## `` com.cra.figaro.util.HashSelectableSet.toStream ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#toStream-0)

```scala
final def toStream: Stream[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Stream[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use .to(LazyList) instead of .toStream Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toStream
```

## `` com.cra.figaro.util.HashSelectableSet.toTraversable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#toTraversable-0)

```scala
final def toTraversable: Iterable[T]
```

Converts this mutable set to an unspecified Iterable. Will return the same collection if this instance is already Iterable.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: Converts this mutable set to an unspecified Iterable. Will return the same collection if this instance is already Iterable. Attributes Returns An Iterable containing all elements of this mutable set. Deprecated [Since version 2.13.0] toTraversable is internal and will be made protected; its name is similar to `toList` or `toSeq`, but it doesn\'t copy non-immutable collections Inherited from: IterableOps

Invocation template:

```scala
receiver.toTraversable
```

## `` com.cra.figaro.util.HashSelectableSet.view ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HashSelectableSet.html#view-fffff78d)

```scala
def view(from: Int, until: Int): View[T]
```

A view over a slice of the elements of this collection.

Type parameters: none.

Parameters, list 1: `` from: Int ``; `` until: Int ``.

Returns: `` View[T] ``.

Source contract/attributes: A view over a slice of the elements of this collection. Attributes Deprecated [Since version 2.13.0] Use .view.slice(from, until) instead of .view(from, until) Inherited from: IterableOps

Invocation template:

```scala
receiver.view(from, until)
```

## `` com.cra.figaro.util.HeapPriorityMap.addOne ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#addOne-c19)

```scala
def addOne(pair: (T, U)): this.type
```

Add the key/value pair to the priority map. If the key is already present, update its value, moving it in the map.

Type parameters: none.

Parameters, list 1: `` pair: (T, U) ``.

Returns: `` this.type ``.

Source contract/attributes: Add the key/value pair to the priority map. If the key is already present, update its value, moving it in the map. Attributes

Invocation template:

```scala
receiver.addOne(pair)
```

## `` com.cra.figaro.util.HeapPriorityMap.clone ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#clone-0)

```scala
override def clone: HeapPriorityMap[T, U]
```

Make a copy of this HeapPriorityMap, returning a new HeapPriorityMap.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` HeapPriorityMap[T, U] ``.

Source contract/attributes: Make a copy of this HeapPriorityMap, returning a new HeapPriorityMap. Attributes Definition Classes MapOps -> Cloneable -> Object

Invocation template:

```scala
receiver.clone
```

## `` com.cra.figaro.util.HeapPriorityMap.extractMin ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#extractMin-ffffff32)

```scala
def extractMin(): (T, U)
```

Extract and return the item with the minimum value.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (T, U) ``.

Source contract/attributes: Extract and return the item with the minimum value. Attributes

Invocation template:

```scala
receiver.extractMin()
```

## `` com.cra.figaro.util.HeapPriorityMap.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#get-fffffb50)

```scala
def get(item: T): Option[U]
```

Get the value associated with the key.

Type parameters: none.

Parameters, list 1: `` item: T ``.

Returns: `` Option[U] ``.

Source contract/attributes: Get the value associated with the key. Attributes

Invocation template:

```scala
receiver.get(item)
```

## `` com.cra.figaro.util.HeapPriorityMap.isEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#isEmpty-0)

```scala
override def isEmpty: Boolean
```

Tests whether the mutable map is empty.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether the mutable map is empty. Note: The default implementation creates and discards an iterator. Note: Implementations in subclasses that are not repeatedly iterable must take care not to consume any elements when isEmpty is called. Attributes Returns true if the mutable map contains no elements, false otherwise. Definition Classes IterableOnceOps

Invocation template:

```scala
receiver.isEmpty
```

## `` com.cra.figaro.util.HeapPriorityMap.iterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#iterator-0)

```scala
def iterator: Iterator[(T, U)]
```

Produce an iterator of the (item, score) pairs in increasing order of score.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[(T, U)] ``.

Source contract/attributes: Produce an iterator of the (item, score) pairs in increasing order of score. Attributes

Invocation template:

```scala
receiver.iterator
```

## `` com.cra.figaro.util.HeapPriorityMap.subtractOne ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#subtractOne-e18)

```scala
def subtractOne(item: T): this.type
```

Remove the key/value pair from the priority map.

Type parameters: none.

Parameters, list 1: `` item: T ``.

Returns: `` this.type ``.

Source contract/attributes: Remove the key/value pair from the priority map. Attributes

Invocation template:

```scala
receiver.subtractOne(item)
```

## `` com.cra.figaro.util.HeapPriorityMap.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#++-1dd)

```scala
final def ++[B >: (T, U)](suffix: IterableOnce[B]): Iterable[B]
```

Alias for concat.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` suffix: IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Alias for concat. Type parameters B the element type of the returned collection Value parameters suffix the iterable to append to this mutable map Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.++[B](suffix)
```

## `` com.cra.figaro.util.HeapPriorityMap.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#++-62)

```scala
def ++[V2 >: U](xs: IterableOnce[(T, V2)]): Map[T, V2]
```

Alias for concat.

Type parameters: `` V2 >: U ``.

Parameters, list 1: `` xs: IterableOnce[(T, V2)] ``.

Returns: `` Map[T, V2] ``.

Source contract/attributes: Alias for concat. Type parameters V2 the value type of the returned map, a supertype of V Value parameters xs the key-value pairs to append Attributes Inherited from: MapOps

Invocation template:

```scala
receiver.++[V2](xs)
```

## `` com.cra.figaro.util.HeapPriorityMap.++= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#++=-fffffb02)

```scala
final def ++=(elems: IterableOnce[(T, U)]): this.type
```

Alias for addAll.

Type parameters: none.

Parameters, list 1: `` elems: IterableOnce[(T, U)] ``.

Returns: `` this.type ``.

Source contract/attributes: Alias for addAll. Attributes Inherited from: Growable

Invocation template:

```scala
receiver.++=(elems)
```

## `` com.cra.figaro.util.HeapPriorityMap.+= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#+=-b4a)

```scala
final def +=(elem: (T, U)): this.type
```

Alias for addOne.

Type parameters: none.

Parameters, list 1: `` elem: (T, U) ``.

Returns: `` this.type ``.

Source contract/attributes: Alias for addOne. Attributes Inherited from: Growable

Invocation template:

```scala
receiver.+=(elem)
```

## `` com.cra.figaro.util.HeapPriorityMap.--= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#--=-fffff8b8)

```scala
final def --=(xs: IterableOnce[T]): this.type
```

Alias for subtractAll.

Type parameters: none.

Parameters, list 1: `` xs: IterableOnce[T] ``.

Returns: `` this.type ``.

Source contract/attributes: Alias for subtractAll. Attributes Inherited from: Shrinkable

Invocation template:

```scala
receiver.--=(xs)
```

## `` com.cra.figaro.util.HeapPriorityMap.-= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#-=-fffff700)

```scala
final def -=(elem: T): this.type
```

Alias for subtractOne.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` this.type ``.

Source contract/attributes: Alias for subtractOne. Attributes Inherited from: Shrinkable

Invocation template:

```scala
receiver.-=(elem)
```

## `` com.cra.figaro.util.HeapPriorityMap.addAll ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#addAll-fffffb02)

```scala
def addAll(elems: IterableOnce[(T, U)]): this.type
```

Add s all elements produced by an IterableOnce to this mutable map.

Type parameters: none.

Parameters, list 1: `` elems: IterableOnce[(T, U)] ``.

Returns: `` this.type ``.

Source contract/attributes: Add s all elements produced by an IterableOnce to this mutable map. Value parameters elems the IterableOnce producing the elements to add. Attributes Returns the mutable map itself. Inherited from: Growable

Invocation template:

```scala
receiver.addAll(elems)
```

## `` com.cra.figaro.util.HeapPriorityMap.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#addString-ff4)

```scala
final def addString(b: StringBuilder): b.type
```

Appends all elements of this mutable map to a string builder. The written text consists of the string representations (w.r.t. the method toString) of all elements of this mutable map without any separator string.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this mutable map to a string builder. The written text consists of the string representations (w.r.t. the method toString) of all elements of this mutable map without any separator string. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() val h = a.addString(b) // 1234 Value parameters b the string builder to which elements are appended. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b)
```

## `` com.cra.figaro.util.HeapPriorityMap.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#addString-e99)

```scala
final def addString(b: StringBuilder, sep: String): b.type
```

Appends all elements of this mutable map to a string builder using a separator string. The written text consists of the string representations (w.r.t. the method toString) of all elements of this mutable map, separated by the string sep.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``; `` sep: String ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this mutable map to a string builder using a separator string. The written text consists of the string representations (w.r.t. the method toString) of all elements of this mutable map, separated by the string sep. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() a.addString(b, ", ") // 1, 2, 3, 4 Value parameters b the string builder to which elements are appended. sep the separator string. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b, sep)
```

## `` com.cra.figaro.util.HeapPriorityMap.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#addString-de3)

```scala
override def addString(sb: StringBuilder, start: String, sep: String, end: String): sb.type
```

Appends all elements of this mutable map to a string builder using start, end, and separator strings. The written text begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this mutable map are separated by the string sep.

Type parameters: none.

Parameters, list 1: `` sb: StringBuilder ``; `` start: String ``; `` sep: String ``; `` end: String ``.

Returns: `` sb.type ``.

Source contract/attributes: Appends all elements of this mutable map to a string builder using start, end, and separator strings. The written text begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this mutable map are separated by the string sep. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() a.addString(b , "List(" , ", " , ")") // List(1, 2, 3, 4) Value parameters b the string builder to which elements are appended. end the ending string. sep the separator string. start the starting string. Attributes Returns the string builder b to which elements were appended. Definition Classes MapOps -> IterableOnceOps Inherited from: MapOps

Invocation template:

```scala
receiver.addString(sb, start, sep, end)
```

## `` com.cra.figaro.util.HeapPriorityMap.andThen ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#andThen-b91)

```scala
def andThen[C](k: PartialFunction[U, C]): PartialFunction[T, C]
```

Composes this partial function with another partial function that gets applied to results of this partial function.

Type parameters: `` C ``.

Parameters, list 1: `` k: PartialFunction[U, C] ``.

Returns: `` PartialFunction[T, C] ``.

Source contract/attributes: Composes this partial function with another partial function that gets applied to results of this partial function. Note that calling isDefinedAt on the resulting partial function may apply the first partial function and execute its side effect. For efficiency, it is recommended to call applyOrElse instead of isDefinedAt or apply. Type parameters C the result type of the transformation function. Value parameters k the transformation function Attributes Returns a partial function with the domain of this partial function narrowed by other partial function, which maps arguments x to k(this(x)). Inherited from: PartialFunction

Invocation template:

```scala
receiver.andThen[C](k)
```

## `` com.cra.figaro.util.HeapPriorityMap.andThen ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#andThen-f71)

```scala
override def andThen[C](k: U => C): PartialFunction[T, C]
```

Composes this partial function with a transformation function that gets applied to results of this partial function.

Type parameters: `` C ``.

Parameters, list 1: `` k: U => C ``.

Returns: `` PartialFunction[T, C] ``.

Source contract/attributes: Composes this partial function with a transformation function that gets applied to results of this partial function. If the runtime type of the function is a PartialFunction then the other andThen method is used (note its cautions). Type parameters C the result type of the transformation function. Value parameters k the transformation function Attributes Returns a partial function with the domain of this partial function, possibly narrowed by the specified function, which maps arguments x to k(this(x)). Definition Classes PartialFunction -> Function1 Inherited from: PartialFunction

Invocation template:

```scala
receiver.andThen[C](k)
```

## `` com.cra.figaro.util.HeapPriorityMap.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#apply-d26)

```scala
def apply(key: T): U
```

Retrieves the value which is associated with the given key. This method invokes the default method of the map if there is no mapping from the given key to a value. Unless overridden, the default method throws a NoSuchElementException.

Type parameters: none.

Parameters, list 1: `` key: T ``.

Returns: `` U ``.

Source contract/attributes: Retrieves the value which is associated with the given key. This method invokes the default method of the map if there is no mapping from the given key to a value. Unless overridden, the default method throws a NoSuchElementException. Value parameters key the key Attributes Returns the value associated with the given key, or the result of the map's default method, if none exists. Inherited from: MapOps

Invocation template:

```scala
receiver.apply(key)
```

## `` com.cra.figaro.util.HeapPriorityMap.applyOrElse ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#applyOrElse-fffffdbf)

```scala
override def applyOrElse[K1 <: T, V1 >: U](x: K1, default: K1 => V1): V1
```

Applies this partial function to the given argument when it is contained in the function domain. Applies fallback function where this partial function is not defined.

Type parameters: `` K1 <: T ``; `` V1 >: U ``.

Parameters, list 1: `` x: K1 ``; `` default: K1 => V1 ``.

Returns: `` V1 ``.

Source contract/attributes: Applies this partial function to the given argument when it is contained in the function domain. Applies fallback function where this partial function is not defined. Note that expression pf.applyOrElse(x, default) is equivalent to if(pf isDefinedAt x) pf(x) else default(x) except that applyOrElse method can be implemented more efficiently. For all partial function literals the compiler generates an applyOrElse implementation which avoids double evaluation of pattern matchers and guards. This makes applyOrElse the basis for the efficient implementation for many operations and scenarios, such as: combining partial functions into orElse/andThen chains does not lead to excessive apply/isDefinedAt evaluation lift and unlift do not evaluate source functions twice on each invocation runWith allows efficient imperative-style combining of partial functions with conditionally applied actions For non-literal partial function classes with nontrivial isDefinedAt method it is recommended to override applyOrElse with custom implementation that avoids double isDefinedAt evaluation. This may result in better performance and more predictable behavior w.r.t. side effects. @param x the function argument @param default the fallback function @return the result of this function or fallback function application. Attributes Definition Classes MapOps -> PartialFunction Inherited from: MapOps

Invocation template:

```scala
receiver.applyOrElse[K1, V1](x, default)
```

## `` com.cra.figaro.util.HeapPriorityMap.canEqual ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#canEqual-4ad)

```scala
def canEqual(that: Any): Boolean
```

Checks whether this instance can possibly equal that.

Type parameters: none.

Parameters, list 1: `` that: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Checks whether this instance can possibly equal that. A method that should be called from every well-designed equals method that is open to be overridden in a subclass. See Programming in Scala, Chapter 28 for discussion and design. Value parameters that the value being probed for possible equality Attributes Returns true if this instance can possibly equal that, otherwise false Inherited from: Map

Invocation template:

```scala
receiver.canEqual(that)
```

## `` com.cra.figaro.util.HeapPriorityMap.clear ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#clear-94c)

```scala
def clear(): Unit
```

Clears the contents of this builder. After execution of this method the builder will contain no elements.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clears the contents of this builder. After execution of this method the builder will contain no elements. Attributes Inherited from: MapOps

Invocation template:

```scala
receiver.clear()
```

## `` com.cra.figaro.util.HeapPriorityMap.collect ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#collect-fffff5b3)

```scala
def collect[B](pf: PartialFunction[(T, U), B]): Iterable[B]
```

Builds a new mutable map by applying a partial function to all elements of this mutable map on which the function is defined.

Type parameters: `` B ``.

Parameters, list 1: `` pf: PartialFunction[(T, U), B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Builds a new mutable map by applying a partial function to all elements of this mutable map on which the function is defined. Type parameters B the element type of the returned mutable map. Value parameters pf the partial function which filters and maps the mutable map. Attributes Returns a new mutable map resulting from applying the given partial function pf to each element on which it is defined and collecting the results. The order of the elements is preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.collect[B](pf)
```

## `` com.cra.figaro.util.HeapPriorityMap.collect ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#collect-fffff957)

```scala
def collect[K2, V2](pf: PartialFunction[(T, U), (K2, V2)]): Map[K2, V2]
```

Builds a new collection by applying a partial function to all elements of this mutable map on which the function is defined.

Type parameters: `` K2 ``; `` V2 ``.

Parameters, list 1: `` pf: PartialFunction[(T, U), (K2, V2)] ``.

Returns: `` Map[K2, V2] ``.

Source contract/attributes: Builds a new collection by applying a partial function to all elements of this mutable map on which the function is defined. Type parameters K2 the key type of the returned mutable map. V2 the value type of the returned mutable map. Value parameters pf the partial function which filters and maps the mutable map. Attributes Returns a new mutable map resulting from applying the given partial function pf to each element on which it is defined and collecting the results. The order of the elements is preserved. Inherited from: MapOps

Invocation template:

```scala
receiver.collect[K2, V2](pf)
```

## `` com.cra.figaro.util.HeapPriorityMap.collectFirst ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#collectFirst-25d)

```scala
def collectFirst[B](pf: PartialFunction[(T, U), B]): Option[B]
```

Finds the first element of the mutable map for which the given partial function is defined, and applies the partial function to it.

Type parameters: `` B ``.

Parameters, list 1: `` pf: PartialFunction[(T, U), B] ``.

Returns: `` Option[B] ``.

Source contract/attributes: Finds the first element of the mutable map for which the given partial function is defined, and applies the partial function to it. Note: may not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Type parameters B the result type of the partial function Value parameters pf the partial function Attributes Returns an option value containing pf applied to the first value for which it is defined, or None if none exists. Example Seq("a", 1, 5L).collectFirst { case x: Int => x*10 } = Some(10) Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.collectFirst[B](pf)
```

## `` com.cra.figaro.util.HeapPriorityMap.compose ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#compose-b91)

```scala
def compose[R](k: PartialFunction[R, T]): PartialFunction[R, U]
```

Composes another partial function k with this partial function so that this partial function gets applied to results of k.

Type parameters: `` R ``.

Parameters, list 1: `` k: PartialFunction[R, T] ``.

Returns: `` PartialFunction[R, U] ``.

Source contract/attributes: Composes another partial function k with this partial function so that this partial function gets applied to results of k. Note that calling isDefinedAt on the resulting partial function may apply the first partial function and execute its side effect. For efficiency, it is recommended to call applyOrElse instead of isDefinedAt or apply. Type parameters R the parameter type of the transformation function. Value parameters k the transformation function Attributes Returns a partial function with the domain of other partial function narrowed by this partial function, which maps arguments x to this(k(x)). Inherited from: PartialFunction

Invocation template:

```scala
receiver.compose[R](k)
```

## `` com.cra.figaro.util.HeapPriorityMap.compose ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#compose-51)

```scala
def compose[A](g: A => T): A => U
```

Composes two instances of Function1 in a new Function1, with this function applied last.

Type parameters: `` A ``.

Parameters, list 1: `` g: A => T ``.

Returns: `` A => U ``.

Source contract/attributes: Composes two instances of Function1 in a new Function1, with this function applied last. Type parameters A the type to which function g can be applied Value parameters g a function A => T1 Attributes Returns a new function f such that f(x) == apply(g(x)) Inherited from: Function1

Invocation template:

```scala
receiver.compose[A](g)
```

## `` com.cra.figaro.util.HeapPriorityMap.concat ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#concat-1dd)

```scala
def concat[B >: (T, U)](suffix: IterableOnce[B]): Iterable[B]
```

Returns a new mutable map containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the mutable map is the most specific superclass encompassing the element types of the two operands.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` suffix: IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Returns a new mutable map containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the mutable map is the most specific superclass encompassing the element types of the two operands. Type parameters B the element type of the returned collection. Value parameters suffix the iterable to append. Attributes Returns a new mutable map which contains all elements of this mutable map followed by all elements of suffix. Inherited from: IterableOps

Invocation template:

```scala
receiver.concat[B](suffix)
```

## `` com.cra.figaro.util.HeapPriorityMap.concat ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#concat-62)

```scala
def concat[V2 >: U](suffix: IterableOnce[(T, V2)]): Map[T, V2]
```

Returns a new mutable map containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the mutable map is the most specific superclass encompassing the element types of the two operands.

Type parameters: `` V2 >: U ``.

Parameters, list 1: `` suffix: IterableOnce[(T, V2)] ``.

Returns: `` Map[T, V2] ``.

Source contract/attributes: Returns a new mutable map containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the mutable map is the most specific superclass encompassing the element types of the two operands. Type parameters V2 the value type of the returned map, a supertype of V Value parameters suffix the iterable to append. Attributes Returns a new mutable map which contains all elements of this mutable map followed by all elements of suffix. Inherited from: MapOps

Invocation template:

```scala
receiver.concat[V2](suffix)
```

## `` com.cra.figaro.util.HeapPriorityMap.contains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#contains-4ad)

```scala
def contains(key: T): Boolean
```

Tests whether this map contains a binding for a key.

Type parameters: none.

Parameters, list 1: `` key: T ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this map contains a binding for a key. Value parameters key the key Attributes Returns true if there is a binding for key in this map, false otherwise. Inherited from: MapOps

Invocation template:

```scala
receiver.contains(key)
```

## `` com.cra.figaro.util.HeapPriorityMap.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#copyToArray-fffffd03)

```scala
def copyToArray[B >: (T, U)](dest: Array[B], start: Int, n: Int): Int
```

Copies elements to an array and returns the number of elements written.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` dest: Array[B] ``; `` start: Int ``; `` n: Int ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array and returns the number of elements written. Fills the given array dest starting at index start with at most n elements of this mutable map. Copying will stop once either all the elements of this mutable map have been copied, or the end of the array is reached, or n elements have been copied. If start is less than zero, it is taken as zero. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. n the maximal number of elements to copy. start the starting index of xs. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest, start, n)
```

## `` com.cra.figaro.util.HeapPriorityMap.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#copyToArray-fffffaa4)

```scala
def copyToArray[B >: (T, U)](dest: Array[B], start: Int): Int
```

Copies elements to an array, returning the number of elements written.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` dest: Array[B] ``; `` start: Int ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array, returning the number of elements written. Fills the given array dest starting at index start with values of this mutable map. Copying will stop once either all the elements of this mutable map have been copied, or the end of the array is reached. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. start the starting index of xs. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest, start)
```

## `` com.cra.figaro.util.HeapPriorityMap.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#copyToArray-fffff3e3)

```scala
def copyToArray[B >: (T, U)](dest: Array[B]): Int
```

Copies elements to an array, returning the number of elements written.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` dest: Array[B] ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array, returning the number of elements written. Fills the given array dest starting at index start with values of this mutable map. Copying will stop once either all the elements of this mutable map have been copied, or the end of the array is reached. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest)
```

## `` com.cra.figaro.util.HeapPriorityMap.corresponds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#corresponds-958)

```scala
def corresponds[B](that: IterableOnce[B])(p: ((T, U), B) => Boolean): Boolean
```

Tests whether every element of this collection's iterator relates to the corresponding element of another collection by satisfying a test predicate.

Type parameters: `` B ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Parameters, list 2: `` p: ((T, U), B) => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether every element of this collection's iterator relates to the corresponding element of another collection by satisfying a test predicate. Note: will not terminate for infinite-sized collections. Type parameters B the type of the elements of that Value parameters p the test predicate, which relates elements from both collections that the other collection Attributes Returns true if both collections have the same length and p(x, y) is true for all corresponding elements x of this iterator and y of that, otherwise false Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.corresponds[B](that)(p)
```

## `` com.cra.figaro.util.HeapPriorityMap.count ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#count-db6)

```scala
def count(p: (T, U) => Boolean): Int
```

Counts the number of elements in the mutable map which satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` Int ``.

Source contract/attributes: Counts the number of elements in the mutable map which satisfy a predicate. Note: will not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns the number of elements satisfying the predicate p. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.count(p)
```

## `` com.cra.figaro.util.HeapPriorityMap.default ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#default-d26)

```scala
def default(key: T): U
```

Defines the default value computation for the map, returned when a key is not found.

Type parameters: none.

Parameters, list 1: `` key: T ``.

Returns: `` U ``.

Source contract/attributes: Defines the default value computation for the map, returned when a key is not found. The method implemented here throws an exception, but it may be overridden by subclasses. Value parameters key the given key value for which a binding is missing. Attributes Returns the value associated with the given key when it is not found in the map Throws NoSuchElementException if no default value is defined Inherited from: MapOps

Invocation template:

```scala
receiver.default(key)
```

## `` com.cra.figaro.util.HeapPriorityMap.drop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#drop-aba)

```scala
def drop(n: Int): Map[T, U]
```

Selects all elements except the first n ones.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Selects all elements except the first n ones. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to drop from this mutable map. Attributes Returns a mutable map consisting of all elements of this mutable map except the first n ones, or else the empty mutable map, if this mutable map has less than n elements. If n is negative, don't drop any elements. Inherited from: IterableOps

Invocation template:

```scala
receiver.drop(n)
```

## `` com.cra.figaro.util.HeapPriorityMap.dropRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#dropRight-aba)

```scala
def dropRight(n: Int): Map[T, U]
```

Selects all elements except last n ones.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Selects all elements except last n ones. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to drop from this mutable map. Attributes Returns a mutable map consisting of all elements of this mutable map except the last n ones, or else the empty mutable map, if this mutable map has less than n elements. If n is negative, don't drop any elements. Inherited from: IterableOps

Invocation template:

```scala
receiver.dropRight(n)
```

## `` com.cra.figaro.util.HeapPriorityMap.dropWhile ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#dropWhile-c24)

```scala
def dropWhile(p: (T, U) => Boolean): Map[T, U]
```

Selects all elements except the longest prefix that satisfies a predicate.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Selects all elements except the longest prefix that satisfies a predicate. The matching prefix starts with the first element of this mutable map, and the element following the prefix is the first element that does not satisfy the predicate. The matching prefix may be empty, so that this method returns the entire mutable map. Example: List(1, 2, 3, 100, 4).dropWhile(n => n < 10) // List(100, 4) List(1, 2, 3, 100, 4).dropWhile(n => n == 0) // List(1, 2, 3, 100, 4) Use span to obtain both the prefix and suffix. Use filterNot to drop all elements that satisfy the predicate. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p The predicate used to test elements. Attributes Returns the longest suffix of this mutable map whose first element does not satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.dropWhile(p)
```

## `` com.cra.figaro.util.HeapPriorityMap.elementWise ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#elementWise-0)

```scala
def elementWise: ElementWiseExtractor[T, U]
```

Returns an extractor object with a unapplySeq method, which extracts each element of a sequence data.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` ElementWiseExtractor[T, U] ``.

Source contract/attributes: Returns an extractor object with a unapplySeq method, which extracts each element of a sequence data. Attributes Example val firstChar: String => Option[Char] = _.headOption Seq("foo", "bar", "baz") match { case firstChar.unlift.elementWise(c0, c1, c2) => println(s"$c0, $c1, $c2") // Output: f, b, b } Inherited from: PartialFunction

Invocation template:

```scala
receiver.elementWise
```

## `` com.cra.figaro.util.HeapPriorityMap.empty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#empty-0)

```scala
override def empty: Map[T, U]
```

The empty mutable map.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Map[T, U] ``.

Source contract/attributes: The empty mutable map. Attributes Returns an empty iterable of type mutable.Map . Definition Classes MapFactoryDefaults -> IterableOps Inherited from: MapFactoryDefaults

Invocation template:

```scala
receiver.empty
```

## `` com.cra.figaro.util.HeapPriorityMap.equals ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#equals-4ad)

```scala
override def equals(o: Any): Boolean
```

Equality of maps is implemented using the lookup method get. This method returns true if

Type parameters: none.

Parameters, list 1: `` o: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Equality of maps is implemented using the lookup method get. This method returns true if the argument o is a Map, the two maps have the same size, and for every (key, value) pair in this map, other.get(key) == Some(value). The implementation of equals checks the canEqual method, so subclasses of Map can narrow down the equality to specific map types. The Map implementations in the standard library can all be compared, their canEqual methods return true. Note: The equals method only respects the equality laws (symmetry, transitivity) if the two maps use the same key equivalence function in their lookup operation. For example, the key equivalence operation in a scala.collection.immutable.TreeMap is defined by its ordering. Comparing a TreeMap with a HashMap leads to unexpected results if ordering.equiv(k1, k2) (used for lookup in TreeMap) is different from k1 == k2 (used for lookup in HashMap). import scala.collection.immutable._ val ord: Ordering[String] = _ compareToIgnoreCase _ val result1 = TreeMap("A" -> 1)(using ord) == HashMap("a" -> 1) // false val result2 = HashMap("a" -> 1) == TreeMap("A" -> 1)(using ord) // true Value parameters o the object to compare this map with for equality Attributes Returns true if o is a Map with the same size and identical key-value mappings Definition Classes Map -> Equals -> Any Inherited from: Map

Invocation template:

```scala
receiver.equals(o)
```

## `` com.cra.figaro.util.HeapPriorityMap.exists ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#exists-fffff66f)

```scala
def exists(p: (T, U) => Boolean): Boolean
```

Tests whether a predicate holds for at least one element of this mutable map.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether a predicate holds for at least one element of this mutable map. Note: may not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns true if the given predicate p is satisfied by at least one element of this mutable map, otherwise false Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.exists(p)
```

## `` com.cra.figaro.util.HeapPriorityMap.filter ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#filter-c24)

```scala
def filter(pred: (T, U) => Boolean): Map[T, U]
```

Selects all elements of this mutable map which satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` pred: (T, U) => Boolean ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Selects all elements of this mutable map which satisfy a predicate. Value parameters p the predicate used to test elements. Attributes Returns a new mutable map consisting of all elements of this mutable map that satisfy the given predicate p. The order of the elements is preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.filter(pred)
```

## `` com.cra.figaro.util.HeapPriorityMap.filterInPlace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#filterInPlace-fffff774)

```scala
def filterInPlace(p: (T, U) => Boolean): this.type
```

Retains only those mappings for which the predicate p returns true.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` this.type ``.

Source contract/attributes: Retains only those mappings for which the predicate p returns true. Value parameters p The test predicate Attributes Inherited from: MapOps

Invocation template:

```scala
receiver.filterInPlace(p)
```

## `` com.cra.figaro.util.HeapPriorityMap.filterNot ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#filterNot-c24)

```scala
def filterNot(pred: (T, U) => Boolean): Map[T, U]
```

Selects all elements of this mutable map which do not satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` pred: (T, U) => Boolean ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Selects all elements of this mutable map which do not satisfy a predicate. Value parameters pred the predicate used to test elements. Attributes Returns a new mutable map consisting of all elements of this mutable map that do not satisfy the given predicate pred. Their order may not be preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.filterNot(pred)
```

## `` com.cra.figaro.util.HeapPriorityMap.find ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#find-94e)

```scala
def find(p: (T, U) => Boolean): Option[(T, U)]
```

Finds the first element of the mutable map satisfying a predicate, if any.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` Option[(T, U)] ``.

Source contract/attributes: Finds the first element of the mutable map satisfying a predicate, if any. Note: may not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the predicate used to test elements. Attributes Returns an option value containing the first element in the mutable map that satisfies p, or None if none exists. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.find(p)
```

## `` com.cra.figaro.util.HeapPriorityMap.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#flatMap-5d3)

```scala
def flatMap[B](f: (T, U) => IterableOnce[B]): Iterable[B]
```

Builds a new mutable map by applying a function to all elements of this mutable map and using the elements of the resulting collections.

Type parameters: `` B ``.

Parameters, list 1: `` f: (T, U) => IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Builds a new mutable map by applying a function to all elements of this mutable map and using the elements of the resulting collections. For example: def getWords(lines: Seq[String]): Seq[String] = lines.flatMap(line => line.split("\\W+")) The type of the resulting collection is guided by the static type of this mutable map. This might cause unexpected results sometimes. For example: // lettersOf will return a Seq[Char] of likely repeated letters, instead of a Set def lettersOf(words: Seq[String]) = words.flatMap(word => word.toSet) // lettersOf2 will return a Set[Char], not a Seq def lettersOf2(words: Seq[String]) = words.toSet.flatMap(word => word.toSeq) // xs will be an Iterable[Int] val xs = Map("a" -> List(11, 111), "b" -> List(22, 222)).flatMap(_._2) // ys will be a Map[Int, Int] val ys = Map("a" -> List(1 -> 11, 1 -> 111), "b" -> List(2 -> 22, 2 -> 222)).flatMap(_._2) Type parameters B the element type of the returned collection. Value parameters f the function to apply to each element. Attributes Returns a new mutable map resulting from applying the given collection-valued function f to each element of this mutable map and concatenating the results. Inherited from: IterableOps

Invocation template:

```scala
receiver.flatMap[B](f)
```

## `` com.cra.figaro.util.HeapPriorityMap.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#flatMap-fffffcb7)

```scala
def flatMap[K2, V2](f: ((T, U)) => IterableOnce[(K2, V2)]): Map[K2, V2]
```

Builds a new map by applying a function to all elements of this mutable map and using the elements of the resulting collections.

Type parameters: `` K2 ``; `` V2 ``.

Parameters, list 1: `` f: ((T, U)) => IterableOnce[(K2, V2)] ``.

Returns: `` Map[K2, V2] ``.

Source contract/attributes: Builds a new map by applying a function to all elements of this mutable map and using the elements of the resulting collections. Type parameters K2 the key type of the returned map V2 the value type of the returned map Value parameters f the function to apply to each element. Attributes Returns a new mutable map resulting from applying the given collection-valued function f to each element of this mutable map and concatenating the results. Inherited from: MapOps

Invocation template:

```scala
receiver.flatMap[K2, V2](f)
```

## `` com.cra.figaro.util.HeapPriorityMap.flatten ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#flatten-5d3)

```scala
def flatten[B](implicit asIterable: (T, U) => IterableOnce[B]): Iterable[B]
```

Given that the elements of this collection are themselves iterable collections, converts this mutable map into a mutable map comprising the elements of these iterable collections.

Type parameters: `` B ``.

Context parameters, list 1: `` asIterable: (T, U) => IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Given that the elements of this collection are themselves iterable collections, converts this mutable map into a mutable map comprising the elements of these iterable collections. The resulting collection's type will be guided by the type of mutable map. For example: val xs = List( Set(1, 2, 3), Set(1, 2, 3) ).flatten // xs == List(1, 2, 3, 1, 2, 3) val ys = Set( List(1, 2, 3), List(3, 2, 1) ).flatten // ys == Set(1, 2, 3) Type parameters B the type of the elements of each iterable collection. Value parameters asIterable an implicit conversion which asserts that the element type of this mutable map is an Iterable. Attributes Returns a new mutable map resulting from concatenating all element collections. Inherited from: IterableOps

Invocation template:

```scala
receiver.flatten[B](using asIterable)
```

## `` com.cra.figaro.util.HeapPriorityMap.fold ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#fold-fffff9e1)

```scala
def fold[A1 >: (T, U)](z: A1)(op: (A1, A1) => A1): A1
```

Applies the given binary operator op to the given initial value z and all elements of this mutable map.

Type parameters: `` A1 >: (T, U) ``.

Parameters, list 1: `` z: A1 ``.

Parameters, list 2: `` op: (A1, A1) => A1 ``.

Returns: `` A1 ``.

Source contract/attributes: Applies the given binary operator op to the given initial value z and all elements of this mutable map. For each application of the operator, each operand is either an element of this mutable map, the initial value, or another such application of the operator. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. The initial value may be used an arbitrary number of times, but at least once. If this collection is ordered, then for any application of the operator, the element(s) appearing in the left operand will precede those in the right. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. In either case, it is also necessary that the initial value be a neutral value for the operator, e.g. Nil for List concatenation or 1 for multiplication. The default implementation in IterableOnce is equivalent to foldLeft but may be overridden for more efficient traversal orders. Note: will not terminate for infinite-sized collections. Type parameters A1 The type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. z An initial value; may be used an arbitrary number of times in the computation of the result; must be a neutral value for op for the result to always be the same across runs. Attributes Returns The result of applying op between all the elements and z, or z if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.fold[A1](z)(op)
```

## `` com.cra.figaro.util.HeapPriorityMap.foldLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#foldLeft-fffff9e1)

```scala
def foldLeft[B](z: B)(op: (B, (T, U)) => B): B
```

Applies the given binary operator op to the given initial value z and all elements of this mutable map, going left to right. Returns the initial value if this mutable map is empty.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, (T, U)) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to the given initial value z and all elements of this mutable map, going left to right. Returns the initial value if this mutable map is empty. "Going left to right" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this mutable map, the result is op( op( ... op( op(z, x,,1,,), x,,2,,) ... ), x,,n,,). If this collection is not ordered, then for each application of the operator, each right operand is an element. In addition, the leftmost operand is the initial value, and each other left operand is itself an application of the operator. The elements of this mutable map and the initial value all appear exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator. Value parameters op A binary operator. z An initial value. Attributes Returns The result of applying op to z and all elements of this mutable map, going left to right. Returns z if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foldLeft[B](z)(op)
```

## `` com.cra.figaro.util.HeapPriorityMap.foldRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#foldRight-fffff9e1)

```scala
def foldRight[B](z: B)(op: ((T, U), B) => B): B
```

Applies the given binary operator op to all elements of this mutable map and the given initial value z, going right to left. Returns the initial value if this mutable map is empty.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: ((T, U), B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this mutable map and the given initial value z, going right to left. Returns the initial value if this mutable map is empty. "Going right to left" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this mutable map, the result is op(x,,1,,, op(x,,2,,, op( ... op(x,,n,,, z) ... ))). If this collection is not ordered, then for each application of the operator, each left operand is an element. In addition, the rightmost operand is the initial value, and each other right operand is itself an application of the operator. The elements of this mutable map and the initial value all appear exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator. Value parameters op A binary operator. z An initial value. Attributes Returns The result of applying op to all elements of this mutable map and z, going right to left. Returns z if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foldRight[B](z)(op)
```

## `` com.cra.figaro.util.HeapPriorityMap.forall ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#forall-fffff66f)

```scala
def forall(p: (T, U) => Boolean): Boolean
```

Tests whether a predicate holds for all elements of this mutable map.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether a predicate holds for all elements of this mutable map. Note: may not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns true if this mutable map is empty or the given predicate p holds for all elements of this mutable map, otherwise false. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.forall(p)
```

## `` com.cra.figaro.util.HeapPriorityMap.foreach ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#foreach-58c)

```scala
def foreach[U](f: (T, U) => U): Unit
```

Applies f to each element for its side effects. Note: U parameter needed to help scalac's type inference.

Type parameters: `` U ``.

Parameters, list 1: `` f: (T, U) => U ``.

Returns: `` Unit ``.

Source contract/attributes: Applies f to each element for its side effects. Note: U parameter needed to help scalac's type inference. Type parameters U the return type of f; the value is discarded, but the type parameter aids type inference Value parameters f the function to apply to each element for its side effects Attributes Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foreach[U](f)
```

## `` com.cra.figaro.util.HeapPriorityMap.foreachEntry ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#foreachEntry-fffff84d)

```scala
def foreachEntry[U](f: (T, U) => U): Unit
```

Applies f to each key/value pair for its side effects Note: [U] parameter needed to help scalac's type inference.

Type parameters: `` U ``.

Parameters, list 1: `` f: (T, U) => U ``.

Returns: `` Unit ``.

Source contract/attributes: Applies f to each key/value pair for its side effects Note: [U] parameter needed to help scalac's type inference. Type parameters U the result type of the function f; not used in the method result but aids type inference Value parameters f the function to apply to each key-value pair Attributes Inherited from: MapOps

Invocation template:

```scala
receiver.foreachEntry[U](f)
```

## `` com.cra.figaro.util.HeapPriorityMap.getOrElse ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#getOrElse-ddf)

```scala
def getOrElse[V1 >: U](key: T, default: => V1): V1
```

Returns the value associated with a key, or a default value if the key is not contained in the map.

Type parameters: `` V1 >: U ``.

Parameters, list 1: `` key: T ``; `` default: => V1 ``.

Returns: `` V1 ``.

Source contract/attributes: Returns the value associated with a key, or a default value if the key is not contained in the map. Type parameters V1 the result type of the default computation. Value parameters default a computation that yields a default value in case no binding for key is found in the map. key the key. Attributes Returns the value associated with key if it exists, otherwise the result of the default computation. Inherited from: MapOps

Invocation template:

```scala
receiver.getOrElse[V1](key, default)
```

## `` com.cra.figaro.util.HeapPriorityMap.getOrElseUpdate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#getOrElseUpdate-fffff230)

```scala
def getOrElseUpdate(key: T, defaultValue: => U): U
```

If given key is already in this map, returns associated value.

Type parameters: none.

Parameters, list 1: `` key: T ``; `` defaultValue: => U ``.

Returns: `` U ``.

Source contract/attributes: If given key is already in this map, returns associated value. Otherwise, computes value from given expression defaultValue, stores with key in map and returns that value. Concurrent map implementations may evaluate the expression defaultValue multiple times, or may evaluate defaultValue without inserting the result. Value parameters defaultValue the computation yielding the value to associate with key, if key is previously unbound. key the key to test Attributes Returns the value associated with key (either previously or as a result of executing the method). Inherited from: MapOps

Invocation template:

```scala
receiver.getOrElseUpdate(key, defaultValue)
```

## `` com.cra.figaro.util.HeapPriorityMap.groupBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#groupBy-f68)

```scala
def groupBy[K](f: (T, U) => K): Map[K, Map[T, U]]
```

Partitions this mutable map into a map of mutable maps according to some discriminator function.

Type parameters: `` K ``.

Parameters, list 1: `` f: (T, U) => K ``.

Returns: `` Map[K, Map[T, U]] ``.

Source contract/attributes: Partitions this mutable map into a map of mutable maps according to some discriminator function. Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters K the type of keys returned by the discriminator function. Value parameters f the discriminator function. Attributes Returns A map from keys to mutable maps such that the following invariant holds: (xs groupBy f)(k) = xs filter (x => f(x) == k) That is, every key `k` is bound to a mutable map of those elements `x` for which `f(x)` equals `k`. Inherited from: IterableOps

Invocation template:

```scala
receiver.groupBy[K](f)
```

## `` com.cra.figaro.util.HeapPriorityMap.groupMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#groupMap-fffff03a)

```scala
def groupMap[K, B](key: (T, U) => K)(f: (T, U) => B): Map[K, Iterable[B]]
```

Partitions this mutable map into a map of mutable maps according to a discriminator function key. Each element in a group is transformed into a value of type B using the value function.

Type parameters: `` K ``; `` B ``.

Parameters, list 1: `` key: (T, U) => K ``.

Parameters, list 2: `` f: (T, U) => B ``.

Returns: `` Map[K, Iterable[B]] ``.

Source contract/attributes: Partitions this mutable map into a map of mutable maps according to a discriminator function key. Each element in a group is transformed into a value of type B using the value function. It is equivalent to groupBy(key).mapValues(_.map(f)), but more efficient. case class User(name: String, age: Int) def namesByAge(users: Seq[User]): Map[Int, Seq[String]] = users.groupMap(_.age)(_.name) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of values returned by the transformation function K the type of keys returned by the discriminator function Value parameters f the element transformation function key the discriminator function Attributes Returns a map associating each key k produced by key with a mutable map of values produced by applying f to elements that map to k Inherited from: IterableOps

Invocation template:

```scala
receiver.groupMap[K, B](key)(f)
```

## `` com.cra.figaro.util.HeapPriorityMap.groupMapReduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#groupMapReduce-8e8)

```scala
def groupMapReduce[K, B](key: (T, U) => K)(f: (T, U) => B)(reduce: (B, B) => B): Map[K, B]
```

Partitions this mutable map into a map according to a discriminator function key. All the values that have the same discriminator are then transformed by the f function and then reduced into a single value with the reduce function.

Type parameters: `` K ``; `` B ``.

Parameters, list 1: `` key: (T, U) => K ``.

Parameters, list 2: `` f: (T, U) => B ``.

Parameters, list 3: `` reduce: (B, B) => B ``.

Returns: `` Map[K, B] ``.

Source contract/attributes: Partitions this mutable map into a map according to a discriminator function key. All the values that have the same discriminator are then transformed by the f function and then reduced into a single value with the reduce function. It is equivalent to groupBy(key).mapValues(_.map(f).reduce(reduce)), but more efficient. def occurrences[A](as: Seq[A]): Map[A, Int] = as.groupMapReduce(identity)(_ => 1)(_ + _) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of values returned by the transformation function K the type of keys returned by the discriminator function Value parameters f the element transformation function key the discriminator function reduce the reduction function used to combine values mapped to the same key Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.groupMapReduce[K, B](key)(f)(reduce)
```

## `` com.cra.figaro.util.HeapPriorityMap.grouped ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#grouped-fffffbef)

```scala
def grouped(size: Int): Iterator[Map[T, U]]
```

Partitions elements in fixed size mutable maps.

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Iterator[Map[T, U]] ``.

Source contract/attributes: Partitions elements in fixed size mutable maps. Value parameters size the number of elements per group Attributes Returns An iterator producing mutable maps of size size, except the last will be less than size size if the elements don't divide evenly. See also scala.collection.Iterator, method grouped Inherited from: IterableOps

Invocation template:

```scala
receiver.grouped(size)
```

## `` com.cra.figaro.util.HeapPriorityMap.hashCode ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#hashCode-ffffff27)

```scala
override def hashCode(): Int
```

Calculates a hash code value for the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Int ``.

Source contract/attributes: Calculates a hash code value for the object. The default hashing algorithm is platform dependent. Note that it is allowed for two objects to have identical hash codes (o1.hashCode.equals(o2.hashCode)) yet not be equal (o1.equals(o2) returns false). A degenerate implementation could always return 0. However, it is required that if two objects are equal (o1.equals(o2) returns true) that they have identical hash codes (o1.hashCode.equals(o2.hashCode)). Therefore, when overriding this method, be sure to verify that the behavior is consistent with the equals method. Attributes Returns the hash code value for this object. Definition Classes Map -> Any Inherited from: Map

Invocation template:

```scala
receiver.hashCode()
```

## `` com.cra.figaro.util.HeapPriorityMap.head ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#head-0)

```scala
def head: (T, U)
```

Selects the first element of this mutable map.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` (T, U) ``.

Source contract/attributes: Selects the first element of this mutable map. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the first element of this mutable map. Throws NoSuchElementException if the mutable map is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.head
```

## `` com.cra.figaro.util.HeapPriorityMap.headOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#headOption-0)

```scala
def headOption: Option[(T, U)]
```

Optionally selects the first element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Option[(T, U)] ``.

Source contract/attributes: Optionally selects the first element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the first element of this mutable map if it is nonempty, None if it is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.headOption
```

## `` com.cra.figaro.util.HeapPriorityMap.init ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#init-0)

```scala
def init: Map[T, U]
```

The initial part of the collection without its last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Map[T, U] ``.

Source contract/attributes: The initial part of the collection without its last element. Note: Even when applied to a view or a lazy collection it will always force the elements. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.init
```

## `` com.cra.figaro.util.HeapPriorityMap.inits ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#inits-0)

```scala
def inits: Iterator[Map[T, U]]
```

Iterates over the inits of this mutable map. The first value will be this mutable map and the final one will be an empty mutable map, with the intervening values the results of successive applications of init.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Map[T, U]] ``.

Source contract/attributes: Iterates over the inits of this mutable map. The first value will be this mutable map and the final one will be an empty mutable map, with the intervening values the results of successive applications of init. Note: Even when applied to a view or a lazy collection it will always force the elements. Attributes Returns an iterator over all the inits of this mutable map Example List(1,2,3).inits = Iterator(List(1,2,3), List(1,2), List(1), Nil) Inherited from: IterableOps

Invocation template:

```scala
receiver.inits
```

## `` com.cra.figaro.util.HeapPriorityMap.isDefinedAt ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#isDefinedAt-4ad)

```scala
def isDefinedAt(key: T): Boolean
```

Tests whether this map contains a binding for a key. This method, which implements an abstract method of trait PartialFunction, is equivalent to contains.

Type parameters: none.

Parameters, list 1: `` key: T ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this map contains a binding for a key. This method, which implements an abstract method of trait PartialFunction, is equivalent to contains. Value parameters key the key Attributes Returns true if there is a binding for key in this map, false otherwise. Inherited from: MapOps

Invocation template:

```scala
receiver.isDefinedAt(key)
```

## `` com.cra.figaro.util.HeapPriorityMap.isTraversableAgain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#isTraversableAgain-0)

```scala
override def isTraversableAgain: Boolean
```

Tests whether this mutable map can be repeatedly traversed. Always true for Iterables and false for Iterators unless overridden.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this mutable map can be repeatedly traversed. Always true for Iterables and false for Iterators unless overridden. Attributes Returns true if it is repeatedly traversable, false otherwise. Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.isTraversableAgain
```

## `` com.cra.figaro.util.HeapPriorityMap.iterableFactory ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#iterableFactory-0)

```scala
override def iterableFactory: IterableFactory[Iterable]
```

The companion object of this mutable map, providing various factory methods.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IterableFactory[Iterable] ``.

Source contract/attributes: The companion object of this mutable map, providing various factory methods. Attributes Returns the companion factory object for this collection type Note When implementing a custom collection type and refining CC to the new type, this method needs to be overridden to return a factory for the new type (the compiler will issue an error otherwise). Definition Classes Iterable -> Iterable -> IterableOps Inherited from: Iterable

Invocation template:

```scala
receiver.iterableFactory
```

## `` com.cra.figaro.util.HeapPriorityMap.keySet ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#keySet-0)

```scala
def keySet: Set[T]
```

A set representing the keys contained by this map.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[T] ``.

Source contract/attributes: A set representing the keys contained by this map. For efficiency the resulting set may be a view (maintaining a reference to the map and reflecting modifications to the map), but it may also be a strict collection without reference to the map. To ensure an independent strict collection, use m.keysIterator.toSet To obtain a view on the keys, use scala.collection.View.fromIteratorProvider(m.keysIterator) Specifically, for mutable collections, it is not guaranteed that the set would reflect the changes made onto the map, nor it is guaranteed that the set would not reflect the changes. To guarantee either behavior, obtain a strict collection or a view as above. Attributes Returns a set representing the keys contained by this map Inherited from: MapOps

Invocation template:

```scala
receiver.keySet
```

## `` com.cra.figaro.util.HeapPriorityMap.keyStepper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#keyStepper-190)

```scala
def keyStepper[S <: Stepper[_]](implicit shape: StepperShape[T, S]): S
```

Returns a Stepper for the keys of this map. See method stepper.

Type parameters: `` S <: Stepper[_] ``.

Context parameters, list 1: `` shape: StepperShape[T, S] ``.

Returns: `` S ``.

Source contract/attributes: Returns a Stepper for the keys of this map. See method stepper. Type parameters S the type of Stepper to use, determined by the implicit StepperShape Attributes Inherited from: MapOps

Invocation template:

```scala
receiver.keyStepper[S](using shape)
```

## `` com.cra.figaro.util.HeapPriorityMap.keys ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#keys-0)

```scala
def keys: Iterable[T]
```

An Iterable collection of the keys contained by this map.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: An Iterable collection of the keys contained by this map. For efficiency the resulting collection may be a view (maintaining a reference to the map and reflecting modifications to the map), but it may also be a strict collection without reference to the map. To ensure an independent strict collection, use m.keysIterator.toSet To obtain a view on the keys, use scala.collection.View.fromIteratorProvider(m.keysIterator) Attributes Returns an Iterable collection of the keys contained by this map Inherited from: MapOps

Invocation template:

```scala
receiver.keys
```

## `` com.cra.figaro.util.HeapPriorityMap.keysIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#keysIterator-0)

```scala
def keysIterator: Iterator[T]
```

An Iterator of the keys contained by this map.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[T] ``.

Source contract/attributes: An Iterator of the keys contained by this map. Attributes Returns an Iterator of the keys contained by this map Inherited from: MapOps

Invocation template:

```scala
receiver.keysIterator
```

## `` com.cra.figaro.util.HeapPriorityMap.knownSize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#knownSize-0)

```scala
override def knownSize: Int
```

The number of elements in the collection under construction, if it can be cheaply computed, -1 otherwise.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: The number of elements in the collection under construction, if it can be cheaply computed, -1 otherwise. Attributes Returns The number of elements. The default implementation always returns -1. Definition Classes MapOps -> Growable -> IterableOnce Inherited from: MapOps

Invocation template:

```scala
receiver.knownSize
```

## `` com.cra.figaro.util.HeapPriorityMap.last ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#last-0)

```scala
def last: (T, U)
```

Selects the last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` (T, U) ``.

Source contract/attributes: Selects the last element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns The last element of this mutable map. Throws NoSuchElementException If the mutable map is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.last
```

## `` com.cra.figaro.util.HeapPriorityMap.lastOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#lastOption-0)

```scala
def lastOption: Option[(T, U)]
```

Optionally selects the last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Option[(T, U)] ``.

Source contract/attributes: Optionally selects the last element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the last element of this mutable map if it is nonempty, None if it is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.lastOption
```

## `` com.cra.figaro.util.HeapPriorityMap.lazyZip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#lazyZip-fffffd84)

```scala
def lazyZip[B](that: Iterable[B]): LazyZip2[(T, U), B, this.type]
```

Analogous to zip except that the elements in each collection are not consumed until a strict operation is invoked on the returned LazyZip2 decorator.

Type parameters: `` B ``.

Parameters, list 1: `` that: Iterable[B] ``.

Returns: `` LazyZip2[(T, U), B, this.type] ``.

Source contract/attributes: Analogous to zip except that the elements in each collection are not consumed until a strict operation is invoked on the returned LazyZip2 decorator. Calls to lazyZip can be chained to support higher arities (up to 4) without incurring the expense of constructing and deconstructing intermediary tuples. val xs = List(1, 2, 3) val res = (xs lazyZip xs lazyZip xs lazyZip xs).map((a, b, c, d) => a + b + c + d) // res == List(4, 8, 12) Type parameters B the type of the second element in each eventual pair Value parameters that the iterable providing the second element of each eventual pair Attributes Returns a decorator LazyZip2 that allows strict operations to be performed on the lazily evaluated pairs or chained calls to lazyZip. Implicit conversion to Iterable[(A, B)] is also supported. Inherited from: Iterable

Invocation template:

```scala
receiver.lazyZip[B](that)
```

## `` com.cra.figaro.util.HeapPriorityMap.lift ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#lift-0)

```scala
def lift: T => Option[U]
```

Turns this partial function into a plain function returning an Option result.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` T => Option[U] ``.

Source contract/attributes: Turns this partial function into a plain function returning an Option result. Attributes Returns a function that takes an argument x to Some(this(x)) if this is defined for x, and to None otherwise. See also Function.unlift Inherited from: PartialFunction

Invocation template:

```scala
receiver.lift
```

## `` com.cra.figaro.util.HeapPriorityMap.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#map-5d3)

```scala
def map[B](f: (T, U) => B): Iterable[B]
```

Builds a new mutable map by applying a function to all elements of this mutable map.

Type parameters: `` B ``.

Parameters, list 1: `` f: (T, U) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Builds a new mutable map by applying a function to all elements of this mutable map. Type parameters B the element type of the returned mutable map . Value parameters f the function to apply to each element. Attributes Returns a new mutable map resulting from applying the given function f to each element of this mutable map and collecting the results. Inherited from: IterableOps

Invocation template:

```scala
receiver.map[B](f)
```

## `` com.cra.figaro.util.HeapPriorityMap.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#map-fffffcb7)

```scala
def map[K2, V2](f: ((T, U)) => (K2, V2)): Map[K2, V2]
```

Builds a new map by applying a function to all elements of this mutable map.

Type parameters: `` K2 ``; `` V2 ``.

Parameters, list 1: `` f: ((T, U)) => (K2, V2) ``.

Returns: `` Map[K2, V2] ``.

Source contract/attributes: Builds a new map by applying a function to all elements of this mutable map. Type parameters K2 the key type of the returned map V2 the value type of the returned map Value parameters f the function to apply to each element. Attributes Returns a new mutable map resulting from applying the given function f to each element of this mutable map and collecting the results. Inherited from: MapOps

Invocation template:

```scala
receiver.map[K2, V2](f)
```

## `` com.cra.figaro.util.HeapPriorityMap.mapFactory ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#mapFactory-0)

```scala
override def mapFactory: MapFactory[Map]
```

The companion object of this map, providing various factory methods.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` MapFactory[Map] ``.

Source contract/attributes: The companion object of this map, providing various factory methods. Attributes Returns the MapFactory companion object for this map type Note When implementing a custom collection type and refining CC to the new type, this method needs to be overridden to return a factory for the new type (the compiler will issue an error otherwise). Definition Classes Map -> Map -> MapOps Inherited from: Map

Invocation template:

```scala
receiver.mapFactory
```

## `` com.cra.figaro.util.HeapPriorityMap.mapResult ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#mapResult-34b)

```scala
def mapResult[NewTo](f: Map[T, U] => NewTo): Builder[(T, U), NewTo]
```

A builder resulting from this builder by mapping the result using f.

Type parameters: `` NewTo ``.

Parameters, list 1: `` f: Map[T, U] => NewTo ``.

Returns: `` Builder[(T, U), NewTo] ``.

Source contract/attributes: A builder resulting from this builder by mapping the result using f. Type parameters NewTo the type of collection produced by the returned builder Value parameters f the function to apply to this builder's result Attributes Inherited from: Builder

Invocation template:

```scala
receiver.mapResult[NewTo](f)
```

## `` com.cra.figaro.util.HeapPriorityMap.mapValuesInPlace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#mapValuesInPlace-fffff774)

```scala
def mapValuesInPlace(f: (T, U) => U): this.type
```

Applies a transformation function to all values contained in this map. The transformation function produces new values from existing keys associated values.

Type parameters: none.

Parameters, list 1: `` f: (T, U) => U ``.

Returns: `` this.type ``.

Source contract/attributes: Applies a transformation function to all values contained in this map. The transformation function produces new values from existing keys associated values. Value parameters f the transformation to apply Attributes Returns the map itself. Inherited from: MapOps

Invocation template:

```scala
receiver.mapValuesInPlace(f)
```

## `` com.cra.figaro.util.HeapPriorityMap.max ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#max-ffffff34)

```scala
def max[B >: (T, U)](implicit ord: Ordering[B]): (T, U)
```

Finds the largest element.

Type parameters: `` B >: (T, U) ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` (T, U) ``.

Source contract/attributes: Finds the largest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns the largest element of this mutable map with respect to the ordering ord. Throws UnsupportedOperationException if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.max[B](using ord)
```

## `` com.cra.figaro.util.HeapPriorityMap.maxBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#maxBy-fffff0c5)

```scala
def maxBy[B](f: (T, U) => B)(implicit ord: Ordering[B]): (T, U)
```

Finds the first element which yields the largest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: (T, U) => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` (T, U) ``.

Source contract/attributes: Finds the first element which yields the largest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns the first element of this mutable map with the largest value measured by function f with respect to the ordering ord. Throws UnsupportedOperationException if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxBy[B](f)(using ord)
```

## `` com.cra.figaro.util.HeapPriorityMap.maxByOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#maxByOption-fffff66f)

```scala
def maxByOption[B](f: (T, U) => B)(implicit ord: Ordering[B]): Option[(T, U)]
```

Finds the first element which yields the largest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: (T, U) => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` Option[(T, U)] ``.

Source contract/attributes: Finds the first element which yields the largest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns an option value containing the first element of this mutable map with the largest value measured by function f with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxByOption[B](f)(using ord)
```

## `` com.cra.figaro.util.HeapPriorityMap.maxOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#maxOption-fffff45e)

```scala
def maxOption[B >: (T, U)](implicit ord: Ordering[B]): Option[(T, U)]
```

Finds the largest element.

Type parameters: `` B >: (T, U) ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` Option[(T, U)] ``.

Source contract/attributes: Finds the largest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns an option value containing the largest element of this mutable map with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxOption[B](using ord)
```

## `` com.cra.figaro.util.HeapPriorityMap.min ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#min-ffffff34)

```scala
def min[B >: (T, U)](implicit ord: Ordering[B]): (T, U)
```

Finds the smallest element.

Type parameters: `` B >: (T, U) ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` (T, U) ``.

Source contract/attributes: Finds the smallest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns the smallest element of this mutable map with respect to the ordering ord. Throws UnsupportedOperationException if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.min[B](using ord)
```

## `` com.cra.figaro.util.HeapPriorityMap.minBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#minBy-fffff0c5)

```scala
def minBy[B](f: (T, U) => B)(implicit ord: Ordering[B]): (T, U)
```

Finds the first element which yields the smallest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: (T, U) => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` (T, U) ``.

Source contract/attributes: Finds the first element which yields the smallest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns the first element of this mutable map with the smallest value measured by function f with respect to the ordering ord. Throws UnsupportedOperationException if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minBy[B](f)(using ord)
```

## `` com.cra.figaro.util.HeapPriorityMap.minByOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#minByOption-fffff66f)

```scala
def minByOption[B](f: (T, U) => B)(implicit ord: Ordering[B]): Option[(T, U)]
```

Finds the first element which yields the smallest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: (T, U) => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` Option[(T, U)] ``.

Source contract/attributes: Finds the first element which yields the smallest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns an option value containing the first element of this mutable map with the smallest value measured by function f with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minByOption[B](f)(using ord)
```

## `` com.cra.figaro.util.HeapPriorityMap.minOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#minOption-fffff45e)

```scala
def minOption[B >: (T, U)](implicit ord: Ordering[B]): Option[(T, U)]
```

Finds the smallest element.

Type parameters: `` B >: (T, U) ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` Option[(T, U)] ``.

Source contract/attributes: Finds the smallest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns an option value containing the smallest element of this mutable map with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minOption[B](using ord)
```

## `` com.cra.figaro.util.HeapPriorityMap.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#mkString-0)

```scala
final def mkString: String
```

Displays all elements of this mutable map in a string.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Displays all elements of this mutable map in a string. Delegates to addString, which can be overridden. Attributes Returns a string representation of this mutable map. In the resulting string the string representations (w.r.t. the method toString) of all elements of this mutable map follow each other without any separator string. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString
```

## `` com.cra.figaro.util.HeapPriorityMap.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#mkString-fffff2ca)

```scala
final def mkString(sep: String): String
```

Displays all elements of this mutable map in a string using a separator string.

Type parameters: none.

Parameters, list 1: `` sep: String ``.

Returns: `` String ``.

Source contract/attributes: Displays all elements of this mutable map in a string using a separator string. Delegates to addString, which can be overridden. Value parameters sep the separator string. Attributes Returns a string representation of this mutable map. In the resulting string the string representations (w.r.t. the method toString) of all elements of this mutable map are separated by the string sep. Example List(1, 2, 3).mkString("|") = "1|2|3" Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString(sep)
```

## `` com.cra.figaro.util.HeapPriorityMap.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#mkString-d94)

```scala
final def mkString(start: String, sep: String, end: String): String
```

Displays all elements of this mutable map in a string using start, end, and separator strings.

Type parameters: none.

Parameters, list 1: `` start: String ``; `` sep: String ``; `` end: String ``.

Returns: `` String ``.

Source contract/attributes: Displays all elements of this mutable map in a string using start, end, and separator strings. Delegates to addString, which can be overridden. Value parameters end the ending string. sep the separator string. start the starting string. Attributes Returns a string representation of this mutable map. The resulting string begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this mutable map are separated by the string sep. Example List(1, 2, 3).mkString("(", "; ", ")") = "(1; 2; 3)" Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString(start, sep, end)
```

## `` com.cra.figaro.util.HeapPriorityMap.nonEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#nonEmpty-0)

```scala
def nonEmpty: Boolean
```

Tests whether the mutable map is not empty.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether the mutable map is not empty. Attributes Returns true if the mutable map contains at least one element, false otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.nonEmpty
```

## `` com.cra.figaro.util.HeapPriorityMap.orElse ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#orElse-a52)

```scala
def orElse[A1 <: T, B1 >: U](that: PartialFunction[A1, B1]): PartialFunction[A1, B1]
```

Composes this partial function with a fallback partial function which gets applied where this partial function is not defined.

Type parameters: `` A1 <: T ``; `` B1 >: U ``.

Parameters, list 1: `` that: PartialFunction[A1, B1] ``.

Returns: `` PartialFunction[A1, B1] ``.

Source contract/attributes: Composes this partial function with a fallback partial function which gets applied where this partial function is not defined. Type parameters A1 the argument type of the fallback function B1 the result type of the fallback function Value parameters that the fallback function Attributes Returns a partial function which has as domain the union of the domains of this partial function and that. The resulting partial function takes x to this(x) where this is defined, and to that(x) where it is not. Inherited from: PartialFunction

Invocation template:

```scala
receiver.orElse[A1, B1](that)
```

## `` com.cra.figaro.util.HeapPriorityMap.partition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#partition-243)

```scala
def partition(p: (T, U) => Boolean): (Map[T, U], Map[T, U])
```

A pair of, first, all elements that satisfy predicate p and, second, all elements that do not.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` (Map[T, U], Map[T, U]) ``.

Source contract/attributes: A pair of, first, all elements that satisfy predicate p and, second, all elements that do not. The two mutable map correspond to the result of filter and filterNot, respectively. The default implementation provided here needs to traverse the collection twice. Strict collections have an overridden version of partition in StrictOptimizedIterableOps, which requires only a single traversal. Value parameters p the predicate used to test elements Attributes Returns a pair of mutable maps: the first containing all elements that satisfy p, the second containing those that do not Inherited from: IterableOps

Invocation template:

```scala
receiver.partition(p)
```

## `` com.cra.figaro.util.HeapPriorityMap.partitionMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#partitionMap-2d1)

```scala
def partitionMap[A1, A2](f: (T, U) => Either[A1, A2]): (Iterable[A1], Iterable[A2])
```

Applies a function f to each element of the mutable map and returns a pair of mutable maps: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right.

Type parameters: `` A1 ``; `` A2 ``.

Parameters, list 1: `` f: (T, U) => Either[A1, A2] ``.

Returns: `` (Iterable[A1], Iterable[A2]) ``.

Source contract/attributes: Applies a function f to each element of the mutable map and returns a pair of mutable maps: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right. Example: val xs = $Coll(1, "one", 2, "two", 3, "three") partitionMap { case i: Int => Left(i) case s: String => Right(s) } // xs == ($Coll(1, 2, 3), // $Coll(one, two, three)) Type parameters A1 the element type of the first resulting collection A2 the element type of the second resulting collection Value parameters f the 'split function' mapping the elements of this mutable map to an scala.util.Either Attributes Returns a pair of mutable maps: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right. Inherited from: IterableOps

Invocation template:

```scala
receiver.partitionMap[A1, A2](f)
```

## `` com.cra.figaro.util.HeapPriorityMap.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#product-533)

```scala
def product[B >: (T, U)](implicit num: Numeric[B]): B
```

Multiplies together the elements of this collection.

Type parameters: `` B >: (T, U) ``.

Context parameters, list 1: `` num: Numeric[B] ``.

Returns: `` B ``.

Source contract/attributes: Multiplies together the elements of this collection. The default implementation uses reduce for a known non-empty collection, foldLeft otherwise. Note: will not terminate for infinite-sized collections. Type parameters B the result type of the * operator. Value parameters num an implicit parameter defining a set of numeric operations which includes the * operator to be used in forming the product. Attributes Returns the product of all elements of this mutable map with respect to the * operator in num. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.product[B](using num)
```

## `` com.cra.figaro.util.HeapPriorityMap.put ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#put-fffffa63)

```scala
def put(key: T, value: U): Option[U]
```

Adds a new key/value pair to this map and optionally returns previously bound value. If the map already contains a mapping for the key, it will be overridden by the new value.

Type parameters: none.

Parameters, list 1: `` key: T ``; `` value: U ``.

Returns: `` Option[U] ``.

Source contract/attributes: Adds a new key/value pair to this map and optionally returns previously bound value. If the map already contains a mapping for the key, it will be overridden by the new value. Value parameters key the key to update value the new value Attributes Returns an option value containing the value associated with the key before the put operation was executed, or None if key was not defined in the map before. Inherited from: MapOps

Invocation template:

```scala
receiver.put(key, value)
```

## `` com.cra.figaro.util.HeapPriorityMap.reduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#reduce-fffff3d4)

```scala
def reduce[B >: (T, U)](op: (B, B) => B): B
```

Applies the given binary operator op to all elements of this mutable map.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` op: (B, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this mutable map. For each application of the operator, each operand is either an element of this mutable map or another such application of the operator. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. If this collection is ordered, then for any application of the operator, the element(s) appearing in the left operand will precede those in the right. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. Attributes Returns The result of applying op between all the elements if the mutable map is nonempty. Throws UnsupportedOperationException if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduce[B](op)
```

## `` com.cra.figaro.util.HeapPriorityMap.reduceLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#reduceLeft-fffff3d4)

```scala
def reduceLeft[B >: (T, U)](op: (B, (T, U)) => B): B
```

Applies the given binary operator op to all elements of this mutable map, going left to right.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` op: (B, (T, U)) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this mutable map, going left to right. "Going left to right" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this mutable map, the result is op( op( op( ... op(x,,1,,, x,,2,,) ... ), x,,n-1,,), x,,n,,). If this collection is not ordered, then for each application of the operator, each right operand is an element. In addition, the leftmost operand is the first element of this mutable map and each other left operand is itself an application of the operator. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of applying op to all elements of this mutable map, going left to right. Throws UnsupportedOperationException if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceLeft[B](op)
```

## `` com.cra.figaro.util.HeapPriorityMap.reduceLeftOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#reduceLeftOption-8fe)

```scala
def reduceLeftOption[B >: (T, U)](op: (B, (T, U)) => B): Option[B]
```

If this mutable map is nonempty, reduces it with the given binary operator op, going left to right.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` op: (B, (T, U)) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this mutable map is nonempty, reduces it with the given binary operator op, going left to right. The behavior is the same as reduceLeft except that the value is None if the mutable map is empty. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of reducing this mutable map with op going left to right if the mutable map is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceLeftOption[B](op)
```

## `` com.cra.figaro.util.HeapPriorityMap.reduceOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#reduceOption-8fe)

```scala
def reduceOption[B >: (T, U)](op: (B, B) => B): Option[B]
```

If this mutable map is nonempty, reduces it with the given binary operator op.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` op: (B, B) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this mutable map is nonempty, reduces it with the given binary operator op. The behavior is the same as reduce except that the value is None if the mutable map is empty. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B A type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. Attributes Returns The result of reducing this mutable map with op if the mutable map is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceOption[B](op)
```

## `` com.cra.figaro.util.HeapPriorityMap.reduceRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#reduceRight-fffff3d4)

```scala
def reduceRight[B >: (T, U)](op: ((T, U), B) => B): B
```

Applies the given binary operator op to all elements of this mutable map, going right to left.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` op: ((T, U), B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this mutable map, going right to left. "Going right to left" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this mutable map, the result is op(x,,1,,, op(x,,2,,, op( ... op(x,,n-1,,, x,,n,,) ... ))). If this collection is not ordered, then for each application of the operator, each left operand is an element. In addition, the rightmost operand is the last element of this mutable map and each other right operand is itself an application of the operator. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of applying op to all elements of this mutable map, going right to left. Throws UnsupportedOperationException if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceRight[B](op)
```

## `` com.cra.figaro.util.HeapPriorityMap.reduceRightOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#reduceRightOption-8fe)

```scala
def reduceRightOption[B >: (T, U)](op: ((T, U), B) => B): Option[B]
```

If this mutable map is nonempty, reduces it with the given binary operator op, going right to left.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` op: ((T, U), B) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this mutable map is nonempty, reduces it with the given binary operator op, going right to left. The behavior is the same as reduceRight except that the value is None if the mutable map is empty. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of reducing this mutable map with op going right to left if the mutable map is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceRightOption[B](op)
```

## `` com.cra.figaro.util.HeapPriorityMap.remove ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#remove-fffffb50)

```scala
def remove(key: T): Option[U]
```

Removes a key from this map, returning the value associated previously with that key as an option.

Type parameters: none.

Parameters, list 1: `` key: T ``.

Returns: `` Option[U] ``.

Source contract/attributes: Removes a key from this map, returning the value associated previously with that key as an option. Value parameters key the key to be removed Attributes Returns an option value containing the value associated previously with key, or None if key was not defined in the map before. Inherited from: MapOps

Invocation template:

```scala
receiver.remove(key)
```

## `` com.cra.figaro.util.HeapPriorityMap.result ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#result-fffffe46)

```scala
def result(): Map[T, U]
```

Result collection consisting of all elements appended so far.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[T, U] ``.

Source contract/attributes: Result collection consisting of all elements appended so far. Attributes Inherited from: MapOps

Invocation template:

```scala
receiver.result()
```

## `` com.cra.figaro.util.HeapPriorityMap.runWith ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#runWith-51)

```scala
def runWith[U](action: U => U): T => Boolean
```

Composes this partial function with an action function which gets applied to results of this partial function. The action function is invoked only for its side effects; its result is ignored.

Type parameters: `` U ``.

Parameters, list 1: `` action: U => U ``.

Returns: `` T => Boolean ``.

Source contract/attributes: Composes this partial function with an action function which gets applied to results of this partial function. The action function is invoked only for its side effects; its result is ignored. Note that expression pf.runWith(action)(x) is equivalent to if(pf isDefinedAt x) { action(pf(x)); true } else false except that runWith is implemented via applyOrElse and thus potentially more efficient. Using runWith avoids double evaluation of pattern matchers and guards for partial function literals. @see applyOrElse. @param action the action function @return a function which maps arguments x to isDefinedAt(x). The resulting function runs action(this(x)) where this is defined. Attributes Inherited from: PartialFunction

Invocation template:

```scala
receiver.runWith[U](action)
```

## `` com.cra.figaro.util.HeapPriorityMap.scan ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#scan-fffff9e1)

```scala
def scan[B >: (T, U)](z: B)(op: (B, B) => B): Iterable[B]
```

Computes a prefix scan of the elements of the collection.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, B) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Computes a prefix scan of the elements of the collection. Note: The neutral element z may be applied more than once. Type parameters B element type of the resulting collection Value parameters op the associative operator for the scan z neutral element for the operator op Attributes Returns a new mutable map containing the prefix scan of the elements in this mutable map Inherited from: IterableOps

Invocation template:

```scala
receiver.scan[B](z)(op)
```

## `` com.cra.figaro.util.HeapPriorityMap.scanLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#scanLeft-fffff9e1)

```scala
def scanLeft[B](z: B)(op: (B, (T, U)) => B): Iterable[B]
```

Produces a mutable map containing cumulative results of applying the operator going left to right, including the initial value.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, (T, U)) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Produces a mutable map containing cumulative results of applying the operator going left to right, including the initial value. Note: will not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Type parameters B the type of the elements in the resulting collection Value parameters op the binary operator applied to the intermediate result and the element z the initial value Attributes Returns collection with intermediate results Inherited from: IterableOps

Invocation template:

```scala
receiver.scanLeft[B](z)(op)
```

## `` com.cra.figaro.util.HeapPriorityMap.scanRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#scanRight-fffff9e1)

```scala
def scanRight[B](z: B)(op: ((T, U), B) => B): Iterable[B]
```

Produces a collection containing cumulative results of applying the operator going right to left. The head of the collection is the last cumulative result.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: ((T, U), B) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Produces a collection containing cumulative results of applying the operator going right to left. The head of the collection is the last cumulative result. Note: will not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: Even when applied to a view or a lazy collection it will always force the elements. Example: List(1, 2, 3, 4).scanRight(0)(_ + _) == List(10, 9, 7, 4, 0) Type parameters B the type of the elements in the resulting collection Value parameters op the binary operator applied to the intermediate result and the element z the initial value Attributes Returns collection with intermediate results Inherited from: IterableOps

Invocation template:

```scala
receiver.scanRight[B](z)(op)
```

## `` com.cra.figaro.util.HeapPriorityMap.size ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#size-0)

```scala
def size: Int
```

The size of this mutable map.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: The size of this mutable map. Note: will not terminate for infinite-sized collections. Attributes Returns the number of elements in this mutable map. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.size
```

## `` com.cra.figaro.util.HeapPriorityMap.sizeCompare ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#sizeCompare-32d)

```scala
def sizeCompare(that: Iterable[_]): Int
```

Compares the size of this mutable map to the size of another Iterable.

Type parameters: none.

Parameters, list 1: `` that: Iterable[_] ``.

Returns: `` Int ``.

Source contract/attributes: Compares the size of this mutable map to the size of another Iterable. Value parameters that the Iterable whose size is compared with this mutable map's size. Attributes Returns A value x where x < 0 if this.size < that.size x == 0 if this.size == that.size x > 0 if this.size > that.size The method as implemented here does not call size directly; its running time is O(this.size min that.size) instead of O(this.size + that.size). The method should be overridden if computing size is cheap and knownSize returns -1. Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeCompare(that)
```

## `` com.cra.figaro.util.HeapPriorityMap.sizeCompare ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#sizeCompare-fffffbe0)

```scala
def sizeCompare(otherSize: Int): Int
```

Compares the size of this mutable map to a test value.

Type parameters: none.

Parameters, list 1: `` otherSize: Int ``.

Returns: `` Int ``.

Source contract/attributes: Compares the size of this mutable map to a test value. Value parameters otherSize the test value that gets compared with the size. Attributes Returns A value x where x < 0 if this.size < otherSize x == 0 if this.size == otherSize x > 0 if this.size > otherSize The default implementation does not call size, which may be arbitrarily expensive. It will use knownSize if nonnegative. Otherwise, it will iterate to compute an element count, with complexity O(size min otherSize). This method should be overridden if computing size is cheap and knownSize returns -1. In that case, the custom implementation should just compare size and otherSize directly. See also sizeIs Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeCompare(otherSize)
```

## `` com.cra.figaro.util.HeapPriorityMap.sizeHint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#sizeHint-fffff4f8)

```scala
final def sizeHint(coll: IterableOnce[_], delta: Int = ...): Unit
```

Gives a hint that the result of this builder is expected to have the same size as the given collection, plus some delta.

Type parameters: none.

Parameters, list 1: `` coll: IterableOnce[_] ``; `` delta: Int = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Gives a hint that the result of this builder is expected to have the same size as the given collection, plus some delta. This method provides a hint only if the collection has a known size, as specified by the following code: val coll: scala.collection.IterableOnce[?] = ??? val delta: Int = 0 def sizeHint(size: Int): Unit = () if (coll.knownSize != -1) if (coll.knownSize + delta <= 0) sizeHint(0) else sizeHint(coll.knownSize + delta) If the delta is negative and the result size is known to be negative, then the size hint is issued at zero. Some builder classes will optimize their representation based on the hint. However, builder implementations are required to work correctly even if the hint is wrong, i.e., if a different number of elements is added. Value parameters coll the collection which serves as a hint for the result's size. delta a correction to add to the coll.size to produce the size hint (zero if omitted). Attributes Inherited from: Builder

Invocation template:

```scala
receiver.sizeHint(coll, delta)
```

## `` com.cra.figaro.util.HeapPriorityMap.sizeHint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#sizeHint-fffff3b3)

```scala
def sizeHint(size: Int): Unit
```

Gives a hint how many elements are expected to be added in total by the time result is called.

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Unit ``.

Source contract/attributes: Gives a hint how many elements are expected to be added in total by the time result is called. Some builder classes will optimize their representation based on the hint. However, builder implementations are required to work correctly even if the hint is wrong, e.g., a different number of elements is added, or the hint is out of range. The default implementation simply ignores the hint. Value parameters size the hint how many elements will be added. Attributes Inherited from: Builder

Invocation template:

```scala
receiver.sizeHint(size)
```

## `` com.cra.figaro.util.HeapPriorityMap.sizeHintBounded ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#sizeHintBounded-fffffd7f)

```scala
final def sizeHintBounded(size: Int, boundingColl: Iterable[_]): Unit
```

Gives a hint how many elements are expected to be added when the next result is called, together with an upper bound given by the size of some other collection. Some builder classes will optimize their representation based on the hint. However, builder implementations are still required to work correctly even if the hint is wrong, i.e. a different number of elements is added.

Type parameters: none.

Parameters, list 1: `` size: Int ``; `` boundingColl: Iterable[_] ``.

Returns: `` Unit ``.

Source contract/attributes: Gives a hint how many elements are expected to be added when the next result is called, together with an upper bound given by the size of some other collection. Some builder classes will optimize their representation based on the hint. However, builder implementations are still required to work correctly even if the hint is wrong, i.e. a different number of elements is added. Value parameters boundingColl the bounding collection. If it is an IndexedSeqLike, then sizes larger than collection's size are reduced. size the hint how many elements will be added. Attributes Inherited from: Builder

Invocation template:

```scala
receiver.sizeHintBounded(size, boundingColl)
```

## `` com.cra.figaro.util.HeapPriorityMap.sizeIs ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#sizeIs-0)

```scala
final def sizeIs: SizeCompareOps
```

Returns a value class containing operations for comparing the size of this mutable map to a test value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` SizeCompareOps ``.

Source contract/attributes: Returns a value class containing operations for comparing the size of this mutable map to a test value. These operations are implemented in terms of `sizeCompare(Int)`, and allow the following more readable usages: this.sizeIs < size // this.sizeCompare(size) < 0 this.sizeIs <= size // this.sizeCompare(size) <= 0 this.sizeIs == size // this.sizeCompare(size) == 0 this.sizeIs != size // this.sizeCompare(size) != 0 this.sizeIs >= size // this.sizeCompare(size) >= 0 this.sizeIs > size // this.sizeCompare(size) > 0 Attributes Returns a wrapper that allows size comparisons using operators such as =, and > Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeIs
```

## `` com.cra.figaro.util.HeapPriorityMap.slice ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#slice-573)

```scala
def slice(from: Int, until: Int): Map[T, U]
```

Selects an interval of elements. The returned mutable map is made up of all elements x which satisfy the invariant:

Type parameters: none.

Parameters, list 1: `` from: Int ``; `` until: Int ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Selects an interval of elements. The returned mutable map is made up of all elements x which satisfy the invariant: from <= indexOf(x) < until Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters from the lowest index to include from this mutable map. until the lowest index to EXCLUDE from this mutable map. Attributes Returns a mutable map containing the elements greater than or equal to index from extending up to (but not including) index until of this mutable map. Example List('a', 'b', 'c', 'd', 'e').slice(1, 3) == List('b', 'c') Inherited from: IterableOps

Invocation template:

```scala
receiver.slice(from, until)
```

## `` com.cra.figaro.util.HeapPriorityMap.sliding ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#sliding-fffff156)

```scala
def sliding(size: Int, step: Int): Iterator[Map[T, U]]
```

Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped).

Type parameters: none.

Parameters, list 1: `` size: Int ``; `` step: Int ``.

Returns: `` Iterator[Map[T, U]] ``.

Source contract/attributes: Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped). The returned iterator will be empty when called on an empty collection. The last element the iterator produces may be smaller than the window size when the original collection isn't exhausted by the window before it and its last element isn't skipped by the step before it. Value parameters size the number of elements per group step the distance between the first elements of successive groups Attributes Returns An iterator producing mutable maps of size size, except the last element (which may be the only element) will be smaller if there are fewer than size elements remaining to be grouped. See also scala.collection.Iterator, method sliding Example List(1, 2, 3, 4, 5).sliding(2, 2) = Iterator(List(1, 2), List(3, 4), List(5)) List(1, 2, 3, 4, 5, 6).sliding(2, 3) = Iterator(List(1, 2), List(4, 5)) Inherited from: IterableOps

Invocation template:

```scala
receiver.sliding(size, step)
```

## `` com.cra.figaro.util.HeapPriorityMap.sliding ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#sliding-fffffbef)

```scala
def sliding(size: Int): Iterator[Map[T, U]]
```

Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped).

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Iterator[Map[T, U]] ``.

Source contract/attributes: Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped). An empty collection returns an empty iterator, and a non-empty collection containing fewer elements than the window size returns an iterator that will produce the original collection as its only element. Value parameters size the number of elements per group Attributes Returns An iterator producing mutable maps of size size, except for a non-empty collection with less than size elements, which returns an iterator that produces the source collection itself as its only element. See also scala.collection.Iterator, method sliding Example List().sliding(2) = empty iterator List(1).sliding(2) = Iterator(List(1)) List(1, 2).sliding(2) = Iterator(List(1, 2)) List(1, 2, 3).sliding(2) = Iterator(List(1, 2), List(2, 3)) Inherited from: IterableOps

Invocation template:

```scala
receiver.sliding(size)
```

## `` com.cra.figaro.util.HeapPriorityMap.span ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#span-243)

```scala
def span(p: (T, U) => Boolean): (Map[T, U], Map[T, U])
```

Splits this mutable map into a prefix/suffix pair according to a predicate.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` (Map[T, U], Map[T, U]) ``.

Source contract/attributes: Splits this mutable map into a prefix/suffix pair according to a predicate. Note: c span p is equivalent to (but possibly more efficient than) (c takeWhile p, c dropWhile p), provided the evaluation of the predicate p does not cause any side effects. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the test predicate Attributes Returns a pair consisting of the longest prefix of this mutable map whose elements all satisfy p, and the rest of this mutable map. Inherited from: IterableOps

Invocation template:

```scala
receiver.span(p)
```

## `` com.cra.figaro.util.HeapPriorityMap.splitAt ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#splitAt-fffffbd9)

```scala
override def splitAt(n: Int): (Map[T, U], Map[T, U])
```

Splits this mutable map into a prefix/suffix pair at a given position.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` (Map[T, U], Map[T, U]) ``.

Source contract/attributes: Splits this mutable map into a prefix/suffix pair at a given position. Note: c splitAt n is equivalent to (but possibly more efficient than) (c take n, c drop n). Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the position at which to split. Attributes Returns a pair of mutable maps consisting of the first n elements of this mutable map, and the other elements. Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.splitAt(n)
```

## `` com.cra.figaro.util.HeapPriorityMap.stepper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#stepper-190)

```scala
def stepper[S <: Stepper[_]](implicit shape: StepperShape[(T, U), S]): S
```

Returns a scala.collection.Stepper for the elements of this collection.

Type parameters: `` S <: Stepper[_] ``.

Context parameters, list 1: `` shape: StepperShape[(T, U), S] ``.

Returns: `` S ``.

Source contract/attributes: Returns a scala.collection.Stepper for the elements of this collection. The Stepper enables creating a Java stream to operate on the collection, see scala.jdk.StreamConverters. For collections holding primitive values, the Stepper can be used as an iterator which doesn't box the elements. The implicit scala.collection.StepperShape parameter defines the resulting Stepper type according to the element type of this collection. For collections of Int, Short, Byte or Char, an scala.collection.IntStepper is returned For collections of Double or Float, a scala.collection.DoubleStepper is returned For collections of Long a scala.collection.LongStepper is returned For any other element type, an scala.collection.AnyStepper is returned Note that this method is overridden in subclasses and the return type is refined to S with EfficientSplit, for example scala.collection.IndexedSeqOps.stepper. For Steppers marked with scala.collection.Stepper.EfficientSplit, the converters in scala.jdk.StreamConverters allow creating parallel streams, whereas bare Steppers can be converted only to sequential streams. Type parameters S the type of the returned Stepper, determined by the implicit StepperShape Attributes Inherited from: IterableOnce

Invocation template:

```scala
receiver.stepper[S](using shape)
```

## `` com.cra.figaro.util.HeapPriorityMap.subtractAll ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#subtractAll-fffff8b8)

```scala
def subtractAll(xs: IterableOnce[T]): this.type
```

Removes all elements produced by an iterator from this mutable map.

Type parameters: none.

Parameters, list 1: `` xs: IterableOnce[T] ``.

Returns: `` this.type ``.

Source contract/attributes: Removes all elements produced by an iterator from this mutable map. Value parameters xs the iterator producing the elements to remove. Attributes Returns the mutable map itself Inherited from: Shrinkable

Invocation template:

```scala
receiver.subtractAll(xs)
```

## `` com.cra.figaro.util.HeapPriorityMap.sum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#sum-533)

```scala
def sum[B >: (T, U)](implicit num: Numeric[B]): B
```

Sums the elements of this collection.

Type parameters: `` B >: (T, U) ``.

Context parameters, list 1: `` num: Numeric[B] ``.

Returns: `` B ``.

Source contract/attributes: Sums the elements of this collection. The default implementation uses reduce for a known non-empty collection, foldLeft otherwise. Note: will not terminate for infinite-sized collections. Type parameters B the result type of the + operator. Value parameters num an implicit parameter defining a set of numeric operations which includes the + operator to be used in forming the sum. Attributes Returns the sum of all elements of this mutable map with respect to the + operator in num. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.sum[B](using num)
```

## `` com.cra.figaro.util.HeapPriorityMap.tail ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#tail-0)

```scala
def tail: Map[T, U]
```

The rest of the collection without its first element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Map[T, U] ``.

Source contract/attributes: The rest of the collection without its first element. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.tail
```

## `` com.cra.figaro.util.HeapPriorityMap.tails ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#tails-0)

```scala
def tails: Iterator[Map[T, U]]
```

Iterates over the tails of this mutable map. The first value will be this mutable map and the final one will be an empty mutable map, with the intervening values the results of successive applications of tail.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Map[T, U]] ``.

Source contract/attributes: Iterates over the tails of this mutable map. The first value will be this mutable map and the final one will be an empty mutable map, with the intervening values the results of successive applications of tail. Attributes Returns an iterator over all the tails of this mutable map Example List(1,2,3).tails = Iterator(List(1,2,3), List(2,3), List(3), Nil) Inherited from: IterableOps

Invocation template:

```scala
receiver.tails
```

## `` com.cra.figaro.util.HeapPriorityMap.take ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#take-aba)

```scala
def take(n: Int): Map[T, U]
```

Selects the first n elements.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Selects the first n elements. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to take from this mutable map. Attributes Returns a mutable map consisting only of the first n elements of this mutable map, or else the whole mutable map, if it has less than n elements. If n is negative, returns an empty mutable map. Inherited from: IterableOps

Invocation template:

```scala
receiver.take(n)
```

## `` com.cra.figaro.util.HeapPriorityMap.takeRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#takeRight-aba)

```scala
def takeRight(n: Int): Map[T, U]
```

Selects the last n elements.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Selects the last n elements. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to take from this mutable map. Attributes Returns a mutable map consisting only of the last n elements of this mutable map, or else the whole mutable map, if it has less than n elements. If n is negative, returns an empty mutable map. Inherited from: IterableOps

Invocation template:

```scala
receiver.takeRight(n)
```

## `` com.cra.figaro.util.HeapPriorityMap.takeWhile ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#takeWhile-c24)

```scala
def takeWhile(p: (T, U) => Boolean): Map[T, U]
```

Takes longest prefix of elements that satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Takes longest prefix of elements that satisfy a predicate. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p The predicate used to test elements. Attributes Returns the longest prefix of this mutable map whose elements all satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.takeWhile(p)
```

## `` com.cra.figaro.util.HeapPriorityMap.tapEach ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#tapEach-5d3)

```scala
override def tapEach[U](f: (T, U) => U): Map[T, U]
```

Applies a side-effecting function to each element in this collection. Strict collections will apply f to their elements immediately, while lazy collections like Views and LazyLists will only apply f on each element if and when that element is evaluated, and each time that element is evaluated.

Type parameters: `` U ``.

Parameters, list 1: `` f: (T, U) => U ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Applies a side-effecting function to each element in this collection. Strict collections will apply f to their elements immediately, while lazy collections like Views and LazyLists will only apply f on each element if and when that element is evaluated, and each time that element is evaluated. Type parameters U the return type of f Value parameters f a function to apply to each element in this mutable map Attributes Returns The same logical collection as this Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.tapEach[U](f)
```

## `` com.cra.figaro.util.HeapPriorityMap.to ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#to-fffff006)

```scala
def to[C1](factory: Factory[(T, U), C1]): C1
```

Given a collection factory factory, converts this mutable map to the appropriate representation for the current element type A. Example uses:

Type parameters: `` C1 ``.

Parameters, list 1: `` factory: Factory[(T, U), C1] ``.

Returns: `` C1 ``.

Source contract/attributes: Given a collection factory factory, converts this mutable map to the appropriate representation for the current element type A. Example uses: import scala.collection.mutable.ArrayBuffer import scala.collection.immutable.BitSet val xs: Iterable[Int] = Seq(1, 2, 3, 4, 5) xs.to(List) xs.to(ArrayBuffer) xs.to(BitSet) // for xs: Iterable[Int] Type parameters C1 the target collection type Value parameters factory the factory for the target collection type Attributes Returns a new collection of type C1 containing all elements of this mutable map Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.to[C1](factory)
```

## `` com.cra.figaro.util.HeapPriorityMap.toArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#toArray-fffff5df)

```scala
def toArray[B >: (T, U) : ClassTag]: Array[B]
```

Converts this mutable map to an Array.

Type parameters: `` B >: (T, U) : ClassTag ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Array[B] ``.

Source contract/attributes: Converts this mutable map to an Array. Implementation note: DO NOT call Array.from from this method. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This mutable map as an Array[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toArray[B]
```

## `` com.cra.figaro.util.HeapPriorityMap.toBuffer ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#toBuffer-fffff87f)

```scala
final def toBuffer[B >: (T, U)]: Buffer[B]
```

Converts this mutable map to a Buffer.

Type parameters: `` B >: (T, U) ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Buffer[B] ``.

Source contract/attributes: Converts this mutable map to a Buffer. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This mutable map as a Buffer[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toBuffer[B]
```

## `` com.cra.figaro.util.HeapPriorityMap.toIndexedSeq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#toIndexedSeq-0)

```scala
def toIndexedSeq: IndexedSeq[(T, U)]
```

Converts this mutable map to an IndexedSeq.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IndexedSeq[(T, U)] ``.

Source contract/attributes: Converts this mutable map to an IndexedSeq. Attributes Returns This mutable map as an IndexedSeq[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toIndexedSeq
```

## `` com.cra.figaro.util.HeapPriorityMap.toList ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#toList-0)

```scala
def toList: List[(T, U)]
```

Converts this mutable map to a List.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(T, U)] ``.

Source contract/attributes: Converts this mutable map to a List. Attributes Returns This mutable map as a List[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toList
```

## `` com.cra.figaro.util.HeapPriorityMap.toMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#toMap-c0c)

```scala
def toMap[K, V](implicit ev: (T, U) <:< (K, V)): Map[K, V]
```

Converts this mutable map to a Map, given an implicit coercion from the mutable map's type to a key-value tuple.

Type parameters: `` K ``; `` V ``.

Context parameters, list 1: `` ev: (T, U) <:< (K, V) ``.

Returns: `` Map[K, V] ``.

Source contract/attributes: Converts this mutable map to a Map, given an implicit coercion from the mutable map's type to a key-value tuple. Type parameters K The key type for the resulting map. V The value type for the resulting map. Value parameters ev an implicit evidence that A is a subtype of (K, V) Attributes Returns This mutable map as a Map[K, V]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toMap[K, V](using ev)
```

## `` com.cra.figaro.util.HeapPriorityMap.toSeq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#toSeq-0)

```scala
def toSeq: Seq[(T, U)]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Seq[(T, U)] ``.

Source contract/attributes: Attributes Returns This mutable map as a Seq[A]. This is equivalent to to(Seq) but might be faster. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toSeq
```

## `` com.cra.figaro.util.HeapPriorityMap.toSet ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#toSet-fffffe9f)

```scala
def toSet[B >: (T, U)]: Set[B]
```

Converts this mutable map to a Set.

Type parameters: `` B >: (T, U) ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[B] ``.

Source contract/attributes: Converts this mutable map to a Set. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This mutable map as a Set[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toSet[B]
```

## `` com.cra.figaro.util.HeapPriorityMap.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#toString-665)

```scala
override def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Map -> Function1 -> Iterable -> Any Inherited from: Map

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.util.HeapPriorityMap.toVector ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#toVector-0)

```scala
def toVector: Vector[(T, U)]
```

Converts this mutable map to a Vector.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Vector[(T, U)] ``.

Source contract/attributes: Converts this mutable map to a Vector. Attributes Returns This mutable map as a Vector[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toVector
```

## `` com.cra.figaro.util.HeapPriorityMap.transpose ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#transpose-5d3)

```scala
def transpose[B](implicit asIterable: (T, U) => Iterable[B]): Iterable[Iterable[B]]
```

Transposes this mutable map of iterable collections into a mutable map of mutable maps.

Type parameters: `` B ``.

Context parameters, list 1: `` asIterable: (T, U) => Iterable[B] ``.

Returns: `` Iterable[Iterable[B]] ``.

Source contract/attributes: Transposes this mutable map of iterable collections into a mutable map of mutable maps. The resulting collection's type will be guided by the static type of mutable map. For example: val xs = List( Set(1, 2, 3), Set(4, 5, 6)).transpose // xs == List( // List(1, 4), // List(2, 5), // List(3, 6)) val ys = Vector( List(1, 2, 3), List(4, 5, 6)).transpose // ys == Vector( // Vector(1, 4), // Vector(2, 5), // Vector(3, 6)) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of the elements of each iterable collection. Value parameters asIterable an implicit conversion which asserts that the element type of this mutable map is an Iterable. Attributes Returns a two-dimensional mutable map of mutable maps which has as nth row the nth column of this mutable map. Throws IllegalArgumentException if all collections in this mutable map are not of the same size. Inherited from: IterableOps

Invocation template:

```scala
receiver.transpose[B](using asIterable)
```

## `` com.cra.figaro.util.HeapPriorityMap.unapply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#unapply-fffffb50)

```scala
def unapply(a: T): Option[U]
```

Tries to extract a B from an A in a pattern matching expression.

Type parameters: none.

Parameters, list 1: `` a: T ``.

Returns: `` Option[U] ``.

Source contract/attributes: Tries to extract a B from an A in a pattern matching expression. Attributes Inherited from: PartialFunction

Invocation template:

```scala
receiver.unapply(a)
```

## `` com.cra.figaro.util.HeapPriorityMap.unzip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#unzip-2d1)

```scala
def unzip[A1, A2](implicit asPair: (T, U) => (A1, A2)): (Iterable[A1], Iterable[A2])
```

Converts this mutable map of pairs into two collections of the first and second half of each pair.

Type parameters: `` A1 ``; `` A2 ``.

Context parameters, list 1: `` asPair: (T, U) => (A1, A2) ``.

Returns: `` (Iterable[A1], Iterable[A2]) ``.

Source contract/attributes: Converts this mutable map of pairs into two collections of the first and second half of each pair. val xs = $Coll( (1, "one"), (2, "two"), (3, "three")).unzip // xs == ($Coll(1, 2, 3), // $Coll(one, two, three)) Type parameters A1 the type of the first half of the element pairs A2 the type of the second half of the element pairs Value parameters asPair an implicit conversion which asserts that the element type of this mutable map is a pair. Attributes Returns a pair of mutable maps, containing the first, respectively second half of each element pair of this mutable map. Inherited from: IterableOps

Invocation template:

```scala
receiver.unzip[A1, A2](using asPair)
```

## `` com.cra.figaro.util.HeapPriorityMap.unzip3 ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#unzip3-a31)

```scala
def unzip3[A1, A2, A3](implicit asTriple: (T, U) => (A1, A2, A3)): (Iterable[A1], Iterable[A2], Iterable[A3])
```

Converts this mutable map of triples into three collections of the first, second, and third element of each triple.

Type parameters: `` A1 ``; `` A2 ``; `` A3 ``.

Context parameters, list 1: `` asTriple: (T, U) => (A1, A2, A3) ``.

Returns: `` (Iterable[A1], Iterable[A2], Iterable[A3]) ``.

Source contract/attributes: Converts this mutable map of triples into three collections of the first, second, and third element of each triple. val xs = $Coll( (1, "one", '1'), (2, "two", '2'), (3, "three", '3')).unzip3 // xs == ($Coll(1, 2, 3), // $Coll(one, two, three), // $Coll(1, 2, 3)) Type parameters A1 the type of the first member of the element triples A2 the type of the second member of the element triples A3 the type of the third member of the element triples Value parameters asTriple an implicit conversion which asserts that the element type of this mutable map is a triple. Attributes Returns a triple of mutable maps, containing the first, second, respectively third member of each element triple of this mutable map. Inherited from: IterableOps

Invocation template:

```scala
receiver.unzip3[A1, A2, A3](using asTriple)
```

## `` com.cra.figaro.util.HeapPriorityMap.update ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#update-ef2)

```scala
def update(key: T, value: U): Unit
```

Adds a new key/value pair to this map. If the map already contains a mapping for the key, it will be overridden by the new value.

Type parameters: none.

Parameters, list 1: `` key: T ``; `` value: U ``.

Returns: `` Unit ``.

Source contract/attributes: Adds a new key/value pair to this map. If the map already contains a mapping for the key, it will be overridden by the new value. Value parameters key The key to update value The new value Attributes Inherited from: MapOps

Invocation template:

```scala
receiver.update(key, value)
```

## `` com.cra.figaro.util.HeapPriorityMap.updateWith ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#updateWith-fffffbdb)

```scala
def updateWith(key: T)(remappingFunction: Option[U] => Option[U]): Option[U]
```

Updates a mapping for the specified key and its current optionally mapped value (Some if there is current mapping, None if not).

Type parameters: none.

Parameters, list 1: `` key: T ``.

Parameters, list 2: `` remappingFunction: Option[U] => Option[U] ``.

Returns: `` Option[U] ``.

Source contract/attributes: Updates a mapping for the specified key and its current optionally mapped value (Some if there is current mapping, None if not). If the remapping function returns Some(v), the mapping is updated with the new value v. If the remapping function returns None, the mapping is removed (or remains absent if initially absent). If the function itself throws an exception, the exception is rethrown, and the current mapping is left unchanged. Value parameters key the key value remappingFunction a function that receives current optionally mapped value and returns a new mapping Attributes Returns the new value associated with the specified key Inherited from: MapOps

Invocation template:

```scala
receiver.updateWith(key)(remappingFunction)
```

## `` com.cra.figaro.util.HeapPriorityMap.valueStepper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#valueStepper-190)

```scala
def valueStepper[S <: Stepper[_]](implicit shape: StepperShape[U, S]): S
```

Returns a Stepper for the values of this map. See method stepper.

Type parameters: `` S <: Stepper[_] ``.

Context parameters, list 1: `` shape: StepperShape[U, S] ``.

Returns: `` S ``.

Source contract/attributes: Returns a Stepper for the values of this map. See method stepper. Type parameters S the type of Stepper to use, determined by the implicit StepperShape Attributes Inherited from: MapOps

Invocation template:

```scala
receiver.valueStepper[S](using shape)
```

## `` com.cra.figaro.util.HeapPriorityMap.values ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#values-0)

```scala
def values: Iterable[U]
```

Collects all values of this map in an iterable collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[U] ``.

Source contract/attributes: Collects all values of this map in an iterable collection. Attributes Returns the values of this map as an iterable. Inherited from: MapOps

Invocation template:

```scala
receiver.values
```

## `` com.cra.figaro.util.HeapPriorityMap.valuesIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#valuesIterator-0)

```scala
def valuesIterator: Iterator[U]
```

Creates an iterator for all values in this map.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[U] ``.

Source contract/attributes: Creates an iterator for all values in this map. Attributes Returns an iterator over all values that are associated with some key in this map. Inherited from: MapOps

Invocation template:

```scala
receiver.valuesIterator
```

## `` com.cra.figaro.util.HeapPriorityMap.view ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#view-0)

```scala
override def view: MapView[T, U]
```

A view over the elements of this collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` MapView[T, U] ``.

Source contract/attributes: A view over the elements of this collection. Attributes Definition Classes MapOps -> IterableOps Inherited from: MapOps

Invocation template:

```scala
receiver.view
```

## `` com.cra.figaro.util.HeapPriorityMap.withDefault ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#withDefault-fffff5dd)

```scala
def withDefault(d: T => U): Map[T, U]
```

The same map with a given default function. Note: The default is only used for apply. Other methods like get, contains, iterator, keys, etc. are not affected by withDefaultValue.

Type parameters: none.

Parameters, list 1: `` d: T => U ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: The same map with a given default function. Note: The default is only used for apply. Other methods like get, contains, iterator, keys, etc. are not affected by withDefaultValue. Invoking transformer methods (e.g. map) will not preserve the default value. Value parameters d the function mapping keys to values, used for non-present keys Attributes Returns a wrapper of the map with a default value Inherited from: Map

Invocation template:

```scala
receiver.withDefault(d)
```

## `` com.cra.figaro.util.HeapPriorityMap.withDefaultValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#withDefaultValue-fffff3df)

```scala
def withDefaultValue(d: U): Map[T, U]
```

The same map with a given default value. Note: The default is only used for apply. Other methods like get, contains, iterator, keys, etc. are not affected by withDefaultValue.

Type parameters: none.

Parameters, list 1: `` d: U ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: The same map with a given default value. Note: The default is only used for apply. Other methods like get, contains, iterator, keys, etc. are not affected by withDefaultValue. Invoking transformer methods (e.g. map) will not preserve the default value. Value parameters d default value used for non-present keys Attributes Returns a wrapper of the map with a default value Inherited from: Map

Invocation template:

```scala
receiver.withDefaultValue(d)
```

## `` com.cra.figaro.util.HeapPriorityMap.withFilter ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#withFilter-fffff355)

```scala
override def withFilter(p: ((T, U)) => Boolean): WithFilter[T, U, Iterable, Map]
```

Creates a non-strict filter of this mutable map.

Type parameters: none.

Parameters, list 1: `` p: ((T, U)) => Boolean ``.

Returns: `` WithFilter[T, U, Iterable, Map] ``.

Source contract/attributes: Creates a non-strict filter of this mutable map. Note: the difference between c filter p and c withFilter p is that the former creates a new collection, whereas the latter only restricts the domain of subsequent map, flatMap, foreach, and withFilter operations. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the predicate used to test elements. Attributes Returns an object of class WithFilter, which supports map, flatMap, foreach, and withFilter operations. All these operations apply to those elements of this mutable map which satisfy the predicate p. Definition Classes MapFactoryDefaults -> IterableOps Inherited from: MapFactoryDefaults

Invocation template:

```scala
receiver.withFilter(p)
```

## `` com.cra.figaro.util.HeapPriorityMap.zip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#zip-1dd)

```scala
def zip[B](that: IterableOnce[B]): Iterable[((T, U), B)]
```

Returns a mutable map formed from this mutable map and another iterable collection by combining corresponding elements in pairs. If one of the two collections is longer than the other, its remaining elements are ignored.

Type parameters: `` B ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Returns: `` Iterable[((T, U), B)] ``.

Source contract/attributes: Returns a mutable map formed from this mutable map and another iterable collection by combining corresponding elements in pairs. If one of the two collections is longer than the other, its remaining elements are ignored. Type parameters B the type of the second half of the returned pairs Value parameters that The iterable providing the second half of each result pair Attributes Returns a new mutable map containing pairs consisting of corresponding elements of this mutable map and that. The length of the returned collection is the minimum of the lengths of this mutable map and that. Inherited from: IterableOps

Invocation template:

```scala
receiver.zip[B](that)
```

## `` com.cra.figaro.util.HeapPriorityMap.zipAll ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#zipAll-fffff401)

```scala
def zipAll[A1 >: (T, U), B](that: Iterable[B], thisElem: A1, thatElem: B): Iterable[(A1, B)]
```

Returns a mutable map formed from this mutable map and another iterable collection by combining corresponding elements in pairs. If one of the two collections is shorter than the other, placeholder elements are used to extend the shorter collection to the length of the longer.

Type parameters: `` A1 >: (T, U) ``; `` B ``.

Parameters, list 1: `` that: Iterable[B] ``; `` thisElem: A1 ``; `` thatElem: B ``.

Returns: `` Iterable[(A1, B)] ``.

Source contract/attributes: Returns a mutable map formed from this mutable map and another iterable collection by combining corresponding elements in pairs. If one of the two collections is shorter than the other, placeholder elements are used to extend the shorter collection to the length of the longer. Type parameters A1 the type of the first element in each result pair (supertype of A) B the type of the second element in each result pair Value parameters that the iterable providing the second half of each result pair thatElem the element to be used to fill up the result if that is shorter than this mutable map. thisElem the element to be used to fill up the result if this mutable map is shorter than that. Attributes Returns a new mutable map containing pairs consisting of corresponding elements of this mutable map and that. The length of the returned collection is the maximum of the lengths of this mutable map and that. If this mutable map is shorter than that, thisElem values are used to pad the result. If that is shorter than this mutable map, thatElem values are used to pad the result. Inherited from: IterableOps

Invocation template:

```scala
receiver.zipAll[A1, B](that, thisElem, thatElem)
```

## `` com.cra.figaro.util.HeapPriorityMap.zipWithIndex ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#zipWithIndex-0)

```scala
def zipWithIndex: Iterable[((T, U), Int)]
```

Zips this mutable map with its indices.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[((T, U), Int)] ``.

Source contract/attributes: Zips this mutable map with its indices. Attributes Returns A new mutable map containing pairs consisting of all elements of this mutable map paired with their index. Indices start at 0. Example List("a", "b", "c").zipWithIndex == List(("a", 0), ("b", 1), ("c", 2)) Inherited from: IterableOps

Invocation template:

```scala
receiver.zipWithIndex
```

## `` com.cra.figaro.util.HeapPriorityMap.+ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#+-f18)

```scala
def +[V1 >: U](elem1: (T, V1), elem2: (T, V1), elems: (T, V1)*): Map[T, V1]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` V1 >: U ``.

Parameters, list 1: `` elem1: (T, V1) ``; `` elem2: (T, V1) ``; `` elems: (T, V1)* ``.

Returns: `` Map[T, V1] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use ++ with an explicit collection argument instead of + with varargs Inherited from: MapOps

Invocation template:

```scala
receiver.+[V1](elem1, elem2, elems*)
```

## `` com.cra.figaro.util.HeapPriorityMap.+ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#+-fffff27b)

```scala
def +[V1 >: U](kv: (T, V1)): Map[T, V1]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` V1 >: U ``.

Parameters, list 1: `` kv: (T, V1) ``.

Returns: `` Map[T, V1] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Consider requiring an immutable Map or fall back to Map.concat. Inherited from: MapOps

Invocation template:

```scala
receiver.+[V1](kv)
```

## `` com.cra.figaro.util.HeapPriorityMap.++: ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#++:-1dd)

```scala
def ++:[B >: (T, U)](that: IterableOnce[B]): Iterable[B]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use ++ instead of ++: for collections of type Iterable Inherited from: IterableOps

Invocation template:

```scala
receiver.++:[B](that)
```

## `` com.cra.figaro.util.HeapPriorityMap.++: ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#++:-62)

```scala
def ++:[V1 >: U](that: IterableOnce[(T, V1)]): Map[T, V1]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` V1 >: U ``.

Parameters, list 1: `` that: IterableOnce[(T, V1)] ``.

Returns: `` Map[T, V1] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use ++ instead of ++: for collections of type Iterable Inherited from: MapOps

Invocation template:

```scala
receiver.++:[V1](that)
```

## `` com.cra.figaro.util.HeapPriorityMap.+= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#+=-fffff3cc)

```scala
final def +=(elem1: (T, U), elem2: (T, U), elems: (T, U)*): this.type
```

Add s two or more elements to this mutable map.

Type parameters: none.

Parameters, list 1: `` elem1: (T, U) ``; `` elem2: (T, U) ``; `` elems: (T, U)* ``.

Returns: `` this.type ``.

Source contract/attributes: Add s two or more elements to this mutable map. Value parameters elem1 the first element to add. elem2 the second element to add. elems the remaining elements to add. Attributes Returns the mutable map itself Deprecated [Since version 2.13.0] Use `++=` aka `addAll` instead of varargs `+=`; infix operations with an operand of multiple args will be deprecated Inherited from: Growable

Invocation template:

```scala
receiver.+=(elem1, elem2, elems*)
```

## `` com.cra.figaro.util.HeapPriorityMap.- ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#--fffff355)

```scala
final def -(key1: T, key2: T, keys: T*): Map[T, U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` key1: T ``; `` key2: T ``; `` keys: T* ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use -- or removeAll on an immutable Map Inherited from: MapOps

Invocation template:

```scala
receiver.-(key1, key2, keys*)
```

## `` com.cra.figaro.util.HeapPriorityMap.- ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#--253)

```scala
final def -(key: T): Map[T, U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` key: T ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use - or remove on an immutable Map Inherited from: MapOps

Invocation template:

```scala
receiver.-(key)
```

## `` com.cra.figaro.util.HeapPriorityMap.-- ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#---fffff66e)

```scala
def --(keys: IterableOnce[T]): Map[T, U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` keys: IterableOnce[T] ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Consider requiring an immutable Map. Inherited from: MapOps

Invocation template:

```scala
receiver.--(keys)
```

## `` com.cra.figaro.util.HeapPriorityMap.-= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#-=-fffff702)

```scala
def -=(elem1: T, elem2: T, elems: T*): this.type
```

Removes two or more elements from this mutable map.

Type parameters: none.

Parameters, list 1: `` elem1: T ``; `` elem2: T ``; `` elems: T* ``.

Returns: `` this.type ``.

Source contract/attributes: Removes two or more elements from this mutable map. Value parameters elem1 the first element to remove. elem2 the second element to remove. elems the remaining elements to remove. Attributes Returns the mutable map itself Deprecated [Since version 2.13.3] Use `--=` aka `subtractAll` instead of varargs `-=`; infix operations with an operand of multiple args will be deprecated Inherited from: Shrinkable

Invocation template:

```scala
receiver.-=(elem1, elem2, elems*)
```

## `` com.cra.figaro.util.HeapPriorityMap./: ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#/:-fffff9e1)

```scala
final def /:[B](z: B)(op: (B, (T, U)) => B): B
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, (T, U)) => B ``.

Returns: `` B ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use foldLeft instead of /: Inherited from: IterableOnceOps

Invocation template:

```scala
receiver./:[B](z)(op)
```

## `` com.cra.figaro.util.HeapPriorityMap.:\ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#:\-fffff9e1)

```scala
final def :\[B](z: B)(op: ((T, U), B) => B): B
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: ((T, U), B) => B ``.

Returns: `` B ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use foldRight instead of :\\ Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.:\[B](z)(op)
```

## `` com.cra.figaro.util.HeapPriorityMap.aggregate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#aggregate-952)

```scala
def aggregate[B](z: => B)(seqop: (B, (T, U)) => B, combop: (B, B) => B): B
```

Aggregates the results of applying an operator to subsequent elements.

Type parameters: `` B ``.

Parameters, list 1: `` z: => B ``.

Parameters, list 2: `` seqop: (B, (T, U)) => B ``; `` combop: (B, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Aggregates the results of applying an operator to subsequent elements. Since this method degenerates to foldLeft for sequential (non-parallel) collections, where the combining operation is ignored, it is advisable to prefer foldLeft for that case. For parallel collections, use the aggregate method specified by scala.collection.parallel.ParIterableLike. Type parameters B the result type, produced by seqop, combop, and by this function as a final result. Value parameters combop an associative operator for combining sequential results, unused for sequential collections. seqop the binary operator used to accumulate the result. z the start value, a neutral element for seqop. Attributes Deprecated [Since version 2.13.0] For sequential collections, prefer `foldLeft(z)(seqop)`. For parallel collections, use `ParIterableLike#aggregate`. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.aggregate[B](z)(seqop, combop)
```

## `` com.cra.figaro.util.HeapPriorityMap.companion ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#companion-0)

```scala
def companion: IterableFactory[Iterable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IterableFactory[Iterable] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use iterableFactory instead Inherited from: IterableOps

Invocation template:

```scala
receiver.companion
```

## `` com.cra.figaro.util.HeapPriorityMap.copyToBuffer ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#copyToBuffer-f0b)

```scala
final def copyToBuffer[B >: (T, U)](dest: Buffer[B]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` dest: Buffer[B] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use `dest ++= coll` instead Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToBuffer[B](dest)
```

## `` com.cra.figaro.util.HeapPriorityMap.filterKeys ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#filterKeys-5ea)

```scala
def filterKeys(p: T => Boolean): MapView[T, U]
```

Filters this map by retaining only keys satisfying a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` MapView[T, U] ``.

Source contract/attributes: Filters this map by retaining only keys satisfying a predicate. Value parameters p the predicate used to test keys Attributes Returns an immutable map consisting only of those key value pairs of this map where the key satisfies the predicate p. The resulting map wraps the original map without copying any elements. Deprecated [Since version 2.13.0] Use .view.filterKeys(f). A future version will include a strict version of this method (for now, .view.filterKeys(p).toMap). Inherited from: MapOps

Invocation template:

```scala
receiver.filterKeys(p)
```

## `` com.cra.figaro.util.HeapPriorityMap.hasDefiniteSize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#hasDefiniteSize-0)

```scala
def hasDefiniteSize: Boolean
```

Tests whether this mutable map is known to have a finite size. All strict collections are known to have finite size. For a non-strict collection such as Stream, the predicate returns **true** if all elements have been computed. It returns **false** if the stream is not yet evaluated to the end. Non-empty Iterators usually return **false** even if they were created from a collection with a known finite size.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this mutable map is known to have a finite size. All strict collections are known to have finite size. For a non-strict collection such as Stream, the predicate returns **true** if all elements have been computed. It returns **false** if the stream is not yet evaluated to the end. Non-empty Iterators usually return **false** even if they were created from a collection with a known finite size. Note: many collection methods will not work on collections of infinite sizes. The typical failure mode is an infinite loop. These methods always attempt a traversal without checking first that hasDefiniteSize returns **true**. However, checking hasDefiniteSize can provide an assurance that size is well-defined and non-termination is not a concern. Attributes Returns **true** if this collection is known to have finite size, **false** otherwise. See also method knownSize for a more useful alternative Deprecated [Since version 2.13.0] Check .knownSize instead of .hasDefiniteSize for more actionable information (see scaladoc for details) This method is deprecated in 2.13 because it does not provide any actionable information. As noted above, even the collection library itself does not use it. When there is no guarantee that a collection is finite, it is generally best to attempt a computation anyway and document that it will not terminate for infinite collections rather than backing out because this would prevent performing the computation on collections that are in fact finite even though hasDefiniteSize returns false. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.hasDefiniteSize
```

## `` com.cra.figaro.util.HeapPriorityMap.mapValues ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#mapValues-99)

```scala
def mapValues[W](f: U => W): MapView[T, W]
```

Transforms this map by applying a function to every retrieved value.

Type parameters: `` W ``.

Parameters, list 1: `` f: U => W ``.

Returns: `` MapView[T, W] ``.

Source contract/attributes: Transforms this map by applying a function to every retrieved value. Value parameters f the function used to transform values of this map. Attributes Returns a map view which maps every key of this map to f(this(key)). The resulting map wraps the original map without copying any elements. Deprecated [Since version 2.13.0] Use .view.mapValues(f). A future version will include a strict version of this method (for now, .view.mapValues(f).toMap). Inherited from: MapOps

Invocation template:

```scala
receiver.mapValues[W](f)
```

## `` com.cra.figaro.util.HeapPriorityMap.repr ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#repr-0)

```scala
final def repr: Map[T, U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Map[T, U] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use coll instead of repr in a collection implementation, use the collection value itself from the outside Inherited from: IterableOps

Invocation template:

```scala
receiver.repr
```

## `` com.cra.figaro.util.HeapPriorityMap.retain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#retain-fffff774)

```scala
final def retain(p: (T, U) => Boolean): this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` this.type ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use filterInPlace instead Inherited from: MapOps

Invocation template:

```scala
receiver.retain(p)
```

## `` com.cra.figaro.util.HeapPriorityMap.seq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#seq-0)

```scala
def seq: this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` this.type ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Iterable.seq always returns the iterable itself Inherited from: Iterable

Invocation template:

```scala
receiver.seq
```

## `` com.cra.figaro.util.HeapPriorityMap.toIterable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#toIterable-0)

```scala
final def toIterable: this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` this.type ``.

Source contract/attributes: Attributes Returns This collection as an Iterable[A]. No new collection will be built if this is already an Iterable[A]. Deprecated [Since version 2.13.7] toIterable is internal and will be made protected; its name is similar to `toList` or `toSeq`, but it doesn\'t copy non-immutable collections Inherited from: Iterable

Invocation template:

```scala
receiver.toIterable
```

## `` com.cra.figaro.util.HeapPriorityMap.toIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#toIterator-0)

```scala
final def toIterator: Iterator[(T, U)]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[(T, U)] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use .iterator instead of .toIterator Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toIterator
```

## `` com.cra.figaro.util.HeapPriorityMap.toStream ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#toStream-0)

```scala
final def toStream: Stream[(T, U)]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Stream[(T, U)] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use .to(LazyList) instead of .toStream Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toStream
```

## `` com.cra.figaro.util.HeapPriorityMap.toTraversable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#toTraversable-0)

```scala
final def toTraversable: Iterable[(T, U)]
```

Converts this mutable map to an unspecified Iterable. Will return the same collection if this instance is already Iterable.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[(T, U)] ``.

Source contract/attributes: Converts this mutable map to an unspecified Iterable. Will return the same collection if this instance is already Iterable. Attributes Returns An Iterable containing all elements of this mutable map. Deprecated [Since version 2.13.0] toTraversable is internal and will be made protected; its name is similar to `toList` or `toSeq`, but it doesn\'t copy non-immutable collections Inherited from: IterableOps

Invocation template:

```scala
receiver.toTraversable
```

## `` com.cra.figaro.util.HeapPriorityMap.transform ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#transform-fffff774)

```scala
final def transform(f: (T, U) => U): this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` f: (T, U) => U ``.

Returns: `` this.type ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use mapValuesInPlace instead Inherited from: MapOps

Invocation template:

```scala
receiver.transform(f)
```

## `` com.cra.figaro.util.HeapPriorityMap.updated ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#updated-a0f)

```scala
def updated[V1 >: U](key: T, value: V1): Map[T, V1]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` V1 >: U ``.

Parameters, list 1: `` key: T ``; `` value: V1 ``.

Returns: `` Map[T, V1] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use m.clone().addOne((k,v)) instead of m.updated(k, v) Inherited from: MapOps

Invocation template:

```scala
receiver.updated[V1](key, value)
```

## `` com.cra.figaro.util.HeapPriorityMap.view ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/HeapPriorityMap.html#view-fffff78d)

```scala
def view(from: Int, until: Int): View[(T, U)]
```

A view over a slice of the elements of this collection.

Type parameters: none.

Parameters, list 1: `` from: Int ``; `` until: Int ``.

Returns: `` View[(T, U)] ``.

Source contract/attributes: A view over a slice of the elements of this collection. Attributes Deprecated [Since version 2.13.0] Use .view.slice(from, until) instead of .view(from, until) Inherited from: IterableOps

Invocation template:

```scala
receiver.view(from, until)
```

## `` com.cra.figaro.util.`package`.InvalidDistributionException.addSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidDistributionException.html#addSuppressed-4b8)

```scala
final def addSuppressed(x$0: Throwable): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.addSuppressed(x$0)
```

## `` com.cra.figaro.util.`package`.InvalidDistributionException.fillInStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidDistributionException.html#fillInStackTrace-26c)

```scala
def fillInStackTrace(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.fillInStackTrace()
```

## `` com.cra.figaro.util.`package`.InvalidDistributionException.getCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidDistributionException.html#getCause-26c)

```scala
def getCause(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getCause()
```

## `` com.cra.figaro.util.`package`.InvalidDistributionException.getLocalizedMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidDistributionException.html#getLocalizedMessage-665)

```scala
def getLocalizedMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getLocalizedMessage()
```

## `` com.cra.figaro.util.`package`.InvalidDistributionException.getMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidDistributionException.html#getMessage-665)

```scala
def getMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getMessage()
```

## `` com.cra.figaro.util.`package`.InvalidDistributionException.getStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidDistributionException.html#getStackTrace-fffffa6d)

```scala
def getStackTrace(): Array[StackTraceElement]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[StackTraceElement] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getStackTrace()
```

## `` com.cra.figaro.util.`package`.InvalidDistributionException.getSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidDistributionException.html#getSuppressed-fffff2ce)

```scala
final def getSuppressed(): Array[Throwable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[Throwable] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getSuppressed()
```

## `` com.cra.figaro.util.`package`.InvalidDistributionException.initCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidDistributionException.html#initCause-fffff080)

```scala
def initCause(x$0: Throwable): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.initCause(x$0)
```

## `` com.cra.figaro.util.`package`.InvalidDistributionException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidDistributionException.html#printStackTrace-fffff230)

```scala
def printStackTrace(x$0: PrintWriter): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintWriter ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.util.`package`.InvalidDistributionException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidDistributionException.html#printStackTrace-fffff0fd)

```scala
def printStackTrace(x$0: PrintStream): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintStream ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.util.`package`.InvalidDistributionException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidDistributionException.html#printStackTrace-94c)

```scala
def printStackTrace(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace()
```

## `` com.cra.figaro.util.`package`.InvalidDistributionException.setStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidDistributionException.html#setStackTrace-f79)

```scala
def setStackTrace(x$0: Array[StackTraceElement]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Array[StackTraceElement] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.setStackTrace(x$0)
```

## `` com.cra.figaro.util.`package`.InvalidDistributionException.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidDistributionException.html#toString-665)

```scala
def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Inherited from: Throwable

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.util.`package`.InvalidMultinomialIndexException.addSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidMultinomialIndexException.html#addSuppressed-4b8)

```scala
final def addSuppressed(x$0: Throwable): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.addSuppressed(x$0)
```

## `` com.cra.figaro.util.`package`.InvalidMultinomialIndexException.fillInStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidMultinomialIndexException.html#fillInStackTrace-26c)

```scala
def fillInStackTrace(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.fillInStackTrace()
```

## `` com.cra.figaro.util.`package`.InvalidMultinomialIndexException.getCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidMultinomialIndexException.html#getCause-26c)

```scala
def getCause(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getCause()
```

## `` com.cra.figaro.util.`package`.InvalidMultinomialIndexException.getLocalizedMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidMultinomialIndexException.html#getLocalizedMessage-665)

```scala
def getLocalizedMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getLocalizedMessage()
```

## `` com.cra.figaro.util.`package`.InvalidMultinomialIndexException.getMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidMultinomialIndexException.html#getMessage-665)

```scala
def getMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getMessage()
```

## `` com.cra.figaro.util.`package`.InvalidMultinomialIndexException.getStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidMultinomialIndexException.html#getStackTrace-fffffa6d)

```scala
def getStackTrace(): Array[StackTraceElement]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[StackTraceElement] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getStackTrace()
```

## `` com.cra.figaro.util.`package`.InvalidMultinomialIndexException.getSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidMultinomialIndexException.html#getSuppressed-fffff2ce)

```scala
final def getSuppressed(): Array[Throwable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[Throwable] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getSuppressed()
```

## `` com.cra.figaro.util.`package`.InvalidMultinomialIndexException.initCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidMultinomialIndexException.html#initCause-fffff080)

```scala
def initCause(x$0: Throwable): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.initCause(x$0)
```

## `` com.cra.figaro.util.`package`.InvalidMultinomialIndexException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidMultinomialIndexException.html#printStackTrace-fffff230)

```scala
def printStackTrace(x$0: PrintWriter): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintWriter ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.util.`package`.InvalidMultinomialIndexException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidMultinomialIndexException.html#printStackTrace-fffff0fd)

```scala
def printStackTrace(x$0: PrintStream): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintStream ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.util.`package`.InvalidMultinomialIndexException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidMultinomialIndexException.html#printStackTrace-94c)

```scala
def printStackTrace(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace()
```

## `` com.cra.figaro.util.`package`.InvalidMultinomialIndexException.setStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidMultinomialIndexException.html#setStackTrace-f79)

```scala
def setStackTrace(x$0: Array[StackTraceElement]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Array[StackTraceElement] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.setStackTrace(x$0)
```

## `` com.cra.figaro.util.`package`.InvalidMultinomialIndexException.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/InvalidMultinomialIndexException.html#toString-665)

```scala
def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Inherited from: Throwable

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.util.`package`.LessThanOneTotalProbabilityException.addSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/LessThanOneTotalProbabilityException.html#addSuppressed-4b8)

```scala
final def addSuppressed(x$0: Throwable): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.addSuppressed(x$0)
```

## `` com.cra.figaro.util.`package`.LessThanOneTotalProbabilityException.fillInStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/LessThanOneTotalProbabilityException.html#fillInStackTrace-26c)

```scala
def fillInStackTrace(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.fillInStackTrace()
```

## `` com.cra.figaro.util.`package`.LessThanOneTotalProbabilityException.getCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/LessThanOneTotalProbabilityException.html#getCause-26c)

```scala
def getCause(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getCause()
```

## `` com.cra.figaro.util.`package`.LessThanOneTotalProbabilityException.getLocalizedMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/LessThanOneTotalProbabilityException.html#getLocalizedMessage-665)

```scala
def getLocalizedMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getLocalizedMessage()
```

## `` com.cra.figaro.util.`package`.LessThanOneTotalProbabilityException.getMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/LessThanOneTotalProbabilityException.html#getMessage-665)

```scala
def getMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getMessage()
```

## `` com.cra.figaro.util.`package`.LessThanOneTotalProbabilityException.getStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/LessThanOneTotalProbabilityException.html#getStackTrace-fffffa6d)

```scala
def getStackTrace(): Array[StackTraceElement]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[StackTraceElement] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getStackTrace()
```

## `` com.cra.figaro.util.`package`.LessThanOneTotalProbabilityException.getSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/LessThanOneTotalProbabilityException.html#getSuppressed-fffff2ce)

```scala
final def getSuppressed(): Array[Throwable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[Throwable] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getSuppressed()
```

## `` com.cra.figaro.util.`package`.LessThanOneTotalProbabilityException.initCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/LessThanOneTotalProbabilityException.html#initCause-fffff080)

```scala
def initCause(x$0: Throwable): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.initCause(x$0)
```

## `` com.cra.figaro.util.`package`.LessThanOneTotalProbabilityException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/LessThanOneTotalProbabilityException.html#printStackTrace-fffff230)

```scala
def printStackTrace(x$0: PrintWriter): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintWriter ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.util.`package`.LessThanOneTotalProbabilityException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/LessThanOneTotalProbabilityException.html#printStackTrace-fffff0fd)

```scala
def printStackTrace(x$0: PrintStream): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintStream ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.util.`package`.LessThanOneTotalProbabilityException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/LessThanOneTotalProbabilityException.html#printStackTrace-94c)

```scala
def printStackTrace(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace()
```

## `` com.cra.figaro.util.`package`.LessThanOneTotalProbabilityException.setStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/LessThanOneTotalProbabilityException.html#setStackTrace-f79)

```scala
def setStackTrace(x$0: Array[StackTraceElement]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Array[StackTraceElement] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.setStackTrace(x$0)
```

## `` com.cra.figaro.util.`package`.LessThanOneTotalProbabilityException.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/LessThanOneTotalProbabilityException.html#toString-665)

```scala
def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Inherited from: Throwable

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.util.LogStatistics.oneSidedTTest ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/LogStatistics$.html#oneSidedTTest-a59)

```scala
def oneSidedTTest(v1: LogStatistics, v2: LogStatistics): Double
```

Performs a one-sided t-test for the comparison of v1.logMean and v2.logMean. This compares the smaller mean to the larger mean, i.e. it computes a p-value for min(v1.logMean, v2.logMean) < max(v1.logMean, v2.logMean).

Type parameters: none.

Parameters, list 1: `` v1: LogStatistics ``; `` v2: LogStatistics ``.

Returns: `` Double ``.

Source contract/attributes: Performs a one-sided t-test for the comparison of v1.logMean and v2.logMean. This compares the smaller mean to the larger mean, i.e. it computes a p-value for min(v1.logMean, v2.logMean) < max(v1.logMean, v2.logMean). Value parameters v1 Mean, variance, and sample count from first distribution. Requires v1.count > 1. v2 Mean, variance, and sample count from second distribution. Requires v2.count > 1. Attributes Returns A p-value for the hypothesis. A small p-value indicates high confidence that the population mean of the sample with lesser mean is less than the population mean of the other sample.

Invocation template:

```scala
com.cra.figaro.util.LogStatistics.oneSidedTTest(v1, v2)
```

## `` com.cra.figaro.util.LogStatistics.multiplyByConstant ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/LogStatistics.html#multiplyByConstant-5cd)

```scala
def multiplyByConstant(logConstant: Double): LogStatistics
```

Returns the statistics corresponding to the distribution wherein each sample is multiplied by the given constant.

Type parameters: none.

Parameters, list 1: `` logConstant: Double ``.

Returns: `` LogStatistics ``.

Source contract/attributes: Returns the statistics corresponding to the distribution wherein each sample is multiplied by the given constant. Value parameters logConstant The log of the constant to multiply by. Attributes Returns Statistics such that the mean and variance are updated, and the count is unchanged.

Invocation template:

```scala
receiver.multiplyByConstant(logConstant)
```

## `` com.cra.figaro.util.LogStatistics.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/LogStatistics.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.util.LogStatistics.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/LogStatistics.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.util.MapResampler.resample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MapResampler.html#resample-993)

```scala
def resample(): T
```

Choose a sample with probability proportional to its weight.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` T ``.

Source contract/attributes: Choose a sample with probability proportional to its weight. Attributes

Invocation template:

```scala
receiver.resample()
```

## `` com.cra.figaro.util.MultiSet.addMany ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#addMany-fffff53e)

```scala
def addMany(t: T, count: Int): MultiSet[T]
```

Add several instances of this value to the set.

Type parameters: none.

Parameters, list 1: `` t: T ``; `` count: Int ``.

Returns: `` MultiSet[T] ``.

Source contract/attributes: Add several instances of this value to the set. Attributes

Invocation template:

```scala
receiver.addMany(t, count)
```

## `` com.cra.figaro.util.MultiSet.addOne ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#addOne-ffffff5d)

```scala
def addOne(t: T): MultiSet[T]
```

Add an instance of this value to the set.

Type parameters: none.

Parameters, list 1: `` t: T ``.

Returns: `` MultiSet[T] ``.

Source contract/attributes: Add an instance of this value to the set. Attributes

Invocation template:

```scala
receiver.addOne(t)
```

## `` com.cra.figaro.util.MultiSet.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#apply-fffffaf4)

```scala
def apply(t: T): Int
```

Return the number of instances of the value in the set.

Type parameters: none.

Parameters, list 1: `` t: T ``.

Returns: `` Int ``.

Source contract/attributes: Return the number of instances of the value in the set. Attributes

Invocation template:

```scala
receiver.apply(t)
```

## `` com.cra.figaro.util.MultiSet.counts ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#counts-0)

```scala
def counts: Set[(T, Int)]
```

Creates a list of (element, count) pairs. Order is arbitrary.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[(T, Int)] ``.

Source contract/attributes: Creates a list of (element, count) pairs. Order is arbitrary. Attributes

Invocation template:

```scala
receiver.counts
```

## `` com.cra.figaro.util.MultiSet.elements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#elements-0)

```scala
def elements: List[T]
```

Creates a list of elements, where each element may appear multiple times. Order is arbitrary.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[T] ``.

Source contract/attributes: Creates a list of elements, where each element may appear multiple times. Order is arbitrary. Attributes

Invocation template:

```scala
receiver.elements
```

## `` com.cra.figaro.util.MultiSet.foreach ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#foreach-58c)

```scala
override def foreach[U](fn: T => U): Unit
```

Iterate over instances in this set and apply the given function.

Type parameters: `` U ``.

Parameters, list 1: `` fn: T => U ``.

Returns: `` Unit ``.

Source contract/attributes: Iterate over instances in this set and apply the given function. Attributes Definition Classes IterableOnceOps

Invocation template:

```scala
receiver.foreach[U](fn)
```

## `` com.cra.figaro.util.MultiSet.removeAll ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#removeAll-ffffff5d)

```scala
def removeAll(t: T): MultiSet[T]
```

Remove all instances of this value from the set.

Type parameters: none.

Parameters, list 1: `` t: T ``.

Returns: `` MultiSet[T] ``.

Source contract/attributes: Remove all instances of this value from the set. Attributes

Invocation template:

```scala
receiver.removeAll(t)
```

## `` com.cra.figaro.util.MultiSet.removeOne ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#removeOne-ffffff5d)

```scala
def removeOne(t: T): MultiSet[T]
```

Remove one instance of this value from the set.

Type parameters: none.

Parameters, list 1: `` t: T ``.

Returns: `` MultiSet[T] ``.

Source contract/attributes: Remove one instance of this value from the set. Attributes

Invocation template:

```scala
receiver.removeOne(t)
```

## `` com.cra.figaro.util.MultiSet.union ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#union-d94)

```scala
def union(that: MultiSet[T]): MultiSet[T]
```

Return the union of another set with this set.

Type parameters: none.

Parameters, list 1: `` that: MultiSet[T] ``.

Returns: `` MultiSet[T] ``.

Source contract/attributes: Return the union of another set with this set. Attributes

Invocation template:

```scala
receiver.union(that)
```

## `` com.cra.figaro.util.MultiSet.equals ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#equals-4ad)

```scala
override def equals(that: Any): Boolean
```

Returns true if the counts are the same.

Type parameters: none.

Parameters, list 1: `` that: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Returns true if the counts are the same. Attributes Definition Classes Any

Invocation template:

```scala
receiver.equals(that)
```

## `` com.cra.figaro.util.MultiSet.hashCode ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#hashCode-0)

```scala
override def hashCode: Int
```

Calculates a hash code value for the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Calculates a hash code value for the object. The default hashing algorithm is platform dependent. Note that it is allowed for two objects to have identical hash codes (o1.hashCode.equals(o2.hashCode)) yet not be equal (o1.equals(o2) returns false). A degenerate implementation could always return 0. However, it is required that if two objects are equal (o1.equals(o2) returns true) that they have identical hash codes (o1.hashCode.equals(o2.hashCode)). Therefore, when overriding this method, be sure to verify that the behavior is consistent with the equals method. Attributes Returns the hash code value for this object. Definition Classes Any

Invocation template:

```scala
receiver.hashCode
```

## `` com.cra.figaro.util.MultiSet.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#map-c8a)

```scala
override def map[U](fn: T => U): MultiSet[U]
```

Map this set to another set by applying the supplied function.

Type parameters: `` U ``.

Parameters, list 1: `` fn: T => U ``.

Returns: `` MultiSet[U] ``.

Source contract/attributes: Map this set to another set by applying the supplied function. Attributes Definition Classes IterableOps -> IterableOnceOps

Invocation template:

```scala
receiver.map[U](fn)
```

## `` com.cra.figaro.util.MultiSet.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#++-1dd)

```scala
final def ++[B >: T](suffix: IterableOnce[B]): Iterable[B]
```

Alias for concat.

Type parameters: `` B >: T ``.

Parameters, list 1: `` suffix: IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Alias for concat. Type parameters B the element type of the returned collection Value parameters suffix the iterable to append to this iterable collection Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.++[B](suffix)
```

## `` com.cra.figaro.util.MultiSet.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#addString-ff4)

```scala
final def addString(b: StringBuilder): b.type
```

Appends all elements of this iterable collection to a string builder. The written text consists of the string representations (w.r.t. the method toString) of all elements of this iterable collection without any separator string.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this iterable collection to a string builder. The written text consists of the string representations (w.r.t. the method toString) of all elements of this iterable collection without any separator string. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() val h = a.addString(b) // 1234 Value parameters b the string builder to which elements are appended. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b)
```

## `` com.cra.figaro.util.MultiSet.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#addString-e99)

```scala
final def addString(b: StringBuilder, sep: String): b.type
```

Appends all elements of this iterable collection to a string builder using a separator string. The written text consists of the string representations (w.r.t. the method toString) of all elements of this iterable collection, separated by the string sep.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``; `` sep: String ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this iterable collection to a string builder using a separator string. The written text consists of the string representations (w.r.t. the method toString) of all elements of this iterable collection, separated by the string sep. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() a.addString(b, ", ") // 1, 2, 3, 4 Value parameters b the string builder to which elements are appended. sep the separator string. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b, sep)
```

## `` com.cra.figaro.util.MultiSet.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#addString-de3)

```scala
def addString(b: StringBuilder, start: String, sep: String, end: String): b.type
```

Appends all elements of this iterable collection to a string builder using start, end, and separator strings. The written text begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this iterable collection are separated by the string sep.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``; `` start: String ``; `` sep: String ``; `` end: String ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this iterable collection to a string builder using start, end, and separator strings. The written text begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this iterable collection are separated by the string sep. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() a.addString(b , "List(" , ", " , ")") // List(1, 2, 3, 4) Value parameters b the string builder to which elements are appended. end the ending string. sep the separator string. start the starting string. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b, start, sep, end)
```

## `` com.cra.figaro.util.MultiSet.collect ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#collect-fffff5b3)

```scala
def collect[B](pf: PartialFunction[T, B]): Iterable[B]
```

Builds a new iterable collection by applying a partial function to all elements of this iterable collection on which the function is defined.

Type parameters: `` B ``.

Parameters, list 1: `` pf: PartialFunction[T, B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Builds a new iterable collection by applying a partial function to all elements of this iterable collection on which the function is defined. Type parameters B the element type of the returned iterable collection. Value parameters pf the partial function which filters and maps the iterable collection. Attributes Returns a new iterable collection resulting from applying the given partial function pf to each element on which it is defined and collecting the results. The order of the elements is preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.collect[B](pf)
```

## `` com.cra.figaro.util.MultiSet.collectFirst ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#collectFirst-25d)

```scala
def collectFirst[B](pf: PartialFunction[T, B]): Option[B]
```

Finds the first element of the iterable collection for which the given partial function is defined, and applies the partial function to it.

Type parameters: `` B ``.

Parameters, list 1: `` pf: PartialFunction[T, B] ``.

Returns: `` Option[B] ``.

Source contract/attributes: Finds the first element of the iterable collection for which the given partial function is defined, and applies the partial function to it. Note: may not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Type parameters B the result type of the partial function Value parameters pf the partial function Attributes Returns an option value containing pf applied to the first value for which it is defined, or None if none exists. Example Seq("a", 1, 5L).collectFirst { case x: Int => x*10 } = Some(10) Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.collectFirst[B](pf)
```

## `` com.cra.figaro.util.MultiSet.concat ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#concat-1dd)

```scala
def concat[B >: T](suffix: IterableOnce[B]): Iterable[B]
```

Returns a new iterable collection containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the iterable collection is the most specific superclass encompassing the element types of the two operands.

Type parameters: `` B >: T ``.

Parameters, list 1: `` suffix: IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Returns a new iterable collection containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the iterable collection is the most specific superclass encompassing the element types of the two operands. Type parameters B the element type of the returned collection. Value parameters suffix the iterable to append. Attributes Returns a new iterable collection which contains all elements of this iterable collection followed by all elements of suffix. Inherited from: IterableOps

Invocation template:

```scala
receiver.concat[B](suffix)
```

## `` com.cra.figaro.util.MultiSet.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#copyToArray-fffffd03)

```scala
def copyToArray[B >: T](dest: Array[B], start: Int, n: Int): Int
```

Copies elements to an array and returns the number of elements written.

Type parameters: `` B >: T ``.

Parameters, list 1: `` dest: Array[B] ``; `` start: Int ``; `` n: Int ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array and returns the number of elements written. Fills the given array dest starting at index start with at most n elements of this iterable collection. Copying will stop once either all the elements of this iterable collection have been copied, or the end of the array is reached, or n elements have been copied. If start is less than zero, it is taken as zero. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. n the maximal number of elements to copy. start the starting index of xs. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest, start, n)
```

## `` com.cra.figaro.util.MultiSet.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#copyToArray-fffffaa4)

```scala
def copyToArray[B >: T](dest: Array[B], start: Int): Int
```

Copies elements to an array, returning the number of elements written.

Type parameters: `` B >: T ``.

Parameters, list 1: `` dest: Array[B] ``; `` start: Int ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array, returning the number of elements written. Fills the given array dest starting at index start with values of this iterable collection. Copying will stop once either all the elements of this iterable collection have been copied, or the end of the array is reached. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. start the starting index of xs. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest, start)
```

## `` com.cra.figaro.util.MultiSet.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#copyToArray-fffff3e3)

```scala
def copyToArray[B >: T](dest: Array[B]): Int
```

Copies elements to an array, returning the number of elements written.

Type parameters: `` B >: T ``.

Parameters, list 1: `` dest: Array[B] ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array, returning the number of elements written. Fills the given array dest starting at index start with values of this iterable collection. Copying will stop once either all the elements of this iterable collection have been copied, or the end of the array is reached. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest)
```

## `` com.cra.figaro.util.MultiSet.corresponds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#corresponds-958)

```scala
def corresponds[B](that: IterableOnce[B])(p: (T, B) => Boolean): Boolean
```

Tests whether every element of this collection's iterator relates to the corresponding element of another collection by satisfying a test predicate.

Type parameters: `` B ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Parameters, list 2: `` p: (T, B) => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether every element of this collection's iterator relates to the corresponding element of another collection by satisfying a test predicate. Note: will not terminate for infinite-sized collections. Type parameters B the type of the elements of that Value parameters p the test predicate, which relates elements from both collections that the other collection Attributes Returns true if both collections have the same length and p(x, y) is true for all corresponding elements x of this iterator and y of that, otherwise false Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.corresponds[B](that)(p)
```

## `` com.cra.figaro.util.MultiSet.count ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#count-db6)

```scala
def count(p: T => Boolean): Int
```

Counts the number of elements in the iterable collection which satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Int ``.

Source contract/attributes: Counts the number of elements in the iterable collection which satisfy a predicate. Note: will not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns the number of elements satisfying the predicate p. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.count(p)
```

## `` com.cra.figaro.util.MultiSet.drop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#drop-aba)

```scala
def drop(n: Int): Iterable[T]
```

Selects all elements except the first n ones.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Selects all elements except the first n ones. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to drop from this iterable collection. Attributes Returns a iterable collection consisting of all elements of this iterable collection except the first n ones, or else the empty iterable collection, if this iterable collection has less than n elements. If n is negative, don't drop any elements. Inherited from: IterableOps

Invocation template:

```scala
receiver.drop(n)
```

## `` com.cra.figaro.util.MultiSet.dropRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#dropRight-aba)

```scala
def dropRight(n: Int): Iterable[T]
```

Selects all elements except last n ones.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Selects all elements except last n ones. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to drop from this iterable collection. Attributes Returns a iterable collection consisting of all elements of this iterable collection except the last n ones, or else the empty iterable collection, if this iterable collection has less than n elements. If n is negative, don't drop any elements. Inherited from: IterableOps

Invocation template:

```scala
receiver.dropRight(n)
```

## `` com.cra.figaro.util.MultiSet.dropWhile ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#dropWhile-c24)

```scala
def dropWhile(p: T => Boolean): Iterable[T]
```

Selects all elements except the longest prefix that satisfies a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Selects all elements except the longest prefix that satisfies a predicate. The matching prefix starts with the first element of this iterable collection, and the element following the prefix is the first element that does not satisfy the predicate. The matching prefix may be empty, so that this method returns the entire iterable collection. Example: List(1, 2, 3, 100, 4).dropWhile(n => n < 10) // List(100, 4) List(1, 2, 3, 100, 4).dropWhile(n => n == 0) // List(1, 2, 3, 100, 4) Use span to obtain both the prefix and suffix. Use filterNot to drop all elements that satisfy the predicate. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p The predicate used to test elements. Attributes Returns the longest suffix of this iterable collection whose first element does not satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.dropWhile(p)
```

## `` com.cra.figaro.util.MultiSet.empty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#empty-0)

```scala
override def empty: Iterable[T]
```

The empty iterable collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: The empty iterable collection. Attributes Returns an empty iterable of type Iterable. Definition Classes IterableFactoryDefaults -> IterableOps Inherited from: IterableFactoryDefaults

Invocation template:

```scala
receiver.empty
```

## `` com.cra.figaro.util.MultiSet.exists ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#exists-fffff66f)

```scala
def exists(p: T => Boolean): Boolean
```

Tests whether a predicate holds for at least one element of this iterable collection.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether a predicate holds for at least one element of this iterable collection. Note: may not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns true if the given predicate p is satisfied by at least one element of this iterable collection, otherwise false Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.exists(p)
```

## `` com.cra.figaro.util.MultiSet.filter ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#filter-c24)

```scala
def filter(pred: T => Boolean): Iterable[T]
```

Selects all elements of this iterable collection which satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` pred: T => Boolean ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Selects all elements of this iterable collection which satisfy a predicate. Value parameters p the predicate used to test elements. Attributes Returns a new iterable collection consisting of all elements of this iterable collection that satisfy the given predicate p. The order of the elements is preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.filter(pred)
```

## `` com.cra.figaro.util.MultiSet.filterNot ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#filterNot-c24)

```scala
def filterNot(pred: T => Boolean): Iterable[T]
```

Selects all elements of this iterable collection which do not satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` pred: T => Boolean ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Selects all elements of this iterable collection which do not satisfy a predicate. Value parameters pred the predicate used to test elements. Attributes Returns a new iterable collection consisting of all elements of this iterable collection that do not satisfy the given predicate pred. Their order may not be preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.filterNot(pred)
```

## `` com.cra.figaro.util.MultiSet.find ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#find-94e)

```scala
def find(p: T => Boolean): Option[T]
```

Finds the first element of the iterable collection satisfying a predicate, if any.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the first element of the iterable collection satisfying a predicate, if any. Note: may not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the predicate used to test elements. Attributes Returns an option value containing the first element in the iterable collection that satisfies p, or None if none exists. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.find(p)
```

## `` com.cra.figaro.util.MultiSet.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#flatMap-5d3)

```scala
def flatMap[B](f: T => IterableOnce[B]): Iterable[B]
```

Builds a new iterable collection by applying a function to all elements of this iterable collection and using the elements of the resulting collections.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Builds a new iterable collection by applying a function to all elements of this iterable collection and using the elements of the resulting collections. For example: def getWords(lines: Seq[String]): Seq[String] = lines.flatMap(line => line.split("\\W+")) The type of the resulting collection is guided by the static type of this iterable collection. This might cause unexpected results sometimes. For example: // lettersOf will return a Seq[Char] of likely repeated letters, instead of a Set def lettersOf(words: Seq[String]) = words.flatMap(word => word.toSet) // lettersOf2 will return a Set[Char], not a Seq def lettersOf2(words: Seq[String]) = words.toSet.flatMap(word => word.toSeq) // xs will be an Iterable[Int] val xs = Map("a" -> List(11, 111), "b" -> List(22, 222)).flatMap(_._2) // ys will be a Map[Int, Int] val ys = Map("a" -> List(1 -> 11, 1 -> 111), "b" -> List(2 -> 22, 2 -> 222)).flatMap(_._2) Type parameters B the element type of the returned collection. Value parameters f the function to apply to each element. Attributes Returns a new iterable collection resulting from applying the given collection-valued function f to each element of this iterable collection and concatenating the results. Inherited from: IterableOps

Invocation template:

```scala
receiver.flatMap[B](f)
```

## `` com.cra.figaro.util.MultiSet.flatten ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#flatten-5d3)

```scala
def flatten[B](implicit asIterable: T => IterableOnce[B]): Iterable[B]
```

Given that the elements of this collection are themselves iterable collections, converts this iterable collection into a iterable collection comprising the elements of these iterable collections.

Type parameters: `` B ``.

Context parameters, list 1: `` asIterable: T => IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Given that the elements of this collection are themselves iterable collections, converts this iterable collection into a iterable collection comprising the elements of these iterable collections. The resulting collection's type will be guided by the type of iterable collection. For example: val xs = List( Set(1, 2, 3), Set(1, 2, 3) ).flatten // xs == List(1, 2, 3, 1, 2, 3) val ys = Set( List(1, 2, 3), List(3, 2, 1) ).flatten // ys == Set(1, 2, 3) Type parameters B the type of the elements of each iterable collection. Value parameters asIterable an implicit conversion which asserts that the element type of this iterable collection is an Iterable. Attributes Returns a new iterable collection resulting from concatenating all element collections. Inherited from: IterableOps

Invocation template:

```scala
receiver.flatten[B](using asIterable)
```

## `` com.cra.figaro.util.MultiSet.fold ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#fold-fffff9e1)

```scala
def fold[A1 >: T](z: A1)(op: (A1, A1) => A1): A1
```

Applies the given binary operator op to the given initial value z and all elements of this iterable collection.

Type parameters: `` A1 >: T ``.

Parameters, list 1: `` z: A1 ``.

Parameters, list 2: `` op: (A1, A1) => A1 ``.

Returns: `` A1 ``.

Source contract/attributes: Applies the given binary operator op to the given initial value z and all elements of this iterable collection. For each application of the operator, each operand is either an element of this iterable collection, the initial value, or another such application of the operator. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. The initial value may be used an arbitrary number of times, but at least once. If this collection is ordered, then for any application of the operator, the element(s) appearing in the left operand will precede those in the right. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. In either case, it is also necessary that the initial value be a neutral value for the operator, e.g. Nil for List concatenation or 1 for multiplication. The default implementation in IterableOnce is equivalent to foldLeft but may be overridden for more efficient traversal orders. Note: will not terminate for infinite-sized collections. Type parameters A1 The type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. z An initial value; may be used an arbitrary number of times in the computation of the result; must be a neutral value for op for the result to always be the same across runs. Attributes Returns The result of applying op between all the elements and z, or z if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.fold[A1](z)(op)
```

## `` com.cra.figaro.util.MultiSet.foldLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#foldLeft-fffff9e1)

```scala
def foldLeft[B](z: B)(op: (B, T) => B): B
```

Applies the given binary operator op to the given initial value z and all elements of this iterable collection, going left to right. Returns the initial value if this iterable collection is empty.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, T) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to the given initial value z and all elements of this iterable collection, going left to right. Returns the initial value if this iterable collection is empty. "Going left to right" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this iterable collection, the result is op( op( ... op( op(z, x,,1,,), x,,2,,) ... ), x,,n,,). If this collection is not ordered, then for each application of the operator, each right operand is an element. In addition, the leftmost operand is the initial value, and each other left operand is itself an application of the operator. The elements of this iterable collection and the initial value all appear exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator. Value parameters op A binary operator. z An initial value. Attributes Returns The result of applying op to z and all elements of this iterable collection, going left to right. Returns z if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foldLeft[B](z)(op)
```

## `` com.cra.figaro.util.MultiSet.foldRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#foldRight-fffff9e1)

```scala
def foldRight[B](z: B)(op: (T, B) => B): B
```

Applies the given binary operator op to all elements of this iterable collection and the given initial value z, going right to left. Returns the initial value if this iterable collection is empty.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (T, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this iterable collection and the given initial value z, going right to left. Returns the initial value if this iterable collection is empty. "Going right to left" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this iterable collection, the result is op(x,,1,,, op(x,,2,,, op( ... op(x,,n,,, z) ... ))). If this collection is not ordered, then for each application of the operator, each left operand is an element. In addition, the rightmost operand is the initial value, and each other right operand is itself an application of the operator. The elements of this iterable collection and the initial value all appear exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator. Value parameters op A binary operator. z An initial value. Attributes Returns The result of applying op to all elements of this iterable collection and z, going right to left. Returns z if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foldRight[B](z)(op)
```

## `` com.cra.figaro.util.MultiSet.forall ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#forall-fffff66f)

```scala
def forall(p: T => Boolean): Boolean
```

Tests whether a predicate holds for all elements of this iterable collection.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether a predicate holds for all elements of this iterable collection. Note: may not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns true if this iterable collection is empty or the given predicate p holds for all elements of this iterable collection, otherwise false. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.forall(p)
```

## `` com.cra.figaro.util.MultiSet.groupBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#groupBy-f68)

```scala
def groupBy[K](f: T => K): Map[K, Iterable[T]]
```

Partitions this iterable collection into a map of iterable collections according to some discriminator function.

Type parameters: `` K ``.

Parameters, list 1: `` f: T => K ``.

Returns: `` Map[K, Iterable[T]] ``.

Source contract/attributes: Partitions this iterable collection into a map of iterable collections according to some discriminator function. Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters K the type of keys returned by the discriminator function. Value parameters f the discriminator function. Attributes Returns A map from keys to iterable collections such that the following invariant holds: (xs groupBy f)(k) = xs filter (x => f(x) == k) That is, every key `k` is bound to a iterable collection of those elements `x` for which `f(x)` equals `k`. Inherited from: IterableOps

Invocation template:

```scala
receiver.groupBy[K](f)
```

## `` com.cra.figaro.util.MultiSet.groupMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#groupMap-fffff03a)

```scala
def groupMap[K, B](key: T => K)(f: T => B): Map[K, Iterable[B]]
```

Partitions this iterable collection into a map of iterable collections according to a discriminator function key. Each element in a group is transformed into a value of type B using the value function.

Type parameters: `` K ``; `` B ``.

Parameters, list 1: `` key: T => K ``.

Parameters, list 2: `` f: T => B ``.

Returns: `` Map[K, Iterable[B]] ``.

Source contract/attributes: Partitions this iterable collection into a map of iterable collections according to a discriminator function key. Each element in a group is transformed into a value of type B using the value function. It is equivalent to groupBy(key).mapValues(_.map(f)), but more efficient. case class User(name: String, age: Int) def namesByAge(users: Seq[User]): Map[Int, Seq[String]] = users.groupMap(_.age)(_.name) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of values returned by the transformation function K the type of keys returned by the discriminator function Value parameters f the element transformation function key the discriminator function Attributes Returns a map associating each key k produced by key with a iterable collection of values produced by applying f to elements that map to k Inherited from: IterableOps

Invocation template:

```scala
receiver.groupMap[K, B](key)(f)
```

## `` com.cra.figaro.util.MultiSet.groupMapReduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#groupMapReduce-8e8)

```scala
def groupMapReduce[K, B](key: T => K)(f: T => B)(reduce: (B, B) => B): Map[K, B]
```

Partitions this iterable collection into a map according to a discriminator function key. All the values that have the same discriminator are then transformed by the f function and then reduced into a single value with the reduce function.

Type parameters: `` K ``; `` B ``.

Parameters, list 1: `` key: T => K ``.

Parameters, list 2: `` f: T => B ``.

Parameters, list 3: `` reduce: (B, B) => B ``.

Returns: `` Map[K, B] ``.

Source contract/attributes: Partitions this iterable collection into a map according to a discriminator function key. All the values that have the same discriminator are then transformed by the f function and then reduced into a single value with the reduce function. It is equivalent to groupBy(key).mapValues(_.map(f).reduce(reduce)), but more efficient. def occurrences[A](as: Seq[A]): Map[A, Int] = as.groupMapReduce(identity)(_ => 1)(_ + _) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of values returned by the transformation function K the type of keys returned by the discriminator function Value parameters f the element transformation function key the discriminator function reduce the reduction function used to combine values mapped to the same key Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.groupMapReduce[K, B](key)(f)(reduce)
```

## `` com.cra.figaro.util.MultiSet.grouped ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#grouped-fffffbef)

```scala
def grouped(size: Int): Iterator[Iterable[T]]
```

Partitions elements in fixed size iterable collections.

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Iterator[Iterable[T]] ``.

Source contract/attributes: Partitions elements in fixed size iterable collections. Value parameters size the number of elements per group Attributes Returns An iterator producing iterable collections of size size, except the last will be less than size size if the elements don't divide evenly. See also scala.collection.Iterator, method grouped Inherited from: IterableOps

Invocation template:

```scala
receiver.grouped(size)
```

## `` com.cra.figaro.util.MultiSet.head ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#head-0)

```scala
def head: T
```

Selects the first element of this iterable collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` T ``.

Source contract/attributes: Selects the first element of this iterable collection. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the first element of this iterable collection. Throws NoSuchElementException if the iterable collection is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.head
```

## `` com.cra.figaro.util.MultiSet.headOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#headOption-0)

```scala
def headOption: Option[T]
```

Optionally selects the first element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Option[T] ``.

Source contract/attributes: Optionally selects the first element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the first element of this iterable collection if it is nonempty, None if it is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.headOption
```

## `` com.cra.figaro.util.MultiSet.init ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#init-0)

```scala
def init: Iterable[T]
```

The initial part of the collection without its last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: The initial part of the collection without its last element. Note: Even when applied to a view or a lazy collection it will always force the elements. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.init
```

## `` com.cra.figaro.util.MultiSet.inits ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#inits-0)

```scala
def inits: Iterator[Iterable[T]]
```

Iterates over the inits of this iterable collection. The first value will be this iterable collection and the final one will be an empty iterable collection, with the intervening values the results of successive applications of init.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Iterable[T]] ``.

Source contract/attributes: Iterates over the inits of this iterable collection. The first value will be this iterable collection and the final one will be an empty iterable collection, with the intervening values the results of successive applications of init. Note: Even when applied to a view or a lazy collection it will always force the elements. Attributes Returns an iterator over all the inits of this iterable collection Example List(1,2,3).inits = Iterator(List(1,2,3), List(1,2), List(1), Nil) Inherited from: IterableOps

Invocation template:

```scala
receiver.inits
```

## `` com.cra.figaro.util.MultiSet.isEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#isEmpty-0)

```scala
def isEmpty: Boolean
```

Tests whether the iterable collection is empty.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether the iterable collection is empty. Note: The default implementation creates and discards an iterator. Note: Implementations in subclasses that are not repeatedly iterable must take care not to consume any elements when isEmpty is called. Attributes Returns true if the iterable collection contains no elements, false otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.isEmpty
```

## `` com.cra.figaro.util.MultiSet.isTraversableAgain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#isTraversableAgain-0)

```scala
override def isTraversableAgain: Boolean
```

Tests whether this iterable collection can be repeatedly traversed. Always true for Iterables and false for Iterators unless overridden.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this iterable collection can be repeatedly traversed. Always true for Iterables and false for Iterators unless overridden. Attributes Returns true if it is repeatedly traversable, false otherwise. Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.isTraversableAgain
```

## `` com.cra.figaro.util.MultiSet.iterableFactory ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#iterableFactory-0)

```scala
def iterableFactory: IterableFactory[Iterable]
```

The companion object of this iterable collection, providing various factory methods.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IterableFactory[Iterable] ``.

Source contract/attributes: The companion object of this iterable collection, providing various factory methods. Attributes Returns the companion factory object for this collection type Note When implementing a custom collection type and refining CC to the new type, this method needs to be overridden to return a factory for the new type (the compiler will issue an error otherwise). Inherited from: Iterable

Invocation template:

```scala
receiver.iterableFactory
```

## `` com.cra.figaro.util.MultiSet.knownSize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#knownSize-0)

```scala
def knownSize: Int
```

The number of elements in this iterable collection, if it can be cheaply computed, -1 otherwise. Cheaply usually means: Not requiring a collection traversal.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: The number of elements in this iterable collection, if it can be cheaply computed, -1 otherwise. Cheaply usually means: Not requiring a collection traversal. Attributes Inherited from: IterableOnce

Invocation template:

```scala
receiver.knownSize
```

## `` com.cra.figaro.util.MultiSet.last ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#last-0)

```scala
def last: T
```

Selects the last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` T ``.

Source contract/attributes: Selects the last element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns The last element of this iterable collection. Throws NoSuchElementException If the iterable collection is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.last
```

## `` com.cra.figaro.util.MultiSet.lastOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#lastOption-0)

```scala
def lastOption: Option[T]
```

Optionally selects the last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Option[T] ``.

Source contract/attributes: Optionally selects the last element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the last element of this iterable collection if it is nonempty, None if it is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.lastOption
```

## `` com.cra.figaro.util.MultiSet.lazyZip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#lazyZip-fffffd84)

```scala
def lazyZip[B](that: Iterable[B]): LazyZip2[T, B, this.type]
```

Analogous to zip except that the elements in each collection are not consumed until a strict operation is invoked on the returned LazyZip2 decorator.

Type parameters: `` B ``.

Parameters, list 1: `` that: Iterable[B] ``.

Returns: `` LazyZip2[T, B, this.type] ``.

Source contract/attributes: Analogous to zip except that the elements in each collection are not consumed until a strict operation is invoked on the returned LazyZip2 decorator. Calls to lazyZip can be chained to support higher arities (up to 4) without incurring the expense of constructing and deconstructing intermediary tuples. val xs = List(1, 2, 3) val res = (xs lazyZip xs lazyZip xs lazyZip xs).map((a, b, c, d) => a + b + c + d) // res == List(4, 8, 12) Type parameters B the type of the second element in each eventual pair Value parameters that the iterable providing the second element of each eventual pair Attributes Returns a decorator LazyZip2 that allows strict operations to be performed on the lazily evaluated pairs or chained calls to lazyZip. Implicit conversion to Iterable[(A, B)] is also supported. Inherited from: Iterable

Invocation template:

```scala
receiver.lazyZip[B](that)
```

## `` com.cra.figaro.util.MultiSet.max ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#max-ffffff34)

```scala
def max[B >: T](implicit ord: Ordering[B]): T
```

Finds the largest element.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` T ``.

Source contract/attributes: Finds the largest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns the largest element of this iterable collection with respect to the ordering ord. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.max[B](using ord)
```

## `` com.cra.figaro.util.MultiSet.maxBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#maxBy-fffff0c5)

```scala
def maxBy[B](f: T => B)(implicit ord: Ordering[B]): T
```

Finds the first element which yields the largest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` T ``.

Source contract/attributes: Finds the first element which yields the largest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns the first element of this iterable collection with the largest value measured by function f with respect to the ordering ord. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxBy[B](f)(using ord)
```

## `` com.cra.figaro.util.MultiSet.maxByOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#maxByOption-fffff66f)

```scala
def maxByOption[B](f: T => B)(implicit ord: Ordering[B]): Option[T]
```

Finds the first element which yields the largest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the first element which yields the largest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns an option value containing the first element of this iterable collection with the largest value measured by function f with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxByOption[B](f)(using ord)
```

## `` com.cra.figaro.util.MultiSet.maxOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#maxOption-fffff45e)

```scala
def maxOption[B >: T](implicit ord: Ordering[B]): Option[T]
```

Finds the largest element.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the largest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns an option value containing the largest element of this iterable collection with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxOption[B](using ord)
```

## `` com.cra.figaro.util.MultiSet.min ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#min-ffffff34)

```scala
def min[B >: T](implicit ord: Ordering[B]): T
```

Finds the smallest element.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` T ``.

Source contract/attributes: Finds the smallest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns the smallest element of this iterable collection with respect to the ordering ord. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.min[B](using ord)
```

## `` com.cra.figaro.util.MultiSet.minBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#minBy-fffff0c5)

```scala
def minBy[B](f: T => B)(implicit ord: Ordering[B]): T
```

Finds the first element which yields the smallest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` T ``.

Source contract/attributes: Finds the first element which yields the smallest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns the first element of this iterable collection with the smallest value measured by function f with respect to the ordering ord. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minBy[B](f)(using ord)
```

## `` com.cra.figaro.util.MultiSet.minByOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#minByOption-fffff66f)

```scala
def minByOption[B](f: T => B)(implicit ord: Ordering[B]): Option[T]
```

Finds the first element which yields the smallest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the first element which yields the smallest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns an option value containing the first element of this iterable collection with the smallest value measured by function f with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minByOption[B](f)(using ord)
```

## `` com.cra.figaro.util.MultiSet.minOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#minOption-fffff45e)

```scala
def minOption[B >: T](implicit ord: Ordering[B]): Option[T]
```

Finds the smallest element.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the smallest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns an option value containing the smallest element of this iterable collection with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minOption[B](using ord)
```

## `` com.cra.figaro.util.MultiSet.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#mkString-0)

```scala
final def mkString: String
```

Displays all elements of this iterable collection in a string.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Displays all elements of this iterable collection in a string. Delegates to addString, which can be overridden. Attributes Returns a string representation of this iterable collection. In the resulting string the string representations (w.r.t. the method toString) of all elements of this iterable collection follow each other without any separator string. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString
```

## `` com.cra.figaro.util.MultiSet.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#mkString-fffff2ca)

```scala
final def mkString(sep: String): String
```

Displays all elements of this iterable collection in a string using a separator string.

Type parameters: none.

Parameters, list 1: `` sep: String ``.

Returns: `` String ``.

Source contract/attributes: Displays all elements of this iterable collection in a string using a separator string. Delegates to addString, which can be overridden. Value parameters sep the separator string. Attributes Returns a string representation of this iterable collection. In the resulting string the string representations (w.r.t. the method toString) of all elements of this iterable collection are separated by the string sep. Example List(1, 2, 3).mkString("|") = "1|2|3" Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString(sep)
```

## `` com.cra.figaro.util.MultiSet.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#mkString-d94)

```scala
final def mkString(start: String, sep: String, end: String): String
```

Displays all elements of this iterable collection in a string using start, end, and separator strings.

Type parameters: none.

Parameters, list 1: `` start: String ``; `` sep: String ``; `` end: String ``.

Returns: `` String ``.

Source contract/attributes: Displays all elements of this iterable collection in a string using start, end, and separator strings. Delegates to addString, which can be overridden. Value parameters end the ending string. sep the separator string. start the starting string. Attributes Returns a string representation of this iterable collection. The resulting string begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this iterable collection are separated by the string sep. Example List(1, 2, 3).mkString("(", "; ", ")") = "(1; 2; 3)" Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString(start, sep, end)
```

## `` com.cra.figaro.util.MultiSet.nonEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#nonEmpty-0)

```scala
def nonEmpty: Boolean
```

Tests whether the iterable collection is not empty.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether the iterable collection is not empty. Attributes Returns true if the iterable collection contains at least one element, false otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.nonEmpty
```

## `` com.cra.figaro.util.MultiSet.partition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#partition-243)

```scala
def partition(p: T => Boolean): (Iterable[T], Iterable[T])
```

A pair of, first, all elements that satisfy predicate p and, second, all elements that do not.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` (Iterable[T], Iterable[T]) ``.

Source contract/attributes: A pair of, first, all elements that satisfy predicate p and, second, all elements that do not. The two iterable collection correspond to the result of filter and filterNot, respectively. The default implementation provided here needs to traverse the collection twice. Strict collections have an overridden version of partition in StrictOptimizedIterableOps, which requires only a single traversal. Value parameters p the predicate used to test elements Attributes Returns a pair of iterable collections: the first containing all elements that satisfy p, the second containing those that do not Inherited from: IterableOps

Invocation template:

```scala
receiver.partition(p)
```

## `` com.cra.figaro.util.MultiSet.partitionMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#partitionMap-2d1)

```scala
def partitionMap[A1, A2](f: T => Either[A1, A2]): (Iterable[A1], Iterable[A2])
```

Applies a function f to each element of the iterable collection and returns a pair of iterable collections: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right.

Type parameters: `` A1 ``; `` A2 ``.

Parameters, list 1: `` f: T => Either[A1, A2] ``.

Returns: `` (Iterable[A1], Iterable[A2]) ``.

Source contract/attributes: Applies a function f to each element of the iterable collection and returns a pair of iterable collections: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right. Example: val xs = $Coll(1, "one", 2, "two", 3, "three") partitionMap { case i: Int => Left(i) case s: String => Right(s) } // xs == ($Coll(1, 2, 3), // $Coll(one, two, three)) Type parameters A1 the element type of the first resulting collection A2 the element type of the second resulting collection Value parameters f the 'split function' mapping the elements of this iterable collection to an scala.util.Either Attributes Returns a pair of iterable collections: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right. Inherited from: IterableOps

Invocation template:

```scala
receiver.partitionMap[A1, A2](f)
```

## `` com.cra.figaro.util.MultiSet.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#product-533)

```scala
def product[B >: T](implicit num: Numeric[B]): B
```

Multiplies together the elements of this collection.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` num: Numeric[B] ``.

Returns: `` B ``.

Source contract/attributes: Multiplies together the elements of this collection. The default implementation uses reduce for a known non-empty collection, foldLeft otherwise. Note: will not terminate for infinite-sized collections. Type parameters B the result type of the * operator. Value parameters num an implicit parameter defining a set of numeric operations which includes the * operator to be used in forming the product. Attributes Returns the product of all elements of this iterable collection with respect to the * operator in num. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.product[B](using num)
```

## `` com.cra.figaro.util.MultiSet.reduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#reduce-fffff3d4)

```scala
def reduce[B >: T](op: (B, B) => B): B
```

Applies the given binary operator op to all elements of this iterable collection.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (B, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this iterable collection. For each application of the operator, each operand is either an element of this iterable collection or another such application of the operator. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. If this collection is ordered, then for any application of the operator, the element(s) appearing in the left operand will precede those in the right. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. Attributes Returns The result of applying op between all the elements if the iterable collection is nonempty. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduce[B](op)
```

## `` com.cra.figaro.util.MultiSet.reduceLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#reduceLeft-fffff3d4)

```scala
def reduceLeft[B >: T](op: (B, T) => B): B
```

Applies the given binary operator op to all elements of this iterable collection, going left to right.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (B, T) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this iterable collection, going left to right. "Going left to right" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this iterable collection, the result is op( op( op( ... op(x,,1,,, x,,2,,) ... ), x,,n-1,,), x,,n,,). If this collection is not ordered, then for each application of the operator, each right operand is an element. In addition, the leftmost operand is the first element of this iterable collection and each other left operand is itself an application of the operator. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of applying op to all elements of this iterable collection, going left to right. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceLeft[B](op)
```

## `` com.cra.figaro.util.MultiSet.reduceLeftOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#reduceLeftOption-8fe)

```scala
def reduceLeftOption[B >: T](op: (B, T) => B): Option[B]
```

If this iterable collection is nonempty, reduces it with the given binary operator op, going left to right.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (B, T) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this iterable collection is nonempty, reduces it with the given binary operator op, going left to right. The behavior is the same as reduceLeft except that the value is None if the iterable collection is empty. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of reducing this iterable collection with op going left to right if the iterable collection is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceLeftOption[B](op)
```

## `` com.cra.figaro.util.MultiSet.reduceOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#reduceOption-8fe)

```scala
def reduceOption[B >: T](op: (B, B) => B): Option[B]
```

If this iterable collection is nonempty, reduces it with the given binary operator op.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (B, B) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this iterable collection is nonempty, reduces it with the given binary operator op. The behavior is the same as reduce except that the value is None if the iterable collection is empty. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B A type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. Attributes Returns The result of reducing this iterable collection with op if the iterable collection is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceOption[B](op)
```

## `` com.cra.figaro.util.MultiSet.reduceRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#reduceRight-fffff3d4)

```scala
def reduceRight[B >: T](op: (T, B) => B): B
```

Applies the given binary operator op to all elements of this iterable collection, going right to left.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (T, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this iterable collection, going right to left. "Going right to left" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this iterable collection, the result is op(x,,1,,, op(x,,2,,, op( ... op(x,,n-1,,, x,,n,,) ... ))). If this collection is not ordered, then for each application of the operator, each left operand is an element. In addition, the rightmost operand is the last element of this iterable collection and each other right operand is itself an application of the operator. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of applying op to all elements of this iterable collection, going right to left. Throws UnsupportedOperationException if this iterable collection is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceRight[B](op)
```

## `` com.cra.figaro.util.MultiSet.reduceRightOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#reduceRightOption-8fe)

```scala
def reduceRightOption[B >: T](op: (T, B) => B): Option[B]
```

If this iterable collection is nonempty, reduces it with the given binary operator op, going right to left.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (T, B) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this iterable collection is nonempty, reduces it with the given binary operator op, going right to left. The behavior is the same as reduceRight except that the value is None if the iterable collection is empty. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of reducing this iterable collection with op going right to left if the iterable collection is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceRightOption[B](op)
```

## `` com.cra.figaro.util.MultiSet.scan ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#scan-fffff9e1)

```scala
def scan[B >: T](z: B)(op: (B, B) => B): Iterable[B]
```

Computes a prefix scan of the elements of the collection.

Type parameters: `` B >: T ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, B) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Computes a prefix scan of the elements of the collection. Note: The neutral element z may be applied more than once. Type parameters B element type of the resulting collection Value parameters op the associative operator for the scan z neutral element for the operator op Attributes Returns a new iterable collection containing the prefix scan of the elements in this iterable collection Inherited from: IterableOps

Invocation template:

```scala
receiver.scan[B](z)(op)
```

## `` com.cra.figaro.util.MultiSet.scanLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#scanLeft-fffff9e1)

```scala
def scanLeft[B](z: B)(op: (B, T) => B): Iterable[B]
```

Produces a iterable collection containing cumulative results of applying the operator going left to right, including the initial value.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, T) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Produces a iterable collection containing cumulative results of applying the operator going left to right, including the initial value. Note: will not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Type parameters B the type of the elements in the resulting collection Value parameters op the binary operator applied to the intermediate result and the element z the initial value Attributes Returns collection with intermediate results Inherited from: IterableOps

Invocation template:

```scala
receiver.scanLeft[B](z)(op)
```

## `` com.cra.figaro.util.MultiSet.scanRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#scanRight-fffff9e1)

```scala
def scanRight[B](z: B)(op: (T, B) => B): Iterable[B]
```

Produces a collection containing cumulative results of applying the operator going right to left. The head of the collection is the last cumulative result.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (T, B) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Produces a collection containing cumulative results of applying the operator going right to left. The head of the collection is the last cumulative result. Note: will not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: Even when applied to a view or a lazy collection it will always force the elements. Example: List(1, 2, 3, 4).scanRight(0)(_ + _) == List(10, 9, 7, 4, 0) Type parameters B the type of the elements in the resulting collection Value parameters op the binary operator applied to the intermediate result and the element z the initial value Attributes Returns collection with intermediate results Inherited from: IterableOps

Invocation template:

```scala
receiver.scanRight[B](z)(op)
```

## `` com.cra.figaro.util.MultiSet.size ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#size-0)

```scala
def size: Int
```

The size of this iterable collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: The size of this iterable collection. Note: will not terminate for infinite-sized collections. Attributes Returns the number of elements in this iterable collection. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.size
```

## `` com.cra.figaro.util.MultiSet.sizeCompare ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#sizeCompare-32d)

```scala
def sizeCompare(that: Iterable[_]): Int
```

Compares the size of this iterable collection to the size of another Iterable.

Type parameters: none.

Parameters, list 1: `` that: Iterable[_] ``.

Returns: `` Int ``.

Source contract/attributes: Compares the size of this iterable collection to the size of another Iterable. Value parameters that the Iterable whose size is compared with this iterable collection's size. Attributes Returns A value x where x < 0 if this.size < that.size x == 0 if this.size == that.size x > 0 if this.size > that.size The method as implemented here does not call size directly; its running time is O(this.size min that.size) instead of O(this.size + that.size). The method should be overridden if computing size is cheap and knownSize returns -1. Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeCompare(that)
```

## `` com.cra.figaro.util.MultiSet.sizeCompare ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#sizeCompare-fffffbe0)

```scala
def sizeCompare(otherSize: Int): Int
```

Compares the size of this iterable collection to a test value.

Type parameters: none.

Parameters, list 1: `` otherSize: Int ``.

Returns: `` Int ``.

Source contract/attributes: Compares the size of this iterable collection to a test value. Value parameters otherSize the test value that gets compared with the size. Attributes Returns A value x where x < 0 if this.size < otherSize x == 0 if this.size == otherSize x > 0 if this.size > otherSize The default implementation does not call size, which may be arbitrarily expensive. It will use knownSize if nonnegative. Otherwise, it will iterate to compute an element count, with complexity O(size min otherSize). This method should be overridden if computing size is cheap and knownSize returns -1. In that case, the custom implementation should just compare size and otherSize directly. See also sizeIs Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeCompare(otherSize)
```

## `` com.cra.figaro.util.MultiSet.sizeIs ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#sizeIs-0)

```scala
final def sizeIs: SizeCompareOps
```

Returns a value class containing operations for comparing the size of this iterable collection to a test value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` SizeCompareOps ``.

Source contract/attributes: Returns a value class containing operations for comparing the size of this iterable collection to a test value. These operations are implemented in terms of `sizeCompare(Int)`, and allow the following more readable usages: this.sizeIs < size // this.sizeCompare(size) < 0 this.sizeIs <= size // this.sizeCompare(size) <= 0 this.sizeIs == size // this.sizeCompare(size) == 0 this.sizeIs != size // this.sizeCompare(size) != 0 this.sizeIs >= size // this.sizeCompare(size) >= 0 this.sizeIs > size // this.sizeCompare(size) > 0 Attributes Returns a wrapper that allows size comparisons using operators such as =, and > Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeIs
```

## `` com.cra.figaro.util.MultiSet.slice ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#slice-573)

```scala
def slice(from: Int, until: Int): Iterable[T]
```

Selects an interval of elements. The returned iterable collection is made up of all elements x which satisfy the invariant:

Type parameters: none.

Parameters, list 1: `` from: Int ``; `` until: Int ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Selects an interval of elements. The returned iterable collection is made up of all elements x which satisfy the invariant: from <= indexOf(x) < until Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters from the lowest index to include from this iterable collection. until the lowest index to EXCLUDE from this iterable collection. Attributes Returns a iterable collection containing the elements greater than or equal to index from extending up to (but not including) index until of this iterable collection. Example List('a', 'b', 'c', 'd', 'e').slice(1, 3) == List('b', 'c') Inherited from: IterableOps

Invocation template:

```scala
receiver.slice(from, until)
```

## `` com.cra.figaro.util.MultiSet.sliding ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#sliding-fffff156)

```scala
def sliding(size: Int, step: Int): Iterator[Iterable[T]]
```

Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped).

Type parameters: none.

Parameters, list 1: `` size: Int ``; `` step: Int ``.

Returns: `` Iterator[Iterable[T]] ``.

Source contract/attributes: Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped). The returned iterator will be empty when called on an empty collection. The last element the iterator produces may be smaller than the window size when the original collection isn't exhausted by the window before it and its last element isn't skipped by the step before it. Value parameters size the number of elements per group step the distance between the first elements of successive groups Attributes Returns An iterator producing iterable collections of size size, except the last element (which may be the only element) will be smaller if there are fewer than size elements remaining to be grouped. See also scala.collection.Iterator, method sliding Example List(1, 2, 3, 4, 5).sliding(2, 2) = Iterator(List(1, 2), List(3, 4), List(5)) List(1, 2, 3, 4, 5, 6).sliding(2, 3) = Iterator(List(1, 2), List(4, 5)) Inherited from: IterableOps

Invocation template:

```scala
receiver.sliding(size, step)
```

## `` com.cra.figaro.util.MultiSet.sliding ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#sliding-fffffbef)

```scala
def sliding(size: Int): Iterator[Iterable[T]]
```

Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped).

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Iterator[Iterable[T]] ``.

Source contract/attributes: Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped). An empty collection returns an empty iterator, and a non-empty collection containing fewer elements than the window size returns an iterator that will produce the original collection as its only element. Value parameters size the number of elements per group Attributes Returns An iterator producing iterable collections of size size, except for a non-empty collection with less than size elements, which returns an iterator that produces the source collection itself as its only element. See also scala.collection.Iterator, method sliding Example List().sliding(2) = empty iterator List(1).sliding(2) = Iterator(List(1)) List(1, 2).sliding(2) = Iterator(List(1, 2)) List(1, 2, 3).sliding(2) = Iterator(List(1, 2), List(2, 3)) Inherited from: IterableOps

Invocation template:

```scala
receiver.sliding(size)
```

## `` com.cra.figaro.util.MultiSet.span ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#span-243)

```scala
def span(p: T => Boolean): (Iterable[T], Iterable[T])
```

Splits this iterable collection into a prefix/suffix pair according to a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` (Iterable[T], Iterable[T]) ``.

Source contract/attributes: Splits this iterable collection into a prefix/suffix pair according to a predicate. Note: c span p is equivalent to (but possibly more efficient than) (c takeWhile p, c dropWhile p), provided the evaluation of the predicate p does not cause any side effects. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the test predicate Attributes Returns a pair consisting of the longest prefix of this iterable collection whose elements all satisfy p, and the rest of this iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.span(p)
```

## `` com.cra.figaro.util.MultiSet.splitAt ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#splitAt-fffffbd9)

```scala
override def splitAt(n: Int): (Iterable[T], Iterable[T])
```

Splits this iterable collection into a prefix/suffix pair at a given position.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` (Iterable[T], Iterable[T]) ``.

Source contract/attributes: Splits this iterable collection into a prefix/suffix pair at a given position. Note: c splitAt n is equivalent to (but possibly more efficient than) (c take n, c drop n). Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the position at which to split. Attributes Returns a pair of iterable collections consisting of the first n elements of this iterable collection, and the other elements. Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.splitAt(n)
```

## `` com.cra.figaro.util.MultiSet.stepper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#stepper-190)

```scala
def stepper[S <: Stepper[_]](implicit shape: StepperShape[T, S]): S
```

Returns a scala.collection.Stepper for the elements of this collection.

Type parameters: `` S <: Stepper[_] ``.

Context parameters, list 1: `` shape: StepperShape[T, S] ``.

Returns: `` S ``.

Source contract/attributes: Returns a scala.collection.Stepper for the elements of this collection. The Stepper enables creating a Java stream to operate on the collection, see scala.jdk.StreamConverters. For collections holding primitive values, the Stepper can be used as an iterator which doesn't box the elements. The implicit scala.collection.StepperShape parameter defines the resulting Stepper type according to the element type of this collection. For collections of Int, Short, Byte or Char, an scala.collection.IntStepper is returned For collections of Double or Float, a scala.collection.DoubleStepper is returned For collections of Long a scala.collection.LongStepper is returned For any other element type, an scala.collection.AnyStepper is returned Note that this method is overridden in subclasses and the return type is refined to S with EfficientSplit, for example scala.collection.IndexedSeqOps.stepper. For Steppers marked with scala.collection.Stepper.EfficientSplit, the converters in scala.jdk.StreamConverters allow creating parallel streams, whereas bare Steppers can be converted only to sequential streams. Type parameters S the type of the returned Stepper, determined by the implicit StepperShape Attributes Inherited from: IterableOnce

Invocation template:

```scala
receiver.stepper[S](using shape)
```

## `` com.cra.figaro.util.MultiSet.sum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#sum-533)

```scala
def sum[B >: T](implicit num: Numeric[B]): B
```

Sums the elements of this collection.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` num: Numeric[B] ``.

Returns: `` B ``.

Source contract/attributes: Sums the elements of this collection. The default implementation uses reduce for a known non-empty collection, foldLeft otherwise. Note: will not terminate for infinite-sized collections. Type parameters B the result type of the + operator. Value parameters num an implicit parameter defining a set of numeric operations which includes the + operator to be used in forming the sum. Attributes Returns the sum of all elements of this iterable collection with respect to the + operator in num. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.sum[B](using num)
```

## `` com.cra.figaro.util.MultiSet.tail ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#tail-0)

```scala
def tail: Iterable[T]
```

The rest of the collection without its first element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: The rest of the collection without its first element. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.tail
```

## `` com.cra.figaro.util.MultiSet.tails ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#tails-0)

```scala
def tails: Iterator[Iterable[T]]
```

Iterates over the tails of this iterable collection. The first value will be this iterable collection and the final one will be an empty iterable collection, with the intervening values the results of successive applications of tail.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Iterable[T]] ``.

Source contract/attributes: Iterates over the tails of this iterable collection. The first value will be this iterable collection and the final one will be an empty iterable collection, with the intervening values the results of successive applications of tail. Attributes Returns an iterator over all the tails of this iterable collection Example List(1,2,3).tails = Iterator(List(1,2,3), List(2,3), List(3), Nil) Inherited from: IterableOps

Invocation template:

```scala
receiver.tails
```

## `` com.cra.figaro.util.MultiSet.take ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#take-aba)

```scala
def take(n: Int): Iterable[T]
```

Selects the first n elements.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Selects the first n elements. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to take from this iterable collection. Attributes Returns a iterable collection consisting only of the first n elements of this iterable collection, or else the whole iterable collection, if it has less than n elements. If n is negative, returns an empty iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.take(n)
```

## `` com.cra.figaro.util.MultiSet.takeRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#takeRight-aba)

```scala
def takeRight(n: Int): Iterable[T]
```

Selects the last n elements.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Selects the last n elements. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to take from this iterable collection. Attributes Returns a iterable collection consisting only of the last n elements of this iterable collection, or else the whole iterable collection, if it has less than n elements. If n is negative, returns an empty iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.takeRight(n)
```

## `` com.cra.figaro.util.MultiSet.takeWhile ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#takeWhile-c24)

```scala
def takeWhile(p: T => Boolean): Iterable[T]
```

Takes longest prefix of elements that satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Takes longest prefix of elements that satisfy a predicate. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p The predicate used to test elements. Attributes Returns the longest prefix of this iterable collection whose elements all satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.takeWhile(p)
```

## `` com.cra.figaro.util.MultiSet.tapEach ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#tapEach-5d3)

```scala
override def tapEach[U](f: T => U): Iterable[T]
```

Applies a side-effecting function to each element in this collection. Strict collections will apply f to their elements immediately, while lazy collections like Views and LazyLists will only apply f on each element if and when that element is evaluated, and each time that element is evaluated.

Type parameters: `` U ``.

Parameters, list 1: `` f: T => U ``.

Returns: `` Iterable[T] ``.

Source contract/attributes: Applies a side-effecting function to each element in this collection. Strict collections will apply f to their elements immediately, while lazy collections like Views and LazyLists will only apply f on each element if and when that element is evaluated, and each time that element is evaluated. Type parameters U the return type of f Value parameters f a function to apply to each element in this iterable collection Attributes Returns The same logical collection as this Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.tapEach[U](f)
```

## `` com.cra.figaro.util.MultiSet.to ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#to-fffff006)

```scala
def to[C1](factory: Factory[T, C1]): C1
```

Given a collection factory factory, converts this iterable collection to the appropriate representation for the current element type A. Example uses:

Type parameters: `` C1 ``.

Parameters, list 1: `` factory: Factory[T, C1] ``.

Returns: `` C1 ``.

Source contract/attributes: Given a collection factory factory, converts this iterable collection to the appropriate representation for the current element type A. Example uses: import scala.collection.mutable.ArrayBuffer import scala.collection.immutable.BitSet val xs: Iterable[Int] = Seq(1, 2, 3, 4, 5) xs.to(List) xs.to(ArrayBuffer) xs.to(BitSet) // for xs: Iterable[Int] Type parameters C1 the target collection type Value parameters factory the factory for the target collection type Attributes Returns a new collection of type C1 containing all elements of this iterable collection Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.to[C1](factory)
```

## `` com.cra.figaro.util.MultiSet.toArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#toArray-fffff5df)

```scala
def toArray[B >: T : ClassTag]: Array[B]
```

Converts this iterable collection to an Array.

Type parameters: `` B >: T : ClassTag ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Array[B] ``.

Source contract/attributes: Converts this iterable collection to an Array. Implementation note: DO NOT call Array.from from this method. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This iterable collection as an Array[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toArray[B]
```

## `` com.cra.figaro.util.MultiSet.toBuffer ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#toBuffer-fffff87f)

```scala
final def toBuffer[B >: T]: Buffer[B]
```

Converts this iterable collection to a Buffer.

Type parameters: `` B >: T ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Buffer[B] ``.

Source contract/attributes: Converts this iterable collection to a Buffer. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This iterable collection as a Buffer[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toBuffer[B]
```

## `` com.cra.figaro.util.MultiSet.toIndexedSeq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#toIndexedSeq-0)

```scala
def toIndexedSeq: IndexedSeq[T]
```

Converts this iterable collection to an IndexedSeq.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IndexedSeq[T] ``.

Source contract/attributes: Converts this iterable collection to an IndexedSeq. Attributes Returns This iterable collection as an IndexedSeq[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toIndexedSeq
```

## `` com.cra.figaro.util.MultiSet.toList ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#toList-0)

```scala
def toList: List[T]
```

Converts this iterable collection to a List.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[T] ``.

Source contract/attributes: Converts this iterable collection to a List. Attributes Returns This iterable collection as a List[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toList
```

## `` com.cra.figaro.util.MultiSet.toMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#toMap-c0c)

```scala
def toMap[K, V](implicit ev: T <:< (K, V)): Map[K, V]
```

Converts this iterable collection to a Map, given an implicit coercion from the iterable collection's type to a key-value tuple.

Type parameters: `` K ``; `` V ``.

Context parameters, list 1: `` ev: T <:< (K, V) ``.

Returns: `` Map[K, V] ``.

Source contract/attributes: Converts this iterable collection to a Map, given an implicit coercion from the iterable collection's type to a key-value tuple. Type parameters K The key type for the resulting map. V The value type for the resulting map. Value parameters ev an implicit evidence that A is a subtype of (K, V) Attributes Returns This iterable collection as a Map[K, V]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toMap[K, V](using ev)
```

## `` com.cra.figaro.util.MultiSet.toSeq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#toSeq-0)

```scala
def toSeq: Seq[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Seq[T] ``.

Source contract/attributes: Attributes Returns This iterable collection as a Seq[A]. This is equivalent to to(Seq) but might be faster. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toSeq
```

## `` com.cra.figaro.util.MultiSet.toSet ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#toSet-fffffe9f)

```scala
def toSet[B >: T]: Set[B]
```

Converts this iterable collection to a Set.

Type parameters: `` B >: T ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[B] ``.

Source contract/attributes: Converts this iterable collection to a Set. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This iterable collection as a Set[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toSet[B]
```

## `` com.cra.figaro.util.MultiSet.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#toString-665)

```scala
override def toString(): String
```

Converts this iterable collection to a string.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Converts this iterable collection to a string. Attributes Returns a string representation of this collection. By default this string consists of the className of this iterable collection, followed by all elements separated by commas and enclosed in parentheses. Definition Classes Iterable -> Any Inherited from: Iterable

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.util.MultiSet.toVector ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#toVector-0)

```scala
def toVector: Vector[T]
```

Converts this iterable collection to a Vector.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Vector[T] ``.

Source contract/attributes: Converts this iterable collection to a Vector. Attributes Returns This iterable collection as a Vector[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toVector
```

## `` com.cra.figaro.util.MultiSet.transpose ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#transpose-5d3)

```scala
def transpose[B](implicit asIterable: T => Iterable[B]): Iterable[Iterable[B]]
```

Transposes this iterable collection of iterable collections into a iterable collection of iterable collections.

Type parameters: `` B ``.

Context parameters, list 1: `` asIterable: T => Iterable[B] ``.

Returns: `` Iterable[Iterable[B]] ``.

Source contract/attributes: Transposes this iterable collection of iterable collections into a iterable collection of iterable collections. The resulting collection's type will be guided by the static type of iterable collection. For example: val xs = List( Set(1, 2, 3), Set(4, 5, 6)).transpose // xs == List( // List(1, 4), // List(2, 5), // List(3, 6)) val ys = Vector( List(1, 2, 3), List(4, 5, 6)).transpose // ys == Vector( // Vector(1, 4), // Vector(2, 5), // Vector(3, 6)) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of the elements of each iterable collection. Value parameters asIterable an implicit conversion which asserts that the element type of this iterable collection is an Iterable. Attributes Returns a two-dimensional iterable collection of iterable collections which has as nth row the nth column of this iterable collection. Throws IllegalArgumentException if all collections in this iterable collection are not of the same size. Inherited from: IterableOps

Invocation template:

```scala
receiver.transpose[B](using asIterable)
```

## `` com.cra.figaro.util.MultiSet.unzip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#unzip-2d1)

```scala
def unzip[A1, A2](implicit asPair: T => (A1, A2)): (Iterable[A1], Iterable[A2])
```

Converts this iterable collection of pairs into two collections of the first and second half of each pair.

Type parameters: `` A1 ``; `` A2 ``.

Context parameters, list 1: `` asPair: T => (A1, A2) ``.

Returns: `` (Iterable[A1], Iterable[A2]) ``.

Source contract/attributes: Converts this iterable collection of pairs into two collections of the first and second half of each pair. val xs = $Coll( (1, "one"), (2, "two"), (3, "three")).unzip // xs == ($Coll(1, 2, 3), // $Coll(one, two, three)) Type parameters A1 the type of the first half of the element pairs A2 the type of the second half of the element pairs Value parameters asPair an implicit conversion which asserts that the element type of this iterable collection is a pair. Attributes Returns a pair of iterable collections, containing the first, respectively second half of each element pair of this iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.unzip[A1, A2](using asPair)
```

## `` com.cra.figaro.util.MultiSet.unzip3 ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#unzip3-a31)

```scala
def unzip3[A1, A2, A3](implicit asTriple: T => (A1, A2, A3)): (Iterable[A1], Iterable[A2], Iterable[A3])
```

Converts this iterable collection of triples into three collections of the first, second, and third element of each triple.

Type parameters: `` A1 ``; `` A2 ``; `` A3 ``.

Context parameters, list 1: `` asTriple: T => (A1, A2, A3) ``.

Returns: `` (Iterable[A1], Iterable[A2], Iterable[A3]) ``.

Source contract/attributes: Converts this iterable collection of triples into three collections of the first, second, and third element of each triple. val xs = $Coll( (1, "one", '1'), (2, "two", '2'), (3, "three", '3')).unzip3 // xs == ($Coll(1, 2, 3), // $Coll(one, two, three), // $Coll(1, 2, 3)) Type parameters A1 the type of the first member of the element triples A2 the type of the second member of the element triples A3 the type of the third member of the element triples Value parameters asTriple an implicit conversion which asserts that the element type of this iterable collection is a triple. Attributes Returns a triple of iterable collections, containing the first, second, respectively third member of each element triple of this iterable collection. Inherited from: IterableOps

Invocation template:

```scala
receiver.unzip3[A1, A2, A3](using asTriple)
```

## `` com.cra.figaro.util.MultiSet.view ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#view-0)

```scala
def view: View[T]
```

A view over the elements of this collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` View[T] ``.

Source contract/attributes: A view over the elements of this collection. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.view
```

## `` com.cra.figaro.util.MultiSet.withFilter ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#withFilter-fffffb75)

```scala
def withFilter(p: T => Boolean): WithFilter[T, Iterable]
```

Creates a non-strict filter of this iterable collection.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` WithFilter[T, Iterable] ``.

Source contract/attributes: Creates a non-strict filter of this iterable collection. Note: the difference between c filter p and c withFilter p is that the former creates a new collection, whereas the latter only restricts the domain of subsequent map, flatMap, foreach, and withFilter operations. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the predicate used to test elements. Attributes Returns an object of class WithFilter, which supports map, flatMap, foreach, and withFilter operations. All these operations apply to those elements of this iterable collection which satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.withFilter(p)
```

## `` com.cra.figaro.util.MultiSet.zip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#zip-1dd)

```scala
def zip[B](that: IterableOnce[B]): Iterable[(T, B)]
```

Returns a iterable collection formed from this iterable collection and another iterable collection by combining corresponding elements in pairs. If one of the two collections is longer than the other, its remaining elements are ignored.

Type parameters: `` B ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Returns: `` Iterable[(T, B)] ``.

Source contract/attributes: Returns a iterable collection formed from this iterable collection and another iterable collection by combining corresponding elements in pairs. If one of the two collections is longer than the other, its remaining elements are ignored. Type parameters B the type of the second half of the returned pairs Value parameters that The iterable providing the second half of each result pair Attributes Returns a new iterable collection containing pairs consisting of corresponding elements of this iterable collection and that. The length of the returned collection is the minimum of the lengths of this iterable collection and that. Inherited from: IterableOps

Invocation template:

```scala
receiver.zip[B](that)
```

## `` com.cra.figaro.util.MultiSet.zipAll ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#zipAll-fffff401)

```scala
def zipAll[A1 >: T, B](that: Iterable[B], thisElem: A1, thatElem: B): Iterable[(A1, B)]
```

Returns a iterable collection formed from this iterable collection and another iterable collection by combining corresponding elements in pairs. If one of the two collections is shorter than the other, placeholder elements are used to extend the shorter collection to the length of the longer.

Type parameters: `` A1 >: T ``; `` B ``.

Parameters, list 1: `` that: Iterable[B] ``; `` thisElem: A1 ``; `` thatElem: B ``.

Returns: `` Iterable[(A1, B)] ``.

Source contract/attributes: Returns a iterable collection formed from this iterable collection and another iterable collection by combining corresponding elements in pairs. If one of the two collections is shorter than the other, placeholder elements are used to extend the shorter collection to the length of the longer. Type parameters A1 the type of the first element in each result pair (supertype of A) B the type of the second element in each result pair Value parameters that the iterable providing the second half of each result pair thatElem the element to be used to fill up the result if that is shorter than this iterable collection. thisElem the element to be used to fill up the result if this iterable collection is shorter than that. Attributes Returns a new iterable collection containing pairs consisting of corresponding elements of this iterable collection and that. The length of the returned collection is the maximum of the lengths of this iterable collection and that. If this iterable collection is shorter than that, thisElem values are used to pad the result. If that is shorter than this iterable collection, thatElem values are used to pad the result. Inherited from: IterableOps

Invocation template:

```scala
receiver.zipAll[A1, B](that, thisElem, thatElem)
```

## `` com.cra.figaro.util.MultiSet.zipWithIndex ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#zipWithIndex-0)

```scala
def zipWithIndex: Iterable[(T, Int)]
```

Zips this iterable collection with its indices.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[(T, Int)] ``.

Source contract/attributes: Zips this iterable collection with its indices. Attributes Returns A new iterable collection containing pairs consisting of all elements of this iterable collection paired with their index. Indices start at 0. Example List("a", "b", "c").zipWithIndex == List(("a", 0), ("b", 1), ("c", 2)) Inherited from: IterableOps

Invocation template:

```scala
receiver.zipWithIndex
```

## `` com.cra.figaro.util.MultiSet.iterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#iterator-0)

```scala
def iterator: Iterator[T]
```

An scala.collection.Iterator over the elements of this iterable collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[T] ``.

Source contract/attributes: An scala.collection.Iterator over the elements of this iterable collection. If an IterableOnce object is in fact an scala.collection.Iterator, this method always returns itself, in its current state, but if it is an scala.collection.Iterable, this method always returns a new scala.collection.Iterator. Attributes Returns an iterator over all elements of this iterable collection Inherited from: IterableOnce

Invocation template:

```scala
receiver.iterator
```

## `` com.cra.figaro.util.MultiSet.++: ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#++:-1dd)

```scala
def ++:[B >: T](that: IterableOnce[B]): Iterable[B]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B >: T ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use ++ instead of ++: for collections of type Iterable Inherited from: IterableOps

Invocation template:

```scala
receiver.++:[B](that)
```

## `` com.cra.figaro.util.MultiSet./: ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#/:-fffff9e1)

```scala
final def /:[B](z: B)(op: (B, T) => B): B
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, T) => B ``.

Returns: `` B ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use foldLeft instead of /: Inherited from: IterableOnceOps

Invocation template:

```scala
receiver./:[B](z)(op)
```

## `` com.cra.figaro.util.MultiSet.:\ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#:\-fffff9e1)

```scala
final def :\[B](z: B)(op: (T, B) => B): B
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (T, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use foldRight instead of :\\ Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.:\[B](z)(op)
```

## `` com.cra.figaro.util.MultiSet.aggregate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#aggregate-952)

```scala
def aggregate[B](z: => B)(seqop: (B, T) => B, combop: (B, B) => B): B
```

Aggregates the results of applying an operator to subsequent elements.

Type parameters: `` B ``.

Parameters, list 1: `` z: => B ``.

Parameters, list 2: `` seqop: (B, T) => B ``; `` combop: (B, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Aggregates the results of applying an operator to subsequent elements. Since this method degenerates to foldLeft for sequential (non-parallel) collections, where the combining operation is ignored, it is advisable to prefer foldLeft for that case. For parallel collections, use the aggregate method specified by scala.collection.parallel.ParIterableLike. Type parameters B the result type, produced by seqop, combop, and by this function as a final result. Value parameters combop an associative operator for combining sequential results, unused for sequential collections. seqop the binary operator used to accumulate the result. z the start value, a neutral element for seqop. Attributes Deprecated [Since version 2.13.0] For sequential collections, prefer `foldLeft(z)(seqop)`. For parallel collections, use `ParIterableLike#aggregate`. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.aggregate[B](z)(seqop, combop)
```

## `` com.cra.figaro.util.MultiSet.companion ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#companion-0)

```scala
def companion: IterableFactory[Iterable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IterableFactory[Iterable] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use iterableFactory instead Inherited from: IterableOps

Invocation template:

```scala
receiver.companion
```

## `` com.cra.figaro.util.MultiSet.copyToBuffer ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#copyToBuffer-f0b)

```scala
final def copyToBuffer[B >: T](dest: Buffer[B]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B >: T ``.

Parameters, list 1: `` dest: Buffer[B] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use `dest ++= coll` instead Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToBuffer[B](dest)
```

## `` com.cra.figaro.util.MultiSet.hasDefiniteSize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#hasDefiniteSize-0)

```scala
def hasDefiniteSize: Boolean
```

Tests whether this iterable collection is known to have a finite size. All strict collections are known to have finite size. For a non-strict collection such as Stream, the predicate returns **true** if all elements have been computed. It returns **false** if the stream is not yet evaluated to the end. Non-empty Iterators usually return **false** even if they were created from a collection with a known finite size.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this iterable collection is known to have a finite size. All strict collections are known to have finite size. For a non-strict collection such as Stream, the predicate returns **true** if all elements have been computed. It returns **false** if the stream is not yet evaluated to the end. Non-empty Iterators usually return **false** even if they were created from a collection with a known finite size. Note: many collection methods will not work on collections of infinite sizes. The typical failure mode is an infinite loop. These methods always attempt a traversal without checking first that hasDefiniteSize returns **true**. However, checking hasDefiniteSize can provide an assurance that size is well-defined and non-termination is not a concern. Attributes Returns **true** if this collection is known to have finite size, **false** otherwise. See also method knownSize for a more useful alternative Deprecated [Since version 2.13.0] Check .knownSize instead of .hasDefiniteSize for more actionable information (see scaladoc for details) This method is deprecated in 2.13 because it does not provide any actionable information. As noted above, even the collection library itself does not use it. When there is no guarantee that a collection is finite, it is generally best to attempt a computation anyway and document that it will not terminate for infinite collections rather than backing out because this would prevent performing the computation on collections that are in fact finite even though hasDefiniteSize returns false. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.hasDefiniteSize
```

## `` com.cra.figaro.util.MultiSet.repr ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#repr-0)

```scala
final def repr: Iterable[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use coll instead of repr in a collection implementation, use the collection value itself from the outside Inherited from: IterableOps

Invocation template:

```scala
receiver.repr
```

## `` com.cra.figaro.util.MultiSet.seq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#seq-0)

```scala
def seq: this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` this.type ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Iterable.seq always returns the iterable itself Inherited from: Iterable

Invocation template:

```scala
receiver.seq
```

## `` com.cra.figaro.util.MultiSet.toIterable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#toIterable-0)

```scala
final def toIterable: this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` this.type ``.

Source contract/attributes: Attributes Returns This collection as an Iterable[A]. No new collection will be built if this is already an Iterable[A]. Deprecated [Since version 2.13.7] toIterable is internal and will be made protected; its name is similar to `toList` or `toSeq`, but it doesn\'t copy non-immutable collections Inherited from: Iterable

Invocation template:

```scala
receiver.toIterable
```

## `` com.cra.figaro.util.MultiSet.toIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#toIterator-0)

```scala
final def toIterator: Iterator[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use .iterator instead of .toIterator Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toIterator
```

## `` com.cra.figaro.util.MultiSet.toStream ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#toStream-0)

```scala
final def toStream: Stream[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Stream[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use .to(LazyList) instead of .toStream Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toStream
```

## `` com.cra.figaro.util.MultiSet.toTraversable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#toTraversable-0)

```scala
final def toTraversable: Iterable[T]
```

Converts this iterable collection to an unspecified Iterable. Will return the same collection if this instance is already Iterable.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: Converts this iterable collection to an unspecified Iterable. Will return the same collection if this instance is already Iterable. Attributes Returns An Iterable containing all elements of this iterable collection. Deprecated [Since version 2.13.0] toTraversable is internal and will be made protected; its name is similar to `toList` or `toSeq`, but it doesn\'t copy non-immutable collections Inherited from: IterableOps

Invocation template:

```scala
receiver.toTraversable
```

## `` com.cra.figaro.util.MultiSet.view ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/MultiSet.html#view-fffff78d)

```scala
def view(from: Int, until: Int): View[T]
```

A view over a slice of the elements of this collection.

Type parameters: none.

Parameters, list 1: `` from: Int ``; `` until: Int ``.

Returns: `` View[T] ``.

Source contract/attributes: A view over a slice of the elements of this collection. Attributes Deprecated [Since version 2.13.0] Use .view.slice(from, until) instead of .view(from, until) Inherited from: IterableOps

Invocation template:

```scala
receiver.view(from, until)
```

## `` com.cra.figaro.util.`package`.NegativeProbabilityException.addSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/NegativeProbabilityException.html#addSuppressed-4b8)

```scala
final def addSuppressed(x$0: Throwable): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.addSuppressed(x$0)
```

## `` com.cra.figaro.util.`package`.NegativeProbabilityException.fillInStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/NegativeProbabilityException.html#fillInStackTrace-26c)

```scala
def fillInStackTrace(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.fillInStackTrace()
```

## `` com.cra.figaro.util.`package`.NegativeProbabilityException.getCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/NegativeProbabilityException.html#getCause-26c)

```scala
def getCause(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getCause()
```

## `` com.cra.figaro.util.`package`.NegativeProbabilityException.getLocalizedMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/NegativeProbabilityException.html#getLocalizedMessage-665)

```scala
def getLocalizedMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getLocalizedMessage()
```

## `` com.cra.figaro.util.`package`.NegativeProbabilityException.getMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/NegativeProbabilityException.html#getMessage-665)

```scala
def getMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getMessage()
```

## `` com.cra.figaro.util.`package`.NegativeProbabilityException.getStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/NegativeProbabilityException.html#getStackTrace-fffffa6d)

```scala
def getStackTrace(): Array[StackTraceElement]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[StackTraceElement] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getStackTrace()
```

## `` com.cra.figaro.util.`package`.NegativeProbabilityException.getSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/NegativeProbabilityException.html#getSuppressed-fffff2ce)

```scala
final def getSuppressed(): Array[Throwable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[Throwable] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getSuppressed()
```

## `` com.cra.figaro.util.`package`.NegativeProbabilityException.initCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/NegativeProbabilityException.html#initCause-fffff080)

```scala
def initCause(x$0: Throwable): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.initCause(x$0)
```

## `` com.cra.figaro.util.`package`.NegativeProbabilityException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/NegativeProbabilityException.html#printStackTrace-fffff230)

```scala
def printStackTrace(x$0: PrintWriter): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintWriter ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.util.`package`.NegativeProbabilityException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/NegativeProbabilityException.html#printStackTrace-fffff0fd)

```scala
def printStackTrace(x$0: PrintStream): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintStream ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.util.`package`.NegativeProbabilityException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/NegativeProbabilityException.html#printStackTrace-94c)

```scala
def printStackTrace(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace()
```

## `` com.cra.figaro.util.`package`.NegativeProbabilityException.setStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/NegativeProbabilityException.html#setStackTrace-f79)

```scala
def setStackTrace(x$0: Array[StackTraceElement]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Array[StackTraceElement] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.setStackTrace(x$0)
```

## `` com.cra.figaro.util.`package`.NegativeProbabilityException.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/NegativeProbabilityException.html#toString-665)

```scala
def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Inherited from: Throwable

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.util.OnlineLogStatistics.record ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/OnlineLogStatistics.html#record-fffff7a5)

```scala
def record(logWeight: Double): Unit
```

Record the weight in the rolling mean and variance computation.

Type parameters: none.

Parameters, list 1: `` logWeight: Double ``.

Returns: `` Unit ``.

Source contract/attributes: Record the weight in the rolling mean and variance computation. Value parameters logWeight Log of the weight to record. Attributes

Invocation template:

```scala
receiver.record(logWeight)
```

## `` com.cra.figaro.util.OnlineLogStatistics.totalLogStatistics ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/OnlineLogStatistics.html#totalLogStatistics-0)

```scala
def totalLogStatistics: LogStatistics
```

Return the combined statistics for the log probability of evidence over all runs of this sampler. If the number of observations is 0, the returned log mean is -Infinity. If the number of observations is 0 or 1, the returned log variance is NaN.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` LogStatistics ``.

Source contract/attributes: Return the combined statistics for the log probability of evidence over all runs of this sampler. If the number of observations is 0, the returned log mean is -Infinity. If the number of observations is 0 or 1, the returned log variance is NaN. Attributes

Invocation template:

```scala
receiver.totalLogStatistics
```

## `` com.cra.figaro.util.PriorityMap.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap$.html#apply-982)

```scala
def apply[T, U](pairs: (T, U)*)(implicit ord: Ordering[U]): PriorityMap[T, U]
```

Create a priority map containing the given key/value pairs. Uses the heap priority map implementation.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` pairs: (T, U)* ``.

Context parameters, list 2: `` ord: Ordering[U] ``.

Returns: `` PriorityMap[T, U] ``.

Source contract/attributes: Create a priority map containing the given key/value pairs. Uses the heap priority map implementation. Attributes

Invocation template:

```scala
com.cra.figaro.util.PriorityMap.apply[T, U](pairs*)(using ord)
```

## `` com.cra.figaro.util.PriorityMap.extractMin ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#extractMin-ffffff32)

```scala
def extractMin(): (T, U)
```

Extract and return the item with the minimum value.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (T, U) ``.

Source contract/attributes: Extract and return the item with the minimum value. Attributes

Invocation template:

```scala
receiver.extractMin()
```

## `` com.cra.figaro.util.PriorityMap.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#++-1dd)

```scala
final def ++[B >: (T, U)](suffix: IterableOnce[B]): Iterable[B]
```

Alias for concat.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` suffix: IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Alias for concat. Type parameters B the element type of the returned collection Value parameters suffix the iterable to append to this mutable map Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.++[B](suffix)
```

## `` com.cra.figaro.util.PriorityMap.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#++-62)

```scala
def ++[V2 >: U](xs: IterableOnce[(T, V2)]): Map[T, V2]
```

Alias for concat.

Type parameters: `` V2 >: U ``.

Parameters, list 1: `` xs: IterableOnce[(T, V2)] ``.

Returns: `` Map[T, V2] ``.

Source contract/attributes: Alias for concat. Type parameters V2 the value type of the returned map, a supertype of V Value parameters xs the key-value pairs to append Attributes Inherited from: MapOps

Invocation template:

```scala
receiver.++[V2](xs)
```

## `` com.cra.figaro.util.PriorityMap.++= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#++=-fffffb02)

```scala
final def ++=(elems: IterableOnce[(T, U)]): this.type
```

Alias for addAll.

Type parameters: none.

Parameters, list 1: `` elems: IterableOnce[(T, U)] ``.

Returns: `` this.type ``.

Source contract/attributes: Alias for addAll. Attributes Inherited from: Growable

Invocation template:

```scala
receiver.++=(elems)
```

## `` com.cra.figaro.util.PriorityMap.+= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#+=-b4a)

```scala
final def +=(elem: (T, U)): this.type
```

Alias for addOne.

Type parameters: none.

Parameters, list 1: `` elem: (T, U) ``.

Returns: `` this.type ``.

Source contract/attributes: Alias for addOne. Attributes Inherited from: Growable

Invocation template:

```scala
receiver.+=(elem)
```

## `` com.cra.figaro.util.PriorityMap.--= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#--=-fffff8b8)

```scala
final def --=(xs: IterableOnce[T]): this.type
```

Alias for subtractAll.

Type parameters: none.

Parameters, list 1: `` xs: IterableOnce[T] ``.

Returns: `` this.type ``.

Source contract/attributes: Alias for subtractAll. Attributes Inherited from: Shrinkable

Invocation template:

```scala
receiver.--=(xs)
```

## `` com.cra.figaro.util.PriorityMap.-= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#-=-fffff700)

```scala
final def -=(elem: T): this.type
```

Alias for subtractOne.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` this.type ``.

Source contract/attributes: Alias for subtractOne. Attributes Inherited from: Shrinkable

Invocation template:

```scala
receiver.-=(elem)
```

## `` com.cra.figaro.util.PriorityMap.addAll ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#addAll-fffffb02)

```scala
def addAll(elems: IterableOnce[(T, U)]): this.type
```

Add s all elements produced by an IterableOnce to this mutable map.

Type parameters: none.

Parameters, list 1: `` elems: IterableOnce[(T, U)] ``.

Returns: `` this.type ``.

Source contract/attributes: Add s all elements produced by an IterableOnce to this mutable map. Value parameters elems the IterableOnce producing the elements to add. Attributes Returns the mutable map itself. Inherited from: Growable

Invocation template:

```scala
receiver.addAll(elems)
```

## `` com.cra.figaro.util.PriorityMap.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#addString-ff4)

```scala
final def addString(b: StringBuilder): b.type
```

Appends all elements of this mutable map to a string builder. The written text consists of the string representations (w.r.t. the method toString) of all elements of this mutable map without any separator string.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this mutable map to a string builder. The written text consists of the string representations (w.r.t. the method toString) of all elements of this mutable map without any separator string. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() val h = a.addString(b) // 1234 Value parameters b the string builder to which elements are appended. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b)
```

## `` com.cra.figaro.util.PriorityMap.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#addString-e99)

```scala
final def addString(b: StringBuilder, sep: String): b.type
```

Appends all elements of this mutable map to a string builder using a separator string. The written text consists of the string representations (w.r.t. the method toString) of all elements of this mutable map, separated by the string sep.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``; `` sep: String ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this mutable map to a string builder using a separator string. The written text consists of the string representations (w.r.t. the method toString) of all elements of this mutable map, separated by the string sep. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() a.addString(b, ", ") // 1, 2, 3, 4 Value parameters b the string builder to which elements are appended. sep the separator string. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b, sep)
```

## `` com.cra.figaro.util.PriorityMap.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#addString-de3)

```scala
override def addString(sb: StringBuilder, start: String, sep: String, end: String): sb.type
```

Appends all elements of this mutable map to a string builder using start, end, and separator strings. The written text begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this mutable map are separated by the string sep.

Type parameters: none.

Parameters, list 1: `` sb: StringBuilder ``; `` start: String ``; `` sep: String ``; `` end: String ``.

Returns: `` sb.type ``.

Source contract/attributes: Appends all elements of this mutable map to a string builder using start, end, and separator strings. The written text begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this mutable map are separated by the string sep. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() a.addString(b , "List(" , ", " , ")") // List(1, 2, 3, 4) Value parameters b the string builder to which elements are appended. end the ending string. sep the separator string. start the starting string. Attributes Returns the string builder b to which elements were appended. Definition Classes MapOps -> IterableOnceOps Inherited from: MapOps

Invocation template:

```scala
receiver.addString(sb, start, sep, end)
```

## `` com.cra.figaro.util.PriorityMap.andThen ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#andThen-b91)

```scala
def andThen[C](k: PartialFunction[U, C]): PartialFunction[T, C]
```

Composes this partial function with another partial function that gets applied to results of this partial function.

Type parameters: `` C ``.

Parameters, list 1: `` k: PartialFunction[U, C] ``.

Returns: `` PartialFunction[T, C] ``.

Source contract/attributes: Composes this partial function with another partial function that gets applied to results of this partial function. Note that calling isDefinedAt on the resulting partial function may apply the first partial function and execute its side effect. For efficiency, it is recommended to call applyOrElse instead of isDefinedAt or apply. Type parameters C the result type of the transformation function. Value parameters k the transformation function Attributes Returns a partial function with the domain of this partial function narrowed by other partial function, which maps arguments x to k(this(x)). Inherited from: PartialFunction

Invocation template:

```scala
receiver.andThen[C](k)
```

## `` com.cra.figaro.util.PriorityMap.andThen ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#andThen-f71)

```scala
override def andThen[C](k: U => C): PartialFunction[T, C]
```

Composes this partial function with a transformation function that gets applied to results of this partial function.

Type parameters: `` C ``.

Parameters, list 1: `` k: U => C ``.

Returns: `` PartialFunction[T, C] ``.

Source contract/attributes: Composes this partial function with a transformation function that gets applied to results of this partial function. If the runtime type of the function is a PartialFunction then the other andThen method is used (note its cautions). Type parameters C the result type of the transformation function. Value parameters k the transformation function Attributes Returns a partial function with the domain of this partial function, possibly narrowed by the specified function, which maps arguments x to k(this(x)). Definition Classes PartialFunction -> Function1 Inherited from: PartialFunction

Invocation template:

```scala
receiver.andThen[C](k)
```

## `` com.cra.figaro.util.PriorityMap.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#apply-d26)

```scala
def apply(key: T): U
```

Retrieves the value which is associated with the given key. This method invokes the default method of the map if there is no mapping from the given key to a value. Unless overridden, the default method throws a NoSuchElementException.

Type parameters: none.

Parameters, list 1: `` key: T ``.

Returns: `` U ``.

Source contract/attributes: Retrieves the value which is associated with the given key. This method invokes the default method of the map if there is no mapping from the given key to a value. Unless overridden, the default method throws a NoSuchElementException. Value parameters key the key Attributes Returns the value associated with the given key, or the result of the map's default method, if none exists. Inherited from: MapOps

Invocation template:

```scala
receiver.apply(key)
```

## `` com.cra.figaro.util.PriorityMap.applyOrElse ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#applyOrElse-fffffdbf)

```scala
override def applyOrElse[K1 <: T, V1 >: U](x: K1, default: K1 => V1): V1
```

Applies this partial function to the given argument when it is contained in the function domain. Applies fallback function where this partial function is not defined.

Type parameters: `` K1 <: T ``; `` V1 >: U ``.

Parameters, list 1: `` x: K1 ``; `` default: K1 => V1 ``.

Returns: `` V1 ``.

Source contract/attributes: Applies this partial function to the given argument when it is contained in the function domain. Applies fallback function where this partial function is not defined. Note that expression pf.applyOrElse(x, default) is equivalent to if(pf isDefinedAt x) pf(x) else default(x) except that applyOrElse method can be implemented more efficiently. For all partial function literals the compiler generates an applyOrElse implementation which avoids double evaluation of pattern matchers and guards. This makes applyOrElse the basis for the efficient implementation for many operations and scenarios, such as: combining partial functions into orElse/andThen chains does not lead to excessive apply/isDefinedAt evaluation lift and unlift do not evaluate source functions twice on each invocation runWith allows efficient imperative-style combining of partial functions with conditionally applied actions For non-literal partial function classes with nontrivial isDefinedAt method it is recommended to override applyOrElse with custom implementation that avoids double isDefinedAt evaluation. This may result in better performance and more predictable behavior w.r.t. side effects. @param x the function argument @param default the fallback function @return the result of this function or fallback function application. Attributes Definition Classes MapOps -> PartialFunction Inherited from: MapOps

Invocation template:

```scala
receiver.applyOrElse[K1, V1](x, default)
```

## `` com.cra.figaro.util.PriorityMap.canEqual ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#canEqual-4ad)

```scala
def canEqual(that: Any): Boolean
```

Checks whether this instance can possibly equal that.

Type parameters: none.

Parameters, list 1: `` that: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Checks whether this instance can possibly equal that. A method that should be called from every well-designed equals method that is open to be overridden in a subclass. See Programming in Scala, Chapter 28 for discussion and design. Value parameters that the value being probed for possible equality Attributes Returns true if this instance can possibly equal that, otherwise false Inherited from: Map

Invocation template:

```scala
receiver.canEqual(that)
```

## `` com.cra.figaro.util.PriorityMap.clear ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#clear-94c)

```scala
def clear(): Unit
```

Clears the contents of this builder. After execution of this method the builder will contain no elements.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clears the contents of this builder. After execution of this method the builder will contain no elements. Attributes Inherited from: MapOps

Invocation template:

```scala
receiver.clear()
```

## `` com.cra.figaro.util.PriorityMap.clone ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#clone-fffffe46)

```scala
override def clone(): Map[T, U]
```

Create a copy of the receiver object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[T, U] ``.

Source contract/attributes: Create a copy of the receiver object. The default implementation of the clone method is platform dependent. Attributes Returns a copy of the receiver object. Note not specified by SLS as a member of AnyRef Definition Classes MapOps -> Cloneable -> Object Inherited from: MapOps

Invocation template:

```scala
receiver.clone()
```

## `` com.cra.figaro.util.PriorityMap.collect ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#collect-fffff5b3)

```scala
def collect[B](pf: PartialFunction[(T, U), B]): Iterable[B]
```

Builds a new mutable map by applying a partial function to all elements of this mutable map on which the function is defined.

Type parameters: `` B ``.

Parameters, list 1: `` pf: PartialFunction[(T, U), B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Builds a new mutable map by applying a partial function to all elements of this mutable map on which the function is defined. Type parameters B the element type of the returned mutable map. Value parameters pf the partial function which filters and maps the mutable map. Attributes Returns a new mutable map resulting from applying the given partial function pf to each element on which it is defined and collecting the results. The order of the elements is preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.collect[B](pf)
```

## `` com.cra.figaro.util.PriorityMap.collect ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#collect-fffff957)

```scala
def collect[K2, V2](pf: PartialFunction[(T, U), (K2, V2)]): Map[K2, V2]
```

Builds a new collection by applying a partial function to all elements of this mutable map on which the function is defined.

Type parameters: `` K2 ``; `` V2 ``.

Parameters, list 1: `` pf: PartialFunction[(T, U), (K2, V2)] ``.

Returns: `` Map[K2, V2] ``.

Source contract/attributes: Builds a new collection by applying a partial function to all elements of this mutable map on which the function is defined. Type parameters K2 the key type of the returned mutable map. V2 the value type of the returned mutable map. Value parameters pf the partial function which filters and maps the mutable map. Attributes Returns a new mutable map resulting from applying the given partial function pf to each element on which it is defined and collecting the results. The order of the elements is preserved. Inherited from: MapOps

Invocation template:

```scala
receiver.collect[K2, V2](pf)
```

## `` com.cra.figaro.util.PriorityMap.collectFirst ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#collectFirst-25d)

```scala
def collectFirst[B](pf: PartialFunction[(T, U), B]): Option[B]
```

Finds the first element of the mutable map for which the given partial function is defined, and applies the partial function to it.

Type parameters: `` B ``.

Parameters, list 1: `` pf: PartialFunction[(T, U), B] ``.

Returns: `` Option[B] ``.

Source contract/attributes: Finds the first element of the mutable map for which the given partial function is defined, and applies the partial function to it. Note: may not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Type parameters B the result type of the partial function Value parameters pf the partial function Attributes Returns an option value containing pf applied to the first value for which it is defined, or None if none exists. Example Seq("a", 1, 5L).collectFirst { case x: Int => x*10 } = Some(10) Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.collectFirst[B](pf)
```

## `` com.cra.figaro.util.PriorityMap.compose ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#compose-b91)

```scala
def compose[R](k: PartialFunction[R, T]): PartialFunction[R, U]
```

Composes another partial function k with this partial function so that this partial function gets applied to results of k.

Type parameters: `` R ``.

Parameters, list 1: `` k: PartialFunction[R, T] ``.

Returns: `` PartialFunction[R, U] ``.

Source contract/attributes: Composes another partial function k with this partial function so that this partial function gets applied to results of k. Note that calling isDefinedAt on the resulting partial function may apply the first partial function and execute its side effect. For efficiency, it is recommended to call applyOrElse instead of isDefinedAt or apply. Type parameters R the parameter type of the transformation function. Value parameters k the transformation function Attributes Returns a partial function with the domain of other partial function narrowed by this partial function, which maps arguments x to this(k(x)). Inherited from: PartialFunction

Invocation template:

```scala
receiver.compose[R](k)
```

## `` com.cra.figaro.util.PriorityMap.compose ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#compose-51)

```scala
def compose[A](g: A => T): A => U
```

Composes two instances of Function1 in a new Function1, with this function applied last.

Type parameters: `` A ``.

Parameters, list 1: `` g: A => T ``.

Returns: `` A => U ``.

Source contract/attributes: Composes two instances of Function1 in a new Function1, with this function applied last. Type parameters A the type to which function g can be applied Value parameters g a function A => T1 Attributes Returns a new function f such that f(x) == apply(g(x)) Inherited from: Function1

Invocation template:

```scala
receiver.compose[A](g)
```

## `` com.cra.figaro.util.PriorityMap.concat ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#concat-1dd)

```scala
def concat[B >: (T, U)](suffix: IterableOnce[B]): Iterable[B]
```

Returns a new mutable map containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the mutable map is the most specific superclass encompassing the element types of the two operands.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` suffix: IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Returns a new mutable map containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the mutable map is the most specific superclass encompassing the element types of the two operands. Type parameters B the element type of the returned collection. Value parameters suffix the iterable to append. Attributes Returns a new mutable map which contains all elements of this mutable map followed by all elements of suffix. Inherited from: IterableOps

Invocation template:

```scala
receiver.concat[B](suffix)
```

## `` com.cra.figaro.util.PriorityMap.concat ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#concat-62)

```scala
def concat[V2 >: U](suffix: IterableOnce[(T, V2)]): Map[T, V2]
```

Returns a new mutable map containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the mutable map is the most specific superclass encompassing the element types of the two operands.

Type parameters: `` V2 >: U ``.

Parameters, list 1: `` suffix: IterableOnce[(T, V2)] ``.

Returns: `` Map[T, V2] ``.

Source contract/attributes: Returns a new mutable map containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the mutable map is the most specific superclass encompassing the element types of the two operands. Type parameters V2 the value type of the returned map, a supertype of V Value parameters suffix the iterable to append. Attributes Returns a new mutable map which contains all elements of this mutable map followed by all elements of suffix. Inherited from: MapOps

Invocation template:

```scala
receiver.concat[V2](suffix)
```

## `` com.cra.figaro.util.PriorityMap.contains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#contains-4ad)

```scala
def contains(key: T): Boolean
```

Tests whether this map contains a binding for a key.

Type parameters: none.

Parameters, list 1: `` key: T ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this map contains a binding for a key. Value parameters key the key Attributes Returns true if there is a binding for key in this map, false otherwise. Inherited from: MapOps

Invocation template:

```scala
receiver.contains(key)
```

## `` com.cra.figaro.util.PriorityMap.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#copyToArray-fffffd03)

```scala
def copyToArray[B >: (T, U)](dest: Array[B], start: Int, n: Int): Int
```

Copies elements to an array and returns the number of elements written.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` dest: Array[B] ``; `` start: Int ``; `` n: Int ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array and returns the number of elements written. Fills the given array dest starting at index start with at most n elements of this mutable map. Copying will stop once either all the elements of this mutable map have been copied, or the end of the array is reached, or n elements have been copied. If start is less than zero, it is taken as zero. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. n the maximal number of elements to copy. start the starting index of xs. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest, start, n)
```

## `` com.cra.figaro.util.PriorityMap.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#copyToArray-fffffaa4)

```scala
def copyToArray[B >: (T, U)](dest: Array[B], start: Int): Int
```

Copies elements to an array, returning the number of elements written.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` dest: Array[B] ``; `` start: Int ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array, returning the number of elements written. Fills the given array dest starting at index start with values of this mutable map. Copying will stop once either all the elements of this mutable map have been copied, or the end of the array is reached. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. start the starting index of xs. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest, start)
```

## `` com.cra.figaro.util.PriorityMap.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#copyToArray-fffff3e3)

```scala
def copyToArray[B >: (T, U)](dest: Array[B]): Int
```

Copies elements to an array, returning the number of elements written.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` dest: Array[B] ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array, returning the number of elements written. Fills the given array dest starting at index start with values of this mutable map. Copying will stop once either all the elements of this mutable map have been copied, or the end of the array is reached. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest)
```

## `` com.cra.figaro.util.PriorityMap.corresponds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#corresponds-958)

```scala
def corresponds[B](that: IterableOnce[B])(p: ((T, U), B) => Boolean): Boolean
```

Tests whether every element of this collection's iterator relates to the corresponding element of another collection by satisfying a test predicate.

Type parameters: `` B ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Parameters, list 2: `` p: ((T, U), B) => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether every element of this collection's iterator relates to the corresponding element of another collection by satisfying a test predicate. Note: will not terminate for infinite-sized collections. Type parameters B the type of the elements of that Value parameters p the test predicate, which relates elements from both collections that the other collection Attributes Returns true if both collections have the same length and p(x, y) is true for all corresponding elements x of this iterator and y of that, otherwise false Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.corresponds[B](that)(p)
```

## `` com.cra.figaro.util.PriorityMap.count ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#count-db6)

```scala
def count(p: (T, U) => Boolean): Int
```

Counts the number of elements in the mutable map which satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` Int ``.

Source contract/attributes: Counts the number of elements in the mutable map which satisfy a predicate. Note: will not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns the number of elements satisfying the predicate p. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.count(p)
```

## `` com.cra.figaro.util.PriorityMap.default ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#default-d26)

```scala
def default(key: T): U
```

Defines the default value computation for the map, returned when a key is not found.

Type parameters: none.

Parameters, list 1: `` key: T ``.

Returns: `` U ``.

Source contract/attributes: Defines the default value computation for the map, returned when a key is not found. The method implemented here throws an exception, but it may be overridden by subclasses. Value parameters key the given key value for which a binding is missing. Attributes Returns the value associated with the given key when it is not found in the map Throws NoSuchElementException if no default value is defined Inherited from: MapOps

Invocation template:

```scala
receiver.default(key)
```

## `` com.cra.figaro.util.PriorityMap.drop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#drop-aba)

```scala
def drop(n: Int): Map[T, U]
```

Selects all elements except the first n ones.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Selects all elements except the first n ones. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to drop from this mutable map. Attributes Returns a mutable map consisting of all elements of this mutable map except the first n ones, or else the empty mutable map, if this mutable map has less than n elements. If n is negative, don't drop any elements. Inherited from: IterableOps

Invocation template:

```scala
receiver.drop(n)
```

## `` com.cra.figaro.util.PriorityMap.dropRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#dropRight-aba)

```scala
def dropRight(n: Int): Map[T, U]
```

Selects all elements except last n ones.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Selects all elements except last n ones. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to drop from this mutable map. Attributes Returns a mutable map consisting of all elements of this mutable map except the last n ones, or else the empty mutable map, if this mutable map has less than n elements. If n is negative, don't drop any elements. Inherited from: IterableOps

Invocation template:

```scala
receiver.dropRight(n)
```

## `` com.cra.figaro.util.PriorityMap.dropWhile ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#dropWhile-c24)

```scala
def dropWhile(p: (T, U) => Boolean): Map[T, U]
```

Selects all elements except the longest prefix that satisfies a predicate.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Selects all elements except the longest prefix that satisfies a predicate. The matching prefix starts with the first element of this mutable map, and the element following the prefix is the first element that does not satisfy the predicate. The matching prefix may be empty, so that this method returns the entire mutable map. Example: List(1, 2, 3, 100, 4).dropWhile(n => n < 10) // List(100, 4) List(1, 2, 3, 100, 4).dropWhile(n => n == 0) // List(1, 2, 3, 100, 4) Use span to obtain both the prefix and suffix. Use filterNot to drop all elements that satisfy the predicate. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p The predicate used to test elements. Attributes Returns the longest suffix of this mutable map whose first element does not satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.dropWhile(p)
```

## `` com.cra.figaro.util.PriorityMap.elementWise ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#elementWise-0)

```scala
def elementWise: ElementWiseExtractor[T, U]
```

Returns an extractor object with a unapplySeq method, which extracts each element of a sequence data.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` ElementWiseExtractor[T, U] ``.

Source contract/attributes: Returns an extractor object with a unapplySeq method, which extracts each element of a sequence data. Attributes Example val firstChar: String => Option[Char] = _.headOption Seq("foo", "bar", "baz") match { case firstChar.unlift.elementWise(c0, c1, c2) => println(s"$c0, $c1, $c2") // Output: f, b, b } Inherited from: PartialFunction

Invocation template:

```scala
receiver.elementWise
```

## `` com.cra.figaro.util.PriorityMap.empty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#empty-0)

```scala
override def empty: Map[T, U]
```

The empty mutable map.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Map[T, U] ``.

Source contract/attributes: The empty mutable map. Attributes Returns an empty iterable of type mutable.Map . Definition Classes MapFactoryDefaults -> IterableOps Inherited from: MapFactoryDefaults

Invocation template:

```scala
receiver.empty
```

## `` com.cra.figaro.util.PriorityMap.equals ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#equals-4ad)

```scala
override def equals(o: Any): Boolean
```

Equality of maps is implemented using the lookup method get. This method returns true if

Type parameters: none.

Parameters, list 1: `` o: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Equality of maps is implemented using the lookup method get. This method returns true if the argument o is a Map, the two maps have the same size, and for every (key, value) pair in this map, other.get(key) == Some(value). The implementation of equals checks the canEqual method, so subclasses of Map can narrow down the equality to specific map types. The Map implementations in the standard library can all be compared, their canEqual methods return true. Note: The equals method only respects the equality laws (symmetry, transitivity) if the two maps use the same key equivalence function in their lookup operation. For example, the key equivalence operation in a scala.collection.immutable.TreeMap is defined by its ordering. Comparing a TreeMap with a HashMap leads to unexpected results if ordering.equiv(k1, k2) (used for lookup in TreeMap) is different from k1 == k2 (used for lookup in HashMap). import scala.collection.immutable._ val ord: Ordering[String] = _ compareToIgnoreCase _ val result1 = TreeMap("A" -> 1)(using ord) == HashMap("a" -> 1) // false val result2 = HashMap("a" -> 1) == TreeMap("A" -> 1)(using ord) // true Value parameters o the object to compare this map with for equality Attributes Returns true if o is a Map with the same size and identical key-value mappings Definition Classes Map -> Equals -> Any Inherited from: Map

Invocation template:

```scala
receiver.equals(o)
```

## `` com.cra.figaro.util.PriorityMap.exists ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#exists-fffff66f)

```scala
def exists(p: (T, U) => Boolean): Boolean
```

Tests whether a predicate holds for at least one element of this mutable map.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether a predicate holds for at least one element of this mutable map. Note: may not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns true if the given predicate p is satisfied by at least one element of this mutable map, otherwise false Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.exists(p)
```

## `` com.cra.figaro.util.PriorityMap.filter ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#filter-c24)

```scala
def filter(pred: (T, U) => Boolean): Map[T, U]
```

Selects all elements of this mutable map which satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` pred: (T, U) => Boolean ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Selects all elements of this mutable map which satisfy a predicate. Value parameters p the predicate used to test elements. Attributes Returns a new mutable map consisting of all elements of this mutable map that satisfy the given predicate p. The order of the elements is preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.filter(pred)
```

## `` com.cra.figaro.util.PriorityMap.filterInPlace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#filterInPlace-fffff774)

```scala
def filterInPlace(p: (T, U) => Boolean): this.type
```

Retains only those mappings for which the predicate p returns true.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` this.type ``.

Source contract/attributes: Retains only those mappings for which the predicate p returns true. Value parameters p The test predicate Attributes Inherited from: MapOps

Invocation template:

```scala
receiver.filterInPlace(p)
```

## `` com.cra.figaro.util.PriorityMap.filterNot ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#filterNot-c24)

```scala
def filterNot(pred: (T, U) => Boolean): Map[T, U]
```

Selects all elements of this mutable map which do not satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` pred: (T, U) => Boolean ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Selects all elements of this mutable map which do not satisfy a predicate. Value parameters pred the predicate used to test elements. Attributes Returns a new mutable map consisting of all elements of this mutable map that do not satisfy the given predicate pred. Their order may not be preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.filterNot(pred)
```

## `` com.cra.figaro.util.PriorityMap.find ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#find-94e)

```scala
def find(p: (T, U) => Boolean): Option[(T, U)]
```

Finds the first element of the mutable map satisfying a predicate, if any.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` Option[(T, U)] ``.

Source contract/attributes: Finds the first element of the mutable map satisfying a predicate, if any. Note: may not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the predicate used to test elements. Attributes Returns an option value containing the first element in the mutable map that satisfies p, or None if none exists. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.find(p)
```

## `` com.cra.figaro.util.PriorityMap.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#flatMap-5d3)

```scala
def flatMap[B](f: (T, U) => IterableOnce[B]): Iterable[B]
```

Builds a new mutable map by applying a function to all elements of this mutable map and using the elements of the resulting collections.

Type parameters: `` B ``.

Parameters, list 1: `` f: (T, U) => IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Builds a new mutable map by applying a function to all elements of this mutable map and using the elements of the resulting collections. For example: def getWords(lines: Seq[String]): Seq[String] = lines.flatMap(line => line.split("\\W+")) The type of the resulting collection is guided by the static type of this mutable map. This might cause unexpected results sometimes. For example: // lettersOf will return a Seq[Char] of likely repeated letters, instead of a Set def lettersOf(words: Seq[String]) = words.flatMap(word => word.toSet) // lettersOf2 will return a Set[Char], not a Seq def lettersOf2(words: Seq[String]) = words.toSet.flatMap(word => word.toSeq) // xs will be an Iterable[Int] val xs = Map("a" -> List(11, 111), "b" -> List(22, 222)).flatMap(_._2) // ys will be a Map[Int, Int] val ys = Map("a" -> List(1 -> 11, 1 -> 111), "b" -> List(2 -> 22, 2 -> 222)).flatMap(_._2) Type parameters B the element type of the returned collection. Value parameters f the function to apply to each element. Attributes Returns a new mutable map resulting from applying the given collection-valued function f to each element of this mutable map and concatenating the results. Inherited from: IterableOps

Invocation template:

```scala
receiver.flatMap[B](f)
```

## `` com.cra.figaro.util.PriorityMap.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#flatMap-fffffcb7)

```scala
def flatMap[K2, V2](f: ((T, U)) => IterableOnce[(K2, V2)]): Map[K2, V2]
```

Builds a new map by applying a function to all elements of this mutable map and using the elements of the resulting collections.

Type parameters: `` K2 ``; `` V2 ``.

Parameters, list 1: `` f: ((T, U)) => IterableOnce[(K2, V2)] ``.

Returns: `` Map[K2, V2] ``.

Source contract/attributes: Builds a new map by applying a function to all elements of this mutable map and using the elements of the resulting collections. Type parameters K2 the key type of the returned map V2 the value type of the returned map Value parameters f the function to apply to each element. Attributes Returns a new mutable map resulting from applying the given collection-valued function f to each element of this mutable map and concatenating the results. Inherited from: MapOps

Invocation template:

```scala
receiver.flatMap[K2, V2](f)
```

## `` com.cra.figaro.util.PriorityMap.flatten ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#flatten-5d3)

```scala
def flatten[B](implicit asIterable: (T, U) => IterableOnce[B]): Iterable[B]
```

Given that the elements of this collection are themselves iterable collections, converts this mutable map into a mutable map comprising the elements of these iterable collections.

Type parameters: `` B ``.

Context parameters, list 1: `` asIterable: (T, U) => IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Given that the elements of this collection are themselves iterable collections, converts this mutable map into a mutable map comprising the elements of these iterable collections. The resulting collection's type will be guided by the type of mutable map. For example: val xs = List( Set(1, 2, 3), Set(1, 2, 3) ).flatten // xs == List(1, 2, 3, 1, 2, 3) val ys = Set( List(1, 2, 3), List(3, 2, 1) ).flatten // ys == Set(1, 2, 3) Type parameters B the type of the elements of each iterable collection. Value parameters asIterable an implicit conversion which asserts that the element type of this mutable map is an Iterable. Attributes Returns a new mutable map resulting from concatenating all element collections. Inherited from: IterableOps

Invocation template:

```scala
receiver.flatten[B](using asIterable)
```

## `` com.cra.figaro.util.PriorityMap.fold ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#fold-fffff9e1)

```scala
def fold[A1 >: (T, U)](z: A1)(op: (A1, A1) => A1): A1
```

Applies the given binary operator op to the given initial value z and all elements of this mutable map.

Type parameters: `` A1 >: (T, U) ``.

Parameters, list 1: `` z: A1 ``.

Parameters, list 2: `` op: (A1, A1) => A1 ``.

Returns: `` A1 ``.

Source contract/attributes: Applies the given binary operator op to the given initial value z and all elements of this mutable map. For each application of the operator, each operand is either an element of this mutable map, the initial value, or another such application of the operator. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. The initial value may be used an arbitrary number of times, but at least once. If this collection is ordered, then for any application of the operator, the element(s) appearing in the left operand will precede those in the right. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. In either case, it is also necessary that the initial value be a neutral value for the operator, e.g. Nil for List concatenation or 1 for multiplication. The default implementation in IterableOnce is equivalent to foldLeft but may be overridden for more efficient traversal orders. Note: will not terminate for infinite-sized collections. Type parameters A1 The type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. z An initial value; may be used an arbitrary number of times in the computation of the result; must be a neutral value for op for the result to always be the same across runs. Attributes Returns The result of applying op between all the elements and z, or z if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.fold[A1](z)(op)
```

## `` com.cra.figaro.util.PriorityMap.foldLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#foldLeft-fffff9e1)

```scala
def foldLeft[B](z: B)(op: (B, (T, U)) => B): B
```

Applies the given binary operator op to the given initial value z and all elements of this mutable map, going left to right. Returns the initial value if this mutable map is empty.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, (T, U)) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to the given initial value z and all elements of this mutable map, going left to right. Returns the initial value if this mutable map is empty. "Going left to right" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this mutable map, the result is op( op( ... op( op(z, x,,1,,), x,,2,,) ... ), x,,n,,). If this collection is not ordered, then for each application of the operator, each right operand is an element. In addition, the leftmost operand is the initial value, and each other left operand is itself an application of the operator. The elements of this mutable map and the initial value all appear exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator. Value parameters op A binary operator. z An initial value. Attributes Returns The result of applying op to z and all elements of this mutable map, going left to right. Returns z if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foldLeft[B](z)(op)
```

## `` com.cra.figaro.util.PriorityMap.foldRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#foldRight-fffff9e1)

```scala
def foldRight[B](z: B)(op: ((T, U), B) => B): B
```

Applies the given binary operator op to all elements of this mutable map and the given initial value z, going right to left. Returns the initial value if this mutable map is empty.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: ((T, U), B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this mutable map and the given initial value z, going right to left. Returns the initial value if this mutable map is empty. "Going right to left" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this mutable map, the result is op(x,,1,,, op(x,,2,,, op( ... op(x,,n,,, z) ... ))). If this collection is not ordered, then for each application of the operator, each left operand is an element. In addition, the rightmost operand is the initial value, and each other right operand is itself an application of the operator. The elements of this mutable map and the initial value all appear exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator. Value parameters op A binary operator. z An initial value. Attributes Returns The result of applying op to all elements of this mutable map and z, going right to left. Returns z if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foldRight[B](z)(op)
```

## `` com.cra.figaro.util.PriorityMap.forall ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#forall-fffff66f)

```scala
def forall(p: (T, U) => Boolean): Boolean
```

Tests whether a predicate holds for all elements of this mutable map.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether a predicate holds for all elements of this mutable map. Note: may not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns true if this mutable map is empty or the given predicate p holds for all elements of this mutable map, otherwise false. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.forall(p)
```

## `` com.cra.figaro.util.PriorityMap.foreach ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#foreach-58c)

```scala
def foreach[U](f: (T, U) => U): Unit
```

Applies f to each element for its side effects. Note: U parameter needed to help scalac's type inference.

Type parameters: `` U ``.

Parameters, list 1: `` f: (T, U) => U ``.

Returns: `` Unit ``.

Source contract/attributes: Applies f to each element for its side effects. Note: U parameter needed to help scalac's type inference. Type parameters U the return type of f; the value is discarded, but the type parameter aids type inference Value parameters f the function to apply to each element for its side effects Attributes Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foreach[U](f)
```

## `` com.cra.figaro.util.PriorityMap.foreachEntry ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#foreachEntry-fffff84d)

```scala
def foreachEntry[U](f: (T, U) => U): Unit
```

Applies f to each key/value pair for its side effects Note: [U] parameter needed to help scalac's type inference.

Type parameters: `` U ``.

Parameters, list 1: `` f: (T, U) => U ``.

Returns: `` Unit ``.

Source contract/attributes: Applies f to each key/value pair for its side effects Note: [U] parameter needed to help scalac's type inference. Type parameters U the result type of the function f; not used in the method result but aids type inference Value parameters f the function to apply to each key-value pair Attributes Inherited from: MapOps

Invocation template:

```scala
receiver.foreachEntry[U](f)
```

## `` com.cra.figaro.util.PriorityMap.getOrElse ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#getOrElse-ddf)

```scala
def getOrElse[V1 >: U](key: T, default: => V1): V1
```

Returns the value associated with a key, or a default value if the key is not contained in the map.

Type parameters: `` V1 >: U ``.

Parameters, list 1: `` key: T ``; `` default: => V1 ``.

Returns: `` V1 ``.

Source contract/attributes: Returns the value associated with a key, or a default value if the key is not contained in the map. Type parameters V1 the result type of the default computation. Value parameters default a computation that yields a default value in case no binding for key is found in the map. key the key. Attributes Returns the value associated with key if it exists, otherwise the result of the default computation. Inherited from: MapOps

Invocation template:

```scala
receiver.getOrElse[V1](key, default)
```

## `` com.cra.figaro.util.PriorityMap.getOrElseUpdate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#getOrElseUpdate-fffff230)

```scala
def getOrElseUpdate(key: T, defaultValue: => U): U
```

If given key is already in this map, returns associated value.

Type parameters: none.

Parameters, list 1: `` key: T ``; `` defaultValue: => U ``.

Returns: `` U ``.

Source contract/attributes: If given key is already in this map, returns associated value. Otherwise, computes value from given expression defaultValue, stores with key in map and returns that value. Concurrent map implementations may evaluate the expression defaultValue multiple times, or may evaluate defaultValue without inserting the result. Value parameters defaultValue the computation yielding the value to associate with key, if key is previously unbound. key the key to test Attributes Returns the value associated with key (either previously or as a result of executing the method). Inherited from: MapOps

Invocation template:

```scala
receiver.getOrElseUpdate(key, defaultValue)
```

## `` com.cra.figaro.util.PriorityMap.groupBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#groupBy-f68)

```scala
def groupBy[K](f: (T, U) => K): Map[K, Map[T, U]]
```

Partitions this mutable map into a map of mutable maps according to some discriminator function.

Type parameters: `` K ``.

Parameters, list 1: `` f: (T, U) => K ``.

Returns: `` Map[K, Map[T, U]] ``.

Source contract/attributes: Partitions this mutable map into a map of mutable maps according to some discriminator function. Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters K the type of keys returned by the discriminator function. Value parameters f the discriminator function. Attributes Returns A map from keys to mutable maps such that the following invariant holds: (xs groupBy f)(k) = xs filter (x => f(x) == k) That is, every key `k` is bound to a mutable map of those elements `x` for which `f(x)` equals `k`. Inherited from: IterableOps

Invocation template:

```scala
receiver.groupBy[K](f)
```

## `` com.cra.figaro.util.PriorityMap.groupMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#groupMap-fffff03a)

```scala
def groupMap[K, B](key: (T, U) => K)(f: (T, U) => B): Map[K, Iterable[B]]
```

Partitions this mutable map into a map of mutable maps according to a discriminator function key. Each element in a group is transformed into a value of type B using the value function.

Type parameters: `` K ``; `` B ``.

Parameters, list 1: `` key: (T, U) => K ``.

Parameters, list 2: `` f: (T, U) => B ``.

Returns: `` Map[K, Iterable[B]] ``.

Source contract/attributes: Partitions this mutable map into a map of mutable maps according to a discriminator function key. Each element in a group is transformed into a value of type B using the value function. It is equivalent to groupBy(key).mapValues(_.map(f)), but more efficient. case class User(name: String, age: Int) def namesByAge(users: Seq[User]): Map[Int, Seq[String]] = users.groupMap(_.age)(_.name) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of values returned by the transformation function K the type of keys returned by the discriminator function Value parameters f the element transformation function key the discriminator function Attributes Returns a map associating each key k produced by key with a mutable map of values produced by applying f to elements that map to k Inherited from: IterableOps

Invocation template:

```scala
receiver.groupMap[K, B](key)(f)
```

## `` com.cra.figaro.util.PriorityMap.groupMapReduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#groupMapReduce-8e8)

```scala
def groupMapReduce[K, B](key: (T, U) => K)(f: (T, U) => B)(reduce: (B, B) => B): Map[K, B]
```

Partitions this mutable map into a map according to a discriminator function key. All the values that have the same discriminator are then transformed by the f function and then reduced into a single value with the reduce function.

Type parameters: `` K ``; `` B ``.

Parameters, list 1: `` key: (T, U) => K ``.

Parameters, list 2: `` f: (T, U) => B ``.

Parameters, list 3: `` reduce: (B, B) => B ``.

Returns: `` Map[K, B] ``.

Source contract/attributes: Partitions this mutable map into a map according to a discriminator function key. All the values that have the same discriminator are then transformed by the f function and then reduced into a single value with the reduce function. It is equivalent to groupBy(key).mapValues(_.map(f).reduce(reduce)), but more efficient. def occurrences[A](as: Seq[A]): Map[A, Int] = as.groupMapReduce(identity)(_ => 1)(_ + _) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of values returned by the transformation function K the type of keys returned by the discriminator function Value parameters f the element transformation function key the discriminator function reduce the reduction function used to combine values mapped to the same key Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.groupMapReduce[K, B](key)(f)(reduce)
```

## `` com.cra.figaro.util.PriorityMap.grouped ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#grouped-fffffbef)

```scala
def grouped(size: Int): Iterator[Map[T, U]]
```

Partitions elements in fixed size mutable maps.

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Iterator[Map[T, U]] ``.

Source contract/attributes: Partitions elements in fixed size mutable maps. Value parameters size the number of elements per group Attributes Returns An iterator producing mutable maps of size size, except the last will be less than size size if the elements don't divide evenly. See also scala.collection.Iterator, method grouped Inherited from: IterableOps

Invocation template:

```scala
receiver.grouped(size)
```

## `` com.cra.figaro.util.PriorityMap.hashCode ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#hashCode-ffffff27)

```scala
override def hashCode(): Int
```

Calculates a hash code value for the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Int ``.

Source contract/attributes: Calculates a hash code value for the object. The default hashing algorithm is platform dependent. Note that it is allowed for two objects to have identical hash codes (o1.hashCode.equals(o2.hashCode)) yet not be equal (o1.equals(o2) returns false). A degenerate implementation could always return 0. However, it is required that if two objects are equal (o1.equals(o2) returns true) that they have identical hash codes (o1.hashCode.equals(o2.hashCode)). Therefore, when overriding this method, be sure to verify that the behavior is consistent with the equals method. Attributes Returns the hash code value for this object. Definition Classes Map -> Any Inherited from: Map

Invocation template:

```scala
receiver.hashCode()
```

## `` com.cra.figaro.util.PriorityMap.head ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#head-0)

```scala
def head: (T, U)
```

Selects the first element of this mutable map.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` (T, U) ``.

Source contract/attributes: Selects the first element of this mutable map. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the first element of this mutable map. Throws NoSuchElementException if the mutable map is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.head
```

## `` com.cra.figaro.util.PriorityMap.headOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#headOption-0)

```scala
def headOption: Option[(T, U)]
```

Optionally selects the first element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Option[(T, U)] ``.

Source contract/attributes: Optionally selects the first element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the first element of this mutable map if it is nonempty, None if it is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.headOption
```

## `` com.cra.figaro.util.PriorityMap.init ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#init-0)

```scala
def init: Map[T, U]
```

The initial part of the collection without its last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Map[T, U] ``.

Source contract/attributes: The initial part of the collection without its last element. Note: Even when applied to a view or a lazy collection it will always force the elements. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.init
```

## `` com.cra.figaro.util.PriorityMap.inits ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#inits-0)

```scala
def inits: Iterator[Map[T, U]]
```

Iterates over the inits of this mutable map. The first value will be this mutable map and the final one will be an empty mutable map, with the intervening values the results of successive applications of init.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Map[T, U]] ``.

Source contract/attributes: Iterates over the inits of this mutable map. The first value will be this mutable map and the final one will be an empty mutable map, with the intervening values the results of successive applications of init. Note: Even when applied to a view or a lazy collection it will always force the elements. Attributes Returns an iterator over all the inits of this mutable map Example List(1,2,3).inits = Iterator(List(1,2,3), List(1,2), List(1), Nil) Inherited from: IterableOps

Invocation template:

```scala
receiver.inits
```

## `` com.cra.figaro.util.PriorityMap.isDefinedAt ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#isDefinedAt-4ad)

```scala
def isDefinedAt(key: T): Boolean
```

Tests whether this map contains a binding for a key. This method, which implements an abstract method of trait PartialFunction, is equivalent to contains.

Type parameters: none.

Parameters, list 1: `` key: T ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this map contains a binding for a key. This method, which implements an abstract method of trait PartialFunction, is equivalent to contains. Value parameters key the key Attributes Returns true if there is a binding for key in this map, false otherwise. Inherited from: MapOps

Invocation template:

```scala
receiver.isDefinedAt(key)
```

## `` com.cra.figaro.util.PriorityMap.isEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#isEmpty-0)

```scala
def isEmpty: Boolean
```

Tests whether the mutable map is empty.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether the mutable map is empty. Note: The default implementation creates and discards an iterator. Note: Implementations in subclasses that are not repeatedly iterable must take care not to consume any elements when isEmpty is called. Attributes Returns true if the mutable map contains no elements, false otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.isEmpty
```

## `` com.cra.figaro.util.PriorityMap.isTraversableAgain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#isTraversableAgain-0)

```scala
override def isTraversableAgain: Boolean
```

Tests whether this mutable map can be repeatedly traversed. Always true for Iterables and false for Iterators unless overridden.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this mutable map can be repeatedly traversed. Always true for Iterables and false for Iterators unless overridden. Attributes Returns true if it is repeatedly traversable, false otherwise. Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.isTraversableAgain
```

## `` com.cra.figaro.util.PriorityMap.iterableFactory ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#iterableFactory-0)

```scala
override def iterableFactory: IterableFactory[Iterable]
```

The companion object of this mutable map, providing various factory methods.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IterableFactory[Iterable] ``.

Source contract/attributes: The companion object of this mutable map, providing various factory methods. Attributes Returns the companion factory object for this collection type Note When implementing a custom collection type and refining CC to the new type, this method needs to be overridden to return a factory for the new type (the compiler will issue an error otherwise). Definition Classes Iterable -> Iterable -> IterableOps Inherited from: Iterable

Invocation template:

```scala
receiver.iterableFactory
```

## `` com.cra.figaro.util.PriorityMap.keySet ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#keySet-0)

```scala
def keySet: Set[T]
```

A set representing the keys contained by this map.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[T] ``.

Source contract/attributes: A set representing the keys contained by this map. For efficiency the resulting set may be a view (maintaining a reference to the map and reflecting modifications to the map), but it may also be a strict collection without reference to the map. To ensure an independent strict collection, use m.keysIterator.toSet To obtain a view on the keys, use scala.collection.View.fromIteratorProvider(m.keysIterator) Specifically, for mutable collections, it is not guaranteed that the set would reflect the changes made onto the map, nor it is guaranteed that the set would not reflect the changes. To guarantee either behavior, obtain a strict collection or a view as above. Attributes Returns a set representing the keys contained by this map Inherited from: MapOps

Invocation template:

```scala
receiver.keySet
```

## `` com.cra.figaro.util.PriorityMap.keyStepper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#keyStepper-190)

```scala
def keyStepper[S <: Stepper[_]](implicit shape: StepperShape[T, S]): S
```

Returns a Stepper for the keys of this map. See method stepper.

Type parameters: `` S <: Stepper[_] ``.

Context parameters, list 1: `` shape: StepperShape[T, S] ``.

Returns: `` S ``.

Source contract/attributes: Returns a Stepper for the keys of this map. See method stepper. Type parameters S the type of Stepper to use, determined by the implicit StepperShape Attributes Inherited from: MapOps

Invocation template:

```scala
receiver.keyStepper[S](using shape)
```

## `` com.cra.figaro.util.PriorityMap.keys ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#keys-0)

```scala
def keys: Iterable[T]
```

An Iterable collection of the keys contained by this map.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: An Iterable collection of the keys contained by this map. For efficiency the resulting collection may be a view (maintaining a reference to the map and reflecting modifications to the map), but it may also be a strict collection without reference to the map. To ensure an independent strict collection, use m.keysIterator.toSet To obtain a view on the keys, use scala.collection.View.fromIteratorProvider(m.keysIterator) Attributes Returns an Iterable collection of the keys contained by this map Inherited from: MapOps

Invocation template:

```scala
receiver.keys
```

## `` com.cra.figaro.util.PriorityMap.keysIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#keysIterator-0)

```scala
def keysIterator: Iterator[T]
```

An Iterator of the keys contained by this map.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[T] ``.

Source contract/attributes: An Iterator of the keys contained by this map. Attributes Returns an Iterator of the keys contained by this map Inherited from: MapOps

Invocation template:

```scala
receiver.keysIterator
```

## `` com.cra.figaro.util.PriorityMap.knownSize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#knownSize-0)

```scala
override def knownSize: Int
```

The number of elements in the collection under construction, if it can be cheaply computed, -1 otherwise.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: The number of elements in the collection under construction, if it can be cheaply computed, -1 otherwise. Attributes Returns The number of elements. The default implementation always returns -1. Definition Classes MapOps -> Growable -> IterableOnce Inherited from: MapOps

Invocation template:

```scala
receiver.knownSize
```

## `` com.cra.figaro.util.PriorityMap.last ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#last-0)

```scala
def last: (T, U)
```

Selects the last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` (T, U) ``.

Source contract/attributes: Selects the last element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns The last element of this mutable map. Throws NoSuchElementException If the mutable map is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.last
```

## `` com.cra.figaro.util.PriorityMap.lastOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#lastOption-0)

```scala
def lastOption: Option[(T, U)]
```

Optionally selects the last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Option[(T, U)] ``.

Source contract/attributes: Optionally selects the last element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the last element of this mutable map if it is nonempty, None if it is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.lastOption
```

## `` com.cra.figaro.util.PriorityMap.lazyZip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#lazyZip-fffffd84)

```scala
def lazyZip[B](that: Iterable[B]): LazyZip2[(T, U), B, this.type]
```

Analogous to zip except that the elements in each collection are not consumed until a strict operation is invoked on the returned LazyZip2 decorator.

Type parameters: `` B ``.

Parameters, list 1: `` that: Iterable[B] ``.

Returns: `` LazyZip2[(T, U), B, this.type] ``.

Source contract/attributes: Analogous to zip except that the elements in each collection are not consumed until a strict operation is invoked on the returned LazyZip2 decorator. Calls to lazyZip can be chained to support higher arities (up to 4) without incurring the expense of constructing and deconstructing intermediary tuples. val xs = List(1, 2, 3) val res = (xs lazyZip xs lazyZip xs lazyZip xs).map((a, b, c, d) => a + b + c + d) // res == List(4, 8, 12) Type parameters B the type of the second element in each eventual pair Value parameters that the iterable providing the second element of each eventual pair Attributes Returns a decorator LazyZip2 that allows strict operations to be performed on the lazily evaluated pairs or chained calls to lazyZip. Implicit conversion to Iterable[(A, B)] is also supported. Inherited from: Iterable

Invocation template:

```scala
receiver.lazyZip[B](that)
```

## `` com.cra.figaro.util.PriorityMap.lift ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#lift-0)

```scala
def lift: T => Option[U]
```

Turns this partial function into a plain function returning an Option result.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` T => Option[U] ``.

Source contract/attributes: Turns this partial function into a plain function returning an Option result. Attributes Returns a function that takes an argument x to Some(this(x)) if this is defined for x, and to None otherwise. See also Function.unlift Inherited from: PartialFunction

Invocation template:

```scala
receiver.lift
```

## `` com.cra.figaro.util.PriorityMap.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#map-5d3)

```scala
def map[B](f: (T, U) => B): Iterable[B]
```

Builds a new mutable map by applying a function to all elements of this mutable map.

Type parameters: `` B ``.

Parameters, list 1: `` f: (T, U) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Builds a new mutable map by applying a function to all elements of this mutable map. Type parameters B the element type of the returned mutable map . Value parameters f the function to apply to each element. Attributes Returns a new mutable map resulting from applying the given function f to each element of this mutable map and collecting the results. Inherited from: IterableOps

Invocation template:

```scala
receiver.map[B](f)
```

## `` com.cra.figaro.util.PriorityMap.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#map-fffffcb7)

```scala
def map[K2, V2](f: ((T, U)) => (K2, V2)): Map[K2, V2]
```

Builds a new map by applying a function to all elements of this mutable map.

Type parameters: `` K2 ``; `` V2 ``.

Parameters, list 1: `` f: ((T, U)) => (K2, V2) ``.

Returns: `` Map[K2, V2] ``.

Source contract/attributes: Builds a new map by applying a function to all elements of this mutable map. Type parameters K2 the key type of the returned map V2 the value type of the returned map Value parameters f the function to apply to each element. Attributes Returns a new mutable map resulting from applying the given function f to each element of this mutable map and collecting the results. Inherited from: MapOps

Invocation template:

```scala
receiver.map[K2, V2](f)
```

## `` com.cra.figaro.util.PriorityMap.mapFactory ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#mapFactory-0)

```scala
override def mapFactory: MapFactory[Map]
```

The companion object of this map, providing various factory methods.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` MapFactory[Map] ``.

Source contract/attributes: The companion object of this map, providing various factory methods. Attributes Returns the MapFactory companion object for this map type Note When implementing a custom collection type and refining CC to the new type, this method needs to be overridden to return a factory for the new type (the compiler will issue an error otherwise). Definition Classes Map -> Map -> MapOps Inherited from: Map

Invocation template:

```scala
receiver.mapFactory
```

## `` com.cra.figaro.util.PriorityMap.mapResult ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#mapResult-34b)

```scala
def mapResult[NewTo](f: Map[T, U] => NewTo): Builder[(T, U), NewTo]
```

A builder resulting from this builder by mapping the result using f.

Type parameters: `` NewTo ``.

Parameters, list 1: `` f: Map[T, U] => NewTo ``.

Returns: `` Builder[(T, U), NewTo] ``.

Source contract/attributes: A builder resulting from this builder by mapping the result using f. Type parameters NewTo the type of collection produced by the returned builder Value parameters f the function to apply to this builder's result Attributes Inherited from: Builder

Invocation template:

```scala
receiver.mapResult[NewTo](f)
```

## `` com.cra.figaro.util.PriorityMap.mapValuesInPlace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#mapValuesInPlace-fffff774)

```scala
def mapValuesInPlace(f: (T, U) => U): this.type
```

Applies a transformation function to all values contained in this map. The transformation function produces new values from existing keys associated values.

Type parameters: none.

Parameters, list 1: `` f: (T, U) => U ``.

Returns: `` this.type ``.

Source contract/attributes: Applies a transformation function to all values contained in this map. The transformation function produces new values from existing keys associated values. Value parameters f the transformation to apply Attributes Returns the map itself. Inherited from: MapOps

Invocation template:

```scala
receiver.mapValuesInPlace(f)
```

## `` com.cra.figaro.util.PriorityMap.max ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#max-ffffff34)

```scala
def max[B >: (T, U)](implicit ord: Ordering[B]): (T, U)
```

Finds the largest element.

Type parameters: `` B >: (T, U) ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` (T, U) ``.

Source contract/attributes: Finds the largest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns the largest element of this mutable map with respect to the ordering ord. Throws UnsupportedOperationException if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.max[B](using ord)
```

## `` com.cra.figaro.util.PriorityMap.maxBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#maxBy-fffff0c5)

```scala
def maxBy[B](f: (T, U) => B)(implicit ord: Ordering[B]): (T, U)
```

Finds the first element which yields the largest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: (T, U) => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` (T, U) ``.

Source contract/attributes: Finds the first element which yields the largest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns the first element of this mutable map with the largest value measured by function f with respect to the ordering ord. Throws UnsupportedOperationException if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxBy[B](f)(using ord)
```

## `` com.cra.figaro.util.PriorityMap.maxByOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#maxByOption-fffff66f)

```scala
def maxByOption[B](f: (T, U) => B)(implicit ord: Ordering[B]): Option[(T, U)]
```

Finds the first element which yields the largest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: (T, U) => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` Option[(T, U)] ``.

Source contract/attributes: Finds the first element which yields the largest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns an option value containing the first element of this mutable map with the largest value measured by function f with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxByOption[B](f)(using ord)
```

## `` com.cra.figaro.util.PriorityMap.maxOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#maxOption-fffff45e)

```scala
def maxOption[B >: (T, U)](implicit ord: Ordering[B]): Option[(T, U)]
```

Finds the largest element.

Type parameters: `` B >: (T, U) ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` Option[(T, U)] ``.

Source contract/attributes: Finds the largest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns an option value containing the largest element of this mutable map with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxOption[B](using ord)
```

## `` com.cra.figaro.util.PriorityMap.min ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#min-ffffff34)

```scala
def min[B >: (T, U)](implicit ord: Ordering[B]): (T, U)
```

Finds the smallest element.

Type parameters: `` B >: (T, U) ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` (T, U) ``.

Source contract/attributes: Finds the smallest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns the smallest element of this mutable map with respect to the ordering ord. Throws UnsupportedOperationException if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.min[B](using ord)
```

## `` com.cra.figaro.util.PriorityMap.minBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#minBy-fffff0c5)

```scala
def minBy[B](f: (T, U) => B)(implicit ord: Ordering[B]): (T, U)
```

Finds the first element which yields the smallest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: (T, U) => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` (T, U) ``.

Source contract/attributes: Finds the first element which yields the smallest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns the first element of this mutable map with the smallest value measured by function f with respect to the ordering ord. Throws UnsupportedOperationException if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minBy[B](f)(using ord)
```

## `` com.cra.figaro.util.PriorityMap.minByOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#minByOption-fffff66f)

```scala
def minByOption[B](f: (T, U) => B)(implicit ord: Ordering[B]): Option[(T, U)]
```

Finds the first element which yields the smallest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: (T, U) => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` Option[(T, U)] ``.

Source contract/attributes: Finds the first element which yields the smallest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns an option value containing the first element of this mutable map with the smallest value measured by function f with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minByOption[B](f)(using ord)
```

## `` com.cra.figaro.util.PriorityMap.minOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#minOption-fffff45e)

```scala
def minOption[B >: (T, U)](implicit ord: Ordering[B]): Option[(T, U)]
```

Finds the smallest element.

Type parameters: `` B >: (T, U) ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` Option[(T, U)] ``.

Source contract/attributes: Finds the smallest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns an option value containing the smallest element of this mutable map with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minOption[B](using ord)
```

## `` com.cra.figaro.util.PriorityMap.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#mkString-0)

```scala
final def mkString: String
```

Displays all elements of this mutable map in a string.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Displays all elements of this mutable map in a string. Delegates to addString, which can be overridden. Attributes Returns a string representation of this mutable map. In the resulting string the string representations (w.r.t. the method toString) of all elements of this mutable map follow each other without any separator string. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString
```

## `` com.cra.figaro.util.PriorityMap.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#mkString-fffff2ca)

```scala
final def mkString(sep: String): String
```

Displays all elements of this mutable map in a string using a separator string.

Type parameters: none.

Parameters, list 1: `` sep: String ``.

Returns: `` String ``.

Source contract/attributes: Displays all elements of this mutable map in a string using a separator string. Delegates to addString, which can be overridden. Value parameters sep the separator string. Attributes Returns a string representation of this mutable map. In the resulting string the string representations (w.r.t. the method toString) of all elements of this mutable map are separated by the string sep. Example List(1, 2, 3).mkString("|") = "1|2|3" Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString(sep)
```

## `` com.cra.figaro.util.PriorityMap.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#mkString-d94)

```scala
final def mkString(start: String, sep: String, end: String): String
```

Displays all elements of this mutable map in a string using start, end, and separator strings.

Type parameters: none.

Parameters, list 1: `` start: String ``; `` sep: String ``; `` end: String ``.

Returns: `` String ``.

Source contract/attributes: Displays all elements of this mutable map in a string using start, end, and separator strings. Delegates to addString, which can be overridden. Value parameters end the ending string. sep the separator string. start the starting string. Attributes Returns a string representation of this mutable map. The resulting string begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this mutable map are separated by the string sep. Example List(1, 2, 3).mkString("(", "; ", ")") = "(1; 2; 3)" Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString(start, sep, end)
```

## `` com.cra.figaro.util.PriorityMap.nonEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#nonEmpty-0)

```scala
def nonEmpty: Boolean
```

Tests whether the mutable map is not empty.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether the mutable map is not empty. Attributes Returns true if the mutable map contains at least one element, false otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.nonEmpty
```

## `` com.cra.figaro.util.PriorityMap.orElse ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#orElse-a52)

```scala
def orElse[A1 <: T, B1 >: U](that: PartialFunction[A1, B1]): PartialFunction[A1, B1]
```

Composes this partial function with a fallback partial function which gets applied where this partial function is not defined.

Type parameters: `` A1 <: T ``; `` B1 >: U ``.

Parameters, list 1: `` that: PartialFunction[A1, B1] ``.

Returns: `` PartialFunction[A1, B1] ``.

Source contract/attributes: Composes this partial function with a fallback partial function which gets applied where this partial function is not defined. Type parameters A1 the argument type of the fallback function B1 the result type of the fallback function Value parameters that the fallback function Attributes Returns a partial function which has as domain the union of the domains of this partial function and that. The resulting partial function takes x to this(x) where this is defined, and to that(x) where it is not. Inherited from: PartialFunction

Invocation template:

```scala
receiver.orElse[A1, B1](that)
```

## `` com.cra.figaro.util.PriorityMap.partition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#partition-243)

```scala
def partition(p: (T, U) => Boolean): (Map[T, U], Map[T, U])
```

A pair of, first, all elements that satisfy predicate p and, second, all elements that do not.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` (Map[T, U], Map[T, U]) ``.

Source contract/attributes: A pair of, first, all elements that satisfy predicate p and, second, all elements that do not. The two mutable map correspond to the result of filter and filterNot, respectively. The default implementation provided here needs to traverse the collection twice. Strict collections have an overridden version of partition in StrictOptimizedIterableOps, which requires only a single traversal. Value parameters p the predicate used to test elements Attributes Returns a pair of mutable maps: the first containing all elements that satisfy p, the second containing those that do not Inherited from: IterableOps

Invocation template:

```scala
receiver.partition(p)
```

## `` com.cra.figaro.util.PriorityMap.partitionMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#partitionMap-2d1)

```scala
def partitionMap[A1, A2](f: (T, U) => Either[A1, A2]): (Iterable[A1], Iterable[A2])
```

Applies a function f to each element of the mutable map and returns a pair of mutable maps: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right.

Type parameters: `` A1 ``; `` A2 ``.

Parameters, list 1: `` f: (T, U) => Either[A1, A2] ``.

Returns: `` (Iterable[A1], Iterable[A2]) ``.

Source contract/attributes: Applies a function f to each element of the mutable map and returns a pair of mutable maps: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right. Example: val xs = $Coll(1, "one", 2, "two", 3, "three") partitionMap { case i: Int => Left(i) case s: String => Right(s) } // xs == ($Coll(1, 2, 3), // $Coll(one, two, three)) Type parameters A1 the element type of the first resulting collection A2 the element type of the second resulting collection Value parameters f the 'split function' mapping the elements of this mutable map to an scala.util.Either Attributes Returns a pair of mutable maps: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right. Inherited from: IterableOps

Invocation template:

```scala
receiver.partitionMap[A1, A2](f)
```

## `` com.cra.figaro.util.PriorityMap.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#product-533)

```scala
def product[B >: (T, U)](implicit num: Numeric[B]): B
```

Multiplies together the elements of this collection.

Type parameters: `` B >: (T, U) ``.

Context parameters, list 1: `` num: Numeric[B] ``.

Returns: `` B ``.

Source contract/attributes: Multiplies together the elements of this collection. The default implementation uses reduce for a known non-empty collection, foldLeft otherwise. Note: will not terminate for infinite-sized collections. Type parameters B the result type of the * operator. Value parameters num an implicit parameter defining a set of numeric operations which includes the * operator to be used in forming the product. Attributes Returns the product of all elements of this mutable map with respect to the * operator in num. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.product[B](using num)
```

## `` com.cra.figaro.util.PriorityMap.put ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#put-fffffa63)

```scala
def put(key: T, value: U): Option[U]
```

Adds a new key/value pair to this map and optionally returns previously bound value. If the map already contains a mapping for the key, it will be overridden by the new value.

Type parameters: none.

Parameters, list 1: `` key: T ``; `` value: U ``.

Returns: `` Option[U] ``.

Source contract/attributes: Adds a new key/value pair to this map and optionally returns previously bound value. If the map already contains a mapping for the key, it will be overridden by the new value. Value parameters key the key to update value the new value Attributes Returns an option value containing the value associated with the key before the put operation was executed, or None if key was not defined in the map before. Inherited from: MapOps

Invocation template:

```scala
receiver.put(key, value)
```

## `` com.cra.figaro.util.PriorityMap.reduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#reduce-fffff3d4)

```scala
def reduce[B >: (T, U)](op: (B, B) => B): B
```

Applies the given binary operator op to all elements of this mutable map.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` op: (B, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this mutable map. For each application of the operator, each operand is either an element of this mutable map or another such application of the operator. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. If this collection is ordered, then for any application of the operator, the element(s) appearing in the left operand will precede those in the right. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. Attributes Returns The result of applying op between all the elements if the mutable map is nonempty. Throws UnsupportedOperationException if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduce[B](op)
```

## `` com.cra.figaro.util.PriorityMap.reduceLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#reduceLeft-fffff3d4)

```scala
def reduceLeft[B >: (T, U)](op: (B, (T, U)) => B): B
```

Applies the given binary operator op to all elements of this mutable map, going left to right.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` op: (B, (T, U)) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this mutable map, going left to right. "Going left to right" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this mutable map, the result is op( op( op( ... op(x,,1,,, x,,2,,) ... ), x,,n-1,,), x,,n,,). If this collection is not ordered, then for each application of the operator, each right operand is an element. In addition, the leftmost operand is the first element of this mutable map and each other left operand is itself an application of the operator. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of applying op to all elements of this mutable map, going left to right. Throws UnsupportedOperationException if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceLeft[B](op)
```

## `` com.cra.figaro.util.PriorityMap.reduceLeftOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#reduceLeftOption-8fe)

```scala
def reduceLeftOption[B >: (T, U)](op: (B, (T, U)) => B): Option[B]
```

If this mutable map is nonempty, reduces it with the given binary operator op, going left to right.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` op: (B, (T, U)) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this mutable map is nonempty, reduces it with the given binary operator op, going left to right. The behavior is the same as reduceLeft except that the value is None if the mutable map is empty. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of reducing this mutable map with op going left to right if the mutable map is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceLeftOption[B](op)
```

## `` com.cra.figaro.util.PriorityMap.reduceOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#reduceOption-8fe)

```scala
def reduceOption[B >: (T, U)](op: (B, B) => B): Option[B]
```

If this mutable map is nonempty, reduces it with the given binary operator op.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` op: (B, B) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this mutable map is nonempty, reduces it with the given binary operator op. The behavior is the same as reduce except that the value is None if the mutable map is empty. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B A type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. Attributes Returns The result of reducing this mutable map with op if the mutable map is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceOption[B](op)
```

## `` com.cra.figaro.util.PriorityMap.reduceRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#reduceRight-fffff3d4)

```scala
def reduceRight[B >: (T, U)](op: ((T, U), B) => B): B
```

Applies the given binary operator op to all elements of this mutable map, going right to left.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` op: ((T, U), B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this mutable map, going right to left. "Going right to left" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this mutable map, the result is op(x,,1,,, op(x,,2,,, op( ... op(x,,n-1,,, x,,n,,) ... ))). If this collection is not ordered, then for each application of the operator, each left operand is an element. In addition, the rightmost operand is the last element of this mutable map and each other right operand is itself an application of the operator. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of applying op to all elements of this mutable map, going right to left. Throws UnsupportedOperationException if this mutable map is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceRight[B](op)
```

## `` com.cra.figaro.util.PriorityMap.reduceRightOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#reduceRightOption-8fe)

```scala
def reduceRightOption[B >: (T, U)](op: ((T, U), B) => B): Option[B]
```

If this mutable map is nonempty, reduces it with the given binary operator op, going right to left.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` op: ((T, U), B) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this mutable map is nonempty, reduces it with the given binary operator op, going right to left. The behavior is the same as reduceRight except that the value is None if the mutable map is empty. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of reducing this mutable map with op going right to left if the mutable map is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceRightOption[B](op)
```

## `` com.cra.figaro.util.PriorityMap.remove ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#remove-fffffb50)

```scala
def remove(key: T): Option[U]
```

Removes a key from this map, returning the value associated previously with that key as an option.

Type parameters: none.

Parameters, list 1: `` key: T ``.

Returns: `` Option[U] ``.

Source contract/attributes: Removes a key from this map, returning the value associated previously with that key as an option. Value parameters key the key to be removed Attributes Returns an option value containing the value associated previously with key, or None if key was not defined in the map before. Inherited from: MapOps

Invocation template:

```scala
receiver.remove(key)
```

## `` com.cra.figaro.util.PriorityMap.result ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#result-fffffe46)

```scala
def result(): Map[T, U]
```

Result collection consisting of all elements appended so far.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[T, U] ``.

Source contract/attributes: Result collection consisting of all elements appended so far. Attributes Inherited from: MapOps

Invocation template:

```scala
receiver.result()
```

## `` com.cra.figaro.util.PriorityMap.runWith ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#runWith-51)

```scala
def runWith[U](action: U => U): T => Boolean
```

Composes this partial function with an action function which gets applied to results of this partial function. The action function is invoked only for its side effects; its result is ignored.

Type parameters: `` U ``.

Parameters, list 1: `` action: U => U ``.

Returns: `` T => Boolean ``.

Source contract/attributes: Composes this partial function with an action function which gets applied to results of this partial function. The action function is invoked only for its side effects; its result is ignored. Note that expression pf.runWith(action)(x) is equivalent to if(pf isDefinedAt x) { action(pf(x)); true } else false except that runWith is implemented via applyOrElse and thus potentially more efficient. Using runWith avoids double evaluation of pattern matchers and guards for partial function literals. @see applyOrElse. @param action the action function @return a function which maps arguments x to isDefinedAt(x). The resulting function runs action(this(x)) where this is defined. Attributes Inherited from: PartialFunction

Invocation template:

```scala
receiver.runWith[U](action)
```

## `` com.cra.figaro.util.PriorityMap.scan ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#scan-fffff9e1)

```scala
def scan[B >: (T, U)](z: B)(op: (B, B) => B): Iterable[B]
```

Computes a prefix scan of the elements of the collection.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, B) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Computes a prefix scan of the elements of the collection. Note: The neutral element z may be applied more than once. Type parameters B element type of the resulting collection Value parameters op the associative operator for the scan z neutral element for the operator op Attributes Returns a new mutable map containing the prefix scan of the elements in this mutable map Inherited from: IterableOps

Invocation template:

```scala
receiver.scan[B](z)(op)
```

## `` com.cra.figaro.util.PriorityMap.scanLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#scanLeft-fffff9e1)

```scala
def scanLeft[B](z: B)(op: (B, (T, U)) => B): Iterable[B]
```

Produces a mutable map containing cumulative results of applying the operator going left to right, including the initial value.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, (T, U)) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Produces a mutable map containing cumulative results of applying the operator going left to right, including the initial value. Note: will not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Type parameters B the type of the elements in the resulting collection Value parameters op the binary operator applied to the intermediate result and the element z the initial value Attributes Returns collection with intermediate results Inherited from: IterableOps

Invocation template:

```scala
receiver.scanLeft[B](z)(op)
```

## `` com.cra.figaro.util.PriorityMap.scanRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#scanRight-fffff9e1)

```scala
def scanRight[B](z: B)(op: ((T, U), B) => B): Iterable[B]
```

Produces a collection containing cumulative results of applying the operator going right to left. The head of the collection is the last cumulative result.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: ((T, U), B) => B ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Produces a collection containing cumulative results of applying the operator going right to left. The head of the collection is the last cumulative result. Note: will not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: Even when applied to a view or a lazy collection it will always force the elements. Example: List(1, 2, 3, 4).scanRight(0)(_ + _) == List(10, 9, 7, 4, 0) Type parameters B the type of the elements in the resulting collection Value parameters op the binary operator applied to the intermediate result and the element z the initial value Attributes Returns collection with intermediate results Inherited from: IterableOps

Invocation template:

```scala
receiver.scanRight[B](z)(op)
```

## `` com.cra.figaro.util.PriorityMap.size ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#size-0)

```scala
def size: Int
```

The size of this mutable map.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: The size of this mutable map. Note: will not terminate for infinite-sized collections. Attributes Returns the number of elements in this mutable map. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.size
```

## `` com.cra.figaro.util.PriorityMap.sizeCompare ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#sizeCompare-32d)

```scala
def sizeCompare(that: Iterable[_]): Int
```

Compares the size of this mutable map to the size of another Iterable.

Type parameters: none.

Parameters, list 1: `` that: Iterable[_] ``.

Returns: `` Int ``.

Source contract/attributes: Compares the size of this mutable map to the size of another Iterable. Value parameters that the Iterable whose size is compared with this mutable map's size. Attributes Returns A value x where x < 0 if this.size < that.size x == 0 if this.size == that.size x > 0 if this.size > that.size The method as implemented here does not call size directly; its running time is O(this.size min that.size) instead of O(this.size + that.size). The method should be overridden if computing size is cheap and knownSize returns -1. Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeCompare(that)
```

## `` com.cra.figaro.util.PriorityMap.sizeCompare ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#sizeCompare-fffffbe0)

```scala
def sizeCompare(otherSize: Int): Int
```

Compares the size of this mutable map to a test value.

Type parameters: none.

Parameters, list 1: `` otherSize: Int ``.

Returns: `` Int ``.

Source contract/attributes: Compares the size of this mutable map to a test value. Value parameters otherSize the test value that gets compared with the size. Attributes Returns A value x where x < 0 if this.size < otherSize x == 0 if this.size == otherSize x > 0 if this.size > otherSize The default implementation does not call size, which may be arbitrarily expensive. It will use knownSize if nonnegative. Otherwise, it will iterate to compute an element count, with complexity O(size min otherSize). This method should be overridden if computing size is cheap and knownSize returns -1. In that case, the custom implementation should just compare size and otherSize directly. See also sizeIs Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeCompare(otherSize)
```

## `` com.cra.figaro.util.PriorityMap.sizeHint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#sizeHint-fffff4f8)

```scala
final def sizeHint(coll: IterableOnce[_], delta: Int = ...): Unit
```

Gives a hint that the result of this builder is expected to have the same size as the given collection, plus some delta.

Type parameters: none.

Parameters, list 1: `` coll: IterableOnce[_] ``; `` delta: Int = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Gives a hint that the result of this builder is expected to have the same size as the given collection, plus some delta. This method provides a hint only if the collection has a known size, as specified by the following code: val coll: scala.collection.IterableOnce[?] = ??? val delta: Int = 0 def sizeHint(size: Int): Unit = () if (coll.knownSize != -1) if (coll.knownSize + delta <= 0) sizeHint(0) else sizeHint(coll.knownSize + delta) If the delta is negative and the result size is known to be negative, then the size hint is issued at zero. Some builder classes will optimize their representation based on the hint. However, builder implementations are required to work correctly even if the hint is wrong, i.e., if a different number of elements is added. Value parameters coll the collection which serves as a hint for the result's size. delta a correction to add to the coll.size to produce the size hint (zero if omitted). Attributes Inherited from: Builder

Invocation template:

```scala
receiver.sizeHint(coll, delta)
```

## `` com.cra.figaro.util.PriorityMap.sizeHint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#sizeHint-fffff3b3)

```scala
def sizeHint(size: Int): Unit
```

Gives a hint how many elements are expected to be added in total by the time result is called.

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Unit ``.

Source contract/attributes: Gives a hint how many elements are expected to be added in total by the time result is called. Some builder classes will optimize their representation based on the hint. However, builder implementations are required to work correctly even if the hint is wrong, e.g., a different number of elements is added, or the hint is out of range. The default implementation simply ignores the hint. Value parameters size the hint how many elements will be added. Attributes Inherited from: Builder

Invocation template:

```scala
receiver.sizeHint(size)
```

## `` com.cra.figaro.util.PriorityMap.sizeHintBounded ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#sizeHintBounded-fffffd7f)

```scala
final def sizeHintBounded(size: Int, boundingColl: Iterable[_]): Unit
```

Gives a hint how many elements are expected to be added when the next result is called, together with an upper bound given by the size of some other collection. Some builder classes will optimize their representation based on the hint. However, builder implementations are still required to work correctly even if the hint is wrong, i.e. a different number of elements is added.

Type parameters: none.

Parameters, list 1: `` size: Int ``; `` boundingColl: Iterable[_] ``.

Returns: `` Unit ``.

Source contract/attributes: Gives a hint how many elements are expected to be added when the next result is called, together with an upper bound given by the size of some other collection. Some builder classes will optimize their representation based on the hint. However, builder implementations are still required to work correctly even if the hint is wrong, i.e. a different number of elements is added. Value parameters boundingColl the bounding collection. If it is an IndexedSeqLike, then sizes larger than collection's size are reduced. size the hint how many elements will be added. Attributes Inherited from: Builder

Invocation template:

```scala
receiver.sizeHintBounded(size, boundingColl)
```

## `` com.cra.figaro.util.PriorityMap.sizeIs ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#sizeIs-0)

```scala
final def sizeIs: SizeCompareOps
```

Returns a value class containing operations for comparing the size of this mutable map to a test value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` SizeCompareOps ``.

Source contract/attributes: Returns a value class containing operations for comparing the size of this mutable map to a test value. These operations are implemented in terms of `sizeCompare(Int)`, and allow the following more readable usages: this.sizeIs < size // this.sizeCompare(size) < 0 this.sizeIs <= size // this.sizeCompare(size) <= 0 this.sizeIs == size // this.sizeCompare(size) == 0 this.sizeIs != size // this.sizeCompare(size) != 0 this.sizeIs >= size // this.sizeCompare(size) >= 0 this.sizeIs > size // this.sizeCompare(size) > 0 Attributes Returns a wrapper that allows size comparisons using operators such as =, and > Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeIs
```

## `` com.cra.figaro.util.PriorityMap.slice ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#slice-573)

```scala
def slice(from: Int, until: Int): Map[T, U]
```

Selects an interval of elements. The returned mutable map is made up of all elements x which satisfy the invariant:

Type parameters: none.

Parameters, list 1: `` from: Int ``; `` until: Int ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Selects an interval of elements. The returned mutable map is made up of all elements x which satisfy the invariant: from <= indexOf(x) < until Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters from the lowest index to include from this mutable map. until the lowest index to EXCLUDE from this mutable map. Attributes Returns a mutable map containing the elements greater than or equal to index from extending up to (but not including) index until of this mutable map. Example List('a', 'b', 'c', 'd', 'e').slice(1, 3) == List('b', 'c') Inherited from: IterableOps

Invocation template:

```scala
receiver.slice(from, until)
```

## `` com.cra.figaro.util.PriorityMap.sliding ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#sliding-fffff156)

```scala
def sliding(size: Int, step: Int): Iterator[Map[T, U]]
```

Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped).

Type parameters: none.

Parameters, list 1: `` size: Int ``; `` step: Int ``.

Returns: `` Iterator[Map[T, U]] ``.

Source contract/attributes: Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped). The returned iterator will be empty when called on an empty collection. The last element the iterator produces may be smaller than the window size when the original collection isn't exhausted by the window before it and its last element isn't skipped by the step before it. Value parameters size the number of elements per group step the distance between the first elements of successive groups Attributes Returns An iterator producing mutable maps of size size, except the last element (which may be the only element) will be smaller if there are fewer than size elements remaining to be grouped. See also scala.collection.Iterator, method sliding Example List(1, 2, 3, 4, 5).sliding(2, 2) = Iterator(List(1, 2), List(3, 4), List(5)) List(1, 2, 3, 4, 5, 6).sliding(2, 3) = Iterator(List(1, 2), List(4, 5)) Inherited from: IterableOps

Invocation template:

```scala
receiver.sliding(size, step)
```

## `` com.cra.figaro.util.PriorityMap.sliding ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#sliding-fffffbef)

```scala
def sliding(size: Int): Iterator[Map[T, U]]
```

Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped).

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Iterator[Map[T, U]] ``.

Source contract/attributes: Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped). An empty collection returns an empty iterator, and a non-empty collection containing fewer elements than the window size returns an iterator that will produce the original collection as its only element. Value parameters size the number of elements per group Attributes Returns An iterator producing mutable maps of size size, except for a non-empty collection with less than size elements, which returns an iterator that produces the source collection itself as its only element. See also scala.collection.Iterator, method sliding Example List().sliding(2) = empty iterator List(1).sliding(2) = Iterator(List(1)) List(1, 2).sliding(2) = Iterator(List(1, 2)) List(1, 2, 3).sliding(2) = Iterator(List(1, 2), List(2, 3)) Inherited from: IterableOps

Invocation template:

```scala
receiver.sliding(size)
```

## `` com.cra.figaro.util.PriorityMap.span ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#span-243)

```scala
def span(p: (T, U) => Boolean): (Map[T, U], Map[T, U])
```

Splits this mutable map into a prefix/suffix pair according to a predicate.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` (Map[T, U], Map[T, U]) ``.

Source contract/attributes: Splits this mutable map into a prefix/suffix pair according to a predicate. Note: c span p is equivalent to (but possibly more efficient than) (c takeWhile p, c dropWhile p), provided the evaluation of the predicate p does not cause any side effects. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the test predicate Attributes Returns a pair consisting of the longest prefix of this mutable map whose elements all satisfy p, and the rest of this mutable map. Inherited from: IterableOps

Invocation template:

```scala
receiver.span(p)
```

## `` com.cra.figaro.util.PriorityMap.splitAt ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#splitAt-fffffbd9)

```scala
override def splitAt(n: Int): (Map[T, U], Map[T, U])
```

Splits this mutable map into a prefix/suffix pair at a given position.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` (Map[T, U], Map[T, U]) ``.

Source contract/attributes: Splits this mutable map into a prefix/suffix pair at a given position. Note: c splitAt n is equivalent to (but possibly more efficient than) (c take n, c drop n). Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the position at which to split. Attributes Returns a pair of mutable maps consisting of the first n elements of this mutable map, and the other elements. Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.splitAt(n)
```

## `` com.cra.figaro.util.PriorityMap.stepper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#stepper-190)

```scala
def stepper[S <: Stepper[_]](implicit shape: StepperShape[(T, U), S]): S
```

Returns a scala.collection.Stepper for the elements of this collection.

Type parameters: `` S <: Stepper[_] ``.

Context parameters, list 1: `` shape: StepperShape[(T, U), S] ``.

Returns: `` S ``.

Source contract/attributes: Returns a scala.collection.Stepper for the elements of this collection. The Stepper enables creating a Java stream to operate on the collection, see scala.jdk.StreamConverters. For collections holding primitive values, the Stepper can be used as an iterator which doesn't box the elements. The implicit scala.collection.StepperShape parameter defines the resulting Stepper type according to the element type of this collection. For collections of Int, Short, Byte or Char, an scala.collection.IntStepper is returned For collections of Double or Float, a scala.collection.DoubleStepper is returned For collections of Long a scala.collection.LongStepper is returned For any other element type, an scala.collection.AnyStepper is returned Note that this method is overridden in subclasses and the return type is refined to S with EfficientSplit, for example scala.collection.IndexedSeqOps.stepper. For Steppers marked with scala.collection.Stepper.EfficientSplit, the converters in scala.jdk.StreamConverters allow creating parallel streams, whereas bare Steppers can be converted only to sequential streams. Type parameters S the type of the returned Stepper, determined by the implicit StepperShape Attributes Inherited from: IterableOnce

Invocation template:

```scala
receiver.stepper[S](using shape)
```

## `` com.cra.figaro.util.PriorityMap.subtractAll ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#subtractAll-fffff8b8)

```scala
def subtractAll(xs: IterableOnce[T]): this.type
```

Removes all elements produced by an iterator from this mutable map.

Type parameters: none.

Parameters, list 1: `` xs: IterableOnce[T] ``.

Returns: `` this.type ``.

Source contract/attributes: Removes all elements produced by an iterator from this mutable map. Value parameters xs the iterator producing the elements to remove. Attributes Returns the mutable map itself Inherited from: Shrinkable

Invocation template:

```scala
receiver.subtractAll(xs)
```

## `` com.cra.figaro.util.PriorityMap.sum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#sum-533)

```scala
def sum[B >: (T, U)](implicit num: Numeric[B]): B
```

Sums the elements of this collection.

Type parameters: `` B >: (T, U) ``.

Context parameters, list 1: `` num: Numeric[B] ``.

Returns: `` B ``.

Source contract/attributes: Sums the elements of this collection. The default implementation uses reduce for a known non-empty collection, foldLeft otherwise. Note: will not terminate for infinite-sized collections. Type parameters B the result type of the + operator. Value parameters num an implicit parameter defining a set of numeric operations which includes the + operator to be used in forming the sum. Attributes Returns the sum of all elements of this mutable map with respect to the + operator in num. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.sum[B](using num)
```

## `` com.cra.figaro.util.PriorityMap.tail ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#tail-0)

```scala
def tail: Map[T, U]
```

The rest of the collection without its first element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Map[T, U] ``.

Source contract/attributes: The rest of the collection without its first element. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.tail
```

## `` com.cra.figaro.util.PriorityMap.tails ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#tails-0)

```scala
def tails: Iterator[Map[T, U]]
```

Iterates over the tails of this mutable map. The first value will be this mutable map and the final one will be an empty mutable map, with the intervening values the results of successive applications of tail.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Map[T, U]] ``.

Source contract/attributes: Iterates over the tails of this mutable map. The first value will be this mutable map and the final one will be an empty mutable map, with the intervening values the results of successive applications of tail. Attributes Returns an iterator over all the tails of this mutable map Example List(1,2,3).tails = Iterator(List(1,2,3), List(2,3), List(3), Nil) Inherited from: IterableOps

Invocation template:

```scala
receiver.tails
```

## `` com.cra.figaro.util.PriorityMap.take ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#take-aba)

```scala
def take(n: Int): Map[T, U]
```

Selects the first n elements.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Selects the first n elements. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to take from this mutable map. Attributes Returns a mutable map consisting only of the first n elements of this mutable map, or else the whole mutable map, if it has less than n elements. If n is negative, returns an empty mutable map. Inherited from: IterableOps

Invocation template:

```scala
receiver.take(n)
```

## `` com.cra.figaro.util.PriorityMap.takeRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#takeRight-aba)

```scala
def takeRight(n: Int): Map[T, U]
```

Selects the last n elements.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Selects the last n elements. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to take from this mutable map. Attributes Returns a mutable map consisting only of the last n elements of this mutable map, or else the whole mutable map, if it has less than n elements. If n is negative, returns an empty mutable map. Inherited from: IterableOps

Invocation template:

```scala
receiver.takeRight(n)
```

## `` com.cra.figaro.util.PriorityMap.takeWhile ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#takeWhile-c24)

```scala
def takeWhile(p: (T, U) => Boolean): Map[T, U]
```

Takes longest prefix of elements that satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Takes longest prefix of elements that satisfy a predicate. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p The predicate used to test elements. Attributes Returns the longest prefix of this mutable map whose elements all satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.takeWhile(p)
```

## `` com.cra.figaro.util.PriorityMap.tapEach ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#tapEach-5d3)

```scala
override def tapEach[U](f: (T, U) => U): Map[T, U]
```

Applies a side-effecting function to each element in this collection. Strict collections will apply f to their elements immediately, while lazy collections like Views and LazyLists will only apply f on each element if and when that element is evaluated, and each time that element is evaluated.

Type parameters: `` U ``.

Parameters, list 1: `` f: (T, U) => U ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Applies a side-effecting function to each element in this collection. Strict collections will apply f to their elements immediately, while lazy collections like Views and LazyLists will only apply f on each element if and when that element is evaluated, and each time that element is evaluated. Type parameters U the return type of f Value parameters f a function to apply to each element in this mutable map Attributes Returns The same logical collection as this Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.tapEach[U](f)
```

## `` com.cra.figaro.util.PriorityMap.to ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#to-fffff006)

```scala
def to[C1](factory: Factory[(T, U), C1]): C1
```

Given a collection factory factory, converts this mutable map to the appropriate representation for the current element type A. Example uses:

Type parameters: `` C1 ``.

Parameters, list 1: `` factory: Factory[(T, U), C1] ``.

Returns: `` C1 ``.

Source contract/attributes: Given a collection factory factory, converts this mutable map to the appropriate representation for the current element type A. Example uses: import scala.collection.mutable.ArrayBuffer import scala.collection.immutable.BitSet val xs: Iterable[Int] = Seq(1, 2, 3, 4, 5) xs.to(List) xs.to(ArrayBuffer) xs.to(BitSet) // for xs: Iterable[Int] Type parameters C1 the target collection type Value parameters factory the factory for the target collection type Attributes Returns a new collection of type C1 containing all elements of this mutable map Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.to[C1](factory)
```

## `` com.cra.figaro.util.PriorityMap.toArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#toArray-fffff5df)

```scala
def toArray[B >: (T, U) : ClassTag]: Array[B]
```

Converts this mutable map to an Array.

Type parameters: `` B >: (T, U) : ClassTag ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Array[B] ``.

Source contract/attributes: Converts this mutable map to an Array. Implementation note: DO NOT call Array.from from this method. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This mutable map as an Array[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toArray[B]
```

## `` com.cra.figaro.util.PriorityMap.toBuffer ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#toBuffer-fffff87f)

```scala
final def toBuffer[B >: (T, U)]: Buffer[B]
```

Converts this mutable map to a Buffer.

Type parameters: `` B >: (T, U) ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Buffer[B] ``.

Source contract/attributes: Converts this mutable map to a Buffer. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This mutable map as a Buffer[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toBuffer[B]
```

## `` com.cra.figaro.util.PriorityMap.toIndexedSeq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#toIndexedSeq-0)

```scala
def toIndexedSeq: IndexedSeq[(T, U)]
```

Converts this mutable map to an IndexedSeq.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IndexedSeq[(T, U)] ``.

Source contract/attributes: Converts this mutable map to an IndexedSeq. Attributes Returns This mutable map as an IndexedSeq[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toIndexedSeq
```

## `` com.cra.figaro.util.PriorityMap.toList ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#toList-0)

```scala
def toList: List[(T, U)]
```

Converts this mutable map to a List.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(T, U)] ``.

Source contract/attributes: Converts this mutable map to a List. Attributes Returns This mutable map as a List[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toList
```

## `` com.cra.figaro.util.PriorityMap.toMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#toMap-c0c)

```scala
def toMap[K, V](implicit ev: (T, U) <:< (K, V)): Map[K, V]
```

Converts this mutable map to a Map, given an implicit coercion from the mutable map's type to a key-value tuple.

Type parameters: `` K ``; `` V ``.

Context parameters, list 1: `` ev: (T, U) <:< (K, V) ``.

Returns: `` Map[K, V] ``.

Source contract/attributes: Converts this mutable map to a Map, given an implicit coercion from the mutable map's type to a key-value tuple. Type parameters K The key type for the resulting map. V The value type for the resulting map. Value parameters ev an implicit evidence that A is a subtype of (K, V) Attributes Returns This mutable map as a Map[K, V]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toMap[K, V](using ev)
```

## `` com.cra.figaro.util.PriorityMap.toSeq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#toSeq-0)

```scala
def toSeq: Seq[(T, U)]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Seq[(T, U)] ``.

Source contract/attributes: Attributes Returns This mutable map as a Seq[A]. This is equivalent to to(Seq) but might be faster. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toSeq
```

## `` com.cra.figaro.util.PriorityMap.toSet ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#toSet-fffffe9f)

```scala
def toSet[B >: (T, U)]: Set[B]
```

Converts this mutable map to a Set.

Type parameters: `` B >: (T, U) ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[B] ``.

Source contract/attributes: Converts this mutable map to a Set. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This mutable map as a Set[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toSet[B]
```

## `` com.cra.figaro.util.PriorityMap.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#toString-665)

```scala
override def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Map -> Function1 -> Iterable -> Any Inherited from: Map

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.util.PriorityMap.toVector ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#toVector-0)

```scala
def toVector: Vector[(T, U)]
```

Converts this mutable map to a Vector.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Vector[(T, U)] ``.

Source contract/attributes: Converts this mutable map to a Vector. Attributes Returns This mutable map as a Vector[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toVector
```

## `` com.cra.figaro.util.PriorityMap.transpose ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#transpose-5d3)

```scala
def transpose[B](implicit asIterable: (T, U) => Iterable[B]): Iterable[Iterable[B]]
```

Transposes this mutable map of iterable collections into a mutable map of mutable maps.

Type parameters: `` B ``.

Context parameters, list 1: `` asIterable: (T, U) => Iterable[B] ``.

Returns: `` Iterable[Iterable[B]] ``.

Source contract/attributes: Transposes this mutable map of iterable collections into a mutable map of mutable maps. The resulting collection's type will be guided by the static type of mutable map. For example: val xs = List( Set(1, 2, 3), Set(4, 5, 6)).transpose // xs == List( // List(1, 4), // List(2, 5), // List(3, 6)) val ys = Vector( List(1, 2, 3), List(4, 5, 6)).transpose // ys == Vector( // Vector(1, 4), // Vector(2, 5), // Vector(3, 6)) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of the elements of each iterable collection. Value parameters asIterable an implicit conversion which asserts that the element type of this mutable map is an Iterable. Attributes Returns a two-dimensional mutable map of mutable maps which has as nth row the nth column of this mutable map. Throws IllegalArgumentException if all collections in this mutable map are not of the same size. Inherited from: IterableOps

Invocation template:

```scala
receiver.transpose[B](using asIterable)
```

## `` com.cra.figaro.util.PriorityMap.unapply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#unapply-fffffb50)

```scala
def unapply(a: T): Option[U]
```

Tries to extract a B from an A in a pattern matching expression.

Type parameters: none.

Parameters, list 1: `` a: T ``.

Returns: `` Option[U] ``.

Source contract/attributes: Tries to extract a B from an A in a pattern matching expression. Attributes Inherited from: PartialFunction

Invocation template:

```scala
receiver.unapply(a)
```

## `` com.cra.figaro.util.PriorityMap.unzip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#unzip-2d1)

```scala
def unzip[A1, A2](implicit asPair: (T, U) => (A1, A2)): (Iterable[A1], Iterable[A2])
```

Converts this mutable map of pairs into two collections of the first and second half of each pair.

Type parameters: `` A1 ``; `` A2 ``.

Context parameters, list 1: `` asPair: (T, U) => (A1, A2) ``.

Returns: `` (Iterable[A1], Iterable[A2]) ``.

Source contract/attributes: Converts this mutable map of pairs into two collections of the first and second half of each pair. val xs = $Coll( (1, "one"), (2, "two"), (3, "three")).unzip // xs == ($Coll(1, 2, 3), // $Coll(one, two, three)) Type parameters A1 the type of the first half of the element pairs A2 the type of the second half of the element pairs Value parameters asPair an implicit conversion which asserts that the element type of this mutable map is a pair. Attributes Returns a pair of mutable maps, containing the first, respectively second half of each element pair of this mutable map. Inherited from: IterableOps

Invocation template:

```scala
receiver.unzip[A1, A2](using asPair)
```

## `` com.cra.figaro.util.PriorityMap.unzip3 ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#unzip3-a31)

```scala
def unzip3[A1, A2, A3](implicit asTriple: (T, U) => (A1, A2, A3)): (Iterable[A1], Iterable[A2], Iterable[A3])
```

Converts this mutable map of triples into three collections of the first, second, and third element of each triple.

Type parameters: `` A1 ``; `` A2 ``; `` A3 ``.

Context parameters, list 1: `` asTriple: (T, U) => (A1, A2, A3) ``.

Returns: `` (Iterable[A1], Iterable[A2], Iterable[A3]) ``.

Source contract/attributes: Converts this mutable map of triples into three collections of the first, second, and third element of each triple. val xs = $Coll( (1, "one", '1'), (2, "two", '2'), (3, "three", '3')).unzip3 // xs == ($Coll(1, 2, 3), // $Coll(one, two, three), // $Coll(1, 2, 3)) Type parameters A1 the type of the first member of the element triples A2 the type of the second member of the element triples A3 the type of the third member of the element triples Value parameters asTriple an implicit conversion which asserts that the element type of this mutable map is a triple. Attributes Returns a triple of mutable maps, containing the first, second, respectively third member of each element triple of this mutable map. Inherited from: IterableOps

Invocation template:

```scala
receiver.unzip3[A1, A2, A3](using asTriple)
```

## `` com.cra.figaro.util.PriorityMap.update ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#update-ef2)

```scala
def update(key: T, value: U): Unit
```

Adds a new key/value pair to this map. If the map already contains a mapping for the key, it will be overridden by the new value.

Type parameters: none.

Parameters, list 1: `` key: T ``; `` value: U ``.

Returns: `` Unit ``.

Source contract/attributes: Adds a new key/value pair to this map. If the map already contains a mapping for the key, it will be overridden by the new value. Value parameters key The key to update value The new value Attributes Inherited from: MapOps

Invocation template:

```scala
receiver.update(key, value)
```

## `` com.cra.figaro.util.PriorityMap.updateWith ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#updateWith-fffffbdb)

```scala
def updateWith(key: T)(remappingFunction: Option[U] => Option[U]): Option[U]
```

Updates a mapping for the specified key and its current optionally mapped value (Some if there is current mapping, None if not).

Type parameters: none.

Parameters, list 1: `` key: T ``.

Parameters, list 2: `` remappingFunction: Option[U] => Option[U] ``.

Returns: `` Option[U] ``.

Source contract/attributes: Updates a mapping for the specified key and its current optionally mapped value (Some if there is current mapping, None if not). If the remapping function returns Some(v), the mapping is updated with the new value v. If the remapping function returns None, the mapping is removed (or remains absent if initially absent). If the function itself throws an exception, the exception is rethrown, and the current mapping is left unchanged. Value parameters key the key value remappingFunction a function that receives current optionally mapped value and returns a new mapping Attributes Returns the new value associated with the specified key Inherited from: MapOps

Invocation template:

```scala
receiver.updateWith(key)(remappingFunction)
```

## `` com.cra.figaro.util.PriorityMap.valueStepper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#valueStepper-190)

```scala
def valueStepper[S <: Stepper[_]](implicit shape: StepperShape[U, S]): S
```

Returns a Stepper for the values of this map. See method stepper.

Type parameters: `` S <: Stepper[_] ``.

Context parameters, list 1: `` shape: StepperShape[U, S] ``.

Returns: `` S ``.

Source contract/attributes: Returns a Stepper for the values of this map. See method stepper. Type parameters S the type of Stepper to use, determined by the implicit StepperShape Attributes Inherited from: MapOps

Invocation template:

```scala
receiver.valueStepper[S](using shape)
```

## `` com.cra.figaro.util.PriorityMap.values ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#values-0)

```scala
def values: Iterable[U]
```

Collects all values of this map in an iterable collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[U] ``.

Source contract/attributes: Collects all values of this map in an iterable collection. Attributes Returns the values of this map as an iterable. Inherited from: MapOps

Invocation template:

```scala
receiver.values
```

## `` com.cra.figaro.util.PriorityMap.valuesIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#valuesIterator-0)

```scala
def valuesIterator: Iterator[U]
```

Creates an iterator for all values in this map.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[U] ``.

Source contract/attributes: Creates an iterator for all values in this map. Attributes Returns an iterator over all values that are associated with some key in this map. Inherited from: MapOps

Invocation template:

```scala
receiver.valuesIterator
```

## `` com.cra.figaro.util.PriorityMap.view ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#view-0)

```scala
override def view: MapView[T, U]
```

A view over the elements of this collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` MapView[T, U] ``.

Source contract/attributes: A view over the elements of this collection. Attributes Definition Classes MapOps -> IterableOps Inherited from: MapOps

Invocation template:

```scala
receiver.view
```

## `` com.cra.figaro.util.PriorityMap.withDefault ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#withDefault-fffff5dd)

```scala
def withDefault(d: T => U): Map[T, U]
```

The same map with a given default function. Note: The default is only used for apply. Other methods like get, contains, iterator, keys, etc. are not affected by withDefaultValue.

Type parameters: none.

Parameters, list 1: `` d: T => U ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: The same map with a given default function. Note: The default is only used for apply. Other methods like get, contains, iterator, keys, etc. are not affected by withDefaultValue. Invoking transformer methods (e.g. map) will not preserve the default value. Value parameters d the function mapping keys to values, used for non-present keys Attributes Returns a wrapper of the map with a default value Inherited from: Map

Invocation template:

```scala
receiver.withDefault(d)
```

## `` com.cra.figaro.util.PriorityMap.withDefaultValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#withDefaultValue-fffff3df)

```scala
def withDefaultValue(d: U): Map[T, U]
```

The same map with a given default value. Note: The default is only used for apply. Other methods like get, contains, iterator, keys, etc. are not affected by withDefaultValue.

Type parameters: none.

Parameters, list 1: `` d: U ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: The same map with a given default value. Note: The default is only used for apply. Other methods like get, contains, iterator, keys, etc. are not affected by withDefaultValue. Invoking transformer methods (e.g. map) will not preserve the default value. Value parameters d default value used for non-present keys Attributes Returns a wrapper of the map with a default value Inherited from: Map

Invocation template:

```scala
receiver.withDefaultValue(d)
```

## `` com.cra.figaro.util.PriorityMap.withFilter ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#withFilter-fffff355)

```scala
override def withFilter(p: ((T, U)) => Boolean): WithFilter[T, U, Iterable, Map]
```

Creates a non-strict filter of this mutable map.

Type parameters: none.

Parameters, list 1: `` p: ((T, U)) => Boolean ``.

Returns: `` WithFilter[T, U, Iterable, Map] ``.

Source contract/attributes: Creates a non-strict filter of this mutable map. Note: the difference between c filter p and c withFilter p is that the former creates a new collection, whereas the latter only restricts the domain of subsequent map, flatMap, foreach, and withFilter operations. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the predicate used to test elements. Attributes Returns an object of class WithFilter, which supports map, flatMap, foreach, and withFilter operations. All these operations apply to those elements of this mutable map which satisfy the predicate p. Definition Classes MapFactoryDefaults -> IterableOps Inherited from: MapFactoryDefaults

Invocation template:

```scala
receiver.withFilter(p)
```

## `` com.cra.figaro.util.PriorityMap.zip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#zip-1dd)

```scala
def zip[B](that: IterableOnce[B]): Iterable[((T, U), B)]
```

Returns a mutable map formed from this mutable map and another iterable collection by combining corresponding elements in pairs. If one of the two collections is longer than the other, its remaining elements are ignored.

Type parameters: `` B ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Returns: `` Iterable[((T, U), B)] ``.

Source contract/attributes: Returns a mutable map formed from this mutable map and another iterable collection by combining corresponding elements in pairs. If one of the two collections is longer than the other, its remaining elements are ignored. Type parameters B the type of the second half of the returned pairs Value parameters that The iterable providing the second half of each result pair Attributes Returns a new mutable map containing pairs consisting of corresponding elements of this mutable map and that. The length of the returned collection is the minimum of the lengths of this mutable map and that. Inherited from: IterableOps

Invocation template:

```scala
receiver.zip[B](that)
```

## `` com.cra.figaro.util.PriorityMap.zipAll ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#zipAll-fffff401)

```scala
def zipAll[A1 >: (T, U), B](that: Iterable[B], thisElem: A1, thatElem: B): Iterable[(A1, B)]
```

Returns a mutable map formed from this mutable map and another iterable collection by combining corresponding elements in pairs. If one of the two collections is shorter than the other, placeholder elements are used to extend the shorter collection to the length of the longer.

Type parameters: `` A1 >: (T, U) ``; `` B ``.

Parameters, list 1: `` that: Iterable[B] ``; `` thisElem: A1 ``; `` thatElem: B ``.

Returns: `` Iterable[(A1, B)] ``.

Source contract/attributes: Returns a mutable map formed from this mutable map and another iterable collection by combining corresponding elements in pairs. If one of the two collections is shorter than the other, placeholder elements are used to extend the shorter collection to the length of the longer. Type parameters A1 the type of the first element in each result pair (supertype of A) B the type of the second element in each result pair Value parameters that the iterable providing the second half of each result pair thatElem the element to be used to fill up the result if that is shorter than this mutable map. thisElem the element to be used to fill up the result if this mutable map is shorter than that. Attributes Returns a new mutable map containing pairs consisting of corresponding elements of this mutable map and that. The length of the returned collection is the maximum of the lengths of this mutable map and that. If this mutable map is shorter than that, thisElem values are used to pad the result. If that is shorter than this mutable map, thatElem values are used to pad the result. Inherited from: IterableOps

Invocation template:

```scala
receiver.zipAll[A1, B](that, thisElem, thatElem)
```

## `` com.cra.figaro.util.PriorityMap.zipWithIndex ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#zipWithIndex-0)

```scala
def zipWithIndex: Iterable[((T, U), Int)]
```

Zips this mutable map with its indices.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[((T, U), Int)] ``.

Source contract/attributes: Zips this mutable map with its indices. Attributes Returns A new mutable map containing pairs consisting of all elements of this mutable map paired with their index. Indices start at 0. Example List("a", "b", "c").zipWithIndex == List(("a", 0), ("b", 1), ("c", 2)) Inherited from: IterableOps

Invocation template:

```scala
receiver.zipWithIndex
```

## `` com.cra.figaro.util.PriorityMap.addOne ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#addOne-b4a)

```scala
def addOne(elem: (T, U)): this.type
```

Add s a single element to this mutable map.

Type parameters: none.

Parameters, list 1: `` elem: (T, U) ``.

Returns: `` this.type ``.

Source contract/attributes: Add s a single element to this mutable map. Value parameters elem the element to add. Attributes Returns the mutable map itself Inherited from: Growable

Invocation template:

```scala
receiver.addOne(elem)
```

## `` com.cra.figaro.util.PriorityMap.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#get-fffffb50)

```scala
def get(key: T): Option[U]
```

Optionally returns the value associated with a key.

Type parameters: none.

Parameters, list 1: `` key: T ``.

Returns: `` Option[U] ``.

Source contract/attributes: Optionally returns the value associated with a key. Value parameters key the key value Attributes Returns an option value containing the value associated with key in this map, or None if none exists. Inherited from: MapOps

Invocation template:

```scala
receiver.get(key)
```

## `` com.cra.figaro.util.PriorityMap.iterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#iterator-0)

```scala
def iterator: Iterator[(T, U)]
```

An scala.collection.Iterator over the elements of this mutable map.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[(T, U)] ``.

Source contract/attributes: An scala.collection.Iterator over the elements of this mutable map. If an IterableOnce object is in fact an scala.collection.Iterator, this method always returns itself, in its current state, but if it is an scala.collection.Iterable, this method always returns a new scala.collection.Iterator. Attributes Returns an iterator over all elements of this mutable map Inherited from: IterableOnce

Invocation template:

```scala
receiver.iterator
```

## `` com.cra.figaro.util.PriorityMap.subtractOne ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#subtractOne-fffff700)

```scala
def subtractOne(elem: T): this.type
```

Removes a single element from this mutable map.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` this.type ``.

Source contract/attributes: Removes a single element from this mutable map. Value parameters elem the element to remove. Attributes Returns the mutable map itself Inherited from: Shrinkable

Invocation template:

```scala
receiver.subtractOne(elem)
```

## `` com.cra.figaro.util.PriorityMap.+ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#+-f18)

```scala
def +[V1 >: U](elem1: (T, V1), elem2: (T, V1), elems: (T, V1)*): Map[T, V1]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` V1 >: U ``.

Parameters, list 1: `` elem1: (T, V1) ``; `` elem2: (T, V1) ``; `` elems: (T, V1)* ``.

Returns: `` Map[T, V1] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use ++ with an explicit collection argument instead of + with varargs Inherited from: MapOps

Invocation template:

```scala
receiver.+[V1](elem1, elem2, elems*)
```

## `` com.cra.figaro.util.PriorityMap.+ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#+-fffff27b)

```scala
def +[V1 >: U](kv: (T, V1)): Map[T, V1]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` V1 >: U ``.

Parameters, list 1: `` kv: (T, V1) ``.

Returns: `` Map[T, V1] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Consider requiring an immutable Map or fall back to Map.concat. Inherited from: MapOps

Invocation template:

```scala
receiver.+[V1](kv)
```

## `` com.cra.figaro.util.PriorityMap.++: ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#++:-1dd)

```scala
def ++:[B >: (T, U)](that: IterableOnce[B]): Iterable[B]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Returns: `` Iterable[B] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use ++ instead of ++: for collections of type Iterable Inherited from: IterableOps

Invocation template:

```scala
receiver.++:[B](that)
```

## `` com.cra.figaro.util.PriorityMap.++: ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#++:-62)

```scala
def ++:[V1 >: U](that: IterableOnce[(T, V1)]): Map[T, V1]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` V1 >: U ``.

Parameters, list 1: `` that: IterableOnce[(T, V1)] ``.

Returns: `` Map[T, V1] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use ++ instead of ++: for collections of type Iterable Inherited from: MapOps

Invocation template:

```scala
receiver.++:[V1](that)
```

## `` com.cra.figaro.util.PriorityMap.+= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#+=-fffff3cc)

```scala
final def +=(elem1: (T, U), elem2: (T, U), elems: (T, U)*): this.type
```

Add s two or more elements to this mutable map.

Type parameters: none.

Parameters, list 1: `` elem1: (T, U) ``; `` elem2: (T, U) ``; `` elems: (T, U)* ``.

Returns: `` this.type ``.

Source contract/attributes: Add s two or more elements to this mutable map. Value parameters elem1 the first element to add. elem2 the second element to add. elems the remaining elements to add. Attributes Returns the mutable map itself Deprecated [Since version 2.13.0] Use `++=` aka `addAll` instead of varargs `+=`; infix operations with an operand of multiple args will be deprecated Inherited from: Growable

Invocation template:

```scala
receiver.+=(elem1, elem2, elems*)
```

## `` com.cra.figaro.util.PriorityMap.- ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#--fffff355)

```scala
final def -(key1: T, key2: T, keys: T*): Map[T, U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` key1: T ``; `` key2: T ``; `` keys: T* ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use -- or removeAll on an immutable Map Inherited from: MapOps

Invocation template:

```scala
receiver.-(key1, key2, keys*)
```

## `` com.cra.figaro.util.PriorityMap.- ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#--253)

```scala
final def -(key: T): Map[T, U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` key: T ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use - or remove on an immutable Map Inherited from: MapOps

Invocation template:

```scala
receiver.-(key)
```

## `` com.cra.figaro.util.PriorityMap.-- ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#---fffff66e)

```scala
def --(keys: IterableOnce[T]): Map[T, U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` keys: IterableOnce[T] ``.

Returns: `` Map[T, U] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Consider requiring an immutable Map. Inherited from: MapOps

Invocation template:

```scala
receiver.--(keys)
```

## `` com.cra.figaro.util.PriorityMap.-= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#-=-fffff702)

```scala
def -=(elem1: T, elem2: T, elems: T*): this.type
```

Removes two or more elements from this mutable map.

Type parameters: none.

Parameters, list 1: `` elem1: T ``; `` elem2: T ``; `` elems: T* ``.

Returns: `` this.type ``.

Source contract/attributes: Removes two or more elements from this mutable map. Value parameters elem1 the first element to remove. elem2 the second element to remove. elems the remaining elements to remove. Attributes Returns the mutable map itself Deprecated [Since version 2.13.3] Use `--=` aka `subtractAll` instead of varargs `-=`; infix operations with an operand of multiple args will be deprecated Inherited from: Shrinkable

Invocation template:

```scala
receiver.-=(elem1, elem2, elems*)
```

## `` com.cra.figaro.util.PriorityMap./: ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#/:-fffff9e1)

```scala
final def /:[B](z: B)(op: (B, (T, U)) => B): B
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, (T, U)) => B ``.

Returns: `` B ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use foldLeft instead of /: Inherited from: IterableOnceOps

Invocation template:

```scala
receiver./:[B](z)(op)
```

## `` com.cra.figaro.util.PriorityMap.:\ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#:\-fffff9e1)

```scala
final def :\[B](z: B)(op: ((T, U), B) => B): B
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: ((T, U), B) => B ``.

Returns: `` B ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use foldRight instead of :\\ Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.:\[B](z)(op)
```

## `` com.cra.figaro.util.PriorityMap.aggregate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#aggregate-952)

```scala
def aggregate[B](z: => B)(seqop: (B, (T, U)) => B, combop: (B, B) => B): B
```

Aggregates the results of applying an operator to subsequent elements.

Type parameters: `` B ``.

Parameters, list 1: `` z: => B ``.

Parameters, list 2: `` seqop: (B, (T, U)) => B ``; `` combop: (B, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Aggregates the results of applying an operator to subsequent elements. Since this method degenerates to foldLeft for sequential (non-parallel) collections, where the combining operation is ignored, it is advisable to prefer foldLeft for that case. For parallel collections, use the aggregate method specified by scala.collection.parallel.ParIterableLike. Type parameters B the result type, produced by seqop, combop, and by this function as a final result. Value parameters combop an associative operator for combining sequential results, unused for sequential collections. seqop the binary operator used to accumulate the result. z the start value, a neutral element for seqop. Attributes Deprecated [Since version 2.13.0] For sequential collections, prefer `foldLeft(z)(seqop)`. For parallel collections, use `ParIterableLike#aggregate`. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.aggregate[B](z)(seqop, combop)
```

## `` com.cra.figaro.util.PriorityMap.companion ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#companion-0)

```scala
def companion: IterableFactory[Iterable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IterableFactory[Iterable] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use iterableFactory instead Inherited from: IterableOps

Invocation template:

```scala
receiver.companion
```

## `` com.cra.figaro.util.PriorityMap.copyToBuffer ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#copyToBuffer-f0b)

```scala
final def copyToBuffer[B >: (T, U)](dest: Buffer[B]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B >: (T, U) ``.

Parameters, list 1: `` dest: Buffer[B] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use `dest ++= coll` instead Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToBuffer[B](dest)
```

## `` com.cra.figaro.util.PriorityMap.filterKeys ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#filterKeys-5ea)

```scala
def filterKeys(p: T => Boolean): MapView[T, U]
```

Filters this map by retaining only keys satisfying a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` MapView[T, U] ``.

Source contract/attributes: Filters this map by retaining only keys satisfying a predicate. Value parameters p the predicate used to test keys Attributes Returns an immutable map consisting only of those key value pairs of this map where the key satisfies the predicate p. The resulting map wraps the original map without copying any elements. Deprecated [Since version 2.13.0] Use .view.filterKeys(f). A future version will include a strict version of this method (for now, .view.filterKeys(p).toMap). Inherited from: MapOps

Invocation template:

```scala
receiver.filterKeys(p)
```

## `` com.cra.figaro.util.PriorityMap.hasDefiniteSize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#hasDefiniteSize-0)

```scala
def hasDefiniteSize: Boolean
```

Tests whether this mutable map is known to have a finite size. All strict collections are known to have finite size. For a non-strict collection such as Stream, the predicate returns **true** if all elements have been computed. It returns **false** if the stream is not yet evaluated to the end. Non-empty Iterators usually return **false** even if they were created from a collection with a known finite size.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this mutable map is known to have a finite size. All strict collections are known to have finite size. For a non-strict collection such as Stream, the predicate returns **true** if all elements have been computed. It returns **false** if the stream is not yet evaluated to the end. Non-empty Iterators usually return **false** even if they were created from a collection with a known finite size. Note: many collection methods will not work on collections of infinite sizes. The typical failure mode is an infinite loop. These methods always attempt a traversal without checking first that hasDefiniteSize returns **true**. However, checking hasDefiniteSize can provide an assurance that size is well-defined and non-termination is not a concern. Attributes Returns **true** if this collection is known to have finite size, **false** otherwise. See also method knownSize for a more useful alternative Deprecated [Since version 2.13.0] Check .knownSize instead of .hasDefiniteSize for more actionable information (see scaladoc for details) This method is deprecated in 2.13 because it does not provide any actionable information. As noted above, even the collection library itself does not use it. When there is no guarantee that a collection is finite, it is generally best to attempt a computation anyway and document that it will not terminate for infinite collections rather than backing out because this would prevent performing the computation on collections that are in fact finite even though hasDefiniteSize returns false. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.hasDefiniteSize
```

## `` com.cra.figaro.util.PriorityMap.mapValues ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#mapValues-99)

```scala
def mapValues[W](f: U => W): MapView[T, W]
```

Transforms this map by applying a function to every retrieved value.

Type parameters: `` W ``.

Parameters, list 1: `` f: U => W ``.

Returns: `` MapView[T, W] ``.

Source contract/attributes: Transforms this map by applying a function to every retrieved value. Value parameters f the function used to transform values of this map. Attributes Returns a map view which maps every key of this map to f(this(key)). The resulting map wraps the original map without copying any elements. Deprecated [Since version 2.13.0] Use .view.mapValues(f). A future version will include a strict version of this method (for now, .view.mapValues(f).toMap). Inherited from: MapOps

Invocation template:

```scala
receiver.mapValues[W](f)
```

## `` com.cra.figaro.util.PriorityMap.repr ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#repr-0)

```scala
final def repr: Map[T, U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Map[T, U] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use coll instead of repr in a collection implementation, use the collection value itself from the outside Inherited from: IterableOps

Invocation template:

```scala
receiver.repr
```

## `` com.cra.figaro.util.PriorityMap.retain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#retain-fffff774)

```scala
final def retain(p: (T, U) => Boolean): this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` p: (T, U) => Boolean ``.

Returns: `` this.type ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use filterInPlace instead Inherited from: MapOps

Invocation template:

```scala
receiver.retain(p)
```

## `` com.cra.figaro.util.PriorityMap.seq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#seq-0)

```scala
def seq: this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` this.type ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Iterable.seq always returns the iterable itself Inherited from: Iterable

Invocation template:

```scala
receiver.seq
```

## `` com.cra.figaro.util.PriorityMap.toIterable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#toIterable-0)

```scala
final def toIterable: this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` this.type ``.

Source contract/attributes: Attributes Returns This collection as an Iterable[A]. No new collection will be built if this is already an Iterable[A]. Deprecated [Since version 2.13.7] toIterable is internal and will be made protected; its name is similar to `toList` or `toSeq`, but it doesn\'t copy non-immutable collections Inherited from: Iterable

Invocation template:

```scala
receiver.toIterable
```

## `` com.cra.figaro.util.PriorityMap.toIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#toIterator-0)

```scala
final def toIterator: Iterator[(T, U)]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[(T, U)] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use .iterator instead of .toIterator Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toIterator
```

## `` com.cra.figaro.util.PriorityMap.toStream ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#toStream-0)

```scala
final def toStream: Stream[(T, U)]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Stream[(T, U)] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use .to(LazyList) instead of .toStream Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toStream
```

## `` com.cra.figaro.util.PriorityMap.toTraversable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#toTraversable-0)

```scala
final def toTraversable: Iterable[(T, U)]
```

Converts this mutable map to an unspecified Iterable. Will return the same collection if this instance is already Iterable.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[(T, U)] ``.

Source contract/attributes: Converts this mutable map to an unspecified Iterable. Will return the same collection if this instance is already Iterable. Attributes Returns An Iterable containing all elements of this mutable map. Deprecated [Since version 2.13.0] toTraversable is internal and will be made protected; its name is similar to `toList` or `toSeq`, but it doesn\'t copy non-immutable collections Inherited from: IterableOps

Invocation template:

```scala
receiver.toTraversable
```

## `` com.cra.figaro.util.PriorityMap.transform ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#transform-fffff774)

```scala
final def transform(f: (T, U) => U): this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` f: (T, U) => U ``.

Returns: `` this.type ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use mapValuesInPlace instead Inherited from: MapOps

Invocation template:

```scala
receiver.transform(f)
```

## `` com.cra.figaro.util.PriorityMap.updated ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#updated-a0f)

```scala
def updated[V1 >: U](key: T, value: V1): Map[T, V1]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` V1 >: U ``.

Parameters, list 1: `` key: T ``; `` value: V1 ``.

Returns: `` Map[T, V1] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use m.clone().addOne((k,v)) instead of m.updated(k, v) Inherited from: MapOps

Invocation template:

```scala
receiver.updated[V1](key, value)
```

## `` com.cra.figaro.util.PriorityMap.view ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/PriorityMap.html#view-fffff78d)

```scala
def view(from: Int, until: Int): View[(T, U)]
```

A view over a slice of the elements of this collection.

Type parameters: none.

Parameters, list 1: `` from: Int ``; `` until: Int ``.

Returns: `` View[(T, U)] ``.

Source contract/attributes: A view over a slice of the elements of this collection. Attributes Deprecated [Since version 2.13.0] Use .view.slice(from, until) instead of .view(from, until) Inherited from: IterableOps

Invocation template:

```scala
receiver.view(from, until)
```

## `` com.cra.figaro.util.Resampler.resample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/Resampler.html#resample-993)

```scala
def resample(): T
```

Choose a sample with probability proportional to its weight.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` T ``.

Source contract/attributes: Choose a sample with probability proportional to its weight. Attributes

Invocation template:

```scala
receiver.resample()
```

## `` com.cra.figaro.util.SelectableSet.select ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#select-993)

```scala
def select(): T
```

Select an element uniformly at random.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` T ``.

Source contract/attributes: Select an element uniformly at random. Attributes

Invocation template:

```scala
receiver.select()
```

## `` com.cra.figaro.util.SelectableSet.& ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#&-dde)

```scala
final def &(that: Set[T]): Set[T]
```

Alias for intersect.

Type parameters: none.

Parameters, list 1: `` that: Set[T] ``.

Returns: `` Set[T] ``.

Source contract/attributes: Alias for intersect. Value parameters that the set to intersect with Attributes Inherited from: SetOps

Invocation template:

```scala
receiver.&(that)
```

## `` com.cra.figaro.util.SelectableSet.&~ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#&~-dde)

```scala
final def &~(that: Set[T]): Set[T]
```

Alias for diff.

Type parameters: none.

Parameters, list 1: `` that: Set[T] ``.

Returns: `` Set[T] ``.

Source contract/attributes: Alias for diff. Value parameters that the set of elements to exclude Attributes Inherited from: SetOps

Invocation template:

```scala
receiver.&~(that)
```

## `` com.cra.figaro.util.SelectableSet.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#++-1dd)

```scala
final def ++[B >: T](suffix: IterableOnce[B]): Set[B]
```

Alias for concat.

Type parameters: `` B >: T ``.

Parameters, list 1: `` suffix: IterableOnce[B] ``.

Returns: `` Set[B] ``.

Source contract/attributes: Alias for concat. Type parameters B the element type of the returned collection Value parameters suffix the iterable to append to this mutable set Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.++[B](suffix)
```

## `` com.cra.figaro.util.SelectableSet.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#++-fffff87d)

```scala
final def ++(that: IterableOnce[T]): Set[T]
```

Alias for concat.

Type parameters: none.

Parameters, list 1: `` that: IterableOnce[T] ``.

Returns: `` Set[T] ``.

Source contract/attributes: Alias for concat. Value parameters that the collection containing the elements to add Attributes Inherited from: SetOps

Invocation template:

```scala
receiver.++(that)
```

## `` com.cra.figaro.util.SelectableSet.++= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#++=-fffffb02)

```scala
final def ++=(elems: IterableOnce[T]): this.type
```

Alias for addAll.

Type parameters: none.

Parameters, list 1: `` elems: IterableOnce[T] ``.

Returns: `` this.type ``.

Source contract/attributes: Alias for addAll. Attributes Inherited from: Growable

Invocation template:

```scala
receiver.++=(elems)
```

## `` com.cra.figaro.util.SelectableSet.+= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#+=-b4a)

```scala
final def +=(elem: T): this.type
```

Alias for addOne.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` this.type ``.

Source contract/attributes: Alias for addOne. Attributes Inherited from: Growable

Invocation template:

```scala
receiver.+=(elem)
```

## `` com.cra.figaro.util.SelectableSet.--= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#--=-fffff8b8)

```scala
final def --=(xs: IterableOnce[T]): this.type
```

Alias for subtractAll.

Type parameters: none.

Parameters, list 1: `` xs: IterableOnce[T] ``.

Returns: `` this.type ``.

Source contract/attributes: Alias for subtractAll. Attributes Inherited from: Shrinkable

Invocation template:

```scala
receiver.--=(xs)
```

## `` com.cra.figaro.util.SelectableSet.-= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#-=-fffff700)

```scala
final def -=(elem: T): this.type
```

Alias for subtractOne.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` this.type ``.

Source contract/attributes: Alias for subtractOne. Attributes Inherited from: Shrinkable

Invocation template:

```scala
receiver.-=(elem)
```

## `` com.cra.figaro.util.SelectableSet.add ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#add-4ad)

```scala
def add(elem: T): Boolean
```

Checks whether the set contains the given element, and adds it if not.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` Boolean ``.

Source contract/attributes: Checks whether the set contains the given element, and adds it if not. Value parameters elem the element to be added Attributes Returns true if the element was added Inherited from: SetOps

Invocation template:

```scala
receiver.add(elem)
```

## `` com.cra.figaro.util.SelectableSet.addAll ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#addAll-fffffb02)

```scala
def addAll(elems: IterableOnce[T]): this.type
```

Add s all elements produced by an IterableOnce to this mutable set.

Type parameters: none.

Parameters, list 1: `` elems: IterableOnce[T] ``.

Returns: `` this.type ``.

Source contract/attributes: Add s all elements produced by an IterableOnce to this mutable set. Value parameters elems the IterableOnce producing the elements to add. Attributes Returns the mutable set itself. Inherited from: Growable

Invocation template:

```scala
receiver.addAll(elems)
```

## `` com.cra.figaro.util.SelectableSet.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#addString-ff4)

```scala
final def addString(b: StringBuilder): b.type
```

Appends all elements of this mutable set to a string builder. The written text consists of the string representations (w.r.t. the method toString) of all elements of this mutable set without any separator string.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this mutable set to a string builder. The written text consists of the string representations (w.r.t. the method toString) of all elements of this mutable set without any separator string. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() val h = a.addString(b) // 1234 Value parameters b the string builder to which elements are appended. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b)
```

## `` com.cra.figaro.util.SelectableSet.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#addString-e99)

```scala
final def addString(b: StringBuilder, sep: String): b.type
```

Appends all elements of this mutable set to a string builder using a separator string. The written text consists of the string representations (w.r.t. the method toString) of all elements of this mutable set, separated by the string sep.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``; `` sep: String ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this mutable set to a string builder using a separator string. The written text consists of the string representations (w.r.t. the method toString) of all elements of this mutable set, separated by the string sep. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() a.addString(b, ", ") // 1, 2, 3, 4 Value parameters b the string builder to which elements are appended. sep the separator string. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b, sep)
```

## `` com.cra.figaro.util.SelectableSet.addString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#addString-de3)

```scala
def addString(b: StringBuilder, start: String, sep: String, end: String): b.type
```

Appends all elements of this mutable set to a string builder using start, end, and separator strings. The written text begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this mutable set are separated by the string sep.

Type parameters: none.

Parameters, list 1: `` b: StringBuilder ``; `` start: String ``; `` sep: String ``; `` end: String ``.

Returns: `` b.type ``.

Source contract/attributes: Appends all elements of this mutable set to a string builder using start, end, and separator strings. The written text begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this mutable set are separated by the string sep. Example: val a = List(1,2,3,4) // List(1, 2, 3, 4) val b = new StringBuilder() a.addString(b , "List(" , ", " , ")") // List(1, 2, 3, 4) Value parameters b the string builder to which elements are appended. end the ending string. sep the separator string. start the starting string. Attributes Returns the string builder b to which elements were appended. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.addString(b, start, sep, end)
```

## `` com.cra.figaro.util.SelectableSet.andThen ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#andThen-51)

```scala
def andThen[A](g: Boolean => A): T => A
```

Composes two instances of Function1 in a new Function1, with this function applied first.

Type parameters: `` A ``.

Parameters, list 1: `` g: Boolean => A ``.

Returns: `` T => A ``.

Source contract/attributes: Composes two instances of Function1 in a new Function1, with this function applied first. Type parameters A the result type of function g Value parameters g a function R => A Attributes Returns a new function f such that f(x) == g(apply(x)) Inherited from: Function1

Invocation template:

```scala
receiver.andThen[A](g)
```

## `` com.cra.figaro.util.SelectableSet.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#apply-4ad)

```scala
final def apply(elem: T): Boolean
```

Tests if some element is contained in this set.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests if some element is contained in this set. This method is equivalent to contains. It allows sets to be interpreted as predicates. Value parameters elem the element to test for membership. Attributes Returns true if elem is contained in this set, false otherwise. Inherited from: SetOps

Invocation template:

```scala
receiver.apply(elem)
```

## `` com.cra.figaro.util.SelectableSet.canEqual ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#canEqual-4ad)

```scala
def canEqual(that: Any): Boolean
```

Checks whether this instance can possibly equal that.

Type parameters: none.

Parameters, list 1: `` that: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Checks whether this instance can possibly equal that. A method that should be called from every well-designed equals method that is open to be overridden in a subclass. See Programming in Scala, Chapter 28 for discussion and design. Value parameters that the value being probed for possible equality Attributes Returns true if this instance can possibly equal that, otherwise false Inherited from: Set

Invocation template:

```scala
receiver.canEqual(that)
```

## `` com.cra.figaro.util.SelectableSet.clone ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#clone-180)

```scala
override def clone(): Set[T]
```

Create a copy of the receiver object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[T] ``.

Source contract/attributes: Create a copy of the receiver object. The default implementation of the clone method is platform dependent. Attributes Returns a copy of the receiver object. Note not specified by SLS as a member of AnyRef Definition Classes SetOps -> Cloneable -> Object Inherited from: SetOps

Invocation template:

```scala
receiver.clone()
```

## `` com.cra.figaro.util.SelectableSet.collect ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#collect-fffff5b3)

```scala
def collect[B](pf: PartialFunction[T, B]): Set[B]
```

Builds a new mutable set by applying a partial function to all elements of this mutable set on which the function is defined.

Type parameters: `` B ``.

Parameters, list 1: `` pf: PartialFunction[T, B] ``.

Returns: `` Set[B] ``.

Source contract/attributes: Builds a new mutable set by applying a partial function to all elements of this mutable set on which the function is defined. Type parameters B the element type of the returned mutable set. Value parameters pf the partial function which filters and maps the mutable set. Attributes Returns a new mutable set resulting from applying the given partial function pf to each element on which it is defined and collecting the results. The order of the elements is preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.collect[B](pf)
```

## `` com.cra.figaro.util.SelectableSet.collectFirst ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#collectFirst-25d)

```scala
def collectFirst[B](pf: PartialFunction[T, B]): Option[B]
```

Finds the first element of the mutable set for which the given partial function is defined, and applies the partial function to it.

Type parameters: `` B ``.

Parameters, list 1: `` pf: PartialFunction[T, B] ``.

Returns: `` Option[B] ``.

Source contract/attributes: Finds the first element of the mutable set for which the given partial function is defined, and applies the partial function to it. Note: may not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Type parameters B the result type of the partial function Value parameters pf the partial function Attributes Returns an option value containing pf applied to the first value for which it is defined, or None if none exists. Example Seq("a", 1, 5L).collectFirst { case x: Int => x*10 } = Some(10) Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.collectFirst[B](pf)
```

## `` com.cra.figaro.util.SelectableSet.compose ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#compose-51)

```scala
def compose[A](g: A => T): A => Boolean
```

Composes two instances of Function1 in a new Function1, with this function applied last.

Type parameters: `` A ``.

Parameters, list 1: `` g: A => T ``.

Returns: `` A => Boolean ``.

Source contract/attributes: Composes two instances of Function1 in a new Function1, with this function applied last. Type parameters A the type to which function g can be applied Value parameters g a function A => T1 Attributes Returns a new function f such that f(x) == apply(g(x)) Inherited from: Function1

Invocation template:

```scala
receiver.compose[A](g)
```

## `` com.cra.figaro.util.SelectableSet.concat ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#concat-1dd)

```scala
def concat[B >: T](suffix: IterableOnce[B]): Set[B]
```

Returns a new mutable set containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the mutable set is the most specific superclass encompassing the element types of the two operands.

Type parameters: `` B >: T ``.

Parameters, list 1: `` suffix: IterableOnce[B] ``.

Returns: `` Set[B] ``.

Source contract/attributes: Returns a new mutable set containing the elements from the left hand operand followed by the elements from the right hand operand. The element type of the mutable set is the most specific superclass encompassing the element types of the two operands. Type parameters B the element type of the returned collection. Value parameters suffix the iterable to append. Attributes Returns a new mutable set which contains all elements of this mutable set followed by all elements of suffix. Inherited from: IterableOps

Invocation template:

```scala
receiver.concat[B](suffix)
```

## `` com.cra.figaro.util.SelectableSet.concat ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#concat-fffff87d)

```scala
def concat(that: IterableOnce[T]): Set[T]
```

Creates a new mutable set by adding all elements contained in another collection to this mutable set, omitting duplicates.

Type parameters: none.

Parameters, list 1: `` that: IterableOnce[T] ``.

Returns: `` Set[T] ``.

Source contract/attributes: Creates a new mutable set by adding all elements contained in another collection to this mutable set, omitting duplicates. Example: scala> val a = Set(1, 2) concat Set(2, 3) a: scala.collection.immutable.Set[Int] = Set(1, 2, 3) Value parameters that the collection containing the elements to add. Attributes Returns a new mutable set with the given elements added, omitting duplicates. Inherited from: SetOps

Invocation template:

```scala
receiver.concat(that)
```

## `` com.cra.figaro.util.SelectableSet.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#copyToArray-fffffd03)

```scala
def copyToArray[B >: T](dest: Array[B], start: Int, n: Int): Int
```

Copies elements to an array and returns the number of elements written.

Type parameters: `` B >: T ``.

Parameters, list 1: `` dest: Array[B] ``; `` start: Int ``; `` n: Int ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array and returns the number of elements written. Fills the given array dest starting at index start with at most n elements of this mutable set. Copying will stop once either all the elements of this mutable set have been copied, or the end of the array is reached, or n elements have been copied. If start is less than zero, it is taken as zero. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. n the maximal number of elements to copy. start the starting index of xs. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest, start, n)
```

## `` com.cra.figaro.util.SelectableSet.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#copyToArray-fffffaa4)

```scala
def copyToArray[B >: T](dest: Array[B], start: Int): Int
```

Copies elements to an array, returning the number of elements written.

Type parameters: `` B >: T ``.

Parameters, list 1: `` dest: Array[B] ``; `` start: Int ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array, returning the number of elements written. Fills the given array dest starting at index start with values of this mutable set. Copying will stop once either all the elements of this mutable set have been copied, or the end of the array is reached. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. start the starting index of xs. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest, start)
```

## `` com.cra.figaro.util.SelectableSet.copyToArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#copyToArray-fffff3e3)

```scala
def copyToArray[B >: T](dest: Array[B]): Int
```

Copies elements to an array, returning the number of elements written.

Type parameters: `` B >: T ``.

Parameters, list 1: `` dest: Array[B] ``.

Returns: `` Int ``.

Source contract/attributes: Copies elements to an array, returning the number of elements written. Fills the given array dest starting at index start with values of this mutable set. Copying will stop once either all the elements of this mutable set have been copied, or the end of the array is reached. Type parameters B the type of the elements of the array. Value parameters dest the array to fill. Attributes Returns the number of elements written to the array Note Reuse: After calling this method, one should discard the iterator it was called on. Using it is undefined and subject to change. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToArray[B](dest)
```

## `` com.cra.figaro.util.SelectableSet.corresponds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#corresponds-958)

```scala
def corresponds[B](that: IterableOnce[B])(p: (T, B) => Boolean): Boolean
```

Tests whether every element of this collection's iterator relates to the corresponding element of another collection by satisfying a test predicate.

Type parameters: `` B ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Parameters, list 2: `` p: (T, B) => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether every element of this collection's iterator relates to the corresponding element of another collection by satisfying a test predicate. Note: will not terminate for infinite-sized collections. Type parameters B the type of the elements of that Value parameters p the test predicate, which relates elements from both collections that the other collection Attributes Returns true if both collections have the same length and p(x, y) is true for all corresponding elements x of this iterator and y of that, otherwise false Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.corresponds[B](that)(p)
```

## `` com.cra.figaro.util.SelectableSet.count ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#count-db6)

```scala
def count(p: T => Boolean): Int
```

Counts the number of elements in the mutable set which satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Int ``.

Source contract/attributes: Counts the number of elements in the mutable set which satisfy a predicate. Note: will not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns the number of elements satisfying the predicate p. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.count(p)
```

## `` com.cra.figaro.util.SelectableSet.diff ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#diff-7a6)

```scala
def diff(that: Set[T]): Set[T]
```

Computes the difference of this set and another set.

Type parameters: none.

Parameters, list 1: `` that: Set[T] ``.

Returns: `` Set[T] ``.

Source contract/attributes: Computes the difference of this set and another set. Value parameters that the set of elements to exclude. Attributes Returns a set containing those elements of this set that are not also contained in the given set that. Inherited from: SetOps

Invocation template:

```scala
receiver.diff(that)
```

## `` com.cra.figaro.util.SelectableSet.drop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#drop-aba)

```scala
def drop(n: Int): Set[T]
```

Selects all elements except the first n ones.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Set[T] ``.

Source contract/attributes: Selects all elements except the first n ones. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to drop from this mutable set. Attributes Returns a mutable set consisting of all elements of this mutable set except the first n ones, or else the empty mutable set, if this mutable set has less than n elements. If n is negative, don't drop any elements. Inherited from: IterableOps

Invocation template:

```scala
receiver.drop(n)
```

## `` com.cra.figaro.util.SelectableSet.dropRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#dropRight-aba)

```scala
def dropRight(n: Int): Set[T]
```

Selects all elements except last n ones.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Set[T] ``.

Source contract/attributes: Selects all elements except last n ones. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to drop from this mutable set. Attributes Returns a mutable set consisting of all elements of this mutable set except the last n ones, or else the empty mutable set, if this mutable set has less than n elements. If n is negative, don't drop any elements. Inherited from: IterableOps

Invocation template:

```scala
receiver.dropRight(n)
```

## `` com.cra.figaro.util.SelectableSet.dropWhile ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#dropWhile-c24)

```scala
def dropWhile(p: T => Boolean): Set[T]
```

Selects all elements except the longest prefix that satisfies a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Set[T] ``.

Source contract/attributes: Selects all elements except the longest prefix that satisfies a predicate. The matching prefix starts with the first element of this mutable set, and the element following the prefix is the first element that does not satisfy the predicate. The matching prefix may be empty, so that this method returns the entire mutable set. Example: List(1, 2, 3, 100, 4).dropWhile(n => n < 10) // List(100, 4) List(1, 2, 3, 100, 4).dropWhile(n => n == 0) // List(1, 2, 3, 100, 4) Use span to obtain both the prefix and suffix. Use filterNot to drop all elements that satisfy the predicate. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p The predicate used to test elements. Attributes Returns the longest suffix of this mutable set whose first element does not satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.dropWhile(p)
```

## `` com.cra.figaro.util.SelectableSet.empty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#empty-0)

```scala
override def empty: Set[T]
```

The empty mutable set.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[T] ``.

Source contract/attributes: The empty mutable set. Attributes Returns an empty iterable of type mutable.Set . Definition Classes IterableFactoryDefaults -> IterableOps Inherited from: IterableFactoryDefaults

Invocation template:

```scala
receiver.empty
```

## `` com.cra.figaro.util.SelectableSet.equals ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#equals-4ad)

```scala
override def equals(that: Any): Boolean
```

Equality of sets is implemented using the lookup method contains. This method returns true if

Type parameters: none.

Parameters, list 1: `` that: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Equality of sets is implemented using the lookup method contains. This method returns true if the argument that is a Set, the two sets have the same size, and for every element this set, other.contains(element) == true. The implementation of equals checks the canEqual method, so subclasses of Set can narrow down the equality to specific set types. The Set implementations in the standard library can all be compared, their canEqual methods return true. Note: The equals method only respects the equality laws (symmetry, transitivity) if the two sets use the same element equivalence function in their lookup operation. For example, the element equivalence operation in a scala.collection.immutable.TreeSet is defined by its ordering. Comparing a TreeSet with a HashSet leads to unexpected results if ordering.equiv(e1, e2) (used for lookup in TreeSet) is different from e1 == e2 (used for lookup in HashSet). import scala.collection.immutable._ val ord: Ordering[String] = _ compareToIgnoreCase _ val result1 = TreeSet("A")(using ord) == HashSet("a") // false val result2 = HashSet("a") == TreeSet("A")(using ord) // true Value parameters that The set to which this set is compared Attributes Returns true if the two sets are equal according to the description Definition Classes Set -> Equals -> Any Inherited from: Set

Invocation template:

```scala
receiver.equals(that)
```

## `` com.cra.figaro.util.SelectableSet.exists ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#exists-fffff66f)

```scala
def exists(p: T => Boolean): Boolean
```

Tests whether a predicate holds for at least one element of this mutable set.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether a predicate holds for at least one element of this mutable set. Note: may not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns true if the given predicate p is satisfied by at least one element of this mutable set, otherwise false Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.exists(p)
```

## `` com.cra.figaro.util.SelectableSet.filter ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#filter-c24)

```scala
def filter(pred: T => Boolean): Set[T]
```

Selects all elements of this mutable set which satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` pred: T => Boolean ``.

Returns: `` Set[T] ``.

Source contract/attributes: Selects all elements of this mutable set which satisfy a predicate. Value parameters p the predicate used to test elements. Attributes Returns a new mutable set consisting of all elements of this mutable set that satisfy the given predicate p. The order of the elements is preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.filter(pred)
```

## `` com.cra.figaro.util.SelectableSet.filterInPlace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#filterInPlace-acf)

```scala
def filterInPlace(p: T => Boolean): this.type
```

Removes all elements from the set for which do not satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` this.type ``.

Source contract/attributes: Removes all elements from the set for which do not satisfy a predicate. Value parameters p the predicate used to test elements. Only elements for which p returns true are retained in the set; all others are removed. Attributes Inherited from: SetOps

Invocation template:

```scala
receiver.filterInPlace(p)
```

## `` com.cra.figaro.util.SelectableSet.filterNot ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#filterNot-c24)

```scala
def filterNot(pred: T => Boolean): Set[T]
```

Selects all elements of this mutable set which do not satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` pred: T => Boolean ``.

Returns: `` Set[T] ``.

Source contract/attributes: Selects all elements of this mutable set which do not satisfy a predicate. Value parameters pred the predicate used to test elements. Attributes Returns a new mutable set consisting of all elements of this mutable set that do not satisfy the given predicate pred. Their order may not be preserved. Inherited from: IterableOps

Invocation template:

```scala
receiver.filterNot(pred)
```

## `` com.cra.figaro.util.SelectableSet.find ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#find-94e)

```scala
def find(p: T => Boolean): Option[T]
```

Finds the first element of the mutable set satisfying a predicate, if any.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the first element of the mutable set satisfying a predicate, if any. Note: may not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the predicate used to test elements. Attributes Returns an option value containing the first element in the mutable set that satisfies p, or None if none exists. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.find(p)
```

## `` com.cra.figaro.util.SelectableSet.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#flatMap-5d3)

```scala
def flatMap[B](f: T => IterableOnce[B]): Set[B]
```

Builds a new mutable set by applying a function to all elements of this mutable set and using the elements of the resulting collections.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => IterableOnce[B] ``.

Returns: `` Set[B] ``.

Source contract/attributes: Builds a new mutable set by applying a function to all elements of this mutable set and using the elements of the resulting collections. For example: def getWords(lines: Seq[String]): Seq[String] = lines.flatMap(line => line.split("\\W+")) The type of the resulting collection is guided by the static type of this mutable set. This might cause unexpected results sometimes. For example: // lettersOf will return a Seq[Char] of likely repeated letters, instead of a Set def lettersOf(words: Seq[String]) = words.flatMap(word => word.toSet) // lettersOf2 will return a Set[Char], not a Seq def lettersOf2(words: Seq[String]) = words.toSet.flatMap(word => word.toSeq) // xs will be an Iterable[Int] val xs = Map("a" -> List(11, 111), "b" -> List(22, 222)).flatMap(_._2) // ys will be a Map[Int, Int] val ys = Map("a" -> List(1 -> 11, 1 -> 111), "b" -> List(2 -> 22, 2 -> 222)).flatMap(_._2) Type parameters B the element type of the returned collection. Value parameters f the function to apply to each element. Attributes Returns a new mutable set resulting from applying the given collection-valued function f to each element of this mutable set and concatenating the results. Inherited from: IterableOps

Invocation template:

```scala
receiver.flatMap[B](f)
```

## `` com.cra.figaro.util.SelectableSet.flatten ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#flatten-5d3)

```scala
def flatten[B](implicit asIterable: T => IterableOnce[B]): Set[B]
```

Given that the elements of this collection are themselves iterable collections, converts this mutable set into a mutable set comprising the elements of these iterable collections.

Type parameters: `` B ``.

Context parameters, list 1: `` asIterable: T => IterableOnce[B] ``.

Returns: `` Set[B] ``.

Source contract/attributes: Given that the elements of this collection are themselves iterable collections, converts this mutable set into a mutable set comprising the elements of these iterable collections. The resulting collection's type will be guided by the type of mutable set. For example: val xs = List( Set(1, 2, 3), Set(1, 2, 3) ).flatten // xs == List(1, 2, 3, 1, 2, 3) val ys = Set( List(1, 2, 3), List(3, 2, 1) ).flatten // ys == Set(1, 2, 3) Type parameters B the type of the elements of each iterable collection. Value parameters asIterable an implicit conversion which asserts that the element type of this mutable set is an Iterable. Attributes Returns a new mutable set resulting from concatenating all element collections. Inherited from: IterableOps

Invocation template:

```scala
receiver.flatten[B](using asIterable)
```

## `` com.cra.figaro.util.SelectableSet.fold ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#fold-fffff9e1)

```scala
def fold[A1 >: T](z: A1)(op: (A1, A1) => A1): A1
```

Applies the given binary operator op to the given initial value z and all elements of this mutable set.

Type parameters: `` A1 >: T ``.

Parameters, list 1: `` z: A1 ``.

Parameters, list 2: `` op: (A1, A1) => A1 ``.

Returns: `` A1 ``.

Source contract/attributes: Applies the given binary operator op to the given initial value z and all elements of this mutable set. For each application of the operator, each operand is either an element of this mutable set, the initial value, or another such application of the operator. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. The initial value may be used an arbitrary number of times, but at least once. If this collection is ordered, then for any application of the operator, the element(s) appearing in the left operand will precede those in the right. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. In either case, it is also necessary that the initial value be a neutral value for the operator, e.g. Nil for List concatenation or 1 for multiplication. The default implementation in IterableOnce is equivalent to foldLeft but may be overridden for more efficient traversal orders. Note: will not terminate for infinite-sized collections. Type parameters A1 The type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. z An initial value; may be used an arbitrary number of times in the computation of the result; must be a neutral value for op for the result to always be the same across runs. Attributes Returns The result of applying op between all the elements and z, or z if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.fold[A1](z)(op)
```

## `` com.cra.figaro.util.SelectableSet.foldLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#foldLeft-fffff9e1)

```scala
def foldLeft[B](z: B)(op: (B, T) => B): B
```

Applies the given binary operator op to the given initial value z and all elements of this mutable set, going left to right. Returns the initial value if this mutable set is empty.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, T) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to the given initial value z and all elements of this mutable set, going left to right. Returns the initial value if this mutable set is empty. "Going left to right" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this mutable set, the result is op( op( ... op( op(z, x,,1,,), x,,2,,) ... ), x,,n,,). If this collection is not ordered, then for each application of the operator, each right operand is an element. In addition, the leftmost operand is the initial value, and each other left operand is itself an application of the operator. The elements of this mutable set and the initial value all appear exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator. Value parameters op A binary operator. z An initial value. Attributes Returns The result of applying op to z and all elements of this mutable set, going left to right. Returns z if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foldLeft[B](z)(op)
```

## `` com.cra.figaro.util.SelectableSet.foldRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#foldRight-fffff9e1)

```scala
def foldRight[B](z: B)(op: (T, B) => B): B
```

Applies the given binary operator op to all elements of this mutable set and the given initial value z, going right to left. Returns the initial value if this mutable set is empty.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (T, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this mutable set and the given initial value z, going right to left. Returns the initial value if this mutable set is empty. "Going right to left" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this mutable set, the result is op(x,,1,,, op(x,,2,,, op( ... op(x,,n,,, z) ... ))). If this collection is not ordered, then for each application of the operator, each left operand is an element. In addition, the rightmost operand is the initial value, and each other right operand is itself an application of the operator. The elements of this mutable set and the initial value all appear exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator. Value parameters op A binary operator. z An initial value. Attributes Returns The result of applying op to all elements of this mutable set and z, going right to left. Returns z if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foldRight[B](z)(op)
```

## `` com.cra.figaro.util.SelectableSet.forall ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#forall-fffff66f)

```scala
def forall(p: T => Boolean): Boolean
```

Tests whether a predicate holds for all elements of this mutable set.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether a predicate holds for all elements of this mutable set. Note: may not terminate for infinite-sized collections. Value parameters p the predicate used to test elements. Attributes Returns true if this mutable set is empty or the given predicate p holds for all elements of this mutable set, otherwise false. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.forall(p)
```

## `` com.cra.figaro.util.SelectableSet.foreach ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#foreach-58c)

```scala
def foreach[U](f: T => U): Unit
```

Applies f to each element for its side effects. Note: U parameter needed to help scalac's type inference.

Type parameters: `` U ``.

Parameters, list 1: `` f: T => U ``.

Returns: `` Unit ``.

Source contract/attributes: Applies f to each element for its side effects. Note: U parameter needed to help scalac's type inference. Type parameters U the return type of f; the value is discarded, but the type parameter aids type inference Value parameters f the function to apply to each element for its side effects Attributes Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.foreach[U](f)
```

## `` com.cra.figaro.util.SelectableSet.groupBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#groupBy-f68)

```scala
def groupBy[K](f: T => K): Map[K, Set[T]]
```

Partitions this mutable set into a map of mutable sets according to some discriminator function.

Type parameters: `` K ``.

Parameters, list 1: `` f: T => K ``.

Returns: `` Map[K, Set[T]] ``.

Source contract/attributes: Partitions this mutable set into a map of mutable sets according to some discriminator function. Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters K the type of keys returned by the discriminator function. Value parameters f the discriminator function. Attributes Returns A map from keys to mutable sets such that the following invariant holds: (xs groupBy f)(k) = xs filter (x => f(x) == k) That is, every key `k` is bound to a mutable set of those elements `x` for which `f(x)` equals `k`. Inherited from: IterableOps

Invocation template:

```scala
receiver.groupBy[K](f)
```

## `` com.cra.figaro.util.SelectableSet.groupMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#groupMap-fffff03a)

```scala
def groupMap[K, B](key: T => K)(f: T => B): Map[K, Set[B]]
```

Partitions this mutable set into a map of mutable sets according to a discriminator function key. Each element in a group is transformed into a value of type B using the value function.

Type parameters: `` K ``; `` B ``.

Parameters, list 1: `` key: T => K ``.

Parameters, list 2: `` f: T => B ``.

Returns: `` Map[K, Set[B]] ``.

Source contract/attributes: Partitions this mutable set into a map of mutable sets according to a discriminator function key. Each element in a group is transformed into a value of type B using the value function. It is equivalent to groupBy(key).mapValues(_.map(f)), but more efficient. case class User(name: String, age: Int) def namesByAge(users: Seq[User]): Map[Int, Seq[String]] = users.groupMap(_.age)(_.name) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of values returned by the transformation function K the type of keys returned by the discriminator function Value parameters f the element transformation function key the discriminator function Attributes Returns a map associating each key k produced by key with a mutable set of values produced by applying f to elements that map to k Inherited from: IterableOps

Invocation template:

```scala
receiver.groupMap[K, B](key)(f)
```

## `` com.cra.figaro.util.SelectableSet.groupMapReduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#groupMapReduce-8e8)

```scala
def groupMapReduce[K, B](key: T => K)(f: T => B)(reduce: (B, B) => B): Map[K, B]
```

Partitions this mutable set into a map according to a discriminator function key. All the values that have the same discriminator are then transformed by the f function and then reduced into a single value with the reduce function.

Type parameters: `` K ``; `` B ``.

Parameters, list 1: `` key: T => K ``.

Parameters, list 2: `` f: T => B ``.

Parameters, list 3: `` reduce: (B, B) => B ``.

Returns: `` Map[K, B] ``.

Source contract/attributes: Partitions this mutable set into a map according to a discriminator function key. All the values that have the same discriminator are then transformed by the f function and then reduced into a single value with the reduce function. It is equivalent to groupBy(key).mapValues(_.map(f).reduce(reduce)), but more efficient. def occurrences[A](as: Seq[A]): Map[A, Int] = as.groupMapReduce(identity)(_ => 1)(_ + _) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of values returned by the transformation function K the type of keys returned by the discriminator function Value parameters f the element transformation function key the discriminator function reduce the reduction function used to combine values mapped to the same key Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.groupMapReduce[K, B](key)(f)(reduce)
```

## `` com.cra.figaro.util.SelectableSet.grouped ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#grouped-fffffbef)

```scala
def grouped(size: Int): Iterator[Set[T]]
```

Partitions elements in fixed size mutable sets.

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Iterator[Set[T]] ``.

Source contract/attributes: Partitions elements in fixed size mutable sets. Value parameters size the number of elements per group Attributes Returns An iterator producing mutable sets of size size, except the last will be less than size size if the elements don't divide evenly. See also scala.collection.Iterator, method grouped Inherited from: IterableOps

Invocation template:

```scala
receiver.grouped(size)
```

## `` com.cra.figaro.util.SelectableSet.hashCode ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#hashCode-ffffff27)

```scala
override def hashCode(): Int
```

Calculates a hash code value for the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Int ``.

Source contract/attributes: Calculates a hash code value for the object. The default hashing algorithm is platform dependent. Note that it is allowed for two objects to have identical hash codes (o1.hashCode.equals(o2.hashCode)) yet not be equal (o1.equals(o2) returns false). A degenerate implementation could always return 0. However, it is required that if two objects are equal (o1.equals(o2) returns true) that they have identical hash codes (o1.hashCode.equals(o2.hashCode)). Therefore, when overriding this method, be sure to verify that the behavior is consistent with the equals method. Attributes Returns the hash code value for this object. Definition Classes Set -> Any Inherited from: Set

Invocation template:

```scala
receiver.hashCode()
```

## `` com.cra.figaro.util.SelectableSet.head ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#head-0)

```scala
def head: T
```

Selects the first element of this mutable set.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` T ``.

Source contract/attributes: Selects the first element of this mutable set. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the first element of this mutable set. Throws NoSuchElementException if the mutable set is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.head
```

## `` com.cra.figaro.util.SelectableSet.headOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#headOption-0)

```scala
def headOption: Option[T]
```

Optionally selects the first element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Option[T] ``.

Source contract/attributes: Optionally selects the first element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the first element of this mutable set if it is nonempty, None if it is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.headOption
```

## `` com.cra.figaro.util.SelectableSet.init ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#init-0)

```scala
def init: Set[T]
```

The initial part of the collection without its last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[T] ``.

Source contract/attributes: The initial part of the collection without its last element. Note: Even when applied to a view or a lazy collection it will always force the elements. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.init
```

## `` com.cra.figaro.util.SelectableSet.inits ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#inits-0)

```scala
def inits: Iterator[Set[T]]
```

Iterates over the inits of this mutable set. The first value will be this mutable set and the final one will be an empty mutable set, with the intervening values the results of successive applications of init.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Set[T]] ``.

Source contract/attributes: Iterates over the inits of this mutable set. The first value will be this mutable set and the final one will be an empty mutable set, with the intervening values the results of successive applications of init. Note: Even when applied to a view or a lazy collection it will always force the elements. Attributes Returns an iterator over all the inits of this mutable set Example List(1,2,3).inits = Iterator(List(1,2,3), List(1,2), List(1), Nil) Inherited from: IterableOps

Invocation template:

```scala
receiver.inits
```

## `` com.cra.figaro.util.SelectableSet.intersect ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#intersect-dde)

```scala
def intersect(that: Set[T]): Set[T]
```

Computes the intersection between this set and another set.

Type parameters: none.

Parameters, list 1: `` that: Set[T] ``.

Returns: `` Set[T] ``.

Source contract/attributes: Computes the intersection between this set and another set. Value parameters that the set to intersect with. Attributes Returns a new set consisting of all elements that are both in this set and in the given set that. Inherited from: SetOps

Invocation template:

```scala
receiver.intersect(that)
```

## `` com.cra.figaro.util.SelectableSet.isEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#isEmpty-0)

```scala
def isEmpty: Boolean
```

Tests whether the mutable set is empty.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether the mutable set is empty. Note: The default implementation creates and discards an iterator. Note: Implementations in subclasses that are not repeatedly iterable must take care not to consume any elements when isEmpty is called. Attributes Returns true if the mutable set contains no elements, false otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.isEmpty
```

## `` com.cra.figaro.util.SelectableSet.isTraversableAgain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#isTraversableAgain-0)

```scala
override def isTraversableAgain: Boolean
```

Tests whether this mutable set can be repeatedly traversed. Always true for Iterables and false for Iterators unless overridden.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this mutable set can be repeatedly traversed. Always true for Iterables and false for Iterators unless overridden. Attributes Returns true if it is repeatedly traversable, false otherwise. Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.isTraversableAgain
```

## `` com.cra.figaro.util.SelectableSet.iterableFactory ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#iterableFactory-0)

```scala
override def iterableFactory: IterableFactory[Set]
```

The companion object of this mutable set, providing various factory methods.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IterableFactory[Set] ``.

Source contract/attributes: The companion object of this mutable set, providing various factory methods. Attributes Returns the companion factory object for this collection type Note When implementing a custom collection type and refining CC to the new type, this method needs to be overridden to return a factory for the new type (the compiler will issue an error otherwise). Definition Classes Set -> Set -> Iterable -> Iterable -> IterableOps Inherited from: Set

Invocation template:

```scala
receiver.iterableFactory
```

## `` com.cra.figaro.util.SelectableSet.knownSize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#knownSize-0)

```scala
override def knownSize: Int
```

The number of elements in the collection under construction, if it can be cheaply computed, -1 otherwise.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: The number of elements in the collection under construction, if it can be cheaply computed, -1 otherwise. Attributes Returns The number of elements. The default implementation always returns -1. Definition Classes SetOps -> Growable -> IterableOnce Inherited from: SetOps

Invocation template:

```scala
receiver.knownSize
```

## `` com.cra.figaro.util.SelectableSet.last ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#last-0)

```scala
def last: T
```

Selects the last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` T ``.

Source contract/attributes: Selects the last element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns The last element of this mutable set. Throws NoSuchElementException If the mutable set is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.last
```

## `` com.cra.figaro.util.SelectableSet.lastOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#lastOption-0)

```scala
def lastOption: Option[T]
```

Optionally selects the last element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Option[T] ``.

Source contract/attributes: Optionally selects the last element. Note: might return different results for different runs, unless the underlying collection type is ordered. Attributes Returns the last element of this mutable set if it is nonempty, None if it is empty. Inherited from: IterableOps

Invocation template:

```scala
receiver.lastOption
```

## `` com.cra.figaro.util.SelectableSet.lazyZip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#lazyZip-fffffd84)

```scala
def lazyZip[B](that: Iterable[B]): LazyZip2[T, B, this.type]
```

Analogous to zip except that the elements in each collection are not consumed until a strict operation is invoked on the returned LazyZip2 decorator.

Type parameters: `` B ``.

Parameters, list 1: `` that: Iterable[B] ``.

Returns: `` LazyZip2[T, B, this.type] ``.

Source contract/attributes: Analogous to zip except that the elements in each collection are not consumed until a strict operation is invoked on the returned LazyZip2 decorator. Calls to lazyZip can be chained to support higher arities (up to 4) without incurring the expense of constructing and deconstructing intermediary tuples. val xs = List(1, 2, 3) val res = (xs lazyZip xs lazyZip xs lazyZip xs).map((a, b, c, d) => a + b + c + d) // res == List(4, 8, 12) Type parameters B the type of the second element in each eventual pair Value parameters that the iterable providing the second element of each eventual pair Attributes Returns a decorator LazyZip2 that allows strict operations to be performed on the lazily evaluated pairs or chained calls to lazyZip. Implicit conversion to Iterable[(A, B)] is also supported. Inherited from: Iterable

Invocation template:

```scala
receiver.lazyZip[B](that)
```

## `` com.cra.figaro.util.SelectableSet.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#map-5d3)

```scala
def map[B](f: T => B): Set[B]
```

Builds a new mutable set by applying a function to all elements of this mutable set.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => B ``.

Returns: `` Set[B] ``.

Source contract/attributes: Builds a new mutable set by applying a function to all elements of this mutable set. Type parameters B the element type of the returned mutable set . Value parameters f the function to apply to each element. Attributes Returns a new mutable set resulting from applying the given function f to each element of this mutable set and collecting the results. Inherited from: IterableOps

Invocation template:

```scala
receiver.map[B](f)
```

## `` com.cra.figaro.util.SelectableSet.mapResult ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#mapResult-34b)

```scala
def mapResult[NewTo](f: Set[T] => NewTo): Builder[T, NewTo]
```

A builder resulting from this builder by mapping the result using f.

Type parameters: `` NewTo ``.

Parameters, list 1: `` f: Set[T] => NewTo ``.

Returns: `` Builder[T, NewTo] ``.

Source contract/attributes: A builder resulting from this builder by mapping the result using f. Type parameters NewTo the type of collection produced by the returned builder Value parameters f the function to apply to this builder's result Attributes Inherited from: Builder

Invocation template:

```scala
receiver.mapResult[NewTo](f)
```

## `` com.cra.figaro.util.SelectableSet.max ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#max-ffffff34)

```scala
def max[B >: T](implicit ord: Ordering[B]): T
```

Finds the largest element.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` T ``.

Source contract/attributes: Finds the largest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns the largest element of this mutable set with respect to the ordering ord. Throws UnsupportedOperationException if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.max[B](using ord)
```

## `` com.cra.figaro.util.SelectableSet.maxBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#maxBy-fffff0c5)

```scala
def maxBy[B](f: T => B)(implicit ord: Ordering[B]): T
```

Finds the first element which yields the largest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` T ``.

Source contract/attributes: Finds the first element which yields the largest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns the first element of this mutable set with the largest value measured by function f with respect to the ordering ord. Throws UnsupportedOperationException if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxBy[B](f)(using ord)
```

## `` com.cra.figaro.util.SelectableSet.maxByOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#maxByOption-fffff66f)

```scala
def maxByOption[B](f: T => B)(implicit ord: Ordering[B]): Option[T]
```

Finds the first element which yields the largest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the first element which yields the largest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns an option value containing the first element of this mutable set with the largest value measured by function f with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxByOption[B](f)(using ord)
```

## `` com.cra.figaro.util.SelectableSet.maxOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#maxOption-fffff45e)

```scala
def maxOption[B >: T](implicit ord: Ordering[B]): Option[T]
```

Finds the largest element.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the largest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns an option value containing the largest element of this mutable set with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.maxOption[B](using ord)
```

## `` com.cra.figaro.util.SelectableSet.min ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#min-ffffff34)

```scala
def min[B >: T](implicit ord: Ordering[B]): T
```

Finds the smallest element.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` T ``.

Source contract/attributes: Finds the smallest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns the smallest element of this mutable set with respect to the ordering ord. Throws UnsupportedOperationException if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.min[B](using ord)
```

## `` com.cra.figaro.util.SelectableSet.minBy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#minBy-fffff0c5)

```scala
def minBy[B](f: T => B)(implicit ord: Ordering[B]): T
```

Finds the first element which yields the smallest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` T ``.

Source contract/attributes: Finds the first element which yields the smallest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns the first element of this mutable set with the smallest value measured by function f with respect to the ordering ord. Throws UnsupportedOperationException if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minBy[B](f)(using ord)
```

## `` com.cra.figaro.util.SelectableSet.minByOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#minByOption-fffff66f)

```scala
def minByOption[B](f: T => B)(implicit ord: Ordering[B]): Option[T]
```

Finds the first element which yields the smallest value measured by function f.

Type parameters: `` B ``.

Parameters, list 1: `` f: T => B ``.

Context parameters, list 2: `` ord: Ordering[B] ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the first element which yields the smallest value measured by function f. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the function f. Value parameters f The measuring function. ord An ordering to be used for comparing elements. Attributes Returns an option value containing the first element of this mutable set with the smallest value measured by function f with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minByOption[B](f)(using ord)
```

## `` com.cra.figaro.util.SelectableSet.minOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#minOption-fffff45e)

```scala
def minOption[B >: T](implicit ord: Ordering[B]): Option[T]
```

Finds the smallest element.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` ord: Ordering[B] ``.

Returns: `` Option[T] ``.

Source contract/attributes: Finds the smallest element. Note: will not terminate for infinite-sized collections. Type parameters B The type over which the ordering is defined. Value parameters ord An ordering to be used for comparing elements. Attributes Returns an option value containing the smallest element of this mutable set with respect to the ordering ord. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.minOption[B](using ord)
```

## `` com.cra.figaro.util.SelectableSet.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#mkString-0)

```scala
final def mkString: String
```

Displays all elements of this mutable set in a string.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Displays all elements of this mutable set in a string. Delegates to addString, which can be overridden. Attributes Returns a string representation of this mutable set. In the resulting string the string representations (w.r.t. the method toString) of all elements of this mutable set follow each other without any separator string. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString
```

## `` com.cra.figaro.util.SelectableSet.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#mkString-fffff2ca)

```scala
final def mkString(sep: String): String
```

Displays all elements of this mutable set in a string using a separator string.

Type parameters: none.

Parameters, list 1: `` sep: String ``.

Returns: `` String ``.

Source contract/attributes: Displays all elements of this mutable set in a string using a separator string. Delegates to addString, which can be overridden. Value parameters sep the separator string. Attributes Returns a string representation of this mutable set. In the resulting string the string representations (w.r.t. the method toString) of all elements of this mutable set are separated by the string sep. Example List(1, 2, 3).mkString("|") = "1|2|3" Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString(sep)
```

## `` com.cra.figaro.util.SelectableSet.mkString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#mkString-d94)

```scala
final def mkString(start: String, sep: String, end: String): String
```

Displays all elements of this mutable set in a string using start, end, and separator strings.

Type parameters: none.

Parameters, list 1: `` start: String ``; `` sep: String ``; `` end: String ``.

Returns: `` String ``.

Source contract/attributes: Displays all elements of this mutable set in a string using start, end, and separator strings. Delegates to addString, which can be overridden. Value parameters end the ending string. sep the separator string. start the starting string. Attributes Returns a string representation of this mutable set. The resulting string begins with the string start and ends with the string end. Inside, the string representations (w.r.t. the method toString) of all elements of this mutable set are separated by the string sep. Example List(1, 2, 3).mkString("(", "; ", ")") = "(1; 2; 3)" Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.mkString(start, sep, end)
```

## `` com.cra.figaro.util.SelectableSet.nonEmpty ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#nonEmpty-0)

```scala
def nonEmpty: Boolean
```

Tests whether the mutable set is not empty.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether the mutable set is not empty. Attributes Returns true if the mutable set contains at least one element, false otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.nonEmpty
```

## `` com.cra.figaro.util.SelectableSet.partition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#partition-243)

```scala
def partition(p: T => Boolean): (Set[T], Set[T])
```

A pair of, first, all elements that satisfy predicate p and, second, all elements that do not.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` (Set[T], Set[T]) ``.

Source contract/attributes: A pair of, first, all elements that satisfy predicate p and, second, all elements that do not. The two mutable set correspond to the result of filter and filterNot, respectively. The default implementation provided here needs to traverse the collection twice. Strict collections have an overridden version of partition in StrictOptimizedIterableOps, which requires only a single traversal. Value parameters p the predicate used to test elements Attributes Returns a pair of mutable sets: the first containing all elements that satisfy p, the second containing those that do not Inherited from: IterableOps

Invocation template:

```scala
receiver.partition(p)
```

## `` com.cra.figaro.util.SelectableSet.partitionMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#partitionMap-2d1)

```scala
def partitionMap[A1, A2](f: T => Either[A1, A2]): (Set[A1], Set[A2])
```

Applies a function f to each element of the mutable set and returns a pair of mutable sets: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right.

Type parameters: `` A1 ``; `` A2 ``.

Parameters, list 1: `` f: T => Either[A1, A2] ``.

Returns: `` (Set[A1], Set[A2]) ``.

Source contract/attributes: Applies a function f to each element of the mutable set and returns a pair of mutable sets: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right. Example: val xs = $Coll(1, "one", 2, "two", 3, "three") partitionMap { case i: Int => Left(i) case s: String => Right(s) } // xs == ($Coll(1, 2, 3), // $Coll(one, two, three)) Type parameters A1 the element type of the first resulting collection A2 the element type of the second resulting collection Value parameters f the 'split function' mapping the elements of this mutable set to an scala.util.Either Attributes Returns a pair of mutable sets: the first one made of those values returned by f that were wrapped in scala.util.Left, and the second one made of those wrapped in scala.util.Right. Inherited from: IterableOps

Invocation template:

```scala
receiver.partitionMap[A1, A2](f)
```

## `` com.cra.figaro.util.SelectableSet.product ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#product-533)

```scala
def product[B >: T](implicit num: Numeric[B]): B
```

Multiplies together the elements of this collection.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` num: Numeric[B] ``.

Returns: `` B ``.

Source contract/attributes: Multiplies together the elements of this collection. The default implementation uses reduce for a known non-empty collection, foldLeft otherwise. Note: will not terminate for infinite-sized collections. Type parameters B the result type of the * operator. Value parameters num an implicit parameter defining a set of numeric operations which includes the * operator to be used in forming the product. Attributes Returns the product of all elements of this mutable set with respect to the * operator in num. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.product[B](using num)
```

## `` com.cra.figaro.util.SelectableSet.reduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#reduce-fffff3d4)

```scala
def reduce[B >: T](op: (B, B) => B): B
```

Applies the given binary operator op to all elements of this mutable set.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (B, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this mutable set. For each application of the operator, each operand is either an element of this mutable set or another such application of the operator. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. If this collection is ordered, then for any application of the operator, the element(s) appearing in the left operand will precede those in the right. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. Attributes Returns The result of applying op between all the elements if the mutable set is nonempty. Throws UnsupportedOperationException if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduce[B](op)
```

## `` com.cra.figaro.util.SelectableSet.reduceLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#reduceLeft-fffff3d4)

```scala
def reduceLeft[B >: T](op: (B, T) => B): B
```

Applies the given binary operator op to all elements of this mutable set, going left to right.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (B, T) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this mutable set, going left to right. "Going left to right" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this mutable set, the result is op( op( op( ... op(x,,1,,, x,,2,,) ... ), x,,n-1,,), x,,n,,). If this collection is not ordered, then for each application of the operator, each right operand is an element. In addition, the leftmost operand is the first element of this mutable set and each other left operand is itself an application of the operator. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of applying op to all elements of this mutable set, going left to right. Throws UnsupportedOperationException if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceLeft[B](op)
```

## `` com.cra.figaro.util.SelectableSet.reduceLeftOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#reduceLeftOption-8fe)

```scala
def reduceLeftOption[B >: T](op: (B, T) => B): Option[B]
```

If this mutable set is nonempty, reduces it with the given binary operator op, going left to right.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (B, T) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this mutable set is nonempty, reduces it with the given binary operator op, going left to right. The behavior is the same as reduceLeft except that the value is None if the mutable set is empty. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of reducing this mutable set with op going left to right if the mutable set is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceLeftOption[B](op)
```

## `` com.cra.figaro.util.SelectableSet.reduceOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#reduceOption-8fe)

```scala
def reduceOption[B >: T](op: (B, B) => B): Option[B]
```

If this mutable set is nonempty, reduces it with the given binary operator op.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (B, B) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this mutable set is nonempty, reduces it with the given binary operator op. The behavior is the same as reduce except that the value is None if the mutable set is empty. The order of applications of the operator is unspecified and may be nondeterministic. Each element appears exactly once in the computation. Note: might return different results for different runs, unless either of the following conditions is met: (1) the operator is associative, and the underlying collection type is ordered; or (2) the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B A type parameter for the binary operator, a supertype of A. Value parameters op A binary operator; must be associative for the result to always be the same across runs. Attributes Returns The result of reducing this mutable set with op if the mutable set is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceOption[B](op)
```

## `` com.cra.figaro.util.SelectableSet.reduceRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#reduceRight-fffff3d4)

```scala
def reduceRight[B >: T](op: (T, B) => B): B
```

Applies the given binary operator op to all elements of this mutable set, going right to left.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (T, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Applies the given binary operator op to all elements of this mutable set, going right to left. "Going right to left" only makes sense if this collection is ordered: then if x,,1,,, x,,2,,, ..., x,,n,, are the elements of this mutable set, the result is op(x,,1,,, op(x,,2,,, op( ... op(x,,n-1,,, x,,n,,) ... ))). If this collection is not ordered, then for each application of the operator, each left operand is an element. In addition, the rightmost operand is the last element of this mutable set and each other right operand is itself an application of the operator. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of applying op to all elements of this mutable set, going right to left. Throws UnsupportedOperationException if this mutable set is empty. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceRight[B](op)
```

## `` com.cra.figaro.util.SelectableSet.reduceRightOption ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#reduceRightOption-8fe)

```scala
def reduceRightOption[B >: T](op: (T, B) => B): Option[B]
```

If this mutable set is nonempty, reduces it with the given binary operator op, going right to left.

Type parameters: `` B >: T ``.

Parameters, list 1: `` op: (T, B) => B ``.

Returns: `` Option[B] ``.

Source contract/attributes: If this mutable set is nonempty, reduces it with the given binary operator op, going right to left. The behavior is the same as reduceRight except that the value is None if the mutable set is empty. Each element appears exactly once in the computation. Note: might return different results for different runs, unless the underlying collection type is ordered or the operator is associative and commutative. Note: will not terminate for infinite-sized collections. Type parameters B The result type of the binary operator, a supertype of A. Value parameters op A binary operator. Attributes Returns The result of reducing this mutable set with op going right to left if the mutable set is nonempty, inside a Some, and None otherwise. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.reduceRightOption[B](op)
```

## `` com.cra.figaro.util.SelectableSet.remove ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#remove-4ad)

```scala
def remove(elem: T): Boolean
```

Removes an element from this set.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` Boolean ``.

Source contract/attributes: Removes an element from this set. Value parameters elem the element to be removed Attributes Returns true if this set contained the element before it was removed Inherited from: SetOps

Invocation template:

```scala
receiver.remove(elem)
```

## `` com.cra.figaro.util.SelectableSet.result ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#result-180)

```scala
def result(): Set[T]
```

Result collection consisting of all elements appended so far.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[T] ``.

Source contract/attributes: Result collection consisting of all elements appended so far. Attributes Inherited from: SetOps

Invocation template:

```scala
receiver.result()
```

## `` com.cra.figaro.util.SelectableSet.scan ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#scan-fffff9e1)

```scala
def scan[B >: T](z: B)(op: (B, B) => B): Set[B]
```

Computes a prefix scan of the elements of the collection.

Type parameters: `` B >: T ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, B) => B ``.

Returns: `` Set[B] ``.

Source contract/attributes: Computes a prefix scan of the elements of the collection. Note: The neutral element z may be applied more than once. Type parameters B element type of the resulting collection Value parameters op the associative operator for the scan z neutral element for the operator op Attributes Returns a new mutable set containing the prefix scan of the elements in this mutable set Inherited from: IterableOps

Invocation template:

```scala
receiver.scan[B](z)(op)
```

## `` com.cra.figaro.util.SelectableSet.scanLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#scanLeft-fffff9e1)

```scala
def scanLeft[B](z: B)(op: (B, T) => B): Set[B]
```

Produces a mutable set containing cumulative results of applying the operator going left to right, including the initial value.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, T) => B ``.

Returns: `` Set[B] ``.

Source contract/attributes: Produces a mutable set containing cumulative results of applying the operator going left to right, including the initial value. Note: will not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Type parameters B the type of the elements in the resulting collection Value parameters op the binary operator applied to the intermediate result and the element z the initial value Attributes Returns collection with intermediate results Inherited from: IterableOps

Invocation template:

```scala
receiver.scanLeft[B](z)(op)
```

## `` com.cra.figaro.util.SelectableSet.scanRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#scanRight-fffff9e1)

```scala
def scanRight[B](z: B)(op: (T, B) => B): Set[B]
```

Produces a collection containing cumulative results of applying the operator going right to left. The head of the collection is the last cumulative result.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (T, B) => B ``.

Returns: `` Set[B] ``.

Source contract/attributes: Produces a collection containing cumulative results of applying the operator going right to left. The head of the collection is the last cumulative result. Note: will not terminate for infinite-sized collections. Note: might return different results for different runs, unless the underlying collection type is ordered. Note: Even when applied to a view or a lazy collection it will always force the elements. Example: List(1, 2, 3, 4).scanRight(0)(_ + _) == List(10, 9, 7, 4, 0) Type parameters B the type of the elements in the resulting collection Value parameters op the binary operator applied to the intermediate result and the element z the initial value Attributes Returns collection with intermediate results Inherited from: IterableOps

Invocation template:

```scala
receiver.scanRight[B](z)(op)
```

## `` com.cra.figaro.util.SelectableSet.size ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#size-0)

```scala
def size: Int
```

The size of this mutable set.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: The size of this mutable set. Note: will not terminate for infinite-sized collections. Attributes Returns the number of elements in this mutable set. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.size
```

## `` com.cra.figaro.util.SelectableSet.sizeCompare ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#sizeCompare-32d)

```scala
def sizeCompare(that: Iterable[_]): Int
```

Compares the size of this mutable set to the size of another Iterable.

Type parameters: none.

Parameters, list 1: `` that: Iterable[_] ``.

Returns: `` Int ``.

Source contract/attributes: Compares the size of this mutable set to the size of another Iterable. Value parameters that the Iterable whose size is compared with this mutable set's size. Attributes Returns A value x where x < 0 if this.size < that.size x == 0 if this.size == that.size x > 0 if this.size > that.size The method as implemented here does not call size directly; its running time is O(this.size min that.size) instead of O(this.size + that.size). The method should be overridden if computing size is cheap and knownSize returns -1. Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeCompare(that)
```

## `` com.cra.figaro.util.SelectableSet.sizeCompare ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#sizeCompare-fffffbe0)

```scala
def sizeCompare(otherSize: Int): Int
```

Compares the size of this mutable set to a test value.

Type parameters: none.

Parameters, list 1: `` otherSize: Int ``.

Returns: `` Int ``.

Source contract/attributes: Compares the size of this mutable set to a test value. Value parameters otherSize the test value that gets compared with the size. Attributes Returns A value x where x < 0 if this.size < otherSize x == 0 if this.size == otherSize x > 0 if this.size > otherSize The default implementation does not call size, which may be arbitrarily expensive. It will use knownSize if nonnegative. Otherwise, it will iterate to compute an element count, with complexity O(size min otherSize). This method should be overridden if computing size is cheap and knownSize returns -1. In that case, the custom implementation should just compare size and otherSize directly. See also sizeIs Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeCompare(otherSize)
```

## `` com.cra.figaro.util.SelectableSet.sizeHint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#sizeHint-fffff4f8)

```scala
final def sizeHint(coll: IterableOnce[_], delta: Int = ...): Unit
```

Gives a hint that the result of this builder is expected to have the same size as the given collection, plus some delta.

Type parameters: none.

Parameters, list 1: `` coll: IterableOnce[_] ``; `` delta: Int = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Gives a hint that the result of this builder is expected to have the same size as the given collection, plus some delta. This method provides a hint only if the collection has a known size, as specified by the following code: val coll: scala.collection.IterableOnce[?] = ??? val delta: Int = 0 def sizeHint(size: Int): Unit = () if (coll.knownSize != -1) if (coll.knownSize + delta <= 0) sizeHint(0) else sizeHint(coll.knownSize + delta) If the delta is negative and the result size is known to be negative, then the size hint is issued at zero. Some builder classes will optimize their representation based on the hint. However, builder implementations are required to work correctly even if the hint is wrong, i.e., if a different number of elements is added. Value parameters coll the collection which serves as a hint for the result's size. delta a correction to add to the coll.size to produce the size hint (zero if omitted). Attributes Inherited from: Builder

Invocation template:

```scala
receiver.sizeHint(coll, delta)
```

## `` com.cra.figaro.util.SelectableSet.sizeHint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#sizeHint-fffff3b3)

```scala
def sizeHint(size: Int): Unit
```

Gives a hint how many elements are expected to be added in total by the time result is called.

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Unit ``.

Source contract/attributes: Gives a hint how many elements are expected to be added in total by the time result is called. Some builder classes will optimize their representation based on the hint. However, builder implementations are required to work correctly even if the hint is wrong, e.g., a different number of elements is added, or the hint is out of range. The default implementation simply ignores the hint. Value parameters size the hint how many elements will be added. Attributes Inherited from: Builder

Invocation template:

```scala
receiver.sizeHint(size)
```

## `` com.cra.figaro.util.SelectableSet.sizeHintBounded ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#sizeHintBounded-fffffd7f)

```scala
final def sizeHintBounded(size: Int, boundingColl: Iterable[_]): Unit
```

Gives a hint how many elements are expected to be added when the next result is called, together with an upper bound given by the size of some other collection. Some builder classes will optimize their representation based on the hint. However, builder implementations are still required to work correctly even if the hint is wrong, i.e. a different number of elements is added.

Type parameters: none.

Parameters, list 1: `` size: Int ``; `` boundingColl: Iterable[_] ``.

Returns: `` Unit ``.

Source contract/attributes: Gives a hint how many elements are expected to be added when the next result is called, together with an upper bound given by the size of some other collection. Some builder classes will optimize their representation based on the hint. However, builder implementations are still required to work correctly even if the hint is wrong, i.e. a different number of elements is added. Value parameters boundingColl the bounding collection. If it is an IndexedSeqLike, then sizes larger than collection's size are reduced. size the hint how many elements will be added. Attributes Inherited from: Builder

Invocation template:

```scala
receiver.sizeHintBounded(size, boundingColl)
```

## `` com.cra.figaro.util.SelectableSet.sizeIs ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#sizeIs-0)

```scala
final def sizeIs: SizeCompareOps
```

Returns a value class containing operations for comparing the size of this mutable set to a test value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` SizeCompareOps ``.

Source contract/attributes: Returns a value class containing operations for comparing the size of this mutable set to a test value. These operations are implemented in terms of `sizeCompare(Int)`, and allow the following more readable usages: this.sizeIs < size // this.sizeCompare(size) < 0 this.sizeIs <= size // this.sizeCompare(size) <= 0 this.sizeIs == size // this.sizeCompare(size) == 0 this.sizeIs != size // this.sizeCompare(size) != 0 this.sizeIs >= size // this.sizeCompare(size) >= 0 this.sizeIs > size // this.sizeCompare(size) > 0 Attributes Returns a wrapper that allows size comparisons using operators such as =, and > Inherited from: IterableOps

Invocation template:

```scala
receiver.sizeIs
```

## `` com.cra.figaro.util.SelectableSet.slice ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#slice-573)

```scala
def slice(from: Int, until: Int): Set[T]
```

Selects an interval of elements. The returned mutable set is made up of all elements x which satisfy the invariant:

Type parameters: none.

Parameters, list 1: `` from: Int ``; `` until: Int ``.

Returns: `` Set[T] ``.

Source contract/attributes: Selects an interval of elements. The returned mutable set is made up of all elements x which satisfy the invariant: from <= indexOf(x) < until Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters from the lowest index to include from this mutable set. until the lowest index to EXCLUDE from this mutable set. Attributes Returns a mutable set containing the elements greater than or equal to index from extending up to (but not including) index until of this mutable set. Example List('a', 'b', 'c', 'd', 'e').slice(1, 3) == List('b', 'c') Inherited from: IterableOps

Invocation template:

```scala
receiver.slice(from, until)
```

## `` com.cra.figaro.util.SelectableSet.sliding ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#sliding-fffff156)

```scala
def sliding(size: Int, step: Int): Iterator[Set[T]]
```

Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped).

Type parameters: none.

Parameters, list 1: `` size: Int ``; `` step: Int ``.

Returns: `` Iterator[Set[T]] ``.

Source contract/attributes: Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped). The returned iterator will be empty when called on an empty collection. The last element the iterator produces may be smaller than the window size when the original collection isn't exhausted by the window before it and its last element isn't skipped by the step before it. Value parameters size the number of elements per group step the distance between the first elements of successive groups Attributes Returns An iterator producing mutable sets of size size, except the last element (which may be the only element) will be smaller if there are fewer than size elements remaining to be grouped. See also scala.collection.Iterator, method sliding Example List(1, 2, 3, 4, 5).sliding(2, 2) = Iterator(List(1, 2), List(3, 4), List(5)) List(1, 2, 3, 4, 5, 6).sliding(2, 3) = Iterator(List(1, 2), List(4, 5)) Inherited from: IterableOps

Invocation template:

```scala
receiver.sliding(size, step)
```

## `` com.cra.figaro.util.SelectableSet.sliding ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#sliding-fffffbef)

```scala
def sliding(size: Int): Iterator[Set[T]]
```

Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped).

Type parameters: none.

Parameters, list 1: `` size: Int ``.

Returns: `` Iterator[Set[T]] ``.

Source contract/attributes: Groups elements in fixed size blocks by passing a "sliding window" over them (as opposed to partitioning them, as is done in grouped). An empty collection returns an empty iterator, and a non-empty collection containing fewer elements than the window size returns an iterator that will produce the original collection as its only element. Value parameters size the number of elements per group Attributes Returns An iterator producing mutable sets of size size, except for a non-empty collection with less than size elements, which returns an iterator that produces the source collection itself as its only element. See also scala.collection.Iterator, method sliding Example List().sliding(2) = empty iterator List(1).sliding(2) = Iterator(List(1)) List(1, 2).sliding(2) = Iterator(List(1, 2)) List(1, 2, 3).sliding(2) = Iterator(List(1, 2), List(2, 3)) Inherited from: IterableOps

Invocation template:

```scala
receiver.sliding(size)
```

## `` com.cra.figaro.util.SelectableSet.span ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#span-243)

```scala
def span(p: T => Boolean): (Set[T], Set[T])
```

Splits this mutable set into a prefix/suffix pair according to a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` (Set[T], Set[T]) ``.

Source contract/attributes: Splits this mutable set into a prefix/suffix pair according to a predicate. Note: c span p is equivalent to (but possibly more efficient than) (c takeWhile p, c dropWhile p), provided the evaluation of the predicate p does not cause any side effects. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the test predicate Attributes Returns a pair consisting of the longest prefix of this mutable set whose elements all satisfy p, and the rest of this mutable set. Inherited from: IterableOps

Invocation template:

```scala
receiver.span(p)
```

## `` com.cra.figaro.util.SelectableSet.splitAt ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#splitAt-fffffbd9)

```scala
override def splitAt(n: Int): (Set[T], Set[T])
```

Splits this mutable set into a prefix/suffix pair at a given position.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` (Set[T], Set[T]) ``.

Source contract/attributes: Splits this mutable set into a prefix/suffix pair at a given position. Note: c splitAt n is equivalent to (but possibly more efficient than) (c take n, c drop n). Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the position at which to split. Attributes Returns a pair of mutable sets consisting of the first n elements of this mutable set, and the other elements. Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.splitAt(n)
```

## `` com.cra.figaro.util.SelectableSet.stepper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#stepper-190)

```scala
def stepper[S <: Stepper[_]](implicit shape: StepperShape[T, S]): S
```

Returns a scala.collection.Stepper for the elements of this collection.

Type parameters: `` S <: Stepper[_] ``.

Context parameters, list 1: `` shape: StepperShape[T, S] ``.

Returns: `` S ``.

Source contract/attributes: Returns a scala.collection.Stepper for the elements of this collection. The Stepper enables creating a Java stream to operate on the collection, see scala.jdk.StreamConverters. For collections holding primitive values, the Stepper can be used as an iterator which doesn't box the elements. The implicit scala.collection.StepperShape parameter defines the resulting Stepper type according to the element type of this collection. For collections of Int, Short, Byte or Char, an scala.collection.IntStepper is returned For collections of Double or Float, a scala.collection.DoubleStepper is returned For collections of Long a scala.collection.LongStepper is returned For any other element type, an scala.collection.AnyStepper is returned Note that this method is overridden in subclasses and the return type is refined to S with EfficientSplit, for example scala.collection.IndexedSeqOps.stepper. For Steppers marked with scala.collection.Stepper.EfficientSplit, the converters in scala.jdk.StreamConverters allow creating parallel streams, whereas bare Steppers can be converted only to sequential streams. Type parameters S the type of the returned Stepper, determined by the implicit StepperShape Attributes Inherited from: IterableOnce

Invocation template:

```scala
receiver.stepper[S](using shape)
```

## `` com.cra.figaro.util.SelectableSet.subsetOf ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#subsetOf-b06)

```scala
def subsetOf(that: Set[T]): Boolean
```

Tests whether this set is a subset of another set.

Type parameters: none.

Parameters, list 1: `` that: Set[T] ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this set is a subset of another set. Value parameters that the set to test. Attributes Returns true if this set is a subset of that, i.e. if every element of this set is also an element of that. Inherited from: SetOps

Invocation template:

```scala
receiver.subsetOf(that)
```

## `` com.cra.figaro.util.SelectableSet.subsets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#subsets-fffff136)

```scala
def subsets(): Iterator[Set[T]]
```

An iterator over all subsets of this set.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Iterator[Set[T]] ``.

Source contract/attributes: An iterator over all subsets of this set. Attributes Returns the iterator. Inherited from: SetOps

Invocation template:

```scala
receiver.subsets()
```

## `` com.cra.figaro.util.SelectableSet.subsets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#subsets-fffffbef)

```scala
def subsets(len: Int): Iterator[Set[T]]
```

An iterator over all subsets of this set of the given size. If the requested size is impossible, an empty iterator is returned.

Type parameters: none.

Parameters, list 1: `` len: Int ``.

Returns: `` Iterator[Set[T]] ``.

Source contract/attributes: An iterator over all subsets of this set of the given size. If the requested size is impossible, an empty iterator is returned. Value parameters len the size of the subsets. Attributes Returns the iterator. Inherited from: SetOps

Invocation template:

```scala
receiver.subsets(len)
```

## `` com.cra.figaro.util.SelectableSet.subtractAll ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#subtractAll-fffff8b8)

```scala
def subtractAll(xs: IterableOnce[T]): this.type
```

Removes all elements produced by an iterator from this mutable set.

Type parameters: none.

Parameters, list 1: `` xs: IterableOnce[T] ``.

Returns: `` this.type ``.

Source contract/attributes: Removes all elements produced by an iterator from this mutable set. Value parameters xs the iterator producing the elements to remove. Attributes Returns the mutable set itself Inherited from: Shrinkable

Invocation template:

```scala
receiver.subtractAll(xs)
```

## `` com.cra.figaro.util.SelectableSet.sum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#sum-533)

```scala
def sum[B >: T](implicit num: Numeric[B]): B
```

Sums the elements of this collection.

Type parameters: `` B >: T ``.

Context parameters, list 1: `` num: Numeric[B] ``.

Returns: `` B ``.

Source contract/attributes: Sums the elements of this collection. The default implementation uses reduce for a known non-empty collection, foldLeft otherwise. Note: will not terminate for infinite-sized collections. Type parameters B the result type of the + operator. Value parameters num an implicit parameter defining a set of numeric operations which includes the + operator to be used in forming the sum. Attributes Returns the sum of all elements of this mutable set with respect to the + operator in num. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.sum[B](using num)
```

## `` com.cra.figaro.util.SelectableSet.tail ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#tail-0)

```scala
def tail: Set[T]
```

The rest of the collection without its first element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[T] ``.

Source contract/attributes: The rest of the collection without its first element. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.tail
```

## `` com.cra.figaro.util.SelectableSet.tails ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#tails-0)

```scala
def tails: Iterator[Set[T]]
```

Iterates over the tails of this mutable set. The first value will be this mutable set and the final one will be an empty mutable set, with the intervening values the results of successive applications of tail.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Set[T]] ``.

Source contract/attributes: Iterates over the tails of this mutable set. The first value will be this mutable set and the final one will be an empty mutable set, with the intervening values the results of successive applications of tail. Attributes Returns an iterator over all the tails of this mutable set Example List(1,2,3).tails = Iterator(List(1,2,3), List(2,3), List(3), Nil) Inherited from: IterableOps

Invocation template:

```scala
receiver.tails
```

## `` com.cra.figaro.util.SelectableSet.take ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#take-aba)

```scala
def take(n: Int): Set[T]
```

Selects the first n elements.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Set[T] ``.

Source contract/attributes: Selects the first n elements. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to take from this mutable set. Attributes Returns a mutable set consisting only of the first n elements of this mutable set, or else the whole mutable set, if it has less than n elements. If n is negative, returns an empty mutable set. Inherited from: IterableOps

Invocation template:

```scala
receiver.take(n)
```

## `` com.cra.figaro.util.SelectableSet.takeRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#takeRight-aba)

```scala
def takeRight(n: Int): Set[T]
```

Selects the last n elements.

Type parameters: none.

Parameters, list 1: `` n: Int ``.

Returns: `` Set[T] ``.

Source contract/attributes: Selects the last n elements. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters n the number of elements to take from this mutable set. Attributes Returns a mutable set consisting only of the last n elements of this mutable set, or else the whole mutable set, if it has less than n elements. If n is negative, returns an empty mutable set. Inherited from: IterableOps

Invocation template:

```scala
receiver.takeRight(n)
```

## `` com.cra.figaro.util.SelectableSet.takeWhile ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#takeWhile-c24)

```scala
def takeWhile(p: T => Boolean): Set[T]
```

Takes longest prefix of elements that satisfy a predicate.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Set[T] ``.

Source contract/attributes: Takes longest prefix of elements that satisfy a predicate. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p The predicate used to test elements. Attributes Returns the longest prefix of this mutable set whose elements all satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.takeWhile(p)
```

## `` com.cra.figaro.util.SelectableSet.tapEach ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#tapEach-5d3)

```scala
override def tapEach[U](f: T => U): Set[T]
```

Applies a side-effecting function to each element in this collection. Strict collections will apply f to their elements immediately, while lazy collections like Views and LazyLists will only apply f on each element if and when that element is evaluated, and each time that element is evaluated.

Type parameters: `` U ``.

Parameters, list 1: `` f: T => U ``.

Returns: `` Set[T] ``.

Source contract/attributes: Applies a side-effecting function to each element in this collection. Strict collections will apply f to their elements immediately, while lazy collections like Views and LazyLists will only apply f on each element if and when that element is evaluated, and each time that element is evaluated. Type parameters U the return type of f Value parameters f a function to apply to each element in this mutable set Attributes Returns The same logical collection as this Definition Classes IterableOps -> IterableOnceOps Inherited from: IterableOps

Invocation template:

```scala
receiver.tapEach[U](f)
```

## `` com.cra.figaro.util.SelectableSet.to ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#to-fffff006)

```scala
def to[C1](factory: Factory[T, C1]): C1
```

Given a collection factory factory, converts this mutable set to the appropriate representation for the current element type A. Example uses:

Type parameters: `` C1 ``.

Parameters, list 1: `` factory: Factory[T, C1] ``.

Returns: `` C1 ``.

Source contract/attributes: Given a collection factory factory, converts this mutable set to the appropriate representation for the current element type A. Example uses: import scala.collection.mutable.ArrayBuffer import scala.collection.immutable.BitSet val xs: Iterable[Int] = Seq(1, 2, 3, 4, 5) xs.to(List) xs.to(ArrayBuffer) xs.to(BitSet) // for xs: Iterable[Int] Type parameters C1 the target collection type Value parameters factory the factory for the target collection type Attributes Returns a new collection of type C1 containing all elements of this mutable set Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.to[C1](factory)
```

## `` com.cra.figaro.util.SelectableSet.toArray ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#toArray-fffff5df)

```scala
def toArray[B >: T : ClassTag]: Array[B]
```

Converts this mutable set to an Array.

Type parameters: `` B >: T : ClassTag ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Array[B] ``.

Source contract/attributes: Converts this mutable set to an Array. Implementation note: DO NOT call Array.from from this method. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This mutable set as an Array[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toArray[B]
```

## `` com.cra.figaro.util.SelectableSet.toBuffer ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#toBuffer-fffff87f)

```scala
final def toBuffer[B >: T]: Buffer[B]
```

Converts this mutable set to a Buffer.

Type parameters: `` B >: T ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Buffer[B] ``.

Source contract/attributes: Converts this mutable set to a Buffer. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This mutable set as a Buffer[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toBuffer[B]
```

## `` com.cra.figaro.util.SelectableSet.toIndexedSeq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#toIndexedSeq-0)

```scala
def toIndexedSeq: IndexedSeq[T]
```

Converts this mutable set to an IndexedSeq.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IndexedSeq[T] ``.

Source contract/attributes: Converts this mutable set to an IndexedSeq. Attributes Returns This mutable set as an IndexedSeq[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toIndexedSeq
```

## `` com.cra.figaro.util.SelectableSet.toList ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#toList-0)

```scala
def toList: List[T]
```

Converts this mutable set to a List.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[T] ``.

Source contract/attributes: Converts this mutable set to a List. Attributes Returns This mutable set as a List[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toList
```

## `` com.cra.figaro.util.SelectableSet.toMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#toMap-c0c)

```scala
def toMap[K, V](implicit ev: T <:< (K, V)): Map[K, V]
```

Converts this mutable set to a Map, given an implicit coercion from the mutable set's type to a key-value tuple.

Type parameters: `` K ``; `` V ``.

Context parameters, list 1: `` ev: T <:< (K, V) ``.

Returns: `` Map[K, V] ``.

Source contract/attributes: Converts this mutable set to a Map, given an implicit coercion from the mutable set's type to a key-value tuple. Type parameters K The key type for the resulting map. V The value type for the resulting map. Value parameters ev an implicit evidence that A is a subtype of (K, V) Attributes Returns This mutable set as a Map[K, V]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toMap[K, V](using ev)
```

## `` com.cra.figaro.util.SelectableSet.toSeq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#toSeq-0)

```scala
def toSeq: Seq[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Seq[T] ``.

Source contract/attributes: Attributes Returns This mutable set as a Seq[A]. This is equivalent to to(Seq) but might be faster. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toSeq
```

## `` com.cra.figaro.util.SelectableSet.toSet ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#toSet-fffffe9f)

```scala
def toSet[B >: T]: Set[B]
```

Converts this mutable set to a Set.

Type parameters: `` B >: T ``.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[B] ``.

Source contract/attributes: Converts this mutable set to a Set. Type parameters B The type of elements of the result, a supertype of A. Attributes Returns This mutable set as a Set[B]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toSet[B]
```

## `` com.cra.figaro.util.SelectableSet.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#toString-665)

```scala
override def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Set -> Function1 -> Iterable -> Any Inherited from: Set

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.util.SelectableSet.toVector ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#toVector-0)

```scala
def toVector: Vector[T]
```

Converts this mutable set to a Vector.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Vector[T] ``.

Source contract/attributes: Converts this mutable set to a Vector. Attributes Returns This mutable set as a Vector[A]. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toVector
```

## `` com.cra.figaro.util.SelectableSet.transpose ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#transpose-5d3)

```scala
def transpose[B](implicit asIterable: T => Iterable[B]): Set[Set[B]]
```

Transposes this mutable set of iterable collections into a mutable set of mutable sets.

Type parameters: `` B ``.

Context parameters, list 1: `` asIterable: T => Iterable[B] ``.

Returns: `` Set[Set[B]] ``.

Source contract/attributes: Transposes this mutable set of iterable collections into a mutable set of mutable sets. The resulting collection's type will be guided by the static type of mutable set. For example: val xs = List( Set(1, 2, 3), Set(4, 5, 6)).transpose // xs == List( // List(1, 4), // List(2, 5), // List(3, 6)) val ys = Vector( List(1, 2, 3), List(4, 5, 6)).transpose // ys == Vector( // Vector(1, 4), // Vector(2, 5), // Vector(3, 6)) Note: Even when applied to a view or a lazy collection it will always force the elements. Type parameters B the type of the elements of each iterable collection. Value parameters asIterable an implicit conversion which asserts that the element type of this mutable set is an Iterable. Attributes Returns a two-dimensional mutable set of mutable sets which has as nth row the nth column of this mutable set. Throws IllegalArgumentException if all collections in this mutable set are not of the same size. Inherited from: IterableOps

Invocation template:

```scala
receiver.transpose[B](using asIterable)
```

## `` com.cra.figaro.util.SelectableSet.union ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#union-dde)

```scala
final def union(that: Set[T]): Set[T]
```

Computes the union between of set and another set.

Type parameters: none.

Parameters, list 1: `` that: Set[T] ``.

Returns: `` Set[T] ``.

Source contract/attributes: Computes the union between of set and another set. Value parameters that the set to form the union with. Attributes Returns a new set consisting of all elements that are in this set or in the given set that. Inherited from: SetOps

Invocation template:

```scala
receiver.union(that)
```

## `` com.cra.figaro.util.SelectableSet.unzip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#unzip-2d1)

```scala
def unzip[A1, A2](implicit asPair: T => (A1, A2)): (Set[A1], Set[A2])
```

Converts this mutable set of pairs into two collections of the first and second half of each pair.

Type parameters: `` A1 ``; `` A2 ``.

Context parameters, list 1: `` asPair: T => (A1, A2) ``.

Returns: `` (Set[A1], Set[A2]) ``.

Source contract/attributes: Converts this mutable set of pairs into two collections of the first and second half of each pair. val xs = $Coll( (1, "one"), (2, "two"), (3, "three")).unzip // xs == ($Coll(1, 2, 3), // $Coll(one, two, three)) Type parameters A1 the type of the first half of the element pairs A2 the type of the second half of the element pairs Value parameters asPair an implicit conversion which asserts that the element type of this mutable set is a pair. Attributes Returns a pair of mutable sets, containing the first, respectively second half of each element pair of this mutable set. Inherited from: IterableOps

Invocation template:

```scala
receiver.unzip[A1, A2](using asPair)
```

## `` com.cra.figaro.util.SelectableSet.unzip3 ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#unzip3-a31)

```scala
def unzip3[A1, A2, A3](implicit asTriple: T => (A1, A2, A3)): (Set[A1], Set[A2], Set[A3])
```

Converts this mutable set of triples into three collections of the first, second, and third element of each triple.

Type parameters: `` A1 ``; `` A2 ``; `` A3 ``.

Context parameters, list 1: `` asTriple: T => (A1, A2, A3) ``.

Returns: `` (Set[A1], Set[A2], Set[A3]) ``.

Source contract/attributes: Converts this mutable set of triples into three collections of the first, second, and third element of each triple. val xs = $Coll( (1, "one", '1'), (2, "two", '2'), (3, "three", '3')).unzip3 // xs == ($Coll(1, 2, 3), // $Coll(one, two, three), // $Coll(1, 2, 3)) Type parameters A1 the type of the first member of the element triples A2 the type of the second member of the element triples A3 the type of the third member of the element triples Value parameters asTriple an implicit conversion which asserts that the element type of this mutable set is a triple. Attributes Returns a triple of mutable sets, containing the first, second, respectively third member of each element triple of this mutable set. Inherited from: IterableOps

Invocation template:

```scala
receiver.unzip3[A1, A2, A3](using asTriple)
```

## `` com.cra.figaro.util.SelectableSet.update ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#update-9b9)

```scala
def update(elem: T, included: Boolean): Unit
```

Updates the presence of a single element in this set.

Type parameters: none.

Parameters, list 1: `` elem: T ``; `` included: Boolean ``.

Returns: `` Unit ``.

Source contract/attributes: Updates the presence of a single element in this set. This method allows one to add or remove an element elem from this set depending on the value of parameter included. Typically, one would use the following syntax: val set = scala.collection.mutable.Set(1) val elem = 1 set(elem) = true // adds element set(elem) = false // removes element Value parameters elem the element to be added or removed included a flag indicating whether element should be included or excluded. Attributes Inherited from: SetOps

Invocation template:

```scala
receiver.update(elem, included)
```

## `` com.cra.figaro.util.SelectableSet.view ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#view-0)

```scala
def view: View[T]
```

A view over the elements of this collection.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` View[T] ``.

Source contract/attributes: A view over the elements of this collection. Attributes Inherited from: IterableOps

Invocation template:

```scala
receiver.view
```

## `` com.cra.figaro.util.SelectableSet.withFilter ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#withFilter-fffffb75)

```scala
def withFilter(p: T => Boolean): WithFilter[T, Set]
```

Creates a non-strict filter of this mutable set.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` WithFilter[T, Set] ``.

Source contract/attributes: Creates a non-strict filter of this mutable set. Note: the difference between c filter p and c withFilter p is that the former creates a new collection, whereas the latter only restricts the domain of subsequent map, flatMap, foreach, and withFilter operations. Note: might return different results for different runs, unless the underlying collection type is ordered. Value parameters p the predicate used to test elements. Attributes Returns an object of class WithFilter, which supports map, flatMap, foreach, and withFilter operations. All these operations apply to those elements of this mutable set which satisfy the predicate p. Inherited from: IterableOps

Invocation template:

```scala
receiver.withFilter(p)
```

## `` com.cra.figaro.util.SelectableSet.zip ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#zip-1dd)

```scala
def zip[B](that: IterableOnce[B]): Set[(T, B)]
```

Returns a mutable set formed from this mutable set and another iterable collection by combining corresponding elements in pairs. If one of the two collections is longer than the other, its remaining elements are ignored.

Type parameters: `` B ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Returns: `` Set[(T, B)] ``.

Source contract/attributes: Returns a mutable set formed from this mutable set and another iterable collection by combining corresponding elements in pairs. If one of the two collections is longer than the other, its remaining elements are ignored. Type parameters B the type of the second half of the returned pairs Value parameters that The iterable providing the second half of each result pair Attributes Returns a new mutable set containing pairs consisting of corresponding elements of this mutable set and that. The length of the returned collection is the minimum of the lengths of this mutable set and that. Inherited from: IterableOps

Invocation template:

```scala
receiver.zip[B](that)
```

## `` com.cra.figaro.util.SelectableSet.zipAll ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#zipAll-fffff401)

```scala
def zipAll[A1 >: T, B](that: Iterable[B], thisElem: A1, thatElem: B): Set[(A1, B)]
```

Returns a mutable set formed from this mutable set and another iterable collection by combining corresponding elements in pairs. If one of the two collections is shorter than the other, placeholder elements are used to extend the shorter collection to the length of the longer.

Type parameters: `` A1 >: T ``; `` B ``.

Parameters, list 1: `` that: Iterable[B] ``; `` thisElem: A1 ``; `` thatElem: B ``.

Returns: `` Set[(A1, B)] ``.

Source contract/attributes: Returns a mutable set formed from this mutable set and another iterable collection by combining corresponding elements in pairs. If one of the two collections is shorter than the other, placeholder elements are used to extend the shorter collection to the length of the longer. Type parameters A1 the type of the first element in each result pair (supertype of A) B the type of the second element in each result pair Value parameters that the iterable providing the second half of each result pair thatElem the element to be used to fill up the result if that is shorter than this mutable set. thisElem the element to be used to fill up the result if this mutable set is shorter than that. Attributes Returns a new mutable set containing pairs consisting of corresponding elements of this mutable set and that. The length of the returned collection is the maximum of the lengths of this mutable set and that. If this mutable set is shorter than that, thisElem values are used to pad the result. If that is shorter than this mutable set, thatElem values are used to pad the result. Inherited from: IterableOps

Invocation template:

```scala
receiver.zipAll[A1, B](that, thisElem, thatElem)
```

## `` com.cra.figaro.util.SelectableSet.zipWithIndex ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#zipWithIndex-0)

```scala
def zipWithIndex: Set[(T, Int)]
```

Zips this mutable set with its indices.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[(T, Int)] ``.

Source contract/attributes: Zips this mutable set with its indices. Attributes Returns A new mutable set containing pairs consisting of all elements of this mutable set paired with their index. Indices start at 0. Example List("a", "b", "c").zipWithIndex == List(("a", 0), ("b", 1), ("c", 2)) Inherited from: IterableOps

Invocation template:

```scala
receiver.zipWithIndex
```

## `` com.cra.figaro.util.SelectableSet.| ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#|-dde)

```scala
final def |(that: Set[T]): Set[T]
```

Alias for union.

Type parameters: none.

Parameters, list 1: `` that: Set[T] ``.

Returns: `` Set[T] ``.

Source contract/attributes: Alias for union. Value parameters that the set to form the union with Attributes Inherited from: SetOps

Invocation template:

```scala
receiver.|(that)
```

## `` com.cra.figaro.util.SelectableSet.addOne ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#addOne-b4a)

```scala
def addOne(elem: T): this.type
```

Add s a single element to this mutable set.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` this.type ``.

Source contract/attributes: Add s a single element to this mutable set. Value parameters elem the element to add. Attributes Returns the mutable set itself Inherited from: Growable

Invocation template:

```scala
receiver.addOne(elem)
```

## `` com.cra.figaro.util.SelectableSet.clear ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#clear-94c)

```scala
def clear(): Unit
```

Clears the contents of this builder. After execution of this method the builder will contain no elements.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clears the contents of this builder. After execution of this method the builder will contain no elements. Attributes Inherited from: Builder

Invocation template:

```scala
receiver.clear()
```

## `` com.cra.figaro.util.SelectableSet.contains ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#contains-4ad)

```scala
def contains(elem: T): Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: SetOps

Invocation template:

```scala
receiver.contains(elem)
```

## `` com.cra.figaro.util.SelectableSet.iterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#iterator-0)

```scala
def iterator: Iterator[T]
```

An scala.collection.Iterator over the elements of this mutable set.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[T] ``.

Source contract/attributes: An scala.collection.Iterator over the elements of this mutable set. If an IterableOnce object is in fact an scala.collection.Iterator, this method always returns itself, in its current state, but if it is an scala.collection.Iterable, this method always returns a new scala.collection.Iterator. Attributes Returns an iterator over all elements of this mutable set Inherited from: IterableOnce

Invocation template:

```scala
receiver.iterator
```

## `` com.cra.figaro.util.SelectableSet.subtractOne ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#subtractOne-fffff700)

```scala
def subtractOne(elem: T): this.type
```

Removes a single element from this mutable set.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` this.type ``.

Source contract/attributes: Removes a single element from this mutable set. Value parameters elem the element to remove. Attributes Returns the mutable set itself Inherited from: Shrinkable

Invocation template:

```scala
receiver.subtractOne(elem)
```

## `` com.cra.figaro.util.SelectableSet.+ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#+-fffff5c7)

```scala
def +(elem1: T, elem2: T, elems: T*): Set[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` elem1: T ``; `` elem2: T ``; `` elems: T* ``.

Returns: `` Set[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use ++ with an explicit collection argument instead of + with varargs Inherited from: SetOps

Invocation template:

```scala
receiver.+(elem1, elem2, elems*)
```

## `` com.cra.figaro.util.SelectableSet.+ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#+-fffff2c5)

```scala
def +(elem: T): Set[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` Set[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Consider requiring an immutable Set or fall back to Set.union Inherited from: SetOps

Invocation template:

```scala
receiver.+(elem)
```

## `` com.cra.figaro.util.SelectableSet.++: ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#++:-1dd)

```scala
def ++:[B >: T](that: IterableOnce[B]): Set[B]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B >: T ``.

Parameters, list 1: `` that: IterableOnce[B] ``.

Returns: `` Set[B] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use ++ instead of ++: for collections of type Iterable Inherited from: IterableOps

Invocation template:

```scala
receiver.++:[B](that)
```

## `` com.cra.figaro.util.SelectableSet.+= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#+=-fffff3cc)

```scala
final def +=(elem1: T, elem2: T, elems: T*): this.type
```

Add s two or more elements to this mutable set.

Type parameters: none.

Parameters, list 1: `` elem1: T ``; `` elem2: T ``; `` elems: T* ``.

Returns: `` this.type ``.

Source contract/attributes: Add s two or more elements to this mutable set. Value parameters elem1 the first element to add. elem2 the second element to add. elems the remaining elements to add. Attributes Returns the mutable set itself Deprecated [Since version 2.13.0] Use `++=` aka `addAll` instead of varargs `+=`; infix operations with an operand of multiple args will be deprecated Inherited from: Growable

Invocation template:

```scala
receiver.+=(elem1, elem2, elems*)
```

## `` com.cra.figaro.util.SelectableSet.- ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#--fffff5c7)

```scala
def -(elem1: T, elem2: T, elems: T*): Set[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` elem1: T ``; `` elem2: T ``; `` elems: T* ``.

Returns: `` Set[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use &- with an explicit collection argument instead of - with varargs Inherited from: SetOps

Invocation template:

```scala
receiver.-(elem1, elem2, elems*)
```

## `` com.cra.figaro.util.SelectableSet.- ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#--fffff2c5)

```scala
def -(elem: T): Set[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` elem: T ``.

Returns: `` Set[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Consider requiring an immutable Set or fall back to Set.diff Inherited from: SetOps

Invocation template:

```scala
receiver.-(elem)
```

## `` com.cra.figaro.util.SelectableSet.-- ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#---fffff87d)

```scala
def --(that: IterableOnce[T]): Set[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` that: IterableOnce[T] ``.

Returns: `` Set[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Consider requiring an immutable Set Inherited from: SetOps

Invocation template:

```scala
receiver.--(that)
```

## `` com.cra.figaro.util.SelectableSet.-= ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#-=-fffff702)

```scala
def -=(elem1: T, elem2: T, elems: T*): this.type
```

Removes two or more elements from this mutable set.

Type parameters: none.

Parameters, list 1: `` elem1: T ``; `` elem2: T ``; `` elems: T* ``.

Returns: `` this.type ``.

Source contract/attributes: Removes two or more elements from this mutable set. Value parameters elem1 the first element to remove. elem2 the second element to remove. elems the remaining elements to remove. Attributes Returns the mutable set itself Deprecated [Since version 2.13.3] Use `--=` aka `subtractAll` instead of varargs `-=`; infix operations with an operand of multiple args will be deprecated Inherited from: Shrinkable

Invocation template:

```scala
receiver.-=(elem1, elem2, elems*)
```

## `` com.cra.figaro.util.SelectableSet./: ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#/:-fffff9e1)

```scala
final def /:[B](z: B)(op: (B, T) => B): B
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (B, T) => B ``.

Returns: `` B ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use foldLeft instead of /: Inherited from: IterableOnceOps

Invocation template:

```scala
receiver./:[B](z)(op)
```

## `` com.cra.figaro.util.SelectableSet.:\ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#:\-fffff9e1)

```scala
final def :\[B](z: B)(op: (T, B) => B): B
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B ``.

Parameters, list 1: `` z: B ``.

Parameters, list 2: `` op: (T, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use foldRight instead of :\\ Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.:\[B](z)(op)
```

## `` com.cra.figaro.util.SelectableSet.aggregate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#aggregate-952)

```scala
def aggregate[B](z: => B)(seqop: (B, T) => B, combop: (B, B) => B): B
```

Aggregates the results of applying an operator to subsequent elements.

Type parameters: `` B ``.

Parameters, list 1: `` z: => B ``.

Parameters, list 2: `` seqop: (B, T) => B ``; `` combop: (B, B) => B ``.

Returns: `` B ``.

Source contract/attributes: Aggregates the results of applying an operator to subsequent elements. Since this method degenerates to foldLeft for sequential (non-parallel) collections, where the combining operation is ignored, it is advisable to prefer foldLeft for that case. For parallel collections, use the aggregate method specified by scala.collection.parallel.ParIterableLike. Type parameters B the result type, produced by seqop, combop, and by this function as a final result. Value parameters combop an associative operator for combining sequential results, unused for sequential collections. seqop the binary operator used to accumulate the result. z the start value, a neutral element for seqop. Attributes Deprecated [Since version 2.13.0] For sequential collections, prefer `foldLeft(z)(seqop)`. For parallel collections, use `ParIterableLike#aggregate`. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.aggregate[B](z)(seqop, combop)
```

## `` com.cra.figaro.util.SelectableSet.companion ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#companion-0)

```scala
def companion: IterableFactory[Set]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` IterableFactory[Set] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use iterableFactory instead Inherited from: IterableOps

Invocation template:

```scala
receiver.companion
```

## `` com.cra.figaro.util.SelectableSet.copyToBuffer ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#copyToBuffer-f0b)

```scala
final def copyToBuffer[B >: T](dest: Buffer[B]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` B >: T ``.

Parameters, list 1: `` dest: Buffer[B] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use `dest ++= coll` instead Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.copyToBuffer[B](dest)
```

## `` com.cra.figaro.util.SelectableSet.hasDefiniteSize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#hasDefiniteSize-0)

```scala
def hasDefiniteSize: Boolean
```

Tests whether this mutable set is known to have a finite size. All strict collections are known to have finite size. For a non-strict collection such as Stream, the predicate returns **true** if all elements have been computed. It returns **false** if the stream is not yet evaluated to the end. Non-empty Iterators usually return **false** even if they were created from a collection with a known finite size.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Tests whether this mutable set is known to have a finite size. All strict collections are known to have finite size. For a non-strict collection such as Stream, the predicate returns **true** if all elements have been computed. It returns **false** if the stream is not yet evaluated to the end. Non-empty Iterators usually return **false** even if they were created from a collection with a known finite size. Note: many collection methods will not work on collections of infinite sizes. The typical failure mode is an infinite loop. These methods always attempt a traversal without checking first that hasDefiniteSize returns **true**. However, checking hasDefiniteSize can provide an assurance that size is well-defined and non-termination is not a concern. Attributes Returns **true** if this collection is known to have finite size, **false** otherwise. See also method knownSize for a more useful alternative Deprecated [Since version 2.13.0] Check .knownSize instead of .hasDefiniteSize for more actionable information (see scaladoc for details) This method is deprecated in 2.13 because it does not provide any actionable information. As noted above, even the collection library itself does not use it. When there is no guarantee that a collection is finite, it is generally best to attempt a computation anyway and document that it will not terminate for infinite collections rather than backing out because this would prevent performing the computation on collections that are in fact finite even though hasDefiniteSize returns false. Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.hasDefiniteSize
```

## `` com.cra.figaro.util.SelectableSet.repr ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#repr-0)

```scala
final def repr: Set[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use coll instead of repr in a collection implementation, use the collection value itself from the outside Inherited from: IterableOps

Invocation template:

```scala
receiver.repr
```

## `` com.cra.figaro.util.SelectableSet.retain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#retain-c9d)

```scala
final def retain(p: T => Boolean): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` p: T => Boolean ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use filterInPlace instead Inherited from: SetOps

Invocation template:

```scala
receiver.retain(p)
```

## `` com.cra.figaro.util.SelectableSet.seq ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#seq-0)

```scala
def seq: this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` this.type ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Iterable.seq always returns the iterable itself Inherited from: Iterable

Invocation template:

```scala
receiver.seq
```

## `` com.cra.figaro.util.SelectableSet.toIterable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#toIterable-0)

```scala
final def toIterable: this.type
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` this.type ``.

Source contract/attributes: Attributes Returns This collection as an Iterable[A]. No new collection will be built if this is already an Iterable[A]. Deprecated [Since version 2.13.7] toIterable is internal and will be made protected; its name is similar to `toList` or `toSeq`, but it doesn\'t copy non-immutable collections Inherited from: Iterable

Invocation template:

```scala
receiver.toIterable
```

## `` com.cra.figaro.util.SelectableSet.toIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#toIterator-0)

```scala
final def toIterator: Iterator[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use .iterator instead of .toIterator Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toIterator
```

## `` com.cra.figaro.util.SelectableSet.toStream ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#toStream-0)

```scala
final def toStream: Stream[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Stream[T] ``.

Source contract/attributes: Attributes Deprecated [Since version 2.13.0] Use .to(LazyList) instead of .toStream Inherited from: IterableOnceOps

Invocation template:

```scala
receiver.toStream
```

## `` com.cra.figaro.util.SelectableSet.toTraversable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#toTraversable-0)

```scala
final def toTraversable: Iterable[T]
```

Converts this mutable set to an unspecified Iterable. Will return the same collection if this instance is already Iterable.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterable[T] ``.

Source contract/attributes: Converts this mutable set to an unspecified Iterable. Will return the same collection if this instance is already Iterable. Attributes Returns An Iterable containing all elements of this mutable set. Deprecated [Since version 2.13.0] toTraversable is internal and will be made protected; its name is similar to `toList` or `toSeq`, but it doesn\'t copy non-immutable collections Inherited from: IterableOps

Invocation template:

```scala
receiver.toTraversable
```

## `` com.cra.figaro.util.SelectableSet.view ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/SelectableSet.html#view-fffff78d)

```scala
def view(from: Int, until: Int): View[T]
```

A view over a slice of the elements of this collection.

Type parameters: none.

Parameters, list 1: `` from: Int ``; `` until: Int ``.

Returns: `` View[T] ``.

Source contract/attributes: A view over a slice of the elements of this collection. Attributes Deprecated [Since version 2.13.0] Use .view.slice(from, until) instead of .view(from, until) Inherited from: IterableOps

Invocation template:

```scala
receiver.view(from, until)
```

## `` com.cra.figaro.util.TopSort.topologicallySort ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/TopSort$.html#topologicallySort-fffff0c8)

```scala
def topologicallySort(updatesNeeded: Iterable[Element[_]], universe: Universe): List[Element[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` updatesNeeded: Iterable[Element[_]] ``; `` universe: Universe ``.

Returns: `` List[Element[_]] ``.

Invocation template:

```scala
com.cra.figaro.util.TopSort.topologicallySort(updatesNeeded, universe)
```

## `` com.cra.figaro.util.`package`.ZeroTotalUnnormalizedProbabilityException.addSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ZeroTotalUnnormalizedProbabilityException.html#addSuppressed-4b8)

```scala
final def addSuppressed(x$0: Throwable): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.addSuppressed(x$0)
```

## `` com.cra.figaro.util.`package`.ZeroTotalUnnormalizedProbabilityException.fillInStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ZeroTotalUnnormalizedProbabilityException.html#fillInStackTrace-26c)

```scala
def fillInStackTrace(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.fillInStackTrace()
```

## `` com.cra.figaro.util.`package`.ZeroTotalUnnormalizedProbabilityException.getCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ZeroTotalUnnormalizedProbabilityException.html#getCause-26c)

```scala
def getCause(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getCause()
```

## `` com.cra.figaro.util.`package`.ZeroTotalUnnormalizedProbabilityException.getLocalizedMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ZeroTotalUnnormalizedProbabilityException.html#getLocalizedMessage-665)

```scala
def getLocalizedMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getLocalizedMessage()
```

## `` com.cra.figaro.util.`package`.ZeroTotalUnnormalizedProbabilityException.getMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ZeroTotalUnnormalizedProbabilityException.html#getMessage-665)

```scala
def getMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getMessage()
```

## `` com.cra.figaro.util.`package`.ZeroTotalUnnormalizedProbabilityException.getStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ZeroTotalUnnormalizedProbabilityException.html#getStackTrace-fffffa6d)

```scala
def getStackTrace(): Array[StackTraceElement]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[StackTraceElement] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getStackTrace()
```

## `` com.cra.figaro.util.`package`.ZeroTotalUnnormalizedProbabilityException.getSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ZeroTotalUnnormalizedProbabilityException.html#getSuppressed-fffff2ce)

```scala
final def getSuppressed(): Array[Throwable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[Throwable] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getSuppressed()
```

## `` com.cra.figaro.util.`package`.ZeroTotalUnnormalizedProbabilityException.initCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ZeroTotalUnnormalizedProbabilityException.html#initCause-fffff080)

```scala
def initCause(x$0: Throwable): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.initCause(x$0)
```

## `` com.cra.figaro.util.`package`.ZeroTotalUnnormalizedProbabilityException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ZeroTotalUnnormalizedProbabilityException.html#printStackTrace-fffff230)

```scala
def printStackTrace(x$0: PrintWriter): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintWriter ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.util.`package`.ZeroTotalUnnormalizedProbabilityException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ZeroTotalUnnormalizedProbabilityException.html#printStackTrace-fffff0fd)

```scala
def printStackTrace(x$0: PrintStream): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintStream ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.util.`package`.ZeroTotalUnnormalizedProbabilityException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ZeroTotalUnnormalizedProbabilityException.html#printStackTrace-94c)

```scala
def printStackTrace(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace()
```

## `` com.cra.figaro.util.`package`.ZeroTotalUnnormalizedProbabilityException.setStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ZeroTotalUnnormalizedProbabilityException.html#setStackTrace-f79)

```scala
def setStackTrace(x$0: Array[StackTraceElement]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Array[StackTraceElement] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.setStackTrace(x$0)
```

## `` com.cra.figaro.util.`package`.ZeroTotalUnnormalizedProbabilityException.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util/ZeroTotalUnnormalizedProbabilityException.html#toString-665)

```scala
def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Inherited from: Throwable

Invocation template:

```scala
receiver.toString()
```
