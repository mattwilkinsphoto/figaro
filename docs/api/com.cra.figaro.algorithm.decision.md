<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.algorithm.decision

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Clean up the sampler, freeing memory.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clean up the sampler, freeing memory. Attributes Definition Classes MetropolisHastings -> Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the sampler.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the sampler. Attributes Definition Classes AnytimeSampler -> Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.acceptRejectRatio ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#acceptRejectRatio-0)

```scala
def acceptRejectRatio: Double
```

Get the acceptance ratio for the sampler.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Get the acceptance ratio for the sampler. Attributes Inherited from: MetropolisHastings

Invocation template:

```scala
receiver.acceptRejectRatio
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.cleanup ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#cleanup-94c)

```scala
def cleanup(): Unit
```

Cleans up the temporary elements created during sampling

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Cleans up the temporary elements created during sampling Attributes Inherited from: DecisionMetropolisHastings

Invocation template:

```scala
receiver.cleanup()
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#computeDistribution-fffffef0)

```scala
def computeDistribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQuerySampler

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#computeExpectation-fffffa66)

```scala
def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQuerySampler

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.computeUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#computeUtility-fffff328)

```scala
def computeUtility(): Map[(T, U), DecisionSample]
```

Compute the utility of each parent/decision tuple and return a DecisionSample. Each decision algorithm must define how this is done since it is used to set the policy for a decision. For sampling algorithms, this will me a map of parent/decision tuples to a utility and a weight for that combination. For factored algorithms, the DecisionSample will contain the exact expected utility with a weight of 1.0.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[(T, U), DecisionSample] ``.

Source contract/attributes: Compute the utility of each parent/decision tuple and return a DecisionSample. Each decision algorithm must define how this is done since it is used to set the policy for a decision. For sampling algorithms, this will me a map of parent/decision tuples to a utility and a weight for that combination. For factored algorithms, the DecisionSample will contain the exact expected utility with a weight of 1.0. Attributes Inherited from: DecisionMetropolisHastings

Invocation template:

```scala
receiver.computeUtility()
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#distribution-fffffef0)

```scala
def distribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.getSampleCount ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#getSampleCount-0)

```scala
def getSampleCount: Int
```

Number of samples taken

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples taken Attributes Inherited from: BaseUnweightedSampler

Invocation template:

```scala
receiver.getSampleCount
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.getTotalWeight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#getTotalWeight-0)

```scala
def getTotalWeight: Double
```

Total weight of samples taken, in log space

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Total weight of samples taken, in log space Attributes Inherited from: UnweightedSampler

Invocation template:

```scala
receiver.getTotalWeight
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.getUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#getUtility-fffffba0)

```scala
def getUtility(p: T, d: U): DecisionSample
```

Get the total utility and weight for a specific value of a parent and decision.

Type parameters: none.

Parameters, list 1: `` p: T ``; `` d: U ``.

Returns: `` DecisionSample ``.

Source contract/attributes: Get the total utility and weight for a specific value of a parent and decision. Attributes Inherited from: DecisionAlgorithm

Invocation template:

```scala
receiver.getUtility(p, d)
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.getUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#getUtility-fffff328)

```scala
def getUtility(): Map[(T, U), DecisionSample]
```

Get the total utility and weight for all sampled values of the parent and decision.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[(T, U), DecisionSample] ``.

Source contract/attributes: Get the total utility and weight for all sampled values of the parent and decision. Attributes Inherited from: DecisionAlgorithm

Invocation template:

```scala
receiver.getUtility()
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.handle ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#handle-fffff940)

```scala
def handle(service: Service): Response
```

A handler of services provided by the algorithm.

Type parameters: none.

Parameters, list 1: `` service: Service ``.

Returns: `` Response ``.

Source contract/attributes: A handler of services provided by the algorithm. Attributes Inherited from: AnytimeProbQuery

Invocation template:

```scala
receiver.handle(service)
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single step of the algorithm. The algorithm must be able to provide answers after each step.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single step of the algorithm. The algorithm must be able to provide answers after each step. Attributes Inherited from: AnytimeSampler

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#sample-ffffff32)

```scala
override def sample(): (Boolean, Sample)
```

Produce a single sample. In decision MH, we always update the target (parent and decision) since the utilities mights have changed

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (Boolean, Sample) ``.

Source contract/attributes: Produce a single sample. In decision MH, we always update the target (parent and decision) since the utilities mights have changed Attributes Definition Classes DecisionMetropolisHastings -> MetropolisHastings -> BaseUnweightedSampler Inherited from: DecisionMetropolisHastings

Invocation template:

```scala
receiver.sample()
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.sampleFromPosterior ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#sampleFromPosterior-fffffdb4)

```scala
def sampleFromPosterior[T](element: Element[T]): Stream[T]
```

Sample an value from the posterior of this element

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` Stream[T] ``.

Source contract/attributes: Sample an value from the posterior of this element Attributes Inherited from: UnweightedSampler

Invocation template:

```scala
receiver.sampleFromPosterior[T](element)
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.setPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#setPolicy-fffff07c)

```scala
def setPolicy(e: Decision[T, U]): Unit
```

Sets the policy for the given decision. This will get the computed utility of the algorithm and call setPolicy on the decision. Note there is no error checking here, so the decision in the argument must match the target decision in the algorithm.

Type parameters: none.

Parameters, list 1: `` e: Decision[T, U] ``.

Returns: `` Unit ``.

Source contract/attributes: Sets the policy for the given decision. This will get the computed utility of the algorithm and call setPolicy on the decision. Note there is no error checking here, so the decision in the argument must match the target decision in the algorithm. Attributes Inherited from: DecisionAlgorithm

Invocation template:

```scala
receiver.setPolicy(e)
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.shutdown ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#shutdown-0)

```scala
def shutdown: Unit
```

Release all resources from this anytime algorithm.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Unit ``.

Source contract/attributes: Release all resources from this anytime algorithm. Attributes Inherited from: Anytime

Invocation template:

```scala
receiver.shutdown
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.stopUpdate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#stopUpdate-94c)

```scala
override def stopUpdate(): Unit
```

Override the stopUpdate function in anytime to call the sampler update function

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Override the stopUpdate function in anytime to call the sampler update function Attributes Definition Classes AnytimeSampler -> Anytime Inherited from: AnytimeSampler

Invocation template:

```scala
receiver.stopUpdate()
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.test ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#test-fffff570)

```scala
def test(numSamples: Int, predicates: Seq[Predicate[_]], elementsToTrack: Seq[Element[_]]): (Double, Map[Predicate[_], Double], Map[Element[_], Double])
```

Test Metropolis-Hastings by repeatedly running a single step from the same initial state. For each of a set of predicates, the fraction of times the predicate is satisfied by the resulting state is returned. By the resulting state, we mean the new state if it is accepted and the original state if not.

Type parameters: none.

Parameters, list 1: `` numSamples: Int ``; `` predicates: Seq[Predicate[_]] ``; `` elementsToTrack: Seq[Element[_]] ``.

Returns: `` (Double, Map[Predicate[_], Double], Map[Element[_], Double]) ``.

Source contract/attributes: Test Metropolis-Hastings by repeatedly running a single step from the same initial state. For each of a set of predicates, the fraction of times the predicate is satisfied by the resulting state is returned. By the resulting state, we mean the new state if it is accepted and the original state if not. Attributes Inherited from: MetropolisHastings

Invocation template:

```scala
receiver.test(numSamples, predicates, elementsToTrack)
```

## `` com.cra.figaro.algorithm.decision.AnytimeDecisionMetropolisHastings.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/AnytimeDecisionMetropolisHastings.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.decision.DecisionAlgorithm.computeUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionAlgorithm.html#computeUtility-fffff328)

```scala
def computeUtility(): Map[(T, U), DecisionSample]
```

Compute the utility of each parent/decision tuple and return a DecisionSample. Each decision algorithm must define how this is done since it is used to set the policy for a decision. For sampling algorithms, this will me a map of parent/decision tuples to a utility and a weight for that combination. For factored algorithms, the DecisionSample will contain the exact expected utility with a weight of 1.0.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[(T, U), DecisionSample] ``.

Source contract/attributes: Compute the utility of each parent/decision tuple and return a DecisionSample. Each decision algorithm must define how this is done since it is used to set the policy for a decision. For sampling algorithms, this will me a map of parent/decision tuples to a utility and a weight for that combination. For factored algorithms, the DecisionSample will contain the exact expected utility with a weight of 1.0. Attributes

Invocation template:

```scala
receiver.computeUtility()
```

## `` com.cra.figaro.algorithm.decision.DecisionAlgorithm.getUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionAlgorithm.html#getUtility-fffff328)

```scala
def getUtility(): Map[(T, U), DecisionSample]
```

Get the total utility and weight for all sampled values of the parent and decision.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[(T, U), DecisionSample] ``.

Source contract/attributes: Get the total utility and weight for all sampled values of the parent and decision. Attributes

Invocation template:

```scala
receiver.getUtility()
```

## `` com.cra.figaro.algorithm.decision.DecisionAlgorithm.getUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionAlgorithm.html#getUtility-fffffba0)

```scala
def getUtility(p: T, d: U): DecisionSample
```

Get the total utility and weight for a specific value of a parent and decision.

Type parameters: none.

Parameters, list 1: `` p: T ``; `` d: U ``.

Returns: `` DecisionSample ``.

Source contract/attributes: Get the total utility and weight for a specific value of a parent and decision. Attributes

Invocation template:

```scala
receiver.getUtility(p, d)
```

## `` com.cra.figaro.algorithm.decision.DecisionAlgorithm.setPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionAlgorithm.html#setPolicy-fffff07c)

```scala
def setPolicy(e: Decision[T, U]): Unit
```

Sets the policy for the given decision. This will get the computed utility of the algorithm and call setPolicy on the decision. Note there is no error checking here, so the decision in the argument must match the target decision in the algorithm.

Type parameters: none.

Parameters, list 1: `` e: Decision[T, U] ``.

Returns: `` Unit ``.

Source contract/attributes: Sets the policy for the given decision. This will get the computed utility of the algorithm and call setPolicy on the decision. Note there is no error checking here, so the decision in the argument must match the target decision in the algorithm. Attributes

Invocation template:

```scala
receiver.setPolicy(e)
```

## `` com.cra.figaro.algorithm.decision.DecisionAlgorithm.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionAlgorithm.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.decision.DecisionAlgorithm.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionAlgorithm.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.decision.DecisionAlgorithm.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionAlgorithm.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.decision.DecisionAlgorithm.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionAlgorithm.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.decision.DecisionAlgorithm.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionAlgorithm.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.decision.DecisionAlgorithm.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionAlgorithm.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.decision.DecisionAlgorithm.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionAlgorithm.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance$.html#apply-c58)

```scala
def apply[T, U](utilityNodes: List[Element[_]], target: Decision[T, U])(implicit universe: Universe): DecisionImportance[T, U] & AnytimeProbQuerySampler
```

Create an Anytime DecisionImportance sampler with the given decision over the given universe.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` utilityNodes: List[Element[_]] ``; `` target: Decision[T, U] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` DecisionImportance[T, U] & AnytimeProbQuerySampler ``.

Source contract/attributes: Create an Anytime DecisionImportance sampler with the given decision over the given universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionImportance.apply[T, U](utilityNodes, target)(using universe)
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance$.html#apply-fffffb75)

```scala
def apply[T, U](myNumSamples: Int, utilityNodes: List[Element[_]], target: Decision[T, U])(implicit universe: Universe): DecisionImportance[T, U] & OneTimeProbQuerySampler
```

Create an OneTime DecisionImportance sampler with the given decision over the given universe using the given number of samples.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` myNumSamples: Int ``; `` utilityNodes: List[Element[_]] ``; `` target: Decision[T, U] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` DecisionImportance[T, U] & OneTimeProbQuerySampler ``.

Source contract/attributes: Create an OneTime DecisionImportance sampler with the given decision over the given universe using the given number of samples. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionImportance.apply[T, U](myNumSamples, utilityNodes, target)(using universe)
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.this ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#<init>-fffff838)

```scala
def this(universe: Universe, utilityNodes: List[Element[_]], decisionTarget: Decision[T, U])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` universe: Universe ``; `` utilityNodes: List[Element[_]] ``; `` decisionTarget: Decision[T, U] ``.

Returns: `` com.cra.figaro.algorithm.decision.DecisionImportance ``.

Invocation template:

```scala
new com.cra.figaro.algorithm.decision.DecisionImportance(universe, utilityNodes, decisionTarget)
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.cleanup ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#cleanup-94c)

```scala
def cleanup(): Unit
```

Cleans up the temporary elements created during sampling.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Cleans up the temporary elements created during sampling. Attributes

Invocation template:

```scala
receiver.cleanup()
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.computeUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#computeUtility-fffff328)

```scala
def computeUtility(): Map[(T, U), DecisionSample]
```

Compute the utility of each parent/decision tuple and return a DecisionSample. Each decision algorithm must define how this is done since it is used to set the policy for a decision. For sampling algorithms, this will me a map of parent/decision tuples to a utility and a weight for that combination. For factored algorithms, the DecisionSample will contain the exact expected utility with a weight of 1.0.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[(T, U), DecisionSample] ``.

Source contract/attributes: Compute the utility of each parent/decision tuple and return a DecisionSample. Each decision algorithm must define how this is done since it is used to set the policy for a decision. For sampling algorithms, this will me a map of parent/decision tuples to a utility and a weight for that combination. For factored algorithms, the DecisionSample will contain the exact expected utility with a weight of 1.0. Attributes

Invocation template:

```scala
receiver.computeUtility()
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#computeDistribution-fffffef0)

```scala
def computeDistribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQuerySampler

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#computeExpectation-fffffa66)

```scala
def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQuerySampler

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#distribution-fffffef0)

```scala
def distribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.getSamples ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#getSamples-ffffff27)

```scala
def getSamples(): Int
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Int ``.

Source contract/attributes: Attributes Inherited from: Importance

Invocation template:

```scala
receiver.getSamples()
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.getTotalWeight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#getTotalWeight-0)

```scala
def getTotalWeight: Double
```

Total weight of samples taken.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Total weight of samples taken. Attributes Inherited from: WeightedSampler

Invocation template:

```scala
receiver.getTotalWeight
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.getUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#getUtility-fffffba0)

```scala
def getUtility(p: T, d: U): DecisionSample
```

Get the total utility and weight for a specific value of a parent and decision.

Type parameters: none.

Parameters, list 1: `` p: T ``; `` d: U ``.

Returns: `` DecisionSample ``.

Source contract/attributes: Get the total utility and weight for a specific value of a parent and decision. Attributes Inherited from: DecisionAlgorithm

Invocation template:

```scala
receiver.getUtility(p, d)
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.getUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#getUtility-fffff328)

```scala
def getUtility(): Map[(T, U), DecisionSample]
```

Get the total utility and weight for all sampled values of the parent and decision.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[(T, U), DecisionSample] ``.

Source contract/attributes: Get the total utility and weight for all sampled values of the parent and decision. Attributes Inherited from: DecisionAlgorithm

Invocation template:

```scala
receiver.getUtility()
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#kill-94c)

```scala
override def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Definition Classes Importance -> Algorithm Inherited from: Importance

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.logProbEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#logProbEvidence-0)

```scala
def logProbEvidence: Double
```

The computed probability of evidence.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: The computed probability of evidence. Attributes Inherited from: Importance

Invocation template:

```scala
receiver.logProbEvidence
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#sample-ffffff32)

```scala
final def sample(): Sample
```

Produce a single sample.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Sample ``.

Source contract/attributes: Produce a single sample. Attributes Inherited from: Importance

Invocation template:

```scala
receiver.sample()
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.sampleFromPosterior ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#sampleFromPosterior-fffffdb4)

```scala
def sampleFromPosterior[T](element: Element[T]): Stream[T]
```

Sample an value from the posterior of this element

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` Stream[T] ``.

Source contract/attributes: Sample an value from the posterior of this element Attributes Inherited from: WeightedSampler

Invocation template:

```scala
receiver.sampleFromPosterior[T](element)
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.setPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#setPolicy-fffff07c)

```scala
def setPolicy(e: Decision[T, U]): Unit
```

Sets the policy for the given decision. This will get the computed utility of the algorithm and call setPolicy on the decision. Note there is no error checking here, so the decision in the argument must match the target decision in the algorithm.

Type parameters: none.

Parameters, list 1: `` e: Decision[T, U] ``.

Returns: `` Unit ``.

Source contract/attributes: Sets the policy for the given decision. This will get the computed utility of the algorithm and call setPolicy on the decision. Note there is no error checking here, so the decision in the argument must match the target decision in the algorithm. Attributes Inherited from: DecisionAlgorithm

Invocation template:

```scala
receiver.setPolicy(e)
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.decision.DecisionImportance.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionImportance.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings$.html#apply-80a)

```scala
def apply[T, U](scheme: ProposalScheme, utilityNodes: List[Element[_]], target: Decision[T, U])(implicit universe: Universe): AnytimeDecisionMetropolisHastings[T, U]
```

Create an Anytime DecisionMetropolis-Hastings sampler using the given proposal scheme with the given decision.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` scheme: ProposalScheme ``; `` utilityNodes: List[Element[_]] ``; `` target: Decision[T, U] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` AnytimeDecisionMetropolisHastings[T, U] ``.

Source contract/attributes: Create an Anytime DecisionMetropolis-Hastings sampler using the given proposal scheme with the given decision. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.apply[T, U](scheme, utilityNodes, target)(using universe)
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings$.html#apply-fffff867)

```scala
def apply[T, U](numSamples: Int, scheme: ProposalScheme, utilityNodes: List[Element[_]], target: Decision[T, U])(implicit universe: Universe): OneTimeDecisionMetropolisHastings[T, U]
```

Create a OneTime DecisionMetropolis-Hastings sampler using the given number of samples and proposal scheme with the given decision.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` numSamples: Int ``; `` scheme: ProposalScheme ``; `` utilityNodes: List[Element[_]] ``; `` target: Decision[T, U] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` OneTimeDecisionMetropolisHastings[T, U] ``.

Source contract/attributes: Create a OneTime DecisionMetropolis-Hastings sampler using the given number of samples and proposal scheme with the given decision. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.apply[T, U](numSamples, scheme, utilityNodes, target)(using universe)
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings$.html#apply-fffffea5)

```scala
def apply[T, U](scheme: ProposalScheme, burnIn: Int, utilityNodes: List[Element[_]], target: Decision[T, U])(implicit universe: Universe): AnytimeDecisionMetropolisHastings[T, U]
```

Create an Anytime DecisionMetropolis-Hastings sampler using the given proposal scheme and number of burn-in samples with the given decision.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` scheme: ProposalScheme ``; `` burnIn: Int ``; `` utilityNodes: List[Element[_]] ``; `` target: Decision[T, U] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` AnytimeDecisionMetropolisHastings[T, U] ``.

Source contract/attributes: Create an Anytime DecisionMetropolis-Hastings sampler using the given proposal scheme and number of burn-in samples with the given decision. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.apply[T, U](scheme, burnIn, utilityNodes, target)(using universe)
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings$.html#apply-7dc)

```scala
def apply[T, U](numSamples: Int, scheme: ProposalScheme, burnIn: Int, utilityNodes: List[Element[_]], target: Decision[T, U])(implicit universe: Universe): OneTimeDecisionMetropolisHastings[T, U]
```

Create a OneTime DecisionMetropolis-Hastings sampler using the given number of samples, proposal scheme, and number of burn-in samples with the given decision.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` numSamples: Int ``; `` scheme: ProposalScheme ``; `` burnIn: Int ``; `` utilityNodes: List[Element[_]] ``; `` target: Decision[T, U] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` OneTimeDecisionMetropolisHastings[T, U] ``.

Source contract/attributes: Create a OneTime DecisionMetropolis-Hastings sampler using the given number of samples, proposal scheme, and number of burn-in samples with the given decision. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.apply[T, U](numSamples, scheme, burnIn, utilityNodes, target)(using universe)
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings$.html#apply-66a)

```scala
def apply[T, U](scheme: ProposalScheme, burnIn: Int, interval: Int, utilityNodes: List[Element[_]], target: Decision[T, U])(implicit universe: Universe): AnytimeDecisionMetropolisHastings[T, U]
```

Create an Anytime DecisionMetropolis-Hastings sampler using the given proposal scheme, number of burn-in samples, and interval between samples with the given decision.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` scheme: ProposalScheme ``; `` burnIn: Int ``; `` interval: Int ``; `` utilityNodes: List[Element[_]] ``; `` target: Decision[T, U] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` AnytimeDecisionMetropolisHastings[T, U] ``.

Source contract/attributes: Create an Anytime DecisionMetropolis-Hastings sampler using the given proposal scheme, number of burn-in samples, and interval between samples with the given decision. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.apply[T, U](scheme, burnIn, interval, utilityNodes, target)(using universe)
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings$.html#apply-fffffc07)

```scala
def apply[T, U](numSamples: Int, scheme: ProposalScheme, burnIn: Int, interval: Int, utilityNodes: List[Element[_]], target: Decision[T, U])(implicit universe: Universe): OneTimeDecisionMetropolisHastings[T, U]
```

Create a OneTime DecisionMetropolis-Hastings sampler using the given number of samples, proposal scheme, number of burn-in samples, and interval between samples with the given decision.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` numSamples: Int ``; `` scheme: ProposalScheme ``; `` burnIn: Int ``; `` interval: Int ``; `` utilityNodes: List[Element[_]] ``; `` target: Decision[T, U] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` OneTimeDecisionMetropolisHastings[T, U] ``.

Source contract/attributes: Create a OneTime DecisionMetropolis-Hastings sampler using the given number of samples, proposal scheme, number of burn-in samples, and interval between samples with the given decision. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.apply[T, U](numSamples, scheme, burnIn, interval, utilityNodes, target)(using universe)
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.this ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings.html#<init>-a19)

```scala
def this(universe: Universe, proposalScheme: ProposalScheme, burnIn: Int, interval: Int, utilityNodes: List[Element[_]], decisionTarget: Decision[T, U])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` universe: Universe ``; `` proposalScheme: ProposalScheme ``; `` burnIn: Int ``; `` interval: Int ``; `` utilityNodes: List[Element[_]] ``; `` decisionTarget: Decision[T, U] ``.

Returns: `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings ``.

Invocation template:

```scala
new com.cra.figaro.algorithm.decision.DecisionMetropolisHastings(universe, proposalScheme, burnIn, interval, utilityNodes, decisionTarget)
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.cleanup ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings.html#cleanup-94c)

```scala
def cleanup(): Unit
```

Cleans up the temporary elements created during sampling

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Cleans up the temporary elements created during sampling Attributes

Invocation template:

```scala
receiver.cleanup()
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.computeUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings.html#computeUtility-fffff328)

```scala
def computeUtility(): Map[(T, U), DecisionSample]
```

Compute the utility of each parent/decision tuple and return a DecisionSample. Each decision algorithm must define how this is done since it is used to set the policy for a decision. For sampling algorithms, this will me a map of parent/decision tuples to a utility and a weight for that combination. For factored algorithms, the DecisionSample will contain the exact expected utility with a weight of 1.0.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[(T, U), DecisionSample] ``.

Source contract/attributes: Compute the utility of each parent/decision tuple and return a DecisionSample. Each decision algorithm must define how this is done since it is used to set the policy for a decision. For sampling algorithms, this will me a map of parent/decision tuples to a utility and a weight for that combination. For factored algorithms, the DecisionSample will contain the exact expected utility with a weight of 1.0. Attributes

Invocation template:

```scala
receiver.computeUtility()
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings.html#sample-ffffff32)

```scala
override def sample(): (Boolean, Sample)
```

Produce a single sample. In decision MH, we always update the target (parent and decision) since the utilities mights have changed

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (Boolean, Sample) ``.

Source contract/attributes: Produce a single sample. In decision MH, we always update the target (parent and decision) since the utilities mights have changed Attributes Definition Classes MetropolisHastings -> BaseUnweightedSampler

Invocation template:

```scala
receiver.sample()
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.acceptRejectRatio ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings.html#acceptRejectRatio-0)

```scala
def acceptRejectRatio: Double
```

Get the acceptance ratio for the sampler.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Get the acceptance ratio for the sampler. Attributes Inherited from: MetropolisHastings

Invocation template:

```scala
receiver.acceptRejectRatio
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Clean up the sampler, freeing memory.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clean up the sampler, freeing memory. Attributes Definition Classes MetropolisHastings -> Algorithm Inherited from: MetropolisHastings

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.getSampleCount ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings.html#getSampleCount-0)

```scala
def getSampleCount: Int
```

Number of samples taken

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples taken Attributes Inherited from: BaseUnweightedSampler

Invocation template:

```scala
receiver.getSampleCount
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.getUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings.html#getUtility-fffffba0)

```scala
def getUtility(p: T, d: U): DecisionSample
```

Get the total utility and weight for a specific value of a parent and decision.

Type parameters: none.

Parameters, list 1: `` p: T ``; `` d: U ``.

Returns: `` DecisionSample ``.

Source contract/attributes: Get the total utility and weight for a specific value of a parent and decision. Attributes Inherited from: DecisionAlgorithm

Invocation template:

```scala
receiver.getUtility(p, d)
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.getUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings.html#getUtility-fffff328)

```scala
def getUtility(): Map[(T, U), DecisionSample]
```

Get the total utility and weight for all sampled values of the parent and decision.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[(T, U), DecisionSample] ``.

Source contract/attributes: Get the total utility and weight for all sampled values of the parent and decision. Attributes Inherited from: DecisionAlgorithm

Invocation template:

```scala
receiver.getUtility()
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.setPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings.html#setPolicy-fffff07c)

```scala
def setPolicy(e: Decision[T, U]): Unit
```

Sets the policy for the given decision. This will get the computed utility of the algorithm and call setPolicy on the decision. Note there is no error checking here, so the decision in the argument must match the target decision in the algorithm.

Type parameters: none.

Parameters, list 1: `` e: Decision[T, U] ``.

Returns: `` Unit ``.

Source contract/attributes: Sets the policy for the given decision. This will get the computed utility of the algorithm and call setPolicy on the decision. Note there is no error checking here, so the decision in the argument must match the target decision in the algorithm. Attributes Inherited from: DecisionAlgorithm

Invocation template:

```scala
receiver.setPolicy(e)
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.decision.DecisionMetropolisHastings.test ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionMetropolisHastings.html#test-fffff570)

```scala
def test(numSamples: Int, predicates: Seq[Predicate[_]], elementsToTrack: Seq[Element[_]]): (Double, Map[Predicate[_], Double], Map[Element[_], Double])
```

Test Metropolis-Hastings by repeatedly running a single step from the same initial state. For each of a set of predicates, the fraction of times the predicate is satisfied by the resulting state is returned. By the resulting state, we mean the new state if it is accepted and the original state if not.

Type parameters: none.

Parameters, list 1: `` numSamples: Int ``; `` predicates: Seq[Predicate[_]] ``; `` elementsToTrack: Seq[Element[_]] ``.

Returns: `` (Double, Map[Predicate[_], Double], Map[Element[_], Double]) ``.

Source contract/attributes: Test Metropolis-Hastings by repeatedly running a single step from the same initial state. For each of a set of predicates, the fraction of times the predicate is satisfied by the resulting state is returned. By the resulting state, we mean the new state if it is accepted and the original state if not. Attributes Inherited from: MetropolisHastings

Invocation template:

```scala
receiver.test(numSamples, predicates, elementsToTrack)
```

## `` com.cra.figaro.algorithm.decision.DecisionPolicy.UWMAX ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionPolicy$.html#UWMAX-bb3)

```scala
def UWMAX[U](nn: List[(Double, U, DecisionSample)]): (U, Double)
```

Computes the maximal decision from a set of (decision, utility) samples, returned from a nearest neighbor algorithm. It is unweighted because the samples are NOT weighted by their distance from the parent value.

Type parameters: `` U ``.

Parameters, list 1: `` nn: List[(Double, U, DecisionSample)] ``.

Returns: `` (U, Double) ``.

Source contract/attributes: Computes the maximal decision from a set of (decision, utility) samples, returned from a nearest neighbor algorithm. It is unweighted because the samples are NOT weighted by their distance from the parent value. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionPolicy.UWMAX[U](nn)
```

## `` com.cra.figaro.algorithm.decision.DecisionPolicy.WMAX ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionPolicy$.html#WMAX-bb3)

```scala
def WMAX[U](nn: List[(Double, U, DecisionSample)]): (U, Double)
```

Computes the maximal decision from a set of (decision, utility) samples, returned from the nearest neighbor algorithm. This method IS weighted. Samples who's parent value closer to the query parent value are weighted more when computing the expected value.

Type parameters: `` U ``.

Parameters, list 1: `` nn: List[(Double, U, DecisionSample)] ``.

Returns: `` (U, Double) ``.

Source contract/attributes: Computes the maximal decision from a set of (decision, utility) samples, returned from the nearest neighbor algorithm. This method IS weighted. Samples who's parent value closer to the query parent value are weighted more when computing the expected value. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionPolicy.WMAX[U](nn)
```

## `` com.cra.figaro.algorithm.decision.DecisionPolicy.toFcn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionPolicy.html#toFcn-491)

```scala
def toFcn(): T => Element[U]
```

The function that returns a decision (Element[U]) given the value of the parent T.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` T => Element[U] ``.

Source contract/attributes: The function that returns a decision (Element[U]) given the value of the parent T. Attributes

Invocation template:

```scala
receiver.toFcn()
```

## `` com.cra.figaro.algorithm.decision.DecisionPolicy.toUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionPolicy.html#toUtility-491)

```scala
def toUtility(): T => Element[Double]
```

The function that returns the expected utility (Element[Double]) given the value of the parent T.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` T => Element[Double] ``.

Source contract/attributes: The function that returns the expected utility (Element[Double]) given the value of the parent T. Attributes

Invocation template:

```scala
receiver.toUtility()
```

## `` com.cra.figaro.algorithm.decision.DecisionPolicyExact.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionPolicyExact$.html#apply-60f)

```scala
def apply[T, U](policy: Map[(T, U), DecisionSample]): DecisionPolicyExact[T, U]
```

Create an exact decision policy from a Map of (parent, decision) tuples to a DecisionSample.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` policy: Map[(T, U), DecisionSample] ``.

Returns: `` DecisionPolicyExact[T, U] ``.

Source contract/attributes: Create an exact decision policy from a Map of (parent, decision) tuples to a DecisionSample. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionPolicyExact.apply[T, U](policy)
```

## `` com.cra.figaro.algorithm.decision.DecisionPolicyExact.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionPolicyExact$.html#apply-c90)

```scala
def apply[T, U](Alg: DecisionAlgorithm[T, U]): DecisionPolicyExact[T, U]
```

Create an exact decision policy from a DecisionAlgorithm.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` Alg: DecisionAlgorithm[T, U] ``.

Returns: `` DecisionPolicyExact[T, U] ``.

Source contract/attributes: Create an exact decision policy from a DecisionAlgorithm. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionPolicyExact.apply[T, U](Alg)
```

## `` com.cra.figaro.algorithm.decision.DecisionPolicyExact.toFcn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionPolicyExact.html#toFcn-491)

```scala
override def toFcn(): T => Element[U]
```

The function that returns a decision (Element[U]) given the value of the parent T.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` T => Element[U] ``.

Source contract/attributes: The function that returns a decision (Element[U]) given the value of the parent T. Attributes Definition Classes DecisionPolicy

Invocation template:

```scala
receiver.toFcn()
```

## `` com.cra.figaro.algorithm.decision.DecisionPolicyExact.toUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionPolicyExact.html#toUtility-491)

```scala
override def toUtility(): T => Element[Double]
```

The function that returns the expected utility (Element[Double]) given the value of the parent T.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` T => Element[Double] ``.

Source contract/attributes: The function that returns the expected utility (Element[Double]) given the value of the parent T. Attributes Definition Classes DecisionPolicy

Invocation template:

```scala
receiver.toUtility()
```

## `` com.cra.figaro.algorithm.decision.DecisionPolicyNN.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionPolicyNN$.html#apply-d56)

```scala
def apply[T : DistanceConversion, U](Alg: DecisionAlgorithm[T, U], combineFcn: List[(Double, U, DecisionSample)] => (U, Double), kNN: Double): DecisionPolicyNN[T, U]
```

Create an approximate decision policy from a DecisionAlgorithm, using the supplied combination function and kNN. This uses the default index (VP-Tree).

Type parameters: `` T : DistanceConversion ``; `` U ``.

Parameters, list 1: `` Alg: DecisionAlgorithm[T, U] ``; `` combineFcn: List[(Double, U, DecisionSample)] => (U, Double) ``; `` kNN: Double ``.

Returns: `` DecisionPolicyNN[T, U] ``.

Source contract/attributes: Create an approximate decision policy from a DecisionAlgorithm, using the supplied combination function and kNN. This uses the default index (VP-Tree). Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionPolicyNN.apply[T, U](Alg, combineFcn, kNN)
```

## `` com.cra.figaro.algorithm.decision.DecisionPolicyNN.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionPolicyNN$.html#apply-2f7)

```scala
def apply[T : DistanceConversion, U](policy: Map[(T, U), DecisionSample], combineFcn: List[(Double, U, DecisionSample)] => (U, Double), kNN: Double): DecisionPolicyNN[T, U]
```

Create an approximate decision policy from a Map of (parent, decision) tuples to a DecisionSample, using the supplied combination function and kNN. This uses the default index (VP-Tree).

Type parameters: `` T : DistanceConversion ``; `` U ``.

Parameters, list 1: `` policy: Map[(T, U), DecisionSample] ``; `` combineFcn: List[(Double, U, DecisionSample)] => (U, Double) ``; `` kNN: Double ``.

Returns: `` DecisionPolicyNN[T, U] ``.

Source contract/attributes: Create an approximate decision policy from a Map of (parent, decision) tuples to a DecisionSample, using the supplied combination function and kNN. This uses the default index (VP-Tree). Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionPolicyNN.apply[T, U](policy, combineFcn, kNN)
```

## `` com.cra.figaro.algorithm.decision.DecisionPolicyNN.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionPolicyNN$.html#apply-419)

```scala
def apply[T : DistanceConversion, U](nnIndex: Index[T, U], combineFcn: List[(Double, U, DecisionSample)] => (U, Double), kNN: Double): DecisionPolicyNN[T, U]
```

Create an approximate decision policy from an index, using the supplied combination function and kNN.

Type parameters: `` T : DistanceConversion ``; `` U ``.

Parameters, list 1: `` nnIndex: Index[T, U] ``; `` combineFcn: List[(Double, U, DecisionSample)] => (U, Double) ``; `` kNN: Double ``.

Returns: `` DecisionPolicyNN[T, U] ``.

Source contract/attributes: Create an approximate decision policy from an index, using the supplied combination function and kNN. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionPolicyNN.apply[T, U](nnIndex, combineFcn, kNN)
```

## `` com.cra.figaro.algorithm.decision.DecisionPolicyNN.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionPolicyNN$.html#apply-fffffc75)

```scala
def apply[T : DistanceConversion, U](Alg: DecisionAlgorithm[T, U], kNN: Double): DecisionPolicy[T, U]
```

Create an approximate decision policy from a DecisionAlgorithm, using the supplied kNN. This uses the default combination function (unweighted maximum) and index (VP-Tree).

Type parameters: `` T : DistanceConversion ``; `` U ``.

Parameters, list 1: `` Alg: DecisionAlgorithm[T, U] ``; `` kNN: Double ``.

Returns: `` DecisionPolicy[T, U] ``.

Source contract/attributes: Create an approximate decision policy from a DecisionAlgorithm, using the supplied kNN. This uses the default combination function (unweighted maximum) and index (VP-Tree). Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionPolicyNN.apply[T, U](Alg, kNN)
```

## `` com.cra.figaro.algorithm.decision.DecisionPolicyNN.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionPolicyNN$.html#apply-cb4)

```scala
def apply[T : DistanceConversion, U](policy: Map[(T, U), DecisionSample], kNN: Double = ...): DecisionPolicy[T, U]
```

Create an approximate decision policy from a Map of (parent, decision) tuples to a DecisionSample, using the supplied kNN. This uses the default combination function (unweighted maximum) and index (VP-Tree).

Type parameters: `` T : DistanceConversion ``; `` U ``.

Parameters, list 1: `` policy: Map[(T, U), DecisionSample] ``; `` kNN: Double = ... ``.

Returns: `` DecisionPolicy[T, U] ``.

Source contract/attributes: Create an approximate decision policy from a Map of (parent, decision) tuples to a DecisionSample, using the supplied kNN. This uses the default combination function (unweighted maximum) and index (VP-Tree). Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionPolicyNN.apply[T, U](policy, kNN)
```

## `` com.cra.figaro.algorithm.decision.DecisionPolicyNN.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionPolicyNN$.html#apply-fffffb52)

```scala
def apply[T : DistanceConversion, U](nnIndex: Index[T, U], kNN: Double): DecisionPolicy[T, U]
```

Create an approximate decision policy from an index, using the supplied kNN. This uses the default combination function (unweighted maximum).

Type parameters: `` T : DistanceConversion ``; `` U ``.

Parameters, list 1: `` nnIndex: Index[T, U] ``; `` kNN: Double ``.

Returns: `` DecisionPolicy[T, U] ``.

Source contract/attributes: Create an approximate decision policy from an index, using the supplied kNN. This uses the default combination function (unweighted maximum). Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionPolicyNN.apply[T, U](nnIndex, kNN)
```

## `` com.cra.figaro.algorithm.decision.DecisionPolicyNN.getNumNNSamples ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionPolicyNN.html#getNumNNSamples-0)

```scala
def getNumNNSamples: Int
```

Returns the number of nearest neighbors to use. If kNN is greater than 1, then return kNN. If kNN is less than 1, then return kNN* Number of Samples.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Returns the number of nearest neighbors to use. If kNN is greater than 1, then return kNN. If kNN is less than 1, then return kNN* Number of Samples. Attributes

Invocation template:

```scala
receiver.getNumNNSamples
```

## `` com.cra.figaro.algorithm.decision.DecisionPolicyNN.setNumNNSamples ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionPolicyNN.html#setNumNNSamples-fffff7a5)

```scala
def setNumNNSamples(i: Double): Unit
```

Set the number of nearest neighbor samples to use in policies based on nearest neighbor.

Type parameters: none.

Parameters, list 1: `` i: Double ``.

Returns: `` Unit ``.

Source contract/attributes: Set the number of nearest neighbor samples to use in policies based on nearest neighbor. Attributes

Invocation template:

```scala
receiver.setNumNNSamples(i)
```

## `` com.cra.figaro.algorithm.decision.DecisionPolicyNN.toFcn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionPolicyNN.html#toFcn-491)

```scala
override def toFcn(): T => Element[U]
```

The function that returns a decision (Element[U]) given the value of the parent T.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` T => Element[U] ``.

Source contract/attributes: The function that returns a decision (Element[U]) given the value of the parent T. Attributes Definition Classes DecisionPolicy

Invocation template:

```scala
receiver.toFcn()
```

## `` com.cra.figaro.algorithm.decision.DecisionPolicyNN.toUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionPolicyNN.html#toUtility-491)

```scala
override def toUtility(): T => Element[Double]
```

The function that returns the expected utility (Element[Double]) given the value of the parent T.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` T => Element[Double] ``.

Source contract/attributes: The function that returns the expected utility (Element[Double]) given the value of the parent T. Attributes Definition Classes DecisionPolicy

Invocation template:

```scala
receiver.toUtility()
```

## `` com.cra.figaro.algorithm.decision.DecisionVariableElimination.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionVariableElimination$.html#apply-fffffdda)

```scala
def apply[T, U](utilityNodes: List[Element[_]], target: Decision[T, U])(implicit universe: Universe): ProbQueryVariableEliminationDecision[T, U]
```

Create a decision variable elimination instance with the given decision variables and indicated utility nodes.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` utilityNodes: List[Element[_]] ``; `` target: Decision[T, U] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryVariableEliminationDecision[T, U] ``.

Source contract/attributes: Create a decision variable elimination instance with the given decision variables and indicated utility nodes. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionVariableElimination.apply[T, U](utilityNodes, target)(using universe)
```

## `` com.cra.figaro.algorithm.decision.DecisionVariableElimination.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionVariableElimination$.html#apply-fffffaec)

```scala
def apply[T, U](dependentUniverses: List[(Universe, List[NamedEvidence[_]])], utilityNodes: List[Element[_]], target: Decision[T, U])(implicit universe: Universe): ProbQueryVariableEliminationDecision[T, U]
```

Create a decision variable elimination algorithm with the given decision variables and indicated utility nodes and using the given dependent universes in the current default universe.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` utilityNodes: List[Element[_]] ``; `` target: Decision[T, U] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryVariableEliminationDecision[T, U] ``.

Source contract/attributes: Create a decision variable elimination algorithm with the given decision variables and indicated utility nodes and using the given dependent universes in the current default universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionVariableElimination.apply[T, U](dependentUniverses, utilityNodes, target)(using universe)
```

## `` com.cra.figaro.algorithm.decision.DecisionVariableElimination.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/DecisionVariableElimination$.html#apply-b5a)

```scala
def apply[T, U](dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double, utilityNodes: List[Element[_]], target: Decision[T, U])(implicit universe: Universe): ProbQueryVariableEliminationDecision[T, U]
```

Create a decision variable elimination algorithm with the given decision variables and indicated utility nodes and using the given dependent universes in the current default universe. Use the given dependent algorithm function to determine the algorithm to use to compute probability of evidence in each dependent universe.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``; `` utilityNodes: List[Element[_]] ``; `` target: Decision[T, U] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryVariableEliminationDecision[T, U] ``.

Source contract/attributes: Create a decision variable elimination algorithm with the given decision variables and indicated utility nodes and using the given dependent universes in the current default universe. Use the given dependent algorithm function to determine the algorithm to use to compute probability of evidence in each dependent universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.DecisionVariableElimination.apply[T, U](dependentUniverses, dependentAlgorithm, utilityNodes, target)(using universe)
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionAlgorithm.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionAlgorithm.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Definition Classes Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionAlgorithm.getUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionAlgorithm.html#getUtility-182)

```scala
def getUtility[T, U](D: Decision[T, U], p: T, d: U): DecisionSample
```

Get the utility for a specific parent and decision in the multi-decision algorithm.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` D: Decision[T, U] ``; `` p: T ``; `` d: U ``.

Returns: `` DecisionSample ``.

Source contract/attributes: Get the utility for a specific parent and decision in the multi-decision algorithm. Attributes

Invocation template:

```scala
receiver.getUtility[T, U](D, p, d)
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionAlgorithm.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionAlgorithm.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionAlgorithm.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionAlgorithm.html#run-8ac)

```scala
def run(oneStep: Boolean): Unit
```

Run in a debug mode where only a single decision is run each time.

Type parameters: none.

Parameters, list 1: `` oneStep: Boolean ``.

Returns: `` Unit ``.

Source contract/attributes: Run in a debug mode where only a single decision is run each time. Attributes

Invocation template:

```scala
receiver.run(oneStep)
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionAlgorithm.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionAlgorithm.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionAlgorithm.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionAlgorithm.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionAlgorithm.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionAlgorithm.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionAlgorithm.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionAlgorithm.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionAlgorithm.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionAlgorithm.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionAlgorithm.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionAlgorithm.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionImportance.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionImportance$.html#apply-fffff9c4)

```scala
def apply(numSamples: Int, utilityNodes: List[Element[_]], targets: Decision[_, _]*)(implicit universe: Universe): OneTimeMultiDecisionImportance
```

Create a OneTime multi-decision Importance sampler using the given number of samples, the given target utilities, and target decisions.

Type parameters: none.

Parameters, list 1: `` numSamples: Int ``; `` utilityNodes: List[Element[_]] ``; `` targets: Decision[_, _]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` OneTimeMultiDecisionImportance ``.

Source contract/attributes: Create a OneTime multi-decision Importance sampler using the given number of samples, the given target utilities, and target decisions. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.MultiDecisionImportance.apply(numSamples, utilityNodes, targets*)(using universe)
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionMetropolisHastings.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionMetropolisHastings$.html#apply-435)

```scala
def apply(numSamples: Int, proposalMaker: ProposalMakerType, utilityNodes: List[Element[_]], targets: Decision[_, _]*)(implicit universe: Universe): OneTimeMultiDecisionMetropolisHastings
```

Create a OneTime multi-decision Metropolis-Hastings sampler using the given number of samples, proposal maker, given target utilities, and target decisions.

Type parameters: none.

Parameters, list 1: `` numSamples: Int ``; `` proposalMaker: ProposalMakerType ``; `` utilityNodes: List[Element[_]] ``; `` targets: Decision[_, _]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` OneTimeMultiDecisionMetropolisHastings ``.

Source contract/attributes: Create a OneTime multi-decision Metropolis-Hastings sampler using the given number of samples, proposal maker, given target utilities, and target decisions. Value parameters proposalMaker A function that specifies how to create a proposal for each decision in the target list Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.MultiDecisionMetropolisHastings.apply(numSamples, proposalMaker, utilityNodes, targets*)(using universe)
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionMetropolisHastings.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionMetropolisHastings$.html#apply-fffff064)

```scala
def apply(numSamples: Int, proposalMaker: ProposalMakerType, burnIn: Int, utilityNodes: List[Element[_]], targets: Decision[_, _]*)(implicit universe: Universe): OneTimeMultiDecisionMetropolisHastings
```

Create a OneTime multi-decision Metropolis-Hastings sampler using the given number of samples, proposal maker, burn in time, given target utilities, and target decisions.

Type parameters: none.

Parameters, list 1: `` numSamples: Int ``; `` proposalMaker: ProposalMakerType ``; `` burnIn: Int ``; `` utilityNodes: List[Element[_]] ``; `` targets: Decision[_, _]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` OneTimeMultiDecisionMetropolisHastings ``.

Source contract/attributes: Create a OneTime multi-decision Metropolis-Hastings sampler using the given number of samples, proposal maker, burn in time, given target utilities, and target decisions. Value parameters proposalMaker A function that specifies how to create a proposal for each decision in the target list Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.MultiDecisionMetropolisHastings.apply(numSamples, proposalMaker, burnIn, utilityNodes, targets*)(using universe)
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionMetropolisHastings.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionMetropolisHastings$.html#apply-915)

```scala
def apply(numSamples: Int, proposalMaker: ProposalMakerType, burnIn: Int, interval: Int, utilityNodes: List[Element[_]], targets: Decision[_, _]*)(implicit universe: Universe): OneTimeMultiDecisionMetropolisHastings
```

Create a OneTime multi-decision Metropolis-Hastings sampler using the given number of samples, proposal maker, burn in time, interval, given target utilities, and target decisions.

Type parameters: none.

Parameters, list 1: `` numSamples: Int ``; `` proposalMaker: ProposalMakerType ``; `` burnIn: Int ``; `` interval: Int ``; `` utilityNodes: List[Element[_]] ``; `` targets: Decision[_, _]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` OneTimeMultiDecisionMetropolisHastings ``.

Source contract/attributes: Create a OneTime multi-decision Metropolis-Hastings sampler using the given number of samples, proposal maker, burn in time, interval, given target utilities, and target decisions. Value parameters proposalMaker A function that specifies how to create a proposal for each decision in the target list Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.MultiDecisionMetropolisHastings.apply(numSamples, proposalMaker, burnIn, interval, utilityNodes, targets*)(using universe)
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionVariableElimination.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionVariableElimination$.html#apply-989)

```scala
def apply(utilityNodes: List[Element[_]], targets: Element[_]*)(implicit universe: Universe): MultiDecisionVariableElimination
```

Create a OneTime multi-decision Variable Elimination the given target utilities, and target decisions.

Type parameters: none.

Parameters, list 1: `` utilityNodes: List[Element[_]] ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` MultiDecisionVariableElimination ``.

Source contract/attributes: Create a OneTime multi-decision Variable Elimination the given target utilities, and target decisions. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.decision.MultiDecisionVariableElimination.apply(utilityNodes, targets*)(using universe)
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionVariableElimination.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionVariableElimination.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Definition Classes MultiDecisionAlgorithm -> Algorithm Inherited from: MultiDecisionAlgorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionVariableElimination.getUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionVariableElimination.html#getUtility-182)

```scala
def getUtility[T, U](D: Decision[T, U], p: T, d: U): DecisionSample
```

Get the utility for a specific parent and decision in the multi-decision algorithm.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` D: Decision[T, U] ``; `` p: T ``; `` d: U ``.

Returns: `` DecisionSample ``.

Source contract/attributes: Get the utility for a specific parent and decision in the multi-decision algorithm. Attributes Inherited from: MultiDecisionAlgorithm

Invocation template:

```scala
receiver.getUtility[T, U](D, p, d)
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionVariableElimination.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionVariableElimination.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionVariableElimination.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionVariableElimination.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionVariableElimination.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionVariableElimination.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionVariableElimination.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionVariableElimination.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionVariableElimination.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionVariableElimination.html#run-8ac)

```scala
def run(oneStep: Boolean): Unit
```

Run in a debug mode where only a single decision is run each time.

Type parameters: none.

Parameters, list 1: `` oneStep: Boolean ``.

Returns: `` Unit ``.

Source contract/attributes: Run in a debug mode where only a single decision is run each time. Attributes Inherited from: MultiDecisionAlgorithm

Invocation template:

```scala
receiver.run(oneStep)
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionVariableElimination.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionVariableElimination.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Inherited from: MultiDecisionAlgorithm

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionVariableElimination.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionVariableElimination.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.decision.MultiDecisionVariableElimination.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/MultiDecisionVariableElimination.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeSampler -> OneTime

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.acceptRejectRatio ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#acceptRejectRatio-0)

```scala
def acceptRejectRatio: Double
```

Get the acceptance ratio for the sampler.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Get the acceptance ratio for the sampler. Attributes Inherited from: MetropolisHastings

Invocation template:

```scala
receiver.acceptRejectRatio
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Clean up the sampler, freeing memory.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clean up the sampler, freeing memory. Attributes Definition Classes MetropolisHastings -> Algorithm Inherited from: MetropolisHastings

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.cleanup ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#cleanup-94c)

```scala
def cleanup(): Unit
```

Cleans up the temporary elements created during sampling

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Cleans up the temporary elements created during sampling Attributes Inherited from: DecisionMetropolisHastings

Invocation template:

```scala
receiver.cleanup()
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#computeDistribution-fffffef0)

```scala
def computeDistribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQuerySampler

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#computeExpectation-fffffa66)

```scala
def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQuerySampler

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.computeUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#computeUtility-fffff328)

```scala
def computeUtility(): Map[(T, U), DecisionSample]
```

Compute the utility of each parent/decision tuple and return a DecisionSample. Each decision algorithm must define how this is done since it is used to set the policy for a decision. For sampling algorithms, this will me a map of parent/decision tuples to a utility and a weight for that combination. For factored algorithms, the DecisionSample will contain the exact expected utility with a weight of 1.0.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[(T, U), DecisionSample] ``.

Source contract/attributes: Compute the utility of each parent/decision tuple and return a DecisionSample. Each decision algorithm must define how this is done since it is used to set the policy for a decision. For sampling algorithms, this will me a map of parent/decision tuples to a utility and a weight for that combination. For factored algorithms, the DecisionSample will contain the exact expected utility with a weight of 1.0. Attributes Inherited from: DecisionMetropolisHastings

Invocation template:

```scala
receiver.computeUtility()
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#distribution-fffffef0)

```scala
def distribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.getSampleCount ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#getSampleCount-0)

```scala
def getSampleCount: Int
```

Number of samples taken

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Source contract/attributes: Number of samples taken Attributes Inherited from: BaseUnweightedSampler

Invocation template:

```scala
receiver.getSampleCount
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.getTotalWeight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#getTotalWeight-0)

```scala
def getTotalWeight: Double
```

Total weight of samples taken, in log space

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Total weight of samples taken, in log space Attributes Inherited from: UnweightedSampler

Invocation template:

```scala
receiver.getTotalWeight
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.getUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#getUtility-fffffba0)

```scala
def getUtility(p: T, d: U): DecisionSample
```

Get the total utility and weight for a specific value of a parent and decision.

Type parameters: none.

Parameters, list 1: `` p: T ``; `` d: U ``.

Returns: `` DecisionSample ``.

Source contract/attributes: Get the total utility and weight for a specific value of a parent and decision. Attributes Inherited from: DecisionAlgorithm

Invocation template:

```scala
receiver.getUtility(p, d)
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.getUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#getUtility-fffff328)

```scala
def getUtility(): Map[(T, U), DecisionSample]
```

Get the total utility and weight for all sampled values of the parent and decision.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[(T, U), DecisionSample] ``.

Source contract/attributes: Get the total utility and weight for all sampled values of the parent and decision. Attributes Inherited from: DecisionAlgorithm

Invocation template:

```scala
receiver.getUtility()
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.sample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#sample-ffffff32)

```scala
override def sample(): (Boolean, Sample)
```

Produce a single sample. In decision MH, we always update the target (parent and decision) since the utilities mights have changed

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` (Boolean, Sample) ``.

Source contract/attributes: Produce a single sample. In decision MH, we always update the target (parent and decision) since the utilities mights have changed Attributes Definition Classes DecisionMetropolisHastings -> MetropolisHastings -> BaseUnweightedSampler Inherited from: DecisionMetropolisHastings

Invocation template:

```scala
receiver.sample()
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.sampleFromPosterior ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#sampleFromPosterior-fffffdb4)

```scala
def sampleFromPosterior[T](element: Element[T]): Stream[T]
```

Sample an value from the posterior of this element

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` Stream[T] ``.

Source contract/attributes: Sample an value from the posterior of this element Attributes Inherited from: UnweightedSampler

Invocation template:

```scala
receiver.sampleFromPosterior[T](element)
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.setPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#setPolicy-fffff07c)

```scala
def setPolicy(e: Decision[T, U]): Unit
```

Sets the policy for the given decision. This will get the computed utility of the algorithm and call setPolicy on the decision. Note there is no error checking here, so the decision in the argument must match the target decision in the algorithm.

Type parameters: none.

Parameters, list 1: `` e: Decision[T, U] ``.

Returns: `` Unit ``.

Source contract/attributes: Sets the policy for the given decision. This will get the computed utility of the algorithm and call setPolicy on the decision. Note there is no error checking here, so the decision in the argument must match the target decision in the algorithm. Attributes Inherited from: DecisionAlgorithm

Invocation template:

```scala
receiver.setPolicy(e)
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.test ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#test-fffff570)

```scala
def test(numSamples: Int, predicates: Seq[Predicate[_]], elementsToTrack: Seq[Element[_]]): (Double, Map[Predicate[_], Double], Map[Element[_], Double])
```

Test Metropolis-Hastings by repeatedly running a single step from the same initial state. For each of a set of predicates, the fraction of times the predicate is satisfied by the resulting state is returned. By the resulting state, we mean the new state if it is accepted and the original state if not.

Type parameters: none.

Parameters, list 1: `` numSamples: Int ``; `` predicates: Seq[Predicate[_]] ``; `` elementsToTrack: Seq[Element[_]] ``.

Returns: `` (Double, Map[Predicate[_], Double], Map[Element[_], Double]) ``.

Source contract/attributes: Test Metropolis-Hastings by repeatedly running a single step from the same initial state. For each of a set of predicates, the fraction of times the predicate is satisfied by the resulting state is returned. By the resulting state, we mean the new state if it is accepted and the original state if not. Attributes Inherited from: MetropolisHastings

Invocation template:

```scala
receiver.test(numSamples, predicates, elementsToTrack)
```

## `` com.cra.figaro.algorithm.decision.OneTimeDecisionMetropolisHastings.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeDecisionMetropolisHastings.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionImportance.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionImportance.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Definition Classes MultiDecisionAlgorithm -> Algorithm Inherited from: MultiDecisionAlgorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionImportance.getUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionImportance.html#getUtility-182)

```scala
def getUtility[T, U](D: Decision[T, U], p: T, d: U): DecisionSample
```

Get the utility for a specific parent and decision in the multi-decision algorithm.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` D: Decision[T, U] ``; `` p: T ``; `` d: U ``.

Returns: `` DecisionSample ``.

Source contract/attributes: Get the utility for a specific parent and decision in the multi-decision algorithm. Attributes Inherited from: MultiDecisionAlgorithm

Invocation template:

```scala
receiver.getUtility[T, U](D, p, d)
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionImportance.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionImportance.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionImportance.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionImportance.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionImportance.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionImportance.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionImportance.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionImportance.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionImportance.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionImportance.html#run-8ac)

```scala
def run(oneStep: Boolean): Unit
```

Run in a debug mode where only a single decision is run each time.

Type parameters: none.

Parameters, list 1: `` oneStep: Boolean ``.

Returns: `` Unit ``.

Source contract/attributes: Run in a debug mode where only a single decision is run each time. Attributes Inherited from: MultiDecisionAlgorithm

Invocation template:

```scala
receiver.run(oneStep)
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionImportance.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionImportance.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Inherited from: MultiDecisionAlgorithm

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionImportance.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionImportance.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionImportance.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionImportance.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionMetropolisHastings.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionMetropolisHastings.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Definition Classes MultiDecisionAlgorithm -> Algorithm Inherited from: MultiDecisionAlgorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionMetropolisHastings.getUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionMetropolisHastings.html#getUtility-182)

```scala
def getUtility[T, U](D: Decision[T, U], p: T, d: U): DecisionSample
```

Get the utility for a specific parent and decision in the multi-decision algorithm.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` D: Decision[T, U] ``; `` p: T ``; `` d: U ``.

Returns: `` DecisionSample ``.

Source contract/attributes: Get the utility for a specific parent and decision in the multi-decision algorithm. Attributes Inherited from: MultiDecisionAlgorithm

Invocation template:

```scala
receiver.getUtility[T, U](D, p, d)
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionMetropolisHastings.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionMetropolisHastings.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionMetropolisHastings.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionMetropolisHastings.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionMetropolisHastings.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionMetropolisHastings.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionMetropolisHastings.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionMetropolisHastings.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionMetropolisHastings.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionMetropolisHastings.html#run-8ac)

```scala
def run(oneStep: Boolean): Unit
```

Run in a debug mode where only a single decision is run each time.

Type parameters: none.

Parameters, list 1: `` oneStep: Boolean ``.

Returns: `` Unit ``.

Source contract/attributes: Run in a debug mode where only a single decision is run each time. Attributes Inherited from: MultiDecisionAlgorithm

Invocation template:

```scala
receiver.run(oneStep)
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionMetropolisHastings.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionMetropolisHastings.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Inherited from: MultiDecisionAlgorithm

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionMetropolisHastings.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionMetropolisHastings.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.decision.OneTimeMultiDecisionMetropolisHastings.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeMultiDecisionMetropolisHastings.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#distribution-fffffef0)

```scala
def distribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.getUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#getUtility-fffffba0)

```scala
def getUtility(p: T, d: U): DecisionSample
```

Get the total utility and weight for a specific value of a parent and decision.

Type parameters: none.

Parameters, list 1: `` p: T ``; `` d: U ``.

Returns: `` DecisionSample ``.

Source contract/attributes: Get the total utility and weight for a specific value of a parent and decision. Attributes Inherited from: DecisionAlgorithm

Invocation template:

```scala
receiver.getUtility(p, d)
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.getUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#getUtility-fffff328)

```scala
def getUtility(): Map[(T, U), DecisionSample]
```

Get the total utility and weight for all sampled values of the parent and decision.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[(T, U), DecisionSample] ``.

Source contract/attributes: Get the total utility and weight for all sampled values of the parent and decision. Attributes Inherited from: DecisionAlgorithm

Invocation template:

```scala
receiver.getUtility()
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.setPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#setPolicy-fffff07c)

```scala
def setPolicy(e: Decision[T, U]): Unit
```

Sets the policy for the given decision. This will get the computed utility of the algorithm and call setPolicy on the decision. Note there is no error checking here, so the decision in the argument must match the target decision in the algorithm.

Type parameters: none.

Parameters, list 1: `` e: Decision[T, U] ``.

Returns: `` Unit ``.

Source contract/attributes: Sets the policy for the given decision. This will get the computed utility of the algorithm and call setPolicy on the decision. Note there is no error checking here, so the decision in the argument must match the target decision in the algorithm. Attributes Inherited from: DecisionAlgorithm

Invocation template:

```scala
receiver.setPolicy(e)
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#computeDistribution-fffffef0)

```scala
def computeDistribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#computeExpectation-fffffa66)

```scala
def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.computeUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#computeUtility-fffff328)

```scala
def computeUtility(): Map[(T, U), DecisionSample]
```

Compute the utility of each parent/decision tuple and return a DecisionSample. Each decision algorithm must define how this is done since it is used to set the policy for a decision. For sampling algorithms, this will me a map of parent/decision tuples to a utility and a weight for that combination. For factored algorithms, the DecisionSample will contain the exact expected utility with a weight of 1.0.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[(T, U), DecisionSample] ``.

Source contract/attributes: Compute the utility of each parent/decision tuple and return a DecisionSample. Each decision algorithm must define how this is done since it is used to set the policy for a decision. For sampling algorithms, this will me a map of parent/decision tuples to a utility and a weight for that combination. For factored algorithms, the DecisionSample will contain the exact expected utility with a weight of 1.0. Attributes Inherited from: DecisionAlgorithm

Invocation template:

```scala
receiver.computeUtility()
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.queryTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#queryTargets-0)

```scala
def queryTargets: Seq[Element[_]]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Seq[Element[_]] ``.

Source contract/attributes: Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.queryTargets
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Inherited from: OneTime

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.decision.OneTimeProbQueryDecision.universe ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/OneTimeProbQueryDecision.html#universe-0)

```scala
def universe: Universe
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Universe ``.

Source contract/attributes: Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.universe
```

## `` com.cra.figaro.algorithm.decision.ProbabilisticVariableEliminationDecision.getUtilityNodes ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbabilisticVariableEliminationDecision.html#getUtilityNodes-0)

```scala
def getUtilityNodes: List[Element[_]]
```

Retrieve utility nodes in the model

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Retrieve utility nodes in the model Attributes

Invocation template:

```scala
receiver.getUtilityNodes
```

## `` com.cra.figaro.algorithm.decision.ProbabilisticVariableEliminationDecision.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbabilisticVariableEliminationDecision.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upper: Boolean = ...): List[Factor[(Double, Double)]]
```

Create the factors for decision factors. Each factor is hardcoded as a tuple of (Double, Double), where the first value is the probability and the second is the utility.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upper: Boolean = ... ``.

Returns: `` List[Factor[(Double, Double)]] ``.

Source contract/attributes: Create the factors for decision factors. Each factor is hardcoded as a tuple of (Double, Double), where the first value is the probability and the second is the utility. Attributes

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upper)
```

## `` com.cra.figaro.algorithm.decision.ProbabilisticVariableEliminationDecision.makeUtilFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbabilisticVariableEliminationDecision.html#makeUtilFactor-fffffc80)

```scala
def makeUtilFactor(e: Element[_]): Factor[(Double, Double)]
```

Makes a utility factor an element designated as a utility. This is factor of a tuple (Double, Double) where the first value is 1.0 and the second is a possible utility of the element.

Type parameters: none.

Parameters, list 1: `` e: Element[_] ``.

Returns: `` Factor[(Double, Double)] ``.

Source contract/attributes: Makes a utility factor an element designated as a utility. This is factor of a tuple (Double, Double) where the first value is 1.0 and the second is a possible utility of the element. Attributes

Invocation template:

```scala
receiver.makeUtilFactor(e)
```

## `` com.cra.figaro.algorithm.decision.ProbabilisticVariableEliminationDecision.starterElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbabilisticVariableEliminationDecision.html#starterElements-0)

```scala
override def starterElements: List[Element[_]]
```

Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables. Attributes Definition Classes VariableElimination

Invocation template:

```scala
receiver.starterElements
```

## `` com.cra.figaro.algorithm.decision.ProbabilisticVariableEliminationDecision.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbabilisticVariableEliminationDecision.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.decision.ProbabilisticVariableEliminationDecision.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbabilisticVariableEliminationDecision.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.decision.ProbabilisticVariableEliminationDecision.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbabilisticVariableEliminationDecision.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.decision.ProbabilisticVariableEliminationDecision.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbabilisticVariableEliminationDecision.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.decision.ProbabilisticVariableEliminationDecision.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbabilisticVariableEliminationDecision.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.decision.ProbabilisticVariableEliminationDecision.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbabilisticVariableEliminationDecision.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.decision.ProbabilisticVariableEliminationDecision.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbabilisticVariableEliminationDecision.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Inherited from: VariableElimination

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.decision.ProbabilisticVariableEliminationDecision.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbabilisticVariableEliminationDecision.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.decision.ProbabilisticVariableEliminationDecision.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbabilisticVariableEliminationDecision.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.decision.ProbabilisticVariableEliminationDecision.finish ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbabilisticVariableEliminationDecision.html#finish-fffff214)

```scala
def finish(factorsAfterElimination: MultiSet[Factor[(Double, Double)]], eliminationOrder: List[Variable[_]]): Unit
```

All implementation of variable elimination must specify what to do after variables have been eliminated.

Type parameters: none.

Parameters, list 1: `` factorsAfterElimination: MultiSet[Factor[(Double, Double)]] ``; `` eliminationOrder: List[Variable[_]] ``.

Returns: `` Unit ``.

Source contract/attributes: All implementation of variable elimination must specify what to do after variables have been eliminated. Attributes Inherited from: VariableElimination

Invocation template:

```scala
receiver.finish(factorsAfterElimination, eliminationOrder)
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#computeDistribution-fffffdb4)

```scala
def computeDistribution[T](target: Element[T]): Stream[(Double, T)]
```

Returns distribution of the target, ignoring utilities.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Returns distribution of the target, ignoring utilities. Attributes

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#computeExpectation-c2a)

```scala
def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Returns expectation of the target, ignoring utilities

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Returns expectation of the target, ignoring utilities Attributes

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.computeUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#computeUtility-fffff328)

```scala
def computeUtility(): Map[(T, U), DecisionSample]
```

Returns the computed utility of all parent/decision tuple values. For VE, these are not samples but the actual computed expected utility for all combinations of the parent and decision.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[(T, U), DecisionSample] ``.

Source contract/attributes: Returns the computed utility of all parent/decision tuple values. For VE, these are not samples but the actual computed expected utility for all combinations of the parent and decision. Attributes

Invocation template:

```scala
receiver.computeUtility()
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.finish ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#finish-fffff214)

```scala
def finish(factorsAfterElimination: MultiSet[Factor[(Double, Double)]], eliminationOrder: List[Variable[_]]): Unit
```

All implementation of variable elimination must specify what to do after variables have been eliminated.

Type parameters: none.

Parameters, list 1: `` factorsAfterElimination: MultiSet[Factor[(Double, Double)]] ``; `` eliminationOrder: List[Variable[_]] ``.

Returns: `` Unit ``.

Source contract/attributes: All implementation of variable elimination must specify what to do after variables have been eliminated. Attributes

Invocation template:

```scala
receiver.finish(factorsAfterElimination, eliminationOrder)
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.getUtilityNodes ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#getUtilityNodes-0)

```scala
def getUtilityNodes: List[Element[_]]
```

Retrieve utility nodes in the model

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Retrieve utility nodes in the model Attributes

Invocation template:

```scala
receiver.getUtilityNodes
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#distribution-fffffef0)

```scala
def distribution[T](target: Element[T]): Stream[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upper: Boolean = ...): List[Factor[(Double, Double)]]
```

Create the factors for decision factors. Each factor is hardcoded as a tuple of (Double, Double), where the first value is the probability and the second is the utility.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upper: Boolean = ... ``.

Returns: `` List[Factor[(Double, Double)]] ``.

Source contract/attributes: Create the factors for decision factors. Each factor is hardcoded as a tuple of (Double, Double), where the first value is the probability and the second is the utility. Attributes Inherited from: ProbabilisticVariableEliminationDecision

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upper)
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.getUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#getUtility-fffffba0)

```scala
def getUtility(p: T, d: U): DecisionSample
```

Get the total utility and weight for a specific value of a parent and decision.

Type parameters: none.

Parameters, list 1: `` p: T ``; `` d: U ``.

Returns: `` DecisionSample ``.

Source contract/attributes: Get the total utility and weight for a specific value of a parent and decision. Attributes Inherited from: DecisionAlgorithm

Invocation template:

```scala
receiver.getUtility(p, d)
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.getUtility ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#getUtility-fffff328)

```scala
def getUtility(): Map[(T, U), DecisionSample]
```

Get the total utility and weight for all sampled values of the parent and decision.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Map[(T, U), DecisionSample] ``.

Source contract/attributes: Get the total utility and weight for all sampled values of the parent and decision. Attributes Inherited from: DecisionAlgorithm

Invocation template:

```scala
receiver.getUtility()
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.makeUtilFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#makeUtilFactor-fffffc80)

```scala
def makeUtilFactor(e: Element[_]): Factor[(Double, Double)]
```

Makes a utility factor an element designated as a utility. This is factor of a tuple (Double, Double) where the first value is 1.0 and the second is a possible utility of the element.

Type parameters: none.

Parameters, list 1: `` e: Element[_] ``.

Returns: `` Factor[(Double, Double)] ``.

Source contract/attributes: Makes a utility factor an element designated as a utility. This is factor of a tuple (Double, Double) where the first value is 1.0 and the second is a possible utility of the element. Attributes Inherited from: ProbabilisticVariableEliminationDecision

Invocation template:

```scala
receiver.makeUtilFactor(e)
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Inherited from: VariableElimination

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.setPolicy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#setPolicy-fffff07c)

```scala
def setPolicy(e: Decision[T, U]): Unit
```

Sets the policy for the given decision. This will get the computed utility of the algorithm and call setPolicy on the decision. Note there is no error checking here, so the decision in the argument must match the target decision in the algorithm.

Type parameters: none.

Parameters, list 1: `` e: Decision[T, U] ``.

Returns: `` Unit ``.

Source contract/attributes: Sets the policy for the given decision. This will get the computed utility of the algorithm and call setPolicy on the decision. Note there is no error checking here, so the decision in the argument must match the target decision in the algorithm. Attributes Inherited from: DecisionAlgorithm

Invocation template:

```scala
receiver.setPolicy(e)
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.starterElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#starterElements-0)

```scala
override def starterElements: List[Element[_]]
```

Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables. Attributes Definition Classes ProbabilisticVariableEliminationDecision -> VariableElimination Inherited from: ProbabilisticVariableEliminationDecision

Invocation template:

```scala
receiver.starterElements
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.decision.ProbQueryVariableEliminationDecision.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/decision/ProbQueryVariableEliminationDecision.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```
