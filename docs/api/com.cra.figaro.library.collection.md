<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.library.collection

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.library.collection.Container.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container$.html#apply-53)

```scala
def apply[T](elements: Element[T]*): Container[Int, T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` elements: Element[T]* ``.

Returns: `` Container[Int, T] ``.

Invocation template:

```scala
com.cra.figaro.library.collection.Container.apply[T](elements*)
```

## `` com.cra.figaro.library.collection.Container.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container$.html#apply-fffffdbe)

```scala
def apply[T](numItems: Element[Int], generator: Int => Element[T])(implicit name: Name[FixedSizeArray[T]], collection: ElementCollection): FixedSizeArrayElement[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` numItems: Element[Int] ``; `` generator: Int => Element[T] ``.

Context parameters, list 2: `` name: Name[FixedSizeArray[T]] ``; `` collection: ElementCollection ``.

Returns: `` FixedSizeArrayElement[T] ``.

Invocation template:

```scala
com.cra.figaro.library.collection.Container.apply[T](numItems, generator)(using name, collection)
```

## `` com.cra.figaro.library.collection.Container.toContainerElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container$.html#toContainerElement-74a)

```scala
implicit def toContainerElement[I, T](container: Container[I, T]): ContainerElement[I, T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` I ``; `` T ``.

Parameters, list 1: `` container: Container[I, T] ``.

Returns: `` ContainerElement[I, T] ``.

Invocation template:

```scala
com.cra.figaro.library.collection.Container.toContainerElement[I, T](container)
```

## `` com.cra.figaro.library.collection.Container.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#++-fffff0a0)

```scala
def ++(that: Container[Index, Value]): Container[Index, Value]
```

Returns a new container containing the elements of this container and the argument. If an index is defined in both container, the element of the argument is used.

Type parameters: none.

Parameters, list 1: `` that: Container[Index, Value] ``.

Returns: `` Container[Index, Value] ``.

Source contract/attributes: Returns a new container containing the elements of this container and the argument. If an index is defined in both container, the element of the argument is used. Attributes

Invocation template:

```scala
receiver.++(that)
```

## `` com.cra.figaro.library.collection.Container.aggregate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#aggregate-fffff132)

```scala
def aggregate[Value2](start: => Value2)(seqop: (Value2, Value) => Value2, combop: (Value2, Value2) => Value2): Element[Value2]
```

Aggregate the results of applying an operator to each element.

Type parameters: `` Value2 ``.

Parameters, list 1: `` start: => Value2 ``.

Parameters, list 2: `` seqop: (Value2, Value) => Value2 ``; `` combop: (Value2, Value2) => Value2 ``.

Returns: `` Element[Value2] ``.

Source contract/attributes: Aggregate the results of applying an operator to each element. Attributes

Invocation template:

```scala
receiver.aggregate[Value2](start)(seqop, combop)
```

## `` com.cra.figaro.library.collection.Container.aggregate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#aggregate-3b7)

```scala
def aggregate[Value2](start: => Value2)(seqop: (Value2, Value) => Value2, combop: (Value2, Value2) => Value2)(implicit name: Name[Value2], collection: ElementCollection): Element[Value2]
```

Aggregate the results of applying an operator to each element.

Type parameters: `` Value2 ``.

Parameters, list 1: `` start: => Value2 ``.

Parameters, list 2: `` seqop: (Value2, Value) => Value2 ``; `` combop: (Value2, Value2) => Value2 ``.

Context parameters, list 3: `` name: Name[Value2] ``; `` collection: ElementCollection ``.

Returns: `` Element[Value2] ``.

Source contract/attributes: Aggregate the results of applying an operator to each element. Attributes

Invocation template:

```scala
receiver.aggregate[Value2](start)(seqop, combop)(using name, collection)
```

## `` com.cra.figaro.library.collection.Container.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#apply-ade)

```scala
def apply(i: Index): Element[Value]
```

Creates an element whose value is the value at the corresponding index of the value of the process element.

Type parameters: none.

Parameters, list 1: `` i: Index ``.

Returns: `` Element[Value] ``.

Source contract/attributes: Creates an element whose value is the value at the corresponding index of the value of the process element. Attributes

Invocation template:

```scala
receiver.apply(i)
```

## `` com.cra.figaro.library.collection.Container.chain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#chain-fcd)

```scala
def chain[Value2](f: Value => Element[Value2]): ContainerElement[Index, Value2]
```

Chain the given function pointwise through the value of the container element.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Element[Value2] ``.

Returns: `` ContainerElement[Index, Value2] ``.

Source contract/attributes: Chain the given function pointwise through the value of the container element. Attributes

Invocation template:

```scala
receiver.chain[Value2](f)
```

## `` com.cra.figaro.library.collection.Container.chain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#chain-42f)

```scala
override def chain[Value2](f: Value => Element[Value2]): Container[Index, Value2]
```

Chain every value in this container through the given function, returning a new container.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Element[Value2] ``.

Returns: `` Container[Index, Value2] ``.

Source contract/attributes: Chain every value in this container through the given function, returning a new container. Attributes Definition Classes Process

Invocation template:

```scala
receiver.chain[Value2](f)
```

## `` com.cra.figaro.library.collection.Container.count ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#count-20)

```scala
def count(f: Value => Boolean): Element[Int]
```

Returns an element representing the number of elements in the container whose values satisfy the predicate.

Type parameters: none.

Parameters, list 1: `` f: Value => Boolean ``.

Returns: `` Element[Int] ``.

Source contract/attributes: Returns an element representing the number of elements in the container whose values satisfy the predicate. Attributes

Invocation template:

```scala
receiver.count(f)
```

## `` com.cra.figaro.library.collection.Container.count ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#count-989)

```scala
def count(f: Value => Boolean)(implicit name: Name[Int], collection: ElementCollection): Element[Int]
```

Returns an element representing the number of elements in the container whose values satisfy the predicate.

Type parameters: none.

Parameters, list 1: `` f: Value => Boolean ``.

Context parameters, list 2: `` name: Name[Int] ``; `` collection: ElementCollection ``.

Returns: `` Element[Int] ``.

Source contract/attributes: Returns an element representing the number of elements in the container whose values satisfy the predicate. Attributes

Invocation template:

```scala
receiver.count(f)(using name, collection)
```

## `` com.cra.figaro.library.collection.Container.elements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#elements-0)

```scala
def elements: Seq[Element[Value]]
```

Return all the elements in this container as an ordinary Scala Seq. This method caches the elements, like apply, so the same elements will be returned every time.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Seq[Element[Value]] ``.

Source contract/attributes: Return all the elements in this container as an ordinary Scala Seq. This method caches the elements, like apply, so the same elements will be returned every time. Attributes

Invocation template:

```scala
receiver.elements
```

## `` com.cra.figaro.library.collection.Container.exists ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#exists-20)

```scala
def exists(pred: Value => Boolean): Element[Boolean]
```

Returns an element representing whether the value of any element in the container satisfies the predicate.

Type parameters: none.

Parameters, list 1: `` pred: Value => Boolean ``.

Returns: `` Element[Boolean] ``.

Source contract/attributes: Returns an element representing whether the value of any element in the container satisfies the predicate. Attributes

Invocation template:

```scala
receiver.exists(pred)
```

## `` com.cra.figaro.library.collection.Container.exists ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#exists-989)

```scala
def exists(pred: Value => Boolean)(implicit name: Name[Boolean], collection: ElementCollection): Element[Boolean]
```

Returns an element representing whether the value of any element in the container satisfies the predicate.

Type parameters: none.

Parameters, list 1: `` pred: Value => Boolean ``.

Context parameters, list 2: `` name: Name[Boolean] ``; `` collection: ElementCollection ``.

Returns: `` Element[Boolean] ``.

Source contract/attributes: Returns an element representing whether the value of any element in the container satisfies the predicate. Attributes

Invocation template:

```scala
receiver.exists(pred)(using name, collection)
```

## `` com.cra.figaro.library.collection.Container.findIndex ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#findIndex-989)

```scala
def findIndex(pred: Value => Boolean)(implicit name: Name[Option[Index]], collection: ElementCollection): Element[Option[Index]]
```

Returns an element representing the optional index of the first element in the container whose value satisfies the predicate. The result has value None if no element is found.

Type parameters: none.

Parameters, list 1: `` pred: Value => Boolean ``.

Context parameters, list 2: `` name: Name[Option[Index]] ``; `` collection: ElementCollection ``.

Returns: `` Element[Option[Index]] ``.

Source contract/attributes: Returns an element representing the optional index of the first element in the container whose value satisfies the predicate. The result has value None if no element is found. Attributes

Invocation template:

```scala
receiver.findIndex(pred)(using name, collection)
```

## `` com.cra.figaro.library.collection.Container.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#flatMap-42f)

```scala
override def flatMap[Value2](f: Value => Element[Value2]): Container[Index, Value2]
```

Chain every value in this container through the given function, returning a new container.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Element[Value2] ``.

Returns: `` Container[Index, Value2] ``.

Source contract/attributes: Chain every value in this container through the given function, returning a new container. Attributes Definition Classes Process

Invocation template:

```scala
receiver.flatMap[Value2](f)
```

## `` com.cra.figaro.library.collection.Container.foldLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#foldLeft-c3)

```scala
def foldLeft[Value2](start: Value2)(f: (Value2, Value) => Value2): Element[Value2]
```

Produce the element over values obtained by selecting a particular container and folding through its values.

Type parameters: `` Value2 ``.

Parameters, list 1: `` start: Value2 ``.

Parameters, list 2: `` f: (Value2, Value) => Value2 ``.

Returns: `` Element[Value2] ``.

Source contract/attributes: Produce the element over values obtained by selecting a particular container and folding through its values. Attributes

Invocation template:

```scala
receiver.foldLeft[Value2](start)(f)
```

## `` com.cra.figaro.library.collection.Container.foldLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#foldLeft-ec6)

```scala
def foldLeft[Value2](start: Value2)(f: (Value2, Value) => Value2)(implicit name: Name[Value2], collection: ElementCollection): Element[Value2]
```

Fold the values in this container through the given function.

Type parameters: `` Value2 ``.

Parameters, list 1: `` start: Value2 ``.

Parameters, list 2: `` f: (Value2, Value) => Value2 ``.

Context parameters, list 3: `` name: Name[Value2] ``; `` collection: ElementCollection ``.

Returns: `` Element[Value2] ``.

Source contract/attributes: Fold the values in this container through the given function. Attributes

Invocation template:

```scala
receiver.foldLeft[Value2](start)(f)(using name, collection)
```

## `` com.cra.figaro.library.collection.Container.foldRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#foldRight-c3)

```scala
def foldRight[Value2](start: Value2)(f: (Value, Value2) => Value2): Element[Value2]
```

Produce the element over values obtained by selecting a particular container and folding through its values.

Type parameters: `` Value2 ``.

Parameters, list 1: `` start: Value2 ``.

Parameters, list 2: `` f: (Value, Value2) => Value2 ``.

Returns: `` Element[Value2] ``.

Source contract/attributes: Produce the element over values obtained by selecting a particular container and folding through its values. Attributes

Invocation template:

```scala
receiver.foldRight[Value2](start)(f)
```

## `` com.cra.figaro.library.collection.Container.foldRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#foldRight-ec6)

```scala
def foldRight[Value2](start: Value2)(f: (Value, Value2) => Value2)(implicit name: Name[Value2], collection: ElementCollection): Element[Value2]
```

Fold the values in this container through the given function.

Type parameters: `` Value2 ``.

Parameters, list 1: `` start: Value2 ``.

Parameters, list 2: `` f: (Value, Value2) => Value2 ``.

Context parameters, list 3: `` name: Name[Value2] ``; `` collection: ElementCollection ``.

Returns: `` Element[Value2] ``.

Source contract/attributes: Fold the values in this container through the given function. Attributes

Invocation template:

```scala
receiver.foldRight[Value2](start)(f)(using name, collection)
```

## `` com.cra.figaro.library.collection.Container.forall ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#forall-20)

```scala
def forall(pred: Value => Boolean): Element[Boolean]
```

Returns an element representing whether the values of all elements in the container satisfy the predicate.

Type parameters: none.

Parameters, list 1: `` pred: Value => Boolean ``.

Returns: `` Element[Boolean] ``.

Source contract/attributes: Returns an element representing whether the values of all elements in the container satisfy the predicate. Attributes

Invocation template:

```scala
receiver.forall(pred)
```

## `` com.cra.figaro.library.collection.Container.forall ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#forall-989)

```scala
def forall(pred: Value => Boolean)(implicit name: Name[Boolean], collection: ElementCollection): Element[Boolean]
```

Returns an element representing whether the values of all elements in the container satisfy the predicate.

Type parameters: none.

Parameters, list 1: `` pred: Value => Boolean ``.

Context parameters, list 2: `` name: Name[Boolean] ``; `` collection: ElementCollection ``.

Returns: `` Element[Boolean] ``.

Source contract/attributes: Returns an element representing whether the values of all elements in the container satisfy the predicate. Attributes

Invocation template:

```scala
receiver.forall(pred)(using name, collection)
```

## `` com.cra.figaro.library.collection.Container.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#get-ade)

```scala
def get(i: Index): Element[Option[Value]]
```

Creates an element whose value is the value at the corresponding index in the value of the container element, if the index is in range, None otherwise.

Type parameters: none.

Parameters, list 1: `` i: Index ``.

Returns: `` Element[Option[Value]] ``.

Source contract/attributes: Creates an element whose value is the value at the corresponding index in the value of the container element, if the index is in range, None otherwise. Attributes

Invocation template:

```scala
receiver.get(i)
```

## `` com.cra.figaro.library.collection.Container.length ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#length-0)

```scala
def length: Element[Int]
```

Returns an element representing the length of the container.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Element[Int] ``.

Source contract/attributes: Returns an element representing the length of the container. Attributes

Invocation template:

```scala
receiver.length
```

## `` com.cra.figaro.library.collection.Container.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#map-fcd)

```scala
def map[Value2](f: Value => Value2): ContainerElement[Index, Value2]
```

Map the given function pointwise through the value of the container element.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Value2 ``.

Returns: `` ContainerElement[Index, Value2] ``.

Source contract/attributes: Map the given function pointwise through the value of the container element. Attributes

Invocation template:

```scala
receiver.map[Value2](f)
```

## `` com.cra.figaro.library.collection.Container.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#map-42f)

```scala
override def map[Value2](f: Value => Value2): Container[Index, Value2]
```

Apply the given function to every value in this container, returning a new container.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Value2 ``.

Returns: `` Container[Index, Value2] ``.

Source contract/attributes: Apply the given function to every value in this container, returning a new container. Attributes Definition Classes Process

Invocation template:

```scala
receiver.map[Value2](f)
```

## `` com.cra.figaro.library.collection.Container.randomElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#randomElement-6d1)

```scala
def randomElement(): Element[Value]
```

Select a random element from the container. Ensures that no IndexOutOfRange exception should be thrown.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Element[Value] ``.

Source contract/attributes: Select a random element from the container. Ensures that no IndexOutOfRange exception should be thrown. Attributes

Invocation template:

```scala
receiver.randomElement()
```

## `` com.cra.figaro.library.collection.Container.randomElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#randomElement-ffffffd6)

```scala
def randomElement(): Chain[Int, Value]
```

Choose a random element from this container.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Chain[Int, Value] ``.

Source contract/attributes: Choose a random element from this container. Attributes

Invocation template:

```scala
receiver.randomElement()
```

## `` com.cra.figaro.library.collection.Container.rangeCheck ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#rangeCheck-4ad)

```scala
def rangeCheck(index: Index): Boolean
```

Check whether the given index has an element.

Type parameters: none.

Parameters, list 1: `` index: Index ``.

Returns: `` Boolean ``.

Source contract/attributes: Check whether the given index has an element. Attributes

Invocation template:

```scala
receiver.rangeCheck(index)
```

## `` com.cra.figaro.library.collection.Container.reduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#reduce-ffffffff)

```scala
def reduce(f: (Value, Value) => Value): Element[Value]
```

Produce the element over values obtained by selecting a particular container and reducing through its values.

Type parameters: none.

Parameters, list 1: `` f: (Value, Value) => Value ``.

Returns: `` Element[Value] ``.

Source contract/attributes: Produce the element over values obtained by selecting a particular container and reducing through its values. Attributes

Invocation template:

```scala
receiver.reduce(f)
```

## `` com.cra.figaro.library.collection.Container.reduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#reduce-fffff50a)

```scala
def reduce(f: (Value, Value) => Value)(implicit name: Name[Value], collection: ElementCollection): Element[Value]
```

Reduce the values in this container through the given function.

Type parameters: none.

Parameters, list 1: `` f: (Value, Value) => Value ``.

Context parameters, list 2: `` name: Name[Value] ``; `` collection: ElementCollection ``.

Returns: `` Element[Value] ``.

Source contract/attributes: Reduce the values in this container through the given function. Attributes

Invocation template:

```scala
receiver.reduce(f)(using name, collection)
```

## `` com.cra.figaro.library.collection.Container.toMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#toMap-0)

```scala
def toMap: Map[Index, Element[Value]]
```

Convert this container into an ordinary Scala map.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Map[Index, Element[Value]] ``.

Source contract/attributes: Convert this container into an ordinary Scala map. Attributes

Invocation template:

```scala
receiver.toMap
```

## `` com.cra.figaro.library.collection.Container.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#++-fffffca0)

```scala
def ++(that: Process[Index, Value]): Process[Index, Value]
```

Returns a new process containing the elements of this process and the argument. If an index is defined in both processes, the element of the argument is used.

Type parameters: none.

Parameters, list 1: `` that: Process[Index, Value] ``.

Returns: `` Process[Index, Value] ``.

Source contract/attributes: Returns a new process containing the elements of this process and the argument. If an index is defined in both processes, the element of the argument is used. Attributes Inherited from: Process

Invocation template:

```scala
receiver.++(that)
```

## `` com.cra.figaro.library.collection.Container.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#apply-ee2)

```scala
def apply(indices: Iterable[Index]): Map[Index, Element[Value]]
```

Get the elements representing the value of the process at the given indices. Throws IndexOutOfRangeException if any index has no value.

Type parameters: none.

Parameters, list 1: `` indices: Iterable[Index] ``.

Returns: `` Map[Index, Element[Value]] ``.

Source contract/attributes: Get the elements representing the value of the process at the given indices. Throws IndexOutOfRangeException if any index has no value. Attributes Inherited from: Process

Invocation template:

```scala
receiver.apply(indices)
```

## `` com.cra.figaro.library.collection.Container.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#apply-ade)

```scala
def apply(index: Index): Element[Value]
```

Get an element representing the value of the process at the given index. Throws IndexOutOfRangeException if the index has no value.

Type parameters: none.

Parameters, list 1: `` index: Index ``.

Returns: `` Element[Value] ``.

Source contract/attributes: Get an element representing the value of the process at the given index. Throws IndexOutOfRangeException if the index has no value. This apply method is cached, so calling process(index) always returns the same element. Attributes Inherited from: Process

Invocation template:

```scala
receiver.apply(index)
```

## `` com.cra.figaro.library.collection.Container.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#get-ee2)

```scala
def get(indices: Iterable[Index]): Map[Index, Element[Option[Value]]]
```

Safely get the elements over optional values at all of the indices. Any index that is not in range will always have value None. Dependencies between elements for indices in range will be produced.

Type parameters: none.

Parameters, list 1: `` indices: Iterable[Index] ``.

Returns: `` Map[Index, Element[Option[Value]]] ``.

Source contract/attributes: Safely get the elements over optional values at all of the indices. Any index that is not in range will always have value None. Dependencies between elements for indices in range will be produced. Attributes Inherited from: Process

Invocation template:

```scala
receiver.get(indices)
```

## `` com.cra.figaro.library.collection.Container.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#get-ade)

```scala
def get(index: Index): Element[Option[Value]]
```

Safely get an element over an optional value at the index. If the index is in range, the value of the element will be Some(something). If the index is out of range, the value of the element will be None.

Type parameters: none.

Parameters, list 1: `` index: Index ``.

Returns: `` Element[Option[Value]] ``.

Source contract/attributes: Safely get an element over an optional value at the index. If the index is in range, the value of the element will be Some(something). If the index is out of range, the value of the element will be None. Attributes Inherited from: Process

Invocation template:

```scala
receiver.get(index)
```

## `` com.cra.figaro.library.collection.Container.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#generate-ade)

```scala
def generate(index: Index): Element[Value]
```

Produce the element corresponding to the value of the process at the given index. This method can assume that the indices has already been range checked.

Type parameters: none.

Parameters, list 1: `` index: Index ``.

Returns: `` Element[Value] ``.

Source contract/attributes: Produce the element corresponding to the value of the process at the given index. This method can assume that the indices has already been range checked. Attributes Inherited from: Process

Invocation template:

```scala
receiver.generate(index)
```

## `` com.cra.figaro.library.collection.Container.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Container.html#generate-3fa)

```scala
def generate(indices: List[Index]): Map[Index, Element[Value]]
```

Produce the elements representing the value of the process at the given indices. Ensures that any dependencies between the elements are represented. This method must be implemented by implementations of Process. The return value maps each provided index to the corresponding element. This method can assume that the indices has already been range checked.

Type parameters: none.

Parameters, list 1: `` indices: List[Index] ``.

Returns: `` Map[Index, Element[Value]] ``.

Source contract/attributes: Produce the elements representing the value of the process at the given indices. Ensures that any dependencies between the elements are represented. This method must be implemented by implementations of Process. The return value maps each provided index to the corresponding element. This method can assume that the indices has already been range checked. Attributes Inherited from: Process

Invocation template:

```scala
receiver.generate(indices)
```

## `` com.cra.figaro.library.collection.ContainerElement.aggregate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/ContainerElement.html#aggregate-fffff132)

```scala
def aggregate[Value2](start: => Value2)(seqop: (Value2, Value) => Value2, combop: (Value2, Value2) => Value2): Element[Value2]
```

Aggregate the results of applying an operator to each element.

Type parameters: `` Value2 ``.

Parameters, list 1: `` start: => Value2 ``.

Parameters, list 2: `` seqop: (Value2, Value) => Value2 ``; `` combop: (Value2, Value2) => Value2 ``.

Returns: `` Element[Value2] ``.

Source contract/attributes: Aggregate the results of applying an operator to each element. Attributes

Invocation template:

```scala
receiver.aggregate[Value2](start)(seqop, combop)
```

## `` com.cra.figaro.library.collection.ContainerElement.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/ContainerElement.html#apply-ade)

```scala
def apply(i: Index): Element[Value]
```

Creates an element whose value is the value at the corresponding index of the value of the process element.

Type parameters: none.

Parameters, list 1: `` i: Index ``.

Returns: `` Element[Value] ``.

Source contract/attributes: Creates an element whose value is the value at the corresponding index of the value of the process element. Attributes

Invocation template:

```scala
receiver.apply(i)
```

## `` com.cra.figaro.library.collection.ContainerElement.chain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/ContainerElement.html#chain-fcd)

```scala
def chain[Value2](f: Value => Element[Value2]): ContainerElement[Index, Value2]
```

Chain the given function pointwise through the value of the container element.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Element[Value2] ``.

Returns: `` ContainerElement[Index, Value2] ``.

Source contract/attributes: Chain the given function pointwise through the value of the container element. Attributes

Invocation template:

```scala
receiver.chain[Value2](f)
```

## `` com.cra.figaro.library.collection.ContainerElement.count ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/ContainerElement.html#count-20)

```scala
def count(f: Value => Boolean): Element[Int]
```

Returns an element representing the number of elements in the container whose values satisfy the predicate.

Type parameters: none.

Parameters, list 1: `` f: Value => Boolean ``.

Returns: `` Element[Int] ``.

Source contract/attributes: Returns an element representing the number of elements in the container whose values satisfy the predicate. Attributes

Invocation template:

```scala
receiver.count(f)
```

## `` com.cra.figaro.library.collection.ContainerElement.exists ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/ContainerElement.html#exists-20)

```scala
def exists(pred: Value => Boolean): Element[Boolean]
```

Returns an element representing whether the value of any element in the container satisfies the predicate.

Type parameters: none.

Parameters, list 1: `` pred: Value => Boolean ``.

Returns: `` Element[Boolean] ``.

Source contract/attributes: Returns an element representing whether the value of any element in the container satisfies the predicate. Attributes

Invocation template:

```scala
receiver.exists(pred)
```

## `` com.cra.figaro.library.collection.ContainerElement.foldLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/ContainerElement.html#foldLeft-c3)

```scala
def foldLeft[Value2](start: Value2)(f: (Value2, Value) => Value2): Element[Value2]
```

Produce the element over values obtained by selecting a particular container and folding through its values.

Type parameters: `` Value2 ``.

Parameters, list 1: `` start: Value2 ``.

Parameters, list 2: `` f: (Value2, Value) => Value2 ``.

Returns: `` Element[Value2] ``.

Source contract/attributes: Produce the element over values obtained by selecting a particular container and folding through its values. Attributes

Invocation template:

```scala
receiver.foldLeft[Value2](start)(f)
```

## `` com.cra.figaro.library.collection.ContainerElement.foldRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/ContainerElement.html#foldRight-c3)

```scala
def foldRight[Value2](start: Value2)(f: (Value, Value2) => Value2): Element[Value2]
```

Produce the element over values obtained by selecting a particular container and folding through its values.

Type parameters: `` Value2 ``.

Parameters, list 1: `` start: Value2 ``.

Parameters, list 2: `` f: (Value, Value2) => Value2 ``.

Returns: `` Element[Value2] ``.

Source contract/attributes: Produce the element over values obtained by selecting a particular container and folding through its values. Attributes

Invocation template:

```scala
receiver.foldRight[Value2](start)(f)
```

## `` com.cra.figaro.library.collection.ContainerElement.forall ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/ContainerElement.html#forall-20)

```scala
def forall(pred: Value => Boolean): Element[Boolean]
```

Returns an element representing whether the values of all elements in the container satisfy the predicate.

Type parameters: none.

Parameters, list 1: `` pred: Value => Boolean ``.

Returns: `` Element[Boolean] ``.

Source contract/attributes: Returns an element representing whether the values of all elements in the container satisfy the predicate. Attributes

Invocation template:

```scala
receiver.forall(pred)
```

## `` com.cra.figaro.library.collection.ContainerElement.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/ContainerElement.html#get-ade)

```scala
def get(i: Index): Element[Option[Value]]
```

Creates an element whose value is the value at the corresponding index in the value of the container element, if the index is in range, None otherwise.

Type parameters: none.

Parameters, list 1: `` i: Index ``.

Returns: `` Element[Option[Value]] ``.

Source contract/attributes: Creates an element whose value is the value at the corresponding index in the value of the container element, if the index is in range, None otherwise. Attributes

Invocation template:

```scala
receiver.get(i)
```

## `` com.cra.figaro.library.collection.ContainerElement.length ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/ContainerElement.html#length-0)

```scala
def length: Element[Int]
```

Returns an element representing the length of the container.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Element[Int] ``.

Source contract/attributes: Returns an element representing the length of the container. Attributes

Invocation template:

```scala
receiver.length
```

## `` com.cra.figaro.library.collection.ContainerElement.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/ContainerElement.html#map-fcd)

```scala
def map[Value2](f: Value => Value2): ContainerElement[Index, Value2]
```

Map the given function pointwise through the value of the container element.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Value2 ``.

Returns: `` ContainerElement[Index, Value2] ``.

Source contract/attributes: Map the given function pointwise through the value of the container element. Attributes

Invocation template:

```scala
receiver.map[Value2](f)
```

## `` com.cra.figaro.library.collection.ContainerElement.randomElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/ContainerElement.html#randomElement-6d1)

```scala
def randomElement(): Element[Value]
```

Select a random element from the container. Ensures that no IndexOutOfRange exception should be thrown.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Element[Value] ``.

Source contract/attributes: Select a random element from the container. Ensures that no IndexOutOfRange exception should be thrown. Attributes

Invocation template:

```scala
receiver.randomElement()
```

## `` com.cra.figaro.library.collection.ContainerElement.reduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/ContainerElement.html#reduce-ffffffff)

```scala
def reduce(f: (Value, Value) => Value): Element[Value]
```

Produce the element over values obtained by selecting a particular container and reducing through its values.

Type parameters: none.

Parameters, list 1: `` f: (Value, Value) => Value ``.

Returns: `` Element[Value] ``.

Source contract/attributes: Produce the element over values obtained by selecting a particular container and reducing through its values. Attributes

Invocation template:

```scala
receiver.reduce(f)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.concat ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#concat-fffffd5d)

```scala
def concat[Index2](that: FixedSizeArray[Value]): FixedSizeArray[Value]
```

Concatenate this container with another one. In the result, all the elements in this container will precede all the elements in the other container. The result is an array containing all the elements of both containers.

Type parameters: `` Index2 ``.

Parameters, list 1: `` that: FixedSizeArray[Value] ``.

Returns: `` FixedSizeArray[Value] ``.

Source contract/attributes: Concatenate this container with another one. In the result, all the elements in this container will precede all the elements in the other container. The result is an array containing all the elements of both containers. Attributes

Invocation template:

```scala
receiver.concat[Index2](that)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#generate-fffff2ca)

```scala
def generate(index: Int): Element[Value]
```

Produce the element corresponding to the value of the process at the given index. This method can assume that the indices has already been range checked.

Type parameters: none.

Parameters, list 1: `` index: Int ``.

Returns: `` Element[Value] ``.

Source contract/attributes: Produce the element corresponding to the value of the process at the given index. This method can assume that the indices has already been range checked. Attributes

Invocation template:

```scala
receiver.generate(index)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#generate-3fa)

```scala
def generate(indices: List[Int]): Map[Int, Element[Value]]
```

Produce the elements representing the value of the process at the given indices. Ensures that any dependencies between the elements are represented. This method must be implemented by implementations of Process. The return value maps each provided index to the corresponding element. This method can assume that the indices has already been range checked.

Type parameters: none.

Parameters, list 1: `` indices: List[Int] ``.

Returns: `` Map[Int, Element[Value]] ``.

Source contract/attributes: Produce the elements representing the value of the process at the given indices. Ensures that any dependencies between the elements are represented. This method must be implemented by implementations of Process. The return value maps each provided index to the corresponding element. This method can assume that the indices has already been range checked. Attributes

Invocation template:

```scala
receiver.generate(indices)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.rangeCheck ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#rangeCheck-ffffff99)

```scala
override def rangeCheck(index: Int): Boolean
```

Check whether the given index has an element.

Type parameters: none.

Parameters, list 1: `` index: Int ``.

Returns: `` Boolean ``.

Source contract/attributes: Check whether the given index has an element. Attributes Definition Classes Container -> Process

Invocation template:

```scala
receiver.rangeCheck(index)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#++-fffff0a0)

```scala
def ++(that: Container[Int, Value]): Container[Int, Value]
```

Returns a new container containing the elements of this container and the argument. If an index is defined in both container, the element of the argument is used.

Type parameters: none.

Parameters, list 1: `` that: Container[Int, Value] ``.

Returns: `` Container[Int, Value] ``.

Source contract/attributes: Returns a new container containing the elements of this container and the argument. If an index is defined in both container, the element of the argument is used. Attributes Inherited from: Container

Invocation template:

```scala
receiver.++(that)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#++-fffffca0)

```scala
def ++(that: Process[Int, Value]): Process[Int, Value]
```

Returns a new process containing the elements of this process and the argument. If an index is defined in both processes, the element of the argument is used.

Type parameters: none.

Parameters, list 1: `` that: Process[Int, Value] ``.

Returns: `` Process[Int, Value] ``.

Source contract/attributes: Returns a new process containing the elements of this process and the argument. If an index is defined in both processes, the element of the argument is used. Attributes Inherited from: Process

Invocation template:

```scala
receiver.++(that)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.aggregate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#aggregate-3b7)

```scala
def aggregate[Value2](start: => Value2)(seqop: (Value2, Value) => Value2, combop: (Value2, Value2) => Value2)(implicit name: Name[Value2], collection: ElementCollection): Element[Value2]
```

Aggregate the results of applying an operator to each element.

Type parameters: `` Value2 ``.

Parameters, list 1: `` start: => Value2 ``.

Parameters, list 2: `` seqop: (Value2, Value) => Value2 ``; `` combop: (Value2, Value2) => Value2 ``.

Context parameters, list 3: `` name: Name[Value2] ``; `` collection: ElementCollection ``.

Returns: `` Element[Value2] ``.

Source contract/attributes: Aggregate the results of applying an operator to each element. Attributes Inherited from: Container

Invocation template:

```scala
receiver.aggregate[Value2](start)(seqop, combop)(using name, collection)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#apply-ee2)

```scala
def apply(indices: Iterable[Int]): Map[Int, Element[Value]]
```

Get the elements representing the value of the process at the given indices. Throws IndexOutOfRangeException if any index has no value.

Type parameters: none.

Parameters, list 1: `` indices: Iterable[Int] ``.

Returns: `` Map[Int, Element[Value]] ``.

Source contract/attributes: Get the elements representing the value of the process at the given indices. Throws IndexOutOfRangeException if any index has no value. Attributes Inherited from: Process

Invocation template:

```scala
receiver.apply(indices)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#apply-ade)

```scala
def apply(index: Int): Element[Value]
```

Get an element representing the value of the process at the given index. Throws IndexOutOfRangeException if the index has no value.

Type parameters: none.

Parameters, list 1: `` index: Int ``.

Returns: `` Element[Value] ``.

Source contract/attributes: Get an element representing the value of the process at the given index. Throws IndexOutOfRangeException if the index has no value. This apply method is cached, so calling process(index) always returns the same element. Attributes Inherited from: Process

Invocation template:

```scala
receiver.apply(index)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.chain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#chain-42f)

```scala
override def chain[Value2](f: Value => Element[Value2]): Container[Int, Value2]
```

Chain every value in this container through the given function, returning a new container.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Element[Value2] ``.

Returns: `` Container[Int, Value2] ``.

Source contract/attributes: Chain every value in this container through the given function, returning a new container. Attributes Definition Classes Container -> Process Inherited from: Container

Invocation template:

```scala
receiver.chain[Value2](f)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.count ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#count-989)

```scala
def count(f: Value => Boolean)(implicit name: Name[Int], collection: ElementCollection): Element[Int]
```

Returns an element representing the number of elements in the container whose values satisfy the predicate.

Type parameters: none.

Parameters, list 1: `` f: Value => Boolean ``.

Context parameters, list 2: `` name: Name[Int] ``; `` collection: ElementCollection ``.

Returns: `` Element[Int] ``.

Source contract/attributes: Returns an element representing the number of elements in the container whose values satisfy the predicate. Attributes Inherited from: Container

Invocation template:

```scala
receiver.count(f)(using name, collection)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.elements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#elements-0)

```scala
def elements: Seq[Element[Value]]
```

Return all the elements in this container as an ordinary Scala Seq. This method caches the elements, like apply, so the same elements will be returned every time.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Seq[Element[Value]] ``.

Source contract/attributes: Return all the elements in this container as an ordinary Scala Seq. This method caches the elements, like apply, so the same elements will be returned every time. Attributes Inherited from: Container

Invocation template:

```scala
receiver.elements
```

## `` com.cra.figaro.library.collection.FixedSizeArray.exists ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#exists-989)

```scala
def exists(pred: Value => Boolean)(implicit name: Name[Boolean], collection: ElementCollection): Element[Boolean]
```

Returns an element representing whether the value of any element in the container satisfies the predicate.

Type parameters: none.

Parameters, list 1: `` pred: Value => Boolean ``.

Context parameters, list 2: `` name: Name[Boolean] ``; `` collection: ElementCollection ``.

Returns: `` Element[Boolean] ``.

Source contract/attributes: Returns an element representing whether the value of any element in the container satisfies the predicate. Attributes Inherited from: Container

Invocation template:

```scala
receiver.exists(pred)(using name, collection)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.findIndex ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#findIndex-989)

```scala
def findIndex(pred: Value => Boolean)(implicit name: Name[Option[Int]], collection: ElementCollection): Element[Option[Int]]
```

Returns an element representing the optional index of the first element in the container whose value satisfies the predicate. The result has value None if no element is found.

Type parameters: none.

Parameters, list 1: `` pred: Value => Boolean ``.

Context parameters, list 2: `` name: Name[Option[Int]] ``; `` collection: ElementCollection ``.

Returns: `` Element[Option[Int]] ``.

Source contract/attributes: Returns an element representing the optional index of the first element in the container whose value satisfies the predicate. The result has value None if no element is found. Attributes Inherited from: Container

Invocation template:

```scala
receiver.findIndex(pred)(using name, collection)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#flatMap-42f)

```scala
override def flatMap[Value2](f: Value => Element[Value2]): Container[Int, Value2]
```

Chain every value in this container through the given function, returning a new container.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Element[Value2] ``.

Returns: `` Container[Int, Value2] ``.

Source contract/attributes: Chain every value in this container through the given function, returning a new container. Attributes Definition Classes Container -> Process Inherited from: Container

Invocation template:

```scala
receiver.flatMap[Value2](f)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.foldLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#foldLeft-ec6)

```scala
def foldLeft[Value2](start: Value2)(f: (Value2, Value) => Value2)(implicit name: Name[Value2], collection: ElementCollection): Element[Value2]
```

Fold the values in this container through the given function.

Type parameters: `` Value2 ``.

Parameters, list 1: `` start: Value2 ``.

Parameters, list 2: `` f: (Value2, Value) => Value2 ``.

Context parameters, list 3: `` name: Name[Value2] ``; `` collection: ElementCollection ``.

Returns: `` Element[Value2] ``.

Source contract/attributes: Fold the values in this container through the given function. Attributes Inherited from: Container

Invocation template:

```scala
receiver.foldLeft[Value2](start)(f)(using name, collection)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.foldRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#foldRight-ec6)

```scala
def foldRight[Value2](start: Value2)(f: (Value, Value2) => Value2)(implicit name: Name[Value2], collection: ElementCollection): Element[Value2]
```

Fold the values in this container through the given function.

Type parameters: `` Value2 ``.

Parameters, list 1: `` start: Value2 ``.

Parameters, list 2: `` f: (Value, Value2) => Value2 ``.

Context parameters, list 3: `` name: Name[Value2] ``; `` collection: ElementCollection ``.

Returns: `` Element[Value2] ``.

Source contract/attributes: Fold the values in this container through the given function. Attributes Inherited from: Container

Invocation template:

```scala
receiver.foldRight[Value2](start)(f)(using name, collection)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.forall ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#forall-989)

```scala
def forall(pred: Value => Boolean)(implicit name: Name[Boolean], collection: ElementCollection): Element[Boolean]
```

Returns an element representing whether the values of all elements in the container satisfy the predicate.

Type parameters: none.

Parameters, list 1: `` pred: Value => Boolean ``.

Context parameters, list 2: `` name: Name[Boolean] ``; `` collection: ElementCollection ``.

Returns: `` Element[Boolean] ``.

Source contract/attributes: Returns an element representing whether the values of all elements in the container satisfy the predicate. Attributes Inherited from: Container

Invocation template:

```scala
receiver.forall(pred)(using name, collection)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#get-ee2)

```scala
def get(indices: Iterable[Int]): Map[Int, Element[Option[Value]]]
```

Safely get the elements over optional values at all of the indices. Any index that is not in range will always have value None. Dependencies between elements for indices in range will be produced.

Type parameters: none.

Parameters, list 1: `` indices: Iterable[Int] ``.

Returns: `` Map[Int, Element[Option[Value]]] ``.

Source contract/attributes: Safely get the elements over optional values at all of the indices. Any index that is not in range will always have value None. Dependencies between elements for indices in range will be produced. Attributes Inherited from: Process

Invocation template:

```scala
receiver.get(indices)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#get-ade)

```scala
def get(index: Int): Element[Option[Value]]
```

Safely get an element over an optional value at the index. If the index is in range, the value of the element will be Some(something). If the index is out of range, the value of the element will be None.

Type parameters: none.

Parameters, list 1: `` index: Int ``.

Returns: `` Element[Option[Value]] ``.

Source contract/attributes: Safely get an element over an optional value at the index. If the index is in range, the value of the element will be Some(something). If the index is out of range, the value of the element will be None. Attributes Inherited from: Process

Invocation template:

```scala
receiver.get(index)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#map-42f)

```scala
override def map[Value2](f: Value => Value2): Container[Int, Value2]
```

Apply the given function to every value in this container, returning a new container.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Value2 ``.

Returns: `` Container[Int, Value2] ``.

Source contract/attributes: Apply the given function to every value in this container, returning a new container. Attributes Definition Classes Container -> Process Inherited from: Container

Invocation template:

```scala
receiver.map[Value2](f)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.randomElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#randomElement-ffffffd6)

```scala
def randomElement(): Chain[Int, Value]
```

Choose a random element from this container.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Chain[Int, Value] ``.

Source contract/attributes: Choose a random element from this container. Attributes Inherited from: Container

Invocation template:

```scala
receiver.randomElement()
```

## `` com.cra.figaro.library.collection.FixedSizeArray.reduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#reduce-fffff50a)

```scala
def reduce(f: (Value, Value) => Value)(implicit name: Name[Value], collection: ElementCollection): Element[Value]
```

Reduce the values in this container through the given function.

Type parameters: none.

Parameters, list 1: `` f: (Value, Value) => Value ``.

Context parameters, list 2: `` name: Name[Value] ``; `` collection: ElementCollection ``.

Returns: `` Element[Value] ``.

Source contract/attributes: Reduce the values in this container through the given function. Attributes Inherited from: Container

Invocation template:

```scala
receiver.reduce(f)(using name, collection)
```

## `` com.cra.figaro.library.collection.FixedSizeArray.toMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArray.html#toMap-0)

```scala
def toMap: Map[Int, Element[Value]]
```

Convert this container into an ordinary Scala map.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Map[Int, Element[Value]] ``.

Source contract/attributes: Convert this container into an ordinary Scala map. Attributes Inherited from: Container

Invocation template:

```scala
receiver.toMap
```

## `` com.cra.figaro.library.collection.FixedSizeArrayElement.concat ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArrayElement.html#concat-fffff140)

```scala
def concat(that: FixedSizeArrayElement[Value]): FixedSizeArrayElement[Value]
```

Concatenate the value of this fixed size array element with the value of another fixed size array element. This method produces an element whose possible values are all the possible concatenations of this with that.

Type parameters: none.

Parameters, list 1: `` that: FixedSizeArrayElement[Value] ``.

Returns: `` FixedSizeArrayElement[Value] ``.

Source contract/attributes: Concatenate the value of this fixed size array element with the value of another fixed size array element. This method produces an element whose possible values are all the possible concatenations of this with that. Attributes

Invocation template:

```scala
receiver.concat(that)
```

## `` com.cra.figaro.library.collection.FixedSizeArrayElement.aggregate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArrayElement.html#aggregate-fffff132)

```scala
def aggregate[Value2](start: => Value2)(seqop: (Value2, Value) => Value2, combop: (Value2, Value2) => Value2): Element[Value2]
```

Aggregate the results of applying an operator to each element.

Type parameters: `` Value2 ``.

Parameters, list 1: `` start: => Value2 ``.

Parameters, list 2: `` seqop: (Value2, Value) => Value2 ``; `` combop: (Value2, Value2) => Value2 ``.

Returns: `` Element[Value2] ``.

Source contract/attributes: Aggregate the results of applying an operator to each element. Attributes Inherited from: ContainerElement

Invocation template:

```scala
receiver.aggregate[Value2](start)(seqop, combop)
```

## `` com.cra.figaro.library.collection.FixedSizeArrayElement.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArrayElement.html#apply-ade)

```scala
def apply(i: Int): Element[Value]
```

Creates an element whose value is the value at the corresponding index of the value of the process element.

Type parameters: none.

Parameters, list 1: `` i: Int ``.

Returns: `` Element[Value] ``.

Source contract/attributes: Creates an element whose value is the value at the corresponding index of the value of the process element. Attributes Inherited from: ContainerElement

Invocation template:

```scala
receiver.apply(i)
```

## `` com.cra.figaro.library.collection.FixedSizeArrayElement.chain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArrayElement.html#chain-fcd)

```scala
def chain[Value2](f: Value => Element[Value2]): ContainerElement[Int, Value2]
```

Chain the given function pointwise through the value of the container element.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Element[Value2] ``.

Returns: `` ContainerElement[Int, Value2] ``.

Source contract/attributes: Chain the given function pointwise through the value of the container element. Attributes Inherited from: ContainerElement

Invocation template:

```scala
receiver.chain[Value2](f)
```

## `` com.cra.figaro.library.collection.FixedSizeArrayElement.count ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArrayElement.html#count-20)

```scala
def count(f: Value => Boolean): Element[Int]
```

Returns an element representing the number of elements in the container whose values satisfy the predicate.

Type parameters: none.

Parameters, list 1: `` f: Value => Boolean ``.

Returns: `` Element[Int] ``.

Source contract/attributes: Returns an element representing the number of elements in the container whose values satisfy the predicate. Attributes Inherited from: ContainerElement

Invocation template:

```scala
receiver.count(f)
```

## `` com.cra.figaro.library.collection.FixedSizeArrayElement.exists ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArrayElement.html#exists-20)

```scala
def exists(pred: Value => Boolean): Element[Boolean]
```

Returns an element representing whether the value of any element in the container satisfies the predicate.

Type parameters: none.

Parameters, list 1: `` pred: Value => Boolean ``.

Returns: `` Element[Boolean] ``.

Source contract/attributes: Returns an element representing whether the value of any element in the container satisfies the predicate. Attributes Inherited from: ContainerElement

Invocation template:

```scala
receiver.exists(pred)
```

## `` com.cra.figaro.library.collection.FixedSizeArrayElement.foldLeft ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArrayElement.html#foldLeft-c3)

```scala
def foldLeft[Value2](start: Value2)(f: (Value2, Value) => Value2): Element[Value2]
```

Produce the element over values obtained by selecting a particular container and folding through its values.

Type parameters: `` Value2 ``.

Parameters, list 1: `` start: Value2 ``.

Parameters, list 2: `` f: (Value2, Value) => Value2 ``.

Returns: `` Element[Value2] ``.

Source contract/attributes: Produce the element over values obtained by selecting a particular container and folding through its values. Attributes Inherited from: ContainerElement

Invocation template:

```scala
receiver.foldLeft[Value2](start)(f)
```

## `` com.cra.figaro.library.collection.FixedSizeArrayElement.foldRight ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArrayElement.html#foldRight-c3)

```scala
def foldRight[Value2](start: Value2)(f: (Value, Value2) => Value2): Element[Value2]
```

Produce the element over values obtained by selecting a particular container and folding through its values.

Type parameters: `` Value2 ``.

Parameters, list 1: `` start: Value2 ``.

Parameters, list 2: `` f: (Value, Value2) => Value2 ``.

Returns: `` Element[Value2] ``.

Source contract/attributes: Produce the element over values obtained by selecting a particular container and folding through its values. Attributes Inherited from: ContainerElement

Invocation template:

```scala
receiver.foldRight[Value2](start)(f)
```

## `` com.cra.figaro.library.collection.FixedSizeArrayElement.forall ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArrayElement.html#forall-20)

```scala
def forall(pred: Value => Boolean): Element[Boolean]
```

Returns an element representing whether the values of all elements in the container satisfy the predicate.

Type parameters: none.

Parameters, list 1: `` pred: Value => Boolean ``.

Returns: `` Element[Boolean] ``.

Source contract/attributes: Returns an element representing whether the values of all elements in the container satisfy the predicate. Attributes Inherited from: ContainerElement

Invocation template:

```scala
receiver.forall(pred)
```

## `` com.cra.figaro.library.collection.FixedSizeArrayElement.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArrayElement.html#get-ade)

```scala
def get(i: Int): Element[Option[Value]]
```

Creates an element whose value is the value at the corresponding index in the value of the container element, if the index is in range, None otherwise.

Type parameters: none.

Parameters, list 1: `` i: Int ``.

Returns: `` Element[Option[Value]] ``.

Source contract/attributes: Creates an element whose value is the value at the corresponding index in the value of the container element, if the index is in range, None otherwise. Attributes Inherited from: ContainerElement

Invocation template:

```scala
receiver.get(i)
```

## `` com.cra.figaro.library.collection.FixedSizeArrayElement.length ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArrayElement.html#length-0)

```scala
def length: Element[Int]
```

Returns an element representing the length of the container.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Element[Int] ``.

Source contract/attributes: Returns an element representing the length of the container. Attributes Inherited from: ContainerElement

Invocation template:

```scala
receiver.length
```

## `` com.cra.figaro.library.collection.FixedSizeArrayElement.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArrayElement.html#map-fcd)

```scala
def map[Value2](f: Value => Value2): ContainerElement[Int, Value2]
```

Map the given function pointwise through the value of the container element.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Value2 ``.

Returns: `` ContainerElement[Int, Value2] ``.

Source contract/attributes: Map the given function pointwise through the value of the container element. Attributes Inherited from: ContainerElement

Invocation template:

```scala
receiver.map[Value2](f)
```

## `` com.cra.figaro.library.collection.FixedSizeArrayElement.randomElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArrayElement.html#randomElement-6d1)

```scala
def randomElement(): Element[Value]
```

Select a random element from the container. Ensures that no IndexOutOfRange exception should be thrown.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Element[Value] ``.

Source contract/attributes: Select a random element from the container. Ensures that no IndexOutOfRange exception should be thrown. Attributes Inherited from: ContainerElement

Invocation template:

```scala
receiver.randomElement()
```

## `` com.cra.figaro.library.collection.FixedSizeArrayElement.reduce ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/FixedSizeArrayElement.html#reduce-ffffffff)

```scala
def reduce(f: (Value, Value) => Value): Element[Value]
```

Produce the element over values obtained by selecting a particular container and reducing through its values.

Type parameters: none.

Parameters, list 1: `` f: (Value, Value) => Value ``.

Returns: `` Element[Value] ``.

Source contract/attributes: Produce the element over values obtained by selecting a particular container and reducing through its values. Attributes Inherited from: ContainerElement

Invocation template:

```scala
receiver.reduce(f)
```

## `` com.cra.figaro.library.collection.IncrementalProcess.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IncrementalProcess.html#++-fffffca0)

```scala
def ++(that: Process[Index, Value]): Process[Index, Value]
```

Returns a new process containing the elements of this process and the argument. If an index is defined in both processes, the element of the argument is used.

Type parameters: none.

Parameters, list 1: `` that: Process[Index, Value] ``.

Returns: `` Process[Index, Value] ``.

Source contract/attributes: Returns a new process containing the elements of this process and the argument. If an index is defined in both processes, the element of the argument is used. Attributes Inherited from: Process

Invocation template:

```scala
receiver.++(that)
```

## `` com.cra.figaro.library.collection.IncrementalProcess.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IncrementalProcess.html#apply-ee2)

```scala
def apply(indices: Iterable[Index]): Map[Index, Element[Value]]
```

Get the elements representing the value of the process at the given indices. Throws IndexOutOfRangeException if any index has no value.

Type parameters: none.

Parameters, list 1: `` indices: Iterable[Index] ``.

Returns: `` Map[Index, Element[Value]] ``.

Source contract/attributes: Get the elements representing the value of the process at the given indices. Throws IndexOutOfRangeException if any index has no value. Attributes Inherited from: Process

Invocation template:

```scala
receiver.apply(indices)
```

## `` com.cra.figaro.library.collection.IncrementalProcess.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IncrementalProcess.html#apply-ade)

```scala
def apply(index: Index): Element[Value]
```

Get an element representing the value of the process at the given index. Throws IndexOutOfRangeException if the index has no value.

Type parameters: none.

Parameters, list 1: `` index: Index ``.

Returns: `` Element[Value] ``.

Source contract/attributes: Get an element representing the value of the process at the given index. Throws IndexOutOfRangeException if the index has no value. This apply method is cached, so calling process(index) always returns the same element. Attributes Inherited from: Process

Invocation template:

```scala
receiver.apply(index)
```

## `` com.cra.figaro.library.collection.IncrementalProcess.chain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IncrementalProcess.html#chain-99d)

```scala
def chain[Value2](f: Value => Element[Value2]): Process[Index, Value2]
```

Chain every value in this process through the given function, returning a new process.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Element[Value2] ``.

Returns: `` Process[Index, Value2] ``.

Source contract/attributes: Chain every value in this process through the given function, returning a new process. Attributes Inherited from: Process

Invocation template:

```scala
receiver.chain[Value2](f)
```

## `` com.cra.figaro.library.collection.IncrementalProcess.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IncrementalProcess.html#flatMap-99d)

```scala
def flatMap[Value2](f: Value => Element[Value2]): Process[Index, Value2]
```

Chain every value in this process through the given function, returning a new process.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Element[Value2] ``.

Returns: `` Process[Index, Value2] ``.

Source contract/attributes: Chain every value in this process through the given function, returning a new process. Attributes Inherited from: Process

Invocation template:

```scala
receiver.flatMap[Value2](f)
```

## `` com.cra.figaro.library.collection.IncrementalProcess.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IncrementalProcess.html#get-ee2)

```scala
def get(indices: Iterable[Index]): Map[Index, Element[Option[Value]]]
```

Safely get the elements over optional values at all of the indices. Any index that is not in range will always have value None. Dependencies between elements for indices in range will be produced.

Type parameters: none.

Parameters, list 1: `` indices: Iterable[Index] ``.

Returns: `` Map[Index, Element[Option[Value]]] ``.

Source contract/attributes: Safely get the elements over optional values at all of the indices. Any index that is not in range will always have value None. Dependencies between elements for indices in range will be produced. Attributes Inherited from: Process

Invocation template:

```scala
receiver.get(indices)
```

## `` com.cra.figaro.library.collection.IncrementalProcess.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IncrementalProcess.html#get-ade)

```scala
def get(index: Index): Element[Option[Value]]
```

Safely get an element over an optional value at the index. If the index is in range, the value of the element will be Some(something). If the index is out of range, the value of the element will be None.

Type parameters: none.

Parameters, list 1: `` index: Index ``.

Returns: `` Element[Option[Value]] ``.

Source contract/attributes: Safely get an element over an optional value at the index. If the index is in range, the value of the element will be Some(something). If the index is out of range, the value of the element will be None. Attributes Inherited from: Process

Invocation template:

```scala
receiver.get(index)
```

## `` com.cra.figaro.library.collection.IncrementalProcess.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IncrementalProcess.html#map-99d)

```scala
def map[Value2](f: Value => Value2): Process[Index, Value2]
```

Apply the given function to every value in this process, returning a new process.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Value2 ``.

Returns: `` Process[Index, Value2] ``.

Source contract/attributes: Apply the given function to every value in this process, returning a new process. Attributes Inherited from: Process

Invocation template:

```scala
receiver.map[Value2](f)
```

## `` com.cra.figaro.library.collection.IncrementalProcess.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IncrementalProcess.html#generate-ade)

```scala
def generate(index: Index): Element[Value]
```

Produce the element corresponding to the value of the process at the given index. This method can assume that the indices has already been range checked.

Type parameters: none.

Parameters, list 1: `` index: Index ``.

Returns: `` Element[Value] ``.

Source contract/attributes: Produce the element corresponding to the value of the process at the given index. This method can assume that the indices has already been range checked. Attributes Inherited from: Process

Invocation template:

```scala
receiver.generate(index)
```

## `` com.cra.figaro.library.collection.IncrementalProcess.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IncrementalProcess.html#generate-3fa)

```scala
def generate(indices: List[Index]): Map[Index, Element[Value]]
```

Produce the elements representing the value of the process at the given indices. Ensures that any dependencies between the elements are represented. This method must be implemented by implementations of Process. The return value maps each provided index to the corresponding element. This method can assume that the indices has already been range checked.

Type parameters: none.

Parameters, list 1: `` indices: List[Index] ``.

Returns: `` Map[Index, Element[Value]] ``.

Source contract/attributes: Produce the elements representing the value of the process at the given indices. Ensures that any dependencies between the elements are represented. This method must be implemented by implementations of Process. The return value maps each provided index to the corresponding element. This method can assume that the indices has already been range checked. Attributes Inherited from: Process

Invocation template:

```scala
receiver.generate(indices)
```

## `` com.cra.figaro.library.collection.IncrementalProcess.rangeCheck ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IncrementalProcess.html#rangeCheck-4ad)

```scala
def rangeCheck(index: Index): Boolean
```

Check whether the given index has an element.

Type parameters: none.

Parameters, list 1: `` index: Index ``.

Returns: `` Boolean ``.

Source contract/attributes: Check whether the given index has an element. Attributes Inherited from: Process

Invocation template:

```scala
receiver.rangeCheck(index)
```

## `` com.cra.figaro.library.collection.IndependentProcess.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IndependentProcess.html#generate-ade)

```scala
def generate(index: Index): Element[Value]
```

Produce the element corresponding to the value of the process at the given index. This method can assume that the indices has already been range checked.

Type parameters: none.

Parameters, list 1: `` index: Index ``.

Returns: `` Element[Value] ``.

Source contract/attributes: Produce the element corresponding to the value of the process at the given index. This method can assume that the indices has already been range checked. Attributes

Invocation template:

```scala
receiver.generate(index)
```

## `` com.cra.figaro.library.collection.IndependentProcess.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IndependentProcess.html#generate-3fa)

```scala
def generate(indices: List[Index]): Map[Index, Element[Value]]
```

Produce the elements representing the value of the process at the given indices. Ensures that any dependencies between the elements are represented. This method must be implemented by implementations of Process. The return value maps each provided index to the corresponding element. This method can assume that the indices has already been range checked.

Type parameters: none.

Parameters, list 1: `` indices: List[Index] ``.

Returns: `` Map[Index, Element[Value]] ``.

Source contract/attributes: Produce the elements representing the value of the process at the given indices. Ensures that any dependencies between the elements are represented. This method must be implemented by implementations of Process. The return value maps each provided index to the corresponding element. This method can assume that the indices has already been range checked. Attributes

Invocation template:

```scala
receiver.generate(indices)
```

## `` com.cra.figaro.library.collection.IndependentProcess.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IndependentProcess.html#++-fffffca0)

```scala
def ++(that: Process[Index, Value]): Process[Index, Value]
```

Returns a new process containing the elements of this process and the argument. If an index is defined in both processes, the element of the argument is used.

Type parameters: none.

Parameters, list 1: `` that: Process[Index, Value] ``.

Returns: `` Process[Index, Value] ``.

Source contract/attributes: Returns a new process containing the elements of this process and the argument. If an index is defined in both processes, the element of the argument is used. Attributes Inherited from: Process

Invocation template:

```scala
receiver.++(that)
```

## `` com.cra.figaro.library.collection.IndependentProcess.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IndependentProcess.html#apply-ee2)

```scala
def apply(indices: Iterable[Index]): Map[Index, Element[Value]]
```

Get the elements representing the value of the process at the given indices. Throws IndexOutOfRangeException if any index has no value.

Type parameters: none.

Parameters, list 1: `` indices: Iterable[Index] ``.

Returns: `` Map[Index, Element[Value]] ``.

Source contract/attributes: Get the elements representing the value of the process at the given indices. Throws IndexOutOfRangeException if any index has no value. Attributes Inherited from: Process

Invocation template:

```scala
receiver.apply(indices)
```

## `` com.cra.figaro.library.collection.IndependentProcess.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IndependentProcess.html#apply-ade)

```scala
def apply(index: Index): Element[Value]
```

Get an element representing the value of the process at the given index. Throws IndexOutOfRangeException if the index has no value.

Type parameters: none.

Parameters, list 1: `` index: Index ``.

Returns: `` Element[Value] ``.

Source contract/attributes: Get an element representing the value of the process at the given index. Throws IndexOutOfRangeException if the index has no value. This apply method is cached, so calling process(index) always returns the same element. Attributes Inherited from: Process

Invocation template:

```scala
receiver.apply(index)
```

## `` com.cra.figaro.library.collection.IndependentProcess.chain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IndependentProcess.html#chain-99d)

```scala
def chain[Value2](f: Value => Element[Value2]): Process[Index, Value2]
```

Chain every value in this process through the given function, returning a new process.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Element[Value2] ``.

Returns: `` Process[Index, Value2] ``.

Source contract/attributes: Chain every value in this process through the given function, returning a new process. Attributes Inherited from: Process

Invocation template:

```scala
receiver.chain[Value2](f)
```

## `` com.cra.figaro.library.collection.IndependentProcess.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IndependentProcess.html#flatMap-99d)

```scala
def flatMap[Value2](f: Value => Element[Value2]): Process[Index, Value2]
```

Chain every value in this process through the given function, returning a new process.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Element[Value2] ``.

Returns: `` Process[Index, Value2] ``.

Source contract/attributes: Chain every value in this process through the given function, returning a new process. Attributes Inherited from: Process

Invocation template:

```scala
receiver.flatMap[Value2](f)
```

## `` com.cra.figaro.library.collection.IndependentProcess.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IndependentProcess.html#get-ee2)

```scala
def get(indices: Iterable[Index]): Map[Index, Element[Option[Value]]]
```

Safely get the elements over optional values at all of the indices. Any index that is not in range will always have value None. Dependencies between elements for indices in range will be produced.

Type parameters: none.

Parameters, list 1: `` indices: Iterable[Index] ``.

Returns: `` Map[Index, Element[Option[Value]]] ``.

Source contract/attributes: Safely get the elements over optional values at all of the indices. Any index that is not in range will always have value None. Dependencies between elements for indices in range will be produced. Attributes Inherited from: Process

Invocation template:

```scala
receiver.get(indices)
```

## `` com.cra.figaro.library.collection.IndependentProcess.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IndependentProcess.html#get-ade)

```scala
def get(index: Index): Element[Option[Value]]
```

Safely get an element over an optional value at the index. If the index is in range, the value of the element will be Some(something). If the index is out of range, the value of the element will be None.

Type parameters: none.

Parameters, list 1: `` index: Index ``.

Returns: `` Element[Option[Value]] ``.

Source contract/attributes: Safely get an element over an optional value at the index. If the index is in range, the value of the element will be Some(something). If the index is out of range, the value of the element will be None. Attributes Inherited from: Process

Invocation template:

```scala
receiver.get(index)
```

## `` com.cra.figaro.library.collection.IndependentProcess.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IndependentProcess.html#map-99d)

```scala
def map[Value2](f: Value => Value2): Process[Index, Value2]
```

Apply the given function to every value in this process, returning a new process.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Value2 ``.

Returns: `` Process[Index, Value2] ``.

Source contract/attributes: Apply the given function to every value in this process, returning a new process. Attributes Inherited from: Process

Invocation template:

```scala
receiver.map[Value2](f)
```

## `` com.cra.figaro.library.collection.IndependentProcess.rangeCheck ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/IndependentProcess.html#rangeCheck-4ad)

```scala
def rangeCheck(index: Index): Boolean
```

Check whether the given index has an element.

Type parameters: none.

Parameters, list 1: `` index: Index ``.

Returns: `` Boolean ``.

Source contract/attributes: Check whether the given index has an element. Attributes Inherited from: Process

Invocation template:

```scala
receiver.rangeCheck(index)
```

## `` com.cra.figaro.library.collection.MakeArray.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#apply-fffff2ca)

```scala
def apply(i: Int): Element[T]
```

Return the i-th item in the list. Throws IllegalArgumentException if i is greater than the current value of numItems

Type parameters: none.

Parameters, list 1: `` i: Int ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return the i-th item in the list. Throws IllegalArgumentException if i is greater than the current value of numItems Attributes

Invocation template:

```scala
receiver.apply(i)
```

## `` com.cra.figaro.library.collection.MakeArray.args ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#args-0)

```scala
override def args: List[Element[_]]
```

The arguments on which the element depends.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: The arguments on which the element depends. Attributes Definition Classes Element

Invocation template:

```scala
receiver.args
```

## `` com.cra.figaro.library.collection.MakeArray.generateValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#generateValue-e96)

```scala
override def generateValue(): FixedSizeArray[T]
```

Generate a value with no randomness.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` FixedSizeArray[T] ``.

Source contract/attributes: Generate a value with no randomness. Attributes Definition Classes Deterministic

Invocation template:

```scala
receiver.generateValue()
```

## `` com.cra.figaro.library.collection.MakeArray.makeValues ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#makeValues-6da)

```scala
def makeValues(depth: Int): ValueSet[FixedSizeArray[T]]
```

Returns the range of values of this element.

Type parameters: none.

Parameters, list 1: `` depth: Int ``.

Returns: `` ValueSet[FixedSizeArray[T]] ``.

Source contract/attributes: Returns the range of values of this element. Value parameters depth The depth of the range computation Attributes

Invocation template:

```scala
receiver.makeValues(depth)
```

## `` com.cra.figaro.library.collection.MakeArray.values ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#values-0)

```scala
def values: LazyValues
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` LazyValues ``.

Invocation template:

```scala
receiver.values
```

## `` com.cra.figaro.library.collection.MakeArray.!== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#!==-894)

```scala
def !==(that: Element[Value])(implicit universe: Universe): Neq[FixedSizeArray[T]]
```

The element that tests inequality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Neq[FixedSizeArray[T]] ``.

Source contract/attributes: The element that tests inequality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.!==(that)(using universe)
```

## `` com.cra.figaro.library.collection.MakeArray.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#===-f71)

```scala
def ===(that: Value)(implicit universe: Universe): Apply1[FixedSizeArray[T], Boolean]
```

The element that tests whether the value of this element is equal to a particular value.

Type parameters: none.

Parameters, list 1: `` that: Value ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Apply1[FixedSizeArray[T], Boolean] ``.

Source contract/attributes: The element that tests whether the value of this element is equal to a particular value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.library.collection.MakeArray.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#===-672)

```scala
def ===(that: Element[Value])(implicit universe: Universe): Eq[FixedSizeArray[T]]
```

The element that tests equality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Eq[FixedSizeArray[T]] ``.

Source contract/attributes: The element that tests equality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.library.collection.MakeArray.activate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#activate-94c)

```scala
def activate(): Unit
```

Activate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Activate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.activate()
```

## `` com.cra.figaro.library.collection.MakeArray.addCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#addCondition-fffff9ad)

```scala
def addCondition(condition: Condition, contingency: Contingency = ...): Unit
```

Add the given condition to the existing conditions of the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` condition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add the given condition to the existing conditions of the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addCondition(condition, contingency)
```

## `` com.cra.figaro.library.collection.MakeArray.addConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#addConstraint-fffff9ad)

```scala
def addConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addConstraint(constraint, contingency)
```

## `` com.cra.figaro.library.collection.MakeArray.addLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#addLogConstraint-fffff9ad)

```scala
def addLogConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a log contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a log contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addLogConstraint(constraint, contingency)
```

## `` com.cra.figaro.library.collection.MakeArray.addPragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#addPragma-b53)

```scala
def addPragma(pragma: Pragma[Value]): Unit
```

Add a pragma to the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Add a pragma to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addPragma(pragma)
```

## `` com.cra.figaro.library.collection.MakeArray.allConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#allConditions-0)

```scala
def allConditions: List[(Condition, Contingency)]
```

All the conditions defined on this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Condition, Contingency)] ``.

Source contract/attributes: All the conditions defined on this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConditions
```

## `` com.cra.figaro.library.collection.MakeArray.allConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#allConstraints-0)

```scala
def allConstraints: List[(Constraint, Contingency)]
```

The current soft constraints on the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Constraint, Contingency)] ``.

Source contract/attributes: The current soft constraints on the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConstraints
```

## `` com.cra.figaro.library.collection.MakeArray.clearContext ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#clearContext-94c)

```scala
def clearContext(): Unit
```

Clears all the temporary elements associated with this element (all elements created in it's context).

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clears all the temporary elements associated with this element (all elements created in it's context). Attributes Inherited from: Element

Invocation template:

```scala
receiver.clearContext()
```

## `` com.cra.figaro.library.collection.MakeArray.condition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#condition-4ad)

```scala
def condition(value: Any): Boolean
```

Tests whether all the element's contingent conditions are satisfied for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether all the element's contingent conditions are satisfied for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.condition(value)
```

## `` com.cra.figaro.library.collection.MakeArray.conditionSatisfied ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#conditionSatisfied-0)

```scala
def conditionSatisfied: Boolean
```

Determines whether the condition on the element is satisfied by the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Determines whether the condition on the element is satisfied by the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.conditionSatisfied
```

## `` com.cra.figaro.library.collection.MakeArray.constraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#constraint-fffffeac)

```scala
def constraint(value: Any): Double
```

Gets the result of all the element's contingent constraints for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Double ``.

Source contract/attributes: Gets the result of all the element's contingent constraints for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraint(value)
```

## `` com.cra.figaro.library.collection.MakeArray.constraintValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#constraintValue-0)

```scala
def constraintValue: Double
```

Determines the value of the constraint on the element applied to the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Determines the value of the constraint on the element applied to the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraintValue
```

## `` com.cra.figaro.library.collection.MakeArray.context ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#context-0)

```scala
def context: List[Element[_]]
```

The elements on which the existence of this element depends.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: The elements on which the existence of this element depends. Attributes Inherited from: Element

Invocation template:

```scala
receiver.context
```

## `` com.cra.figaro.library.collection.MakeArray.deactivate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#deactivate-94c)

```scala
def deactivate(): Unit
```

Deactivate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Deactivate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.deactivate()
```

## `` com.cra.figaro.library.collection.MakeArray.directContextContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#directContextContents-0)

```scala
def directContextContents: Set[Element[_]]
```

Returns the set of elements directly created in the context of this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the set of elements directly created in the context of this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.directContextContents
```

## `` com.cra.figaro.library.collection.MakeArray.elementsIAmContingentOn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#elementsIAmContingentOn-0)

```scala
def elementsIAmContingentOn: Set[Element[_]]
```

Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value. Figaro takes care of handling all this under the hood. However, some algorithms may need to know which elements an element is contingent on. For example, sampling algorithms may need to sample those other elements first. This method is supplied to support this use case. Attributes Inherited from: Element

Invocation template:

```scala
receiver.elementsIAmContingentOn
```

## `` com.cra.figaro.library.collection.MakeArray.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#flatMap-db8)

```scala
def flatMap[U](fn: Value => Element[U])(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => Element[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.flatMap[U](fn)(using name, collection)
```

## `` com.cra.figaro.library.collection.MakeArray.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#generate-94c)

```scala
final def generate(): Unit
```

First generate the randomness, then generate the value given the randomness. Store the results in randomness and value.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: First generate the randomness, then generate the value given the randomness. Store the results in randomness and value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.generate()
```

## `` com.cra.figaro.library.collection.MakeArray.generateRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#generateRandomness-54f)

```scala
def generateRandomness(): Randomness
```

Generate the randomness content.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Randomness ``.

Source contract/attributes: Generate the randomness content. Attributes Inherited from: Deterministic

Invocation template:

```scala
receiver.generateRandomness()
```

## `` com.cra.figaro.library.collection.MakeArray.generateValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#generateValue-fffff0e2)

```scala
def generateValue(rand: Randomness): FixedSizeArray[T]
```

Generate the value of the element deterministically given its randomness and the values of its arguments.

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` FixedSizeArray[T] ``.

Source contract/attributes: Generate the value of the element deterministically given its randomness and the values of its arguments. Attributes Inherited from: Deterministic

Invocation template:

```scala
receiver.generateValue(rand)
```

## `` com.cra.figaro.library.collection.MakeArray.intervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#intervene-fffff71f)

```scala
def intervene(v: Value): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` v: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.intervene(v)
```

## `` com.cra.figaro.library.collection.MakeArray.isCachable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#isCachable-0)

```scala
def isCachable: Boolean
```

The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isCachable
```

## `` com.cra.figaro.library.collection.MakeArray.isTemporary ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#isTemporary-0)

```scala
def isTemporary: Boolean
```

Returns true if this element is temporary, that is, was created in the context of another element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Returns true if this element is temporary, that is, was created in the context of another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isTemporary
```

## `` com.cra.figaro.library.collection.MakeArray.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#map-db8)

```scala
def map[U](fn: Value => U)(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => U ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.map[U](fn)(using name, collection)
```

## `` com.cra.figaro.library.collection.MakeArray.nextRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#nextRandomness-fffff446)

```scala
def nextRandomness(rand: Randomness): (Randomness, Double, Double)
```

Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is:

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` (Randomness, Double, Double) ``.

Source contract/attributes: Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is: P(new -> old) / P(old -> new) and the model probability ratio, which is: P(new) / P(old) The default implementation is to use generateRandomness and returns ones for the proposal and model probability ratios. Attributes Inherited from: Element

Invocation template:

```scala
receiver.nextRandomness(rand)
```

## `` com.cra.figaro.library.collection.MakeArray.observe ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#observe-fffff71f)

```scala
def observe(observation: Value): Unit
```

Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated.

Type parameters: none.

Parameters, list 1: `` observation: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.observe(observation)
```

## `` com.cra.figaro.library.collection.MakeArray.pragmas ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#pragmas-0)

```scala
def pragmas: List[Pragma[Value]]
```

The pragmas attached to the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Pragma[Value]] ``.

Source contract/attributes: The pragmas attached to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.pragmas
```

## `` com.cra.figaro.library.collection.MakeArray.removeConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#removeConditions-71e)

```scala
def removeConditions(contingency: Contingency = ...): Unit
```

Remove all conditions associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all conditions associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConditions(contingency)
```

## `` com.cra.figaro.library.collection.MakeArray.removeConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#removeConstraints-71e)

```scala
def removeConstraints(contingency: Contingency = ...): Unit
```

Remove all constraints associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all constraints associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConstraints(contingency)
```

## `` com.cra.figaro.library.collection.MakeArray.removePragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#removePragma-b53)

```scala
def removePragma(pragma: Pragma[Value]): Unit
```

Remove a pragma from the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Remove a pragma from the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removePragma(pragma)
```

## `` com.cra.figaro.library.collection.MakeArray.score ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#score-fffffdbf)

```scala
def score(oldValue: Value, newValue: Value): Double
```

Compute the constraints on the new value divided by the constraints on the old value.

Type parameters: none.

Parameters, list 1: `` oldValue: Value ``; `` newValue: Value ``.

Returns: `` Double ``.

Source contract/attributes: Compute the constraints on the new value divided by the constraints on the old value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.score(oldValue, newValue)
```

## `` com.cra.figaro.library.collection.MakeArray.set ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#set-fffff71f)

```scala
def set(newValue: Value): Unit
```

Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called.

Type parameters: none.

Parameters, list 1: `` newValue: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called. Attributes Inherited from: Element

Invocation template:

```scala
receiver.set(newValue)
```

## `` com.cra.figaro.library.collection.MakeArray.setCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#setCondition-fffff9ad)

```scala
def setCondition(newCondition: Condition, contingency: Contingency = ...): Unit
```

Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newCondition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setCondition(newCondition, contingency)
```

## `` com.cra.figaro.library.collection.MakeArray.setConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#setConstraint-fffff9ad)

```scala
def setConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.library.collection.MakeArray.setLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#setLogConstraint-fffff9ad)

```scala
def setLogConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setLogConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.library.collection.MakeArray.setRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#setRandomness-fffff71f)

```scala
def setRandomness(newRandomness: Randomness): Unit
```

Set the randomness of this element.

Type parameters: none.

Parameters, list 1: `` newRandomness: Randomness ``.

Returns: `` Unit ``.

Source contract/attributes: Set the randomness of this element. Will generate its value using the new randomness and propagate the effects to elements that depend on it without changing their randomness. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setRandomness(newRandomness)
```

## `` com.cra.figaro.library.collection.MakeArray.toNameString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#toNameString-0)

```scala
def toNameString: String
```

A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString. Attributes Inherited from: Element

Invocation template:

```scala
receiver.toNameString
```

## `` com.cra.figaro.library.collection.MakeArray.unintervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#unintervene-94c)

```scala
def unintervene(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.unintervene()
```

## `` com.cra.figaro.library.collection.MakeArray.unobserve ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#unobserve-94c)

```scala
def unobserve(): Unit
```

Removes conditions on the element and allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Removes conditions on the element and allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unobserve()
```

## `` com.cra.figaro.library.collection.MakeArray.unset ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/MakeArray.html#unset-94c)

```scala
def unset(): Unit
```

Allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unset()
```

## `` com.cra.figaro.library.collection.Process.IndexOutOfRangeException.addSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process$IndexOutOfRangeException.html#addSuppressed-4b8)

```scala
final def addSuppressed(x$0: Throwable): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.addSuppressed(x$0)
```

## `` com.cra.figaro.library.collection.Process.IndexOutOfRangeException.fillInStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process$IndexOutOfRangeException.html#fillInStackTrace-26c)

```scala
def fillInStackTrace(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.fillInStackTrace()
```

## `` com.cra.figaro.library.collection.Process.IndexOutOfRangeException.getCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process$IndexOutOfRangeException.html#getCause-26c)

```scala
def getCause(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getCause()
```

## `` com.cra.figaro.library.collection.Process.IndexOutOfRangeException.getLocalizedMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process$IndexOutOfRangeException.html#getLocalizedMessage-665)

```scala
def getLocalizedMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getLocalizedMessage()
```

## `` com.cra.figaro.library.collection.Process.IndexOutOfRangeException.getMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process$IndexOutOfRangeException.html#getMessage-665)

```scala
def getMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getMessage()
```

## `` com.cra.figaro.library.collection.Process.IndexOutOfRangeException.getStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process$IndexOutOfRangeException.html#getStackTrace-fffffa6d)

```scala
def getStackTrace(): Array[StackTraceElement]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[StackTraceElement] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getStackTrace()
```

## `` com.cra.figaro.library.collection.Process.IndexOutOfRangeException.getSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process$IndexOutOfRangeException.html#getSuppressed-fffff2ce)

```scala
final def getSuppressed(): Array[Throwable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[Throwable] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getSuppressed()
```

## `` com.cra.figaro.library.collection.Process.IndexOutOfRangeException.initCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process$IndexOutOfRangeException.html#initCause-fffff080)

```scala
def initCause(x$0: Throwable): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.initCause(x$0)
```

## `` com.cra.figaro.library.collection.Process.IndexOutOfRangeException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process$IndexOutOfRangeException.html#printStackTrace-fffff230)

```scala
def printStackTrace(x$0: PrintWriter): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintWriter ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.library.collection.Process.IndexOutOfRangeException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process$IndexOutOfRangeException.html#printStackTrace-fffff0fd)

```scala
def printStackTrace(x$0: PrintStream): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintStream ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.library.collection.Process.IndexOutOfRangeException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process$IndexOutOfRangeException.html#printStackTrace-94c)

```scala
def printStackTrace(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace()
```

## `` com.cra.figaro.library.collection.Process.IndexOutOfRangeException.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process$IndexOutOfRangeException.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.library.collection.Process.IndexOutOfRangeException.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process$IndexOutOfRangeException.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.library.collection.Process.IndexOutOfRangeException.setStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process$IndexOutOfRangeException.html#setStackTrace-f79)

```scala
def setStackTrace(x$0: Array[StackTraceElement]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Array[StackTraceElement] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.setStackTrace(x$0)
```

## `` com.cra.figaro.library.collection.Process.IndexOutOfRangeException.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process$IndexOutOfRangeException.html#toString-665)

```scala
def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Inherited from: Throwable

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.library.collection.Process.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process.html#generate-3fa)

```scala
def generate(indices: List[Index]): Map[Index, Element[Value]]
```

Produce the elements representing the value of the process at the given indices. Ensures that any dependencies between the elements are represented. This method must be implemented by implementations of Process. The return value maps each provided index to the corresponding element. This method can assume that the indices has already been range checked.

Type parameters: none.

Parameters, list 1: `` indices: List[Index] ``.

Returns: `` Map[Index, Element[Value]] ``.

Source contract/attributes: Produce the elements representing the value of the process at the given indices. Ensures that any dependencies between the elements are represented. This method must be implemented by implementations of Process. The return value maps each provided index to the corresponding element. This method can assume that the indices has already been range checked. Attributes

Invocation template:

```scala
receiver.generate(indices)
```

## `` com.cra.figaro.library.collection.Process.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process.html#generate-ade)

```scala
def generate(index: Index): Element[Value]
```

Produce the element corresponding to the value of the process at the given index. This method can assume that the indices has already been range checked.

Type parameters: none.

Parameters, list 1: `` index: Index ``.

Returns: `` Element[Value] ``.

Source contract/attributes: Produce the element corresponding to the value of the process at the given index. This method can assume that the indices has already been range checked. Attributes

Invocation template:

```scala
receiver.generate(index)
```

## `` com.cra.figaro.library.collection.Process.rangeCheck ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process.html#rangeCheck-4ad)

```scala
def rangeCheck(index: Index): Boolean
```

Check whether the given index has an element.

Type parameters: none.

Parameters, list 1: `` index: Index ``.

Returns: `` Boolean ``.

Source contract/attributes: Check whether the given index has an element. Attributes

Invocation template:

```scala
receiver.rangeCheck(index)
```

## `` com.cra.figaro.library.collection.Process.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process.html#++-fffffca0)

```scala
def ++(that: Process[Index, Value]): Process[Index, Value]
```

Returns a new process containing the elements of this process and the argument. If an index is defined in both processes, the element of the argument is used.

Type parameters: none.

Parameters, list 1: `` that: Process[Index, Value] ``.

Returns: `` Process[Index, Value] ``.

Source contract/attributes: Returns a new process containing the elements of this process and the argument. If an index is defined in both processes, the element of the argument is used. Attributes

Invocation template:

```scala
receiver.++(that)
```

## `` com.cra.figaro.library.collection.Process.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process.html#apply-ade)

```scala
def apply(index: Index): Element[Value]
```

Get an element representing the value of the process at the given index. Throws IndexOutOfRangeException if the index has no value.

Type parameters: none.

Parameters, list 1: `` index: Index ``.

Returns: `` Element[Value] ``.

Source contract/attributes: Get an element representing the value of the process at the given index. Throws IndexOutOfRangeException if the index has no value. This apply method is cached, so calling process(index) always returns the same element. Attributes

Invocation template:

```scala
receiver.apply(index)
```

## `` com.cra.figaro.library.collection.Process.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process.html#apply-ee2)

```scala
def apply(indices: Iterable[Index]): Map[Index, Element[Value]]
```

Get the elements representing the value of the process at the given indices. Throws IndexOutOfRangeException if any index has no value.

Type parameters: none.

Parameters, list 1: `` indices: Iterable[Index] ``.

Returns: `` Map[Index, Element[Value]] ``.

Source contract/attributes: Get the elements representing the value of the process at the given indices. Throws IndexOutOfRangeException if any index has no value. Attributes

Invocation template:

```scala
receiver.apply(indices)
```

## `` com.cra.figaro.library.collection.Process.chain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process.html#chain-99d)

```scala
def chain[Value2](f: Value => Element[Value2]): Process[Index, Value2]
```

Chain every value in this process through the given function, returning a new process.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Element[Value2] ``.

Returns: `` Process[Index, Value2] ``.

Source contract/attributes: Chain every value in this process through the given function, returning a new process. Attributes

Invocation template:

```scala
receiver.chain[Value2](f)
```

## `` com.cra.figaro.library.collection.Process.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process.html#flatMap-99d)

```scala
def flatMap[Value2](f: Value => Element[Value2]): Process[Index, Value2]
```

Chain every value in this process through the given function, returning a new process.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Element[Value2] ``.

Returns: `` Process[Index, Value2] ``.

Source contract/attributes: Chain every value in this process through the given function, returning a new process. Attributes

Invocation template:

```scala
receiver.flatMap[Value2](f)
```

## `` com.cra.figaro.library.collection.Process.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process.html#get-ade)

```scala
def get(index: Index): Element[Option[Value]]
```

Safely get an element over an optional value at the index. If the index is in range, the value of the element will be Some(something). If the index is out of range, the value of the element will be None.

Type parameters: none.

Parameters, list 1: `` index: Index ``.

Returns: `` Element[Option[Value]] ``.

Source contract/attributes: Safely get an element over an optional value at the index. If the index is in range, the value of the element will be Some(something). If the index is out of range, the value of the element will be None. Attributes

Invocation template:

```scala
receiver.get(index)
```

## `` com.cra.figaro.library.collection.Process.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process.html#get-ee2)

```scala
def get(indices: Iterable[Index]): Map[Index, Element[Option[Value]]]
```

Safely get the elements over optional values at all of the indices. Any index that is not in range will always have value None. Dependencies between elements for indices in range will be produced.

Type parameters: none.

Parameters, list 1: `` indices: Iterable[Index] ``.

Returns: `` Map[Index, Element[Option[Value]]] ``.

Source contract/attributes: Safely get the elements over optional values at all of the indices. Any index that is not in range will always have value None. Dependencies between elements for indices in range will be produced. Attributes

Invocation template:

```scala
receiver.get(indices)
```

## `` com.cra.figaro.library.collection.Process.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/Process.html#map-99d)

```scala
def map[Value2](f: Value => Value2): Process[Index, Value2]
```

Apply the given function to every value in this process, returning a new process.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Value2 ``.

Returns: `` Process[Index, Value2] ``.

Source contract/attributes: Apply the given function to every value in this process, returning a new process. Attributes

Invocation template:

```scala
receiver.map[Value2](f)
```

## `` com.cra.figaro.library.collection.ProcessElement.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/ProcessElement.html#apply-ade)

```scala
def apply(i: Index): Element[Value]
```

Creates an element whose value is the value at the corresponding index of the value of the process element.

Type parameters: none.

Parameters, list 1: `` i: Index ``.

Returns: `` Element[Value] ``.

Source contract/attributes: Creates an element whose value is the value at the corresponding index of the value of the process element. Attributes

Invocation template:

```scala
receiver.apply(i)
```

## `` com.cra.figaro.library.collection.ProcessElement.chain ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/ProcessElement.html#chain-fffff29f)

```scala
def chain[Value2](f: Value => Element[Value2]): ProcessElement[Index, Value2]
```

Chain the given function pointwise through the value of the process element.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Element[Value2] ``.

Returns: `` ProcessElement[Index, Value2] ``.

Source contract/attributes: Chain the given function pointwise through the value of the process element. Attributes

Invocation template:

```scala
receiver.chain[Value2](f)
```

## `` com.cra.figaro.library.collection.ProcessElement.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/ProcessElement.html#get-ade)

```scala
def get(i: Index): Element[Option[Value]]
```

Safely creates an element whose value is the optional value at the corresponding index of the value of the process element. If the value of the process element does not have the corresponding index, the value of this element is None.

Type parameters: none.

Parameters, list 1: `` i: Index ``.

Returns: `` Element[Option[Value]] ``.

Source contract/attributes: Safely creates an element whose value is the optional value at the corresponding index of the value of the process element. If the value of the process element does not have the corresponding index, the value of this element is None. Attributes

Invocation template:

```scala
receiver.get(i)
```

## `` com.cra.figaro.library.collection.ProcessElement.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/ProcessElement.html#map-fffff29f)

```scala
def map[Value2](f: Value => Value2): ProcessElement[Index, Value2]
```

Map the given function pointwise through the value of the process element.

Type parameters: `` Value2 ``.

Parameters, list 1: `` f: Value => Value2 ``.

Returns: `` ProcessElement[Index, Value2] ``.

Source contract/attributes: Map the given function pointwise through the value of the process element. Attributes

Invocation template:

```scala
receiver.map[Value2](f)
```

## `` com.cra.figaro.library.collection.VariableSizeArray.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/library/collection/VariableSizeArray$.html#apply-fffffdbe)

```scala
def apply[Value](numItems: Element[Int], generator: Int => Element[Value])(implicit name: Name[FixedSizeArray[Value]], collection: ElementCollection): FixedSizeArrayElement[Value]
```

Create a FixedSizeArrayElement that holds a MakeArray representing a random process that generates an array of a variable number of items, in which each item is generated by an element defined by a given function.

Type parameters: `` Value ``.

Parameters, list 1: `` numItems: Element[Int] ``; `` generator: Int => Element[Value] ``.

Context parameters, list 2: `` name: Name[FixedSizeArray[Value]] ``; `` collection: ElementCollection ``.

Returns: `` FixedSizeArrayElement[Value] ``.

Source contract/attributes: Create a FixedSizeArrayElement that holds a MakeArray representing a random process that generates an array of a variable number of items, in which each item is generated by an element defined by a given function. Value parameters itemMaker a function that creates the element for generating each item, given the index into the array numItems element representing the number of items in the array Attributes

Invocation template:

```scala
com.cra.figaro.library.collection.VariableSizeArray.apply[Value](numItems, generator)(using name, collection)
```
