<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.experimental.normalproposals

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.MAPValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#MAPValue-0)

```scala
def MAPValue: Double
```

The most likely value of the parameter (maximum a posteriori) given the evidence.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: The most likely value of the parameter (maximum a posteriori) given the evidence. Attributes

Invocation template:

```scala
receiver.MAPValue
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.aValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#aValue-0)

```scala
def aValue: Double
```

Current a value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Current a value. Attributes

Invocation template:

```scala
receiver.aValue
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.bValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#bValue-0)

```scala
def bValue: Double
```

Current b value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Current b value. Attributes

Invocation template:

```scala
receiver.bValue
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.density ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#density-5b2)

```scala
def density(x: Double): Double
```

Density of a value.

Type parameters: none.

Parameters, list 1: `` x: Double ``.

Returns: `` Double ``.

Source contract/attributes: Density of a value. Attributes

Invocation template:

```scala
receiver.density(x)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.expectedValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#expectedValue-0)

```scala
def expectedValue: Double
```

The expected value of the parameter.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: The expected value of the parameter. Attributes

Invocation template:

```scala
receiver.expectedValue
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.generateRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#generateRandomness-999)

```scala
def generateRandomness(): Randomness
```

Generate the randomness content.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Randomness ``.

Source contract/attributes: Generate the randomness content. Attributes

Invocation template:

```scala
receiver.generateRandomness()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.lower ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#lower-0)

```scala
override def lower: Double
```

Exclusive lower bound of the range of the randomness of this element. Defaults to -Infinity. Must be strictly less than the upper bound.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Exclusive lower bound of the range of the randomness of this element. Defaults to -Infinity. Must be strictly less than the upper bound. Attributes Definition Classes NormalProposer

Invocation template:

```scala
receiver.lower
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.makeValues ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#makeValues-6da)

```scala
def makeValues(depth: Int): ValueSet[Double]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` depth: Int ``.

Returns: `` ValueSet[Double] ``.

Invocation template:

```scala
receiver.makeValues(depth)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.maximize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#maximize-fffff9b7)

```scala
def maximize(sufficientStatistics: Seq[Double]): Unit
```

Sets the hyperparameters of this parameter to their most likely value based on the sufficient statistics provided.

Type parameters: none.

Parameters, list 1: `` sufficientStatistics: Seq[Double] ``.

Returns: `` Unit ``.

Source contract/attributes: Sets the hyperparameters of this parameter to their most likely value based on the sufficient statistics provided. Attributes

Invocation template:

```scala
receiver.maximize(sufficientStatistics)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.nextRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#nextRandomness-fffffb4c)

```scala
override def nextRandomness(oldRandomness: Randomness): (Randomness, Double, Double)
```

Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is:

Type parameters: none.

Parameters, list 1: `` oldRandomness: Randomness ``.

Returns: `` (Randomness, Double, Double) ``.

Source contract/attributes: Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is: P(new -> old) / P(old -> new) and the model probability ratio, which is: P(new) / P(old) By default, this implementation proposes a normally distributed randomness from the previous randomness, truncated to be within the appropriate range. The probability ratios returned are associated with the values of this element rather than the randomness. This is for the purpose of simulated annealing, since the most likely randomness is not necessarily the most likely value, depending on the form of the generateValue function. One can override this to only use normal proposals in certain special cases. Attributes Definition Classes NormalProposer -> HasDensity -> Element

Invocation template:

```scala
receiver.nextRandomness(oldRandomness)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.proposalScale ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#proposalScale-0)

```scala
def proposalScale: Double
```

The scale of the normally distributed proposal. This corresponds to the standard deviation of the proposal before truncation. If the randomness has finite variance, this should be less than or equal to its standard deviation. A good place to start is e.g. 20% of the standard deviation.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: The scale of the normally distributed proposal. This corresponds to the standard deviation of the proposal before truncation. If the randomness has finite variance, this should be less than or equal to its standard deviation. A good place to start is e.g. 20% of the standard deviation. Attributes

Invocation template:

```scala
receiver.proposalScale
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.sufficientStatistics ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#sufficientStatistics-2ef)

```scala
override def sufficientStatistics[Boolean](b: Boolean): Seq[Double]
```

Returns a sufficient statistics vector corresponding to the evidence that an element using this parameter took on the value a.

Type parameters: `` Boolean ``.

Parameters, list 1: `` b: Boolean ``.

Returns: `` Seq[Double] ``.

Source contract/attributes: Returns a sufficient statistics vector corresponding to the evidence that an element using this parameter took on the value a. Attributes Definition Classes Parameter

Invocation template:

```scala
receiver.sufficientStatistics[Boolean](b)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Any

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.upper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#upper-0)

```scala
override def upper: Double
```

Exclusive upper bound of the range of the randomness of this element. Defaults to Infinity. Must be strictly greater than the lower bound.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Exclusive upper bound of the range of the randomness of this element. Defaults to Infinity. Must be strictly greater than the lower bound. Attributes Definition Classes NormalProposer

Invocation template:

```scala
receiver.upper
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.zeroSufficientStatistics ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#zeroSufficientStatistics-0)

```scala
override def zeroSufficientStatistics: Seq[Double]
```

Returns an empty sufficient statistics vector.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Seq[Double] ``.

Source contract/attributes: Returns an empty sufficient statistics vector. Attributes Definition Classes Parameter

Invocation template:

```scala
receiver.zeroSufficientStatistics
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.!== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#!==-894)

```scala
def !==(that: Element[Value])(implicit universe: Universe): Neq[Double]
```

The element that tests inequality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Neq[Double] ``.

Source contract/attributes: The element that tests inequality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.!==(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#===-f71)

```scala
def ===(that: Value)(implicit universe: Universe): Apply1[Double, Boolean]
```

The element that tests whether the value of this element is equal to a particular value.

Type parameters: none.

Parameters, list 1: `` that: Value ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Apply1[Double, Boolean] ``.

Source contract/attributes: The element that tests whether the value of this element is equal to a particular value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#===-672)

```scala
def ===(that: Element[Value])(implicit universe: Universe): Eq[Double]
```

The element that tests equality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Eq[Double] ``.

Source contract/attributes: The element that tests equality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.activate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#activate-94c)

```scala
def activate(): Unit
```

Activate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Activate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.activate()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.addCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#addCondition-fffff9ad)

```scala
def addCondition(condition: Condition, contingency: Contingency = ...): Unit
```

Add the given condition to the existing conditions of the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` condition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add the given condition to the existing conditions of the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addCondition(condition, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.addConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#addConstraint-fffff9ad)

```scala
def addConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addConstraint(constraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.addLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#addLogConstraint-fffff9ad)

```scala
def addLogConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a log contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a log contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addLogConstraint(constraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.addPragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#addPragma-b53)

```scala
def addPragma(pragma: Pragma[Value]): Unit
```

Add a pragma to the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Add a pragma to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addPragma(pragma)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.allConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#allConditions-0)

```scala
def allConditions: List[(Condition, Contingency)]
```

All the conditions defined on this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Condition, Contingency)] ``.

Source contract/attributes: All the conditions defined on this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConditions
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.allConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#allConstraints-0)

```scala
def allConstraints: List[(Constraint, Contingency)]
```

The current soft constraints on the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Constraint, Contingency)] ``.

Source contract/attributes: The current soft constraints on the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConstraints
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.args ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#args-0)

```scala
def args: List[Element[_]]
```

Returns an empty list.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Returns an empty list. Attributes Inherited from: Atomic

Invocation template:

```scala
receiver.args
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.clearContext ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#clearContext-94c)

```scala
def clearContext(): Unit
```

Clears all the temporary elements associated with this element (all elements created in it's context).

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clears all the temporary elements associated with this element (all elements created in it's context). Attributes Inherited from: Element

Invocation template:

```scala
receiver.clearContext()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.condition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#condition-4ad)

```scala
def condition(value: Any): Boolean
```

Tests whether all the element's contingent conditions are satisfied for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether all the element's contingent conditions are satisfied for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.condition(value)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.conditionSatisfied ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#conditionSatisfied-0)

```scala
def conditionSatisfied: Boolean
```

Determines whether the condition on the element is satisfied by the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Determines whether the condition on the element is satisfied by the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.conditionSatisfied
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.constraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#constraint-fffffeac)

```scala
def constraint(value: Any): Double
```

Gets the result of all the element's contingent constraints for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Double ``.

Source contract/attributes: Gets the result of all the element's contingent constraints for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraint(value)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.constraintValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#constraintValue-0)

```scala
def constraintValue: Double
```

Determines the value of the constraint on the element applied to the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Determines the value of the constraint on the element applied to the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraintValue
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.context ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#context-0)

```scala
def context: List[Element[_]]
```

The elements on which the existence of this element depends.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: The elements on which the existence of this element depends. Attributes Inherited from: Element

Invocation template:

```scala
receiver.context
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.deactivate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#deactivate-94c)

```scala
def deactivate(): Unit
```

Deactivate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Deactivate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.deactivate()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.directContextContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#directContextContents-0)

```scala
def directContextContents: Set[Element[_]]
```

Returns the set of elements directly created in the context of this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the set of elements directly created in the context of this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.directContextContents
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.elementsIAmContingentOn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#elementsIAmContingentOn-0)

```scala
def elementsIAmContingentOn: Set[Element[_]]
```

Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value. Figaro takes care of handling all this under the hood. However, some algorithms may need to know which elements an element is contingent on. For example, sampling algorithms may need to sample those other elements first. This method is supplied to support this use case. Attributes Inherited from: Element

Invocation template:

```scala
receiver.elementsIAmContingentOn
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#flatMap-db8)

```scala
def flatMap[U](fn: Value => Element[U])(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => Element[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.flatMap[U](fn)(using name, collection)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#generate-94c)

```scala
final def generate(): Unit
```

First generate the randomness, then generate the value given the randomness. Store the results in randomness and value.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: First generate the randomness, then generate the value given the randomness. Store the results in randomness and value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.generate()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.generateValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#generateValue-5b2)

```scala
def generateValue(rand: Randomness): Value
```

A strictly monotone differentiable function defined on (lower, upper). Defaults to the identity function.

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` Value ``.

Source contract/attributes: A strictly monotone differentiable function defined on (lower, upper). Defaults to the identity function. Attributes Inherited from: NormalProposer

Invocation template:

```scala
receiver.generateValue(rand)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.generateValueDerivative ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#generateValueDerivative-5b2)

```scala
def generateValueDerivative(rand: Randomness): Double
```

The absolute value of the derivative of generateValue with respect to the randomness given. This is needed to compute a proposal density over the transformed randomness. Defaults to 1.0, corresponding to the case where generateValue is the identity function.

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` Double ``.

Source contract/attributes: The absolute value of the derivative of generateValue with respect to the randomness given. This is needed to compute a proposal density over the transformed randomness. Defaults to 1.0, corresponding to the case where generateValue is the identity function. Attributes Inherited from: NormalProposer

Invocation template:

```scala
receiver.generateValueDerivative(rand)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.intervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#intervene-fffff71f)

```scala
def intervene(v: Value): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` v: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.intervene(v)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.isCachable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#isCachable-0)

```scala
def isCachable: Boolean
```

The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isCachable
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.isTemporary ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#isTemporary-0)

```scala
def isTemporary: Boolean
```

Returns true if this element is temporary, that is, was created in the context of another element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Returns true if this element is temporary, that is, was created in the context of another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isTemporary
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.logp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#logp-5b2)

```scala
def logp(value: Double): Double
```

Log-likelihood of a value.

Type parameters: none.

Parameters, list 1: `` value: Double ``.

Returns: `` Double ``.

Source contract/attributes: Log-likelihood of a value. Attributes Inherited from: Beta

Invocation template:

```scala
receiver.logp(value)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#map-db8)

```scala
def map[U](fn: Value => U)(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => U ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.map[U](fn)(using name, collection)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.observe ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#observe-fffff71f)

```scala
def observe(observation: Value): Unit
```

Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated.

Type parameters: none.

Parameters, list 1: `` observation: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.observe(observation)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.pragmas ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#pragmas-0)

```scala
def pragmas: List[Pragma[Value]]
```

The pragmas attached to the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Pragma[Value]] ``.

Source contract/attributes: The pragmas attached to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.pragmas
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.removeConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#removeConditions-71e)

```scala
def removeConditions(contingency: Contingency = ...): Unit
```

Remove all conditions associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all conditions associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConditions(contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.removeConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#removeConstraints-71e)

```scala
def removeConstraints(contingency: Contingency = ...): Unit
```

Remove all constraints associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all constraints associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConstraints(contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.removePragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#removePragma-b53)

```scala
def removePragma(pragma: Pragma[Value]): Unit
```

Remove a pragma from the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Remove a pragma from the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removePragma(pragma)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.score ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#score-fffffdbf)

```scala
def score(oldValue: Value, newValue: Value): Double
```

Compute the constraints on the new value divided by the constraints on the old value.

Type parameters: none.

Parameters, list 1: `` oldValue: Value ``; `` newValue: Value ``.

Returns: `` Double ``.

Source contract/attributes: Compute the constraints on the new value divided by the constraints on the old value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.score(oldValue, newValue)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.set ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#set-fffff71f)

```scala
def set(newValue: Value): Unit
```

Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called.

Type parameters: none.

Parameters, list 1: `` newValue: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called. Attributes Inherited from: Element

Invocation template:

```scala
receiver.set(newValue)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.setCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#setCondition-fffff9ad)

```scala
def setCondition(newCondition: Condition, contingency: Contingency = ...): Unit
```

Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newCondition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setCondition(newCondition, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.setConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#setConstraint-fffff9ad)

```scala
def setConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.setLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#setLogConstraint-fffff9ad)

```scala
def setLogConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setLogConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.setRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#setRandomness-fffff71f)

```scala
def setRandomness(newRandomness: Randomness): Unit
```

Set the randomness of this element.

Type parameters: none.

Parameters, list 1: `` newRandomness: Randomness ``.

Returns: `` Unit ``.

Source contract/attributes: Set the randomness of this element. Will generate its value using the new randomness and propagate the effects to elements that depend on it without changing their randomness. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setRandomness(newRandomness)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.toNameString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#toNameString-0)

```scala
def toNameString: String
```

A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString. Attributes Inherited from: Element

Invocation template:

```scala
receiver.toNameString
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.unintervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#unintervene-94c)

```scala
def unintervene(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.unintervene()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.unobserve ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#unobserve-94c)

```scala
def unobserve(): Unit
```

Removes conditions on the element and allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Removes conditions on the element and allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unobserve()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicBeta.unset ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicBeta.html#unset-94c)

```scala
def unset(): Unit
```

Allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unset()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.density ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#density-5b2)

```scala
def density(d: Double): Double
```

Density of a value.

Type parameters: none.

Parameters, list 1: `` d: Double ``.

Returns: `` Double ``.

Source contract/attributes: Density of a value. Attributes

Invocation template:

```scala
receiver.density(d)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.generateRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#generateRandomness-999)

```scala
def generateRandomness(): Randomness
```

Generate the randomness content.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Randomness ``.

Source contract/attributes: Generate the randomness content. Attributes

Invocation template:

```scala
receiver.generateRandomness()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.lower ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#lower-0)

```scala
override def lower: Double
```

Exclusive lower bound of the range of the randomness of this element. Defaults to -Infinity. Must be strictly less than the upper bound.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Exclusive lower bound of the range of the randomness of this element. Defaults to -Infinity. Must be strictly less than the upper bound. Attributes Definition Classes NormalProposer

Invocation template:

```scala
receiver.lower
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.proposalScale ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#proposalScale-0)

```scala
def proposalScale: Double
```

The scale of the normally distributed proposal. This corresponds to the standard deviation of the proposal before truncation. If the randomness has finite variance, this should be less than or equal to its standard deviation. A good place to start is e.g. 20% of the standard deviation.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: The scale of the normally distributed proposal. This corresponds to the standard deviation of the proposal before truncation. If the randomness has finite variance, this should be less than or equal to its standard deviation. A good place to start is e.g. 20% of the standard deviation. Attributes

Invocation template:

```scala
receiver.proposalScale
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Any

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.!== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#!==-894)

```scala
def !==(that: Element[Value])(implicit universe: Universe): Neq[Double]
```

The element that tests inequality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Neq[Double] ``.

Source contract/attributes: The element that tests inequality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.!==(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#===-f71)

```scala
def ===(that: Value)(implicit universe: Universe): Apply1[Double, Boolean]
```

The element that tests whether the value of this element is equal to a particular value.

Type parameters: none.

Parameters, list 1: `` that: Value ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Apply1[Double, Boolean] ``.

Source contract/attributes: The element that tests whether the value of this element is equal to a particular value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#===-672)

```scala
def ===(that: Element[Value])(implicit universe: Universe): Eq[Double]
```

The element that tests equality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Eq[Double] ``.

Source contract/attributes: The element that tests equality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.activate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#activate-94c)

```scala
def activate(): Unit
```

Activate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Activate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.activate()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.addCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#addCondition-fffff9ad)

```scala
def addCondition(condition: Condition, contingency: Contingency = ...): Unit
```

Add the given condition to the existing conditions of the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` condition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add the given condition to the existing conditions of the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addCondition(condition, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.addConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#addConstraint-fffff9ad)

```scala
def addConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addConstraint(constraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.addLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#addLogConstraint-fffff9ad)

```scala
def addLogConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a log contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a log contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addLogConstraint(constraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.addPragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#addPragma-b53)

```scala
def addPragma(pragma: Pragma[Value]): Unit
```

Add a pragma to the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Add a pragma to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addPragma(pragma)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.allConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#allConditions-0)

```scala
def allConditions: List[(Condition, Contingency)]
```

All the conditions defined on this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Condition, Contingency)] ``.

Source contract/attributes: All the conditions defined on this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConditions
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.allConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#allConstraints-0)

```scala
def allConstraints: List[(Constraint, Contingency)]
```

The current soft constraints on the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Constraint, Contingency)] ``.

Source contract/attributes: The current soft constraints on the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConstraints
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.args ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#args-0)

```scala
def args: List[Element[_]]
```

Returns an empty list.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Returns an empty list. Attributes Inherited from: Atomic

Invocation template:

```scala
receiver.args
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.clearContext ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#clearContext-94c)

```scala
def clearContext(): Unit
```

Clears all the temporary elements associated with this element (all elements created in it's context).

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clears all the temporary elements associated with this element (all elements created in it's context). Attributes Inherited from: Element

Invocation template:

```scala
receiver.clearContext()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.condition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#condition-4ad)

```scala
def condition(value: Any): Boolean
```

Tests whether all the element's contingent conditions are satisfied for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether all the element's contingent conditions are satisfied for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.condition(value)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.conditionSatisfied ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#conditionSatisfied-0)

```scala
def conditionSatisfied: Boolean
```

Determines whether the condition on the element is satisfied by the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Determines whether the condition on the element is satisfied by the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.conditionSatisfied
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.constraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#constraint-fffffeac)

```scala
def constraint(value: Any): Double
```

Gets the result of all the element's contingent constraints for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Double ``.

Source contract/attributes: Gets the result of all the element's contingent constraints for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraint(value)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.constraintValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#constraintValue-0)

```scala
def constraintValue: Double
```

Determines the value of the constraint on the element applied to the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Determines the value of the constraint on the element applied to the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraintValue
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.context ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#context-0)

```scala
def context: List[Element[_]]
```

The elements on which the existence of this element depends.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: The elements on which the existence of this element depends. Attributes Inherited from: Element

Invocation template:

```scala
receiver.context
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.deactivate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#deactivate-94c)

```scala
def deactivate(): Unit
```

Deactivate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Deactivate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.deactivate()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.directContextContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#directContextContents-0)

```scala
def directContextContents: Set[Element[_]]
```

Returns the set of elements directly created in the context of this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the set of elements directly created in the context of this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.directContextContents
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.elementsIAmContingentOn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#elementsIAmContingentOn-0)

```scala
def elementsIAmContingentOn: Set[Element[_]]
```

Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value. Figaro takes care of handling all this under the hood. However, some algorithms may need to know which elements an element is contingent on. For example, sampling algorithms may need to sample those other elements first. This method is supplied to support this use case. Attributes Inherited from: Element

Invocation template:

```scala
receiver.elementsIAmContingentOn
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#flatMap-db8)

```scala
def flatMap[U](fn: Value => Element[U])(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => Element[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.flatMap[U](fn)(using name, collection)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#generate-94c)

```scala
final def generate(): Unit
```

First generate the randomness, then generate the value given the randomness. Store the results in randomness and value.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: First generate the randomness, then generate the value given the randomness. Store the results in randomness and value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.generate()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.generateValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#generateValue-5b2)

```scala
def generateValue(rand: Randomness): Value
```

A strictly monotone differentiable function defined on (lower, upper). Defaults to the identity function.

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` Value ``.

Source contract/attributes: A strictly monotone differentiable function defined on (lower, upper). Defaults to the identity function. Attributes Inherited from: NormalProposer

Invocation template:

```scala
receiver.generateValue(rand)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.generateValueDerivative ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#generateValueDerivative-5b2)

```scala
def generateValueDerivative(rand: Randomness): Double
```

The absolute value of the derivative of generateValue with respect to the randomness given. This is needed to compute a proposal density over the transformed randomness. Defaults to 1.0, corresponding to the case where generateValue is the identity function.

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` Double ``.

Source contract/attributes: The absolute value of the derivative of generateValue with respect to the randomness given. This is needed to compute a proposal density over the transformed randomness. Defaults to 1.0, corresponding to the case where generateValue is the identity function. Attributes Inherited from: NormalProposer

Invocation template:

```scala
receiver.generateValueDerivative(rand)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.intervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#intervene-fffff71f)

```scala
def intervene(v: Value): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` v: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.intervene(v)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.isCachable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#isCachable-0)

```scala
def isCachable: Boolean
```

The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isCachable
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.isTemporary ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#isTemporary-0)

```scala
def isTemporary: Boolean
```

Returns true if this element is temporary, that is, was created in the context of another element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Returns true if this element is temporary, that is, was created in the context of another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isTemporary
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#map-db8)

```scala
def map[U](fn: Value => U)(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => U ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.map[U](fn)(using name, collection)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.nextRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#nextRandomness-fffffb4c)

```scala
override def nextRandomness(oldRandomness: Randomness): (Randomness, Double, Double)
```

Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is:

Type parameters: none.

Parameters, list 1: `` oldRandomness: Randomness ``.

Returns: `` (Randomness, Double, Double) ``.

Source contract/attributes: Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is: P(new -> old) / P(old -> new) and the model probability ratio, which is: P(new) / P(old) By default, this implementation proposes a normally distributed randomness from the previous randomness, truncated to be within the appropriate range. The probability ratios returned are associated with the values of this element rather than the randomness. This is for the purpose of simulated annealing, since the most likely randomness is not necessarily the most likely value, depending on the form of the generateValue function. One can override this to only use normal proposals in certain special cases. Attributes Definition Classes NormalProposer -> HasDensity -> Element Inherited from: NormalProposer

Invocation template:

```scala
receiver.nextRandomness(oldRandomness)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.observe ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#observe-fffff71f)

```scala
def observe(observation: Value): Unit
```

Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated.

Type parameters: none.

Parameters, list 1: `` observation: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.observe(observation)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.pragmas ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#pragmas-0)

```scala
def pragmas: List[Pragma[Value]]
```

The pragmas attached to the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Pragma[Value]] ``.

Source contract/attributes: The pragmas attached to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.pragmas
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.removeConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#removeConditions-71e)

```scala
def removeConditions(contingency: Contingency = ...): Unit
```

Remove all conditions associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all conditions associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConditions(contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.removeConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#removeConstraints-71e)

```scala
def removeConstraints(contingency: Contingency = ...): Unit
```

Remove all constraints associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all constraints associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConstraints(contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.removePragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#removePragma-b53)

```scala
def removePragma(pragma: Pragma[Value]): Unit
```

Remove a pragma from the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Remove a pragma from the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removePragma(pragma)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.score ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#score-fffffdbf)

```scala
def score(oldValue: Value, newValue: Value): Double
```

Compute the constraints on the new value divided by the constraints on the old value.

Type parameters: none.

Parameters, list 1: `` oldValue: Value ``; `` newValue: Value ``.

Returns: `` Double ``.

Source contract/attributes: Compute the constraints on the new value divided by the constraints on the old value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.score(oldValue, newValue)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.set ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#set-fffff71f)

```scala
def set(newValue: Value): Unit
```

Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called.

Type parameters: none.

Parameters, list 1: `` newValue: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called. Attributes Inherited from: Element

Invocation template:

```scala
receiver.set(newValue)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.setCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#setCondition-fffff9ad)

```scala
def setCondition(newCondition: Condition, contingency: Contingency = ...): Unit
```

Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newCondition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setCondition(newCondition, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.setConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#setConstraint-fffff9ad)

```scala
def setConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.setLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#setLogConstraint-fffff9ad)

```scala
def setLogConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setLogConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.setRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#setRandomness-fffff71f)

```scala
def setRandomness(newRandomness: Randomness): Unit
```

Set the randomness of this element.

Type parameters: none.

Parameters, list 1: `` newRandomness: Randomness ``.

Returns: `` Unit ``.

Source contract/attributes: Set the randomness of this element. Will generate its value using the new randomness and propagate the effects to elements that depend on it without changing their randomness. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setRandomness(newRandomness)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.toNameString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#toNameString-0)

```scala
def toNameString: String
```

A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString. Attributes Inherited from: Element

Invocation template:

```scala
receiver.toNameString
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.unintervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#unintervene-94c)

```scala
def unintervene(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.unintervene()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.unobserve ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#unobserve-94c)

```scala
def unobserve(): Unit
```

Removes conditions on the element and allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Removes conditions on the element and allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unobserve()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.unset ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#unset-94c)

```scala
def unset(): Unit
```

Allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unset()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicExponential.upper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicExponential.html#upper-0)

```scala
def upper: Double
```

Exclusive upper bound of the range of the randomness of this element. Defaults to Infinity. Must be strictly greater than the lower bound.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Exclusive upper bound of the range of the randomness of this element. Defaults to Infinity. Must be strictly greater than the lower bound. Attributes Inherited from: NormalProposer

Invocation template:

```scala
receiver.upper
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.density ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#density-5b2)

```scala
def density(x: Double): Double
```

Density of a value.

Type parameters: none.

Parameters, list 1: `` x: Double ``.

Returns: `` Double ``.

Source contract/attributes: Density of a value. Attributes

Invocation template:

```scala
receiver.density(x)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.generateRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#generateRandomness-999)

```scala
def generateRandomness(): Randomness
```

Generate the randomness content.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Randomness ``.

Source contract/attributes: Generate the randomness content. Attributes

Invocation template:

```scala
receiver.generateRandomness()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.generateValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#generateValue-5b2)

```scala
override def generateValue(rand: Randomness): Value
```

A strictly monotone differentiable function defined on (lower, upper). Defaults to the identity function.

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` Value ``.

Source contract/attributes: A strictly monotone differentiable function defined on (lower, upper). Defaults to the identity function. Attributes Definition Classes NormalProposer -> Element

Invocation template:

```scala
receiver.generateValue(rand)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.generateValueDerivative ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#generateValueDerivative-5b2)

```scala
override def generateValueDerivative(rand: Randomness): Double
```

The absolute value of the derivative of generateValue with respect to the randomness given. This is needed to compute a proposal density over the transformed randomness. Defaults to 1.0, corresponding to the case where generateValue is the identity function.

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` Double ``.

Source contract/attributes: The absolute value of the derivative of generateValue with respect to the randomness given. This is needed to compute a proposal density over the transformed randomness. Defaults to 1.0, corresponding to the case where generateValue is the identity function. Attributes Definition Classes NormalProposer

Invocation template:

```scala
receiver.generateValueDerivative(rand)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.lower ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#lower-0)

```scala
override def lower: Double
```

Exclusive lower bound of the range of the randomness of this element. Defaults to -Infinity. Must be strictly less than the upper bound.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Exclusive lower bound of the range of the randomness of this element. Defaults to -Infinity. Must be strictly less than the upper bound. Attributes Definition Classes NormalProposer

Invocation template:

```scala
receiver.lower
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.nextRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#nextRandomness-fffffb4c)

```scala
override def nextRandomness(oldRandomness: Randomness): (Randomness, Double, Double)
```

Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is:

Type parameters: none.

Parameters, list 1: `` oldRandomness: Randomness ``.

Returns: `` (Randomness, Double, Double) ``.

Source contract/attributes: Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is: P(new -> old) / P(old -> new) and the model probability ratio, which is: P(new) / P(old) By default, this implementation proposes a normally distributed randomness from the previous randomness, truncated to be within the appropriate range. The probability ratios returned are associated with the values of this element rather than the randomness. This is for the purpose of simulated annealing, since the most likely randomness is not necessarily the most likely value, depending on the form of the generateValue function. One can override this to only use normal proposals in certain special cases. Attributes Definition Classes NormalProposer -> HasDensity -> Element

Invocation template:

```scala
receiver.nextRandomness(oldRandomness)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.proposalScale ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#proposalScale-0)

```scala
def proposalScale: Double
```

The scale of the normally distributed proposal. This corresponds to the standard deviation of the proposal before truncation. If the randomness has finite variance, this should be less than or equal to its standard deviation. A good place to start is e.g. 20% of the standard deviation.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: The scale of the normally distributed proposal. This corresponds to the standard deviation of the proposal before truncation. If the randomness has finite variance, this should be less than or equal to its standard deviation. A good place to start is e.g. 20% of the standard deviation. Attributes

Invocation template:

```scala
receiver.proposalScale
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Any

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.!== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#!==-894)

```scala
def !==(that: Element[Value])(implicit universe: Universe): Neq[Double]
```

The element that tests inequality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Neq[Double] ``.

Source contract/attributes: The element that tests inequality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.!==(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#===-f71)

```scala
def ===(that: Value)(implicit universe: Universe): Apply1[Double, Boolean]
```

The element that tests whether the value of this element is equal to a particular value.

Type parameters: none.

Parameters, list 1: `` that: Value ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Apply1[Double, Boolean] ``.

Source contract/attributes: The element that tests whether the value of this element is equal to a particular value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#===-672)

```scala
def ===(that: Element[Value])(implicit universe: Universe): Eq[Double]
```

The element that tests equality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Eq[Double] ``.

Source contract/attributes: The element that tests equality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.activate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#activate-94c)

```scala
def activate(): Unit
```

Activate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Activate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.activate()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.addCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#addCondition-fffff9ad)

```scala
def addCondition(condition: Condition, contingency: Contingency = ...): Unit
```

Add the given condition to the existing conditions of the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` condition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add the given condition to the existing conditions of the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addCondition(condition, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.addConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#addConstraint-fffff9ad)

```scala
def addConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addConstraint(constraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.addLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#addLogConstraint-fffff9ad)

```scala
def addLogConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a log contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a log contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addLogConstraint(constraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.addPragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#addPragma-b53)

```scala
def addPragma(pragma: Pragma[Value]): Unit
```

Add a pragma to the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Add a pragma to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addPragma(pragma)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.allConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#allConditions-0)

```scala
def allConditions: List[(Condition, Contingency)]
```

All the conditions defined on this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Condition, Contingency)] ``.

Source contract/attributes: All the conditions defined on this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConditions
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.allConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#allConstraints-0)

```scala
def allConstraints: List[(Constraint, Contingency)]
```

The current soft constraints on the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Constraint, Contingency)] ``.

Source contract/attributes: The current soft constraints on the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConstraints
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.args ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#args-0)

```scala
def args: List[Element[_]]
```

Returns an empty list.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Returns an empty list. Attributes Inherited from: Atomic

Invocation template:

```scala
receiver.args
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.clearContext ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#clearContext-94c)

```scala
def clearContext(): Unit
```

Clears all the temporary elements associated with this element (all elements created in it's context).

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clears all the temporary elements associated with this element (all elements created in it's context). Attributes Inherited from: Element

Invocation template:

```scala
receiver.clearContext()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.condition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#condition-4ad)

```scala
def condition(value: Any): Boolean
```

Tests whether all the element's contingent conditions are satisfied for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether all the element's contingent conditions are satisfied for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.condition(value)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.conditionSatisfied ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#conditionSatisfied-0)

```scala
def conditionSatisfied: Boolean
```

Determines whether the condition on the element is satisfied by the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Determines whether the condition on the element is satisfied by the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.conditionSatisfied
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.constraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#constraint-fffffeac)

```scala
def constraint(value: Any): Double
```

Gets the result of all the element's contingent constraints for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Double ``.

Source contract/attributes: Gets the result of all the element's contingent constraints for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraint(value)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.constraintValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#constraintValue-0)

```scala
def constraintValue: Double
```

Determines the value of the constraint on the element applied to the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Determines the value of the constraint on the element applied to the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraintValue
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.context ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#context-0)

```scala
def context: List[Element[_]]
```

The elements on which the existence of this element depends.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: The elements on which the existence of this element depends. Attributes Inherited from: Element

Invocation template:

```scala
receiver.context
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.deactivate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#deactivate-94c)

```scala
def deactivate(): Unit
```

Deactivate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Deactivate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.deactivate()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.directContextContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#directContextContents-0)

```scala
def directContextContents: Set[Element[_]]
```

Returns the set of elements directly created in the context of this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the set of elements directly created in the context of this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.directContextContents
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.elementsIAmContingentOn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#elementsIAmContingentOn-0)

```scala
def elementsIAmContingentOn: Set[Element[_]]
```

Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value. Figaro takes care of handling all this under the hood. However, some algorithms may need to know which elements an element is contingent on. For example, sampling algorithms may need to sample those other elements first. This method is supplied to support this use case. Attributes Inherited from: Element

Invocation template:

```scala
receiver.elementsIAmContingentOn
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#flatMap-db8)

```scala
def flatMap[U](fn: Value => Element[U])(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => Element[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.flatMap[U](fn)(using name, collection)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#generate-94c)

```scala
final def generate(): Unit
```

First generate the randomness, then generate the value given the randomness. Store the results in randomness and value.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: First generate the randomness, then generate the value given the randomness. Store the results in randomness and value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.generate()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.intervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#intervene-fffff71f)

```scala
def intervene(v: Value): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` v: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.intervene(v)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.isCachable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#isCachable-0)

```scala
def isCachable: Boolean
```

The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isCachable
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.isTemporary ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#isTemporary-0)

```scala
def isTemporary: Boolean
```

Returns true if this element is temporary, that is, was created in the context of another element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Returns true if this element is temporary, that is, was created in the context of another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isTemporary
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#map-db8)

```scala
def map[U](fn: Value => U)(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => U ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.map[U](fn)(using name, collection)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.observe ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#observe-fffff71f)

```scala
def observe(observation: Value): Unit
```

Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated.

Type parameters: none.

Parameters, list 1: `` observation: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.observe(observation)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.pragmas ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#pragmas-0)

```scala
def pragmas: List[Pragma[Value]]
```

The pragmas attached to the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Pragma[Value]] ``.

Source contract/attributes: The pragmas attached to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.pragmas
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.removeConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#removeConditions-71e)

```scala
def removeConditions(contingency: Contingency = ...): Unit
```

Remove all conditions associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all conditions associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConditions(contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.removeConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#removeConstraints-71e)

```scala
def removeConstraints(contingency: Contingency = ...): Unit
```

Remove all constraints associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all constraints associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConstraints(contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.removePragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#removePragma-b53)

```scala
def removePragma(pragma: Pragma[Value]): Unit
```

Remove a pragma from the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Remove a pragma from the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removePragma(pragma)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.score ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#score-fffffdbf)

```scala
def score(oldValue: Value, newValue: Value): Double
```

Compute the constraints on the new value divided by the constraints on the old value.

Type parameters: none.

Parameters, list 1: `` oldValue: Value ``; `` newValue: Value ``.

Returns: `` Double ``.

Source contract/attributes: Compute the constraints on the new value divided by the constraints on the old value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.score(oldValue, newValue)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.set ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#set-fffff71f)

```scala
def set(newValue: Value): Unit
```

Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called.

Type parameters: none.

Parameters, list 1: `` newValue: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called. Attributes Inherited from: Element

Invocation template:

```scala
receiver.set(newValue)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.setCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#setCondition-fffff9ad)

```scala
def setCondition(newCondition: Condition, contingency: Contingency = ...): Unit
```

Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newCondition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setCondition(newCondition, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.setConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#setConstraint-fffff9ad)

```scala
def setConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.setLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#setLogConstraint-fffff9ad)

```scala
def setLogConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setLogConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.setRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#setRandomness-fffff71f)

```scala
def setRandomness(newRandomness: Randomness): Unit
```

Set the randomness of this element.

Type parameters: none.

Parameters, list 1: `` newRandomness: Randomness ``.

Returns: `` Unit ``.

Source contract/attributes: Set the randomness of this element. Will generate its value using the new randomness and propagate the effects to elements that depend on it without changing their randomness. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setRandomness(newRandomness)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.toNameString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#toNameString-0)

```scala
def toNameString: String
```

A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString. Attributes Inherited from: Element

Invocation template:

```scala
receiver.toNameString
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.unintervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#unintervene-94c)

```scala
def unintervene(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.unintervene()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.unobserve ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#unobserve-94c)

```scala
def unobserve(): Unit
```

Removes conditions on the element and allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Removes conditions on the element and allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unobserve()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.unset ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#unset-94c)

```scala
def unset(): Unit
```

Allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unset()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicGamma.upper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicGamma.html#upper-0)

```scala
def upper: Double
```

Exclusive upper bound of the range of the randomness of this element. Defaults to Infinity. Must be strictly greater than the lower bound.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Exclusive upper bound of the range of the randomness of this element. Defaults to Infinity. Must be strictly greater than the lower bound. Attributes Inherited from: NormalProposer

Invocation template:

```scala
receiver.upper
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.density ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#density-5b2)

```scala
def density(x: Double): Double
```

Density of a value.

Type parameters: none.

Parameters, list 1: `` x: Double ``.

Returns: `` Double ``.

Source contract/attributes: Density of a value. Attributes

Invocation template:

```scala
receiver.density(x)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.generateRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#generateRandomness-999)

```scala
def generateRandomness(): Randomness
```

Generate the randomness content.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Randomness ``.

Source contract/attributes: Generate the randomness content. Attributes

Invocation template:

```scala
receiver.generateRandomness()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.generateValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#generateValue-5b2)

```scala
override def generateValue(rand: Randomness): Value
```

A strictly monotone differentiable function defined on (lower, upper). Defaults to the identity function.

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` Value ``.

Source contract/attributes: A strictly monotone differentiable function defined on (lower, upper). Defaults to the identity function. Attributes Definition Classes NormalProposer -> Element

Invocation template:

```scala
receiver.generateValue(rand)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.generateValueDerivative ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#generateValueDerivative-5b2)

```scala
override def generateValueDerivative(rand: Randomness): Double
```

The absolute value of the derivative of generateValue with respect to the randomness given. This is needed to compute a proposal density over the transformed randomness. Defaults to 1.0, corresponding to the case where generateValue is the identity function.

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` Double ``.

Source contract/attributes: The absolute value of the derivative of generateValue with respect to the randomness given. This is needed to compute a proposal density over the transformed randomness. Defaults to 1.0, corresponding to the case where generateValue is the identity function. Attributes Definition Classes NormalProposer

Invocation template:

```scala
receiver.generateValueDerivative(rand)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.lower ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#lower-0)

```scala
override def lower: Double
```

Exclusive lower bound of the range of the randomness of this element. Defaults to -Infinity. Must be strictly less than the upper bound.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Exclusive lower bound of the range of the randomness of this element. Defaults to -Infinity. Must be strictly less than the upper bound. Attributes Definition Classes NormalProposer

Invocation template:

```scala
receiver.lower
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.nextRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#nextRandomness-fffffb4c)

```scala
override def nextRandomness(oldRandomness: Randomness): (Randomness, Double, Double)
```

Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is:

Type parameters: none.

Parameters, list 1: `` oldRandomness: Randomness ``.

Returns: `` (Randomness, Double, Double) ``.

Source contract/attributes: Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is: P(new -> old) / P(old -> new) and the model probability ratio, which is: P(new) / P(old) By default, this implementation proposes a normally distributed randomness from the previous randomness, truncated to be within the appropriate range. The probability ratios returned are associated with the values of this element rather than the randomness. This is for the purpose of simulated annealing, since the most likely randomness is not necessarily the most likely value, depending on the form of the generateValue function. One can override this to only use normal proposals in certain special cases. Attributes Definition Classes NormalProposer -> HasDensity -> Element

Invocation template:

```scala
receiver.nextRandomness(oldRandomness)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.proposalScale ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#proposalScale-0)

```scala
def proposalScale: Double
```

The scale of the normally distributed proposal. This corresponds to the standard deviation of the proposal before truncation. If the randomness has finite variance, this should be less than or equal to its standard deviation. A good place to start is e.g. 20% of the standard deviation.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: The scale of the normally distributed proposal. This corresponds to the standard deviation of the proposal before truncation. If the randomness has finite variance, this should be less than or equal to its standard deviation. A good place to start is e.g. 20% of the standard deviation. Attributes

Invocation template:

```scala
receiver.proposalScale
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Any

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.!== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#!==-894)

```scala
def !==(that: Element[Value])(implicit universe: Universe): Neq[Double]
```

The element that tests inequality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Neq[Double] ``.

Source contract/attributes: The element that tests inequality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.!==(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#===-f71)

```scala
def ===(that: Value)(implicit universe: Universe): Apply1[Double, Boolean]
```

The element that tests whether the value of this element is equal to a particular value.

Type parameters: none.

Parameters, list 1: `` that: Value ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Apply1[Double, Boolean] ``.

Source contract/attributes: The element that tests whether the value of this element is equal to a particular value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#===-672)

```scala
def ===(that: Element[Value])(implicit universe: Universe): Eq[Double]
```

The element that tests equality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Eq[Double] ``.

Source contract/attributes: The element that tests equality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.activate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#activate-94c)

```scala
def activate(): Unit
```

Activate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Activate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.activate()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.addCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#addCondition-fffff9ad)

```scala
def addCondition(condition: Condition, contingency: Contingency = ...): Unit
```

Add the given condition to the existing conditions of the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` condition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add the given condition to the existing conditions of the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addCondition(condition, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.addConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#addConstraint-fffff9ad)

```scala
def addConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addConstraint(constraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.addLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#addLogConstraint-fffff9ad)

```scala
def addLogConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a log contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a log contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addLogConstraint(constraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.addPragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#addPragma-b53)

```scala
def addPragma(pragma: Pragma[Value]): Unit
```

Add a pragma to the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Add a pragma to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addPragma(pragma)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.allConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#allConditions-0)

```scala
def allConditions: List[(Condition, Contingency)]
```

All the conditions defined on this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Condition, Contingency)] ``.

Source contract/attributes: All the conditions defined on this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConditions
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.allConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#allConstraints-0)

```scala
def allConstraints: List[(Constraint, Contingency)]
```

The current soft constraints on the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Constraint, Contingency)] ``.

Source contract/attributes: The current soft constraints on the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConstraints
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.args ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#args-0)

```scala
def args: List[Element[_]]
```

Returns an empty list.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Returns an empty list. Attributes Inherited from: Atomic

Invocation template:

```scala
receiver.args
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.clearContext ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#clearContext-94c)

```scala
def clearContext(): Unit
```

Clears all the temporary elements associated with this element (all elements created in it's context).

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clears all the temporary elements associated with this element (all elements created in it's context). Attributes Inherited from: Element

Invocation template:

```scala
receiver.clearContext()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.condition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#condition-4ad)

```scala
def condition(value: Any): Boolean
```

Tests whether all the element's contingent conditions are satisfied for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether all the element's contingent conditions are satisfied for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.condition(value)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.conditionSatisfied ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#conditionSatisfied-0)

```scala
def conditionSatisfied: Boolean
```

Determines whether the condition on the element is satisfied by the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Determines whether the condition on the element is satisfied by the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.conditionSatisfied
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.constraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#constraint-fffffeac)

```scala
def constraint(value: Any): Double
```

Gets the result of all the element's contingent constraints for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Double ``.

Source contract/attributes: Gets the result of all the element's contingent constraints for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraint(value)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.constraintValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#constraintValue-0)

```scala
def constraintValue: Double
```

Determines the value of the constraint on the element applied to the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Determines the value of the constraint on the element applied to the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraintValue
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.context ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#context-0)

```scala
def context: List[Element[_]]
```

The elements on which the existence of this element depends.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: The elements on which the existence of this element depends. Attributes Inherited from: Element

Invocation template:

```scala
receiver.context
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.deactivate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#deactivate-94c)

```scala
def deactivate(): Unit
```

Deactivate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Deactivate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.deactivate()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.directContextContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#directContextContents-0)

```scala
def directContextContents: Set[Element[_]]
```

Returns the set of elements directly created in the context of this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the set of elements directly created in the context of this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.directContextContents
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.elementsIAmContingentOn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#elementsIAmContingentOn-0)

```scala
def elementsIAmContingentOn: Set[Element[_]]
```

Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value. Figaro takes care of handling all this under the hood. However, some algorithms may need to know which elements an element is contingent on. For example, sampling algorithms may need to sample those other elements first. This method is supplied to support this use case. Attributes Inherited from: Element

Invocation template:

```scala
receiver.elementsIAmContingentOn
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#flatMap-db8)

```scala
def flatMap[U](fn: Value => Element[U])(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => Element[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.flatMap[U](fn)(using name, collection)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#generate-94c)

```scala
final def generate(): Unit
```

First generate the randomness, then generate the value given the randomness. Store the results in randomness and value.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: First generate the randomness, then generate the value given the randomness. Store the results in randomness and value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.generate()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.intervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#intervene-fffff71f)

```scala
def intervene(v: Value): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` v: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.intervene(v)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.isCachable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#isCachable-0)

```scala
def isCachable: Boolean
```

The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isCachable
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.isTemporary ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#isTemporary-0)

```scala
def isTemporary: Boolean
```

Returns true if this element is temporary, that is, was created in the context of another element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Returns true if this element is temporary, that is, was created in the context of another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isTemporary
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#map-db8)

```scala
def map[U](fn: Value => U)(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => U ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.map[U](fn)(using name, collection)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.observe ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#observe-fffff71f)

```scala
def observe(observation: Value): Unit
```

Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated.

Type parameters: none.

Parameters, list 1: `` observation: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.observe(observation)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.pragmas ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#pragmas-0)

```scala
def pragmas: List[Pragma[Value]]
```

The pragmas attached to the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Pragma[Value]] ``.

Source contract/attributes: The pragmas attached to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.pragmas
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.removeConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#removeConditions-71e)

```scala
def removeConditions(contingency: Contingency = ...): Unit
```

Remove all conditions associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all conditions associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConditions(contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.removeConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#removeConstraints-71e)

```scala
def removeConstraints(contingency: Contingency = ...): Unit
```

Remove all constraints associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all constraints associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConstraints(contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.removePragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#removePragma-b53)

```scala
def removePragma(pragma: Pragma[Value]): Unit
```

Remove a pragma from the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Remove a pragma from the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removePragma(pragma)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.score ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#score-fffffdbf)

```scala
def score(oldValue: Value, newValue: Value): Double
```

Compute the constraints on the new value divided by the constraints on the old value.

Type parameters: none.

Parameters, list 1: `` oldValue: Value ``; `` newValue: Value ``.

Returns: `` Double ``.

Source contract/attributes: Compute the constraints on the new value divided by the constraints on the old value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.score(oldValue, newValue)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.set ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#set-fffff71f)

```scala
def set(newValue: Value): Unit
```

Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called.

Type parameters: none.

Parameters, list 1: `` newValue: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called. Attributes Inherited from: Element

Invocation template:

```scala
receiver.set(newValue)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.setCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#setCondition-fffff9ad)

```scala
def setCondition(newCondition: Condition, contingency: Contingency = ...): Unit
```

Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newCondition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setCondition(newCondition, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.setConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#setConstraint-fffff9ad)

```scala
def setConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.setLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#setLogConstraint-fffff9ad)

```scala
def setLogConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setLogConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.setRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#setRandomness-fffff71f)

```scala
def setRandomness(newRandomness: Randomness): Unit
```

Set the randomness of this element.

Type parameters: none.

Parameters, list 1: `` newRandomness: Randomness ``.

Returns: `` Unit ``.

Source contract/attributes: Set the randomness of this element. Will generate its value using the new randomness and propagate the effects to elements that depend on it without changing their randomness. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setRandomness(newRandomness)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.toNameString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#toNameString-0)

```scala
def toNameString: String
```

A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString. Attributes Inherited from: Element

Invocation template:

```scala
receiver.toNameString
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.unintervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#unintervene-94c)

```scala
def unintervene(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.unintervene()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.unobserve ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#unobserve-94c)

```scala
def unobserve(): Unit
```

Removes conditions on the element and allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Removes conditions on the element and allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unobserve()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.unset ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#unset-94c)

```scala
def unset(): Unit
```

Allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unset()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicInverseGamma.upper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicInverseGamma.html#upper-0)

```scala
def upper: Double
```

Exclusive upper bound of the range of the randomness of this element. Defaults to Infinity. Must be strictly greater than the lower bound.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Exclusive upper bound of the range of the randomness of this element. Defaults to Infinity. Must be strictly greater than the lower bound. Attributes Inherited from: NormalProposer

Invocation template:

```scala
receiver.upper
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.density ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#density-5b2)

```scala
def density(d: Double): Double
```

Density of a value.

Type parameters: none.

Parameters, list 1: `` d: Double ``.

Returns: `` Double ``.

Source contract/attributes: Density of a value. Attributes

Invocation template:

```scala
receiver.density(d)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.generateRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#generateRandomness-999)

```scala
def generateRandomness(): Randomness
```

Generate the randomness content.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Randomness ``.

Source contract/attributes: Generate the randomness content. Attributes

Invocation template:

```scala
receiver.generateRandomness()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.generateValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#generateValue-5b2)

```scala
override def generateValue(rand: Randomness): Value
```

A strictly monotone differentiable function defined on (lower, upper). Defaults to the identity function.

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` Value ``.

Source contract/attributes: A strictly monotone differentiable function defined on (lower, upper). Defaults to the identity function. Attributes Definition Classes NormalProposer -> Element

Invocation template:

```scala
receiver.generateValue(rand)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.generateValueDerivative ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#generateValueDerivative-5b2)

```scala
override def generateValueDerivative(rand: Randomness): Double
```

The absolute value of the derivative of generateValue with respect to the randomness given. This is needed to compute a proposal density over the transformed randomness. Defaults to 1.0, corresponding to the case where generateValue is the identity function.

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` Double ``.

Source contract/attributes: The absolute value of the derivative of generateValue with respect to the randomness given. This is needed to compute a proposal density over the transformed randomness. Defaults to 1.0, corresponding to the case where generateValue is the identity function. Attributes Definition Classes NormalProposer

Invocation template:

```scala
receiver.generateValueDerivative(rand)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.proposalScale ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#proposalScale-0)

```scala
def proposalScale: Double
```

The scale of the normally distributed proposal. This corresponds to the standard deviation of the proposal before truncation. If the randomness has finite variance, this should be less than or equal to its standard deviation. A good place to start is e.g. 20% of the standard deviation.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: The scale of the normally distributed proposal. This corresponds to the standard deviation of the proposal before truncation. If the randomness has finite variance, this should be less than or equal to its standard deviation. A good place to start is e.g. 20% of the standard deviation. Attributes

Invocation template:

```scala
receiver.proposalScale
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Any

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.!== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#!==-894)

```scala
def !==(that: Element[Value])(implicit universe: Universe): Neq[Double]
```

The element that tests inequality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Neq[Double] ``.

Source contract/attributes: The element that tests inequality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.!==(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#===-f71)

```scala
def ===(that: Value)(implicit universe: Universe): Apply1[Double, Boolean]
```

The element that tests whether the value of this element is equal to a particular value.

Type parameters: none.

Parameters, list 1: `` that: Value ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Apply1[Double, Boolean] ``.

Source contract/attributes: The element that tests whether the value of this element is equal to a particular value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#===-672)

```scala
def ===(that: Element[Value])(implicit universe: Universe): Eq[Double]
```

The element that tests equality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Eq[Double] ``.

Source contract/attributes: The element that tests equality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.activate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#activate-94c)

```scala
def activate(): Unit
```

Activate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Activate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.activate()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.addCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#addCondition-fffff9ad)

```scala
def addCondition(condition: Condition, contingency: Contingency = ...): Unit
```

Add the given condition to the existing conditions of the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` condition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add the given condition to the existing conditions of the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addCondition(condition, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.addConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#addConstraint-fffff9ad)

```scala
def addConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addConstraint(constraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.addLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#addLogConstraint-fffff9ad)

```scala
def addLogConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a log contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a log contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addLogConstraint(constraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.addPragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#addPragma-b53)

```scala
def addPragma(pragma: Pragma[Value]): Unit
```

Add a pragma to the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Add a pragma to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addPragma(pragma)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.allConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#allConditions-0)

```scala
def allConditions: List[(Condition, Contingency)]
```

All the conditions defined on this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Condition, Contingency)] ``.

Source contract/attributes: All the conditions defined on this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConditions
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.allConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#allConstraints-0)

```scala
def allConstraints: List[(Constraint, Contingency)]
```

The current soft constraints on the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Constraint, Contingency)] ``.

Source contract/attributes: The current soft constraints on the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConstraints
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.args ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#args-0)

```scala
def args: List[Element[_]]
```

Returns an empty list.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Returns an empty list. Attributes Inherited from: Atomic

Invocation template:

```scala
receiver.args
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.clearContext ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#clearContext-94c)

```scala
def clearContext(): Unit
```

Clears all the temporary elements associated with this element (all elements created in it's context).

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clears all the temporary elements associated with this element (all elements created in it's context). Attributes Inherited from: Element

Invocation template:

```scala
receiver.clearContext()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.condition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#condition-4ad)

```scala
def condition(value: Any): Boolean
```

Tests whether all the element's contingent conditions are satisfied for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether all the element's contingent conditions are satisfied for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.condition(value)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.conditionSatisfied ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#conditionSatisfied-0)

```scala
def conditionSatisfied: Boolean
```

Determines whether the condition on the element is satisfied by the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Determines whether the condition on the element is satisfied by the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.conditionSatisfied
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.constraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#constraint-fffffeac)

```scala
def constraint(value: Any): Double
```

Gets the result of all the element's contingent constraints for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Double ``.

Source contract/attributes: Gets the result of all the element's contingent constraints for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraint(value)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.constraintValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#constraintValue-0)

```scala
def constraintValue: Double
```

Determines the value of the constraint on the element applied to the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Determines the value of the constraint on the element applied to the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraintValue
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.context ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#context-0)

```scala
def context: List[Element[_]]
```

The elements on which the existence of this element depends.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: The elements on which the existence of this element depends. Attributes Inherited from: Element

Invocation template:

```scala
receiver.context
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.deactivate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#deactivate-94c)

```scala
def deactivate(): Unit
```

Deactivate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Deactivate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.deactivate()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.directContextContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#directContextContents-0)

```scala
def directContextContents: Set[Element[_]]
```

Returns the set of elements directly created in the context of this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the set of elements directly created in the context of this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.directContextContents
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.elementsIAmContingentOn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#elementsIAmContingentOn-0)

```scala
def elementsIAmContingentOn: Set[Element[_]]
```

Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value. Figaro takes care of handling all this under the hood. However, some algorithms may need to know which elements an element is contingent on. For example, sampling algorithms may need to sample those other elements first. This method is supplied to support this use case. Attributes Inherited from: Element

Invocation template:

```scala
receiver.elementsIAmContingentOn
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#flatMap-db8)

```scala
def flatMap[U](fn: Value => Element[U])(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => Element[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.flatMap[U](fn)(using name, collection)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#generate-94c)

```scala
final def generate(): Unit
```

First generate the randomness, then generate the value given the randomness. Store the results in randomness and value.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: First generate the randomness, then generate the value given the randomness. Store the results in randomness and value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.generate()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.intervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#intervene-fffff71f)

```scala
def intervene(v: Value): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` v: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.intervene(v)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.isCachable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#isCachable-0)

```scala
def isCachable: Boolean
```

The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isCachable
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.isTemporary ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#isTemporary-0)

```scala
def isTemporary: Boolean
```

Returns true if this element is temporary, that is, was created in the context of another element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Returns true if this element is temporary, that is, was created in the context of another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isTemporary
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.lower ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#lower-0)

```scala
def lower: Double
```

Exclusive lower bound of the range of the randomness of this element. Defaults to -Infinity. Must be strictly less than the upper bound.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Exclusive lower bound of the range of the randomness of this element. Defaults to -Infinity. Must be strictly less than the upper bound. Attributes Inherited from: NormalProposer

Invocation template:

```scala
receiver.lower
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#map-db8)

```scala
def map[U](fn: Value => U)(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => U ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.map[U](fn)(using name, collection)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.nextRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#nextRandomness-fffffb4c)

```scala
override def nextRandomness(oldRandomness: Randomness): (Randomness, Double, Double)
```

Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is:

Type parameters: none.

Parameters, list 1: `` oldRandomness: Randomness ``.

Returns: `` (Randomness, Double, Double) ``.

Source contract/attributes: Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is: P(new -> old) / P(old -> new) and the model probability ratio, which is: P(new) / P(old) By default, this implementation proposes a normally distributed randomness from the previous randomness, truncated to be within the appropriate range. The probability ratios returned are associated with the values of this element rather than the randomness. This is for the purpose of simulated annealing, since the most likely randomness is not necessarily the most likely value, depending on the form of the generateValue function. One can override this to only use normal proposals in certain special cases. Attributes Definition Classes NormalProposer -> HasDensity -> Element Inherited from: NormalProposer

Invocation template:

```scala
receiver.nextRandomness(oldRandomness)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.observe ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#observe-fffff71f)

```scala
def observe(observation: Value): Unit
```

Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated.

Type parameters: none.

Parameters, list 1: `` observation: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.observe(observation)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.pragmas ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#pragmas-0)

```scala
def pragmas: List[Pragma[Value]]
```

The pragmas attached to the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Pragma[Value]] ``.

Source contract/attributes: The pragmas attached to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.pragmas
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.removeConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#removeConditions-71e)

```scala
def removeConditions(contingency: Contingency = ...): Unit
```

Remove all conditions associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all conditions associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConditions(contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.removeConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#removeConstraints-71e)

```scala
def removeConstraints(contingency: Contingency = ...): Unit
```

Remove all constraints associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all constraints associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConstraints(contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.removePragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#removePragma-b53)

```scala
def removePragma(pragma: Pragma[Value]): Unit
```

Remove a pragma from the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Remove a pragma from the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removePragma(pragma)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.score ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#score-fffffdbf)

```scala
def score(oldValue: Value, newValue: Value): Double
```

Compute the constraints on the new value divided by the constraints on the old value.

Type parameters: none.

Parameters, list 1: `` oldValue: Value ``; `` newValue: Value ``.

Returns: `` Double ``.

Source contract/attributes: Compute the constraints on the new value divided by the constraints on the old value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.score(oldValue, newValue)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.set ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#set-fffff71f)

```scala
def set(newValue: Value): Unit
```

Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called.

Type parameters: none.

Parameters, list 1: `` newValue: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called. Attributes Inherited from: Element

Invocation template:

```scala
receiver.set(newValue)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.setCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#setCondition-fffff9ad)

```scala
def setCondition(newCondition: Condition, contingency: Contingency = ...): Unit
```

Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newCondition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setCondition(newCondition, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.setConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#setConstraint-fffff9ad)

```scala
def setConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.setLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#setLogConstraint-fffff9ad)

```scala
def setLogConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setLogConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.setRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#setRandomness-fffff71f)

```scala
def setRandomness(newRandomness: Randomness): Unit
```

Set the randomness of this element.

Type parameters: none.

Parameters, list 1: `` newRandomness: Randomness ``.

Returns: `` Unit ``.

Source contract/attributes: Set the randomness of this element. Will generate its value using the new randomness and propagate the effects to elements that depend on it without changing their randomness. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setRandomness(newRandomness)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.toNameString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#toNameString-0)

```scala
def toNameString: String
```

A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString. Attributes Inherited from: Element

Invocation template:

```scala
receiver.toNameString
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.unintervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#unintervene-94c)

```scala
def unintervene(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.unintervene()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.unobserve ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#unobserve-94c)

```scala
def unobserve(): Unit
```

Removes conditions on the element and allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Removes conditions on the element and allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unobserve()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.unset ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#unset-94c)

```scala
def unset(): Unit
```

Allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unset()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicNormal.upper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicNormal.html#upper-0)

```scala
def upper: Double
```

Exclusive upper bound of the range of the randomness of this element. Defaults to Infinity. Must be strictly greater than the lower bound.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Exclusive upper bound of the range of the randomness of this element. Defaults to Infinity. Must be strictly greater than the lower bound. Attributes Inherited from: NormalProposer

Invocation template:

```scala
receiver.upper
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.density ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#density-5b2)

```scala
def density(d: Double): Double
```

The probability density of a value.

Type parameters: none.

Parameters, list 1: `` d: Double ``.

Returns: `` Double ``.

Source contract/attributes: The probability density of a value. Attributes

Invocation template:

```scala
receiver.density(d)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.generateRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#generateRandomness-999)

```scala
def generateRandomness(): Randomness
```

Generate the randomness content.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Randomness ``.

Source contract/attributes: Generate the randomness content. Attributes

Invocation template:

```scala
receiver.generateRandomness()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.proposalScale ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#proposalScale-0)

```scala
def proposalScale: Double
```

The scale of the normally distributed proposal. This corresponds to the standard deviation of the proposal before truncation. If the randomness has finite variance, this should be less than or equal to its standard deviation. A good place to start is e.g. 20% of the standard deviation.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: The scale of the normally distributed proposal. This corresponds to the standard deviation of the proposal before truncation. If the randomness has finite variance, this should be less than or equal to its standard deviation. A good place to start is e.g. 20% of the standard deviation. Attributes

Invocation template:

```scala
receiver.proposalScale
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Any

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.!== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#!==-894)

```scala
def !==(that: Element[Value])(implicit universe: Universe): Neq[Double]
```

The element that tests inequality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Neq[Double] ``.

Source contract/attributes: The element that tests inequality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.!==(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#===-f71)

```scala
def ===(that: Value)(implicit universe: Universe): Apply1[Double, Boolean]
```

The element that tests whether the value of this element is equal to a particular value.

Type parameters: none.

Parameters, list 1: `` that: Value ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Apply1[Double, Boolean] ``.

Source contract/attributes: The element that tests whether the value of this element is equal to a particular value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#===-672)

```scala
def ===(that: Element[Value])(implicit universe: Universe): Eq[Double]
```

The element that tests equality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Eq[Double] ``.

Source contract/attributes: The element that tests equality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.activate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#activate-94c)

```scala
def activate(): Unit
```

Activate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Activate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.activate()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.addCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#addCondition-fffff9ad)

```scala
def addCondition(condition: Condition, contingency: Contingency = ...): Unit
```

Add the given condition to the existing conditions of the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` condition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add the given condition to the existing conditions of the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addCondition(condition, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.addConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#addConstraint-fffff9ad)

```scala
def addConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addConstraint(constraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.addLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#addLogConstraint-fffff9ad)

```scala
def addLogConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a log contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a log contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addLogConstraint(constraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.addPragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#addPragma-b53)

```scala
def addPragma(pragma: Pragma[Value]): Unit
```

Add a pragma to the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Add a pragma to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addPragma(pragma)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.allConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#allConditions-0)

```scala
def allConditions: List[(Condition, Contingency)]
```

All the conditions defined on this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Condition, Contingency)] ``.

Source contract/attributes: All the conditions defined on this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConditions
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.allConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#allConstraints-0)

```scala
def allConstraints: List[(Constraint, Contingency)]
```

The current soft constraints on the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Constraint, Contingency)] ``.

Source contract/attributes: The current soft constraints on the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConstraints
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.args ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#args-0)

```scala
def args: List[Element[_]]
```

Returns an empty list.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Returns an empty list. Attributes Inherited from: Atomic

Invocation template:

```scala
receiver.args
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.clearContext ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#clearContext-94c)

```scala
def clearContext(): Unit
```

Clears all the temporary elements associated with this element (all elements created in it's context).

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clears all the temporary elements associated with this element (all elements created in it's context). Attributes Inherited from: Element

Invocation template:

```scala
receiver.clearContext()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.condition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#condition-4ad)

```scala
def condition(value: Any): Boolean
```

Tests whether all the element's contingent conditions are satisfied for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether all the element's contingent conditions are satisfied for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.condition(value)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.conditionSatisfied ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#conditionSatisfied-0)

```scala
def conditionSatisfied: Boolean
```

Determines whether the condition on the element is satisfied by the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Determines whether the condition on the element is satisfied by the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.conditionSatisfied
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.constraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#constraint-fffffeac)

```scala
def constraint(value: Any): Double
```

Gets the result of all the element's contingent constraints for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Double ``.

Source contract/attributes: Gets the result of all the element's contingent constraints for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraint(value)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.constraintValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#constraintValue-0)

```scala
def constraintValue: Double
```

Determines the value of the constraint on the element applied to the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Determines the value of the constraint on the element applied to the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraintValue
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.context ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#context-0)

```scala
def context: List[Element[_]]
```

The elements on which the existence of this element depends.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: The elements on which the existence of this element depends. Attributes Inherited from: Element

Invocation template:

```scala
receiver.context
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.deactivate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#deactivate-94c)

```scala
def deactivate(): Unit
```

Deactivate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Deactivate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.deactivate()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.directContextContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#directContextContents-0)

```scala
def directContextContents: Set[Element[_]]
```

Returns the set of elements directly created in the context of this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the set of elements directly created in the context of this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.directContextContents
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.elementsIAmContingentOn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#elementsIAmContingentOn-0)

```scala
def elementsIAmContingentOn: Set[Element[_]]
```

Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value. Figaro takes care of handling all this under the hood. However, some algorithms may need to know which elements an element is contingent on. For example, sampling algorithms may need to sample those other elements first. This method is supplied to support this use case. Attributes Inherited from: Element

Invocation template:

```scala
receiver.elementsIAmContingentOn
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#flatMap-db8)

```scala
def flatMap[U](fn: Value => Element[U])(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => Element[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.flatMap[U](fn)(using name, collection)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#generate-94c)

```scala
final def generate(): Unit
```

First generate the randomness, then generate the value given the randomness. Store the results in randomness and value.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: First generate the randomness, then generate the value given the randomness. Store the results in randomness and value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.generate()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.generateValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#generateValue-5b2)

```scala
def generateValue(rand: Randomness): Value
```

A strictly monotone differentiable function defined on (lower, upper). Defaults to the identity function.

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` Value ``.

Source contract/attributes: A strictly monotone differentiable function defined on (lower, upper). Defaults to the identity function. Attributes Inherited from: NormalProposer

Invocation template:

```scala
receiver.generateValue(rand)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.generateValueDerivative ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#generateValueDerivative-5b2)

```scala
def generateValueDerivative(rand: Randomness): Double
```

The absolute value of the derivative of generateValue with respect to the randomness given. This is needed to compute a proposal density over the transformed randomness. Defaults to 1.0, corresponding to the case where generateValue is the identity function.

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` Double ``.

Source contract/attributes: The absolute value of the derivative of generateValue with respect to the randomness given. This is needed to compute a proposal density over the transformed randomness. Defaults to 1.0, corresponding to the case where generateValue is the identity function. Attributes Inherited from: NormalProposer

Invocation template:

```scala
receiver.generateValueDerivative(rand)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.intervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#intervene-fffff71f)

```scala
def intervene(v: Value): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` v: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.intervene(v)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.isCachable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#isCachable-0)

```scala
def isCachable: Boolean
```

The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isCachable
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.isTemporary ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#isTemporary-0)

```scala
def isTemporary: Boolean
```

Returns true if this element is temporary, that is, was created in the context of another element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Returns true if this element is temporary, that is, was created in the context of another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isTemporary
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#map-db8)

```scala
def map[U](fn: Value => U)(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => U ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.map[U](fn)(using name, collection)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.nextRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#nextRandomness-fffffb4c)

```scala
override def nextRandomness(oldRandomness: Randomness): (Randomness, Double, Double)
```

Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is:

Type parameters: none.

Parameters, list 1: `` oldRandomness: Randomness ``.

Returns: `` (Randomness, Double, Double) ``.

Source contract/attributes: Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is: P(new -> old) / P(old -> new) and the model probability ratio, which is: P(new) / P(old) By default, this implementation proposes a normally distributed randomness from the previous randomness, truncated to be within the appropriate range. The probability ratios returned are associated with the values of this element rather than the randomness. This is for the purpose of simulated annealing, since the most likely randomness is not necessarily the most likely value, depending on the form of the generateValue function. One can override this to only use normal proposals in certain special cases. Attributes Definition Classes NormalProposer -> HasDensity -> Element Inherited from: NormalProposer

Invocation template:

```scala
receiver.nextRandomness(oldRandomness)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.observe ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#observe-fffff71f)

```scala
def observe(observation: Value): Unit
```

Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated.

Type parameters: none.

Parameters, list 1: `` observation: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.observe(observation)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.pragmas ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#pragmas-0)

```scala
def pragmas: List[Pragma[Value]]
```

The pragmas attached to the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Pragma[Value]] ``.

Source contract/attributes: The pragmas attached to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.pragmas
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.removeConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#removeConditions-71e)

```scala
def removeConditions(contingency: Contingency = ...): Unit
```

Remove all conditions associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all conditions associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConditions(contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.removeConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#removeConstraints-71e)

```scala
def removeConstraints(contingency: Contingency = ...): Unit
```

Remove all constraints associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all constraints associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConstraints(contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.removePragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#removePragma-b53)

```scala
def removePragma(pragma: Pragma[Value]): Unit
```

Remove a pragma from the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Remove a pragma from the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removePragma(pragma)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.score ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#score-fffffdbf)

```scala
def score(oldValue: Value, newValue: Value): Double
```

Compute the constraints on the new value divided by the constraints on the old value.

Type parameters: none.

Parameters, list 1: `` oldValue: Value ``; `` newValue: Value ``.

Returns: `` Double ``.

Source contract/attributes: Compute the constraints on the new value divided by the constraints on the old value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.score(oldValue, newValue)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.set ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#set-fffff71f)

```scala
def set(newValue: Value): Unit
```

Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called.

Type parameters: none.

Parameters, list 1: `` newValue: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called. Attributes Inherited from: Element

Invocation template:

```scala
receiver.set(newValue)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.setCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#setCondition-fffff9ad)

```scala
def setCondition(newCondition: Condition, contingency: Contingency = ...): Unit
```

Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newCondition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setCondition(newCondition, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.setConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#setConstraint-fffff9ad)

```scala
def setConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.setLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#setLogConstraint-fffff9ad)

```scala
def setLogConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setLogConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.setRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#setRandomness-fffff71f)

```scala
def setRandomness(newRandomness: Randomness): Unit
```

Set the randomness of this element.

Type parameters: none.

Parameters, list 1: `` newRandomness: Randomness ``.

Returns: `` Unit ``.

Source contract/attributes: Set the randomness of this element. Will generate its value using the new randomness and propagate the effects to elements that depend on it without changing their randomness. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setRandomness(newRandomness)
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.toNameString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#toNameString-0)

```scala
def toNameString: String
```

A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString. Attributes Inherited from: Element

Invocation template:

```scala
receiver.toNameString
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.unintervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#unintervene-94c)

```scala
def unintervene(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.unintervene()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.unobserve ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#unobserve-94c)

```scala
def unobserve(): Unit
```

Removes conditions on the element and allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Removes conditions on the element and allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unobserve()
```

## `` com.cra.figaro.experimental.normalproposals.AtomicUniform.unset ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/AtomicUniform.html#unset-94c)

```scala
def unset(): Unit
```

Allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unset()
```

## `` com.cra.figaro.experimental.normalproposals.Beta.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/Beta$.html#apply-af1)

```scala
def apply(a: Double, b: Double)(implicit name: Name[Double], collection: ElementCollection): AtomicBeta
```

Create a Beta distribution in which the parameters are constants.

Type parameters: none.

Parameters, list 1: `` a: Double ``; `` b: Double ``.

Context parameters, list 2: `` name: Name[Double] ``; `` collection: ElementCollection ``.

Returns: `` AtomicBeta ``.

Source contract/attributes: Create a Beta distribution in which the parameters are constants. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.normalproposals.Beta.apply(a, b)(using name, collection)
```

## `` com.cra.figaro.experimental.normalproposals.Exponential.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/Exponential$.html#apply-fffff74b)

```scala
def apply(lambda: Double)(implicit name: Name[Double], collection: ElementCollection): AtomicExponential
```

Create an exponential distribution in which the parameter is a constant.

Type parameters: none.

Parameters, list 1: `` lambda: Double ``.

Context parameters, list 2: `` name: Name[Double] ``; `` collection: ElementCollection ``.

Returns: `` AtomicExponential ``.

Source contract/attributes: Create an exponential distribution in which the parameter is a constant. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.normalproposals.Exponential.apply(lambda)(using name, collection)
```

## `` com.cra.figaro.experimental.normalproposals.Gamma.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/Gamma$.html#apply-fffffec6)

```scala
def apply(k: Double, theta: Double = ...)(implicit name: Name[Double], collection: ElementCollection): AtomicGamma
```

Create a Gamma element in which both k and theta parameters are constants. Theta defaults to 1.

Type parameters: none.

Parameters, list 1: `` k: Double ``; `` theta: Double = ... ``.

Context parameters, list 2: `` name: Name[Double] ``; `` collection: ElementCollection ``.

Returns: `` AtomicGamma ``.

Source contract/attributes: Create a Gamma element in which both k and theta parameters are constants. Theta defaults to 1. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.normalproposals.Gamma.apply(k, theta)(using name, collection)
```

## `` com.cra.figaro.experimental.normalproposals.InverseGamma.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/InverseGamma$.html#apply-a58)

```scala
def apply(shape: Double, scale: Double)(implicit name: Name[Double], collection: ElementCollection): AtomicInverseGamma
```

Create an InverseGamma element.

Type parameters: none.

Parameters, list 1: `` shape: Double ``; `` scale: Double ``.

Context parameters, list 2: `` name: Name[Double] ``; `` collection: ElementCollection ``.

Returns: `` AtomicInverseGamma ``.

Source contract/attributes: Create an InverseGamma element. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.normalproposals.InverseGamma.apply(shape, scale)(using name, collection)
```

## `` com.cra.figaro.experimental.normalproposals.Normal.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/Normal$.html#apply-fffff5a8)

```scala
def apply(mean: Double, variance: Double)(implicit name: Name[Double], collection: ElementCollection): AtomicNormal
```

Create a normal distribution in which the mean and variance are constants.

Type parameters: none.

Parameters, list 1: `` mean: Double ``; `` variance: Double ``.

Context parameters, list 2: `` name: Name[Double] ``; `` collection: ElementCollection ``.

Returns: `` AtomicNormal ``.

Source contract/attributes: Create a normal distribution in which the mean and variance are constants. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.normalproposals.Normal.apply(mean, variance)(using name, collection)
```

## `` com.cra.figaro.experimental.normalproposals.Normal.density ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/Normal$.html#density-fffff564)

```scala
def density(mean: Double, stDev: Double)(d: Double): Double
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` mean: Double ``; `` stDev: Double ``.

Parameters, list 2: `` d: Double ``.

Returns: `` Double ``.

Invocation template:

```scala
com.cra.figaro.experimental.normalproposals.Normal.density(mean, stDev)(d)
```

## `` com.cra.figaro.experimental.normalproposals.Normal.density ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/Normal$.html#density-fffff8fd)

```scala
def density(mean: Double, variance: Double, normalizer: Double)(d: Double): Double
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` mean: Double ``; `` variance: Double ``; `` normalizer: Double ``.

Parameters, list 2: `` d: Double ``.

Returns: `` Double ``.

Invocation template:

```scala
com.cra.figaro.experimental.normalproposals.Normal.density(mean, variance, normalizer)(d)
```

## `` com.cra.figaro.experimental.normalproposals.Normal.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/Normal$.html#probability-fffff8fd)

```scala
def probability(mean: Double, stDev: Double)(lower: Double, upper: Double): Double
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` mean: Double ``; `` stDev: Double ``.

Parameters, list 2: `` lower: Double ``; `` upper: Double ``.

Returns: `` Double ``.

Invocation template:

```scala
com.cra.figaro.experimental.normalproposals.Normal.probability(mean, stDev)(lower, upper)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.proposalScale ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#proposalScale-0)

```scala
def proposalScale: Double
```

The scale of the normally distributed proposal. This corresponds to the standard deviation of the proposal before truncation. If the randomness has finite variance, this should be less than or equal to its standard deviation. A good place to start is e.g. 20% of the standard deviation.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: The scale of the normally distributed proposal. This corresponds to the standard deviation of the proposal before truncation. If the randomness has finite variance, this should be less than or equal to its standard deviation. A good place to start is e.g. 20% of the standard deviation. Attributes

Invocation template:

```scala
receiver.proposalScale
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.generateValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#generateValue-5b2)

```scala
def generateValue(rand: Randomness): Value
```

A strictly monotone differentiable function defined on (lower, upper). Defaults to the identity function.

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` Value ``.

Source contract/attributes: A strictly monotone differentiable function defined on (lower, upper). Defaults to the identity function. Attributes

Invocation template:

```scala
receiver.generateValue(rand)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.generateValueDerivative ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#generateValueDerivative-5b2)

```scala
def generateValueDerivative(rand: Randomness): Double
```

The absolute value of the derivative of generateValue with respect to the randomness given. This is needed to compute a proposal density over the transformed randomness. Defaults to 1.0, corresponding to the case where generateValue is the identity function.

Type parameters: none.

Parameters, list 1: `` rand: Randomness ``.

Returns: `` Double ``.

Source contract/attributes: The absolute value of the derivative of generateValue with respect to the randomness given. This is needed to compute a proposal density over the transformed randomness. Defaults to 1.0, corresponding to the case where generateValue is the identity function. Attributes

Invocation template:

```scala
receiver.generateValueDerivative(rand)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.lower ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#lower-0)

```scala
def lower: Double
```

Exclusive lower bound of the range of the randomness of this element. Defaults to -Infinity. Must be strictly less than the upper bound.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Exclusive lower bound of the range of the randomness of this element. Defaults to -Infinity. Must be strictly less than the upper bound. Attributes

Invocation template:

```scala
receiver.lower
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.nextRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#nextRandomness-fffffb4c)

```scala
override def nextRandomness(oldRandomness: Randomness): (Randomness, Double, Double)
```

Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is:

Type parameters: none.

Parameters, list 1: `` oldRandomness: Randomness ``.

Returns: `` (Randomness, Double, Double) ``.

Source contract/attributes: Generate the next randomness given the current randomness. Returns three values: The next randomness, the Metropolis-Hastings proposal probability ratio, which is: P(new -> old) / P(old -> new) and the model probability ratio, which is: P(new) / P(old) By default, this implementation proposes a normally distributed randomness from the previous randomness, truncated to be within the appropriate range. The probability ratios returned are associated with the values of this element rather than the randomness. This is for the purpose of simulated annealing, since the most likely randomness is not necessarily the most likely value, depending on the form of the generateValue function. One can override this to only use normal proposals in certain special cases. Attributes Definition Classes HasDensity -> Element

Invocation template:

```scala
receiver.nextRandomness(oldRandomness)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.upper ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#upper-0)

```scala
def upper: Double
```

Exclusive upper bound of the range of the randomness of this element. Defaults to Infinity. Must be strictly greater than the lower bound.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Exclusive upper bound of the range of the randomness of this element. Defaults to Infinity. Must be strictly greater than the lower bound. Attributes

Invocation template:

```scala
receiver.upper
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.!== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#!==-894)

```scala
def !==(that: Element[Value])(implicit universe: Universe): Neq[Double]
```

The element that tests inequality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Neq[Double] ``.

Source contract/attributes: The element that tests inequality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.!==(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#===-f71)

```scala
def ===(that: Value)(implicit universe: Universe): Apply1[Double, Boolean]
```

The element that tests whether the value of this element is equal to a particular value.

Type parameters: none.

Parameters, list 1: `` that: Value ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Apply1[Double, Boolean] ``.

Source contract/attributes: The element that tests whether the value of this element is equal to a particular value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.=== ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#===-672)

```scala
def ===(that: Element[Value])(implicit universe: Universe): Eq[Double]
```

The element that tests equality of this element with another element.

Type parameters: none.

Parameters, list 1: `` that: Element[Value] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` Eq[Double] ``.

Source contract/attributes: The element that tests equality of this element with another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.===(that)(using universe)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.activate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#activate-94c)

```scala
def activate(): Unit
```

Activate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Activate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.activate()
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.addCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#addCondition-fffff9ad)

```scala
def addCondition(condition: Condition, contingency: Contingency = ...): Unit
```

Add the given condition to the existing conditions of the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` condition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add the given condition to the existing conditions of the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addCondition(condition, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.addConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#addConstraint-fffff9ad)

```scala
def addConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addConstraint(constraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.addLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#addLogConstraint-fffff9ad)

```scala
def addLogConstraint(constraint: Constraint, contingency: Contingency = ...): Unit
```

Add a log contingent constraint to the element. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` constraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Add a log contingent constraint to the element. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addLogConstraint(constraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.addPragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#addPragma-b53)

```scala
def addPragma(pragma: Pragma[Value]): Unit
```

Add a pragma to the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Add a pragma to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.addPragma(pragma)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.allConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#allConditions-0)

```scala
def allConditions: List[(Condition, Contingency)]
```

All the conditions defined on this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Condition, Contingency)] ``.

Source contract/attributes: All the conditions defined on this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConditions
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.allConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#allConstraints-0)

```scala
def allConstraints: List[(Constraint, Contingency)]
```

The current soft constraints on the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[(Constraint, Contingency)] ``.

Source contract/attributes: The current soft constraints on the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.allConstraints
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.args ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#args-0)

```scala
def args: List[Element[_]]
```

Returns an empty list.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Returns an empty list. Attributes Inherited from: Atomic

Invocation template:

```scala
receiver.args
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.clearContext ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#clearContext-94c)

```scala
def clearContext(): Unit
```

Clears all the temporary elements associated with this element (all elements created in it's context).

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clears all the temporary elements associated with this element (all elements created in it's context). Attributes Inherited from: Element

Invocation template:

```scala
receiver.clearContext()
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.condition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#condition-4ad)

```scala
def condition(value: Any): Boolean
```

Tests whether all the element's contingent conditions are satisfied for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Boolean ``.

Source contract/attributes: Tests whether all the element's contingent conditions are satisfied for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.condition(value)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.conditionSatisfied ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#conditionSatisfied-0)

```scala
def conditionSatisfied: Boolean
```

Determines whether the condition on the element is satisfied by the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Determines whether the condition on the element is satisfied by the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.conditionSatisfied
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.constraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#constraint-fffffeac)

```scala
def constraint(value: Any): Double
```

Gets the result of all the element's contingent constraints for the given value.

Type parameters: none.

Parameters, list 1: `` value: Any ``.

Returns: `` Double ``.

Source contract/attributes: Gets the result of all the element's contingent constraints for the given value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraint(value)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.constraintValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#constraintValue-0)

```scala
def constraintValue: Double
```

Determines the value of the constraint on the element applied to the current value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Double ``.

Source contract/attributes: Determines the value of the constraint on the element applied to the current value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.constraintValue
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.context ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#context-0)

```scala
def context: List[Element[_]]
```

The elements on which the existence of this element depends.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: The elements on which the existence of this element depends. Attributes Inherited from: Element

Invocation template:

```scala
receiver.context
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.deactivate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#deactivate-94c)

```scala
def deactivate(): Unit
```

Deactivate the element in its universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Deactivate the element in its universe. Attributes Inherited from: Element

Invocation template:

```scala
receiver.deactivate()
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.directContextContents ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#directContextContents-0)

```scala
def directContextContents: Set[Element[_]]
```

Returns the set of elements directly created in the context of this element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the set of elements directly created in the context of this element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.directContextContents
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.elementsIAmContingentOn ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#elementsIAmContingentOn-0)

```scala
def elementsIAmContingentOn: Set[Element[_]]
```

Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the elements that this element is contingent on. These are elements that are required to have a certain value for a condition or constraint to be relevant to this element. The contingency is required because conditions and constraints can be applied to references that are uncertain. Every possible element that could be pointed to by a reference must be given the condition or constraint, but the condition or constraint only applies if the elements earlier in the reference have the required value. Figaro takes care of handling all this under the hood. However, some algorithms may need to know which elements an element is contingent on. For example, sampling algorithms may need to sample those other elements first. This method is supplied to support this use case. Attributes Inherited from: Element

Invocation template:

```scala
receiver.elementsIAmContingentOn
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.flatMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#flatMap-db8)

```scala
def flatMap[U](fn: Value => Element[U])(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => Element[U] ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.flatMap[U](fn)(using name, collection)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.generate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#generate-94c)

```scala
final def generate(): Unit
```

First generate the randomness, then generate the value given the randomness. Store the results in randomness and value.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: First generate the randomness, then generate the value given the randomness. Store the results in randomness and value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.generate()
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.intervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#intervene-fffff71f)

```scala
def intervene(v: Value): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` v: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.intervene(v)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.isCachable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#isCachable-0)

```scala
def isCachable: Boolean
```

The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: The cacheability of the element. Chains create caches of their parent values, and it is useful to know when these values can be effectively cached and reused. In general, continuous distributions are not cacheable. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isCachable
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.isTemporary ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#isTemporary-0)

```scala
def isTemporary: Boolean
```

Returns true if this element is temporary, that is, was created in the context of another element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Returns true if this element is temporary, that is, was created in the context of another element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.isTemporary
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#map-db8)

```scala
def map[U](fn: Value => U)(implicit name: Name[U], collection: ElementCollection): Element[U]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` U ``.

Parameters, list 1: `` fn: Value => U ``.

Context parameters, list 2: `` name: Name[U] ``; `` collection: ElementCollection ``.

Returns: `` Element[U] ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.map[U](fn)(using name, collection)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.observe ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#observe-fffff71f)

```scala
def observe(observation: Value): Unit
```

Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated.

Type parameters: none.

Parameters, list 1: `` observation: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Condition the element by observing a particular value. Propagates the effect to dependent elements and ensures that no other value for the element can be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.observe(observation)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.pragmas ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#pragmas-0)

```scala
def pragmas: List[Pragma[Value]]
```

The pragmas attached to the element.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Pragma[Value]] ``.

Source contract/attributes: The pragmas attached to the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.pragmas
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.removeConditions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#removeConditions-71e)

```scala
def removeConditions(contingency: Contingency = ...): Unit
```

Remove all conditions associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all conditions associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConditions(contingency)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.removeConstraints ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#removeConstraints-71e)

```scala
def removeConstraints(contingency: Contingency = ...): Unit
```

Remove all constraints associated with the given contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Remove all constraints associated with the given contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removeConstraints(contingency)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.removePragma ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#removePragma-b53)

```scala
def removePragma(pragma: Pragma[Value]): Unit
```

Remove a pragma from the element.

Type parameters: none.

Parameters, list 1: `` pragma: Pragma[Value] ``.

Returns: `` Unit ``.

Source contract/attributes: Remove a pragma from the element. Attributes Inherited from: Element

Invocation template:

```scala
receiver.removePragma(pragma)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.score ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#score-fffffdbf)

```scala
def score(oldValue: Value, newValue: Value): Double
```

Compute the constraints on the new value divided by the constraints on the old value.

Type parameters: none.

Parameters, list 1: `` oldValue: Value ``; `` newValue: Value ``.

Returns: `` Double ``.

Source contract/attributes: Compute the constraints on the new value divided by the constraints on the old value. Attributes Inherited from: Element

Invocation template:

```scala
receiver.score(oldValue, newValue)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.set ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#set-fffff71f)

```scala
def set(newValue: Value): Unit
```

Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called.

Type parameters: none.

Parameters, list 1: `` newValue: Value ``.

Returns: `` Unit ``.

Source contract/attributes: Set the value of this element and propagate the effects to elements that depend on it without changing their randomness. Also disallows the value of the element to change until unobserve or unset is called. Attributes Inherited from: Element

Invocation template:

```scala
receiver.set(newValue)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.setCondition ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#setCondition-fffff9ad)

```scala
def setCondition(newCondition: Condition, contingency: Contingency = ...): Unit
```

Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newCondition: Condition ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the condition associated with the contingency. Removes previous conditions associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setCondition(newCondition, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.setConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#setConstraint-fffff9ad)

```scala
def setConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.setLogConstraint ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#setLogConstraint-fffff9ad)

```scala
def setLogConstraint(newConstraint: Constraint, contingency: Contingency = ...): Unit
```

Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty.

Type parameters: none.

Parameters, list 1: `` newConstraint: Constraint ``; `` contingency: Contingency = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Set the log constraint associated with the contingency. Removes previous constraints associated with the contingency. By default, the contingency is empty. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setLogConstraint(newConstraint, contingency)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.setRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#setRandomness-fffff71f)

```scala
def setRandomness(newRandomness: Randomness): Unit
```

Set the randomness of this element.

Type parameters: none.

Parameters, list 1: `` newRandomness: Randomness ``.

Returns: `` Unit ``.

Source contract/attributes: Set the randomness of this element. Will generate its value using the new randomness and propagate the effects to elements that depend on it without changing their randomness. Attributes Inherited from: Element

Invocation template:

```scala
receiver.setRandomness(newRandomness)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.toNameString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#toNameString-0)

```scala
def toNameString: String
```

A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: A string that is the element's name, if it has a non-empty one, otherwise the result of the element's toString. Attributes Inherited from: Element

Invocation template:

```scala
receiver.toNameString
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.unintervene ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#unintervene-94c)

```scala
def unintervene(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Element

Invocation template:

```scala
receiver.unintervene()
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.unobserve ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#unobserve-94c)

```scala
def unobserve(): Unit
```

Removes conditions on the element and allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Removes conditions on the element and allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unobserve()
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.unset ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#unset-94c)

```scala
def unset(): Unit
```

Allows different values of the element to be generated.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Allows different values of the element to be generated. Attributes Inherited from: Element

Invocation template:

```scala
receiver.unset()
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.density ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#density-fffffeac)

```scala
def density(t: Double): Double
```

The probability density of a value.

Type parameters: none.

Parameters, list 1: `` t: Double ``.

Returns: `` Double ``.

Source contract/attributes: The probability density of a value. Attributes Inherited from: HasDensity

Invocation template:

```scala
receiver.density(t)
```

## `` com.cra.figaro.experimental.normalproposals.NormalProposer.generateRandomness ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/NormalProposer.html#generateRandomness-993)

```scala
def generateRandomness(): Randomness
```

Generate the randomness content.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Randomness ``.

Source contract/attributes: Generate the randomness content. Attributes Inherited from: Element

Invocation template:

```scala
receiver.generateRandomness()
```

## `` com.cra.figaro.experimental.normalproposals.Uniform.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/experimental/normalproposals/Uniform$.html#apply-fffff913)

```scala
def apply(lower: Double, upper: Double)(implicit name: Name[Double], collection: ElementCollection): AtomicUniform
```

Create a continuous uniform distribution in which the parameters are constants.

Type parameters: none.

Parameters, list 1: `` lower: Double ``; `` upper: Double ``.

Context parameters, list 2: `` name: Name[Double] ``; `` collection: ElementCollection ``.

Returns: `` AtomicUniform ``.

Source contract/attributes: Create a continuous uniform distribution in which the parameters are constants. Attributes

Invocation template:

```scala
com.cra.figaro.experimental.normalproposals.Uniform.apply(lower, upper)(using name, collection)
```
