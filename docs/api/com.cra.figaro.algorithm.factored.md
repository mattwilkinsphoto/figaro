<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.algorithm.factored

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.algorithm.factored.AbstractFactor.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/AbstractFactor.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.algorithm.factored.AbstractFactor.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/AbstractFactor.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.algorithm.factored.ConstantDensityEstimator.getDensity ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ConstantDensityEstimator.html#getDensity-378)

```scala
def getDensity(pt: Any, samples: List[(Double, Any)]): Double
```

Get's the density of a point from a list of weighted samples

Type parameters: none.

Parameters, list 1: `` pt: Any ``; `` samples: List[(Double, Any)] ``.

Returns: `` Double ``.

Source contract/attributes: Get's the density of a point from a list of weighted samples Attributes

Invocation template:

```scala
receiver.getDensity(pt, samples)
```

## `` com.cra.figaro.algorithm.factored.DensityEstimator.getDensity ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/DensityEstimator.html#getDensity-378)

```scala
def getDensity(pt: Any, samples: List[(Double, Any)]): Double
```

Get's the density of a point from a list of weighted samples

Type parameters: none.

Parameters, list 1: `` pt: Any ``; `` samples: List[(Double, Any)] ``.

Returns: `` Double ``.

Source contract/attributes: Get's the density of a point from a list of weighted samples Attributes

Invocation template:

```scala
receiver.getDensity(pt, samples)
```

## `` com.cra.figaro.algorithm.factored.DensityException.addSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/DensityException.html#addSuppressed-4b8)

```scala
final def addSuppressed(x$0: Throwable): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.addSuppressed(x$0)
```

## `` com.cra.figaro.algorithm.factored.DensityException.fillInStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/DensityException.html#fillInStackTrace-26c)

```scala
def fillInStackTrace(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.fillInStackTrace()
```

## `` com.cra.figaro.algorithm.factored.DensityException.getCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/DensityException.html#getCause-26c)

```scala
def getCause(): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getCause()
```

## `` com.cra.figaro.algorithm.factored.DensityException.getLocalizedMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/DensityException.html#getLocalizedMessage-665)

```scala
def getLocalizedMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getLocalizedMessage()
```

## `` com.cra.figaro.algorithm.factored.DensityException.getMessage ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/DensityException.html#getMessage-665)

```scala
def getMessage(): String
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getMessage()
```

## `` com.cra.figaro.algorithm.factored.DensityException.getStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/DensityException.html#getStackTrace-fffffa6d)

```scala
def getStackTrace(): Array[StackTraceElement]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[StackTraceElement] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getStackTrace()
```

## `` com.cra.figaro.algorithm.factored.DensityException.getSuppressed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/DensityException.html#getSuppressed-fffff2ce)

```scala
final def getSuppressed(): Array[Throwable]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Array[Throwable] ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.getSuppressed()
```

## `` com.cra.figaro.algorithm.factored.DensityException.initCause ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/DensityException.html#initCause-fffff080)

```scala
def initCause(x$0: Throwable): Throwable
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Throwable ``.

Returns: `` Throwable ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.initCause(x$0)
```

## `` com.cra.figaro.algorithm.factored.DensityException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/DensityException.html#printStackTrace-fffff230)

```scala
def printStackTrace(x$0: PrintWriter): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintWriter ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.algorithm.factored.DensityException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/DensityException.html#printStackTrace-fffff0fd)

```scala
def printStackTrace(x$0: PrintStream): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: PrintStream ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace(x$0)
```

## `` com.cra.figaro.algorithm.factored.DensityException.printStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/DensityException.html#printStackTrace-94c)

```scala
def printStackTrace(): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.printStackTrace()
```

## `` com.cra.figaro.algorithm.factored.DensityException.setStackTrace ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/DensityException.html#setStackTrace-f79)

```scala
def setStackTrace(x$0: Array[StackTraceElement]): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` x$0: Array[StackTraceElement] ``.

Returns: `` Unit ``.

Source contract/attributes: Attributes Inherited from: Throwable

Invocation template:

```scala
receiver.setStackTrace(x$0)
```

## `` com.cra.figaro.algorithm.factored.DensityException.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/DensityException.html#toString-665)

```scala
def toString(): String
```

Returns a string representation of the object.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Inherited from: Throwable

Invocation template:

```scala
receiver.toString()
```

## `` com.cra.figaro.algorithm.factored.DoubleDensityEstimator.getDensity ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/DoubleDensityEstimator.html#getDensity-bb2)

```scala
def getDensity(pt: Double, samples: List[(Double, Double)]): Double
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` pt: Double ``; `` samples: List[(Double, Double)] ``.

Returns: `` Double ``.

Invocation template:

```scala
receiver.getDensity(pt, samples)
```

## `` com.cra.figaro.algorithm.factored.DoubleDensityEstimator.getDensity ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/DoubleDensityEstimator.html#getDensity-378)

```scala
def getDensity(pt: Any, samples: List[(Double, Any)]): Double
```

Get's the density of a point from a list of weighted samples

Type parameters: none.

Parameters, list 1: `` pt: Any ``; `` samples: List[(Double, Any)] ``.

Returns: `` Double ``.

Source contract/attributes: Get's the density of a point from a list of weighted samples Attributes Inherited from: DensityEstimator

Invocation template:

```scala
receiver.getDensity(pt, samples)
```

## `` com.cra.figaro.algorithm.factored.FactoredAlgorithm.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/FactoredAlgorithm.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[T]]
```

All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[T]] ``.

Source contract/attributes: All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes. Attributes

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.algorithm.factored.FactoredAlgorithm.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/FactoredAlgorithm.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.factored.FactoredAlgorithm.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/FactoredAlgorithm.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.factored.FactoredAlgorithm.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/FactoredAlgorithm.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.factored.FactoredAlgorithm.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/FactoredAlgorithm.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.factored.FactoredAlgorithm.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/FactoredAlgorithm.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.factored.FactoredAlgorithm.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/FactoredAlgorithm.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.factored.FactoredAlgorithm.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/FactoredAlgorithm.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.factored.FactoredAlgorithm.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/FactoredAlgorithm.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.factored.IntDensityEstimator.getDensity ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/IntDensityEstimator.html#getDensity-fffff964)

```scala
def getDensity(pt: Int, samples: List[(Double, Int)]): Double
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` pt: Int ``; `` samples: List[(Double, Int)] ``.

Returns: `` Double ``.

Invocation template:

```scala
receiver.getDensity(pt, samples)
```

## `` com.cra.figaro.algorithm.factored.IntDensityEstimator.getDensity ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/IntDensityEstimator.html#getDensity-378)

```scala
def getDensity(pt: Any, samples: List[(Double, Any)]): Double
```

Get's the density of a point from a list of weighted samples

Type parameters: none.

Parameters, list 1: `` pt: Any ``; `` samples: List[(Double, Any)] ``.

Returns: `` Double ``.

Source contract/attributes: Get's the density of a point from a list of weighted samples Attributes Inherited from: DensityEstimator

Invocation template:

```scala
receiver.getDensity(pt, samples)
```

## `` com.cra.figaro.algorithm.factored.MPEVariableElimination.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/MPEVariableElimination$.html#apply-fffffd2d)

```scala
def apply()(implicit universe: Universe): MPEVariableElimination
```

Create a most probable explanation computer using variable elimination with the given target query variables in the current default universe.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Context parameters, list 2: `` universe: Universe ``.

Returns: `` MPEVariableElimination ``.

Source contract/attributes: Create a most probable explanation computer using variable elimination with the given target query variables in the current default universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.MPEVariableElimination.apply()(using universe)
```

## `` com.cra.figaro.algorithm.factored.MPEVariableElimination.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/MPEVariableElimination$.html#apply-fffff37f)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])])(implicit universe: Universe): MPEVariableElimination
```

Create a most probable explanation computer using variable elimination with the given target query variables and using the given dependent universes in the current default universe.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` MPEVariableElimination ``.

Source contract/attributes: Create a most probable explanation computer using variable elimination with the given target query variables and using the given dependent universes in the current default universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.MPEVariableElimination.apply(dependentUniverses)(using universe)
```

## `` com.cra.figaro.algorithm.factored.MPEVariableElimination.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/MPEVariableElimination$.html#apply-72d)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double)(implicit universe: Universe): MPEVariableElimination
```

Create a most probable explanation computer using variable elimination with the given target query variables and using the given dependent universes in the current default universe. Use the given dependent algorithm function to determine the algorithm to use to compute probability of evidence in each dependent universe.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` MPEVariableElimination ``.

Source contract/attributes: Create a most probable explanation computer using variable elimination with the given target query variables and using the given dependent universes in the current default universe. Use the given dependent algorithm function to determine the algorithm to use to compute probability of evidence in each dependent universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.MPEVariableElimination.apply(dependentUniverses, dependentAlgorithm)(using universe)
```

## `` com.cra.figaro.algorithm.factored.MPEVariableElimination.finish ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/MPEVariableElimination.html#finish-fffff214)

```scala
def finish(factorsAfterElimination: MultiSet[Factor[Double]], eliminationOrder: List[Variable[_]]): Unit
```

All implementation of variable elimination must specify what to do after variables have been eliminated.

Type parameters: none.

Parameters, list 1: `` factorsAfterElimination: MultiSet[Factor[Double]] ``; `` eliminationOrder: List[Variable[_]] ``.

Returns: `` Unit ``.

Source contract/attributes: All implementation of variable elimination must specify what to do after variables have been eliminated. Attributes

Invocation template:

```scala
receiver.finish(factorsAfterElimination, eliminationOrder)
```

## `` com.cra.figaro.algorithm.factored.MPEVariableElimination.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/MPEVariableElimination.html#getFactors-eb2)

```scala
override def getFactors(allElements: List[Element[_]], targetElements: List[Element[_]], upper: Boolean = ...): List[Factor[Double]]
```

All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes.

Type parameters: none.

Parameters, list 1: `` allElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upper: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes. Attributes Definition Classes ProbabilisticVariableElimination -> FactoredAlgorithm

Invocation template:

```scala
receiver.getFactors(allElements, targetElements, upper)
```

## `` com.cra.figaro.algorithm.factored.MPEVariableElimination.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/MPEVariableElimination.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns the most likely value for the target element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns the most likely value for the target element. Attributes

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.algorithm.factored.MPEVariableElimination.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/MPEVariableElimination.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.factored.MPEVariableElimination.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/MPEVariableElimination.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.factored.MPEVariableElimination.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/MPEVariableElimination.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.factored.MPEVariableElimination.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/MPEVariableElimination.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.factored.MPEVariableElimination.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/MPEVariableElimination.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.factored.MPEVariableElimination.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/MPEVariableElimination.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.factored.MPEVariableElimination.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/MPEVariableElimination.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Inherited from: VariableElimination

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.factored.MPEVariableElimination.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/MPEVariableElimination.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.factored.MPEVariableElimination.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/MPEVariableElimination.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.factored.ParticleGenerator.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ParticleGenerator$.html#apply-fffff499)

```scala
def apply(univ: Universe, de: DensityEstimator, numSamplesFromAtomics: Int, maxNumSamplesAtChain: Int): ParticleGenerator
```

Create a new particle generator for the given universe, using the given density estimatore, number of argument samples and total number of samples

Type parameters: none.

Parameters, list 1: `` univ: Universe ``; `` de: DensityEstimator ``; `` numSamplesFromAtomics: Int ``; `` maxNumSamplesAtChain: Int ``.

Returns: `` ParticleGenerator ``.

Source contract/attributes: Create a new particle generator for the given universe, using the given density estimatore, number of argument samples and total number of samples Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.ParticleGenerator.apply(univ, de, numSamplesFromAtomics, maxNumSamplesAtChain)
```

## `` com.cra.figaro.algorithm.factored.ParticleGenerator.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ParticleGenerator$.html#apply-fffffde7)

```scala
def apply(univ: Universe): ParticleGenerator
```

Create a new particle generate for a universe using a constant density estimator and default samples

Type parameters: none.

Parameters, list 1: `` univ: Universe ``.

Returns: `` ParticleGenerator ``.

Source contract/attributes: Create a new particle generate for a universe using a constant density estimator and default samples Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.ParticleGenerator.apply(univ)
```

## `` com.cra.figaro.algorithm.factored.ParticleGenerator.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ParticleGenerator$.html#apply-a07)

```scala
def apply(univ: Universe, numSamplesFromAtomics: Int, maxNumSamplesAtChain: Int): ParticleGenerator
```

Create a new particle generator for a universe using a constant density estimator and the number of argument samples and total number of samples

Type parameters: none.

Parameters, list 1: `` univ: Universe ``; `` numSamplesFromAtomics: Int ``; `` maxNumSamplesAtChain: Int ``.

Returns: `` ParticleGenerator ``.

Source contract/attributes: Create a new particle generator for a universe using a constant density estimator and the number of argument samples and total number of samples Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.ParticleGenerator.apply(univ, numSamplesFromAtomics, maxNumSamplesAtChain)
```

## `` com.cra.figaro.algorithm.factored.ParticleGenerator.clear ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ParticleGenerator$.html#clear-9dc)

```scala
def clear(univ: Universe): Map[Universe, ParticleGenerator]
```

Clear the particle generate for a universe

Type parameters: none.

Parameters, list 1: `` univ: Universe ``.

Returns: `` Map[Universe, ParticleGenerator] ``.

Source contract/attributes: Clear the particle generate for a universe Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.ParticleGenerator.clear(univ)
```

## `` com.cra.figaro.algorithm.factored.ParticleGenerator.clear ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ParticleGenerator$.html#clear-94c)

```scala
def clear(): Unit
```

Clear all particle generators

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clear all particle generators Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.ParticleGenerator.clear()
```

## `` com.cra.figaro.algorithm.factored.ParticleGenerator.exists ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ParticleGenerator$.html#exists-450)

```scala
def exists(univ: Universe): Boolean
```

Check if a particle generate exists for this universe

Type parameters: none.

Parameters, list 1: `` univ: Universe ``.

Returns: `` Boolean ``.

Source contract/attributes: Check if a particle generate exists for this universe Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.ParticleGenerator.exists(univ)
```

## `` com.cra.figaro.algorithm.factored.ParticleGenerator.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ParticleGenerator.html#apply-bd2)

```scala
def apply[T](elem: Element[T]): List[(Double, T)]
```

Retrieves the stored samples for an element if there are any. Otherwise, this takes the default number of samples, then stores the and returns result.

Type parameters: `` T ``.

Parameters, list 1: `` elem: Element[T] ``.

Returns: `` List[(Double, T)] ``.

Source contract/attributes: Retrieves the stored samples for an element if there are any. Otherwise, this takes the default number of samples, then stores the and returns result. Attributes

Invocation template:

```scala
receiver.apply[T](elem)
```

## `` com.cra.figaro.algorithm.factored.ParticleGenerator.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ParticleGenerator.html#apply-8cb)

```scala
def apply[T](elem: Element[T], numSamples: Int): List[(Double, T)]
```

Retrieves the samples for an element using the indicated number of samples. If the stored result has at least as many samples as numSamples, this returns the stored result. Otherwise, this takes additional samples. It then stores and returns the combined result.

Type parameters: `` T ``.

Parameters, list 1: `` elem: Element[T] ``; `` numSamples: Int ``.

Returns: `` List[(Double, T)] ``.

Source contract/attributes: Retrieves the samples for an element using the indicated number of samples. If the stored result has at least as many samples as numSamples, this returns the stored result. Otherwise, this takes additional samples. It then stores and returns the combined result. Attributes

Invocation template:

```scala
receiver.apply[T](elem, numSamples)
```

## `` com.cra.figaro.algorithm.factored.ParticleGenerator.clear ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ParticleGenerator.html#clear-94c)

```scala
def clear(): Unit
```

Clears all of the samples for elements in this sampler

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Clears all of the samples for elements in this sampler Attributes

Invocation template:

```scala
receiver.clear()
```

## `` com.cra.figaro.algorithm.factored.ParticleGenerator.resample ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ParticleGenerator.html#resample-9b6)

```scala
def resample(elem: Element[_], beliefs: List[(Double, )], oldMessages: List[List[(Double, )]], proposalVariance: Double): Unit
```

Resample and update the element from the indicated beliefs beliefs/oldMessages = (Probability, Value)

Type parameters: none.

Parameters, list 1: `` elem: Element[_] ``; `` beliefs: List[(Double, )] ``; `` oldMessages: List[List[(Double, )]] ``; `` proposalVariance: Double ``.

Returns: `` Unit ``.

Source contract/attributes: Resample and update the element from the indicated beliefs beliefs/oldMessages = (Probability, Value) Attributes

Invocation template:

```scala
receiver.resample(elem, beliefs, oldMessages, proposalVariance)
```

## `` com.cra.figaro.algorithm.factored.ParticleGenerator.sampledElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ParticleGenerator.html#sampledElements-fffffa2e)

```scala
def sampledElements(): Set[Element[_]]
```

Returns the set of sampled elements contained in this sampler

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the set of sampled elements contained in this sampler Attributes

Invocation template:

```scala
receiver.sampledElements()
```

## `` com.cra.figaro.algorithm.factored.ParticleGenerator.samplesTaken ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ParticleGenerator.html#samplesTaken-fffff376)

```scala
def samplesTaken(elem: Element[_]): Int
```

Returns the number of samples that have been taken for the given element

Type parameters: none.

Parameters, list 1: `` elem: Element[_] ``.

Returns: `` Int ``.

Source contract/attributes: Returns the number of samples that have been taken for the given element Attributes

Invocation template:

```scala
receiver.samplesTaken(elem)
```

## `` com.cra.figaro.algorithm.factored.ParticleGenerator.update ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ParticleGenerator.html#update-68)

```scala
def update(elem: Element[_], numSamples: Int, samples: List[(Double, )]): Unit
```

Updates the samples for an element

Type parameters: none.

Parameters, list 1: `` elem: Element[_] ``; `` numSamples: Int ``; `` samples: List[(Double, )] ``.

Returns: `` Unit ``.

Source contract/attributes: Updates the samples for an element Attributes

Invocation template:

```scala
receiver.update(elem, numSamples, samples)
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#computeDistribution-f66)

```scala
def computeDistribution[T](target: Element[T]): LazyList[(Double, T)]
```

Computes the normalized distribution over a single target element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Computes the normalized distribution over a single target element. Attributes

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#computeExpectation-c2a)

```scala
def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Computes the expectation of a given function for single target element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Computes the expectation of a given function for single target element. Attributes

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.finish ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#finish-fffff214)

```scala
def finish(factorsAfterElimination: MultiSet[Factor[Double]], eliminationOrder: List[Variable[_]]): Unit
```

All implementation of variable elimination must specify what to do after variables have been eliminated.

Type parameters: none.

Parameters, list 1: `` factorsAfterElimination: MultiSet[Factor[Double]] ``; `` eliminationOrder: List[Variable[_]] ``.

Returns: `` Unit ``.

Source contract/attributes: All implementation of variable elimination must specify what to do after variables have been eliminated. Attributes

Invocation template:

```scala
receiver.finish(factorsAfterElimination, eliminationOrder)
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#distribution-fffff1a2)

```scala
def distribution[T](target: Element[T]): LazyList[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#getFactors-eb2)

```scala
def getFactors(allElements: List[Element[_]], targetElements: List[Element[_]], upper: Boolean = ...): List[Factor[Double]]
```

All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes.

Type parameters: none.

Parameters, list 1: `` allElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upper: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes. Attributes Inherited from: ProbabilisticVariableElimination

Invocation template:

```scala
receiver.getFactors(allElements, targetElements, upper)
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Inherited from: VariableElimination

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.starterElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#starterElements-0)

```scala
def starterElements: List[Element[_]]
```

Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables. Attributes Inherited from: VariableElimination

Invocation template:

```scala
receiver.starterElements
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.factored.ProbQueryVariableElimination.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbQueryVariableElimination.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.factored.ProbabilisticVariableElimination.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbabilisticVariableElimination.html#getFactors-eb2)

```scala
def getFactors(allElements: List[Element[_]], targetElements: List[Element[_]], upper: Boolean = ...): List[Factor[Double]]
```

All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes.

Type parameters: none.

Parameters, list 1: `` allElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upper: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes. Attributes

Invocation template:

```scala
receiver.getFactors(allElements, targetElements, upper)
```

## `` com.cra.figaro.algorithm.factored.ProbabilisticVariableElimination.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbabilisticVariableElimination.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.factored.ProbabilisticVariableElimination.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbabilisticVariableElimination.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.factored.ProbabilisticVariableElimination.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbabilisticVariableElimination.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.factored.ProbabilisticVariableElimination.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbabilisticVariableElimination.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.factored.ProbabilisticVariableElimination.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbabilisticVariableElimination.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.factored.ProbabilisticVariableElimination.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbabilisticVariableElimination.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.factored.ProbabilisticVariableElimination.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbabilisticVariableElimination.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Inherited from: VariableElimination

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.factored.ProbabilisticVariableElimination.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbabilisticVariableElimination.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.factored.ProbabilisticVariableElimination.starterElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbabilisticVariableElimination.html#starterElements-0)

```scala
def starterElements: List[Element[_]]
```

Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables. Attributes Inherited from: VariableElimination

Invocation template:

```scala
receiver.starterElements
```

## `` com.cra.figaro.algorithm.factored.ProbabilisticVariableElimination.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbabilisticVariableElimination.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.factored.ProbabilisticVariableElimination.finish ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/ProbabilisticVariableElimination.html#finish-fffff214)

```scala
def finish(factorsAfterElimination: MultiSet[Factor[Double]], eliminationOrder: List[Variable[_]]): Unit
```

All implementation of variable elimination must specify what to do after variables have been eliminated.

Type parameters: none.

Parameters, list 1: `` factorsAfterElimination: MultiSet[Factor[Double]] ``; `` eliminationOrder: List[Variable[_]] ``.

Returns: `` Unit ``.

Source contract/attributes: All implementation of variable elimination must specify what to do after variables have been eliminated. Attributes Inherited from: VariableElimination

Invocation template:

```scala
receiver.finish(factorsAfterElimination, eliminationOrder)
```

## `` com.cra.figaro.algorithm.factored.SufficientStatisticsVariableElimination.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/SufficientStatisticsVariableElimination$.html#apply-11a)

```scala
def apply(parameterMap: Map[Parameter[_], Seq[Double]])(implicit universe: Universe): SufficientStatisticsVariableElimination
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` parameterMap: Map[Parameter[_], Seq[Double]] ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` SufficientStatisticsVariableElimination ``.

Invocation template:

```scala
com.cra.figaro.algorithm.factored.SufficientStatisticsVariableElimination.apply(parameterMap)(using universe)
```

## `` com.cra.figaro.algorithm.factored.SufficientStatisticsVariableElimination.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/SufficientStatisticsVariableElimination.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Definition Classes Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.factored.SufficientStatisticsVariableElimination.finish ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/SufficientStatisticsVariableElimination.html#finish-fffff214)

```scala
def finish(factorsAfterElimination: MultiSet[Factor[(Double, Map[Parameter[_], Seq[Double]])]], eliminationOrder: List[Variable[_]]): Unit
```

All implementation of variable elimination must specify what to do after variables have been eliminated.

Type parameters: none.

Parameters, list 1: `` factorsAfterElimination: MultiSet[Factor[(Double, Map[Parameter[_], Seq[Double]])]] ``; `` eliminationOrder: List[Variable[_]] ``.

Returns: `` Unit ``.

Source contract/attributes: All implementation of variable elimination must specify what to do after variables have been eliminated. Attributes

Invocation template:

```scala
receiver.finish(factorsAfterElimination, eliminationOrder)
```

## `` com.cra.figaro.algorithm.factored.SufficientStatisticsVariableElimination.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/SufficientStatisticsVariableElimination.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upper: Boolean = ...): List[Factor[(Double, Map[Parameter[_], Seq[Double]])]]
```

Particular implementations of probability of evidence algorithms must define the following method.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upper: Boolean = ... ``.

Returns: `` List[Factor[(Double, Map[Parameter[_], Seq[Double]])]] ``.

Source contract/attributes: Particular implementations of probability of evidence algorithms must define the following method. Attributes

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upper)
```

## `` com.cra.figaro.algorithm.factored.SufficientStatisticsVariableElimination.getSufficientStatisticsForAllParameters ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/SufficientStatisticsVariableElimination.html#getSufficientStatisticsForAllParameters-0)

```scala
def getSufficientStatisticsForAllParameters: Map[Parameter[_], Seq[Double]]
```

Returns a mapping of parameters to sufficient statistics resulting from elimination of the factors.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Map[Parameter[_], Seq[Double]] ``.

Source contract/attributes: Returns a mapping of parameters to sufficient statistics resulting from elimination of the factors. Attributes

Invocation template:

```scala
receiver.getSufficientStatisticsForAllParameters
```

## `` com.cra.figaro.algorithm.factored.SufficientStatisticsVariableElimination.starterElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/SufficientStatisticsVariableElimination.html#starterElements-0)

```scala
override def starterElements: List[Element[_]]
```

Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables. Attributes Definition Classes VariableElimination

Invocation template:

```scala
receiver.starterElements
```

## `` com.cra.figaro.algorithm.factored.SufficientStatisticsVariableElimination.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/SufficientStatisticsVariableElimination.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.factored.SufficientStatisticsVariableElimination.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/SufficientStatisticsVariableElimination.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.factored.SufficientStatisticsVariableElimination.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/SufficientStatisticsVariableElimination.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.factored.SufficientStatisticsVariableElimination.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/SufficientStatisticsVariableElimination.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.factored.SufficientStatisticsVariableElimination.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/SufficientStatisticsVariableElimination.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.factored.SufficientStatisticsVariableElimination.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/SufficientStatisticsVariableElimination.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Inherited from: VariableElimination

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.factored.SufficientStatisticsVariableElimination.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/SufficientStatisticsVariableElimination.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.factored.SufficientStatisticsVariableElimination.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/SufficientStatisticsVariableElimination.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.factored.VEGraph.cost ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VEGraph$.html#cost-fffff560)

```scala
def cost(factor: AbstractFactor): Double
```

The cost of a factor is the number of entries in it, which is the product of the ranges of its variables.

Type parameters: none.

Parameters, list 1: `` factor: AbstractFactor ``.

Returns: `` Double ``.

Source contract/attributes: The cost of a factor is the number of entries in it, which is the product of the ranges of its variables. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.VEGraph.cost(factor)
```

## `` com.cra.figaro.algorithm.factored.VEGraph.cost ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VEGraph$.html#cost-fd3)

```scala
def cost(factors: Iterable[AbstractFactor]): Double
```

The cost of a set of factors is the sum of the costs of the individual factors.

Type parameters: none.

Parameters, list 1: `` factors: Iterable[AbstractFactor] ``.

Returns: `` Double ``.

Source contract/attributes: The cost of a set of factors is the sum of the costs of the individual factors. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.VEGraph.cost(factors)
```

## `` com.cra.figaro.algorithm.factored.VEGraph.this ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VEGraph.html#<init>-fffff68f)

```scala
def this(factors: Iterable[Factor[_]])
```

Create the initial induced graph from a set of factors.

Type parameters: none.

Parameters, list 1: `` factors: Iterable[Factor[_]] ``.

Returns: `` com.cra.figaro.algorithm.factored.VEGraph ``.

Source contract/attributes: Create the initial induced graph from a set of factors. Attributes

Invocation template:

```scala
new com.cra.figaro.algorithm.factored.VEGraph(factors)
```

## `` com.cra.figaro.algorithm.factored.VEGraph.eliminate ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VEGraph.html#eliminate-fffff20e)

```scala
def eliminate(variable: Variable[_]): (VEGraph, Double)
```

Returns the new graph after eliminating the given variable. This includes a factor involving all the variables appearing in a factor with the eliminated variable, and excludes all factors in which the eliminated variable appears.

Type parameters: none.

Parameters, list 1: `` variable: Variable[_] ``.

Returns: `` (VEGraph, Double) ``.

Source contract/attributes: Returns the new graph after eliminating the given variable. This includes a factor involving all the variables appearing in a factor with the eliminated variable, and excludes all factors in which the eliminated variable appears. Attributes

Invocation template:

```scala
receiver.eliminate(variable)
```

## `` com.cra.figaro.algorithm.factored.VEGraph.score ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VEGraph.html#score-fffffc75)

```scala
def score(variable: Variable[_]): Double
```

Returns the elimination score, which is the increase in cost between the new factor involving the variable and the existing factors (we want to minimize score).

Type parameters: none.

Parameters, list 1: `` variable: Variable[_] ``.

Returns: `` Double ``.

Source contract/attributes: Returns the elimination score, which is the increase in cost between the new factor involving the variable and the existing factors (we want to minimize score). Attributes

Invocation template:

```scala
receiver.score(variable)
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination$.html#apply-aa7)

```scala
def apply(targets: Element[_]*)(implicit universe: Universe): ProbQueryVariableElimination
```

Create a variable elimination computer with the given target query variables in the current default universe.

Type parameters: none.

Parameters, list 1: `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryVariableElimination ``.

Source contract/attributes: Create a variable elimination computer with the given target query variables in the current default universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.VariableElimination.apply(targets*)(using universe)
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination$.html#apply-fffffff9)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], targets: Element[_]*)(implicit universe: Universe): ProbQueryVariableElimination
```

Create a variable elimination computer with the given target query variables and using the given dependent universes in the current default universe.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryVariableElimination ``.

Source contract/attributes: Create a variable elimination computer with the given target query variables and using the given dependent universes in the current default universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.VariableElimination.apply(dependentUniverses, targets*)(using universe)
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination$.html#apply-4a7)

```scala
def apply(dependentUniverses: List[(Universe, List[NamedEvidence[_]])], dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double, targets: Element[_]*)(implicit universe: Universe): ProbQueryVariableElimination
```

Create a variable elimination computer with the given target query variables and using the given dependent universes in the current default universe. Use the given dependent algorithm function to determine the algorithm to use to compute probability of evidence in each dependent universe.

Type parameters: none.

Parameters, list 1: `` dependentUniverses: List[(Universe, List[NamedEvidence[_]])] ``; `` dependentAlgorithm: (Universe, List[NamedEvidence[_]]) => () => Double ``; `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryVariableElimination ``.

Source contract/attributes: Create a variable elimination computer with the given target query variables and using the given dependent universes in the current default universe. Use the given dependent algorithm function to determine the algorithm to use to compute probability of evidence in each dependent universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.VariableElimination.apply(dependentUniverses, dependentAlgorithm, targets*)(using universe)
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.debugged ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination$.html#debugged-aa7)

```scala
def debugged(targets: Element[_]*)(implicit universe: Universe): ProbQueryVariableElimination
```

Create a variable elimination computer with the given target query variables in the current default universe, with debug information enabled.

Type parameters: none.

Parameters, list 1: `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryVariableElimination ``.

Source contract/attributes: Create a variable elimination computer with the given target query variables in the current default universe, with debug information enabled. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.VariableElimination.debugged(targets*)(using universe)
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.eliminationOrder ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination$.html#eliminationOrder-881)

```scala
def eliminationOrder[T](factors: Iterable[Factor[T]], toPreserve: Iterable[Variable[_]], useBestScore: Boolean = ...): (Double, List[Variable[_]])
```

Method for choosing the elimination order. The default order chooses first the variable that minimizes the number of extra factor entries that would be created when it is eliminated. Override this method if you want a different rule.

Type parameters: `` T ``.

Parameters, list 1: `` factors: Iterable[Factor[T]] ``; `` toPreserve: Iterable[Variable[_]] ``; `` useBestScore: Boolean = ... ``.

Returns: `` (Double, List[Variable[_]]) ``.

Source contract/attributes: Method for choosing the elimination order. The default order chooses first the variable that minimizes the number of extra factor entries that would be created when it is eliminated. Override this method if you want a different rule. Returns the score of the ordering as well as the ordering. If useBestScore is set to false, then it returns the total score of the entire eliminiation operation Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.VariableElimination.eliminationOrder[T](factors, toPreserve, useBestScore)
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination$.html#probability-c2a)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Use VE to compute the probability that the given element satisfies the given predicate.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Use VE to compute the probability that the given element satisfies the given predicate. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.VariableElimination.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination$.html#probability-f2c)

```scala
def probability[T](target: Element[T], value: T): Double
```

Use VE to compute the probability that the given element has the given value.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Use VE to compute the probability that the given element has the given value. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.VariableElimination.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.timed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination$.html#timed-aa7)

```scala
def timed(targets: Element[_]*)(implicit universe: Universe): ProbQueryVariableElimination
```

Create a variable elimination computer with the given target query variables in the current default universe, with timing information enabled.

Type parameters: none.

Parameters, list 1: `` targets: Element[_]* ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` ProbQueryVariableElimination ``.

Source contract/attributes: Create a variable elimination computer with the given target query variables in the current default universe, with timing information enabled. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.factored.VariableElimination.timed(targets*)(using universe)
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.finish ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination.html#finish-fffff214)

```scala
def finish(factorsAfterElimination: MultiSet[Factor[T]], eliminationOrder: List[Variable[_]]): Unit
```

All implementation of variable elimination must specify what to do after variables have been eliminated.

Type parameters: none.

Parameters, list 1: `` factorsAfterElimination: MultiSet[Factor[T]] ``; `` eliminationOrder: List[Variable[_]] ``.

Returns: `` Unit ``.

Source contract/attributes: All implementation of variable elimination must specify what to do after variables have been eliminated. Attributes

Invocation template:

```scala
receiver.finish(factorsAfterElimination, eliminationOrder)
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.starterElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination.html#starterElements-0)

```scala
def starterElements: List[Element[_]]
```

Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Elements towards which queries are directed. By default, these are the target elements. This is overridden by DecisionVariableElimination, where it also includes utility variables. Attributes

Invocation template:

```scala
receiver.starterElements
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.factored.VariableElimination.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableElimination.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[T]]
```

All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[T]] ``.

Source contract/attributes: All implementations of factored algorithms must specify a way to get the factors from the given universe and dependent universes. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.algorithm.factored.VariableInfo.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableInfo.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.algorithm.factored.VariableInfo.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/factored/VariableInfo.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```
