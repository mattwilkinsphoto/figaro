<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.algorithm.filtering

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.algorithm.filtering.FactoredFrontier.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/FactoredFrontier$.html#apply-754)

```scala
def apply(static: Universe, initial: Universe, transition: (Universe, Universe) => Universe, iterations: Int): FactoredFrontier
```

A Factored Frontier that runs with one time Belief Propagation and a static universe.

Type parameters: none.

Parameters, list 1: `` static: Universe ``; `` initial: Universe ``; `` transition: (Universe, Universe) => Universe ``; `` iterations: Int ``.

Returns: `` FactoredFrontier ``.

Source contract/attributes: A Factored Frontier that runs with one time Belief Propagation and a static universe. Value parameters initial The universe describing the distribution over the initial state of the system. iterations The number of iterations with which to run Belief Propagation at each time step. static The universe of elements that do not change over time. transition The transition model describing how the current state of the system depends on the static and previous, respectively. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.filtering.FactoredFrontier.apply(static, initial, transition, iterations)
```

## `` com.cra.figaro.algorithm.filtering.FactoredFrontier.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/FactoredFrontier$.html#apply-fffffde3)

```scala
def apply(initial: Universe, transition: Universe => Universe, iterations: Int): FactoredFrontier
```

A Factored Frontier that runs with one time Belief Propagation.

Type parameters: none.

Parameters, list 1: `` initial: Universe ``; `` transition: Universe => Universe ``; `` iterations: Int ``.

Returns: `` FactoredFrontier ``.

Source contract/attributes: A Factored Frontier that runs with one time Belief Propagation. Value parameters initial The universe describing the distribution over the initial state of the system. iterations The number of iterations with which to run Belief Propagation at each time step. transition The transition model describing how the current state of the system depends on the previous. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.filtering.FactoredFrontier.apply(initial, transition, iterations)
```

## `` com.cra.figaro.algorithm.filtering.FactoredFrontier.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/FactoredFrontier$.html#apply-c73)

```scala
def apply(static: Universe, initial: Universe, transition: (Universe, Universe) => Universe, stepTimeMillis: Long): FactoredFrontier
```

A Factored Frontier that runs with anytime Belief Propagation and a static universe.

Type parameters: none.

Parameters, list 1: `` static: Universe ``; `` initial: Universe ``; `` transition: (Universe, Universe) => Universe ``; `` stepTimeMillis: Long ``.

Returns: `` FactoredFrontier ``.

Source contract/attributes: A Factored Frontier that runs with anytime Belief Propagation and a static universe. Value parameters initial The universe describing the distribution over the initial state of the system. static The universe of elements that do not change over time. stepTimeMillis The time, in milliseconds, for which to run Belief Propagation at each time step. transition The transition model describing how the current state of the system depends on the static and previous, respectively. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.filtering.FactoredFrontier.apply(static, initial, transition, stepTimeMillis)
```

## `` com.cra.figaro.algorithm.filtering.FactoredFrontier.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/FactoredFrontier$.html#apply-fffff7c4)

```scala
def apply(initial: Universe, transition: Universe => Universe, stepTimeMillis: Long): FactoredFrontier
```

A Factored Frontier that runs with anytime Belief Propagation.

Type parameters: none.

Parameters, list 1: `` initial: Universe ``; `` transition: Universe => Universe ``; `` stepTimeMillis: Long ``.

Returns: `` FactoredFrontier ``.

Source contract/attributes: A Factored Frontier that runs with anytime Belief Propagation. Value parameters initial The universe describing the distribution over the initial state of the system. stepTimeMillis The time, in milliseconds, for which to run Belief Propagation at each time step. transition The transition model describing how the current state of the system depends on the previous. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.filtering.FactoredFrontier.apply(initial, transition, stepTimeMillis)
```

## `` com.cra.figaro.algorithm.filtering.FactoredFrontier.advanceTime ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/FactoredFrontier.html#advanceTime-fffff9b7)

```scala
def advanceTime(evidence: Seq[NamedEvidence[_]] = ...): Unit
```

Advance the algorithm one time step based on the provided evidence.

Type parameters: none.

Parameters, list 1: `` evidence: Seq[NamedEvidence[_]] = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Advance the algorithm one time step based on the provided evidence. Attributes

Invocation template:

```scala
receiver.advanceTime(evidence)
```

## `` com.cra.figaro.algorithm.filtering.FactoredFrontier.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/FactoredFrontier.html#cleanUp-94c)

```scala
override def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Definition Classes Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.filtering.FactoredFrontier.computeCurrentDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/FactoredFrontier.html#computeCurrentDistribution-fffff445)

```scala
def computeCurrentDistribution[T](reference: Reference[T]): Stream[(Double, T)]
```

Returns the distribution over the element referred to by the reference at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Returns the distribution over the element referred to by the reference at the current time point. Attributes

Invocation template:

```scala
receiver.computeCurrentDistribution[T](reference)
```

## `` com.cra.figaro.algorithm.filtering.FactoredFrontier.computeCurrentExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/FactoredFrontier.html#computeCurrentExpectation-fffff37b)

```scala
def computeCurrentExpectation[T](reference: Reference[T], function: T => Double): Double
```

Returns the expectation of the element referred to by the reference under the given function at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Returns the expectation of the element referred to by the reference under the given function at the current time point. Attributes

Invocation template:

```scala
receiver.computeCurrentExpectation[T](reference, function)
```

## `` com.cra.figaro.algorithm.filtering.FactoredFrontier.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/FactoredFrontier.html#initialize-94c)

```scala
override def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Definition Classes Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.filtering.FactoredFrontier.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/FactoredFrontier.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.filtering.FactoredFrontier.currentDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/FactoredFrontier.html#currentDistribution-fffff445)

```scala
def currentDistribution[T](reference: Reference[T]): Stream[(Double, T)]
```

Returns the distribution over the element referred to by the reference at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Returns the distribution over the element referred to by the reference at the current time point. Attributes Inherited from: OneTimeFiltering

Invocation template:

```scala
receiver.currentDistribution[T](reference)
```

## `` com.cra.figaro.algorithm.filtering.FactoredFrontier.currentExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/FactoredFrontier.html#currentExpectation-fffff37b)

```scala
def currentExpectation[T](reference: Reference[T], function: T => Double): Double
```

Returns the expectation of the element referred to by the reference under the given function at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Returns the expectation of the element referred to by the reference under the given function at the current time point. Attributes Inherited from: OneTimeFiltering

Invocation template:

```scala
receiver.currentExpectation[T](reference, function)
```

## `` com.cra.figaro.algorithm.filtering.FactoredFrontier.currentProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/FactoredFrontier.html#currentProbability-fffff37b)

```scala
def currentProbability[T](reference: Reference[T], predicate: T => Boolean): Double
```

Returns the probability that the element referred to by the reference satisfies the given predicate at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Returns the probability that the element referred to by the reference satisfies the given predicate at the current time point. Attributes Inherited from: OneTimeFiltering

Invocation template:

```scala
receiver.currentProbability[T](reference, predicate)
```

## `` com.cra.figaro.algorithm.filtering.FactoredFrontier.currentProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/FactoredFrontier.html#currentProbability-1fb)

```scala
def currentProbability[T](reference: Reference[T], value: T): Double
```

Returns the probability that the element referred to by the reference produces the given value at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Returns the probability that the element referred to by the reference produces the given value at the current time point. Attributes Inherited from: Filtering

Invocation template:

```scala
receiver.currentProbability[T](reference, value)
```

## `` com.cra.figaro.algorithm.filtering.FactoredFrontier.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/FactoredFrontier.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.filtering.FactoredFrontier.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/FactoredFrontier.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.filtering.FactoredFrontier.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/FactoredFrontier.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.filtering.FactoredFrontier.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/FactoredFrontier.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.filtering.FactoredFrontier.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/FactoredFrontier.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.filtering.Filtering.advanceTime ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/Filtering.html#advanceTime-fffff9b7)

```scala
def advanceTime(evidence: Seq[NamedEvidence[_]]): Unit
```

Advance the filtering one time step, conditioning on the given evidence at the new time point.

Type parameters: none.

Parameters, list 1: `` evidence: Seq[NamedEvidence[_]] ``.

Returns: `` Unit ``.

Source contract/attributes: Advance the filtering one time step, conditioning on the given evidence at the new time point. Attributes

Invocation template:

```scala
receiver.advanceTime(evidence)
```

## `` com.cra.figaro.algorithm.filtering.Filtering.currentDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/Filtering.html#currentDistribution-fffff445)

```scala
def currentDistribution[T](reference: Reference[T]): Stream[(Double, T)]
```

Returns the distribution over the element referred to by the reference at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Returns the distribution over the element referred to by the reference at the current time point. Attributes

Invocation template:

```scala
receiver.currentDistribution[T](reference)
```

## `` com.cra.figaro.algorithm.filtering.Filtering.currentExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/Filtering.html#currentExpectation-fffff37b)

```scala
def currentExpectation[T](reference: Reference[T], function: T => Double): Double
```

Returns the expectation of the element referred to by the reference under the given function at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Returns the expectation of the element referred to by the reference under the given function at the current time point. Attributes

Invocation template:

```scala
receiver.currentExpectation[T](reference, function)
```

## `` com.cra.figaro.algorithm.filtering.Filtering.currentProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/Filtering.html#currentProbability-fffff37b)

```scala
def currentProbability[T](reference: Reference[T], predicate: T => Boolean): Double
```

Returns the probability that the element referred to by the reference satisfies the given predicate at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Returns the probability that the element referred to by the reference satisfies the given predicate at the current time point. Attributes

Invocation template:

```scala
receiver.currentProbability[T](reference, predicate)
```

## `` com.cra.figaro.algorithm.filtering.Filtering.currentProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/Filtering.html#currentProbability-1fb)

```scala
def currentProbability[T](reference: Reference[T], value: T): Double
```

Returns the probability that the element referred to by the reference produces the given value at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Returns the probability that the element referred to by the reference produces the given value at the current time point. Attributes

Invocation template:

```scala
receiver.currentProbability[T](reference, value)
```

## `` com.cra.figaro.algorithm.filtering.Filtering.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/Filtering.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.filtering.Filtering.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/Filtering.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.filtering.Filtering.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/Filtering.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.filtering.Filtering.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/Filtering.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.filtering.Filtering.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/Filtering.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.filtering.Filtering.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/Filtering.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.filtering.Filtering.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/Filtering.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.filtering.OneTimeFiltering.currentDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeFiltering.html#currentDistribution-fffff445)

```scala
def currentDistribution[T](reference: Reference[T]): Stream[(Double, T)]
```

Returns the distribution over the element referred to by the reference at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Returns the distribution over the element referred to by the reference at the current time point. Attributes

Invocation template:

```scala
receiver.currentDistribution[T](reference)
```

## `` com.cra.figaro.algorithm.filtering.OneTimeFiltering.currentExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeFiltering.html#currentExpectation-fffff37b)

```scala
def currentExpectation[T](reference: Reference[T], function: T => Double): Double
```

Returns the expectation of the element referred to by the reference under the given function at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Returns the expectation of the element referred to by the reference under the given function at the current time point. Attributes

Invocation template:

```scala
receiver.currentExpectation[T](reference, function)
```

## `` com.cra.figaro.algorithm.filtering.OneTimeFiltering.currentProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeFiltering.html#currentProbability-fffff37b)

```scala
def currentProbability[T](reference: Reference[T], predicate: T => Boolean): Double
```

Returns the probability that the element referred to by the reference satisfies the given predicate at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Returns the probability that the element referred to by the reference satisfies the given predicate at the current time point. Attributes

Invocation template:

```scala
receiver.currentProbability[T](reference, predicate)
```

## `` com.cra.figaro.algorithm.filtering.OneTimeFiltering.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeFiltering.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.filtering.OneTimeFiltering.currentProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeFiltering.html#currentProbability-1fb)

```scala
def currentProbability[T](reference: Reference[T], value: T): Double
```

Returns the probability that the element referred to by the reference produces the given value at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Returns the probability that the element referred to by the reference produces the given value at the current time point. Attributes Inherited from: Filtering

Invocation template:

```scala
receiver.currentProbability[T](reference, value)
```

## `` com.cra.figaro.algorithm.filtering.OneTimeFiltering.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeFiltering.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.filtering.OneTimeFiltering.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeFiltering.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.filtering.OneTimeFiltering.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeFiltering.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.filtering.OneTimeFiltering.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeFiltering.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.filtering.OneTimeFiltering.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeFiltering.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.filtering.OneTimeFiltering.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeFiltering.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.filtering.OneTimeFiltering.advanceTime ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeFiltering.html#advanceTime-fffff9b7)

```scala
def advanceTime(evidence: Seq[NamedEvidence[_]]): Unit
```

Advance the filtering one time step, conditioning on the given evidence at the new time point.

Type parameters: none.

Parameters, list 1: `` evidence: Seq[NamedEvidence[_]] ``.

Returns: `` Unit ``.

Source contract/attributes: Advance the filtering one time step, conditioning on the given evidence at the new time point. Attributes Inherited from: Filtering

Invocation template:

```scala
receiver.advanceTime(evidence)
```

## `` com.cra.figaro.algorithm.filtering.OneTimeFiltering.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeFiltering.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Inherited from: OneTime

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.filtering.OneTimeParticleFilter.advanceTime ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeParticleFilter.html#advanceTime-fffff9b7)

```scala
def advanceTime(evidence: Seq[NamedEvidence[_]] = ...): Unit
```

Advance the filtering one time step, conditioning on the given evidence at the new time point.

Type parameters: none.

Parameters, list 1: `` evidence: Seq[NamedEvidence[_]] = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Advance the filtering one time step, conditioning on the given evidence at the new time point. Attributes

Invocation template:

```scala
receiver.advanceTime(evidence)
```

## `` com.cra.figaro.algorithm.filtering.OneTimeParticleFilter.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeParticleFilter.html#run-94c)

```scala
def run(): Unit
```

Begin the particle filter, determining the initial distribution.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Begin the particle filter, determining the initial distribution. Attributes

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.filtering.OneTimeParticleFilter.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeParticleFilter.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.filtering.OneTimeParticleFilter.computeCurrentDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeParticleFilter.html#computeCurrentDistribution-fffff445)

```scala
def computeCurrentDistribution[T](reference: Reference[T]): Stream[(Double, T)]
```

Returns the distribution over the element referred to by the reference at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Returns the distribution over the element referred to by the reference at the current time point. Attributes Inherited from: ParticleFilter

Invocation template:

```scala
receiver.computeCurrentDistribution[T](reference)
```

## `` com.cra.figaro.algorithm.filtering.OneTimeParticleFilter.computeCurrentExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeParticleFilter.html#computeCurrentExpectation-fffff37b)

```scala
def computeCurrentExpectation[T](reference: Reference[T], function: T => Double): Double
```

Returns the expectation of the element referred to by the reference under the given function at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Returns the expectation of the element referred to by the reference under the given function at the current time point. Attributes Inherited from: ParticleFilter

Invocation template:

```scala
receiver.computeCurrentExpectation[T](reference, function)
```

## `` com.cra.figaro.algorithm.filtering.OneTimeParticleFilter.currentDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeParticleFilter.html#currentDistribution-fffff445)

```scala
def currentDistribution[T](reference: Reference[T]): Stream[(Double, T)]
```

Returns the distribution over the element referred to by the reference at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Returns the distribution over the element referred to by the reference at the current time point. Attributes Inherited from: OneTimeFiltering

Invocation template:

```scala
receiver.currentDistribution[T](reference)
```

## `` com.cra.figaro.algorithm.filtering.OneTimeParticleFilter.currentExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeParticleFilter.html#currentExpectation-fffff37b)

```scala
def currentExpectation[T](reference: Reference[T], function: T => Double): Double
```

Returns the expectation of the element referred to by the reference under the given function at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Returns the expectation of the element referred to by the reference under the given function at the current time point. Attributes Inherited from: OneTimeFiltering

Invocation template:

```scala
receiver.currentExpectation[T](reference, function)
```

## `` com.cra.figaro.algorithm.filtering.OneTimeParticleFilter.currentProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeParticleFilter.html#currentProbability-fffff37b)

```scala
def currentProbability[T](reference: Reference[T], predicate: T => Boolean): Double
```

Returns the probability that the element referred to by the reference satisfies the given predicate at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Returns the probability that the element referred to by the reference satisfies the given predicate at the current time point. Attributes Inherited from: OneTimeFiltering

Invocation template:

```scala
receiver.currentProbability[T](reference, predicate)
```

## `` com.cra.figaro.algorithm.filtering.OneTimeParticleFilter.currentProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeParticleFilter.html#currentProbability-1fb)

```scala
def currentProbability[T](reference: Reference[T], value: T): Double
```

Returns the probability that the element referred to by the reference produces the given value at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Returns the probability that the element referred to by the reference produces the given value at the current time point. Attributes Inherited from: Filtering

Invocation template:

```scala
receiver.currentProbability[T](reference, value)
```

## `` com.cra.figaro.algorithm.filtering.OneTimeParticleFilter.getlogProbEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeParticleFilter.html#getlogProbEvidence-999)

```scala
def getlogProbEvidence(): Double
```

The computed log probability of evidence.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Double ``.

Source contract/attributes: The computed log probability of evidence. Attributes Inherited from: ParticleFilter

Invocation template:

```scala
receiver.getlogProbEvidence()
```

## `` com.cra.figaro.algorithm.filtering.OneTimeParticleFilter.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeParticleFilter.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.filtering.OneTimeParticleFilter.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeParticleFilter.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.filtering.OneTimeParticleFilter.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeParticleFilter.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.filtering.OneTimeParticleFilter.probEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeParticleFilter.html#probEvidence-999)

```scala
def probEvidence(): Double
```

The computed probability of evidence.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Double ``.

Source contract/attributes: The computed probability of evidence. Attributes Inherited from: ParticleFilter

Invocation template:

```scala
receiver.probEvidence()
```

## `` com.cra.figaro.algorithm.filtering.OneTimeParticleFilter.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeParticleFilter.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.filtering.OneTimeParticleFilter.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeParticleFilter.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.filtering.OneTimeParticleFilter.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/OneTimeParticleFilter.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.filtering.ParFiltering.advanceTime ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParFiltering.html#advanceTime-fffff9b7)

```scala
def advanceTime(evidence: Seq[NamedEvidence[_]]): Unit
```

Advance the filtering one time step, conditioning on the given evidence at the new time point.

Type parameters: none.

Parameters, list 1: `` evidence: Seq[NamedEvidence[_]] ``.

Returns: `` Unit ``.

Source contract/attributes: Advance the filtering one time step, conditioning on the given evidence at the new time point. Attributes

Invocation template:

```scala
receiver.advanceTime(evidence)
```

## `` com.cra.figaro.algorithm.filtering.ParFiltering.currentDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParFiltering.html#currentDistribution-fffff445)

```scala
def currentDistribution[T](reference: Reference[T]): Stream[(Double, T)]
```

Returns the distribution over the element referred to by the reference at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Returns the distribution over the element referred to by the reference at the current time point. Attributes

Invocation template:

```scala
receiver.currentDistribution[T](reference)
```

## `` com.cra.figaro.algorithm.filtering.ParFiltering.currentExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParFiltering.html#currentExpectation-fffff37b)

```scala
def currentExpectation[T](reference: Reference[T], function: T => Double): Double
```

Returns the expectation of the element referred to by the reference under the given function at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Returns the expectation of the element referred to by the reference under the given function at the current time point. Attributes

Invocation template:

```scala
receiver.currentExpectation[T](reference, function)
```

## `` com.cra.figaro.algorithm.filtering.ParFiltering.currentProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParFiltering.html#currentProbability-1fb)

```scala
def currentProbability[T](reference: Reference[T], value: T): Double
```

Returns the probability that the element referred to by the reference produces the given value at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Returns the probability that the element referred to by the reference produces the given value at the current time point. Attributes

Invocation template:

```scala
receiver.currentProbability[T](reference, value)
```

## `` com.cra.figaro.algorithm.filtering.ParFiltering.currentProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParFiltering.html#currentProbability-fffff37b)

```scala
def currentProbability[T](reference: Reference[T], predicate: T => Boolean): Double
```

Returns the probability that the element referred to by the reference satisfies the given predicate at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Returns the probability that the element referred to by the reference satisfies the given predicate at the current time point. Attributes

Invocation template:

```scala
receiver.currentProbability[T](reference, predicate)
```

## `` com.cra.figaro.algorithm.filtering.ParFiltering.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParFiltering.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.filtering.ParFiltering.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParFiltering.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.filtering.ParFiltering.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParFiltering.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.filtering.ParFiltering.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParFiltering.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.filtering.ParFiltering.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParFiltering.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.filtering.ParFiltering.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParFiltering.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.filtering.ParFiltering.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParFiltering.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.filtering.ParFiltering.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParFiltering.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Inherited from: OneTime

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.filtering.ParOneTimeParticleFilter.advanceTime ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParOneTimeParticleFilter.html#advanceTime-fffff9b7)

```scala
def advanceTime(evidence: Seq[NamedEvidence[_]] = ...): Unit
```

Advance the filtering one time step, conditioning on the given evidence at the new time point.

Type parameters: none.

Parameters, list 1: `` evidence: Seq[NamedEvidence[_]] = ... ``.

Returns: `` Unit ``.

Source contract/attributes: Advance the filtering one time step, conditioning on the given evidence at the new time point. Attributes

Invocation template:

```scala
receiver.advanceTime(evidence)
```

## `` com.cra.figaro.algorithm.filtering.ParOneTimeParticleFilter.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParOneTimeParticleFilter.html#run-94c)

```scala
def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.filtering.ParOneTimeParticleFilter.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParOneTimeParticleFilter.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.filtering.ParOneTimeParticleFilter.computeCurrentDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParOneTimeParticleFilter.html#computeCurrentDistribution-fffff445)

```scala
def computeCurrentDistribution[T](reference: Reference[T]): Stream[(Double, T)]
```

Returns the distribution over the element referred to by the reference at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Returns the distribution over the element referred to by the reference at the current time point. Attributes Inherited from: ParticleFilter

Invocation template:

```scala
receiver.computeCurrentDistribution[T](reference)
```

## `` com.cra.figaro.algorithm.filtering.ParOneTimeParticleFilter.computeCurrentExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParOneTimeParticleFilter.html#computeCurrentExpectation-fffff37b)

```scala
def computeCurrentExpectation[T](reference: Reference[T], function: T => Double): Double
```

Returns the expectation of the element referred to by the reference under the given function at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Returns the expectation of the element referred to by the reference under the given function at the current time point. Attributes Inherited from: ParticleFilter

Invocation template:

```scala
receiver.computeCurrentExpectation[T](reference, function)
```

## `` com.cra.figaro.algorithm.filtering.ParOneTimeParticleFilter.currentDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParOneTimeParticleFilter.html#currentDistribution-fffff445)

```scala
def currentDistribution[T](reference: Reference[T]): Stream[(Double, T)]
```

Returns the distribution over the element referred to by the reference at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Returns the distribution over the element referred to by the reference at the current time point. Attributes Inherited from: ParFiltering

Invocation template:

```scala
receiver.currentDistribution[T](reference)
```

## `` com.cra.figaro.algorithm.filtering.ParOneTimeParticleFilter.currentExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParOneTimeParticleFilter.html#currentExpectation-fffff37b)

```scala
def currentExpectation[T](reference: Reference[T], function: T => Double): Double
```

Returns the expectation of the element referred to by the reference under the given function at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Returns the expectation of the element referred to by the reference under the given function at the current time point. Attributes Inherited from: ParFiltering

Invocation template:

```scala
receiver.currentExpectation[T](reference, function)
```

## `` com.cra.figaro.algorithm.filtering.ParOneTimeParticleFilter.currentProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParOneTimeParticleFilter.html#currentProbability-fffff37b)

```scala
def currentProbability[T](reference: Reference[T], predicate: T => Boolean): Double
```

Returns the probability that the element referred to by the reference satisfies the given predicate at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Returns the probability that the element referred to by the reference satisfies the given predicate at the current time point. Attributes Inherited from: ParFiltering

Invocation template:

```scala
receiver.currentProbability[T](reference, predicate)
```

## `` com.cra.figaro.algorithm.filtering.ParOneTimeParticleFilter.currentProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParOneTimeParticleFilter.html#currentProbability-1fb)

```scala
def currentProbability[T](reference: Reference[T], value: T): Double
```

Returns the probability that the element referred to by the reference produces the given value at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Returns the probability that the element referred to by the reference produces the given value at the current time point. Attributes Inherited from: ParFiltering

Invocation template:

```scala
receiver.currentProbability[T](reference, value)
```

## `` com.cra.figaro.algorithm.filtering.ParOneTimeParticleFilter.getlogProbEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParOneTimeParticleFilter.html#getlogProbEvidence-999)

```scala
def getlogProbEvidence(): Double
```

The computed log probability of evidence.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Double ``.

Source contract/attributes: The computed log probability of evidence. Attributes Inherited from: ParticleFilter

Invocation template:

```scala
receiver.getlogProbEvidence()
```

## `` com.cra.figaro.algorithm.filtering.ParOneTimeParticleFilter.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParOneTimeParticleFilter.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.filtering.ParOneTimeParticleFilter.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParOneTimeParticleFilter.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.filtering.ParOneTimeParticleFilter.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParOneTimeParticleFilter.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.filtering.ParOneTimeParticleFilter.probEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParOneTimeParticleFilter.html#probEvidence-999)

```scala
def probEvidence(): Double
```

The computed probability of evidence.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Double ``.

Source contract/attributes: The computed probability of evidence. Attributes Inherited from: ParticleFilter

Invocation template:

```scala
receiver.probEvidence()
```

## `` com.cra.figaro.algorithm.filtering.ParOneTimeParticleFilter.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParOneTimeParticleFilter.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.filtering.ParOneTimeParticleFilter.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParOneTimeParticleFilter.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.filtering.ParOneTimeParticleFilter.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParOneTimeParticleFilter.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.filtering.ParParticleFilter.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParParticleFilter$.html#apply-b8c)

```scala
def apply(static: () => Universe, initial: () => Universe, transition: (Universe, Universe) => Universe, numParticles: Int, numThreads: Int): ParOneTimeParticleFilter
```

A parallel one-time particle filter. Distributes the work of generating particles at each time step over a specified number of threads. After generating the particles, they are recombined before re-sampling occurs. Instead of accepting initial and static universes as input, this method accepts functions that return universes. This is because each thread needs its own set of universes to work on. It is important that any elements created within those functions are explicitly assigned to the returned universe, not the implicit default universe.

Type parameters: none.

Parameters, list 1: `` static: () => Universe ``; `` initial: () => Universe ``; `` transition: (Universe, Universe) => Universe ``; `` numParticles: Int ``; `` numThreads: Int ``.

Returns: `` ParOneTimeParticleFilter ``.

Source contract/attributes: A parallel one-time particle filter. Distributes the work of generating particles at each time step over a specified number of threads. After generating the particles, they are recombined before re-sampling occurs. Instead of accepting initial and static universes as input, this method accepts functions that return universes. This is because each thread needs its own set of universes to work on. It is important that any elements created within those functions are explicitly assigned to the returned universe, not the implicit default universe. Value parameters initial A function that returns a universe describing the distribution over the initial state of the system numParticles Number of particles to use at each time step numThreads The number of threads over which to distribute the work of generating the particles at each step static A function that returns a universe of elements whose values do not change over time transition The transition model describing how the current state of the system depends on the static and previous, respectively Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.filtering.ParParticleFilter.apply(static, initial, transition, numParticles, numThreads)
```

## `` com.cra.figaro.algorithm.filtering.ParParticleFilter.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParParticleFilter$.html#apply-fffff87d)

```scala
def apply(initial: () => Universe, transition: Universe => Universe, numParticles: Int, numThreads: Int): ParOneTimeParticleFilter
```

A parallel one-time particle filter. Distributes the work of generating particles at each time step over a specified number of threads. After generating the particles, they are recombined before re-sampling occurs. Instead of accepting an initial universe as input, this method accepts a function that returns a universe. This is because each thread needs its own set of universes to work on. It is important that any elements created within that function are explicitly assigned to the returned universe, not the implicit default universe.

Type parameters: none.

Parameters, list 1: `` initial: () => Universe ``; `` transition: Universe => Universe ``; `` numParticles: Int ``; `` numThreads: Int ``.

Returns: `` ParOneTimeParticleFilter ``.

Source contract/attributes: A parallel one-time particle filter. Distributes the work of generating particles at each time step over a specified number of threads. After generating the particles, they are recombined before re-sampling occurs. Instead of accepting an initial universe as input, this method accepts a function that returns a universe. This is because each thread needs its own set of universes to work on. It is important that any elements created within that function are explicitly assigned to the returned universe, not the implicit default universe. Value parameters initial A function that returns a universe describing the distribution over the initial state of the system numParticles Number of particles to use at each time step numThreads The number of threads over which to distribute the work of generating the particles at each step transition The transition model describing how the current state of the system depends on the previous Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.filtering.ParParticleFilter.apply(initial, transition, numParticles, numThreads)
```

## `` com.cra.figaro.algorithm.filtering.ParticleFilter.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParticleFilter$.html#apply-778)

```scala
def apply(static: Universe, initial: Universe, transition: (Universe, Universe) => Universe, numParticles: Int): OneTimeParticleFilter
```

A one-time particle filter.

Type parameters: none.

Parameters, list 1: `` static: Universe ``; `` initial: Universe ``; `` transition: (Universe, Universe) => Universe ``; `` numParticles: Int ``.

Returns: `` OneTimeParticleFilter ``.

Source contract/attributes: A one-time particle filter. Value parameters initial The universe describing the distribution over the initial state of the system numParticles Number of particles to use at each time step static The universe of elements whose values do not change over time transition The transition model describing how the current state of the system depends on the static and previous, respectively Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.filtering.ParticleFilter.apply(static, initial, transition, numParticles)
```

## `` com.cra.figaro.algorithm.filtering.ParticleFilter.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParticleFilter$.html#apply-249)

```scala
def apply(initial: Universe, transition: Universe => Universe, numParticles: Int): OneTimeParticleFilter
```

A one-time particle filter in which the static universe is empty.

Type parameters: none.

Parameters, list 1: `` initial: Universe ``; `` transition: Universe => Universe ``; `` numParticles: Int ``.

Returns: `` OneTimeParticleFilter ``.

Source contract/attributes: A one-time particle filter in which the static universe is empty. Value parameters initial The universe describing the distribution over the initial state of the system numParticles Number of particles to use at each time step transition The transition model describing how the current state of the system depends on the previous Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.filtering.ParticleFilter.apply(initial, transition, numParticles)
```

## `` com.cra.figaro.algorithm.filtering.ParticleFilter.par ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParticleFilter$.html#par-0)

```scala
def par: ParParticleFilter.type
```

Reference to parallel implementation.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` ParParticleFilter.type ``.

Source contract/attributes: Reference to parallel implementation. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.filtering.ParticleFilter.par
```

## `` com.cra.figaro.algorithm.filtering.ParticleFilter.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParticleFilter$.html#apply-f59)

```scala
def apply(static: Universe, initial: Universe, transition: Universe => Universe, numParticles: Int): OneTimeParticleFilter
```

A one-time particle filter.

Type parameters: none.

Parameters, list 1: `` static: Universe ``; `` initial: Universe ``; `` transition: Universe => Universe ``; `` numParticles: Int ``.

Returns: `` OneTimeParticleFilter ``.

Source contract/attributes: A one-time particle filter. Value parameters initial The universe describing the distribution over the initial state of the system numParticles Number of particles to use at each time step static The universe of elements whose values do not change over time transition The transition model describing how the current state of the system depends on the previous Attributes Deprecated [Since version 2.3.0.0] If the static universe is defined, use the constructor where the transition function takes two universes

Invocation template:

```scala
com.cra.figaro.algorithm.filtering.ParticleFilter.apply(static, initial, transition, numParticles)
```

## `` com.cra.figaro.algorithm.filtering.ParticleFilter.computeCurrentDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParticleFilter.html#computeCurrentDistribution-fffff445)

```scala
def computeCurrentDistribution[T](reference: Reference[T]): Stream[(Double, T)]
```

Returns the distribution over the element referred to by the reference at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``.

Returns: `` Stream[(Double, T)] ``.

Source contract/attributes: Returns the distribution over the element referred to by the reference at the current time point. Attributes

Invocation template:

```scala
receiver.computeCurrentDistribution[T](reference)
```

## `` com.cra.figaro.algorithm.filtering.ParticleFilter.computeCurrentExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParticleFilter.html#computeCurrentExpectation-fffff37b)

```scala
def computeCurrentExpectation[T](reference: Reference[T], function: T => Double): Double
```

Returns the expectation of the element referred to by the reference under the given function at the current time point.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Returns the expectation of the element referred to by the reference under the given function at the current time point. Attributes

Invocation template:

```scala
receiver.computeCurrentExpectation[T](reference, function)
```

## `` com.cra.figaro.algorithm.filtering.ParticleFilter.getlogProbEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParticleFilter.html#getlogProbEvidence-999)

```scala
def getlogProbEvidence(): Double
```

The computed log probability of evidence.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Double ``.

Source contract/attributes: The computed log probability of evidence. Attributes

Invocation template:

```scala
receiver.getlogProbEvidence()
```

## `` com.cra.figaro.algorithm.filtering.ParticleFilter.probEvidence ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/ParticleFilter.html#probEvidence-999)

```scala
def probEvidence(): Double
```

The computed probability of evidence.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Double ``.

Source contract/attributes: The computed probability of evidence. Attributes

Invocation template:

```scala
receiver.probEvidence()
```

## `` com.cra.figaro.algorithm.filtering.Snapshot.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/Snapshot.html#apply-fffffa13)

```scala
def apply[T](element: Element[T]): T
```

Returns the value of the given element in the snapshot, based on the name of the element.

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns the value of the given element in the snapshot, based on the name of the element. Attributes

Invocation template:

```scala
receiver.apply[T](element)
```

## `` com.cra.figaro.algorithm.filtering.Snapshot.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/Snapshot.html#get-b62)

```scala
def get[T](reference: Reference[T]): T
```

Get the value of the element associated with the given reference.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``.

Returns: `` T ``.

Source contract/attributes: Get the value of the element associated with the given reference. Attributes

Invocation template:

```scala
receiver.get[T](reference)
```

## `` com.cra.figaro.algorithm.filtering.Snapshot.restore ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/Snapshot.html#restore-25c)

```scala
def restore(newUniverse: Universe): Unit
```

Restore the values stored in the snapshot into the given new universe.

Type parameters: none.

Parameters, list 1: `` newUniverse: Universe ``.

Returns: `` Unit ``.

Source contract/attributes: Restore the values stored in the snapshot into the given new universe. Attributes

Invocation template:

```scala
receiver.restore(newUniverse)
```

## `` com.cra.figaro.algorithm.filtering.Snapshot.store ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/Snapshot.html#store-25c)

```scala
def store(universe: Universe): Unit
```

Store the values of all named elements in the universe in this snapshot.

Type parameters: none.

Parameters, list 1: `` universe: Universe ``.

Returns: `` Unit ``.

Source contract/attributes: Store the values of all named elements in the universe in this snapshot. Attributes

Invocation template:

```scala
receiver.store(universe)
```

## `` com.cra.figaro.algorithm.filtering.State.get ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/filtering/State.html#get-b62)

```scala
def get[T](reference: Reference[T]): T
```

Get the value of the element associated with the given reference on the dynamic snapshot, or the static snapshot if the referred element cannot be found in the dynamic snapshot.

Type parameters: `` T ``.

Parameters, list 1: `` reference: Reference[T] ``.

Returns: `` T ``.

Source contract/attributes: Get the value of the element associated with the given reference on the dynamic snapshot, or the static snapshot if the referred element cannot be found in the dynamic snapshot. Attributes

Invocation template:

```scala
receiver.get[T](reference)
```
