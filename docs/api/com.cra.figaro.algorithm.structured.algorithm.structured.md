<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.algorithm.structured.algorithm.structured

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP$.html#apply-fffffcb8)

```scala
def apply(iterations: Int, targets: Element[_]*): StructuredBP
```

Create a structured belief propagation algorithm.

Type parameters: none.

Parameters, list 1: `` iterations: Int ``; `` targets: Element[_]* ``.

Returns: `` StructuredBP ``.

Source contract/attributes: Create a structured belief propagation algorithm. Value parameters iterations the number of iterations to use for each subproblem targets the query targets, which will all be part of the top level problem Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.apply(iterations, targets*)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP$.html#probability-6cf)

```scala
def probability[T](target: Element[T], predicate: T => Boolean, iterations: Int): Double
```

Use BP to compute the probability that the given element satisfies the given predicate.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``; `` iterations: Int ``.

Returns: `` Double ``.

Source contract/attributes: Use BP to compute the probability that the given element satisfies the given predicate. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.probability[T](target, predicate, iterations)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP$.html#probability-60d)

```scala
def probability[T](target: Element[T], value: T, iterations: Int = ...): Double
```

Use BP to compute the probability that the given element has the given value.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``; `` iterations: Int = ... ``.

Returns: `` Double ``.

Source contract/attributes: Use BP to compute the probability that the given element has the given value. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.probability[T](target, value, iterations)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration.

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#computeDistribution-f66)

```scala
override def computeDistribution[T](target: Element[T]): LazyList[(Double, T)]
```

Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#computeExpectation-c2a)

```scala
override def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#distribution-fffff1a2)

```scala
def distribution[T](target: Element[T]): LazyList[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#distribution-fffff084)

```scala
def distribution(target: List[Element[_]]): (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` target: List[Element[_]] ``.

Returns: `` (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])]) ``.

Source contract/attributes: Attributes Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.initialElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#initialElements-0)

```scala
def initialElements: List[Element[_]]
```

Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe. Attributes Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialElements
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm -> Algorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeStructured -> StructuredAlgorithm Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#refiningStrategy-fffffdd6)

```scala
override def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredBP.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredBP.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs$.html#apply-747)

```scala
def apply(numSamples: Int, targets: Element[_]*): StructuredGibbs
```

Create a structured Gibbs algorithm.

Type parameters: none.

Parameters, list 1: `` numSamples: Int ``; `` targets: Element[_]* ``.

Returns: `` StructuredGibbs ``.

Source contract/attributes: Create a structured Gibbs algorithm. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.apply(numSamples, targets*)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs$.html#apply-44a)

```scala
def apply(numSamples: Int, burnIn: Int, interval: Int, blockToSampler: BlockSamplerCreator, targets: Element[_]*): StructuredGibbs
```

Create a structured Gibbs algorithm.

Type parameters: none.

Parameters, list 1: `` numSamples: Int ``; `` burnIn: Int ``; `` interval: Int ``; `` blockToSampler: BlockSamplerCreator ``; `` targets: Element[_]* ``.

Returns: `` StructuredGibbs ``.

Source contract/attributes: Create a structured Gibbs algorithm. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.apply(numSamples, burnIn, interval, blockToSampler, targets*)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs$.html#probability-c2a)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Use Gibbs to compute the probability that the given element satisfies the given predicate.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Use Gibbs to compute the probability that the given element satisfies the given predicate. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs$.html#probability-f2c)

```scala
def probability[T](target: Element[T], value: T): Double
```

Use Gibbs to compute the probability that the given element has the given value.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Use Gibbs to compute the probability that the given element has the given value. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration.

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#computeDistribution-f66)

```scala
override def computeDistribution[T](target: Element[T]): LazyList[(Double, T)]
```

Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#computeExpectation-c2a)

```scala
override def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#distribution-fffff1a2)

```scala
def distribution[T](target: Element[T]): LazyList[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#distribution-fffff084)

```scala
def distribution(target: List[Element[_]]): (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` target: List[Element[_]] ``.

Returns: `` (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])]) ``.

Source contract/attributes: Attributes Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.initialElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#initialElements-0)

```scala
def initialElements: List[Element[_]]
```

Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe. Attributes Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialElements
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm -> Algorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeStructured -> StructuredAlgorithm Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#refiningStrategy-fffffdd6)

```scala
override def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredGibbs.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredGibbs.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP$.html#apply-fffffe4b)

```scala
def apply(iterations: Int)(implicit universe: Universe): StructuredMPEBP
```

Create a structured variable elimination algorithm with the given query targets.

Type parameters: none.

Parameters, list 1: `` iterations: Int ``.

Context parameters, list 2: `` universe: Universe ``.

Returns: `` StructuredMPEBP ``.

Source contract/attributes: Create a structured variable elimination algorithm with the given query targets. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.apply(iterations)(using universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP$.html#mostLikelyValue-a3a)

```scala
def mostLikelyValue[T](target: Element[T], iterations: Int): T
```

Use VE to compute the probability that the given element satisfies the given predicate.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` iterations: Int ``.

Returns: `` T ``.

Source contract/attributes: Use VE to compute the probability that the given element satisfies the given predicate. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.mostLikelyValue[T](target, iterations)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration.

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.initialElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP.html#initialElements-0)

```scala
override def initialElements: List[Element[_]]
```

Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe. Attributes Definition Classes DecompositionMPE -> DecompositionStructuredAlgorithm Inherited from: DecompositionMPE

Invocation template:

```scala
receiver.initialElements
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm -> Algorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star. Attributes Inherited from: StructuredMPEAlgorithm

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredMPEAlgorithm -> StructuredAlgorithm Inherited from: StructuredMPEAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredMPEAlgorithm -> StructuredAlgorithm Inherited from: StructuredMPEAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeStructured -> StructuredAlgorithm Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP.html#refiningStrategy-fffffdd6)

```scala
override def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEBP.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEBP.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE$.html#apply-fffffc85)

```scala
def apply()(implicit universe: Universe): StructuredMPEVE
```

Create a structured variable elimination algorithm with the given query targets.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Context parameters, list 2: `` universe: Universe ``.

Returns: `` StructuredMPEVE ``.

Source contract/attributes: Create a structured variable elimination algorithm with the given query targets. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.apply()(using universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE$.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Use VE to compute the probability that the given element satisfies the given predicate.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Use VE to compute the probability that the given element satisfies the given predicate. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.mostLikelyValue[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration.

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.initialElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE.html#initialElements-0)

```scala
override def initialElements: List[Element[_]]
```

Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe. Attributes Definition Classes DecompositionMPE -> DecompositionStructuredAlgorithm Inherited from: DecompositionMPE

Invocation template:

```scala
receiver.initialElements
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm -> Algorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.mostLikelyValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE.html#mostLikelyValue-fffffa13)

```scala
def mostLikelyValue[T](target: Element[T]): T
```

Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` T ``.

Source contract/attributes: Returns the most likely value for the target element. Throws an IllegalArgumentException if the range of the target contains star. Attributes Inherited from: StructuredMPEAlgorithm

Invocation template:

```scala
receiver.mostLikelyValue[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredMPEAlgorithm -> StructuredAlgorithm Inherited from: StructuredMPEAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredMPEAlgorithm -> StructuredAlgorithm Inherited from: StructuredMPEAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeStructured -> StructuredAlgorithm Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE.html#refiningStrategy-fffffdd6)

```scala
override def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredMPEVE.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredMPEVE.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE$.html#apply-ffffffa0)

```scala
def apply(targets: Element[_]*): StructuredVE
```

Create a structured variable elimination algorithm with the given query targets.

Type parameters: none.

Parameters, list 1: `` targets: Element[_]* ``.

Returns: `` StructuredVE ``.

Source contract/attributes: Create a structured variable elimination algorithm with the given query targets. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.apply(targets*)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE$.html#probability-c2a)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Use VE to compute the probability that the given element satisfies the given predicate.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Use VE to compute the probability that the given element satisfies the given predicate. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE$.html#probability-f2c)

```scala
def probability[T](target: Element[T], value: T): Double
```

Use VE to compute the probability that the given element has the given value.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Use VE to compute the probability that the given element has the given value. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.solvingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#solvingStrategy-dd8)

```scala
def solvingStrategy(): SolvingStrategy
```

Strategy to use for solving at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` SolvingStrategy ``.

Source contract/attributes: Strategy to use for solving at a single iteration. This may return a new strategy for each iteration. Attributes Returns A solving strategy to be used for a single iteration.

Invocation template:

```scala
receiver.solvingStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.checkConstraintBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#checkConstraintBounds-94c)

```scala
def checkConstraintBounds(): Unit
```

Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Verify that all constraint factors satisfy the bounds needed for the correctness of this algorithm. This gets executed before solving. By default, this method does nothing; subclasses can override this to throw an exception if the bounds requirements are not met. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.checkConstraintBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.computeDistribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#computeDistribution-f66)

```scala
override def computeDistribution[T](target: Element[T]): LazyList[(Double, T)]
```

Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Computes the normalized distribution over a single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeDistribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.computeExpectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#computeExpectation-c2a)

```scala
override def computeExpectation[T](target: Element[T], function: T => Double): Double
```

Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Computes the expectation of a given function for single target element. Throws an IllegalArgumentException if the range of the target contains star, or if lower and upper bounds are needed. Attributes Definition Classes StructuredProbQueryAlgorithm -> BaseProbQueryAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.computeExpectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.computeProbability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#computeProbability-fffffa66)

```scala
def computeProbability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.computeProbability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#distribution-fffff1a2)

```scala
def distribution[T](target: Element[T]): LazyList[(Double, T)]
```

Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Returns: `` LazyList[(Double, T)] ``.

Source contract/attributes: Return an estimate of the marginal probability distribution over the target that lists each element with its probability. The result is a lazy stream. It is up to the algorithm how the stream is ordered. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution[T](target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.distribution ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#distribution-fffff084)

```scala
def distribution(target: List[Element[_]]): (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])])
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` target: List[Element[_]] ``.

Returns: `` (List[(String, ProblemComponent[_])], List[(Double, List[Extended[_]])]) ``.

Source contract/attributes: Attributes Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.distribution(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#expectation-579)

```scala
def expectation[T](target: Element[T])(function: T => Double, c: Any = ...): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` function: T => Double ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target)(function, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.expectation ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#expectation-fffffa66)

```scala
def expectation[T](target: Element[T], function: T => Double): Double
```

Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` function: T => Double ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the expectation of the function under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.expectation[T](target, function)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.initialElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#initialElements-0)

```scala
def initialElements: List[Element[_]]
```

Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: Initial elements to pass to the bottom-up strategy for decomposition. Defaults to a list containing all problem targets and all evidence elements in the universe. Attributes Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialElements
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#initialize-94c)

```scala
override def initialize(): Unit
```

Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Initialize the problem by adding all permanent elements to it. This is to ensure that all top-level elements are correctly added to the top-level problem. Attributes Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm -> Algorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.mean ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#mean-fffffeac)

```scala
def mean(target: Element[Double]): Double
```

Return the mean of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the mean of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.mean(target)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.neededBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#neededBounds-fffffa2e)

```scala
def neededBounds(): Set[Bounds]
```

All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Set[Bounds] ``.

Source contract/attributes: All bounds for which this algorithm needs to compute solutions. This is determined by looking for components that have * in their range, and have constraint factors associated with them. If such a component exists, we need both lower and upper bounds. Otherwise, just one of the bounds suffices because they are equivalent; it defaults to lower in this case. Attributes Returns All bounds for which this algorithm should compute solutions. Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.neededBounds()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.posteriorElement ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#posteriorElement-741)

```scala
def posteriorElement[T](target: Element[T], universe: Universe = ...): Element[T]
```

Return an element representing the posterior probability distribution of the given element.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` universe: Universe = ... ``.

Returns: `` Element[T] ``.

Source contract/attributes: Return an element representing the posterior probability distribution of the given element. Attributes Inherited from: ProbQueryAlgorithm

Invocation template:

```scala
receiver.posteriorElement[T](target, universe)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#probability-870)

```scala
def probability[T](target: Element[T], value: T): Double
```

Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` value: T ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability that the target produces the value. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, value)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#probability-579)

```scala
def probability[T](target: Element[T])(predicate: T => Boolean, c: Any = ...): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``.

Parameters, list 2: `` predicate: T => Boolean ``; `` c: Any = ... ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target)(predicate, c)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.probability ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#probability-fffffa66)

```scala
def probability[T](target: Element[T], predicate: T => Boolean): Double
```

Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[T] ``; `` predicate: T => Boolean ``.

Returns: `` Double ``.

Source contract/attributes: Return an estimate of the probability of the predicate under the marginal probability distribution of the target. Throws NotATargetException if called on a target that is not in the list of targets of the algorithm. Throws AlgorithmInactiveException if the algorithm is inactive. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.probability[T](target, predicate)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.problemTargets ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#problemTargets-0)

```scala
override def problemTargets: List[Element[_]]
```

List of targets that should not be eliminated when solving the problem.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` List[Element[_]] ``.

Source contract/attributes: List of targets that should not be eliminated when solving the problem. Attributes Returns Targets for the problem. Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.problemTargets
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.processSolutions ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#processSolutions-fffffa74)

```scala
override def processSolutions(solutions: Map[Bounds, Solution]): Unit
```

Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this.

Type parameters: none.

Parameters, list 1: `` solutions: Map[Bounds, Solution] ``.

Returns: `` Unit ``.

Source contract/attributes: Extract the solution in a way that allows fast queries to the algorithm. This usually involves storing some form of the solution in a variable, but the exact implementation is up to the algorithm that overrides this. Value parameters solutions A map from bounds to computed solutions for those bounds. Contains one key for each of the bounds needed, according to neededBounds(). Attributes Definition Classes StructuredProbQueryAlgorithm -> StructuredAlgorithm Inherited from: StructuredProbQueryAlgorithm

Invocation template:

```scala
receiver.processSolutions(solutions)
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.rangingStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#rangingStrategy-0)

```scala
override def rangingStrategy: RangingStrategy
```

Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` RangingStrategy ``.

Source contract/attributes: Uses the default ranging strategy with ParticleGenerator.defaultNumSamplesFromAtomics values. Attributes Definition Classes OneTimeStructured -> StructuredAlgorithm Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.rangingStrategy
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.refiningStrategy ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#refiningStrategy-fffffdd6)

```scala
override def refiningStrategy(): RefiningStrategy
```

Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` RefiningStrategy ``.

Source contract/attributes: Strategy to use for refinement at a single iteration. This may return a new strategy for each iteration. Attributes Returns A refining strategy to be used for a single iteration. Definition Classes DecompositionStructuredAlgorithm -> StructuredAlgorithm Inherited from: DecompositionStructuredAlgorithm

Invocation template:

```scala
receiver.refiningStrategy()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#run-94c)

```scala
override def run(): Unit
```

Run the algorithm, performing its computation to completion.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm, performing its computation to completion. Attributes Definition Classes OneTimeStructured -> OneTime Inherited from: OneTimeStructured

Invocation template:

```scala
receiver.run()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.runStep ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#runStep-94c)

```scala
def runStep(): Unit
```

Run a single iteration of refinement/solving, then record the solutions.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Run a single iteration of refinement/solving, then record the solutions. Attributes Inherited from: StructuredAlgorithm

Invocation template:

```scala
receiver.runStep()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.structured.algorithm.structured.StructuredVE.variance ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/structured/algorithm/structured/StructuredVE.html#variance-fffffeac)

```scala
def variance(target: Element[Double]): Double
```

Return the variance of the probability density function for the given continuous element.

Type parameters: none.

Parameters, list 1: `` target: Element[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Return the variance of the probability density function for the given continuous element. Attributes Inherited from: BaseProbQueryAlgorithm

Invocation template:

```scala
receiver.variance(target)
```
