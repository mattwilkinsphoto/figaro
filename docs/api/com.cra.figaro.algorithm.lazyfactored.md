<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro.algorithm.lazyfactored

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` com.cra.figaro.algorithm.lazyfactored.BoundedProbFactor.make ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/BoundedProbFactor$.html#make-fffffa43)

```scala
def make(elem: Element[_], upper: Boolean): List[Factor[Double]]
```

Create the probabilistic factors associated with an element.

Type parameters: none.

Parameters, list 1: `` elem: Element[_] ``; `` upper: Boolean ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Create the probabilistic factors associated with an element. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.lazyfactored.BoundedProbFactor.make(elem, upper)
```

## `` com.cra.figaro.algorithm.lazyfactored.BoundedProbFactor.makeDependentFactor ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/BoundedProbFactor$.html#makeDependentFactor-fffffda2)

```scala
def makeDependentFactor(parentUniverse: Universe, dependentUniverse: Universe, probEvidenceComputer: () => Double): (Factor[Double], Factor[Double])
```

Create the probabilistic factor encoding the probability of evidence in the dependent universe as a function of the values of variables in the parent universe. The third argument is the the function to use for computing probability of evidence in the dependent universe. It is assumed that the definition of this function will already contain the right evidence.

Type parameters: none.

Parameters, list 1: `` parentUniverse: Universe ``; `` dependentUniverse: Universe ``; `` probEvidenceComputer: () => Double ``.

Returns: `` (Factor[Double], Factor[Double]) ``.

Source contract/attributes: Create the probabilistic factor encoding the probability of evidence in the dependent universe as a function of the values of variables in the parent universe. The third argument is the the function to use for computing probability of evidence in the dependent universe. It is assumed that the definition of this function will already contain the right evidence. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.lazyfactored.BoundedProbFactor.makeDependentFactor(parentUniverse, dependentUniverse, probEvidenceComputer)
```

## `` com.cra.figaro.algorithm.lazyfactored.Extended.isRegular ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/Extended.html#isRegular-0)

```scala
def isRegular: Boolean
```

Return true if the value is a regular value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Return true if the value is a regular value. Attributes

Invocation template:

```scala
receiver.isRegular
```

## `` com.cra.figaro.algorithm.lazyfactored.Extended.value ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/Extended.html#value-0)

```scala
def value: T
```

Return the underlying value. Throws an IllegalArgumentException when given Star.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` T ``.

Source contract/attributes: Return the underlying value. Throws an IllegalArgumentException when given Star. Attributes

Invocation template:

```scala
receiver.value
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyValues.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyValues$.html#apply-448)

```scala
def apply(universe: Universe = ..., parameterized: Boolean = ...): LazyValues
```

Create an object for computing the range of values of elements in the universe. This object is only created once for a universe.

Type parameters: none.

Parameters, list 1: `` universe: Universe = ... ``; `` parameterized: Boolean = ... ``.

Returns: `` LazyValues ``.

Source contract/attributes: Create an object for computing the range of values of elements in the universe. This object is only created once for a universe. Attributes

Invocation template:

```scala
com.cra.figaro.algorithm.lazyfactored.LazyValues.apply(universe, parameterized)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyValues.clear ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyValues$.html#clear-25c)

```scala
def clear(universe: Universe): Unit
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: `` universe: Universe ``.

Returns: `` Unit ``.

Invocation template:

```scala
com.cra.figaro.algorithm.lazyfactored.LazyValues.clear(universe)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyValues.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyValues.html#apply-65a)

```scala
def apply[T](element: Element[T], depth: Int): ValueSet[T]
```

Returns the range of values of an element. This method is memoized. If it has previously been called on the same element with a depth at least as great as this one, or if a previous call has resulted in a result with no Star, the previous result is reused.

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``; `` depth: Int ``.

Returns: `` ValueSet[T] ``.

Source contract/attributes: Returns the range of values of an element. This method is memoized. If it has previously been called on the same element with a depth at least as great as this one, or if a previous call has resulted in a result with no Star, the previous result is reused. Attributes

Invocation template:

```scala
receiver.apply[T](element, depth)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyValues.apply ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyValues.html#apply-fffffe7a)

```scala
def apply[T](element: Element[T], depth: Int, numArgSamples: Int, numTotalSamples: Int): ValueSet[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``; `` depth: Int ``; `` numArgSamples: Int ``; `` numTotalSamples: Int ``.

Returns: `` ValueSet[T] ``.

Invocation template:

```scala
receiver.apply[T](element, depth, numArgSamples, numTotalSamples)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyValues.expandAll ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyValues.html#expandAll-766)

```scala
def expandAll(elementDepths: Set[(Element[_], Int)]): Unit
```

This code ensures that if there are multiple elements that need to be expanded to a certain depth, then if one uses another, the full value set of the second is used in computing the value set of the first.

Type parameters: none.

Parameters, list 1: `` elementDepths: Set[(Element[_], Int)] ``.

Returns: `` Unit ``.

Source contract/attributes: This code ensures that if there are multiple elements that need to be expanded to a certain depth, then if one uses another, the full value set of the second is used in computing the value set of the first. Attributes

Invocation template:

```scala
receiver.expandAll(elementDepths)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyValues.expandedDepth ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyValues.html#expandedDepth-6bd)

```scala
def expandedDepth[T](element: Element[T]): Option[Int]
```

Returns the depth to which an element has been expanded, if any.

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` Option[Int] ``.

Source contract/attributes: Returns the depth to which an element has been expanded, if any. Attributes

Invocation template:

```scala
receiver.expandedDepth[T](element)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyValues.expandedElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyValues.html#expandedElements-0)

```scala
def expandedElements: Set[Element[_]]
```

Returns the elements whose values have been computed.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Set[Element[_]] ``.

Source contract/attributes: Returns the elements whose values have been computed. Attributes

Invocation template:

```scala
receiver.expandedElements
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyValues.getMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyValues.html#getMap-fffffbb0)

```scala
def getMap[T, U](chain: Chain[T, U]): Map[T, Element[U]]
```

Gets the mapping from parent values to result elements associated with a chain.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` chain: Chain[T, U] ``.

Returns: `` Map[T, Element[U]] ``.

Source contract/attributes: Gets the mapping from parent values to result elements associated with a chain. Attributes

Invocation template:

```scala
receiver.getMap[T, U](chain)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyValues.getMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyValues.html#getMap-bbe)

```scala
def getMap[U](apply: Apply[U]): Map[Any, U]
```

Gets the mapping from apply arguments to values.

Type parameters: `` U ``.

Parameters, list 1: `` apply: Apply[U] ``.

Returns: `` Map[Any, U] ``.

Source contract/attributes: Gets the mapping from apply arguments to values. Attributes

Invocation template:

```scala
receiver.getMap[U](apply)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyValues.getMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyValues.html#getMap-58c)

```scala
def getMap[T1, U](apply: Apply1[T1, U]): Map[T1, U]
```

Gets the mapping from apply arguments to values.

Type parameters: `` T1 ``; `` U ``.

Parameters, list 1: `` apply: Apply1[T1, U] ``.

Returns: `` Map[T1, U] ``.

Source contract/attributes: Gets the mapping from apply arguments to values. Attributes

Invocation template:

```scala
receiver.getMap[T1, U](apply)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyValues.getMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyValues.html#getMap-6ce)

```scala
def getMap[T1, T2, U](apply: Apply2[T1, T2, U]): Map[(T1, T2), U]
```

Gets the mapping from apply arguments to values.

Type parameters: `` T1 ``; `` T2 ``; `` U ``.

Parameters, list 1: `` apply: Apply2[T1, T2, U] ``.

Returns: `` Map[(T1, T2), U] ``.

Source contract/attributes: Gets the mapping from apply arguments to values. Attributes

Invocation template:

```scala
receiver.getMap[T1, T2, U](apply)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyValues.getMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyValues.html#getMap-810)

```scala
def getMap[T1, T2, T3, U](apply: Apply3[T1, T2, T3, U]): Map[(T1, T2, T3), U]
```

Gets the mapping from apply arguments to values.

Type parameters: `` T1 ``; `` T2 ``; `` T3 ``; `` U ``.

Parameters, list 1: `` apply: Apply3[T1, T2, T3, U] ``.

Returns: `` Map[(T1, T2, T3), U] ``.

Source contract/attributes: Gets the mapping from apply arguments to values. Attributes

Invocation template:

```scala
receiver.getMap[T1, T2, T3, U](apply)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyValues.getMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyValues.html#getMap-fffff952)

```scala
def getMap[T1, T2, T3, T4, U](apply: Apply4[T1, T2, T3, T4, U]): Map[(T1, T2, T3, T4), U]
```

Gets the mapping from apply arguments to values.

Type parameters: `` T1 ``; `` T2 ``; `` T3 ``; `` T4 ``; `` U ``.

Parameters, list 1: `` apply: Apply4[T1, T2, T3, T4, U] ``.

Returns: `` Map[(T1, T2, T3, T4), U] ``.

Source contract/attributes: Gets the mapping from apply arguments to values. Attributes

Invocation template:

```scala
receiver.getMap[T1, T2, T3, T4, U](apply)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyValues.getMap ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyValues.html#getMap-fffffa94)

```scala
def getMap[T1, T2, T3, T4, T5, U](apply: Apply5[T1, T2, T3, T4, T5, U]): Map[(T1, T2, T3, T4, T5), U]
```

Gets the mapping from apply arguments to values.

Type parameters: `` T1 ``; `` T2 ``; `` T3 ``; `` T4 ``; `` T5 ``; `` U ``.

Parameters, list 1: `` apply: Apply5[T1, T2, T3, T4, T5, U] ``.

Returns: `` Map[(T1, T2, T3, T4, T5), U] ``.

Source contract/attributes: Gets the mapping from apply arguments to values. Attributes

Invocation template:

```scala
receiver.getMap[T1, T2, T3, T4, T5, U](apply)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyValues.storedValues ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyValues.html#storedValues-473)

```scala
def storedValues[T](element: Element[T]): ValueSet[T]
```

Returns the previously computed values at maximum depth, if any. If the element has not been expanded, we return the empty value set.

Type parameters: `` T ``.

Parameters, list 1: `` element: Element[T] ``.

Returns: `` ValueSet[T] ``.

Source contract/attributes: Returns the previously computed values at maximum depth, if any. If the element has not been expanded, we return the empty value set. Attributes

Invocation template:

```scala
receiver.storedValues[T](element)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyVariableElimination.eliminationOrder ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyVariableElimination.html#eliminationOrder-fffff180)

```scala
def eliminationOrder(allVars: Set[Variable[_]], factors: Iterable[Factor[Double]], toPreserve: Iterable[Variable[_]]): List[Variable[_]]
```

Method for choosing the elimination order. The default order chooses first the variable that minimizes the number of extra factor entries that would be created when it is eliminated. Override this method if you want a different rule.

Type parameters: none.

Parameters, list 1: `` allVars: Set[Variable[_]] ``; `` factors: Iterable[Factor[Double]] ``; `` toPreserve: Iterable[Variable[_]] ``.

Returns: `` List[Variable[_]] ``.

Source contract/attributes: Method for choosing the elimination order. The default order chooses first the variable that minimizes the number of extra factor entries that would be created when it is eliminated. Override this method if you want a different rule. Attributes

Invocation template:

```scala
receiver.eliminationOrder(allVars, factors, toPreserve)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyVariableElimination.finishNoBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyVariableElimination.html#finishNoBounds-b81)

```scala
def finishNoBounds(factorsAfterElimination: Set[Factor[Double]]): Factor[(Double, Double)]
```

Postprocess the factors produced by eliminating variables, assuming the entire model has been expanded so the lower and upper bounds are the same.

Type parameters: none.

Parameters, list 1: `` factorsAfterElimination: Set[Factor[Double]] ``.

Returns: `` Factor[(Double, Double)] ``.

Source contract/attributes: Postprocess the factors produced by eliminating variables, assuming the entire model has been expanded so the lower and upper bounds are the same. Attributes

Invocation template:

```scala
receiver.finishNoBounds(factorsAfterElimination)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyVariableElimination.finishWithBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyVariableElimination.html#finishWithBounds-c53)

```scala
def finishWithBounds(lowerFactors: Set[Factor[Double]], upperFactors: Set[Factor[Double]]): Factor[(Double, Double)]
```

Postprocess the factors produced by eliminating variables, when the lower and upper bounds may be different.

Type parameters: none.

Parameters, list 1: `` lowerFactors: Set[Factor[Double]] ``; `` upperFactors: Set[Factor[Double]] ``.

Returns: `` Factor[(Double, Double)] ``.

Source contract/attributes: Postprocess the factors produced by eliminating variables, when the lower and upper bounds may be different. Value parameters lowerFactors the factors produced with the upperBounds flag = false upperFactors the factors produced with the upperBounds flag = true Attributes

Invocation template:

```scala
receiver.finishWithBounds(lowerFactors, upperFactors)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyVariableElimination.getFactors ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyVariableElimination.html#getFactors-eb2)

```scala
def getFactors(neededElements: List[Element[_]], targetElements: List[Element[_]], upperBounds: Boolean = ...): List[Factor[Double]]
```

Create the necessary factors.

Type parameters: none.

Parameters, list 1: `` neededElements: List[Element[_]] ``; `` targetElements: List[Element[_]] ``; `` upperBounds: Boolean = ... ``.

Returns: `` List[Factor[Double]] ``.

Source contract/attributes: Create the necessary factors. Value parameters neededElements elements that have been expanded that need factors created targetElements query targets upperBounds flag indicating whether lower (false) or upper (true) bounds should be computed for unexpanded parts of the model Attributes

Invocation template:

```scala
receiver.getFactors(neededElements, targetElements, upperBounds)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyVariableElimination.probabilityBounds ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyVariableElimination.html#probabilityBounds-4c5)

```scala
def probabilityBounds[T](target: Element[_], value: T): (Double, Double)
```

Returns the lower and upper bounds of the probability of the target.

Type parameters: `` T ``.

Parameters, list 1: `` target: Element[_] ``; `` value: T ``.

Returns: `` (Double, Double) ``.

Source contract/attributes: Returns the lower and upper bounds of the probability of the target. Attributes

Invocation template:

```scala
receiver.probabilityBounds[T](target, value)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyVariableElimination.run ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyVariableElimination.html#run-fffff3b3)

```scala
def run(depth: Int): Unit
```

Run the algorithm to the given depth.

Type parameters: none.

Parameters, list 1: `` depth: Int ``.

Returns: `` Unit ``.

Source contract/attributes: Run the algorithm to the given depth. Attributes

Invocation template:

```scala
receiver.run(depth)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyVariableElimination.cleanUp ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyVariableElimination.html#cleanUp-94c)

```scala
def cleanUp(): Unit
```

Called when the algorithm is killed. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is killed. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.cleanUp()
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyVariableElimination.doKill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyVariableElimination.html#doKill-94c)

```scala
def doKill(): Unit
```

Kill the algorithm.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm. Attributes Inherited from: LazyAlgorithm

Invocation template:

```scala
receiver.doKill()
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyVariableElimination.doResume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyVariableElimination.html#doResume-94c)

```scala
def doResume(): Unit
```

Resume the algorithm by increasing the depth and running again.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the algorithm by increasing the depth and running again. Attributes Inherited from: LazyAlgorithm

Invocation template:

```scala
receiver.doResume()
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyVariableElimination.doStart ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyVariableElimination.html#doStart-94c)

```scala
def doStart(): Unit
```

Start the algorithm. This will run the algorithm to one depth.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm. This will run the algorithm to one depth. Attributes Inherited from: LazyAlgorithm

Invocation template:

```scala
receiver.doStart()
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyVariableElimination.doStop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyVariableElimination.html#doStop-94c)

```scala
def doStop(): Unit
```

Stop the algorithm.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm. Attributes Inherited from: LazyAlgorithm

Invocation template:

```scala
receiver.doStop()
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyVariableElimination.getNeededElements ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyVariableElimination.html#getNeededElements-91d)

```scala
def getNeededElements(starterElements: List[Element[_]], depth: Int, parameterized: Boolean = ...): (List[Element[_]], Boolean)
```

Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed.

Type parameters: none.

Parameters, list 1: `` starterElements: List[Element[_]] ``; `` depth: Int ``; `` parameterized: Boolean = ... ``.

Returns: `` (List[Element[_]], Boolean) ``.

Source contract/attributes: Get the elements that are needed by the query target variables and the evidence variables. Also compute the values of those variables to the given depth. Only get factors for elements that are actually used by the target variables. This is more efficient. Also, it avoids problems when values of unused elements have not been computed. In addition to getting all the needed elements, it determines if any of the conditioned, constrained, or dependent universe parent elements has * in its range. If any of these elements has * in its range, the lower and upper bounds of factors will be different, so we need to compute both. If they don't, we don't need to compute bounds. Attributes Inherited from: FactoredAlgorithm

Invocation template:

```scala
receiver.getNeededElements(starterElements, depth, parameterized)
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyVariableElimination.initialize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyVariableElimination.html#initialize-94c)

```scala
def initialize(): Unit
```

Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Called when the algorithm is started before running any steps. By default, does nothing. Can be overridden. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.initialize()
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyVariableElimination.isActive ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyVariableElimination.html#isActive-0)

```scala
def isActive: Boolean
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.isActive
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyVariableElimination.kill ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyVariableElimination.html#kill-94c)

```scala
def kill(): Unit
```

Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Kill the algorithm so that it is inactive. It will no longer be able to provide answers.Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.kill()
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyVariableElimination.pump ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyVariableElimination.html#pump-94c)

```scala
def pump(): Unit
```

Increase the depth and run the algorithm again.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Increase the depth and run the algorithm again. Attributes Inherited from: LazyAlgorithm

Invocation template:

```scala
receiver.pump()
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyVariableElimination.resume ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyVariableElimination.html#resume-94c)

```scala
def resume(): Unit
```

Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Resume the computation of the algorithm, if it has been stopped. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.resume()
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyVariableElimination.start ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyVariableElimination.html#start-94c)

```scala
def start(): Unit
```

Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Start the algorithm and make it active. After it returns, the algorithm must be ready to provide answers. Throws AlgorithmActiveException if the algorithm is already active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.start()
```

## `` com.cra.figaro.algorithm.lazyfactored.LazyVariableElimination.stop ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/LazyVariableElimination.html#stop-94c)

```scala
def stop(): Unit
```

Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Unit ``.

Source contract/attributes: Stop the algorithm from computing. The algorithm is still ready to provide answers after it returns. Throws AlgorithmInactiveException if the algorithm is not active. Attributes Inherited from: Algorithm

Invocation template:

```scala
receiver.stop()
```

## `` com.cra.figaro.algorithm.lazyfactored.Regular.isRegular ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/Regular.html#isRegular-0)

```scala
def isRegular: Boolean
```

Return true if the value is a regular value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Return true if the value is a regular value. Attributes

Invocation template:

```scala
receiver.isRegular
```

## `` com.cra.figaro.algorithm.lazyfactored.Regular.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/Regular.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.algorithm.lazyfactored.Regular.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/Regular.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.algorithm.lazyfactored.Star.isRegular ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/Star.html#isRegular-0)

```scala
def isRegular: Boolean
```

Return true if the value is a regular value.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Boolean ``.

Source contract/attributes: Return true if the value is a regular value. Attributes

Invocation template:

```scala
receiver.isRegular
```

## `` com.cra.figaro.algorithm.lazyfactored.Star.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/Star.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Any

Invocation template:

```scala
receiver.toString
```

## `` com.cra.figaro.algorithm.lazyfactored.Star.value ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/Star.html#value-0)

```scala
def value: T
```

Return the underlying value. Throws an IllegalArgumentException when given Star.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` T ``.

Source contract/attributes: Return the underlying value. Throws an IllegalArgumentException when given Star. Attributes

Invocation template:

```scala
receiver.value
```

## `` com.cra.figaro.algorithm.lazyfactored.Star.productElementNames ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/Star.html#productElementNames-0)

```scala
def productElementNames: Iterator[String]
```

An iterator over the names of all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[String] ``.

Source contract/attributes: An iterator over the names of all the elements of this product. Attributes Inherited from: Product

Invocation template:

```scala
receiver.productElementNames
```

## `` com.cra.figaro.algorithm.lazyfactored.Star.productIterator ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/Star.html#productIterator-0)

```scala
def productIterator: Iterator[Any]
```

An iterator over all the elements of this product.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Iterator[Any] ``.

Source contract/attributes: An iterator over all the elements of this product. Attributes Returns in the default implementation, an Iterator[Any] Inherited from: Product

Invocation template:

```scala
receiver.productIterator
```

## `` com.cra.figaro.algorithm.lazyfactored.ValueSet.withStar ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/ValueSet$.html#withStar-452)

```scala
def withStar[T](values: Set[T]): ValueSet[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` values: Set[T] ``.

Returns: `` ValueSet[T] ``.

Invocation template:

```scala
com.cra.figaro.algorithm.lazyfactored.ValueSet.withStar[T](values)
```

## `` com.cra.figaro.algorithm.lazyfactored.ValueSet.withoutStar ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/ValueSet$.html#withoutStar-452)

```scala
def withoutStar[T](values: Set[T]): ValueSet[T]
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: `` T ``.

Parameters, list 1: `` values: Set[T] ``.

Returns: `` ValueSet[T] ``.

Invocation template:

```scala
com.cra.figaro.algorithm.lazyfactored.ValueSet.withoutStar[T](values)
```

## `` com.cra.figaro.algorithm.lazyfactored.ValueSet.+ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/ValueSet.html#+-fffff4ae)

```scala
def +(x: Extended[T]): ValueSet[T]
```

Add an extended value to this value set. If the extended value is a Star and the value set already contains a Star, we do not add it, since there is no need for more than one Star.

Type parameters: none.

Parameters, list 1: `` x: Extended[T] ``.

Returns: `` ValueSet[T] ``.

Source contract/attributes: Add an extended value to this value set. If the extended value is a Star and the value set already contains a Star, we do not add it, since there is no need for more than one Star. Attributes

Invocation template:

```scala
receiver.+(x)
```

## `` com.cra.figaro.algorithm.lazyfactored.ValueSet.++ ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/ValueSet.html#++-6a6)

```scala
def ++(that: ValueSet[T]): ValueSet[T]
```

Returns the union of this value set with that value set. If both have a Star, only one Star is kept in the result.

Type parameters: none.

Parameters, list 1: `` that: ValueSet[T] ``.

Returns: `` ValueSet[T] ``.

Source contract/attributes: Returns the union of this value set with that value set. If both have a Star, only one Star is kept in the result. Attributes

Invocation template:

```scala
receiver.++(that)
```

## `` com.cra.figaro.algorithm.lazyfactored.ValueSet.map ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/ValueSet.html#map-33)

```scala
def map[U](f: T => U): ValueSet[U]
```

Apply a function to each value while keeping the same Star nature.

Type parameters: `` U ``.

Parameters, list 1: `` f: T => U ``.

Returns: `` ValueSet[U] ``.

Source contract/attributes: Apply a function to each value while keeping the same Star nature. Attributes

Invocation template:

```scala
receiver.map[U](f)
```

## `` com.cra.figaro.algorithm.lazyfactored.ValueSet.starValue ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/ValueSet.html#starValue-0)

```scala
def starValue: Star[T]
```

Returns the particular Star value in this value set. Throws an exception if there is no Star.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Star[T] ``.

Source contract/attributes: Returns the particular Star value in this value set. Throws an exception if there is no Star. Attributes

Invocation template:

```scala
receiver.starValue
```

## `` com.cra.figaro.algorithm.lazyfactored.ValueSet.toString ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/algorithm/lazyfactored/ValueSet.html#toString-0)

```scala
override def toString: String
```

Returns a string representation of the object.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` String ``.

Source contract/attributes: Returns a string representation of the object. The default representation is platform dependent. Attributes Returns a string representation of the object. Definition Classes Any

Invocation template:

```scala
receiver.toString
```
