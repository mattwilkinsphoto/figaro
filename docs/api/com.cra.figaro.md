<!-- Generated by tools/docs/build_reference.py; do not edit. -->
# com.cra.figaro

[Reference index](README.md)

Examples are call templates: supply the named arguments with the declared types. Context parameters are shown explicitly with `using`; defaults may be omitted in real calls. `receiver` denotes an existing instance of the listed owner. Type parameters must be in scope. Abstract members require a concrete implementation. The signature, not a template, is authoritative.

## `` .argmax ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#argmax-fffff55c)

```scala
def argmax(seq: Seq[Double]): Int
```

Finds the index of the maximal element in the sequence.

Type parameters: none.

Parameters, list 1: `` seq: Seq[Double] ``.

Returns: `` Int ``.

Source contract/attributes: Finds the index of the maximal element in the sequence. Attributes

Invocation template:

```scala
receiver.argmax(seq)
```

## `` .bound ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#bound-fffff59d)

```scala
def bound(logp: Double, conditions: Boolean*): Double
```

Returns logp if all conditions are true. Else returns -Inf.

Type parameters: none.

Parameters, list 1: `` logp: Double ``; `` conditions: Boolean* ``.

Returns: `` Double ``.

Source contract/attributes: Returns logp if all conditions are true. Else returns -Inf. Attributes

Invocation template:

```scala
receiver.bound(logp, conditions*)
```

## `` .cartesianProduct ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#cartesianProduct-fffffcc7)

```scala
def cartesianProduct(args: List[_]*): List[List[Any]]
```

Returns the Cartesian product of any number of inputs. The results are returned in lexicographic order.

Type parameters: none.

Parameters, list 1: `` args: List[_]* ``.

Returns: `` List[List[Any]] ``.

Source contract/attributes: Returns the Cartesian product of any number of inputs. The results are returned in lexicographic order. Attributes

Invocation template:

```scala
receiver.cartesianProduct(args*)
```

## `` .getNextHashCode ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#getNextHashCode-0)

```scala
def getNextHashCode: Int
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters: none (parameterless member; do not append `()`).

Returns: `` Int ``.

Invocation template:

```scala
receiver.getNextHashCode
```

## `` .getOrElseInsert ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#getOrElseInsert-fffff872)

```scala
def getOrElseInsert[T, U](map: Map[T, U], key: T, default: => U): U
```

Gets the value associated with a key in a map, inserting a default value if it is not found. The default is only evaluated if the key is not found in the map.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` map: Map[T, U] ``; `` key: T ``; `` default: => U ``.

Returns: `` U ``.

Source contract/attributes: Gets the value associated with a key in a map, inserting a default value if it is not found. The default is only evaluated if the key is not found in the map. Attributes

Invocation template:

```scala
receiver.getOrElseInsert[T, U](map, key, default)
```

## `` .getSeed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#getSeed-644)

```scala
def getSeed(): Long
```

No behavioral summary was supplied in the source Scaladoc; inspect the full entry and implementation before using this low-level API.

Type parameters: none.

Parameters, list 1: none (empty argument list `()`).

Returns: `` Long ``.

Invocation template:

```scala
receiver.getSeed()
```

## `` .homogeneousCartesianProduct ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#homogeneousCartesianProduct-fffffa36)

```scala
def homogeneousCartesianProduct[T](args: List[T]*): List[List[T]]
```

Returns the Cartesian product of any number of inputs of the same type. The results are returned in lexicographic order.

Type parameters: `` T ``.

Parameters, list 1: `` args: List[T]* ``.

Returns: `` List[List[T]] ``.

Source contract/attributes: Returns the Cartesian product of any number of inputs of the same type. The results are returned in lexicographic order. Attributes

Invocation template:

```scala
receiver.homogeneousCartesianProduct[T](args*)
```

## `` .indices ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#indices-fffff716)

```scala
def indices[T](traversable: Iterable[T], x: T): List[Int]
```

Returns all indices of the given element in the traversable.

Type parameters: `` T ``.

Parameters, list 1: `` traversable: Iterable[T] ``; `` x: T ``.

Returns: `` List[Int] ``.

Source contract/attributes: Returns all indices of the given element in the traversable. Attributes

Invocation template:

```scala
receiver.indices[T](traversable, x)
```

## `` .insertAtIndices ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#insertAtIndices-36)

```scala
def insertAtIndices[T](traversable: Iterable[T], indices: List[Int], value: T): List[T]
```

Given a traversable, some indices, and a value, inserts the value at the given indices into the traversable. The resulting list contains all the elements of the input traversable as well as the value at the appropriate indices. For example, insertAtIndices(List(1,2), List(0,2,4), 3) yields List(3,1,3,2,3). The indices must be in order to produce the correct result. If the indices are such that there would be a gap in the resulting traversable, IllegalArgumentException is thrown.

Type parameters: `` T ``.

Parameters, list 1: `` traversable: Iterable[T] ``; `` indices: List[Int] ``; `` value: T ``.

Returns: `` List[T] ``.

Source contract/attributes: Given a traversable, some indices, and a value, inserts the value at the given indices into the traversable. The resulting list contains all the elements of the input traversable as well as the value at the appropriate indices. For example, insertAtIndices(List(1,2), List(0,2,4), 3) yields List(3,1,3,2,3). The indices must be in order to produce the correct result. If the indices are such that there would be a gap in the resulting traversable, IllegalArgumentException is thrown. Attributes

Invocation template:

```scala
receiver.insertAtIndices[T](traversable, indices, value)
```

## `` .logDiff ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#logDiff-44b)

```scala
def logDiff(p1: Double, p2: Double): Double
```

Computes the difference of two probabilities in log space. Returns NaN if p2>p1, -Infinity if p2=p1

Type parameters: none.

Parameters, list 1: `` p1: Double ``; `` p2: Double ``.

Returns: `` Double ``.

Source contract/attributes: Computes the difference of two probabilities in log space. Returns NaN if p2>p1, -Infinity if p2=p1 Attributes

Invocation template:

```scala
receiver.logDiff(p1, p2)
```

## `` .logSum ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#logSum-44b)

```scala
def logSum(p1: Double, p2: Double): Double
```

Sums two probabilities in log space.

Type parameters: none.

Parameters, list 1: `` p1: Double ``; `` p2: Double ``.

Returns: `` Double ``.

Source contract/attributes: Sums two probabilities in log space. Attributes

Invocation template:

```scala
receiver.logSum(p1, p2)
```

## `` .logSumMany ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#logSumMany-fd3)

```scala
def logSumMany(xs: Iterable[Double]): Double
```

Computes the sum of many probabilities in log space.

Type parameters: none.

Parameters, list 1: `` xs: Iterable[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Computes the sum of many probabilities in log space. Attributes

Invocation template:

```scala
receiver.logSumMany(xs)
```

## `` .measureTime ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#measureTime-978)

```scala
def measureTime[T](f: () => T, warmup: Int, runs: Int): Double
```

Computes the average time to run the given function. The second argument is the number of times to run it before starting to measure to warm up the JVM. The third argument is the number of times to run and measure.

Type parameters: `` T ``.

Parameters, list 1: `` f: () => T ``; `` warmup: Int ``; `` runs: Int ``.

Returns: `` Double ``.

Source contract/attributes: Computes the average time to run the given function. The second argument is the number of times to run it before starting to measure to warm up the JVM. The third argument is the number of times to run and measure. Attributes

Invocation template:

```scala
receiver.measureTime[T](f, warmup, runs)
```

## `` .memo ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#memo-892)

```scala
def memo[T, U](fn: T => U): T => U
```

Memoize the given function so that it is only applied once to each input with the result stored.

Type parameters: `` T ``; `` U ``.

Parameters, list 1: `` fn: T => U ``.

Returns: `` T => U ``.

Source contract/attributes: Memoize the given function so that it is only applied once to each input with the result stored. Attributes

Invocation template:

```scala
receiver.memo[T, U](fn)
```

## `` .normalize ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#normalize-5c0)

```scala
def normalize(unnormalized: List[Double]): List[Double]
```

Normalize the given list of doubles so that the proportions remain constant and they sum to 1. An exception will be thrown if the inputs sum to 0.

Type parameters: none.

Parameters, list 1: `` unnormalized: List[Double] ``.

Returns: `` List[Double] ``.

Source contract/attributes: Normalize the given list of doubles so that the proportions remain constant and they sum to 1. An exception will be thrown if the inputs sum to 0. Attributes

Invocation template:

```scala
receiver.normalize(unnormalized)
```

## `` .reachable ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#reachable-fffffa39)

```scala
def reachable[T](graph: T => Iterable[T], includeStart: Boolean, start: T*): Set[T]
```

Find the elements that are reachable from given elements in a directed graph. The implementation avoids following the exponential number of paths by marking which nodes have been checked. This technique also allows the algorithm to work on cyclic graphs.

Type parameters: `` T ``.

Parameters, list 1: `` graph: T => Iterable[T] ``; `` includeStart: Boolean ``; `` start: T* ``.

Returns: `` Set[T] ``.

Source contract/attributes: Find the elements that are reachable from given elements in a directed graph. The implementation avoids following the exponential number of paths by marking which nodes have been checked. This technique also allows the algorithm to work on cyclic graphs. Value parameters graph A function that given an element returns a collection of nodes to which there is a directed edge. includeStart Flag to indicate whether to explicitly include the start elements in the returned set or not. start Graph elements from which to begin searching. Attributes Returns A set containing all nodes reachable by directed edges from the start nodes. Note: if a start node is reachable by a path from another start node (or by a cycle containing itself), the node will be included the set even if includeStart is set to false.

Invocation template:

```scala
receiver.reachable[T](graph, includeStart, start*)
```

## `` .round ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#round-fffff1da)

```scala
def round(target: Double, points: Iterable[Double]): Double
```

Round the given double to the nearest element of the non-empty ordered list.

Type parameters: none.

Parameters, list 1: `` target: Double ``; `` points: Iterable[Double] ``.

Returns: `` Double ``.

Source contract/attributes: Round the given double to the nearest element of the non-empty ordered list. Attributes

Invocation template:

```scala
receiver.round(target, points)
```

## `` .sampleMultinomial ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#sampleMultinomial-fffffd94)

```scala
def sampleMultinomial[T](clauses: List[(Double, T)]): T
```

Sample a value given a multinomial distribution. The input is a list of pairs where each pair specifies a value and its associated probability. This method assumes the probabilities sum to 1.

Type parameters: `` T ``.

Parameters, list 1: `` clauses: List[(Double, T)] ``.

Returns: `` T ``.

Source contract/attributes: Sample a value given a multinomial distribution. The input is a list of pairs where each pair specifies a value and its associated probability. This method assumes the probabilities sum to 1. Attributes

Invocation template:

```scala
receiver.sampleMultinomial[T](clauses)
```

## `` .setSeed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#setSeed-fffffa90)

```scala
def setSeed(s: Long): Unit
```

A random number generator.

Type parameters: none.

Parameters, list 1: `` s: Long ``.

Returns: `` Unit ``.

Source contract/attributes: A random number generator. Attributes

Invocation template:

```scala
receiver.setSeed(s)
```

## `` .timed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#timed-fffff637)

```scala
def timed[T](arg: => T, title: String): T
```

Computes and returns the argument, timing how long it takes to produce the answer and printing the result.

Type parameters: `` T ``.

Parameters, list 1: `` arg: => T ``; `` title: String ``.

Returns: `` T ``.

Source contract/attributes: Computes and returns the argument, timing how long it takes to produce the answer and printing the result. Attributes

Invocation template:

```scala
receiver.timed[T](arg, title)
```

## `` .upperTriangle ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#upperTriangle-fffff631)

```scala
def upperTriangle[T](list: List[T]): List[(T, T)]
```

Computes all pairs of elements in the list in which the first element appears before the second.

Type parameters: `` T ``.

Parameters, list 1: `` list: List[T] ``.

Returns: `` List[(T, T)] ``.

Source contract/attributes: Computes all pairs of elements in the list in which the first element appears before the second. Attributes

Invocation template:

```scala
receiver.upperTriangle[T](list)
```

## `` .withRandomSeed ``

[Full Scaladoc entry](../../target/out/jvm/scala-3.9.0/figaro/api/com/cra/figaro/util.html#withRandomSeed-fffffa4e)

```scala
def withRandomSeed[A](seed: Long)(body: => A): A
```

Evaluate synchronous code with a fresh, thread-confined random stream. Nested scopes and exceptions restore the previous stream. Child threads do not inherit it. Outside this scope, the legacy shared generator and its seed behavior are unchanged.

Type parameters: `` A ``.

Parameters, list 1: `` seed: Long ``.

Parameters, list 2: `` body: => A ``.

Returns: `` A ``.

Source contract/attributes: Evaluate synchronous code with a fresh, thread-confined random stream. Nested scopes and exceptions restore the previous stream. Child threads do not inherit it. Outside this scope, the legacy shared generator and its seed behavior are unchanged. Value parameters body computation to run on the calling thread; consume lazy random results inside the scope seed seed for this invocation's java.util.Random stream Attributes Returns the result of body; exceptions propagate after restoring the prior stream Example val draw = withRandomSeed(42L) { random.nextDouble() }

Invocation template:

```scala
receiver.withRandomSeed[A](seed)(body)
```
